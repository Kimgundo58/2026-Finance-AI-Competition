# 프로토타입 기능 구분 — AI 가 필요한 기능 / 필요 없는 기능

- **대상**: 프론트 프로토타입 12화면 (`프론트 프로토타입 검토.md`의 P0~P12 번호 사용)
- **근거**: `서비스 아키텍쳐.md` §4·§7 (판정 1건 = LLM 2회), `프론트 연동 사양.md`
- 핵심 원칙: **검색·룰 조회·금액 비교·효력 결정은 코드가 한다.** AI(LLM)는 ① 정규화와
  ④ 판정 조립, 딱 두 군데(+판단불가 전용 문의 초안)만 맡는다

---

## 1. 판정 1건의 분업 — 흐름도

```
[ AI ] 말과 구조를 만든다       || [ 코드 ] 찾고 비교한다          || [ 프론트 ] 그린다
================================||=================================||================================
    (관여 안 함)                ||     (관여 안 함)                || 입력
                                ||                                 ||     자연어 한 줄 + F5 체크 2개
--------------------------------||---------------------------------||--------------------------------
(1) 정규화  * AI 호출 1         ||     (관여 안 함)                || 화면 4 확인
    품목/금액/용도 추출         ||                                 ||     스트리밍 렌더
    비목후보 추천               ||                                 ||     빈 값 UI
--------------------------------||---------------------------------||--------------------------------
    (관여 안 함)                ||     컨펌 로그 적재              || 비목 최종 선택
                                ||      -> item_alias 승격 후보    ||     사용자가 확정한다
--------------------------------||---------------------------------||--------------------------------
    (관여 안 함)                || (2) 룰 조회 (사업 x 비목)       ||     (대기)
                                || (3) L1/L2 검색 + 참조 폐포      ||
                                ||     L3 통째 로드                ||
                                ||     금액/한도 비교 (F1/F3)      ||
        [ 게이트 ]  검색 스코어 < 임계치  ->  판단불가 직행
--------------------------------||---------------------------------||--------------------------------
(4)-b 판정 조립  * AI 호출 2    ||     (관여 안 함)                || 화면 5 판정
    4-way 판정 + 요약           ||                                 ||     스트리밍 렌더
    해야할일 + 전제 생성        ||                                 ||
    인용은 S번호만              ||                                 ||
--------------------------------||---------------------------------||--------------------------------
    (관여 안 함)                || (5) 전제 해소 (F 대조)          || 화면 5/6/7
                                || (6) S번호 -> DB 원문 치환       ||     판정 / 할일 / 근거
                                ||     신뢰등급 A/B 산정           ||
                                ||     버전스탬프 / 참조사슬       ||
--------------------------------||---------------------------------||--------------------------------
(5') 문의 초안  ! 전용 경로     ||     unmapped_premise 로깅       || 화면 9 문의 초안
    판단불가일 때만             ||                                 ||     복사 버튼
    (2회 셈 밖)                 ||                                 ||
================================||=================================||================================
합계: AI 호출 2회 고정          || 합계: LLM 0회, ~300ms           || 합계: 총 p50 3.2s (설계값)
```

**한 줄**: AI 는 흐름의 양 끝(말 → 구조, 재료 → 판정문)만 잡고, 가운데(찾기·비교·결정)는 전부 코드다.

---

## 2. 프로토타입 기능별 구분

### 2-1. AI 가 필요한 기능 (LLM 호출)

| 기능 | 프로토타입 위치 | AI 슬롯 | 비고 |
|---|---|---|---|
| 자연어 → 구조화 (품목·금액·용도·날짜 추출) | P7 step1 (폼 → 교체 후) | ① 정규화 | 프로토타입은 폼 입력이라 이 AI 가 놀게 됨 — textarea 교체가 전제 |
| **비목 자동 추천** ("AI 추천" 배지) | P7 step2 | ① 정규화 출력 | `비목후보[{비목,신뢰도}]`. 최종 선택은 사용자 |
| **AI 점검 결과** (판정 + AI 해설 + 결제 전 확인 생성) | P8 | ④-b 판정 조립 | 4-way 폐쇄 enum. 인용은 S번호만 뱉고 원문은 코드가 치환 |
| 조항별 해당/비해당/불확실 | (없음 — 신설 ⏸) | ④-a 스크리닝 | 채택 자체가 미결 — 레이아웃만 |
| 기관 문의 초안 생성 | (없음 — 신설) | ⑤ 문장 생성 | **판단불가 경로 전용.** 2회 셈 밖. 프롬프트 미설계 🟡 |
| 스캔 전용 PDF 판독 | P3·P11 업로드의 스캔본 | VLM (오프라인) | `extraction='vlm'` 태깅, A등급 인용 금지 |
| F2 사업계획서 파싱 | (없음 — 후순위 🟡) | F 문서 파싱 | 파싱 후 원문 폐기 |

### 2-2. AI 가 필요 없는 기능 — 코드·룰·DB 가 한다

프로토타입에서 "AI 가 하는 것처럼 보이지만" 실제로는 AI 를 쓰면 안 되는 것들.

| 기능 | 프로토타입 위치 | 실제 담당 |
|---|---|---|
| 규정 검색 · 적용 근거 선정 | P8 적용 근거 카드 | 임베딩+BM25 → RRF → 필터 (코드. 생성 LLM 0회) |
| **필요 증빙 목록** | P8 결제 후 증빙 | `rules.증빙` 룰 테이블 값 그대로 (🟡 검수 전) |
| 사전승인 필요 여부 | P8 · P10 일정 유형 | `rules.사전승인` (🟡 검수 전) |
| 금액 기준 비교 (회의비 1인 단가 등 "위험" 판정 재료) | P8 위험 케이스 | 룰 x F 숫자 비교 (코드) |
| 한도 잔액 (현금·현물 신청 가능 금액) | P6 CSV 컬럼 | F1·F3 계산 (코드) — F 프로필 신설 전제 |
| 인용 조항 원문 표시 | P8 적용 근거 | S번호 → DB 원문 치환 (코드) |
| 신뢰등급 A/B · 버전스탬프 · 참조사슬 | (없음 — 신설) | ⑥에서 코드가 부착. **LLM 자칭 신뢰도 금지** |
| 효력(우선순위) 결정 — 공통 기준 vs 기관 기준 | P11 기준 적용 안내 | `precedence_rules` (코드) |
| L3 업로드 파싱 (PDF·HWPX·HWP) — 조 단위 적재 + "87개 조항 연결" + dangling 알림 | P3 · P11 | 코드 파서 (업로드 시점). HWPX 는 XML 직파싱, HWP 는 `hwp_extract.py` 계열 — **2026-08-30 채택**. 스캔본만 VLM |
| 기관 기준 추가 확인 항목 표 | P11 | 룰 테이블 조회 (정적 하드코딩 → 교체) |
| D-day (사업 종료까지 D-129) | 셸 · P5 홈 | F1 협약종료일 날짜 계산 |
| 판정 이력 · 현황 집계 | P5 · P6 | `decisions` 테이블 (조회 API 미설계 🟡) |

### 2-3. AI 도 서버도 필요 없는 기능 — 프론트 자체

| 기능 | 프로토타입 위치 |
|---|---|
| 온보딩 선택 UI (사업 라디오·기관 검색·진행 표시) | P1~P4 |
| 예시 질문 칩 | P0 · (P9 → 화면 3 이동) |
| 목록 검색·필터·정렬·페이지네이션 | P6 |
| CSV 출력 · 필요 증빙 PDF 생성 | P6 · P8 |
| 체크리스트 체크 상태 (로컬) | P8 집행 준비 |
| 일정·캘린더 (유지한다면 localStorage 전용) | P10 |
| **PMS 붙여넣기 파서** (브라우저 정규식 — 원문은 서버로 안 보냄) | (없음 — F 프로필 신설분) |
| 복사 버튼 · 삭제 confirm · 면책 문구 · 스트리밍 진행 표시 | 전 화면 |

### 2-4. AI 가 필요하지만 만들지 않는 기능 (미채택 — 재제안 금지)

| 기능 | 프로토타입 위치 | 안 만드는 이유 |
|---|---|---|
| **자유 질의 챗봇** | P9 전체 | 판정 1건 = LLM 2회 고정, function calling 미개방. "새 판정 시작"으로 교체 |
| 일괄 N건 동시 점검 | P6 | GPU 직렬 큐 — 동시 배치 미설계 |
| 첨부파일(견적서 등) 판독 | P7·P8 첨부 | 판정 입력에 파일 없음 — 증빙 목록만 안내 |

---

## 3. 경계표 — 누가 무엇을 *안* 하는가

```
AI 가 안 하는 것                  || 코드가 안 하는 것                 || 아무도 안 하는 것 (미채택)
==================================||===================================||==================================
규정 검색 / 검색어 생성           || 판정 문장 생성                    || 자유 질의 챗봇
  -> 코드가 쿼리를 짠다           ||   -> 말은 AI 슬롯의 몫            ||   -> 호출 수 고정 원칙 위반
----------------------------------||-----------------------------------||----------------------------------
필요 증빙 목록 생성               || 비목 추천                         || 일괄 N건 동시 점검
  -> 룰 테이블 값 그대로          ||   -> 정규화(AI)가 후보 산출       ||   -> GPU 직렬 큐
----------------------------------||-----------------------------------||----------------------------------
인용 원문 작성                    || 효력(우선순위) 해석               || 첨부파일 판독 (견적서 등)
  -> S번호를 코드가 치환          ||   -> precedence 룰로 결정         ||   -> 판정 입력에 파일 없음
----------------------------------||-----------------------------------||----------------------------------
신뢰등급 / 확률 산정              || F 스키마 확장                     || 스크린샷 업로드
  -> 자칭 신뢰도 금지, 코드 산정  ||   -> AI 는 결핍 지적만            ||   -> 텍스트 붙여넣기만
----------------------------------||-----------------------------------||----------------------------------
금액 / 한도 비교                  || 되묻기(질문 생성)                 || PMS 원문 서버 전송
  -> 숫자는 코드가 비교           ||   -> 전제[] 로 결핍 반환          ||   -> 브라우저 정규식 파싱만
```

**한 줄**: 프로토타입이 "AI 점검"이라 부르는 것의 대부분(증빙 목록·금액 기준·적용 근거)은
룰 테이블과 코드의 몫이고, AI 가 진짜 필요한 곳은 **자연어를 구조로 바꾸는 입구**와
**재료를 판정문으로 조립하는 출구** 두 군데뿐이다.
