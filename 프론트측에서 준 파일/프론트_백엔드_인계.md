# 프론트 → 백엔드 인계·요청

2026-09-02 · 마감 **09-07 (D-5)** · 작성: 프론트/인프라(효민)
근거: 백엔드 레포 `4ba8d3f` 전수 열람 + 목 서버(`SUDDOE_MOCK=1`) 직접 구동 실측 + 브라우저 E2E

---

# 0. 분담 — 먼저 이것부터

```
🟩 제가 합니다      프론트 전부 · 배포 · 인프라 (GCP · Supabase · Vercel)
🟦 담당자님          백엔드 코드 · DB · 판정엔진 · 규정 데이터
```

**콘솔에서 클릭하는 일은 전부 제가 가져갑니다.** Cloud Run 배포, Cloud SQL 생성, 공개 주소 확보, 도메인·환경변수, 로그인(Supabase Auth)까지 제 몫입니다.

그래서 담당자님께 드리는 요청은 **레포 안에서만 할 수 있는 것**으로 좁혔습니다.

| 구분 | 건수 | 성격 |
|---|--:|---|
| 🔴 코드 수정 | **2** | 각각 몇 줄. 이게 지금 프론트를 막고 있습니다 |
| 🟡 데이터·결정 | **7** | 답만 주시면 제가 진행합니다 |
| 🟢 알림 | **3** | 확인만 해주세요 |

## 지금 상태

```
프론트 화면          ✅ Vercel 배포됨 · git push 하면 자동 갱신
프론트 ↔ API 연동    ✅ 홈·목록·상세·판정·새 계획 작성까지 «끝까지 돔» (목 서버 기준)
공개 주소            🟩 제가 확보합니다 (§2-1)
할일 체크 저장        🔴 CORS 가 PATCH 를 막아 브라우저에서 «불가능» — 담당자님 한 단어
```

---

# 1. 제가 지금까지 한 것

## 1-1. 프로토타입을 코드로 꺼내 배포까지

ChatGPT 캔버스 산출물이 **Next.js 16 + Cloudflare Workers** 구성이었습니다. Vercel 로 옮기면서 빌드를 `vinext`(Workers) → `next build` 로 전환했습니다.

```
GitHub   https://github.com/wori1206/2026-Finance-AI-Competition_Frontend
Vercel   자동 배포 연결 완료
```

🟢 **`프론트_요청서_0902.md` 의 「이 레포는 프론트를 못 고친다」 상태가 해소됐습니다.**
압축된 `bundle.js` 가 아니라 **소스를 직접 보실 수 있습니다.** 화면 본체는 `app/checkumait-app.tsx` 한 파일(약 4,900줄)입니다.

## 1-2. API 연동 — 목 서버로 끝까지 검증

새로 만든 파일은 `lib/` 아래 7개이고, **화면 코드는 최소한만 손댔습니다.**

| 파일 | 하는 일 |
|---|---|
| `lib/config.ts` | 백엔드 주소를 아는 **유일한 곳** |
| `lib/http.ts` | 공통 호출 + **`org_id` 자동 부착** |
| `lib/server-types.ts` | 서버 응답 타입 (목 서버 실측으로 작성) |
| `lib/adapt.ts` | 서버 모양 → 화면 타입 번역 |
| `lib/api.ts` | 화면이 부르는 함수 전부 |
| `lib/sse.ts` | **SSE over POST** (판정·정규화) |
| `lib/judge.ts` · `normalize.ts` · `tasks.ts` | 판정 / 정규화 / 할일 |

**브라우저로 끝까지 돌려 확인한 경로**

| 화면(B체계) | 부르는 것 | 상태 |
|---|---|:--:|
| 6 홈 | `GET /api/plans` (통계+목록 한 번에) | ✅ |
| 7 목록 | `GET /api/plans?크기=20` | ✅ |
| 11 상세 | `GET /api/plans/{id}` | ✅ |
| 11 판정 | `POST /api/judge` **SSE — 이벤트 11종 전부 처리** | ✅ |
| 8 새 계획① | `POST /api/normalize` SSE | ✅ |
| 9 새 계획② | `GET /api/vocab` | ✅ |
| 8→저장 | `POST /api/plans` | ✅ |
| 13 집행 일정 | `GET /api/tasks?일정만=true` | ✅ |
| 11 할일 체크 | `PATCH …/tasks/{task_id}` | 🔴 **CORS 차단** |

## 1-3. 계약을 지킨 부분 — 확인해 주세요

문서에 🔴 로 적어두신 것들입니다. 지켰는지 봐주세요.

- 🔴 **`tasks:sync` 를 부르지 않습니다.** 판정 직후 서버 자동 적재에 맡기고, 프론트는 `GET /api/plans/{id}` 만 다시 부릅니다
- 🔴 **판정 실패 시 자동 재시도 없음.** 판정 1건 = GPU 호출을 지켰습니다. 3분 초과 시 안내만 띄웁니다
- 🔴 **`org_id` 를 읽기·쓰기 «양쪽에» 붙입니다** (`http.ts` 한 곳에서). 09-02 변경 반영
- 🔴 **L3 폴링에 `org_id` 를 싣습니다.** 빼면 404 라는 것 반영
- **SSE 는 `EventSource` 가 아니라 `fetch` + reader** (POST 라서). `: keep-alive` 주석 줄은 자연히 무시됩니다
- **판정 4-way → 배지 3종.** 조건부/판단불가는 같은 노랑이지만 **설명 문구를 다르게** 했습니다

## 1-4. `프론트_요청서_0902.md` §2 의 6건

| # | 항목 | 상태 |
|:--:|---|---|
| 1 | `기계장치비` → `기계장치` | ✅ **완료** — 라벨을 `/api/vocab` 에서 받습니다 |
| 2 | 클라이언트 비목 추정기 | ✅ **호출 경로에서 제외** (코드는 남겨 뒀습니다) |
| 3 | 기준연도 표시 자리 | ⏸ 제가 예정 |
| 4 | 기관 라벨을 사업별로 | ⏸ 제가 예정 |
| 5 | 기관 검색 하드코딩 제거 | ⏸ **기관 목록 API 대기** (§3-6) |
| 6 | `accept` 에서 `.doc` | ⏸ **결정 대기** (§3-5) |

> 2번 — 지우지 않고 **연동 경로가 그 함수를 지나가지 않게** 했습니다. 마감 5일 전에 참조를 전부 추적하는 위험을 피했습니다.
>
> 그 과정에서 하나 더 발견: 질문표(`CATEGORY_QUESTIONS`) 키가 옛 라벨(`기계장치비`)이라 서버 라벨(`기계장치`)로는 못 찾고 **조용히 「외주용역비」 질문으로 떨어졌습니다.** 공백·꼬리표를 흡수하는 매칭으로 막았습니다.

---

# 🟩 2. 제가 앞으로 할 것 — 인프라 포함

## 2-1. 백엔드 공개 주소는 제가 확보합니다

**담당자님이 신경 안 쓰셔도 됩니다.** 두 단계로 갑니다.

### ① 오늘 — 목 서버를 Cloud Run 에 임시 배포

```
🔴 백엔드 레포는 건드리지 않습니다
   Cloud Shell 안에서 Dockerfile 만 만들어 배포합니다. 커밋도 푸시도 없습니다.
   server 폴더만 sparse-checkout 으로 받습니다 (1.3GB → 972KB).
```

- 목 서버는 **DB·GPU 를 안 씁니다.** 파이썬 파일 9개 + 라이브러리 4개면 뜹니다 (검증 완료)
- CORS 는 `SUDDOE_CORS` 환경변수로 Vercel 주소를 넣습니다 — **코드 수정 없음** (검증 완료)
- 목적: 공모전 제출물인 **「작동하는 배포 URL」을 담당자님 일정과 무관하게** 확보

> 팀 운영상 곤란하면 말씀해 주세요. 바로 접겠습니다.

### ② 이어서 — 실서버(`SUDDOE_MOCK=0`)도 제가 올립니다

Cloud SQL 인스턴스 생성, Cloud Run 배포, 시크릿 관리, 도메인·환경변수 전부 제가 합니다.
🔴 **다만 「무엇을 넣어야 도는지」는 담당자님만 아십니다.** → §3-3 에서 그것만 여쭙니다.

## 2-2. 그 밖에 제가 하는 것

| | 할 일 | 담당자 의존 |
|:--:|---|---|
| 1 | Cloud SQL 인스턴스 생성 (PG17 · pgvector · 서울) | 적재는 담당자님 |
| 2 | Supabase Auth 로 **진짜 로그인** 붙이기 | 토큰 «검증» 만 의존 |
| 3 | Vercel 환경변수 · SSO 해제 · 도메인 | 없음 |
| 4 | 마이페이지 F1·F3·F4 (`/api/profile`) | 일정 확인 (§3-5) |
| 5 | 온보딩 사업 선택 (`/api/programs`) | 없음 |
| 6 | 요청서 §2 의 3·4번 (기준연도·기관 라벨) | 없음 |
| 7 | 배지 문구 정리 · 시연 시나리오 4종 리허설 | 없음 |
| 8 | 온보딩 기관 선택 | 🔴 기관 목록 API 대기 |
| 9 | 할일 체크 저장 | 🔴 CORS PATCH 대기 |

---

# 🔴 3. 담당자님께 — 레포 안에서만 되는 것

## 3-1. 🔴 CORS 허용 메서드에 `PATCH` 추가 — 한 단어

**`server/main.py:321`**

```python
allow_methods=["GET", "POST", "PUT", "OPTIONS"],
#                                   ↑ PATCH 가 없습니다
```

계약에 `PATCH /api/plans/{plan_id}/tasks/{task_id}` 가 있는데 **브라우저에서는 절대 못 부릅니다.**

**실측**

```
OPTIONS /api/plans/1/tasks/11
  Origin: http://localhost:3000
  Access-Control-Request-Method: PATCH
→ 400 Bad Request   "Disallowed CORS method"
```

🔴 **`curl` 은 프리플라이트를 안 보내서 잘 되는 것처럼 보입니다.** 저도 `curl` 로는 `(11,'완료')` 로 잘 바뀌는 걸 확인했었습니다. **브라우저를 붙여야만 드러나는 종류**입니다.

```python
allow_methods=["GET", "POST", "PUT", "PATCH", "OPTIONS"],
```

**이거 하나면 할일 체크박스가 바로 삽니다. 프론트는 이미 붙어 있습니다.**

## 3-2. 🔴 `POST /api/plans` 로 만든 계획을 조회하면 500

**재현**

```
POST /api/plans  {"사업명":"초기창업패키지","품목":"테스트","금액":99999,"용도":"확인"}
   → 201, plan_id: 6
GET /api/plans/6
   → 500
```

**서버 로그**

```
File "server/routes_plans.py", line 139, in 상세
    return 계획상세(**행, 정규화=행.get("정규화", {}), 할일=할일, 판정상세=None)
TypeError: server.models.계획상세() got multiple values for keyword argument '정규화'
```

`행` 안에 이미 `정규화` 가 있는데 키워드로 한 번 더 넘겨서 나는 것으로 보입니다.

**영향** — 「새 계획 저장 → 바로 AI 점검」이 통째로 막힙니다. **시연의 핵심 동선**입니다.
**프론트 임시 대응** — 상세 조회가 실패하면 방금 입력한 값으로 판정하도록 우회로를 넣었습니다. 고쳐 주시면 그 우회로는 자연히 안 탑니다.

## 3-3. 🔴 실서버 구동 조건 — 배포는 제가 하되 «레시피»가 필요합니다

인프라는 제가 세웁니다. 그런데 **컨테이너 안에 뭘 넣어야 판정이 도는지**는 담당자님만 아십니다.
레포에 `requirements.txt`·`Dockerfile` 이 없어서(전수 검색 0건) 제가 추측으로 만들면 어긋납니다.

**알려주셔야 하는 것 4가지**

| | 무엇 | 왜 |
|:--:|---|---|
| ① | **파이썬 버전 + 패키지 목록** | `pip freeze` 결과나 `requirements.txt` 한 장이면 충분합니다 |
| ② | **환경변수 중 실제로 필요한 것** | 아래 목록에서 골라 주세요 |
| ③ | **임베딩 모델 로딩 방식** | `KURE-v1`(CPU, 워커당 ~1.5GB)을 이미지에 굽나요, 런타임에 받나요? 메모리 사양이 여기서 갈립니다 |
| ④ | **vLLM 엔드포인트** | `VLLM_URL`·`VLLM_MODEL` 을 어디로 두실 계획인가요 (RunPod?) |

**제가 코드에서 읽어낸 환경변수 전부** — 이 중 실제로 쓰는 것만 표시해 주시면 됩니다.

```
필수로 보이는 것
  SUDDOE_DSN            기본값 postgresql://postgres:devpw@localhost:5432/suddoe
  SUDDOE_MOCK           0 = 실호출
  SUDDOE_CORS           허용 오리진 (제가 Vercel 주소를 넣습니다)
  VLLM_URL · VLLM_MODEL 판정 LLM

선택으로 보이는 것
  SUDDOE_ADMIN_TOKEN · SUDDOE_L3_DIR · SUDDOE_HOST · SUDDOE_PORT
  SUDDOE_사업필터(기본 1) · SUDDOE_GATE_B · SUDDOE_GATE_C
  SUDDOE_CACHE_TTL · SUDDOE_CACHE_MAX · SUDDOE_DAILY_CAP · SUDDOE_IP_HOURLY
  SUDDOE_JUDGE_HEARTBEAT_SEC(기본 12) · SUDDOE_PROGRAM_TTL(기본 300)
  SUDDOE_OPEN_FROM · SUDDOE_OPEN_UNTIL · SUDDOE_ALLOW_EXTERNAL
  SUDDOE_VLLM_SELF_HOSTED · SUDDOE_CONTAINER · RUNPOD_API_KEY · LAW_GO_KR_OC
```

> 🔴 **API 키·비밀번호는 보내지 마세요.** 저는 **이름만** 있으면 됩니다. 값은 GCP Secret Manager 에 담당자님이 직접 넣으시거나, 넣을 자리를 제가 만들어 두고 알려드리겠습니다.

**DB 적재** — Cloud SQL 인스턴스는 제가 만들고 **접속 정보를 드리겠습니다.** `01_schema.sql` 적용과 `load_db.py`/`seed_rules.py` 재실행만 부탁드립니다 (접속 정보만 바꾸면 됩니다).

## 3-4. 🟡 시연용 `org_id` 하나

`격리감사_0902.md` 에 이미 적혀 있는 그 문제입니다.

> *"게스트는 공용 버킷(`org_id IS NULL`)에 모두 들어가 서로의 지출계획이 보인다"*

**시연 중 심사위원이 두 번 눌러보면 남의 계획이 보입니다.**
`tenant.orgs` 412행 중 하나(건국대학교)의 `org_id` 를 주시면 제가 환경변수에 넣겠습니다.
(Cloud SQL 적재가 끝나면 제가 직접 조회해도 됩니다 — 그때는 안 알려주셔도 됩니다.)

> 🔴 **해결(2026-09-04)** — org_id 는 더 이상 환경변수로 안 넘긴다(토큰에서 서버가 정한다,
> `19eae04`). 데모 기관도 건국대가 아니라 경상국립대학교 창업중심대학사업단으로 확정됐다.
> 위 인용은 원문 그대로 남긴다(프론트팀이 준 원본 문서).

## 3-5. 🟡 답만 주시면 되는 것 6가지

각각 **프론트 기본값**을 같이 적었습니다. 답이 늦어도 제가 멈추지 않습니다.

| # | 물음 | 답이 없으면 |
|:--:|---|---|
| 1 | **목 데이터에 「결제후」 할일 2~3건** 넣어주실 수 있나요 | 화면 11 「결제 후 필요 증빙」이 **항상 빈칸**입니다 (§부록 D 실측) |
| 2 | **판정 SSE `해야할일` 에 `구분`·`코드`** 를 실어주실 수 있나요 | 저장 뒤 다시 읽어 쓰고 있습니다. 목 모드에선 영영 못 가릅니다 |
| 3 | **폼 경로에도 `비목후보`** 를 채울까요 | 비어도 안 깨집니다 — `vocab` 10종을 그냥 나열합니다 |
| 4 | **`확정비목` 을 판정 엔진에 넘길까요** (`_비목고정` 자리는 있는데 안 쓰임) | (A) 넘긴다 = 사용자 선택 존중 / (B) 안 넘긴다 = 화면 11 에 **「점검 결과 비목」 표시 자리**를 만들어야 합니다 |
| 5 | **`PUT /api/profile` 은 언제 열릴까요** | 안 열리면 시연에서 F1 협약 입력 장면을 뺍니다 |
| 6 | **`.docx` 를 받을 계획인가요** (`인계_0902` 는 둘 다 415, `요청서_0902` 는 `.doc` 만 제거) | 확답 전까지 `accept` 를 안 건드립니다 |

## 3-6. 🟡 기관 목록 API — 만드실 때 §4-1 을 먼저 읽어주세요

화면 3(온보딩② 기관 선택)에 필요한데 아직 없습니다. **단순 추가가 아니라 보안 결정이 걸린 자리**입니다.

## 3-7. 🟢 P11(규정·지침) 화면

`프론트_화면별_지원표.md` §0 대응표에서 **P11 만 대응 화면이 없습니다.** 누락인가요, 폐기인가요?

---

# ⚠️ 4. 바꾸시면 프론트가 조용히 깨지는 것

프론트가 이미 그 모양에 맞춰 붙어 있습니다.

## 🔴 4-1. `org_id` 를 응답에 싣지 마세요

담당자님이 `격리감사_0902.md` 에 직접 쓰신 문장입니다.

> *"지금 사칭을 어렵게 하는 것은 하나뿐이다 — `org_id` 가 122비트 UUID 이고 **이를 응답에 싣는 엔드포인트가 0개**다."*
> *"화면 3용 기관 목록 API 가 `org_id` 를 응답에 넣는 순간 이 방어가 사라진다."*

**그 화면이 제 담당이라, 제가 그 API 를 요청하는 순간 이 결정이 발생합니다.**

BE4 에 *"개별 패치가 아니라 신원 검증 한 곳으로 풀어야 한다"* 고 적으셨는데, **제가 붙일 Supabase Auth 가 그 「한 곳」입니다** (§5). 프론트가 `Authorization: Bearer <토큰>` 을 보내고 서버가 `sub` → `tenant.accounts` → `org_id` 를 **주입**하면 자기신고가 사라집니다.

> 🟢 **제안** — 기관 목록은 `org_id` 대신 **공개용 식별자(코드·슬러그)** 를 주시고 실제 매핑은 서버 안에서만 하시면, 지금 방어를 유지한 채로 화면 3 을 만들 수 있습니다.

## 🔴 4-2. 계약 표면

| 바꾸면 안 되는 것 | 이유 |
|---|---|
| **SSE 이벤트 «이름»과 «순서»** | `진행→판정→해야할일→인용→전제→참조사슬→결과→저장→완료` 그대로 파싱합니다. 판단불가의 `문의초안` 추가도 반영돼 있습니다 |
| **판정 4-way 어휘** | `가능·조건부·불가·판단불가` + `null`. **다섯 번째 값을 추가하면** 배지 매핑이 깨집니다 |
| **`저장:false` 여도 스트림이 `완료` 까지 가는 것** | 저장 실패해도 화면은 판정을 그립니다. 여기서 스트림을 죽이면 결과가 사용자에게 안 갑니다 |
| **오류를 «오류 이벤트»로 보내는 방식** | HTTP 500 이 아니라 `event: 오류` 라서 따로 잡습니다. 500 으로 바꾸시면 두 경로가 어긋납니다 |
| **응답 필드 이름** | `plan_id·제목·확정비목·금액·판정·집행예정일·updated_at·사업명·상태` 를 그대로 씁니다 |
| **`통계` 를 목록 응답에서 떼어내는 것** | 홈·목록이 이 한 번의 호출로 통계까지 그립니다. 전용 API 로 분리하면 두 번 불러야 합니다 |

## 🔴 4-3. 상태 어휘를 섞지 마세요

```
expense_plans.상태   draft | judged         ← «내가 무엇을 했는가»
decisions.판정       4-way                 ← «AI 가 뭐라 했는가»
plan_tasks.상태      준비필요|집행예정|완료    ← 할일
```

스키마 주석의 *"진행 상태이지 판정이 아닙니다. 4-way 와 섞지 마세요"* 를 그대로 구현했습니다. **상태 어휘 7종으로 되돌리면 프론트 배지가 전부 깨집니다.**

## 🔴 4-4. 비목 별칭 흡수 결정을 되돌리지 마세요

프론트는 **`/api/vocab` 이 주는 문자열을 그대로 라벨로 씁니다.** 하드코딩하지 않습니다.
→ enum 문자열을 바꾸시면 화면 라벨이 같이 바뀝니다. **미리 알려만 주시면** 프론트 수정은 필요 없습니다.

## 🟡 4-5. 목(mock) 응답을 실서버와 «다르게» 만들지 마세요

이미 두 군데가 갈려 있어 실서버 전환 때 터질 자리입니다.

| | 목 | 실서버 |
|---|---|---|
| 폼 경로 `비목후보` | 2건 옴 | **`[]` 빈 배열** (의도라고 주석에 있음) |
| 판정 → 계획 저장 | **저장 안 함** (`판정_저장` 이 가짜 응답) | 저장함 |

프론트는 둘 다 견디게 만들어 뒀습니다(스트림 값 우선). 다만 **목으로 개발하다 실서버로 바꾸는 날 화면이 달라 보이는 종류**라 공유드립니다.

> ⚠️ CORS 오리진(`SUDDOE_CORS`)은 제가 관리합니다. 코드 기본값을 바꾸실 일이 있으면 `http://localhost:3000` 만 남겨 주세요 — 프로토타입이 Vite 가 아니라 **Next.js** 라 5173 이 아니라 **3000** 입니다.

---

# 🟢 5. 인프라 방향 — 문서에 아직 반영이 안 돼 있습니다

`docs/2_구조/2-4_배포와_인증.md` 가 여전히 이렇습니다.

```
Supabase (관리형)  ←  corpus + tenant 전부 저장
호스팅 후보: HF Spaces Docker(PRO) 또는 VM. 미결
```

**정해진 것: DB·API 는 GCP, Supabase 는 인증(Auth)만.** 이 부분은 제가 실행합니다.

이렇게 하면 —

- Supabase 무료 **500MB 한도가 사라집니다** → `chunks`·`chunk_terms`·`refs` 전부 올릴 수 있음
- *"판정엔진이 Supabase 를 볼까 로컬 DB 를 볼까"* 갈림길이 없어집니다
- 골든셋이 anon 키로 새는 위험도 사라집니다 (PostgREST 자동 노출 없음)

GCP 프로젝트를 만들고 담당자님을 **편집자**로 초대해 두었습니다. 리전 `asia-northeast3`(서울).

**Cloud SQL** — PostgreSQL **17** · `create extension vector;`(pgvector 0.8.5 지원) · 2vCPU/8GB · SSD 20GB.
**인스턴스는 제가 만들고 접속 정보를 드립니다.** 담당자님은 `01_schema.sql` + `load_db.py`/`seed_rules.py` 만 돌려주시면 됩니다.

**인증 분담**

```
프론트   Supabase Auth 로 로그인 → Authorization: Bearer <토큰>          ← 제가
백엔드   pyjwt 로 검증 → sub(UUID) → tenant.accounts → org_id 를 «주입»   ← 담당자님
DB      accounts.account_id 가 이미 uuid 라 타입 변경 없음. pw_hash 의 NOT NULL 만 해제
검증키   JWKS 공개 주소로 드립니다 (비밀값 공유 없음)
```

---

# 부록 A. 프론트가 지금 부르는 것 / 안 부르는 것

**부릅니다**

```
GET  /api/plans           홈 통계 + 목록
GET  /api/plans/{id}      상세 (판정·할일·근거)
GET  /api/tasks           집행 일정
GET  /api/vocab           비목 10종
POST /api/normalize (SSE) 새 계획 ① → ②
POST /api/plans           새 계획 저장
POST /api/judge     (SSE) 판정
PATCH …/tasks/{id}        할일 체크 (🔴 CORS 로 막힘)
```

**아직 안 부릅니다** — `/api/programs` · `/api/profile` · `/api/l3/*` · `POST …/tasks`
**절대 안 부릅니다** — `POST …/tasks:sync` (계약대로 서버 자동 적재에 맡김)

# 부록 B. 화면 번호 체계

`docs/7_백엔드/` 와 동일하게 **B체계(화면 1~14)** 를 씁니다.
제가 이전에 드린 「프론트 데이터 요구서」는 **C체계(P0~P12)** 라 대조 시 주의 부탁드립니다.

# 부록 C. 프론트 레포에서 볼 곳

```
app/checkumait-app.tsx    화면 전부 (약 4,900줄, 단일 파일)
lib/api.ts                호출 표면 — 여기만 보시면 프론트가 뭘 부르는지 다 나옵니다
lib/server-types.ts       프론트가 «기대하는» 응답 모양
lib/adapt.ts              서버 모양 → 화면 타입 변환 (배지 매핑 포함)
lib/sse.ts                SSE 파싱
```

**계약이 바뀌면 `lib/` 만 고치면 됩니다.** 화면 코드는 서버 모양을 모릅니다.

# 부록 D. 「결제후 할일 0건」 실측

```
plan 1  개발용 노트북 구매    결제전 2 · 집행 1 · 결제후 0
plan 2  시장조사 인터뷰 회의비  할일 0건
plan 3  특허 출원 수수료      할일 0건
plan 4  TIPS 멘토링 자문료    결제전 1 · 결제후 0
plan 5  전시회 부스 임차      할일 0건
```

`server/mock_data.py` 에서 `"결제후"` 문자열 **0회**.
판정 SSE 의 `해야할일` 에도 `구분` 이 없어, 그것만으로는 두 칸을 못 나눕니다.

```json
[{"항목": "비교견적 2곳 이상 확보", "설명": "50만원 이상 구매는 비교견적을 남깁니다."},
 {"항목": "사업비 카드로 결제",     "설명": "현금·개인카드 결제는 인정되지 않습니다."},
 {"항목": "자산 등록",             "설명": "취득일로부터 30일 이내에 자산으로 등록합니다."}]
```
