# rule_base.md — 숫자로 끝나는 판단

저장소 `corpus.rules` · `corpus.precedence_rules`. 범위: 룰 경계 · 비목 어휘집 · 스키마 ·
병합 · 컴파일 · 검수. 상위 문서는 `서비스 아키텍쳐.md`.

## 0. 룰이 존재하는 이유

**숫자 비교를 LLM 에게 시키지 않기 위해서다.**

```
"한도 3% 이내" + "신청액 250만원 / 사업비 5,000만원 = 5%"
     → 계산이다. 계산은 코드가 한다. 틀릴 여지가 없어야 한다.

"직접 연관성을 소명하고 사전승인을 받아야 한다"
     → 포섭이다. 룰로 만들면 해석이 고정되어 조용히 틀린다.
```

## 1. 무엇을 룰로 빼는가 (경계)

| 룰로 뺀다 | 룰로 만들지 않는다 |
|---|---|
| 별표·한도표의 **숫자** | 조문 본문의 **자연어 요건** |
| 비율 / 금액 / 개수 상한 | "직접 연관성", "사업 목적에 부합" |
| **명시 금지 목록** (열거된 업종·품목) | "범용성 기자재는 소명 필요" 의 판단 |
| 사전승인 필요 여부 (플래그) | 사전승인을 받을 수 있는지의 판단 |
| 비목별 증빙 서류 목록 | 제출 증빙이 충분한지의 판단 |
| **우선순위 조항** (어느 계층이 이기나) | 우선순위 없을 때의 폴백 판단 |

자연어를 컴파일하면 해석이 그 시점에 고정된다 — 규정이 개정돼도 같은 답을 내고,
그게 틀렸다는 걸 아무도 모른다.

## 1-b. 비목 어휘집 — canonical enum 의 정본

(1) 정규화의 `비목후보`, `rules.비목`, `item_alias.비목`, 골든셋의 비목은 **같은 폐쇄 목록**을
쓴다. vLLM guided_json 의 enum 이 여기서 생성된다 (`LLM.md` §3-7).

| 층 | 원천 | 예 |
|---|---|---|
| 기간(基幹) 어휘 | L1 통합관리지침 제36~45조의 법정 비목 | 재료비 · 외주용역비 · 기계장치 · 무형자산취득비 · 인건비 · 지급수수료 · 여비 · 교육훈련비 · 광고선전비 |
| 사업별 확장 | L2 세부관리기준 `[붙임2]` 비목 해설표 (사업 스코프 한정) | 창업활동비(예비창업) 등 |

- 산출물: `_비목_어휘집.json` — `{비목, 층, 사업스코프[], 근거{doc_id, 조번호}}` (생성 미착수)
- 별칭(맥북 → 기계장치)은 `item_alias` 의 몫 — 어휘집은 목적지 목록일 뿐
- 개정으로 비목이 바뀌면 A4 개정 대응이 어휘집 재검수를 트리거한다

## 2. `rules` — 비목 룰

정본 DDL: `db/init/01_schema.sql` §5. 핵심 컬럼:

| 컬럼 | 뜻 | 비고 |
|---|---|---|
| `layer` / `기관ID` / `사업명` / `비목` | 조회 키 | `UNIQUE` |
| `허용` | 가능 / 조건부 / 불가 | |
| `사전승인` + `사전승인_조건` | 플래그 + 조건 문구 | |
| `한도_유형` / `한도_값` / `한도_단위` | 비율 · 금액 · 개수 | **코드가 비교** |
| `증빙` `TEXT[]` | 비목별 서류 | |
| `금지예시` / `허용예시` | | `item_alias` 와 결합 |
| `근거` `JSONB` | `[{doc_id, 조번호}]` | **없으면 룰이 아니다** |
| `verified` | 사람 검수 여부 | false 인 룰만으로 "가능" 판정 금지 |

- **사업별 × 기관유형별 이중 분기** — 참여율 상한이 소속기관 유형(인건비 확보 여부)에 따라
  100% / 130% 로 갈린다. `rules` 조회 키에 기관유형 축이 필요하고 F4 에 `소속기관유형` 이 있다
- **룰 유형은 단일 형태가 아니다** — 실측 18종 (비율한도+조건, 구성비율, 재원제한, 시기제한,
  금액한도+대상제한, 조건+금지, 금액임계+사전심의, 외부참조, 예외, 사전보고 등).
  현행 3컬럼(`한도_유형/값/단위`)으로는 다 못 담는다 — **분기 스키마 설계가 Stage 1 선결 과제**

### 즉답 경로 — 명시 금지목록

카드 사용제한 업종(유흥·위생·레저·사행·기타 — 노래방, PC방, 골프장 등)과 구매금지 품목은
`item_alias` 와 결합해 **LLM 호출 0회로 즉답 "불가"** 한다.
룰의 주재료는 별표가 아니라 **L2 세부관리기준 본문과 `[붙임2]` 표**다 (판정 유효 별표는 4건뿐).

## 3. `precedence_rules` — 충돌 해소

**비목 룰이 아니라 "어느 계층이 이기는가" 룰이다.**

🔴 **"아래가 엄격하면 이긴다" 가 아니다 — 반대가 규칙이다.** 8개 사업 중 6개(예비 · 초기 ·
재도전 · 도약 · 창업중심대학 · 모두의창업)가 제3조(적용범위)에서 "주관기관 내부규정과 상충되는
경우 본 관리기준을 우선한다" 고 명시한다. **창업팀이 올린 L3 가 더 엄격해도 진다.**
초격차 · 모두의창업은 추가로 `L1 > L2` (미규정 사항만 L2). TIPS 는 해당 조항 없음(운영사 체계).

산출: `scripts/build_precedence.py` → `_precedence_rules.json`.

### 3-1. 병합 알고리즘

L3 는 기존 룰 행을 덮어쓰지 않고 **별도 행으로 쌓는다.** 판정 시 코드가 푼다:

```
effective_rule(사업, 비목, 기관) =
    base    = rules WHERE layer IN ('L1','L2') AND 사업=? AND 비목=?
    overlay = rules WHERE layer='L3'          AND 기관=? AND 비목=?
    p       = precedence_rules WHERE 사업=?

    if p 가 "L2 > L3" 명시:                  # 8개 사업 중 6개
        base 채택. overlay 는 '참고' 로 화면 7 병기
          "귀 기관 규정은 더 엄격하나, 본 사업 관리기준 제3조에 따라 관리기준이 우선합니다"
    elif p 가 "L1 > L2, 미규정만 L2" 명시:    # 초격차 / 모두의창업
        비목이 L1 에 있으면 L1, 없으면 L2. 결과에 L3 오버레이
    else:                                    # 조항 없음 → 폴백
        상위 규범 우선 + 같은 층에서 갈리면 엄격한 쪽
          허용: 불가 > 조건부 > 가능 / 한도: min / 사전승인: OR / 증빙: UNION
```

병합 결과에 **(a) 어느 계층에서 왔는지 (b) 어느 우선순위 조항이 적용됐는지**를 기록한다 —
화면 7 이 "이 한도는 국가 지침이 아니라 OO대 규정입니다" 를 그린다.
근거 조항을 인용하는 제품이 그 조항의 우선순위 지시를 어기면 화면 7 에서 자기모순이 드러난다.

## 4. 검수 대조 원본 — 예비창업패키지 10행

초기창업패키지는 창업활동비가 빠진 9행으로 나머지 동일.

| 비목 | 허용 | 한도 | 사전승인 | 증빙 |
|---|:---:|---|---|---|
| 재료비 | O | — | — | 세금계산서, 견적서, 검수조서(사진), 사업자등록증, 통장사본 |
| 외주용역비 | 조건부 | 선급금 **계약총액 50% 이하** | **2천만원 초과(부가세 포함) 시 사업운영위 사전심의**(7개 점검항목) | +계약서(과업), 결과보고서, 선급금이행보증보험 |
| 기계장치(공구·기구·비품·SW) | 조건부 | **PC 1인 1대**(대표자+신규채용) | — | 세금계산서, 견적서, 검수조서, 사업자등록증, 통장사본 |
| 특허권 등 무형자산 취득비 | O | — | — | 세금계산서, 계약서, 출원(등록)청구서·등록증, 관납료 영수증 |
| 인건비 | 조건부 | 퇴직급여충당금 불가 | 4대보험 예외 시 주관기관 사전승인 | 4대보험가입확인서, 근로계약서, 급여대장, 원천징수영수증 외 |
| 지급수수료 | 조건부 | 멘토링비 1인1일 **30만원** | — | 13개 세부항목별 상이 |
| 여비 | 조건부 | 대중교통 일반석 | **국외출장 = 출국일 이전 보고 필수** | 국내: 실집행 영수증 / 국외: 출장계획서+결과보고서 |
| 교육훈련비 | O | — | — | 교육참가신청서, 교육자료, 영수증, 이수증 외 |
| 광고선전비 | 조건부 | 기념품·유니폼·기프티콘 불가 | — | 세금계산서, 계약서, 결과보고서, 검수확인서 외 |
| 창업활동비 | O | **월 50만원** | — | 통장사본 |
| *(공통)* | | 2천만원 초과 거래 시 비교견적서 | | |

### 룰 소스

| 소스 | 기여 |
|---|---|
| L2 세부관리기준 `[붙임2]` 비목 해설표 | **주 소스** — 정의/증빙/유의사항 |
| L2 본문 (제22조 등) | 사전심의, 국외출장 보고 등 |
| L1 통합관리지침 제36~45조 | 비목 법정 정의 |
| 유효 별표 4건 | 카드 제한 업종 · 참여율 계상 기준 · 제재/환수 기준 2종 |
| 연구재단 QA 사례집 | `금지예시`/`허용예시` 보강 — 출처 R&D 명기, `verified=false` |
| L3 (현재 기관) | 오버레이 (§3-1) |

## 5. 컴파일 (Stage 1)

```
L2 세부관리기준 본문 + [붙임2]·[참고N] 표 + 유효 별표 4건
  ├─ 표 추출 (extract_tables) — 미구현. 컴파일의 병목
  ├─ 숫자/목록만 추출 → rules (verified=false)
  ├─ 제3조 부근 파싱   → precedence_rules (verified=false)
  └─ D단계 사람 검수   → verified=true
```

## 6. 사람 검수 (D단계)

절차는 `룰테이블_검수가이드.md`. 도구는 `scripts/review_rules.py`.

- **LLM 에게 검수를 맡기지 않는다** — 룰을 뽑은 것과 같은 종류의 실수를 반복한다
- `근거` 가 비면 그 행은 룰이 아니다 — 지우거나 조문을 찾아 채운다
- `verified=false` 룰은 단독으로 "가능" 을 만들 수 없다 (조문 인용 동반 필수)

## 7. 룰이 판정에서 하는 일

```
(2) 룰 조회   사업 × 기관유형 × 비목 → 1행
     금지목록 적중  → LLM 없이 즉답 "불가"          [빠른 경로]
     한도 있음     → 코드가 금액 비교 → 결과를 (4)-b B4 블록에 주입
     사전승인 필요 → "해야할일" 후보
(6) 검증      precedence 로 최종 효력 결정
```

룰은 결론을 내리기도 하고(금지목록), 재료만 주기도 한다(한도 비교 결과) — `허용` 값이 정한다.

### 🔑 `우선규범` — 어느 L1 이 우선인가

`L1 > L2` 를 뭉뚱그리면 안 된다. 상위 규범이 사업마다 다르다.

| 사업 | 우선규범 |
|---|---|
| 모두의창업 (일반·기술트랙) | 「중소기업창업 지원사업 운영요령」 |
| 초격차 | 요령 및 지침 (특정 규범명 없음) |

### 🔴 모두의창업 — 일반·기술트랙만 다룬다

모두의창업 세부관리기준은 **제1편 총칙 / 제2편 일반·기술트랙 / 제3편 로컬트랙** 구조다.
**우리 범위는 일반·기술트랙**이고 로컬트랙(제53조 이하)은 다루지 않는다.

로컬트랙은 상위 규범이 통합관리지침이 아니라 「신사업창업사관학교 운영지침」이라
**위임 계통 자체가 다르다.** `precedence_rules` 조회 키가 `사업명` 이라 두 트랙이 같은
키를 쓰므로, 제3편을 남겨두면 **일반·기술트랙 판정에 범위 밖 룰이 딸려온다.**
`build_precedence.범위밖_구간` 이 제3편 헤딩에서 문서를 자른다.

---

## 8. 미결

| # | 항목 |
|---|---|
| 1 | 유형 18종 분기 스키마 설계 — Stage 1 선결 |
| 2 | 표 추출 미구현 (`RAG.md` §5) |
| 3 | D단계 검수 — 룰 19행 + precedence 전량 `verified=false` |
| 4 | 비목 어휘집 파일 생성 (§1-b) |
| 5 | 사업별 계산식 공통 추상화 가능 여부 |
