# archive — 더 이상 쓰지 않는 것

**여기 있는 것은 참조하지 않는다.** 이력 추적·재현성 확보용 보관소다.
현행 코퍼스·문서는 프로젝트 루트와 `2026_Finance_DATA_FOR_RAG/` 를 본다.

정리 2026-08-28. 기존 `_폐기/` 를 이 폴더로 통합했다.

---

## 구_문서/

| 항목 | 폐기 사유 | 대체 |
|---|---|---|
| `데이터셋_활용정리/데이터셋_활용정리.md` | 구 L1~L5 계층 기준. 레이어 정의가 현행(L1중기부/L2창진원/L3주관기관)과 전면 불일치 | `DATASET_STRUCTURE.md` + `프로젝트설명.md` §5 |
| `다운로드_URL_전체목록.md` | 2026-08-10 작성. 구 74파일 데이터셋의 수집 주소록 | 수집 완료. `법령 PDF/_law_sources.json` |
| `법령데이터_수집설계.md` | 2026-08-20 설계 노트. 내용이 실행·검증되어 현황 문서로 승계됨 | `법령_크롤링_현황.md` |

## 구_데이터/

| 항목 | 폐기 사유 |
|---|---|
| `manifest.json` · `manifest.json.bak` | 🔴 **동결.** 2026-08-12 구 데이터셋 기준(405파일 · 구 L1~L4 스킴 · 폐기된 `규정문서/` 경로). **인덱싱 대상 판단에 쓰지 말 것** — 현행은 `DATASET_STRUCTURE.md`, 법령은 `_law_sources.json` 의 `index` 플래그 |
| `L4_기관규정/` (24건) | ⛔ **타 대학·타 기관 규정.** 판정 인덱스 투입 시 남의 학교 규정이 사용자 판정에 인용되는 사고. **용도는 L3 업로드 파이프라인 테스트 입력 전용** (파싱 게이트·참조 해소·기관 간 누수 검사) |
| `L2_부처지침/` · `L3_공고별표/` · `규정문서/` · `kosmes_data/` | 신규 데이터셋(`2026_Finance_DATA_FOR_RAG/`)과 MD5 동일한 중복 |
| `통계_시장규모/` (8건) | 시장 규모 산정용. 판정과 무관 |
| `kised_자료실_제목목록.csv` | 창진원 자료실 크롤링 목록(252행). 수집 완료로 역할 종료 |
| `_이동내역.json` | 구 `_폐기/` 이동 이력 |

## 구_코드/

(비어 있음 — 코드는 전부 `scripts/` 로 이동했다.
`hwp_extract.py` 는 파이프라인에서 기각됐으나 `scan_tables.py` 가 import 하므로 `scripts/` 에 남긴다.)

---

## ⚠️ 인덱서가 지켜야 할 것

```python
# 경로 블랙리스트 — 조건 없이 거부
BLOCKED = ["archive/", "_골든셋/", "_테스트_L3/", "_정리보류/"]
```

`_골든셋/` 은 정답 유출, `archive/` 는 구 코퍼스 오염이다.
"실수로 안 넣기" 가 아니라 **넣을 수 없게** 만든다.
