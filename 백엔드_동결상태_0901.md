# 백엔드 동결 상태 (2026-09-01)

> **이 문서가 답하는 질문 하나**: 프론트가 지금 어디에 코드를 얹어도 되는가.
> 「지금 무엇이 있는가(스펙·스키마)」는 `API_DB_명세_v1.md`(레인 D)가 정본이다.
> 이 문서는 그 중 **무엇이 굳었는가**만 본다 — 겹치는 서술은 넣지 않는다.
>
> 세 갈래만 쓴다. 「사실상 확정」 같은 중간 어휘는 쓰지 않는다.
> 각 항목에 **무엇으로 확인했는지**(테스트 이름 / 실측 쿼리 / 파일:줄)를 붙인다.
> 🔴 **읽을 때 조심할 것** — 「테스트 통과」·「스모크 통과」는 「판정이 맞다」의 증거가
> 아니다. 「올바르게 뽑았다 / 안 죽는다」의 증거일 뿐이다. 아래 각 항목에서
> **관측**(무엇을 봤는지)과 **추론**(그래서 뭐라고 결론지었는지)을 줄을 갈라 쓴다.
>
> 작성: 레인 C(ai-ff). 초안을 그대로 베끼지 않고 항목마다 직접 확인했다 — 확인 결과
> 원안과 다른 곳(③ `accounts` 테이블, `tenant.decisions` 컬럼 수, SSE 이벤트 개수,
> 저장 실패 사유 종류)은 정정하고 표시해 뒀다.

---

## ① 동결 — 프론트가 코드를 얹어도 된다

### 엔드포인트 18경로 · 20메서드

**관측**: `app.openapi()["paths"]` 로 직접 뽑았다(⚠️ `app.routes` 로 세면 안 된다 —
이 FastAPI 버전은 `include_router` 를 평탄화하지 않아 라우터 9개가 통째로 빠진다,
`tests/test_contract.py:602-612` 참고). 결과: **경로 18개, (메서드,경로) 조합 20개.**
레인 A·B·C 라우터(`routes_plans`·`routes_tasks`·`routes_l3`) + `main.py` 자체 엔드포인트
전부 포함.

**추론**: `tests/test_contract.py::test_계약_엔드포인트가_전부_살아있다` 와
`test_계약에_없는_api_가_늘지_않았다` 두 방향(빠짐·늘어남) 다 잠가서 CI 성격으로
돈다 — 프론트가 이 표 밖 경로를 새로 치지 않는 한 이 목록은 안 흔들린다.

### SSE 이벤트와 순서 — `/api/judge`

**관측**: 4-way 전부(가능/조건부/불가/판단불가)와 `/api/normalize` 를 직접 쳐서 이벤트
이름을 순서대로 뽑았다.
```
가능/조건부/불가:  진행 진행 진행 → 판정 → 해야할일 → 인용 → 전제 → 참조사슬 → 결과 → 저장 → 완료
판단불가:          〃                                              → 문의초안 →   〃
normalize:         진행 → 필드×5 → 결과 → 완료
```
**⚠️ 원안 정정(ai-14 확인 완료)**: 원안은 「SSE 이벤트 8종」이라고 했는데 실측하면
**judge 는 상시 9종**(`진행·판정·해야할일·인용·전제·참조사슬·결과·저장·완료`)이고,
판단불가 경로에서만 `문의초안` 이 하나 더 붙어 총 10종이다. 저장 이벤트가 순서에
새로 낀 자리는 원안대로 `결과` 다음·`완료` 앞이 맞다(`server/main.py::judge` 의
`gen()` 실측 순서와 일치). **서버 전체 기준(normalize 포함)으로는 12종**이다 — judge
10 + normalize 전용 `필드`·`오류` 2. 이 문서에서 「이벤트」는 judge 스트림만 가리킨다 —
서버 전체와 갈라 읽을 것(ai-14 지적).

**추론**: 이벤트 이름 목록·순서는 `tests/test_regression_rank.py`·`test_contract.py`
쪽 SSE 순서 테스트가 잠근다(레인 D). 이름을 늘리거나 순서를 바꾸면 그 테스트가
빨개진다 — 하트비트 같은 걸 넣더라도 **새 이벤트 이름을 만들지 않고** SSE 주석
(`: keep-alive`)으로 흘려야 이 동결이 안 깨진다.

### 판정 4-way → 배지 3종 접기

**관측**: `server/_common.py:37,49-55` 의 `탭_판정` 딕셔너리.
```
가능 → 특이사항없음 / 조건부 → 확인필요 / 불가 → 위험 / 판단불가 → 확인필요
```
`tests/test_regression_rank.py::test_탭_매핑이_4way_를_빠짐없이_덮는다`(빠짐·중복 둘 다
검사) · `test_탭이_다른_판정을_안_섞는다`(탭별 허용 판정만 오는지)가 이 매핑을 잠근다.

### `저장` 이벤트 계약과 `저장:false` — 전부 정상 경로

**관측**: `server/main.py::_판정_저장_시도`(실제 호출부, 레인 C) + `server/persist.py`
(실 저장 본체, 레인 A)를 같이 읽었다. `저장:true` 모양은 고정:
```
{"저장": true, "decision_id": int, "plan_id": int,
 "할일": {"생성":int,"갱신":int,"보존_user":int,"보존_날짜수정":int,"코드매칭":int,"코드미상":int}}
```
`저장:false` 는 `{"저장": false, "사유": "..."}` 모양은 고정이지만, **⚠️ 원안 정정**:
`사유` 문자열은 원안이 말한 「3종」보다 많다. 지금 코드에 실제로 존재하는 경로:
`plan_id 없음`(`main.py`) · `캐시 적중 — 새 판정 기록 없음`(`main.py`) ·
`decision_id 없음 — 판정이 기록되지 않았다`(`persist.py:53`) ·
`plan_id {id} 을(를) 찾지 못했습니다 (기관 불일치 포함)`(`persist.py:71`) ·
`저장 실패 ({예외타입})`(`main.py`, persist 쪽 예외 포착).
**동결하는 건 `{저장:bool, 사유?:str}` 모양과 「false 는 항상 정상 경로(빨간 배너
금지)」다 — `사유` 자체는 닫힌 enum 이 아니라 자유 텍스트로 다뤄라.**
🔴 **프론트 계약: `사유` 문자열을 파싱하거나 분기하지 마라.** 문구가 늘거나 바뀌면
그 분기가 깨진다 — 프론트가 알아야 하는 건 `저장:false` 는 정상 경로라는 것 하나뿐이다
(ai-14 지적).
`tests/test_judge_persist.py` 5건(순서·plan_id 없음·캐시 적중·예외 내성·decision_id
결과 미노출)이 이 모양을 지금 그린 상태로 통과한다.

### DB 스키마 — `tenant.*` 12개 + `corpus.check_items`

**관측**: `information_schema.tables WHERE table_schema='tenant'` 실측 —
`accounts, decisions, expense_plans, f_exec, f_personnel, f_profile, incidents,
l3_articles, l3_documents, orgs, plan_tasks, unmapped_premise` **12개** 정확히 일치.
오늘 자 ALTER 셋을 제약조건(`pg_constraint`)으로 직접 확인:
- `plan_tasks_유형_check`: `유형 IN ('기타','계약','비교견적')` — `db/init/05_frontend_alter.sql`
  (파일 자체가 "프론트 요구서(2026-09-01) 반영"으로 표기돼 있다)
- `l3_documents_파싱품질_check`: `IN ('대기','pass','warn','fail')` — '대기' 포함 확인
- `corpus.check_items` 컬럼에 `유형` 존재 확인

**추론**: 세 ALTER 다 스키마에 실제로 반영돼 있다 — "추가 예정 없음"은 ai-14 원안 그대로
받되, 이건 관측이 아니라 조율 세션의 계획 진술이므로 그 프레임 그대로 남긴다.

### `tenant.decisions` — Agent 세션이 동결 승낙

**⚠️ 원안 정정**: 원안은 「11컬럼」이라고 했는데 실측하면 **27컬럼**이다(`decision_id,
created_at, org_id, 사업명, 기관id, 질문원문, 정규화, 비목, 금액, 판정, 신뢰등급, 인용,
해야할일, 지연ms, 모델, 전제, 검색스냅샷, 코퍼스버전, plan_id, 요약, 버전스탬프,
참조사슬, 강등사유, 미매핑전제, 강등코드, 경로, 실패단계` — `information_schema.columns`
실측). 컬럼 **개수**가 동결 여부를 결정짓는 게 아니라 **모양**(이 27개 이름·타입)이
Agent 세션(ai-25)의 동결 승낙 대상이고, 그 세션이 오늘 마감했다는 것도 원안 그대로
남긴다(이건 내가 직접 확인할 방법이 없는 조율 세션 간 사실이라 출처를 ai-14 로 남긴다).

### 모든 정규화 경로가 같은 키를 준다 (목·폼·자연어)

**관측**: 오늘 `server/main.py::_실_정규화` 의 공통 setdefault 지점에
`out.setdefault("하위항목", None)` 을 추가하고 `_목_정규화` 픽스처에도 같은 키를
넣었다(레인 C, 이번 세션). 폼 경로는 못 뽑는 필드라도 키 자체는 항상 온다 — 프론트가
`'하위항목' in 결과` 로 분기해도 경로마다 안 갈린다.
**추론**: 계약의 「실서버로 갈아끼워도 프론트 코드는 한 줄도 안 바뀐다」가 이제 이
필드에서도 성립한다. 이걸로 세 경로(목/폼/자연어)의 응답 키 집합이 전부 같아졌다.

---

## ② 모양은 동결, 값이 아직 안 채워짐

### `tenant.decisions.plan_id`

**상태**: **코드 배선 완료 · 실측 미확인(GPU 팟 미가동).**

**관측**: `server/main.py::_실_판정` 이 `orchestrate.판정(질문, 사업명=…, org_id=…,
plan_id=body.plan_id)` 로 `plan_id` 를 넘기도록 오늘 고쳤다(레인 C). 실 plan_id(295,
테스트 후 삭제)로 직접 `orchestrate.판정()` 을 호출해봤더니
`{'판정': '판단불가', 'decision_id': None}` 이 돌아왔고, 호출 전후로
`tenant.decisions` 의 `max(decision_id)` 는 75 그대로였다 — 새 행이 안 생겼다.
**추론**: 이 환경에 vLLM/임베딩 서빙이 안 떠 있어서(GPU 팟 미가동으로 보인다) 실
판정 자체가 못 돈 것으로 보인다 — `plan_id` 배선이 틀렸다는 증거는 아니다(코드 경로
자체를 못 태워봤으니 맞다는 증거도 아니다). **컬럼 계약은 안 바뀐다 — 프론트 영향은
0이다.** GPU 팟이 뜬 다음 실측이 필요하다(`tests/test_judge_persist.py::
test_저장_이벤트에_decision_id가_실제로_채워진다` 가 그 실측을 xfail 로 잡아두고
있다 — 통과하면 이 항목을 ①로 올린다).

---

## ③ 미결 — 프론트는 자리만 잡아라

### 로그인 · 사이드바 계정/팀명

**⚠️ 원안 정정**: 원안은 「`accounts` 테이블 없음」이라고 했는데 실측하면 **테이블은
있다** — `tenant.accounts(account_id uuid, org_id uuid, email text, pw_hash text,
created_at timestamptz)`, 다만 **행이 0개**고 이 테이블을 읽거나 쓰는 API 엔드포인트가
`server/` 어디에도 없다(엔드포인트 20개 목록에 `/api/auth/*` 류가 없는 게 그 증거).
**추론**: 스키마는 로그인을 받을 준비가 돼 있지만 **배선이 전혀 안 됐다** — "테이블이
없다"가 아니라 "테이블만 있고 아무 것도 안 잇는다"가 정확하다. 현재는 `org_id` 를
요청 파라미터로 받는 방식(게스트 게이트)만 산다. 프론트는 로그인 화면·사이드바
계정/팀명 자리만 잡고, 실제 인증 플로우는 이 표에 없다.

### 화면 11 — 조항별 해당/불해당/불확실

**관측**: `/api/judge` 의 `인용`은 `{조번호,조제목,원문,doc_id}`, `전제`는
`{사실,근거조항,매핑,미충족시}` 뿐이다(`server/main.py:780-783`, `_selftest` 계약
검증). 조항 하나하나에 "해당/불해당/불확실"을 매기는 필드가 응답 어디에도 없다.
**추론**: 이건 Agent 세션(orchestrate/판정 조립) 쪽 출력 확장이 있어야 여는 문인데,
그 세션이 오늘 마감했다(ai-14 진술, 내가 직접 확인한 사실은 아니다) — **당분간 이
문은 안 열린다.** 프론트는 화면 11에 조항별 배지 자리만 잡고 실제 값은 못 받는다.

### `PUT /api/profile` 쓰기 경로 미배선

**관측**: `server/main.py::_실_프로필_저장` 이 그대로 스텁이다 — 실제로 아무것도
안 쓰고 `{"저장": False, "이유": "f_profile 쓰기 경로 미배선 (tenant 소유는 E 세션)", ...}`
을 돌려준다. `tenant.f_profile` 테이블은 컬럼까지 전부 있다(`profile_id, org_id,
사업명, 협약시작일, 협약종료일, 정부지원_현금, 자기부담_현금, 과업범위요약,
updated_at` — 실측). 응답 모양은 `{"저장": bool, "profile_id"?: ..., "이유"?: str}`
로 동결(ai-14 지시) — 지금 목 경로가 주는 `받은값`(디버그 잔재)은 이 모양에 없으니
프론트가 의존하면 안 된다. **이번 세션은 문서만이다 — 구현은 다음이다.**

### 🔴 게스트끼리 서로의 지출계획이 보인다 (구조적, 오너 판단 대기)

**관측**: `server/routes_plans.py:159-163` 의 `_org조건()`.
```python
def _org조건(org_id, 별칭="p"):
    if org_id is None:
        return f"{별칭}.org_id IS NULL", ()   # 게스트 전원이 이 한 버킷
    return f"{별칭}.org_id = %s", (org_id,)
```
`org_id` 가 없는 요청은 전부 같은 "게스트" 버킷으로 묶인다 — `_실_목록`·`_실_통계`·
`_실_상세` 전부 이 함수를 거친다.
**추론**: 로그인이 없는 지금 구조상 게스트 A 의 지출계획이 게스트 B 에게도 보인다.
로그인이 닫히기 전까지는 구조적인 문제라 이 세션에서 고칠 수 있는 게 아니다 —
**해결책을 이 문서에서 정하지 않는다.** 사용자에게 이미 올라간 사안이다.

---

## 알려진 함정

### 「빈 DB 로 재현하면 최신」 ≠ 「기존 DB 에 적용하면 최신」

**관측**: `corpus.check_items.유형` 과 `tenant.l3_documents.파싱품질` 의 `'대기'` 값은
오늘 **살아 있는 DB 에는** 들어가 있다(위 ① 항목에서 `pg_constraint`·컬럼 목록으로
확인). 그런데 `db/init/*.sql` 에는 이 둘이 `CREATE TABLE IF NOT EXISTS` 안 인라인
정의로만 있고, 그 상태를 만드는 **ALTER 문이 없다.**
**추론**: 어제 만든 DB 를 들고 `db/init/*.sql` 을 처음부터 다 돌려도 이 둘은 안
생긴다 — 그 환경에서는 체크항목 `유형` 조회와 L3 업로드 INSERT(`파싱품질='대기'`)가
죽는다. 「스키마를 처음부터 다시 만들면 최신과 같다」와 「기존 DB 에 SQL 을 돌리면
최신이 된다」는 다른 문장이다 — 이 프로젝트에서 그 둘을 같은 걸로 읽어서 사고가 난
적이 있다(레인 B 대조가 이 사각에 걸렸다, ai-14). 레인 B 가 `05_frontend_alter.sql`
에 멱등 ALTER 를 추가하는 중이다.

### `GET /api/l3/{doc_id}` 가 실 경로에서 누구에게나 항상 404

**증상**: `SUDDOE_MOCK=0` 에서 이 엔드포인트는 존재하는 `doc_id` 를 넣어도 항상
404(「L3 문서를 찾을 수 없습니다」)를 돌려준다.
**원인**: `routes_l3.py::_실_상태` 의 쿼리가
```sql
WHERE doc_id = %s AND (%s IS NULL OR org_id = %s)
```
형태다 — psycopg 가 파라미터를 개별 타입으로 바인딩하는데 `$2` 가 `... IS NULL` 자리
에만 나와 타입을 못 정한다(`IndeterminateDatatype: could not determine data type of
parameter $2`). `_질의()` 가 예외를 조용히 삼켜 빈 리스트를 돌려주고, 그걸 호출부가
404 로 바꾼다 — 즉 **격리가 작동해서 막힌 게 아니라 쿼리 자체가 매번 죽어서** 아무
것도 안 나오는 것뿐이다(레인 D 가 격리 테스트에서 발견, ai-14 확인).
**프론트 영향**: 화면 4 온보딩의 L3 업로드 상태 폴링이 **영원히 「분석 중」 스피너에
갇힌다.**
**고치는 방향(다음 세션용)**: `routes_plans.py::_org조건()` 이 이미 이 문제를 파이썬
분기(조건 자체를 두 갈래 SQL 문자열로 나눔)로 피해가고 있다 — `_실_상태` 도 캐스트
(`%s::text IS NULL`)를 박거나 같은 분기 방식을 쓰면 된다. D 가 이 자리에 skip 가드를
걸어놨으니 고치면 그 테스트가 저절로 살아난다. **이 항목은 이번 세션에서 고치지
않는다 — 인수인계용 기록이다.**

---

## 정정 요약 (원안 대비)

| 항목 | 원안 | 실측 |
|---|---|---|
| SSE 이벤트 종류 | 8종 | judge 상시 9종 + 판단불가 전용 `문의초안` 포함 10종 |
| `저장:false` 사유 | 3종(닫힌 목록 뉘앙스) | 5개 문자열이 코드에 실존, 닫힌 enum 아님(자유 텍스트로 다룰 것) |
| `tenant.decisions` 컬럼 수 | 11컬럼 | 27컬럼 |
| `accounts` 테이블 | 없음 | 스키마는 있음(0행) — 배선이 없는 것 |

나머지(18경로·20메서드, 탭 매핑, ALTER 3건, `plan_id` 배선 상태, 조항별 근거 없음,
`PUT /api/profile` 미배선, 게스트 버킷 문제)는 원안과 일치했다.
