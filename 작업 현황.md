# 작업 현황

「써도돼요」 일자별 작업 로그. **최신이 위**에 온다.
설계 결정은 여기 적지 않는다 — 결정은 `구현.md`·`서비스 아키텍쳐.md`, 여기는 **무엇을 했고 무엇이 나왔나**만.

작성 규칙
- 날짜 헤딩은 `## YYYY-MM-DD (요일)`, 항목마다 `HH:MM` 을 단다
- 산출물은 **경로와 수치**를 적는다. "개선했다" 가 아니라 "320 → 181"
- 발견한 함정은 재발 방지 위치(어느 스크립트·어느 문서에 박았는지)까지 적는다
- 미완은 숨기지 않는다. **왜 안 했는지**를 남긴다

---

## 2026-08-30 (일) · 설계 문서 전면 재작성 — "현재 상태만 담은" 스냅샷 체계로

기각 사유·결정 이력·실측 서사를 설계 문서에서 전부 걷어냈다 (이력은 git·이 로그·`실제 구현.md`
가 정본). 3,608줄 → **1,641줄** (-54%) + `구현.md`(412줄) archive 이관.

| 작업 | 결과 |
|---|---|
| `서비스 아키텍쳐.md` 재작성 | 1,090 → 362줄. 정본 스냅샷 — 용어·번호 대응표 신설, 설계 원칙 6개 명문화, 임계치 대장 포함 미결 대장 통합 |
| 축별 4종 재작성 | RAG 727→423 · LLM 409→248 · Agent 377→186 · rule_base 260→175. 사양·DDL·의사코드는 전부 보존 |
| `CLAUDE.md` 재작성 | 204→120줄. 읽기 순서 갱신(아키텍처가 1번), 재논의 금지는 사유 없는 명단으로 |
| `구현.md` 이관 | `archive/구_문서/`. 살아있던 미결(멀티턴 상태·L3 온보딩·에러 정책)은 스냅샷 미결 대장에 흡수, 잔여 참조 6개 파일 정리 |
| 유물 정리 | 신뢰등급 산정 주체 명시 유지 · AzureFoundry "(예비)" 격하 · 화면 결번 사유 명시 · ④-a↔화면 7 의존 명시 |
| `입력_적법성_점검.md` 갱신 주석 | 줄번호 기준 안내 + B-1 조건 2(로컬 LLM) 상태 변화 |
| 배경·절차 문서 3종 재작성 | 프로젝트설명 431→248 (무효 공지·센터 화면·온프레미스 구전제 제거, F축 F1~F7 현행화) · 법령_크롤링_현황 254→131 (조달 방법·API 함정 14·재실행 가이드만, **§7 청킹 방침 제거 — RAG.md 와 충돌하던 것**) · 확인_가이드 359→183 (구 스냅샷 수치·미커밋 경고·구본 흐름도 제거, Recall@5 기준으로 갱신) |

## 2026-08-30 (일) · 아키텍처 실험 전 확정 13건 — 축별 문서 정합화 + 적대적 골든셋 27문항

정밀 점검에서 **상위 문서에 남은 구본 서술 2건(사례 top-3 투입 · top-8)이 축별 문서 최신
결정과 모순**임을 확인하고, "실험 전에 확정 가능한 것" 을 전부 닫았다. 문서 5개 + 스키마 1개 갱신.

| 작업 | 결과 |
|---|---|
| 사례(B급) 경로 일원화 | **판단불가 경로 전용 확정.** `서비스 아키텍쳐.md` §0#17·§1·§4·화면7 정정, `Agent.md` §8 정본화 |
| top-8 → top-5 | 상위 문서를 `RAG.md` §4-2 에 역정합 |
| ④ 출력 스키마 2겹 확정 | LLM 출력 [1겹] / 최종 응답 [2겹]. **신뢰등급·조번호·버전스탬프·참조사슬 = 코드 몫** (`LLM.md` §3-4) |
| 전제.매핑 수식 폐기 | F필드 **참조 목록**으로 확정. 계산식은 룰에서 코드가 도출 |
| (4)-b 조립 사양 신설 | `LLM.md` §3-7 — B0~B6 블록 순서·S번호 부여/치환·비목 어휘집. 프롬프트 미결이 "문구 튜닝"으로 축소 |
| L3 테이블 분리 확정 | `tenant.l3_articles` + 사용자 축 DDL(f_profile 4분면·f_exec 3축·f_personnel·unmapped_premise) `RAG.md` §2-3. CLAUDE.md WHERE절 동시 갱신 |
| 청킹 임계 재정의 | 3,000자 → **KURE 900토큰** (3,000자≈1,500~2,000토큰이라 max_seq 1024 에서 꼬리 잘림). Stage 2 게이트: 초과 0건 |
| 컨텍스트 헤더 신설 | 임베딩·BM25 입력 = `[레이어\|사업\|문서\|장\|조]` + 본문. 저장 원문 불변 (`RAG.md` §3-5) |
| BM25 토큰화 정책 확정 | kiwi 품사 채택 목록 명시 — 동등성 검증의 재현 조건 (`RAG.md` §2-5-1) |
| 스키마 개정 | `01_schema.sql`: chunks.적용대상 · chunk_terms/chunk_len/term_df · decisions 전제/검색스냅샷/코퍼스버전 (기존 컨테이너 재적재 필요) |
| 사다리 6단계 추가 | **배포 양자화(AWQ 4bit)로 골든셋 재실행** — API 측정은 상한 확인일 뿐. 실험 환경 = RunPod+vLLM 확정, 어댑터 OllamaLocal→LocalVLLM |
| 비목 어휘집 사양 | `rule_base.md` §1-b — L1 제36~45조 기간 어휘 + 사업별 [붙임2] 확장. 파일 생성은 미착수 |
| **적대적 골든셋 27문항 작성** | `2026_Finance_DATA_FOR_RAG/_골든셋_스테이징/적대적세트_초안.json` — 부정행위 사례집 창업기업 30건 변환(불가21·조건부6), 원문 인용 13건, 전량 verified=false. **검수 후 `_골든셋/` 이동 필요.** 골든셋 목표 30 → 70± (현 68) |

**추가 (같은 날 오후) · Claude API 전 슬롯 제거 확정**

| 작업 | 결과 |
|---|---|
| ④·⑤ 오픈소스 전환 | ④ = Qwen3 32B thinking + 투표(RunPod·vLLM), ⑤ = Qwen3 8B. 구본 "전 슬롯 API" 폐기 |
| 결정 근거 발굴 | `입력_적법성_점검.md` B-2 — **④-b 는 F 프로필(B5) 포함**이라 "규정 원문만이므로 API 허용" 전제가 애초에 틀려 있었다. B-2 에 해소 주석 |
| 불일치 정리 | 서비스아키텍쳐 §7 표·어댑터 그림(OllamaLocal 잔재)·사다리 문구, LLM.md §1·§6·§8, PNG 2종 재생성 |
| 남는 조건 | 사다리 1~4·6단계 통과가 배치 유지 조건. 실패 시 API 폴백 재론(F축 슬롯 제외). 어댑터 `f_axis` 차단 게이트는 미구현 |

미완: 비목 어휘집 파일 추출, 골든셋 68문항 검수(A9 는 사업별 정답 갈림 문항 — 검수_포인트 필독),
xref_mismatch 테이블(01_schema.sql §9)과 refs 의 관계 정리, 어댑터 f_axis 게이트 구현은 다음 세션.

## 2026-08-30 (일) · 전처리 파이프라인 — Stage 2 앞까지 전 코퍼스 실행

공정 사양을 `데이터 전처리 파이프라인.md` 로 문서화하고, 그 순서대로 전 코퍼스를 돌렸다.
상세 근거·실측은 `실제 구현.md` 2026-08-30 항목.

| 단계 | 결과 |
|---|---|
| Stage 0 | **276문서 / 25,686조 / 실패 0** · 성공 248 · 경고 28 · 제외 114 |
| Stage 0-T 표 추출 | 신설. 22문서 표 578 -> 병합 **469** · 실패 0 |
| Stage 0.5 적용대상 | 현행 9문서 422조 · **결정 247(59%)** · LLM 대기 175 |
| Stage 0.7 참조 그래프 | **271문서 28,243엣지** (전 1,288). 레이어별 해소율 추가 |
| Stage 0.8 우선순위 | 8건 유지 |
| Stage 1-a 비목 어휘집 | **26 비목** (기간 10 / 사업별 16) |

### 신설·폐기

| 대상 | 내용 |
|---|---|
| `scripts/stage0_run.py` | **신설.** Stage 0 오케스트레이터 |
| `scripts/stage0_ingest.py` | 🔴 **폐기 대상.** 2026-08-12 동결된 `manifest.json` 을 읽고 있었다 |
| `scripts/extract_tables.py` | **신설.** Stage 0-T |
| `scripts/tag_apply_target.py` | **신설.** Stage 0.5 결정적 1단 |
| `scripts/build_item_vocab.py` | **신설.** 비목 어휘집 (미실행) |
| `scripts/build_refs.py` | 입력을 PDF → Stage 0 산출물로 전환 |
| `데이터 전처리 파이프라인.md` | **신설.** 공정 정본 |

### 고친 오류 12개

1. **V3 가 폐지 조문을 빈 조로 셌다** — 빈 조 1,964 중 1,667(85%)이 `제N조 삭제 <날짜>`.
   V3 경고 46건이 전부 오탐이었다. 경고 88 → 57
2. 참고 플래그가 경고에 섞였다 → `참고:` 접두 분리
3. **`build_refs` 가 법령 XML 219건을 안 봤다** (`*.pdf` 만 스캔). 엣지 1,288 → 28,188
4. 표 추출에 룰 아닌 문서가 섞였다 — 별지서식 469표·모집공고 31표
5. 섹션 라벨이 본문 참조를 헤더로 오인 (예비창업 p2~4)
6. **2단 조판에서 헤더가 다른 컬럼 텍스트에 붙어 사라졌다** — 초기창업 붙임1·초격차 참고5·
   창업중심대학 참고1. 단어 x좌표로 컬럼 점프를 봐서 해소
7. 🔴 **PDF 가 문자중복 해소를 안 탔다** — 통합관리지침 제14차가 `"창창업업기기업업등등"` 상태로
   들어와 조 88개가 30개로 깨지고 `outline_numbered` 로 오분해됐다. **판정 최상위 근거 문서다.**
   원인은 오케스트레이터가 `pdftext.extract()` 대신 `stage0_extract.extract_pdf()` 를 쓴 것
8. 🔴 **법령 XML 22,551조 전부 `조번호_int` 가 None 이었다** — `extract_xml()` 이 그 키를 안 준다.
   V1 단조성 검증이 219건에서 무력화되고 `ix_articles_doc` 정렬도 의미를 잃었다

9. 🔴 **사례집이 조 단위로 분해돼 인용 법조문을 그 문서의 조로 잡았다** — 권익위재결례집 첫 조가
   `제382조의3(이사의 충실의무)`(상법), KISTEP 은 혁신법 제35조. 셋 다 `quality=high` 라
   그대로면 **남의 법 조문이 사례집 doc_id 를 달고 판정 인덱스에 들어간다.**
   사례 레이어는 조 분해를 태우지 않고 `case_paragraph` 로 뺐다
10. 삭제 판별이 조 형태만 잡았다 — `[별지 제9호서식] 삭제 <1996.3.30>` 같은 별표·서식 폐지분이
   빈 조로 남아 V3 를 다시 오염시켰다

11. 🔴 **규범어 없는 인용을 조 헤딩으로 오인** — 통합관리지침 제14차 제56조③ 의
   `…제재 처분은 제65조(제재 및 환수)에 따른다` 를 헤딩으로 잡았다. **제56조가 ④항을 잃고**
   제65조가 제56조·제57조 사이에 끼어 조번호가 65->57 로 역전됐다. **판정 최상위 근거 문서다.**
   닫는 괄호 뒤 조사(`)에` `)의` `)를`)로 판별하도록 고쳤다
12. 사례 단락 분할이 통짜였다 — 권익위재결례집이 단락 1개 222,683자. 3,000자 상한을 걸었다

7·8 로 219->231 / 57->45, 9·10 으로 242/34, 11·12 로 **248 / 28** 까지 갔다.
태깅 결정률도 50% -> 59% 로 올랐다.

### 🔴 설계 문서와 어긋난 것 2건

**`RAG.md` §3 "절(節) 헤딩이 적용대상을 이미 선언한다" 는 틀렸다.**
7개 사업 장 제목이 총칙·추진체계·선정·협약… **절차 구분이지 주체 구분이 아니다.**
주체를 선언하는 건 조 제목이다. 조제목+본문 주어 규칙으로 50%를 덮고 나머지는 LLM 이 본다.

**현행/구판 구분이 파이프라인 어디에도 없다.** `documents.status` 가 스키마에 있는데
Stage 0 이 안 채운다. 구판까지 태깅하면 판정 인덱스에 안 들어갈 것을 태깅하는 낭비다
(27문서 948조 vs 현행 9문서 370조). 임시로 `tag_apply_target.현행` 상수를 뒀다.

### 팀 반입 자료 — `공유받은 파일/`

비목명 종합(56 비목 × 8사업) · 증빙서류 종합(124종). 세부관리기준 21개 판본 대조본이다.

- **쓴다**: 비목 어휘집 시드 · 룰 커버리지 체크리스트 · 표 추출 검증 대조본 · 증빙 발급처 안내
- **판정 인덱스 투입 금지**: 근거 조번호가 없고 실무 관행 보충이 섞여 A등급 인용 원천이 못 된다

### 다음

- Stage 0.5 2단 LLM 태깅 184조 (RunPod 개통 후. 입력 `_apply_target_todo.json` 준비됨)
- 게이트경고 57건 분류
- `rules` 6개 사업 확장 + D단계 사람 검수
- Stage 2 (청킹·임베딩·BM25·Supabase 적재)

---

## 2026-08-30 (일) · Stage 0 닫음 — 8건 전수 통과

전날 남긴 2건(창업중심대학·TIPS)을 닫았다. 그 과정에서 **"통과" 로 세고 있던 2건
(모두의창업·도약)이 실제로는 깨져 있었다.** 근거·좌표 실측은 `실제 구현.md` 2026-08-30 항목.

| 작업 | 결과 |
|---|---|
| 창업중심대학 역전 1건 원인 규명 | **원문 오류.** 제35조가 둘(이의신청 / 권리 의무 이전) |
| ㄴ 그 과정에서 조 소실 발견 | 옛 중복제거가 이의신청 본문을 덮어써 **조 하나가 사라져 있었다** |
| ㄴ `[참고1]` 매몰 발견 | 45% 임계에 걸려 부칙이 비목 정의·집행기준 5,268자를 삼킴 |
| 섹션 경계 재설계 | 45% 비율 → **부칙 우선.** 8건 전수: 진짜 헤더 21개 전부 부칙 뒤, 오탐 4개 전부 부칙 앞 |
| 조번호 중복 처리 | 키를 `(조번호, 조제목)` 으로. 제목 다르면 둘 다 보존 + `[2]` 접미 |
| TIPS 개요형 전략 신설 | `outline_numbered`. 표제 35개(1..35) 단조, 목차와 일치 |
| 목차 컷 신설 | 점선 지도 10회 이상 (TIPS 96 / 도약 51 / 모두의창업 38) |
| 단위 검증 15항목 | 15/15 통과 (2건 FAIL 은 테스트 offset 오류였고 코드는 정상) |
| `build_refs.py` 재실행 | dangling **215 → 188**, resolved 1,032 → **1,059**, `jo_bare` 6 → **0** |
| `build_precedence.py` 재실행 | 7건 유지. 초격차 원문의 다른 조 파편 제거 |
| `seed_rules.py` 대조 | 🔴 계획의 전제가 틀렸다 — 아래 |
| 문서 정정 | `CLAUDE.md` 파싱 함정 · `RAG.md` §3-1 |

### Stage 0 최종 — 8건 중 8건 통과

| 문서 | 전략 | 조 (전→후) | 최장 (전→후) | 검증 |
|---|---|---|---|---|
| 예비창업 2025 | jo_titled | 35 → 35 | 4,398 → 4,398 | OK |
| 초기창업 2025 | jo_titled | 36 → 36 | 4,293 → 4,293 | OK |
| 재도전 2025 | jo_titled | 35 → 35 | 1,714 → 1,714 | OK |
| 도약 2025 | jo_titled | 47 → 48 | 6,848 → 6,803 | OK |
| 초격차 제10차 | jo_titled | 35 → 35 | 10,100 → 10,100 | OK |
| 모두의창업 | jo_titled | 77 → **103** | 23,728 → **5,615** | OK |
| 창업중심대학 | jo_titled | 42 → **44** | 10,515 → 10,515 | 원문오류 1 |
| TIPS 2026 | **outline_numbered** | 32(역전 7) → **53** | 36,051 → 16,809 | OK |

창업중심대학의 `원문오류:조번호_중복(제35조)` 은 파싱 실패가 아니라 원문 사실이라
V1 과 분리해 센다. 게이트는 통과한다.

### 🔴 계획 §4 의 전제가 틀렸다 — `seed_rules.py` 는 붙임2를 읽지 않는다

"재실행해 `rules` 19행 오염 여부를 확인한다" 는 계획은 성립하지 않는다.
`seed_rules.py` 는 **파일 I/O 가 0줄**이고 19행은 소스에 하드코딩된 손 전사본이다.
재실행해도 값이 바뀌지 않는다. (Postgres 도 미가동이라 실행 자체가 불가했다.)

대신 **정상 분리된 붙임2 원문과 하드코딩 값을 직접 대조**했다. 숫자 6종 전부 일치했고,
대신 다른 것이 나왔다:

🔴 **초기창업 9행의 근거 인용이 전부 틀려 있었다.** 예비·초기 양쪽 다 `제22조` 를 근거로
달았는데 그 조항은 사업마다 번호가 다르다.

```
예비창업 제22조 = 창업기업등 사업비 비목   맞다
초기창업 제22조 = 사업비 관리              무관한 조다
초기창업 제21조 = 창업기업등 사업비 비목   이게 맞다
```

`제21조` 로 고쳤다. 19행(예비 10 / 초기 9) 유지. **DB 미가동이라 적재는 못 했다.**

### 다음 세션이 할 일

**Stage 0 은 닫혔다.** 남은 것은 아래 순서다 (`서비스 아키텍쳐.md` §10 우선순위 기준).

1. **판정층 격리 테스트** (§10-1, 1순위) — 골든셋 50문항에 `근거_원문` 이 100% 있어
   검색 없이 판정층만 잰다. DB·재인덱싱 불필요. 선행물: 출력 스키마 → 프롬프트 ①④ → 평가 하네스
2. `rules` 19행 D단계 사람 검수 — 숫자 6종은 확인됐고 증빙·금지예시·허용예시가 남았다
3. DB 기동 후 적재 (`docker compose` → `seed_rules.py` → `stage0_ingest.py`)

**하지 않기로 유지하는 것** — 표 추출(코드 신설 필요), F 태깅(판정층 테스트가 필요성을 먼저 말한다),
TIPS 자기인용 참조 수집(`build_refs.py` 문형에 없음)

---

## 2026-08-29 (토) · Stage 0 파서 수술 — 다단 조판·인용 오인·refs 결함

F축 태깅(Stage 0.5)을 하려다 **재료가 상해 있어 Stage 0 으로 되돌아왔다.**
조문 본문이 다단 조판으로 섞여 있어 태깅 자체가 불가능했다.
파서 오류 8개를 고쳤고, 그 과정에서 `refs` 의 설계 미완을 발견했다.

상세 근거·좌표 실측은 **`실제 구현.md`** 2026-08-28~29 항목 5개. 여기는 요약과 다음 작업만.

| 시각 | 작업 | 결과 |
|---|---|---|
| — | 창진원 61건 → 판정 대상 8건 선별 | 세부관리기준/운영지침 26 중 현행 8. 나머지는 구판 18·주관기관현황 18·모집공고 8·별지서식 6 |
| — | 조 후보 추출 + 이상 플래그 | 예비 34 / 초기 33. 이상 13·10건 |
| — | **2단 조판 발견** | 조 순서가 `1,2,3,4,9,5,10,11,6,12,7,8…` 로 섞임. 본문이 두 조씩 혼입 |
| — | `pdftext.py` 컬럼 분리 구현 | 거터 x421 검출 → 역전 9→0, 8→0 |
| — | **`refs` 결함 발견** | 엣지 1,136개 중 `src_조번호` 채움 **0개** |
| — | `build_refs.py` 조 단위 스캔 연결 | 1,136 → 1,288 엣지, `src_조번호` **100%** |
| — | `[참고N]` 섹션 인식 추가 | 창업중심대학 최장 조 28,178 → 10,515자 |
| — | 인용부호 접두 추가 | 재도전 역전 1 → 0 |
| — | **4분면(4-up) 배치 발견·처리** | 초격차 제10차. 역전 3→0, 최장 13,965 → 10,100자 |
| — | Stage 0 8건 검증 | **6건 통과** / 창업중심대학 1건 · TIPS 잔존 |

### 고친 오류 12개

| # | 오류 | 출처 | 재발 방지 위치 |
|---|---|---|---|
| 1 | 거터 이상치 1개가 문서 전체를 1단으로 오판 | 이번 세션 | `pdftext.is_two_column()` — median 근처 과반 |
| 2 | 문서 대표 거터 일괄 적용 → 표지 글자 잘림 | 이번 세션 | `pdftext._page_text()` — 페이지별 거터 |
| 3 | `"
"` 리터럴이 파일 기록 시 개행으로 치환 | 이번 세션 | `pdftext.NL = chr(10)` |
| 4 | 인용 필터 과잉 차단 (조 36 → 21) | 이번 세션 | `stage0_articles._is_citation()` — 규범어 접두만 |
| 5 | `「법령명」제N조` 인용을 조로 오인 | 기존 | `RE_CITE_PREFIX` 에 닫는 인용부호 |
| 6 | `RE_ATTACH` 가 `[참고N]` 미인식 → 부칙 컷 연쇄 실패 | 기존 | `RE_ATTACH` 에 `참고` |
| 7 | 4분면 배치 미처리 | 기존 | `pdftext.is_quadrant()` |
| 8 | `refs` 가 `src_조번호` 를 안 채움 | 기존 | `build_refs.py` → `split_articles()` 연결 |

### 🔴 `refs` 결함 — 2단 조판과 무관한 별건이었다

`build_refs.py` 가 **문서 전체 텍스트에 정규식을 돌려** 참조 문자열만 뽑고 있었다.
`src_조번호` 키 자체가 엣지 딕셔너리에 없었다.

- `RAG.md` §4-3 참조 폐포 SQL 의 `WHERE (src_doc_id, src_조번호) IN :진입점집합` 이
  **조인되지 않았다.** 문서 단위로 조인하면 그 문서 참조가 전부 딸려온다
- 중복제거 키가 `(종류, 참조문자열)` 이고 범위가 문서 전체라, 같은 참조가 여러 조에
  나오면 1건만 남았다
- `db/init/01_schema.sql` 은 `src_조번호 TEXT NOT NULL` + `UNIQUE(src_doc_id, src_조번호, 참조문자열)`
  이다. **스키마와 산출물이 처음부터 어긋나 있었다**

원인: `stage0_articles.py` 에 섹션 분리·3단 fallback·V1~V6 검증이 **이미 구현돼 있는데**
`build_refs.py` 가 쓰지 않고 자체 정규식만 돌렸다.

### 문서 정정 필요 — `CLAUDE.md` · `RAG.md` §3-1

둘 다 다단 레이아웃 사례를 **창업중심대학 하나**로 적었고 대응을
*"정규식 앵커를 꼬리에 건다"* 로만 썼다. 실측은 다르다:

- 예비·초기·재도전·창업중심대학·초격차 **5건이 다단**
- 앵커 조정으로는 본문 혼입이 풀리지 않는다. **좌표 기반 컬럼 분리**가 필요하다
- 초격차는 2단이 아니라 **4분면(4-up)** 이다

### Stage 0 현재 상태

| 문서 | 거터 | 4up | 조 | 역전 | 최장 | 판정 |
|---|---|---|---:|---:|---:|---|
| 예비창업 2025 | 421 | | 35 | 0 | 4,398 | OK |
| 초기창업 2025 | 421 | | 36 | 0 | 4,293 | OK |
| 재도전 2025 | 421 | | 35 | 0 | 1,714 | OK |
| 도약 2025 | — | | 47 | 0 | 6,848 | OK |
| 초격차 제10차 | 297 | ✔ | 35 | 0 | 10,100 | OK |
| 모두의창업 | — | | 77 | 0 | 23,728 | OK |
| 창업중심대학 | 421 | | 42 | 1 | 10,515 | 역전 1건 |
| TIPS 2026 | — | | 32 | 7 | 36,051 | 조 체계 아님 |

---

## 🔜 다음 세션이 할 일 (2026-08-30 예정) — Stage 0 마무리

**먼저 읽을 것**: `실제 구현.md` 상단 5개 항목. 좌표 실측·오탐 측정치가 거기 있다.

### 0. 시작 전 확인

```powershell
python scripts/_work/verify_split.py     # 8건 현재 상태 재확인 (약 2분)
```
위 표와 다르면 그 사이 무언가 바뀐 것이다. 원인부터 찾을 것.

### 1. `build_refs.py` 재실행 — **필수. 지금 산출물은 낡았다** (약 6분)

한 번 돌렸으나 그 **뒤에 `stage0_articles.py` 를 3회 수정**했다
(`[참고N]` 인식 / 인용부호 접두 / 4분면). 현재 `_refs.json` 은 그 이전 산출물이다.

```powershell
python scripts/build_refs.py
```

- 비교 기준: `scripts/_work/_refs_before.json` (수정 전 원본, 1,136 엣지 / `src_조번호` 0건)
- 1차 재실행 결과: 48문서 1,288엣지 / resolved 1,032 · shifted 41 · dangling 215 / `src_조번호` 100%
- **이번 재실행은 그보다 엣지가 더 늘 것이다** — `[참고N]` 분리로 조가 늘었기 때문
- 확인할 것: `조분해_전략` 분포. `paragraph` 로 떨어진 문서가 많으면 그 문서들이 조 구조를 잃은 것

### 2. `build_precedence.py` 재실행 (약 3분)

```powershell
python scripts/build_precedence.py
```
- 산출: `2026_Finance_DATA_FOR_RAG/_precedence_rules.json` (기존 7건, `verified=false`)
- **7건이 유지되는지, 원문 문구가 바뀌는지** 확인. `max_pages=8` 이라 제3조 부근만 읽는다
- 7건 목록: 예비·초기·재도전·도약·창업중심대학·모두의창업(L2>L3, `all`) / 초격차·모두의창업(L1>L2, `unspecified_only`)

### 3. 창업중심대학 역전 1건 확인 (약 3분)

`35→34` 하나. 조 42개 중 1건이다. 세 가지 중 하나다:
- 본문 속 인용인데 `_is_citation` 이 못 잡은 형태
- 원문 자체의 조번호 오류
- 아직 남은 조판 문제

```python
# 확인 방법
import sys; sys.path.insert(0,'scripts')
import pdftext
t,_ = pdftext.extract_meta('2026_Finance_DATA_FOR_RAG/창진원/창업중심대학/창업중심대학 세부관리기준2025년 개정.pdf')
i = t.find('제35조'); print(repr(t[i-200:i+120]))
```

### 4. `seed_rules.py` 재실행 — `rules` 19행 오염 확인 (약 10분)

엄밀히는 Stage 1 이지만 **Stage 0 산출물에 직접 의존**한다.
`[붙임2] 비목 해설표` 를 읽어 19행을 만드는데, 그 `[붙임2]` 가 이번에 제대로 분리됐다.

**지금 확인해야 하는 이유**: 19행은 `룰테이블_검수가이드.md` D단계 **사람 검수의 대조 원본**이다.
오염된 채로 검수하면 반나절이 통째로 무의미해진다. 아직 검수를 안 한 게 다행이다.

- 값이 바뀌면 → 이전 19행이 오염됐던 것. `rule_base.md` §4 실측표도 갱신
- 안 바뀌면 → `[붙임2]` 는 원래 온전했던 것. 그대로 D단계 진행 가능

### 5. 문서 정정 (약 5분)

- `CLAUDE.md` 「파싱 함정」 — 다단 사례를 창업중심대학 1건 → **5건**, 대응을 좌표 기반 분리로
- `RAG.md` §3-1 — 동일. 4분면(4-up) 항목 신설

### 하지 않기로 한 것

- **TIPS 별도 파서** — `1.` → `가.` → `1)` 2단계 분할. 약 20분.
  8개 사업 중 1개가 인덱싱 불가로 남지만, 별도 파서 설계라 성격이 다르다.
  Stage 0 을 닫은 뒤 독립 작업으로 잡는다
- **혼재 조 항 단위 분할** — 청킹 단위(조 vs 항) 미결을 앞지른다. `RAG.md` §5 미결 #1

### 그다음 (Stage 0 이 닫히면)

**Stage 0.5 F축 태깅** — 오늘 원래 하려던 것. 계획은 이미 섰다:
- 대상: 예비·초기창업 2건, 창업기업+공통 조문만 (~250조 추정)
- 필드 6개: 적용대상{주관기관|창업기업|공통|**혼재**} / F의존성[] / 요구사실 / 룰후보 / 근거 / 확신도
- **API 가 아니라 세션이 직접 조문을 읽고 태깅한다** (65조 3.8만자는 컨텍스트에 들어온다)
- 검증: 커버리지 / 기존 실측 대조(예비 = 주관기관 10·창업기업 11·공통 12) / 스팟체크 5조 /
  F2 빈도 집계(개인정보 논의 입력) / 혼재 조 목록(청킹 결정 입력)
- F 태깅은 **통계가 아니라 런타임 필드**다 — 검색된 조항들의 F 태그 합집합이
  `(4)-b` 프롬프트에 넣을 F 를 정하고, 없는 F 가 `전제[]` 가 된다 (`Agent.md` §4)

---

## 2026-08-28 (금) · 12:30~13:45 · LLM 슬롯·하네스 설계 확정

같은 날 오전 디렉토리 정리(아래 §12:20)의 연속. 이 세션은 **LLM 을 무엇으로 어디서 돌릴지**를
확정하고 남은 작업을 목록화했다. 결정 본문은 `서비스 아키텍쳐.md` §7 에 반영해야 한다(**미반영**).

| 시각 | 작업 | 결과 |
|---|---|---|
| 13:00 | 대회 요강 실측 (daker.ai API) | 아래 §대회 일정 제약 |
| 13:10 | 실행 환경 실측 (`nvidia-smi` / WMI) | 개인 노트북 GPU = **Intel Arc 내장**, CUDA 없음 → 로컬 추론 불가 |
| 13:20 | LLM 슬롯별 모델·배치 확정 | 아래 §확정 |
| 13:35 | VRAM·비용 산정 | 아래 §자원 산정 |
| 13:45 | 남은 작업 목록화 | 아래 §남은 작업 |
| 14:10 | **GPU 0대 완주 결정** | 아래 §14:10 결정 |

### 대회 일정 제약 — 2026 금융 AI Challenge (주최·주관 금융보안원)

| 항목 | 값 | 파장 |
|---|---|---|
| 산출물 제출 마감 | **2026-09-07** | 기획서 + 기능명세서 + **작동하는 배포 URL** |
| 본선 심사 | 제출물 기반 **내부 비공개 평가 100%** | 🔴 **9/7 이후 측정치는 컷에 반영되지 않는다** |
| 발표 심사 대상 | 상위 **11팀 내외** | 통과해야 후속 실험이 의미를 가진다 |
| 발표 심사 | 2026-10-13 (코드 ZIP + 발표자료 사전 제출) | |
| 평가 기준 | "공모전 주제 적합성, 부적격 여부 평가 등" | **모델 선택은 평가 항목이 아니다** |

→ **LLM 관련 결론은 9/7 전에 나와 있어야 한다.** 초안에서 모델 비교를 9/8 이후로 밀었던 것은 오판이며 정정했다.

### 실행 환경 제약

| 자원 | 상태 | 결론 |
|---|---|---|
| 개인 노트북 | Intel Arc 내장 GPU (CUDA·전용 VRAM 없음) | 로컬 추론 불가 |
| 클라우드 GPU | RunPod 4090 Community $0.34/h | 필요 시 즉시 대체 가능 |

→ 마감 전 LLM 은 **전부 API**.

### 확정 — LLM 슬롯별 모델·배치

| # | 슬롯 | F축 | 배치 | 모델 | 근거 |
|---|---|:-:|---|---|---|
| ① | 정규화 | ✅ | 🔒 로컬 필수 | Qwen3 8B | 사용자 입력이 프롬프트에 들어감 |
| F | 문서 파싱 | ✅ | 🔒 로컬 필수 | Qwen3 8B | 사업계획서·협약서 |
| ④ | 판정·조립 | 규정 원문만 | API 허용 | **Claude Opus 5** | §8.3 "틀린 가능이 최악. 여기서 아끼지 않는다" |
| ⑤ | 문장 생성 | ✕ | 경량 | Claude Haiku 4.5 | 저위험 |

🔑 **Qwen 이 이겨야 하는 슬롯은 ④ 가 아니라 ① 이다.**
④ 는 규정 원문만 들어가 API 가 허용되므로 Qwen 이 판정에서 밀려도 원칙 위반이 아니다.
① 은 F축이 들어가 외부 전송이 막히므로 **여기서만 로컬이 필수**다. ① 은 자연어 → JSON
구조화 추출이라 난이도가 낮아 8B 로 충분할 가능성이 높다.

**Qwen 을 API 로 부르는 안은 기각.** OpenRouter·DashScope 경유는 데이터가 알리바바로 나가므로
Claude 와 동일하게 "외부"다. 데이터 주권 논리는 **가중치가 자체 하드웨어에서 돌 때만** 성립한다.

**Llama 계열 제외.** 한국어 열세(영어 중심 학습) + Llama Community License(MAU 7억 조항).
입력이 전량 한국어 법령·행정규칙이라 구조적으로 불리하고 라이선스도 Apache 2.0 보다 까다롭다.

로컬 후보군은 **Qwen3**(Apache 2.0 · thinking 모드 · 8B/14B/32B 라인업) 주력에
한국어 특화(EXAONE / Kanana) 대조군. 최종 선택은 `LLM.md` §5 원칙대로
**벤치마크가 아니라 골든셋으로** 정한다.

### 자원 산정 (계획치 · 실측 아님)

**추론 VRAM** — vLLM `gpu_memory_utilization=0.9`, 컨텍스트 16K

| 모델 | 정밀도 | 가중치 | KV(16K) | 합계 | 4090 24GB |
|---|---|---:|---:|---:|:-:|
| Qwen3 8B | BF16 | 16.4GB | 2.4GB | ~21GB | ⚠️ 빠듯 |
| Qwen3 8B | AWQ 4bit | 5.5GB | 2.4GB | **~10GB** | ✅ 동시 7건 |
| Qwen3 14B | AWQ 4bit | 9.5GB | 2.6GB | ~14GB | ✅ |
| Qwen3 32B | AWQ 4bit | 19.5GB | 4.2GB | ~26GB | ❌ 2장 필요 |

KV 캐시는 토큰당 **8B 144KB / 14B 160KB / 32B 256KB**. `enable_prefix_caching` 필수 —
자기일관성 투표 N=5 를 병렬로 돌리면 캐시가 5배가 된다.

**학습 VRAM** — QLoRA, batch 1, gradient checkpointing

| 모델 | seq 4K | seq 8K | seq 16K |
|---|---:|---:|---:|
| 8B | ~13GB | ~17GB | ~30GB |
| 14B | ~17GB | ~22GB | ~35GB |
| 32B | ~28GB | ~34GB | 2장 필요 |

⚠️ Qwen 어휘가 151,936 이라 **로짓 메모리가 크다**(seq 8K 에서 5GB). Liger Kernel / Unsloth 의
fused cross-entropy 적용 시 8B/8K 가 17GB → 13GB 로 내려간다. 4090 1장 학습이면 사실상 필수.

**비용** (환율 1,400원 가정)

| 항목 | 구성 | 금액 |
|---|---|---:|
| 개발·평가 | Claude Sonnet 5 + Batch 50% | 약 10만원 |
| Qwen ① 슬롯 측정 | Qwen API (Flash 급 $0.03/$0.13 per M) | 약 3천원 |
| 심사 서빙 5주 | 서버리스 | 수천 원 |
| ⚠️ 상시 GPU 서빙 | 840h × $0.34 | **40만원 — 금지** |

### 남은 작업 — LLM 관련

**마감 전(9/7) 필수**

| 순서 | 항목 | 산출물 | 상태 |
|:-:|---|---|:-:|
| 1 | **출력 스키마** — 4-way 판정 + 인용 조항 + 전제[] JSON 계약 | — | ⬜ 미설계 |
| 2 | **프롬프트 ①·④** | — | ⬜ 미설계 |
| 3 | **평가 하네스** — 문항→프롬프트 조립→호출→파싱→채점 (어댑터로 모델 교체) | — | ⬜ |
| 4 | **검색 결과 스냅샷** — 75문항 1회 실행 후 JSON 고정 (§9) | — | ⬜ |
| 5 | 골든셋 검수 33문항 `verified=false → true` | `_골든셋/_골든셋_초안.json` | ⬜ |
| 6 | Claude baseline 측정 | 판정 정확도 · 치명 오답률 | ⬜ |
| 7 | **Qwen3 8B ① 정규화 슬롯 측정** (API, 로컬 아님) | 필드 일치율 % | ⬜ |
| 8 | LLMAdapter · 판정 오케스트레이터 | — | ⬜ |
| 9 | 인용 검증기 — 문장번호 → DB 원문 치환, 불일치 시 등급 강등 | — | ⬜ |

🔴 **1~4 가 전부의 병목이다.** 없으면 모델을 어디에 올리든 잴 수 없다.

### 파인튜닝 — 지위 정리

| 항목 | 내용 |
|---|---|
| 지위 | **조건부.** 아키텍처 §7 사다리 5단계 — 1~4단계 후 격차가 남을 때만 |
| 표적 | 규정 지식 ✕ → **판정 캘리브레이션** ✓ (확신 없으면 판단불가로 보내도록 신뢰도 교정) |
| 금지 | 기관별 수치(20%/50%) 학습 → 다른 기관에서 조용히 틀린다 |
| 방법 | QLoRA 4bit · Qwen3 8B · seq 8K · 약 17GB · GPU 1장 |
| 데이터 | 골든셋 + 부정행위 사례집 42건 기반, 수백 건 규모 |
| 선행 조건 | 골든셋 `verified=true` — **현재 50문항 전부 false** |
| 소요 | 2주 → **9/7 전 불가** |
| 이번 대회 | ❌ **미수행 확정.** 시간(2주) · 데이터(골든셋 미검수) · GPU(미확보) 3중 불가 |

🔴 **문서 충돌.** `CLAUDE.md` §절대 재논의하지 않는 것 은 파인튜닝을 **기각** 목록에 넣었으나,
`서비스 아키텍쳐.md` §7 은 캘리브레이션 표적으로 **재개방**했다. 문서 우선순위상 아키텍처가 이긴다.
→ `CLAUDE.md` 갱신 필요.

### 14:10 · 결정 — GPU 0대로 대회를 완주한다

자체 GPU 확보 경로가 없고, 본선 심사가 **9/7 제출물 100%** 이므로 9/8 이후 측정치는
심사에 반영되지 않는다. 9/8 이후로 모델 비교·QLoRA 를 잡는 것은 **이미 제출한 시스템과
다른 결과를 설명하게 되므로** 성립하지 않는다. 따라서 GPU 전제 계획은 전부 접는다.

| 작업 | GPU | 수단 | 비용 |
|---|:-:|---|---:|
| 스키마·프롬프트·하네스·골든셋 검수 | ❌ | 코드 작업 | 0 |
| Claude baseline 측정 | ❌ | API | 약 10만원 |
| **Qwen3 ① 정규화 슬롯 측정** | ❌ | **API** (로컬 아님) | 약 3천원 |
| 심사 기간 서빙 | ❌ | 서버리스 + HF Spaces / Neon | 수천 원 |

🔑 **Qwen 능력 측정과 로컬 배치는 분리된다.** 8B 가 ① 슬롯을 감당하는지는 API 로 재도 같은
숫자가 나오고, "Apache 2.0 공개 가중치 · AWQ 4bit 10GB → 단일 GPU 배치 가능" 은 계산으로
성립한다. 로컬에서 직접 돌려야만 증명되는 것이 아니다.

### 문서 반영

| 대상 | 내용 | 상태 |
|---|---|:-:|
| `서비스 아키텍쳐.md` §7 | 슬롯별 모델 확정값 · "Qwen 의 본진은 ①" · 마감 시점 로컬 환경 부재 | ✅ 14:15 반영 |
| `CLAUDE.md` | 파인튜닝 "기각" → "조건부(캘리브레이션 표적)" 정정 | ✅ 14:15 반영 |
| `서비스 아키텍쳐.md` §10 | 모델 비교 시점 (본선 심사가 제출물 100%) | ⬜ 미반영 |

### 🔎 외부 검토 요청 항목

다른 모델에 아키텍처 검토를 맡길 때 **먼저 볼 것**. 각 항목은 현재 입장과 그 근거가 문서에 있다.

| # | 쟁점 | 현재 입장 | 검토 포인트 |
|:-:|---|---|---|
| 1 | ① 정규화만 로컬이면 충분한가 | ④ 는 규정 원문만 들어가므로 API 허용 | ④ 프롬프트에 F축이 새어들 경로가 정말 없는가 |
| 2 | 판정 1건 = LLM 2회 고정 | 유지 | ④-a 스크리닝 도입 시 3회 (§11 #6·#7 미결) |
| 3 | function calling 미개방 | 유지 | 재현성·호출수 고정 대 표현력의 트레이드오프 |
| 4 | 투표 앙상블이 프론티어 1회를 이기는가 | 가설 단계 | 검증 설계가 타당한가 (검색 스냅샷 고정) |
| 5 | 치명 오답률 1순위 · 판단불가율 균형 | 유지 | 임계를 **결과 보기 전에** 정했는가 |
| 6 | 파인튜닝 표적 = 캘리브레이션 | 조건부 | 수백 건으로 캘리브레이션이 실제로 학습되는가 |
| 7 | 골든셋 `verified=false` | 미해결 | 이 상태의 숫자를 baseline 으로 쓰면 안 된다 |
| 8 | L3 업로드 문서의 프롬프트 인젝션 | 데이터로만 취급 | ① 정규화·④ 판정 프롬프트에 방어가 설계돼 있는가 |

---

## 2026-08-28 (금) · 00:00~00:30

전날 밤 작업의 연속. 자정을 넘겨 이어진 분량이라 날짜만 갈라 적는다.

| 시각 | 작업 | 결과 |
|---|---|---|
| 00:02 | 스캔 전용 PDF 전수 조사 | `_scan_inventory.json` — 226건 중 **45건이 스캔전용, A등급은 2건뿐** |
| 00:06 | 참조 그래프 재실행 (문자중복 해소 반영) | `_refs.json` — 1,136 엣지 / dangling **320 → 181** |
| 00:09 | `확인_가이드.md` 완성도표 갱신 | 신규 산출물 4종 반영 |
| 00:11 | (타 에이전트) `서비스 아키텍쳐.md` 작성 | 창업팀 전용 전환 등 **맥락 대전환** — 아래 §맥락 변화 |
| 00:30 | `작업 현황.md` 신설 | 이 문서 |
| 12:20 | **디렉토리 정리** | 루트 파일 12 → 0. `archive/` 신설, 코드 `scripts/` 통합 |

### 12:20 · 디렉토리 정리

루트에 흩어져 있던 코드·구 데이터·구 문서를 정리했다.

| 이동 | 대상 | 사유 |
|---|---|---|
| → `scripts/` | `Law_Crawling.py` · `Crawling.py` · `hwp_extract.py` | 코드는 한곳에. 6개 스크립트가 `Law_Crawling` 을 import 한다 |
| → `archive/구_데이터/` | `manifest.json`(+`.bak`) · `kised_자료실_제목목록.csv` · 기존 `_폐기/` 전체 | 동결·중복·역할 종료 |
| → `archive/구_문서/` | `다운로드_URL_전체목록.md` · `법령데이터_수집설계.md` · `데이터셋_활용정리.md` | 수집 완료 / 현황 문서로 승계 / 구 계층 기준 |
| 삭제 | `__pycache__/` | 빌드 산출물 |

**경로 수정 (안 고치면 6개 스크립트가 깨진다)**

```
scripts/*.py    sys.path.insert(... parent.parent) → parent      6개 파일
Law_Crawling.py ROOT = __file__.parent → parent.parent           데이터 경로 보존
문서            python Law_Crawling.py → python scripts/Law_Crawling.py
```

**검증** — 31개 스크립트 구문 OK · `Law_Crawling` import 성공(ROOT·MD_PATH 정상) ·
`hwp_extract` import 성공(`scan_tables.py` 가 쓴다) · `build_precedence.py` 실행 결과 동일(7건).

`archive/README.md` 에 **항목별 폐기 사유**와 인덱서 경로 블랙리스트를 적어뒀다.

---

## 2026-08-27 (목) · 22:30~24:00

### 요약

문서 최신화로 시작해 **데이터 파이프라인의 실측 함정 4개**를 잡고, 미완 항목 7개를 처리했다.
가장 큰 소득은 **판정 최상위 근거 문서가 지금까지 파싱되지 않고 있었다**는 발견이다.

### 🔴 최우선 발견 — 통합관리지침 제14차가 파싱되지 않고 있었다

| 항목 | 내용 |
|---|---|
| 증상 | `extract_text()` 가 `"제제5조조(전전문문기기관관)"` — **모든 한글이 2배** |
| 결과 | `제\d+조` 정규식 매치 **0개**. 조 0개로 **조용히** 파싱 실패 |
| 원인 | PDF 에 같은 글자가 두 겹으로 겹친 **문자중복 레이어** |
| 해결 | `pdfplumber` 의 `dedupe_chars()` → 제14차에서 조 **86개** 정상 추출 |
| 재발 방지 | `scripts/pdftext.py` 신설. 중복 자동 감지 후 해소. **`pdfplumber.extract_text()` 직접 호출 금지** |

**같은 증상 문서 3건이 하필 전부 핵심이었다.**

| 문서 | 정체 |
|---|---|
| 통합관리지침 제14차 | **판정 최상위 근거** |
| 초기창업패키지 주요질의응답집 별첨4 | **골든셋 정답지** |
| 창업도약패키지 세부관리기준(2022년) | 구판 |

> `구현.md` §8-1 의 *"제14차 PDF 텍스트 추출 정상 — 2026-08-12 실측"* 이 이 현상을 놓친 것이었다.
> 해당 항목을 정정하고 `CLAUDE.md` 에 「파싱 함정」 절을 신설했다.

부수 효과로 **§2.5 실측표가 실물 검증**됐다 — 제33조는 회의비, 제39조가 기계장치·공구·기구.
"구판 조번호를 그대로 따라가면 노트북 질문에 회의비 조항이 나온다" 가 사실로 확인.

### 🔴 두 번째 발견 — L2 > L3 는 예외가 아니라 규칙이다

기존 문서는 *"재도전성공패키지 제3조는 L2가 L3보다 우선"* 을 **한 사업의 예외**처럼 서술했다.
8개 사업 전수 확인 결과 **7개 중 6개**가 제3조(적용범위)에서 동일하게 명시한다:

> *"주관기관의 내부규정과 상충되는 경우 **본 관리기준을 우선한다**"*
> — 예비 · 초기 · 재도전 · 도약 · 창업중심대학 · 모두의창업

| 유형 | 사업 | 효력 |
|---|---|---|
| `all` | 위 6개 | **L2 > L3** — L3 가 더 엄격해도 진다 |
| `unspecified_only` | 초격차 · 모두의창업 | **L1 > L2**, 미규정 사항만 L2 |
| 없음 | TIPS | R&D 라 주관기관이 아니라 운영사 체계 |

**"L3(사용자 문서) 최우선" 으로 설계했으면 6개 사업에서 판정이 뒤집혔다.**
`CLAUDE.md` · `프로젝트설명.md` §2-3 · `구현.md` §1-7 전부 정정.

### 처리한 미완 항목 7개

| # | 항목 | 결과 | 산출물 |
|---|---|---|---|
| 1 | 행정규칙 별표 HWP 변환 | ✅ **103/103** | `_hwp변환/법령 PDF/L1_법령/별표/` |
| 2 | 부정행위 사례집 주관기관 편 판독 | ✅ 30 → **42건** | `사례집/_부정행위사례집_OCR.json` |
| 3 | 우선순위 조항 추출 | ✅ **7건** (`verified=false`) | `_precedence_rules.json` |
| 4 | 참조 그래프 구축 | ✅ **1,136 엣지** | `_refs.json` |
| 5 | 창진원 참조 법령 재도출 | ✅ 인용 엣지 410건으로 대체 | `_refs.json` |
| 6 | 골든셋 문항 작성 | ⚠️ **50문항 초안** (`verified=false`) | `_골든셋/_골든셋_초안.json` |
| 7 | 스캔 문서 처리 | ✅ 전수 조사 + A등급 판독 | `_scan_inventory.json` |

#### 1. 별표 103건 — 변환 과정에서 함정 2개

```
1차 시도 : 성공 48 / 실패 72
  실패 원인 ① 확장자가 .hwp 인데 실제로는 HWPX      67건
             한컴 COM 에 포맷을 틀리게 주면 Open 이 예외 없이 조용히 False
  실패 원인 ② 다운로드 실패로 섞인 HTML 오류페이지    5건
             크기 검사(< 512바이트)만으로는 통과해 버림
2차 시도 : 매직바이트 판별 적용 → 98/103
재수집   : 5건 전부 정상 수신 (일시적 실패였음)
최종     : 103/103
```

재발 방지 — `scripts/convert_hwp.py::sniff()` 매직바이트 판별,
`scripts/fetch_admrul_appendix.py` 다운로드 검증 강화. `CLAUDE.md` API 함정 12·13 추가.

> **재수집한 5건의 정체**: 법령 본문이 아니라 별표·서식이다.
> 4건은 「국가연구개발사업 연구개발비 사용 기준」의 학생인건비 서식, 1건은 기재부 보조사업 총괄표.
> **판정 가치는 낮다.** 진짜 값어치 있는 「보조사업비 카드 사용제한 업종」은 1차에서 확보됐다.
> 소득은 "103/103 완결" 이라는 상태값과 **재사용 가능한 함정 2개**다.

#### 2. 부정행위 사례집 — 주관기관 편 12건 추가

인쇄 33~43쪽(PDF 31~41쪽)을 VLM 판독. 인력 2 · 평가및심의 7 · 사업비 3.

**주관기관 제재는 창업기업과 체계가 다르다.**

```
창업기업  : 참여제한 (3~7년) · 사업비 전액 환수 · 수사의뢰
주관기관  : 주의 → 경고(3회 시 협약해지) → 성과평가 감점(1~3점)
```

사업비 오집행 금액 임계값도 확보 — **1백만원 미만 단순착오** → 환수+제도개선 /
**5백만원 이상 부적절 집행** → 환수+경고조치.

#### 4. 참조 그래프 — dangling 절반으로

```
              1차      2차(수정 후)
엣지        1,087  →   1,136
resolved      735  →     920
shifted        32  →      35
dangling      320  →     181   ← 절반 이하
```

2차 개선 요인 — ① `요령`·`관리기준` 해소 로직 추가 ② 문자중복 해소가 통합관리지침에 적용.

**`shifted` 가 핵심 검증이다.** 2025년판 세부관리기준이 쓰는 `"지침 제33조"`(제12차 표기)를
조제목 매칭으로 현행 제14차 **제36조**로 자동 보정했다. 참조 그래프가 작동한다는 증거.

잔여 dangling 181건은 대부분 R&D 계열(과학기술기본법·출연연법)로 **판정 인덱스 밖이라 정상**.

#### 6. 골든셋 50문항 — ⚠️ 아키텍처 전환으로 절반이 범위 밖

| 대상 | 문항 | 근거 | 상태 |
|---|---:|---|---|
| **창업기업** | **33** | 통합관리지침 제14차 제36~47조 | ✅ 유효 |
| 주관기관 | 16 | 예비창업 세부관리기준 제14~20조 | 🔴 **창업팀 전용 전환으로 범위 밖** |
| 공통 | 1 | 여비 등급 | ✅ 유효 |

창업기업 문항 예시 — 실제 판정 시나리오다:

```
"디자이너 맥북 250만원"     → 조건부 (제39조② 직접관련성 소명 + 사전검토)
"사무실 책상·의자"          → 불가   (제39조② 집기·가구 금지)
"MS Office"                 → 조건부 (제39조④ 범용SW 원칙 금지 + 사전검토)
"대표 본인 인건비"           → 불가   (제41조① 현물만 계상)
"배우자 인건비"              → 불가   (제41조⑥)
"특허 성공보수"              → 불가   (제40조② 대행수수료는 가능)
"주관기관에 안 알리고 결제"   → 불가   (제47조③)  ← 반려 2위 원인
```

**전부 `verified=false` 다.** 근거 조문은 원문 그대로지만 판정(가능/조건부/불가)에 해석이 들어갔다.
특히 `조건부` 17건은 "사전승인만 받으면 되는지" vs "아예 불가인지" 를 사람이 봐야 한다.

#### 7. 스캔 문서 — 전수 조사로 문제 규모가 확정됐다

226건 중 스캔전용 45건. **그런데 A등급은 2건뿐이었다.**

| 등급 | 건수 | 처리 |
|---|---:|---|
| A (판정 근거) | **2** | 재도전 2026판 → 판독 완료 / 나머지 1건은 이름이 "삭제" 인 폐지 별표 |
| B (참고) | 1 | 부정행위 사례집 → 판독 완료 |
| 기타 | 42 | PMS 매뉴얼 — 인덱싱 대상 아님 |

재도전성공패키지 2026년 11차(2026.04.23) 1~6쪽을 판독해 **룰 21건** 확보.
멘토링비 1인 1일 30만원·재창업자별 500만원, 사무실임차료 업종 조건, 기장대행 월 20만원,
중고거래 개인 간 금지, 회의비 참석인원당 5만원, 창업기업 사업비 구성(정부 75%/현금 5%/현물 20%) 등.

**단 출처를 스키마에 박았다:**

```sql
extraction TEXT CHECK (extraction IN ('native','dedupe','hancom','vlm'))
```

`vlm` 이면 **A등급 인용 금지 + 경고 강제.** 손으로 옮긴 텍스트는 원문 문자열 일치 검증이
성립하지 않으므로, 그 사실을 코드가 알게 만들었다 (원칙 4).

### 문서 최신화 (22:30~23:10)

작업 시작 전 4개 문서의 최신성을 점검하고 정정했다.

| 문서 | 조치 |
|---|---|
| `데이터셋_활용정리.md` | **폐기** → `archive/구_문서/`. 구 L1~L5 계층 기준이라 레이어 정의가 전면 불일치 |
| `구현.md` | 계층 L4/L5 → L1/L2/L3 통일 · §8-1 무효 항목 분리 · §8-4 프롬프트 교체 · **§8-5 현재 구현 상태 신설** · 스택 FAISS→pgvector 정정 |
| `확인_가이드.md` | 최상단 🔴 경고(수치가 구 코퍼스 기준) · 폴더구조표 교체 · 완성도표를 "코드/산출물" 2칸으로 분리 |
| `프로젝트설명.md` | OCR·HWP 완료 표기 · 파일맵 실제 트리로 교체 · 270→267 정정 |
| `Rag_Agent구현 파이프라인.md` | **§3.3 충돌 해소 알고리즘 재작성**(순수 엄격성 병합 → 우선순위 조항 우선) · DB `layer` CHECK 에서 L4·L5 제거 · `precedence_rules`·`refs` 테이블 신설 |

### 신규 파일

```
scripts/pdftext.py            문자중복 자동 해소 — 모든 PDF 읽기의 관문
scripts/build_refs.py         참조 그래프 추출
scripts/build_precedence.py   우선순위 조항 추출
scripts/scan_inventory.py     스캔 전용 문서 전수 조사

2026_Finance_DATA_FOR_RAG/_refs.json                    1,136 엣지
2026_Finance_DATA_FOR_RAG/_precedence_rules.json        7건
2026_Finance_DATA_FOR_RAG/_scan_inventory.json          45건
2026_Finance_DATA_FOR_RAG/_골든셋/_골든셋_초안.json      50문항
2026_Finance_DATA_FOR_RAG/창진원/재도전성공패키지/_2026년11차_VLM판독.json   룰 21건
```

### 확정된 함정 (전부 코드·문서에 박음)

| # | 함정 | 방어 위치 |
|---|---|---|
| 12 | `admbyl` 별표는 확장자가 `.hwp` 인데 실제 HWPX 가 67/103 | `convert_hwp.py::sniff()` |
| 13 | 다운로드 실패가 HTML 오류페이지로 위장 (크기 검사 통과) | `fetch_admrul_appendix.py` |
| 14 | 다단 레이아웃이 단어 **사이**로 다른 컬럼을 밀어넣음 | `build_precedence.py` 꼬리 앵커 |
| 15 | **문자중복 레이어** — 조 0개로 조용히 실패 | `pdftext.py` (신설) |

---

## 맥락 변화 기록

일자별 작업과 별개로, **프로젝트 전제가 바뀐 시점**만 여기 모은다.

### 2026-08-28 00:11 — 창업팀 전용 전환 (`서비스 아키텍쳐.md`)

타 에이전트가 작성한 아키텍처 문서가 **기존 문서 4곳과 정면 충돌**한다.
아키텍처 문서 스스로 *"`Rag_Agent구현 파이프라인.md`(08-13 판)와 충돌하면 이 문서가 우선"* 이라 선언.

| # | 기존 | 신규 | 영향받는 문서 |
|---|---|---|---|
| 1 | 센터가 고객, 창업팀은 무료 사용자 | **창업팀 전용**, 센터 화면 A1~A6 제외 | `프로젝트설명.md` §2-1·§4-2, 수익모델 |
| 2 | 로그인 없이 링크로 즉시 사용 | **로그인 도입** | `프로젝트설명.md` §4-1 |
| 3 | 창업팀 문서 수집 ✕ (개인정보 방어선) | **F축 신설**, 창업팀이 입력 | `구현.md` §7 스코프 방어 |
| 4 | 판정 1건 = **LLM 2회** (§8-2 재논의 금지) | **LLM 3단계** (④-a 스크리닝) | `구현.md` §8-2 — 아키텍처 §11 미결 #7 이 충돌을 인지 중 |

**신설된 개념**

- **적용대상 축** — 모든 조문에 `주관기관 | 창업기업 | 공통` 태깅. 실측상 인덱싱 대상의 절반이
  창업팀에 무관하다(예비창업 세부관리기준: 주관기관 102 vs 창업기업 65)
- **사실 축 F1~F7** — 규정 축(대전제)과 사실 축(소전제)을 분리. 포섭 구조에 대응
- **전제[] / `unmapped_premise`** — LLM 은 필드를 발명하지 않고 결핍만 지적. 코드가 3갈래로 해소
- **문장번호 인용(S08)** — 원문 복사 대신 번호 선택 → 문자열 불일치가 구조적으로 불가능
- **KCL 벤치마크** — 법률 추론 능력 외부 검증

**어제 작업과의 접점**

| 내 작업 | 아키텍처 |
|---|---|
| `_refs.json` 참조 그래프 | Stage 0.7 로 이미 설계돼 있었음. **테이블명이 다르다** — 나 `refs` / 아키텍처 `xref_graph` → 통일 필요 |
| 별표 103 변환 | §7-2 에 전수 분류 결과 반영됨. **"판정에 쓸 수 있는 건 4건뿐"** |
| 재도전 2026 판독 | §11 미결 #2 "OCR 작업 중" → **완료로 갱신 필요** |
| 골든셋 50문항 | §9 는 30문항 계획. 다만 **주관기관 16문항이 범위 밖이 됨** |
| L2 > L3 전수 확인 | 아키텍처에 미반영 → 추가 필요 |
| 문자중복 함정 | Stage 0 에 미반영 → 추가 필요 |

---

## 다음 작업 (아키텍처 §10 우선순위 기준)

```
 0. 🔴 커밋            — 8/20~24 작업 309파일 미커밋. 법령 XML 500MB → LFS 검토
 1. 🔴 적용대상 태깅    — scripts/build_apply_target.py 신설 (아래 상세)
 2.    F 의존성 태깅
 3.    룰 테이블 사업별 × 기관유형별 분리
 4.    청킹 단위 확정 (조 vs 항)          ← 재인덱싱이 여기 걸려 있다
 5.    신규 코퍼스 재인덱싱
 6.    baseline 재측정
 7.    적대적 세트 구축 (부정행위 사례집 42건의 sanction 필드)
 8.    골든셋 검수 (50문항 초안 → verified)
 9.    BM25 + RRF
```

**병목은 골든셋 검수와 커밋이다.** 둘 다 사람 손이 필요하다.

---

### ⬜ 다음에 만들 것 — `scripts/build_apply_target.py` (적용대상 태깅)

**무엇** — 각 조문에 `적용대상 ∈ {주관기관, 창업기업, 공통}` 을 부여한다.
검색 시 `WHERE 적용대상 IN ('창업기업','공통')` 으로 항상 필터한다.

**왜 1순위인가** — 창업팀 전용 전환(2026-08-28)으로 **인덱스의 약 1/3이 무관해졌다.**
필터가 없으면 창업팀의 "노트북 사도 되나요?" 에 주관기관 전담인력 인건비 조항이 검색된다.
**Recall 을 아무리 올려도 무의미하다.**

**언제 하나 — 지금 가능. 임베딩·청킹과 무관하다.**

```
Stage 0   파싱
Stage 0.5 태깅  ← 여기. 임베딩 이전이어야 한다
Stage 0.7 참조 그래프
Stage 2   청킹 → 임베딩 → 인덱싱
```

- 임베딩은 "문장 → 벡터", 태깅은 "조각에 라벨" 이라 **서로 독립**이다
- 다만 순서상 태깅이 먼저다 — 청크 적재 시 필드가 채워져 있어야 필터가 걸린다
- **청킹 단위(조 vs 항) 결정을 기다릴 필요 없다.** 조 단위로 태깅하면 항으로 쪼개도 상속된다
- 🔑 재인덱싱을 아직 안 한 게 다행이다. 태깅 없이 임베딩부터 돌렸으면 다시 해야 했다

#### 🔑 LLM 이 거의 필요 없다 — 문서가 스스로 선언한다

아키텍처 §8 은 *"Stage 0.5 태깅 (LLM 1회 + 사람 검수)"* 로 잡았으나, 실측 결과
**절(節) 헤딩이 적용대상을 이미 명시**하고 있다.

```
제5장 사업비의 구성
  <제1절 주관기관>        ← 이하 조는 전부 주관기관
     제14조(주관기관 사업비 구성) · 제17조(인건비) …
  <제2절 창업기업등>       ← 이하는 전부 창업기업
     제21조(창업기업등 사업비 구성) · 제22조(창업기업등 사업비 비목) …
  <제3절 공통 사항>
```

**절 파싱만으로 배정된다.** 프로토타입 실측(2026-08-28):

| 문서 | 조 | 주관기관 | 창업기업 | 공통 |
|---|---:|---:|---:|---:|
| 통합관리지침 제14차 | 81 | 17 | 25 | 39 |
| 예비창업 세부관리기준 2025 | 33 | 10 | 11 | 12 |
| 초기창업 세부관리기준 2025 | 32 | 10 | 12 | 10 |
| 재도전 세부관리기준 2025 | 37 | 2 | 28 | 7 |

→ **LLM 은 절 밖 조문과 애매한 건만** 처리하면 된다. 아키텍처 예상보다 비용이 훨씬 낮다.

#### ⚠️ 구현 시 주의 3가지

| # | 함정 | 대응 |
|---|---|---|
| 1 | **TIPS 는 절이 없다** — 조 체계 자체가 아님(`1.` → `가.` → `1)`) | 별도 처리 경로 필요 |
| 2 | **조 번호 비단조가 섞인다** — 예비창업 33개 조인데 `제65조` 가 잡힘. 다단 레이아웃이 다른 페이지를 끌어온 것 | 태깅 전 필터링. `pdftext.py` 경유는 기본 |
| 3 | **조 안에서 대상이 갈리는 경우** — `제18조 ① 일반수용비는… ② 주관기관 소속 인력 수당은…` | 절 단위로 대부분 커버되나 이런 건 사람 검수 |

#### 산출물

```
scripts/build_apply_target.py  →  2026_Finance_DATA_FOR_RAG/_apply_target.json
   { doc_id, 조번호, 조제목, 적용대상, 근거(절 헤딩 | LLM | 사람), verified }
db/init/01_schema.sql          →  chunks.적용대상 컬럼 + 인덱스
```

**예상 소요**: 절 파싱 기반이라 8개 사업 + 지침 전체가 한 시간 규모.
`verified=false` 로 두고 §3-4 검수 대상에 올린다.
