# 중앙(ai-c4) 최종 상태 — 2026-09-04

오너 부재 중 진행. 관측(실측)과 다음 판을 나눠 적는다.

## 오늘 끝난 것

**QA 준비 (배포·검증 완료)**
- 403 블로커: 로그인은 됐고 명부에 이메일이 없어서였다. QA 계정 2건 로컬·운영 넣음.
- 신원·저장: 비특권 롤로 쓰기→새 연결 되읽기 확인.
- 판정 캐시 DB 이전 · L3 파싱 GUC 수정 · 파싱품질 노출 · 데모데이터(협약금·계획4·할일).
- 프론트: 건국대→경상국립대(별칭 번들 건국대 0건). PR#2 종결.
- 배포 v4~v7 + 계약·캐시 수정. 배포마다 컨테이너 파일 md5 == 커밋 확인.
- 리허설 게이트 6종 통과.

**완전 무인 GPU (4관문 실측)**
1. 팟 열면 vLLM 자동기동 — pre_start 훅, boot.log 확증, 2회 재현.
2. stop→start 후 재자동기동 — 캐시 재사용 240초.
3. 키 적재 후 실판정 도달 — 제어가능=true, 실판정 122초.
4. 15분 유휴 자동정지 발동 — 유휴 899초→991초 중지(검사주기 설계대로).
- 볼륨 캐시가 팟이 아니라 볼륨 단위로 유효. torch.compile 109초→31초.
- 팟 5ofl00dpzidchp 중지 상태 유지. 요청 오면 자동 wake. delete 안 함(무인).

**골든셋·룰 재검토 (ai-c5 팀, PR#27 머지)**
- eval.golden_set 113→315. corpus.rules 5행 수정(틀린 불가 제거). RE-18 오너 판정 불가.
- docs 10벌 낡은 수치 정정.
- 판정 품질 발견: 「불가=0」은 어휘 부재가 아니라 금지적중 즉답+매칭 실패였다.

## 오늘 나온 실결함 (전부 수정·배포)
- 판정 저장이 비특권 롤에서 조용히 실패 — 새 커넥션에 GUC 미설정. v5.
- GPU status 가 죽은 팟을 가동으로 표시 — health 체크 UA 누락(Cloudflare 403). v6.
- 계약 테스트 3건 8.5시간 빨간불 — 교차 가드 안 돌림. 커밋 전 전체 테스트 누락(내 실수).
- 판정 캐시가 룰 칸 수정을 못 잡음 — 개수만 셌다. 내용 해시로 수정. v7.

## 🔴 다음 판 표적
- **참고3 코퍼스 재파싱** (scripts/pdftext.py). 세로 표라벨이 본문에 섞여 rule 458 실판정이 틀린 불가로 났다. 파싱 결함이 판정을 틀리게 한 첫 실측.
- reap Cloud Scheduler 이관 (지금은 워치독 스레드 — Cloud Run 인스턴스와 함께 죽는다).
- 팟 주소 DB화 (VLLM_URL 지금 env 수동) · .gitattributes 로 .sh LF 고정.
- eval run 재측정 (분모 315, 기존 93과 비교 금지) · 실서버 L40 정식 p50.
- 문서 문체 정리 119벌 (레인별, ai-af 목록).
- audit_db.py 골든셋 미검수 기대값 (0→202).

## 지금 비용·안전
- GPU 팟 중지(과금 0, 볼륨 스토리지만) · 잔액 약 $18 · 감시(ai-a7) tripwire 무인 조건.
- main = 60c3649. 프론트 = PR#2. 골든셋 = PR#27.
