# QA 실행 시트 (2026-09-04) — 6시간 판

레인 상세 지시서는 **`scratchpad/QA_레인_프롬프트_0904.md`** 다. 이 파일은 «누가 무엇을 언제» 다.

## 확정 사실 (0904 실측 — 재측정 없이 뒤집지 마라)

```
Supabase   prototype@ssudo.kr / 123456   ✅ 토큰 발급 확인 (ES256 · aud=authenticated)
           test@ssudo.kr                 확인됨 · 마지막 로그인 09-03 04:02
           pooler  aws-0-ap-northeast-2.pooler.supabase.com:5432
           user    postgres.ntjzekolrrrgkcacbpdx  · 비번은 db/.env SUPABASE_DB_PASSWORD
           🔴 prototype 은 팀원이 쓰는 계정이다. 비밀번호를 바꾸지 마라

🔴 블로커 — 원인이 끝까지 닫혔다
   실서버 GET /api/profile · /api/plans · /api/tasks (위 토큰 지참)
     → 403 {"detail": "등록되지 않은 계정이다"}
   기전: Supabase 로그인은 성공 → 토큰 정상 → 그런데 tenant.accounts 에
         prototype@ssudo.kr 이 없어 auth._계정조회() 가 org 를 못 찾는다.
         accounts 의 유일한 1행은 demo@suddoe.local → 건국대학교(오늘 드랍할 기관)
   처방: INSERT 두 줄. 코드 변경 없음. 오너 결정 없음
         org_id = cfeba091-251a-5ae4-8cc9-88c6e6679440
                 (경상국립대학교 창업중심대학사업단 · tenant.orgs 에 실재)
         🔴 로컬과 운영 Cloud SQL «양쪽» 에 넣는다. 로컬만 넣으면 QA 당일 그대로 403 이다
         🔴 운영 Cloud SQL(34.64.37.177) 은 인증된 네트워크가 아니라 타임아웃이다 —
            `gcloud sql connect` 로 IP 를 올려야 붙는다 (아직 아무도 안 함)

배포     Cloud Run 이미지 api:v3 (09-03 22:00) · a70c874(정규화 2000) 미포함
         SUDDOE_GPU_IDLE_MIN=0 → 워치독 꺼짐 · VLLM_URL 은 404 인 죽은 팟
프론트   apiBase 는 이미 실서버 · CORS 이미 설정됨 · 「건국대학교」 11군데 하드코딩
지연     p50 20.2초 = 판정LLM 15.4(76%) + 정규화 3.1(16%) + 나머지 0.3
GPU      A40 $0.83/h · 잔액 $9.15 + 충전 $10 = 약 23 팟-시간
레포     백엔드 Kimgundo58/2026-Finance-AI-Competition · 프론트 wori1206/…_Frontend (둘 다 push 가능)
```

## 오늘 6시간에 «드는 것 / 안 드는 것»

GPU run 1벌 = 67분이고 직렬이다. 세션을 늘려도 안 줄어든다.

| 든다 | 안 든다 (다음 판) |
|---|---|
| Q1 신원·저장 · Q2 GPU 무인 · Q4 속도 · Q5 API·캐시·L3 · Q6 프론트·QA문서 | Q3 재측정 run · 정답셋 확장 |
| 양 레포 배포 + 리허설 | F1~F4 (py 정리·리팩토링·md 전면 최신화) |

**오늘의 목표는 하나다 — 「팀원이 만질 수 있는 제품」을 세우고 양쪽 다 배포한다.**

## 세션 배치

| 세션 | 레인 | 지시서 |
|---|---|---|
| 1 | **Q1** 신원·저장 | 프롬프트 파일 «## Q1» |
| 2 | **Q2** GPU 무인 | «## Q2» |
| 3 | **Q4** 판정 속도 | «## Q4» |
| 4 | **Q5** API·캐시·L3·데모데이터 | «## Q5» |
| 5 | **Q6** 프론트 PR·QA 문서 | «## Q6» |
| — | **중앙** | 아래 «중앙 프롬프트» |
| — | **감시** | 아래 «감시 세션 프롬프트» |

🔴 Q3 는 오늘 안 연다. 판정 품질 재측정은 GPU 직렬 시간이 6시간을 넘긴다.
   Q6 이 만드는 「알려진 것 목록」이 오늘 그 자리를 대신한다.

## 시간표 (6시간)

```
0:00-0:30  중앙 Q0  → 레인 5개 동시 착수 (계획 보고 대기하지 않고 조사부터)
0:30-1:00  중앙이 계획 5건 승인 · 레인 등록
1:00-4:00  집행. 🔴 Q1 은 1시간 안에 「403 이 사라졌다」를 내야 한다 — 나머지가 그 위에 선다
4:00-5:00  PR 5건 검수 → 머지 → 백엔드 v4 빌드·배포 · 프론트 배포
5:00-6:00  중앙 리허설(팀원 역할 완주) → QA 개시 판정
```

---

## 중앙 프롬프트 (붙여넣기)

```
너는 「써도돼요」 QA 직전 판의 중앙 세션이다. 오너가 6시간 병렬 판을 열었다.
먼저 이 둘을 순서대로 읽어라. 이 대화에는 배경이 없다 — 파일이 유일한 진실이다.
  1) scratchpad/QA_실행_0904.md      ← 확정 사실 · 배치 · 시간표
  2) scratchpad/QA_레인_프롬프트_0904.md  ← 레인별 지시서 전문
그 다음 CLAUDE.md · docs/0-3_초록이_가린다.md 를 읽어라.

네 일은 집행이 아니라 «판단·검수·통합·배포» 다.

Q0 (지금 30분 안에):
1. 🔴 최우선 — tenant.accounts 에 prototype@ssudo.kr → 경상국립대 창업중심대학사업단
   (cfeba091-251a-5ae4-8cc9-88c6e6679440) 매핑을 넣는다. 로컬과 «운영 Cloud SQL 양쪽».
   운영은 `gcloud sql connect suddoe-db --user=postgres` 로 IP 를 올려야 붙는다.
   끝나면 실서버 GET /api/profile 에 Bearer 토큰을 실어 403 이 사라졌는지 확인한다.
   (토큰: Supabase /auth/v1/token?grant_type=password · prototype@ssudo.kr / 123456
    apikey 는 프론트 번들의 anon 키. 실행 시트에 절차가 있다)
   → 이게 Q1 보다 먼저인 이유: 다섯 레인 전부가 이 위에 선다. 30분이면 닫힌다
2. 배포 이미지 api:v3 가 91fd456(SAVEPOINT)을 담았는지 «컨테이너 안 파일» 로 확정한다.
   시각 대조는 근거가 아니다
3. 미커밋 scripts/normalize_run.py 를 pytest 태우고 커밋한다 (a70c874 후속)
4. 프론트 레포를 ../2026-Finance-AI-Competition_Frontend 로 클론한다 (본 트리 안에 넣지 마라)
5. 레인 5개의 session_id 를 받아 .claude/_lanes.json 을 채운다 (owns 는 접두사 매칭)

집행 중:
- 워커의 자가검증을 그대로 받지 않는다. 🔴 워커가 낸 명령을 «네가 다시 쳐서» 재현한다
- PR diff 를 읽을 때 주석에 박힌 함정(SAVEPOINT · autocommit · 목/실 키집합 · 이름 가림)이
  지워졌는지 본다. 그 주석이 이 코드베이스에서 제일 비싼 자산이다
- git 쓰기·전체 pytest·PR·머지·배포는 전부 너만 한다
- 🔴 머지했는데 안 올렸으면 안 끝난 것이다. 배포 후 «바뀐 내용이 실제로 응답·번들에 있는지»
  확인한다. 리비전 이름과 200 만 보면 09-03 을 반복한다

마지막:
- 팀원 역할로 리허설 1회 완주 → 실행 시트의 게이트 6개로 QA 개시 여부를 판정한다
- 오늘 못 한 것(Q3·F1~F4)을 docs/9_미결.md 에 «왜 안 했나»와 함께 남긴다
```

---

## 감시 세션 프롬프트 (붙여넣기)

```
너는 감시 세션이다. 코드를 고치지 않는다. 파일을 쓰지 않는다. git 을 치지 않는다.
네 유일한 일은 «중앙이 틀리는 것을 잡는 것» 이다.

읽을 것: scratchpad/QA_실행_0904.md · CLAUDE.md · docs/0-3_초록이_가린다.md
        · docs/0-1_판정_정확도.md 「지표를 읽을 때」

30분마다 중앙에 SendMessage 로 물어라. 답이 아래 중 하나에 걸리면 «멈추라»고 말해라.

■ 이 프로젝트가 실제로 틀렸던 방식 — 같은 모양이 또 나오는지 본다
1. 「통과했다 / 초록이다 / 200 이다 / 예외가 안 났다」를 «맞다»의 근거로 쓰는가
   → 저장은 «새 트랜잭션에서 되읽었나»를 되물어라
2. 목(mock)으로 돈 검사를 실경로의 증거로 쓰는가
3. 로컬 postgres(superuser·bypassrls)로 잰 것을 「운영에서도 된다」로 쓰는가
4. 조건이 다른 두 수치를 빼는가 (문항수·분모·코퍼스버전·룰버전·롤·GPU 가 다른 run)
5. 판단불가율을 «모델선택 / 실패경로»로 안 가르고 한 숫자로 세는가
6. 문서에 적힌 「막혔다 / 없다」를 재측정 없이 물려 쓰는가
   → 0904 에만 이걸로 네 번 뒤집혔다 (Supabase 계정 · 프론트 API 주소 · CORS · accounts 행수)
7. 관측과 추론이 같은 문단에 같은 어조로 섞여 있는가
8. 🔴 «마이너한 방향» 으로 새는가 — 지연 p50 은 판정LLM 76% + 정규화 16% 다.
   비목확정·검색·DB왕복·병렬화에 시간을 쓰고 있으면 그건 0.3초짜리 작업이다. 멈춰라
9. 리팩토링·문서 정리(F 단계)를 오늘 시작하는가 → 오늘 범위가 아니다
10. 머지하고 배포를 안 했는가 → 「안 끝난 것」이다

■ 보고
- 걸린 게 없으면 「30분 경과 · 걸린 것 없음 · 지금 무엇이 돌고 있나」 한 줄
- 걸리면 «어느 항목 · 중앙의 어느 문장 · 무엇을 되물어야 하나» 를 오너와 중앙 양쪽에 보낸다
- 🔴 네가 옳다고 확신해도 코드를 고치지 마라. 지적만 한다
```

---

## 🔴 오너가 할 것 (지금 · 이거 없으면 판이 안 선다)

| | 무엇 | 왜 |
|---|---|---|
| 1 | **RunPod $10 충전** | 잔액 $9.15 = 11시간. Q2·Q4 가 8~12 팟-시간을 쓴다. 중간에 멈춘다 |
| 2 | **세션 6개 연다** — 워커 5 · 감시 1 | 배치표대로. 각 세션에 「공통 헌장 + 레인 한 줄」을 붙여넣는다 |
| 3 | **각 세션의 이름을 중앙에 알린다** | 워커끼리는 서로를 모른다. 중앙이 명단을 만들어 뿌려야 한다 |
| 4 | **`docs/기록/` 건국대 2건 처리 방침** | 소급 수정 금지 원칙과 충돌한다. 「배너만」인지 「지워라」인지 한 마디 |
| 5 | (QA 시작 전) **팀원용 계정 수** | 지금 쓸 수 있는 건 prototype·test 둘이다. 더 필요하면 말해라 — 내가 만든다 |

**오늘 안 해도 되는 것**: 목 서버 재배포(#21 — QA 경로에서 무관해졌다) ·
Supabase 비밀번호 로테이트(08-31 부터 미결이나 QA 뒤가 맞다)
