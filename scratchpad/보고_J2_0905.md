# 보고 — J2 (계측 사각지대 + 허용경로 주입) · 2026-09-05

> ✅ 2026-09-05 19:35 — 워크트리 분리 완료. `금융 AI공모전-J2` (브랜치
> `work/판정-J2-0905`, 3134ce5 에서 분기)에서 커밋 2개로 갈라 완료했다.
> ```
> 커밋 A  23a40c3  J2① — 판정·정규화 메타 계측 (프롬프트 불변 · 기준선 run 대상)
> 커밋 B  e1f6bf8  J2② — 허용경로 B0 주입 (V0 변경 · 다음 실험 run 대상)
> ```
> 아래는 정지 이전 시점의 원기록이다 — 실제 최종 상태는 위 커밋 diff 를 본다.

## ① 계측 사각지대 — `orchestrate.py` (판정 4메타)

**바꾼 줄**: `orchestrate.py` 모델 메타 조립 안, "사고흔적있음/길이" 바로 뒤에 2줄 추가.
```python
"추론content있음": 메타4.get("추론content있음"),
"추론content길이": 메타4.get("추론content길이"),
```

**확인 방법**: 추측으로 키 이름을 박지 않았다 — `normalize_run.py:123`
(`추론content = _메시지.get("reasoning_content") or ""`)이 이미 이 필드를
실측해 쓰고 있었고, 그 메타(`메타1`·`메타4`)에 `추론content있음`/`추론content길이`가
이미 F1 레인(2026-09-05)에서 실려 오는데 `orchestrate.py`의 `모델={...}` 화이트리스트가
버리고 있었다(사고흔적있음/길이 는 이미 있었는데 이 형제 필드만 빠져 있었다). 실제로
불려서 나온 값을 grep 으로 먼저 확인한 뒤 옮겼다 — vLLM 을 새로 호출해 키를 추측하지
않았다.

**판정 영향**: 0. 값만 기록에 실린다. 프롬프트 바이트 불변 — 기준선 run 에 들어가도 된다.

**중앙 추가요청(수신, 미착수)**: 정규화(①) 메타도 같은 방식으로 확장하라는 요청을
ai-04 로부터 받았다 — `메타1` 에서 `토큰`·`종료이유`·`사고흔적있음/길이`·
`추론content있음/길이`를 뽑아 `모델.정규화`가 문자열 하나(`메타1.get("모델")`)가 아니라
dict 를 갖게 한다. **미착수** — 기존 `"정규화"` 키를 읽는 소비처(`eval_e2e.py`·
`docs/0-1*` 집계)가 뭘 기대하는지 먼저 grep 해서 값 타입을 안 깨는 형태로 넓혀야 한다.
새 트리에서 이어간다.

## ② 허용경로 주입 — `llm_validate.py` + `assemble_context.py`

**바꾼 것**:
- `llm_validate.f_경로집합(dsn=)` → `f_경로집합(cur=None, *, dsn=None)`. `cur` 를 주면
  그 커서로, 없으면(자가검사·CLI) `dsn` 으로 새 연결. 기존 호출부 2곳
  (`검증()` 내부 폴백, `--f-paths` CLI)은 위치인자 충돌 안 나게 키워드로 고쳤다.
- `assemble_context.py`: B0 "원칙 넷"→"다섯", `_원칙5(경로집합)` 신설 — F축 경로 21개를
  축별로 묶어 "전제.매핑은 이 목록에서만 고른다" 문장을 만든다. `조립()` 에
  `허용경로=` 키워드 추가 — 안 주면 `f_경로집합(cur)` 로 **내부에서 자동 계산**한다.

**목록 실측 (하드코딩 아님, DB 라이브 조회)**:
```
F1(협약) 6      F1.과업범위요약 · F1.사업명 · F1.자기부담.현금 · F1.정부지원.현금
                · F1.협약시작일 · F1.협약종료일
F2(사업계획서) 1  F2.과업범위요약
F3(집행내역) 6   F3.거래처 · F3.귀속월 · F3.금액 · F3.비목 · F3.인력역할 · F3.재원
F4(인력) 5      F4.겸직 · F4.고용형태 · F4.소속기관유형 · F4.역할 · F4.타사업참여율
F5(거래처자격) 3 F5.구매명의 · F5.전직임직원 · F5.친족
합계 21 — 인계 문서의 숫자와 일치.
```

**확인 방법**: `python scripts/assemble_context.py --gold-id 330` 으로 조립 전문을
직접 뽑아 B0 끝에 원칙5 가 실제로 붙는지 육안 확인. `python scripts/llm_validate.py
--self-test` (DB 불필요) 전건 통과 확인. `f_경로집합(cur)` 를 라이브 DB(읽기 전용
SELECT) 에 직접 태워 21개·그룹별 개수가 위 표와 맞는지 확인 — 모두 실측, 추측 없음.

**🔴 V0 변경 고지 (재확인)**: 이 hunk 는 B0 프롬프트 바이트를 바꾼다. `조립()` 이
`허용경로` 를 안 받아도 **내부 폴백이 항상 발동**하므로, `assemble_context.py` 가
트리에 있는 한 `orchestrate.py` 를 하나도 안 고쳐도 프롬프트가 바뀐다 — 이번 run 과
이전 run 이 프롬프트로 갈린다(`설정.b0_sha1` 로 구분 가능).

## orchestrate.py 안에서 ①/②가 섞인 자리 (분리 필요)

`git diff scripts/orchestrate.py` 기준 3개 hunk:
1. **②** — (4) 조립 직전 `허용경로 = f_경로집합(cur)` 계산 + `조립(...)` 에
   `허용경로=허용경로` 전달. **최적화일 뿐 필수 아님** — 없어도 assemble_context.py
   가 알아서 계산한다(위 참고). 순수 "판정 1건당 DB 왕복 2회→1회" 절감용.
2. **②** — 검증() 호출에 `f경로=허용경로` 1줄. 1번 hunk 의 변수를 쓰므로 짝이다.
3. **①** — 모델 메타에 추론content있음/길이 2줄. 위 절 참고. 유일하게 기준선에
   들어가도 되는 hunk.

즉 **①만 기준선에 태우려면 orchestrate.py 에서 위 3번 hunk만 남기고 1·2번은
llm_validate.py·assemble_context.py 와 함께 다음 실험 run 으로 미룬다.**

## 다음 세션에서 할 일 (인계)
- [ ] `work/판정-J2-0905` (또는 지정 경로) 워크트리에서 세션 재개 → 3파일 diff 재적용
- [ ] orchestrate.py 는 위 3번 hunk 만 남기고 1·2번은 되돌린 뒤(또는 애초에 안 실은
      새 patch 로) 커밋 분리
- [ ] 정규화(①) 메타 확장 착수 — 소비처 grep 먼저
- [ ] `python scripts/llm_validate.py --self-test` · `assemble_context.py --gold-id`
      스모크로 재확인 후 보고
