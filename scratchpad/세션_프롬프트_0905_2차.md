# 세션 프롬프트 — 2026-09-05 2차 (오너 1 + 팀원 4)

> `origin/main` = `299ee13` 기준. 문서 최신화는 끝났고 여기서부터는 **코드**다.
>
> ```
> 오너   세션 1개    LLM 판정 flow — 타임아웃 수정 → 기준선 재측정
> 팀원   중앙 1 + 병렬 3   그 외 백엔드 — A(F축) · B(dangling) · D(프론트 갭)
> ```

---

# 【A】 오너 세션 — LLM 판정 flow

> 새 세션 하나. 아래를 그대로 붙여넣는다.

```
너는 「써도돼요」(창업지원금 지출 사전승인 판정) 리포의 **오너 세션**이다.
담당은 «LLM 판정 flow» 하나다. 그 외 백엔드는 팀원이 별도 브랜치에서 한다.

## 지금 상태
main = 299ee13 · pytest 306 passed · 3 xfailed · 실패 0 · GPU 닫힘 · 잔액 $13.32
정답셋 전량 run: run_id 194 · 295문항 · 일치율 44.7% · 다수결 기준선 55.9% · 치명 1
🔴 295 중 96건(32.5%)이 «타임아웃으로 판정을 못 받았다». 44.7% 는 판정 성능이 아니다.

읽을 것 (순서대로)
  docs/0-1_판정_정확도.md          run 194 실측 · 실패 96건 분해 · 혼동 · 강등코드
  docs/0-1c_검색_적중.md           청크 길이 절벽 · 구조적 미도달 60건
  scratchpad/다음_작업_인계_0905.md 우선순위와 오너 결정 대기 항목
  scratchpad/판정결과기록_0905.md   원본 실측 (수치를 인용하기 전에 여기를 본다)

## 네 파일 (이것만 고친다)
scripts/orchestrate.py · assemble_context.py · llm_validate.py · llm_schema.py
normalize_run.py · retrieve.py · rule_lookup.py · adapter.py · eval_e2e.py · eval_store.py
공유(팀원에게 알리고 고친다): CLAUDE.md · docs/0_현황.md · docs/9_미결.md
🔴 server/** · db/init/** 은 팀원 것이다. 손대지 마라.
🔴 DB 스키마는 «읽기만» 한다 — 팀원이 A(F축)·B(dangling)에서 테이블을 신설하고 있다.

## 1순위 — 타임아웃 수정 후 기준선 재측정   [약 2~2.5시간 · $3.5~4]

원인은 확정됐다 (eval.run_items.원출력.강등사유 전수):
  실패단계 정규화 74   TimeoutError 71 · RemoteDisconnected 2 · getaddrinfo 1
  실패단계 판정LLM 22  HTTP 524(Cloudflare) 19 · getaddrinfo 2 · RemoteDisconnected 1
vLLM 은 무죄다 — preemption 0 · 대기열 0 · abort 0. 클라이언트와 프록시가 먼저 포기했다.

 ⑴ 클라이언트 타임아웃 상향   adapter.py(기본 180, 재시도 «없음») · normalize_run.py
 ⑵ 🔴 HTTP 524 는 상향으로 «못» 푼다 — RunPod 프록시(Cloudflare)의 고정 한계다.
     동시성을 낮추거나 SSH 터널로 프록시를 우회해야 한다
 ⑶ 동시 6 → 4
 ⑷ 전량 295 재측정 = «진짜 기준선»
🔴 동시성을 «올려» 시간을 줄이려던 계획은 폐기했다. 올리면 타임아웃이 더 난다.

🔴 «한 번에 한 변수만» 바꿔서 잰다. 타임아웃 수정과 프롬프트 수정을 같은 run 에
   섞으면 어느 쪽이 움직였는지 못 가른다. 버그 수정 run 과 실험 run 을 갈라라.

## 2순위 — 문안은 확정, 적용 안 된 코드 수정 2건
 ① orchestrate.py:797-817 모델 메타에 «추론content있음/길이» 2줄
     └ 사고(reasoning) 토큰이 계측 사각지대다. 「사고흔적있음=False」는 «생각을 안 한다»
       가 아니라 «reasoning_content 로 갈라져 나가서 그 함수가 못 본다» 였다
 ② llm_validate.f_경로집합(cur=) + assemble_context.py B0 「원칙 5」(허용경로 21개)
     └ 🔴 «허용경로»(F축 매핑 21개)가 프롬프트에 «전혀 안 나간다» — 확인됨.
       assemble_context.py 에 허용경로·f_경로집합 참조 0건. 모델이 유효 경로를 모른 채
       전제를 채운다 → PREMISE_UNMAPPED 58건
     └ F1 협약 6 · F2 사업계획서 1 · F3 집행내역 6 · F4 인력 5 · F5 거래처자격 3
     └ 🔴 이건 «V0 자체» 를 바꾼다. 이후 run 은 오늘 run 과 프롬프트가 다르다
       (`설정.b0_sha1` 이 이미 박히므로 구분은 된다)

## 3순위 — 검색   4순위 — 프롬프트
 · P2 포인터형 38건: 진입점 조가 순수 포인터라 어떤 임베딩으로도 안 뜬다(도달 1/38).
   처방은 임베딩 튜닝이 «아니라» 인덱싱 설계다
 · 청크 1,500자+ 절벽(22.2%). 🔴 재청킹은 골든셋 재고정 비용이 크다 — 1,000자+ 로 내리면
   166문항(52.7%) 재고정이라 사실상 탈락. 표적을 «정답청크가 아닌 경쟁 청크» 로 좁혀라
 · V4(B0 경고 제거)는 우선순위 «내림» — 판단불가 75건 중 74건이 근거✗ 라 효과가 낮고
   근거 없이 답하게 만들어 치명 오답을 늘릴 위험이 있다

## 🔴 하지 마라 — 실측으로 기각된 것
모델 교체(Claude·GPT) · prefix 캐시 튜닝(TTFT 2.41초가 상한) · H100 전환(손익분기 2.01배
vs 기대 1.6~2.0배) · 동시성 상향 · 폐포 OFF 로 지연 단축(병목이 prefill 이 아니다) ·
강등코드를 판정에 배선(정답/오답 선별력 없음) · 파인튜닝 먼저

## 오너 결정이 필요한 것 (내가 정해줄 때까지 진행하지 마라)
 ① 골든셋 P1 22건 재고정 — 제안표는 scratchpad/골든셋검토_G1_구조적미스.md 에 완성돼 있다
 ①-b P2 38건 — ⑴진입점 키워드 보강 ⑵참고·붙임 승격 ⑶분모에서 제외 셋 중
 ② gold 429 정답 「판단불가」가 오류로 보인다(확실). 430·431·432 는 의심
 ③ 게이트 C 평가 방법 — 골든셋에 「선택필요」 0건이라 발동하면 무조건 오답.
    🔴 docs/9_미결.md #7 을 이 run 으로 닫지 마라 — 잴 수 없는 것을 근거로 고치는 셈이다
 ④ V7 신설 여부 — 사전등록 6개를 사후에 늘리는 문제

## GPU
`.claude/skills/runpod_session` 을 따른다. 🔴 **작업이 끝나면 묻지 말고 즉시 닫는다.**
🔴 팟 대장 경로가 두 군데로 갈려 있다(runpod_pod.py 는 scripts/archive/.claude/, guard_gpu.py
   는 루트 .claude/) — 팀원이 E1 에서 고치는 중이다. 그때까진 닫은 뒤 `ls` 로 실물 확인해라.

## 함정 — 여기서 여섯 번 틀렸다
🔴 «잴 수 없는 것» 을 «값이 0» 으로 읽지 마라 (게이트C · 사고토큰 · 트레이스)
🔴 계측이 «어느 층» 을 보는지 확인해라 (vLLM preemption 0 ≠ 제때 끝냈다)
🔴 작은 표본의 패턴을 원인으로 확정하지 마라 (4~8건에서 본 것이 12~28건에서 전부 뒤집혔다)
🔴 조건이 다른 run 끼리 일치율을 빼지 마라 (run 191 65.6% vs run 194 44.7%)
```

---

# 【B】 팀원 — 중앙 1 + 병렬 3

## B-0. 공통 머리말 — 병렬 3개 앞에 반드시 붙인다

```
너는 「써도돼요」(창업지원금 지출 사전승인 판정) 리포의 백엔드 병렬 세션이다.
중앙 세션이 검수·커밋한다. 아래 제약은 예외 없다.

  · 리포 루트 `LLM제외_백엔드_구현_작업_TASK.md` 를 «먼저» 읽는다. 그게 이 작업의 정본이다
  · §2 「건드리면 안 되는 파일」(scripts/ 아래 판정 파일 10개)을 절대 고치지 마라 —
    오너가 같은 기간에 그 파일들로 판정 실험을 돌린다. 한 글자만 바뀌어도 실험이 무효다
  · main 에 직접 커밋·머지 금지. 커밋은 중앙만 한다
  · GPU 팟을 열지 마라. RunPod 명령 금지
  · 네 레인 칸의 파일만 건드린다 (아래 소유권 표)
  · 보고서는 scratchpad/보고_<레인>_0905.md

문서 규칙: 한 문서 100줄 상한 · 관측과 추론을 «시각적으로» 갈라 쓴다 ·
「어떻게 확인했나」를 근거 칸에 남긴다 · 확인 못 한 것은 「미확인」으로 남긴다.
🔴 「테스트가 통과했다」는 «안 죽는다» 의 증거지 «맞다» 의 증거가 아니다.
   MOCK=1 로 통과해도 MOCK=0 으로 한 번 더 봐라 — 2026-09-03 에 목은 `인용` 을,
   실 경로는 `인용목록` 을 돌려줘서 테스트가 전부 초록인 채 실서버 판정 전수가
   근거 없이 나갔다. 검사할 것은 «통과 여부» 가 아니라 «키 집합 일치» 다.

기동:
  git checkout -b work/백엔드-0905 origin/main
  pip install -r requirements.txt && docker compose up -d db
  python -m server.main --selftest && python -m pytest -q
```

## B-1. 파일 소유권 — 어기면 서로 덮어쓴다

| 레인 | 파일 |
|---|---|
| **P1** F축 | `server/main.py` (프로필 구역: `:837-851` · `:1239-1253` · `:1397-1410`) · 협약서 파싱 신규 모듈 |
| **P2** L3 dangling | `server/routes_l3.py` · `scripts/l3_parse.py` · `db/init/**` 의 dangling 테이블 |
| **P3** 프론트 갭 + 잡음 | `server/routes_tasks.py` · `server/main.py` (`/api/vocab` `:627-638` **만**) · `scripts/archive/cli/runpod_pod.py` · `.claude/hooks/guard_gpu.py` |
| 중앙 | `git` 전부 · `pytest` · `docs/9-2` · 최종 조립 |
| 🔴 아무도 안 건드림 | `scripts/orchestrate.py` · `assemble_context.py` · `llm_validate.py` · `llm_schema.py` · `normalize_run.py` · `retrieve.py` · `rule_lookup.py` · `adapter.py` · `eval_e2e.py` · `eval_store.py` |

🔴 **`server/main.py` 를 P1 과 P3 가 같이 만진다.** 구역이 완전히 다르지만(프로필 vs vocab),
충돌을 피하려면 **P1 을 먼저 머지하고 P3 를 리베이스**한다. 중앙이 순서를 지킨다.

## B-2. 중앙 세션 프롬프트

```
너는 백엔드 라운드의 **중앙 세션**이다. 직접 구현하지 말고 감독·검수·커밋만 한다.
리포 루트 `LLM제외_백엔드_구현_작업_TASK.md` 가 이 라운드의 정본이다. 먼저 읽어라.

브랜치 work/백엔드-0905 (origin/main = 299ee13 에서 딴다). main 직접 머지 금지.
오너는 별도 브랜치에서 «LLM 판정 flow» 를 고친다 — TASK.md §2 의 파일 10개가 그것이다.

병렬 3개가 동시에 돈다.
 P1  A. F축 데이터 계층      f_profile UPSERT · f_exec/f_personnel 배선 ·
                             협약서 파싱 통로 신설 · selftest 강화
 P2  B. L3 dangling 상세     테이블 신설 + l3_parse 채우기 + 응답 반영
 P3  D. 프론트 갭 + E. 잡음  화면13 집행일정 · vocab 세목 · accept 정리 ·
                             팟 대장 경로 · cp949

네가 하는 것:
 1. 🔴 **`server/main.py` 를 P1 과 P3 가 같이 만진다.** 구역은 다르다(프로필 vs vocab).
    P1 을 «먼저» 머지하고 P3 를 리베이스시켜라. 순서를 안 지키면 충돌한다
 2. 각 레인 보고가 오면 «근거 칸을 직접 검산» 한다. 숫자를 인용한 줄은 명령을 다시 돌려라.
    🔴 「개수가 맞다」로 통과시키지 마라 — 개수 검산은 통과하는데 내용이 틀린다
 3. 🔴 **DB 스키마 변경이 나오면 즉시 오너에게 알린다.** P1·P2 가 테이블을 신설한다.
    오너의 판정 파이프라인이 같은 DB 를 읽는다
 4. pytest + `python -m server.main --selftest` 를 «MOCK=0 으로도» 돌린다.
    기준선: 306 passed · 3 xfailed · 실패 0 (2026-09-05 main)
 5. `docs/9-2_백엔드_프론트_미결.md` 를 이 라운드 결과로 갱신한다 (네 담당)
 6. 커밋. 🔴 **메시지에 실제 포함 파일만 적는다** — 2026-09-05 에 메시지가 부풀려져
    문서 수정 3벌이 「들어간 줄 알고」 소실됐다. `git show --stat` 목록을 그대로 보고해라
 7. PR 본문에 **DB 스키마 변경 여부를 반드시 적는다**

🔴 스코프를 넓히지 마라. C(인증/IDOR) · F(GPU 자동화) · 화면12 AI CHAT · 판정 정확도는
   이번 라운드 «제외» 다. 레인이 그리로 새면 반려해라.
```

## B-3. P1 — A. F축 데이터 계층

```
<공통 머리말>

레인 P1. **F축 데이터 계층을 정상화한다.** TASK.md §4 의 A1~A4 가 네 것이다.

A1  server/main.py:1250 `_실_프로필_저장()` 이 스텁이다 — `{"저장": False, "이유":
    "f_profile 쓰기 경로 미배선 (tenant 소유는 E 세션)"}` 만 돌려준다. 그 E 세션은 끝났다.
    → tenant.f_profile 실제 UPSERT (org_id · 사업명 · 협약시작일 · 협약종료일 ·
      정부지원_현금 · 자기부담_현금)
    → RLS/GUC 패턴은 server/routes_plans.py 의 `_질의_저장` / `_계획_주인` 을 그대로 따른다.
      🔴 새로 발명하지 마라 — 그쪽이 이미 GUC 를 세우고 트랜잭션을 닫는 순서를 잡아 놨다

A2  tenant.f_exec(F3) · tenant.f_personnel(F4) 가 server/ 전체에서 참조 «0건» 이다.
    server/main.py:1246 `_실_프로필()` 이 `"f3": [], "f4": []` 로 하드코딩한다.
    스키마는 db/init/01_schema.sql:424·439 에 이미 있고 RLS 정책도 :515·517 에 있다.
    → 빈 배열을 걷어내고 실쿼리를 붙인다. 쓰기 경로도 A1 과 같은 자리에서

A3  협약서 파싱 통로가 «없다». scripts/adapter.py:81 의 "F문서파싱" 슬롯은 선언만 있고
    호출부가 코드베이스 어디에도 없다(:392 는 슬롯 목록 순회일 뿐).
    → 협약서 업로드 → «분리 적재»: 특약조항은 L3(tenant.l3_articles),
      협약기간·금액은 F1(tenant.f_profile)
    → 🔴 엔드포인트 본체는 server/ 에 만든다. **scripts/adapter.py 를 수정하지 마라** —
      오너 소유다. 슬롯의 규약을 «읽어서» 맞춰 호출만 한다
    → 설계 근거: docs/부록/도메인_배경_전문.md:147-149

A4  server/main.py:1405 selftest 가 PUT /api/profile 의 status_code == 200 «만» 본다.
    "저장" 필드는 안 본다.
    → `저장 == True` 로 강화. **A1 과 같은 커밋에서 한다.**
      안 하면 「그린인데 저장 안 되는」 상태가 그대로 재발한다

검증
 · PUT /api/profile → 저장==True · 재조회로 값이 실제로 들어갔는지 확인
 · 🔴 MOCK=0 으로 확인해라. MOCK=1 은 목 응답이라 저장 경로를 안 탄다
 · pytest 기준선 306 passed · 3 xfailed · 실패 0

🔴 DB 스키마를 바꾸면 «즉시» 중앙에 알린다. 오너의 판정 파이프라인이 같은 DB 를 읽는다.
보고: scratchpad/보고_P1_0905.md
```

## B-4. P2 — B. L3 dangling 상세

```
<공통 머리말>

레인 P2. **끊긴 참조(dangling)의 «상세» 를 저장하고 응답에 싣는다.** TASK.md §4 의 B1.

지금 상태 (실측 2026-09-05)
 · server/routes_l3.py:299 주석이 그대로 말한다 — "dangling 상세(조·참조·사유)는
   저장할 테이블이 아직 없다 — 자리만 두고 비워 둔다"
 · 그래서 :361 응답의 `dangling=[]` 이 «항상» 빈 배열이다
 · 건수만 tenant.l3_documents.dangling수 에 있다 (scripts/l3_parse.py:78 이 UPDATE)
 · 세는 자리는 scripts/l3_parse.py:132 — "사업비 관련 장만(l3_load 와 같은 기준)"

할 일
 ⑴ 상세 테이블 신설 (db/init/ 아래). 최소 (doc_id · 조 · 참조문자열 · 사유)
 ⑵ scripts/l3_parse.py:132 의 카운트 자리에서 «세면서 같이 적재» 한다.
     🔴 두 번 순회하지 마라 — 카운트와 상세가 어긋나면 그게 다음 버그다
 ⑶ GET /api/l3/{doc_id} 응답에 실제로 싣는다 (routes_l3.py:361)

🔴 **끊긴 참조는 판정 시점이 아니라 «업로드 시점» 에 알린다** (CLAUDE.md). 이 항목의
   존재 이유다. 판정 쪽에 뭘 붙이려 하지 마라.

🔴 사유를 «분류» 해라. 「못 찾았다」 한 종류로 뭉치면 다음 사람이 또 판다.
   최소한 갈라야 할 것: 조 지정 없는 인용(설계상 확장 안 함) / 조 지정 있는데 대상 없음 /
   조번호는 있는데 구판이라 안 맞음(조 제목 재매칭 실패). CLAUDE.md 「RAG 의 축」 참조.

검증
 · dangling 이 있는 L3 문서를 하나 올려 상세가 실제로 나오는지
 · 🔴 `dangling수`(기존 집계) 와 신설 테이블 행수가 «일치» 하는지 — 안 맞으면 ⑵ 가 틀렸다
 · pytest 기준선 306 passed · 3 xfailed · 실패 0

🔴 DB 스키마를 바꾸므로 «즉시» 중앙에 알린다.
보고: scratchpad/보고_P2_0905.md
```

## B-5. P3 — D. 프론트 갭 + E. 잡음

```
<공통 머리말>

레인 P3. **프론트가 앞서 있고 서버가 못 받는 자리를 메운다.** TASK.md §4 의 D1~D4 · E1~E2.
🔴 화면 12(AI CHAT)는 «제외» 다. 손대지 마라.
🔴 server/main.py 는 `/api/vocab`(:627-638) 구역 «만» 만진다. 프로필 구역은 P1 것이다.
   P1 이 먼저 머지되니 중앙이 리베이스를 시킬 것이다.

D1  화면13 집행일정 — 지금 프론트가 localStorage(checkumait-clean-schedules-v2)에만 저장한다.
    🔴 **새 테이블부터 만들지 마라.** server/routes_tasks.py 에 plan_tasks 가 이미 있고
    due_date 와 `일정만` 필터(:195)까지 있다. 그걸로 덮이는지 «먼저 대조» 하고,
    모자란 필드만 더한다. 덮인다면 이 항목은 「프론트를 서버에 붙이는 일」이지
    「백엔드를 만드는 일」이 아니다 — 그 결론도 결론이니 보고에 적어라

D2  화면9 지급수수료 — /api/vocab(server/main.py:627)이 비목 10종만 준다.
    프론트는 지급수수료 «하위 16종» 배열을 기대한다.
    🔴 **`비목` 10종 자체를 늘리지 마라** — guided_json_enum 이 그 10종에 묶여 있어
    판정 스키마가 깨진다(오너 영역). **하위 세목은 «별도 키» 로** 내보낸다.
    별칭은 이미 corpus.item_vocab.별칭 에서 나가고 있으니 그 구조를 먼저 봐라

D3  화면4 업로드 accept — 프론트가 .doc/.docx 까지 받는데 서버는 415 로 거부한다
    (server/routes_l3.py:34 `허용_확장자 = {"pdf","hwpx","hwp"}` · :129).
    🔴 **서버가 맞다** — DOC/DOCX 파서가 없고 만들지 않는다(CLAUDE.md).
    프론트 accept 정리 또는 안내 메시지 쪽으로 푼다. 서버는 그대로 둔다

D4  화면7 근거 렌더링 — 🔴 **백엔드는 이미 다 준다**(server/main.py:1203 이 `인용`/`인용목록`
    양쪽을 받는다). 프론트 저장소는 이 repo 에 «없다».
    → 「구현」이 아니라 「프론트에서 실제로 그려지는지 «확인»」이다.
      확인만 하고 결과를 보고에 적어라. 안 그려지면 프론트 쪽 이슈로 넘긴다

E1  팟 대장 경로 통일 — scripts/archive/cli/runpod_pod.py 는
    scripts/archive/.claude/_runpod_open.json 에 쓰고, .claude/hooks/guard_gpu.py 는
    루트 .claude/_runpod_open.json 을 읽는다. 열 때 훅이 못 보고, 닫을 때 유령 항목으로 운다.
    🔴 오너가 GPU 를 쓰는 중이다. 경로를 옮기면 «둘 다 같은 파일을 보는지» 확인하고
    기존 파일 내용을 잃지 않게 마이그레이션까지 해라

E2  dry 경로 cp949 UnicodeDecodeError — subprocess 읽기 스레드가 cp949 로 디코딩한다.
    인코딩을 명시한다

검증  pytest 기준선 306 passed · 3 xfailed · 실패 0 · MOCK=0 으로도 selftest
보고: scratchpad/보고_P3_0905.md
```

---

## 완료 조건

```
오너 쪽
 └ 타임아웃 수정 후 «진짜 기준선» run 이 저장돼 있다 (실패경로 96 → 한 자릿수)
 └ 그 run 의 설정에 b0_sha1 이 박혀 있다

팀원 쪽
 └ A1~A4 · B1 · D1~D4 · E1~E2 가 각각 「완료 / 불필요(사유) / 보류(사유)」로 닫혔다
 └ pytest 실패 0 · MOCK=0 selftest 통과
 └ PR 본문에 DB 스키마 변경 여부가 적혀 있다
 └ docs/9-2 가 이 라운드 결과로 갱신됐다
```
