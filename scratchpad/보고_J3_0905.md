# 보고 J3 — P2 포인터형 39건, 인덱싱 처방 3안 비교 (읽기 전용, 2026-09-05)

> 코드·DB 미변경. `retrieve.py`·`corpus.chunks`·`corpus.refs`·`eval.golden_chunks` 읽기만 했다.
> 상세 원인은 [[scratchpad/골든셋검토_G1_구조적미스.md]] §4. 여기는 **처방 비교**만 새로 낸다.

## 0. 오늘 재현치 (참고 — run 191/194 과 직접 비교 금지, [[past-run-not-reproducible]])

`retrieve.py` 를 golden_chunks 고정매핑 309문항에 직접 태웠다(LLM·정규화 안 거침 —
run194 의 실패경로 96건 오염과 무관한 순수 검색값):

```
hit@5 = 189/309 = 61.2%   미스 120건
```

이 중 **폐포전용 정답청크만 있는 문항 = 39건**(G1 문서의 "38"과 근사치 — 오늘 골든청크
고정 상태 기준. 차이 원인은 안 팠다). **39건 전수 재확인 결과 도달 여전히 0/39.**

## 1. 39건의 내부 구조 — «인입참조 有無» 로 갈린다 (⑴의 적용 가능 범위를 가른다)

| 문서·조 | gold건 | 청크수 | 진입점(인입참조) |
|---|---:|---:|---|
| 창업중심대학 참고3 | 30 | 11 | **있음** — 제29조(순수 포인터 3문장) |
| 창업중심대학 참고5 | 3 | 9 | **없음** — 자기참조뿐 |
| 초격차 참고3 | 3 | 3 | **없음** — 자기참조뿐 |
| 초격차 참고6 | 1 | 13 | **없음** — 자기참조뿐 |
| L1 붙임2 | 1 | 2 | **있음** — 제65조 |
| TIPS 붙임3 | 1 | 15 | **있음** — 14./16. |
| 합계 | 39 | 53 | 인입참조 있음 32 · 없음 7 |

🔴 새로 확인한 것 — G1 은 창업중심대학·TIPS·L1 3건만 조사했다. **초격차 참고3·참고6 도
참고5 와 같은 "자기참조뿐" 고립**이다(`corpus.refs` 직접 조회, `참조문자열 LIKE '%참고3%'`
등으로 대조). **7/39건은 ⑴(진입점 키워드 보강)을 적용할 진입점 자체가 없다.**

## 2. 처방 3안 비교

| | ⑴ 진입점 키워드 보강 | ⑵ 참고·붙임 진입점 승격 | ⑶ 39건 분모 제외 |
|---|---|---|---|
| 적용 가능 | **32/39**(인입참조 있는 것만) | 39/39 전체 | 39/39 전체 |
| 재색인 범위 | 진입점 조 텍스트만 — 4개 청크(263+1,139+704+8,677자), 재임베딩 CPU 초 단위 | `retrieval_scope` 플래그 UPDATE, 53개 청크. **재임베딩 0건**(폐포전용 19,079건 전부 `embedding IS NOT NULL` 기확인) | 0 |
| golden_chunks 재고정 | **0건** — 정답청크 id 그대로, 진입점 조 텍스트만 바뀜 | **0건** — chunk_id 안 바뀜, scope 만 바뀜 | 0건(단 39건을 채점 분모 밖으로 뺀다는 집계 규칙 변경) |
| 기존 기준선과 비교 | 진입점 4개 청크 관련 32건만 재측정 필요, 나머지 비교 유효 | **후보풀 1,472→1,525(+3.6%)** — 그 53개 청크와 경쟁하던 질의 전부 재측정 필요 | 안 깨짐 — 오히려 `docs/0-1c` 가 이미 "295−39 분모로 다시 재라"고 제안한 방향과 일치 |
| 되돌리기 | 쉬움 — 원문 백업 + UPDATE 1줄, 임베딩도 원복 | **가장 쉬움** — 플래그 UPDATE 1줄 | 완전 가역(라벨일 뿐) |
| **이 안이 틀렸다면 어디서 드러나는가** | 재임베딩 후 top5 재현해도 여전히 안 뜬다(G1 이 이미 지적: "L1·타사업의 같은 이름 조"가 자리를 계속 차지) — 키워드 보강이 그 경쟁 임베딩을 못 이긴다 | hit@5 는 오르는데(2026-09-01 선례: 55.4→60.8%) **B3 프롬프트 자수가 같이 는다** — `docs/9-3` 의 "붙임2 가 B3 의 85%" 충돌이 재발한다. 판정 지연·비용 재측정에서 드러난다 | 판단불가율이 «측정상» 안 줄어도 실사용자가 이 39가지 질문 유형(비목정의·집행기준)을 물으면 서비스는 여전히 판단불가 — QA·실사용 로그에서 드러난다, 오프라인 지표에는 안 드러난다 |

### 🔴 ⑵는 이미 한 번 겪은 충돌 위에 선다

`docs/9-3_실측에서_나온_것.md`: 창업중심대학 참고3(이 39건 중 30건의 문서)을 2026-09-01 에
진입점으로 되돌렸을 때 hit@5 는 55.4→60.8% 로 올랐지만, 그 표가 gold_id=377 판정 프롬프트
B3 의 **85%(28,977/34,191자)** 를 차지했다. ⑵를 실행하면 이 39건 중 30건이 정확히 그 문서
문제와 같은 물건이라 — **"검색이 나빠진다"와 "프롬프트가 는다"가 다시 충돌한다.** 아직
아무도 안 정했다는 것도 그대로다.

## 3. 같이 볼 것 — 청크 길이 절벽(1.5k~2.5k 22.2%) 재청킹 표적, 실측 교집합

`(길이) ∩ (미스 관여 경쟁청크) ∩ (정답청크가 아님)` 을 오늘 검색값(§0)으로 직접 셌다:

```
미스 120건 → 경쟁청크(정답 아님, top5 에 뜬 것) 고유 137개
  >=1500자   13개  ─ 이 중 «다른 문항의 정답청크이기도 한» 것  5개 (24 gold_id 걸림)
                    ─ «어디서도 정답청크가 아닌» 진짜 안전 재청킹 표적   8개
  >=1000자   56개  ─ 겹침 27개(재고정 필요) · 안전 29개
```

**8개(>=1500자, 완전 안전)** 는 쪼개도 golden_chunks 재고정이 0건이다 — 기존 "1,500자+ 전체
재청킹 → 45문항 재고정" 처방보다 훨씬 싸다. 단 8개뿐이라 절벽(47.1%→22.2%) 자체를 얼마나
개선할지는 미지수 — **표적이 좁을수록 싸지만 효과도 좁다**는 트레이드오프를 그대로 옮긴 것.

겹치는 5개 청크 중 3개(`20143~20145`, TIPS "16. 연구개발비의 관리 및 사용")는 §1 과 무관한
**P1(오귀속) 재고정 후보 청크와 같은 군**이다 — J3 표적과 P1 안이 같은 문서를 건드린다.

## 4. 추천

**단기: ⑶ 즉시(오늘) 채택. 중기: ⑵ 는 오너가 §2 의 프롬프트 충돌을 먼저 결정한 뒤.**
⑶는 비용 0·완전 가역·다른 레인(J1 기준선 재측정)을 안 건드리면서 "51.3%/61.2% 가 구조적
도달불가 39건을 포함한 부풀려진/깎인 값"이라는 이미 문서화된 사실을 집계 규칙에 반영하는
것뿐이다 — 채택 안 할 이유가 없다. 다만 ⑶ 는 **측정만 고칠 뿐 서비스 사용성은 그대로**
(39가지 질문 유형에 실사용자는 여전히 판단불가를 받는다)이므로 종착지가 아니다. ⑵ 는
39건을 전부 구조적으로 푸는 유일한 안이고 재색인 비용도 0(플래그만)이지만, 그 30/39가
겹치는 창업중심대학 참고3 승격이 **이미 한 번 검색↔프롬프트 충돌을 냈던 바로 그 조작**이라
`docs/9-3` 충돌 해소 없이 단독 실행하면 같은 문제를 반복한다. ⑴ 은 7/39 에 적용 불가하고
나머지 32건도 "경쟁 임베딩을 이긴다"는 보장이 없어 **가장 불확실**하다 — 1순위로 안 둔다.

## 5. 안 본 것

- ⑵ 실행 시 B3 자수가 정확히 몇 자 느는지 계산 안 함(창업중심대학 참고3 만 11,578자 —
  gold_id=377 류 질문에서 재현 필요)
- ⑴ 의 "핵심 키워드" 를 실제로 무엇으로 넣을지(수기/LLM 생성) 결정 안 함
- >=1000자 안전 29건의 재청킹 실익(절벽 개선폭)은 안 쟀다 — 표적 목록만 냈다
