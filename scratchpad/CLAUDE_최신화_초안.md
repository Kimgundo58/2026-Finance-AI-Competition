# CLAUDE.md 최신화 초안 (2026-09-06 · ai-14) — 파일은 안 고쳤다. 제안만.

형식: 「지금 줄 → 바꿀 줄 → 근거」. 5건 확인 요청 + 직접 찾은 1건(사전승인_조건 정본화 누락은
이미 ai-8c 가 지적한 항목과 같은 자리라 별도로 안 세지 않고 그 항목에 합쳤다).

---

## 1. 「사례는 판단불가 경로 전용」 — 이제 «틀렸다», DB·코드 다 지워졌다

**지금 줄** (CLAUDE.md:37-41)
```
🔴 문서는 결정을 적었고 DB·코드는 아직 그대로다. `case_chunks` 는 이 줄을 쓰는 시점에
«살아 있고», `scripts/orchestrate.py:915 _사례()` 가 판정=판단불가 일 때 아직 부른다
(`:921` 의 `except` 가 삼켜서 «안 죽고 조용히 빈 리스트» 가 된다 — 그래서 더 나쁘다).
드랍은 백업 -> DROP -> 죽은 경로 제거 -> `db/init/01_schema.sql`·`02_frontend.sql` 수정까지가
한 묶음이다. 절차는 `scratchpad/인수인계_0906밤.md` §2-(2)
```

**바꿀 줄**
```
🔴 2026-09-06 09:09 완료 — `case_chunks` 를 드랍했다(백업 193행 →
scratchpad/_백업_case_chunks_0906.json). orchestrate 호출부 2곳(:722·:828)·`_사례()`·
`_F_사례` 임포트·모듈상태["F"] 제거까지 끝났다. `db/init/01_schema.sql`·`02_frontend.sql` 의
CREATE TABLE·v_적재현황 행·golden_set COMMENT 도 반영됐다. `case_search.py` 는 삭제가 아니라
`scripts/archive/` 로 이동. 물리 분리 원칙 자체(판정 인덱스=L1·L2+현재기관 L3 뿐)는 그대로 남는다
```

**근거**: `git show d26b96b`(사례 인덱스 드랍 커밋) · DB 확인
(`select to_regclass('corpus.case_chunks')` → `NULL`, 2026-09-06)

---

## 2. 「별표·한도표는 룰 테이블로 뺀다」 — `사전승인_조건` 이 배열이 된 사실이 빠져 있다

**지금 줄** (CLAUDE.md:32, 「확정 원칙」 안)
```
- 별표·한도표는 검색 대상에 넣지 않고 **룰 테이블**로 뺀다
```

**바꿀 줄** (문장 끝에 이어 붙임)
```
- 별표·한도표는 검색 대상에 넣지 않고 **룰 테이블**로 뺀다
  🔴 **2026-09-06 — `corpus.rules.사전승인_조건` 이 TEXT → TEXT[] 로 배열화됐다**
  (커밋 `fe58488`). 한 (사업·비목) 행에 조건이 «둘 이상» 붙을 수 있다는 뜻이라, 이 칸을
  "칸당 값 하나"로 가정한 이전 서술(룰E 지시 등)은 이 배열화 이후엔 안 맞는다
```

**근거**: `git show fe58488`(사전승인_조건 배열화 커밋) · DB 확인
(`information_schema.columns` → `data_type=ARRAY, udt_name=_text`, 2026-09-06)

---

## 3. 「HWP 조달분 자동 변환 코드 0줄」 — 진행 중이라 «곧» 낡는다 (지금은 아직 맞다)

**지금 줄** (CLAUDE.md:48-50)
```
🔴 **원칙만 열렸고 코드는 없다.** 크롤러는 `scripts/archive/crawling/**` 에 «일회성 수집»
으로만 있고, 폴링 배치·`POST /admin/ingest`·`parse_quality` 자동판정은 **한 줄도 없다**.
작업 항목은 `LLM제외_백엔드_구현_작업_TASK.md` §7. 그때까지 실동작은 종전과 같다
```

**바꿀 줄** — 지금 당장은 바꾸지 않는다. 확인한 결과 **아직은 참**이다
(`grep -rln "POST /admin/ingest\|parse_quality.*자동\|폴링 배치" scripts/ server/` → 0건,
2026-09-06 재확인). ai-8c 가 "레인 L1~L3 가 만드는 중"이라고 알려줬으나 scratchpad 에
그 산출물이 아직 안 보인다. **제안**: 이 문장은 그 레인들이 실제로 코드를 커밋한 뒤에
"몇 줄이 생겼는지"로 고치는 게 맞다 — 지금 미리 "곧 틀린다"로 고치면 «아직 사실이 아닌
것을 사실처럼» 적게 된다. 이 항목은 **watch만 하고 다음 스캔에서 재확인**을 제안한다.

---

## 4. 「지표를 읽을 때 — 다섯 번 틀렸다」 — 오늘 아홉 번째 유형이 새로 나왔다

**지금 줄** (CLAUDE.md:124-136, 마지막 불릿)
```
- **«4단 검증 통과»·«끊긴 참조 0»·«스모크 통과» 는 «판정이 맞다» 의 증거가 아니다** —
  «올바르게 뽑았다 / 안 죽는다» 의 증거다
```

**바꿀 줄** (불릿 하나 추가)
```
- **«4단 검증 통과»·«끊긴 참조 0»·«스모크 통과» 는 «판정이 맞다» 의 증거가 아니다** —
  «올바르게 뽑았다 / 안 죽는다» 의 증거다
- 🔴 **검사기(탐침)가 깨진 것과 대상이 깨진 것을 구분한다.** 탐침은 자기가 아는 표기만
  본다 — 스냅샷이 낡았거나(DB 가 그 사이 바뀜) 표기 단위가 다르면(«1개월» 을 찾는데
  «30일» 로 검사) "없다"로 오판한다. 오늘만 최소 세 번 났다: ① `사전승인_조건` 이 배열로
  바뀐 걸 모르는 검사기가 반복해서 스키마 거부를 냈다 ② «자산등록=30» 검사가 "30일"
  문자열만 찾다 원문의 "1개월"을 놓쳤다 ③ 표복구 전 유사도(0.03~0.05)를 진짜 개정으로
  오인할 뻔했다(한쪽만 표가 살아 있었을 뿐). **«없다»고 결론 내기 전에 탐침 자체의
  전제(스냅샷 시점·표기 단위·아는 스키마)부터 의심한다** — 이 문서의 다른 원칙(§ 확정
  원칙 「모든 실패의 기본값은 판단불가」)과 같은 계열이다
```

**근거**: `git show fe58488`(사전승인_조건 배열화로 인한 검사기 거부 반복 — 오늘 세션
내 여러 레인 보고에 "검산기가 스키마를 몰라 전부 거부" 반복 관측) ·
`scratchpad/기한숫자_근거없음_0906.md`(자산등록 30 vs "1개월" 단위 불일치, ai-14 자체 발견) ·
`scratchpad/recheck_선행확인_0906.md`(표복구 전 유사도 0.03~0.05 오판 위험, ai-36 발견)

🔴 이 항목은 «판단» 이 들어간다 — 기존 5개는 전부 확정 사실 서술인데, 이건 "6번째 불릿을
새로 넣을 가치가 있는가"라는 편집 판단이 섞여 있다. 오너·검토측이 최종 문구·채택 여부를
정해야 한다.

---

## 5. 판정 정의 — B0 에 들어간 「불가/조건부 구분선」이 CLAUDE.md 확정 원칙에 없다

**지금 줄** — CLAUDE.md 의 「확정 원칙 — 재논의 금지」 절엔 판정 4-way(가능/조건부/불가/
판단불가)의 정의 자체가 아예 없다(B0 프롬프트 문구는 `scripts/assemble_context.py` 에만
있고 CLAUDE.md 는 참조하지 않는다).

**바꿀 줄** — 「확정 원칙」에 한 줄 추가 제안
```
- **불가/조건부 경계**: 절차만 더하면 되면 조건부, «계획 자체»(시점·규모·대상)를 바꿔야
  하면 불가다. 질문에 적힌 계획대로 하면 위반이 «확정»되는 경우도 불가다(2026-09-06,
  B0 정의에 반영 — `scripts/assemble_context.py:49-50,103`)
```

**근거**: `git log --grep="계획 자체"` → 커밋 `c13526e`("B0 판정 정의에 «불가/조건부
구분선» 을 넣는다") · `scripts/assemble_context.py:45-51`(가능/조건부/불가/판단불가 B0 원문 확인)

---

## 부가 확인 — 고치자는 아님, 참고

`corpus.item_vocab` 을 DB 로 직접 세니 **18행**이다. ai-8c 가 목록에 준 "17행"과 1행
차이가 난다 — 어느 쪽이 최신인지(내가 지금 막 잰 것 vs ai-8c 가 준 기준시점)는 모른다.
CLAUDE.md 는 이 수치를 직접 인용하지 않으니 여기엔 손댈 게 없지만, docs 스캔 쪽에는
반영했다(아래 파일 참고).
