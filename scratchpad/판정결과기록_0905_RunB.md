# 판정 결과 기록 — Run B (2026-09-05 · ai-04)

> 🔴 **진행 중 문서다.** 「Run B」 절은 run 이 끝나야 채워진다. 그 전까지 인용 금지.
> Run A(=run 194) 절은 **끝난 run 을 새 분해기로 다시 판 것**이라 확정값이다.
> 재현: `PYTHONIOENCODING=utf-8 python scratchpad/판정분해.py <run_id>`

---

## 0. 왜 다시 분해하나 — 「오답」을 한 덩어리로 세면 처방이 안 나온다

run 194 의 「일치율 44.7%」는 판정 실력이 아니었다. **32.5%가 판정을 아예 못 받았다.**
그런데 그 사실은 「판단불가 33.2%」라는 한 숫자 안에 숨어 있었다. 그래서 원인 축을
**배타적으로** 갈라 센다 — 한 문항은 한 칸에만 든다.

```
① 실패경로     판정을 «못 받았다». 파이프라인이 넘어짐 (타임아웃·524·예외)
                ⇒ 오답이 아니라 «미측정» 이다. 처방은 전송·인프라
② 조기종료     게이트 A/B/C 에서 끊겼다. 판정 LLM 자체를 «안 탔다»
                ⇒ 처방은 게이트 임계·룰 테이블
③ 근거 미도달  판정은 했는데 정답 근거가 프롬프트에 «없었다»
                ⇒ 처방은 검색·인덱싱. **모델을 바꿔도 안 움직인다**
④ 판정 오류    근거를 받고도 결론이 틀렸다
                ⇒ 처방은 모델·프롬프트·캘리브레이션. **여기만 모델 안이다**
```

**모델 상한 = (정답 + ④) / 전체.** 이 값이 「모델 교체·파인튜닝으로 갈 수 있는 천장」이다.

---

## 1. Run A (run 194) 재분해 — 확정값

조건: `RunA_전량295_폐포ON_A100동시6` · V0 · 폐포 ON · 동시 6 · top_k 5 ·
코퍼스 `c20551-e20551-r44865-d283-16af74c4` · commit `d9562d7` · max_model_len 40960 ·
스트리밍 **없음** · 팟 `b49dvjw7br1xvf`

```
일치율 132/295 = 44.7%    다수결기준선 55.9%(«불가» 상수)    초과 -11.2
치명 1건    근거적중 50.2%    인용적중 48.5%
```

### 1-1. 🔴 44.7% 도 부풀려져 있다 — 실제는 42.7%

```
«실패했는데 정답» 6건   gold_id 429 430 463 569 573 616
```
정답이 「판단불가」인 문항에서 파이프라인이 넘어지면 결과도 「판단불가」라 **맞은 것으로
센다.** 판정력이 아니라 **두 개의 «모름» 이 우연히 겹친 것**이다. 빼면 **126/295 = 42.7%**.
(0-1 문서의 44.7% 는 이 6건을 포함한 값이다)

### 1-2. 원인 분해

| 원인 | 건수 | 비율 | 처방이 있는 곳 |
|---|---:|---:|---|
| 정답 | 132 | 44.7% | — |
| ① 실패경로 | 90 | 30.5% | 전송·인프라 |
| ④ 판정오류 | 47 | 15.9% | **모델·프롬프트** |
| ③ 근거미도달 | 22 | 7.5% | 검색·인덱싱 |
| ② 조기종료 | 4 | 1.4% | 게이트·룰 |

🔴 **모델 상한 60.7%** (= 정답 132 + ④ 47). 나머지 **116건은 모델 밖**이다 —
모델을 무엇으로 바꿔도, 파인튜닝을 해도 안 움직인다.
(①의 90 과 아래 96 의 차이가 1-1 의 6건이다)

### 1-3. ① 실패경로 96건 — 무엇이 끊었나

```
정규화 74    TimeoutError 71 · RemoteDisconnected 2 · getaddrinfo 1
판정LLM 22   HTTP 524 19 · getaddrinfo 2 · RemoteDisconnected 1
```
**vLLM 은 무죄다** (preemption 0 · 대기열 0 · abort 0). 클라이언트와 프록시가 먼저 포기했다.
원인은 이 세션에서 실측으로 둘로 갈렸다 — `docs/9_미결.md` 「프록시 천장」 절:
- 정규화 74 → 우리 기본값 **60초**. 성공 정규화 p50 이 45.7초라 **중앙값이 벽에 붙어 있었다**
- 판정 22 → 프록시 천장 **125.8초**. 클라이언트 타임아웃(180초)보다 «낮다». 상향으로 못 푼다

### 1-4. ② 조기종료 8건 — 전부가 오답은 아니다

```
게이트 C (비목갈림) 4   ·   게이트 A (금지적중) 4
그중 정답 4건
```
게이트 A 즉답 「불가」는 맞을 수 있다. 조기종료를 곧 실패로 세면 안 된다.

### 1-5. 🔴 판정 분포가 쏠려 있다 — 「가능」이 죽은 선택지다

```
          정답        예측
가능        42   →      4      🔴 42건 중 4건만 «가능» 이라 답했다
조건부      72   →     89
불가       165   →     98
판단불가     16   →     98      (94건이 실패경로)
```

주요 혼동: 불가→판단불가 57 · 불가→조건부 21 · 가능→조건부 20 · 조건부→판단불가 18 ·
가능→판단불가 17 · 조건부→불가 13 · 판단불가→조건부 9 · **조건부→가능 1(치명)**

**모델이 단정을 못 하고 조건부·판단불가로 뭉갠다.** 안전한 쪽으로만 틀린다 —
치명이 1건뿐인 이유가 여기 있다. 안전하지만 쓸모가 없다. 지식이 아니라 **캘리브레이션** 문제다.
⚠️ 다만 이 run 은 판단불가 98 중 94 가 전송 실패라 **쏠림의 크기를 이 run 으로 못 잰다.**

### 1-6. ③ 검색 — 어디서 근거를 못 찾나

**판정을 «탄» 191건 기준 근거적중 144/191 = 75.4%** (전체 분모 50.2% 와 다른 수다 —
실패경로를 뺐다. 🔴 두 수를 섞어 쓰면 안 된다)

정답청크 «최장» 길이별:
```
~500       48/51    94.1%
500~1k     32/37    86.5%
1k~1.5k    54/80    67.5%
1.5k~      10/22    45.5%     ← 길수록 나빠진다
```

문서별 (정답청크 1순위 문서 · 5건 이상 · 나쁜 순):
```
창업중심대학 세부관리기준 2025년 개정          0/18     0.0%   🔴 한 건도 못 닿는다
붙임1. 2026년 TIPS 총괄 운영지침 3차 개정안    3/9     33.3%
초기창업패키지 세부관리기준(2025년)             4/6     66.7%
예비창업패키지 세부관리기준(2025년)            15/22    68.2%
초격차 스타트업 프로젝트 세부관리기준(제10차)     18/23    78.3%
모두의 창업 프로젝트 세부관리기준(개정본)        12/14    85.7%
L1 통합관리지침 제14차개정 20251223           66/70    94.3%
2026년 재도전성공패키지 세부관리기준(11차)      12/12   100.0%
창업도약패키지 세부관리기준(2025년)            13/13   100.0%
```

🔴 **창업중심대학 0/18 이 이 run 최악이고, 원인이 검색 «품질» 이 아니다.**
J3(ai-ce) 실측: 진입점 조가 **순수 포인터**다 — 창업중심대학 제29조는
「①창업기업 사업비는 [참고3]을 따른다」 3문장이고 항목어가 0개다. 내용이 없으니
**어떤 임베딩으로도 유사도를 못 만든다.** 39건 중 7건(초격차 참고3·참고6, 창업중심대학
참고5)은 진입점 조 자체가 없다(자기참조뿐) — 키워드 보강조차 적용 불가.
⇒ 처방은 임베딩 튜닝이 «아니라» **인덱싱 설계**다.

### 1-7. 🔴 근거적중 분모가 문서 안에서 둘이다

`eval.golden_set` 315행 = 채점 **295** + `평가범위='범위밖'` **20**
(범위밖: 330 331 332 333 334 335 336 337 343 346~352 387 388 405 433).
`docs/0-1c` 의 「구조적 미도달 60건」은 **315 분모**, 「근거적중 51.3%」는 **295 분모**다.
J4(ai-92)가 잡았고 내가 DB 로 확인했다. **문서 수정 필요** — 다만 P2 쪽 38(G1) vs
39(J3 재현) 차이를 먼저 맞춰야 새 수를 박을 수 있다.

### 1-8. 강등코드는 오답을 못 골라낸다 — 판정에 배선하지 마라

```
코드                      정답   오답   판정
PREMISE_UNMAPPED          29    29    선별력 없음
CITE_HANG_MISMATCH        17    11    🔴 정답에 더 붙는다 = 순손실
VLM_DOWNGRADE              6     7    오답에 쏠림(미미)
TASK_NUMBER_UNSOURCED      1     1    선별력 없음
TASK_STATE_UNSOURCED       1     1    선별력 없음
B_GRADE_DOWNGRADE          0     1    표본 1
```

### 1-8b. 🔴 run 194 «이전» run 들의 `dirty` 기록은 전부 못 믿는다

`eval_e2e._헤드()` 가 `subprocess(text=True)` 로 `git status` 를 읽었다. Windows 에서
locale(cp949)로 디코딩하는데 이 저장소는 파일명에 한글이 흔하다 — 터지면 바로 아래
`except Exception` 이 삼켜 **`dirty` 가 `None`** 이 됐다. **None 은 「깨끗하다」가 아니라
「못 쟀다」인데 읽는 쪽에서 구분이 안 된다.** run 194 는 미추적 파일이 ASCII 뿐
(`?? scripts/archive/.claude/`)이라 우연히 살았다.
⇒ 고쳤다(`80457e6`). **그 전 run 들의 「깨끗한 트리에서 돌았다」는 근거가 없다.**

### 1-8c. 🔴 `dirty` 는 이름보다 «훨씬 좁다» — `scripts/` 만 본다

고쳐 놓고 다시 보니 더 큰 게 있었다. `_헤드()` 는
`cwd=scripts/` 에서 `git status --porcelain -- .` 를 친다. **`-- .` 가 `scripts/` 로 범위를
묶는다.** 실측:
```
저장소 전체 git status   .claude/_gpu_nag.json(M) · scratchpad/보고_J*.md(??) · … 10줄
_헤드() 의 dirty          ["?? scripts/archive/.claude/"]        ← 1줄
```
⇒ `docs/`·`.claude/`·`db/init/`·`2026_Finance_DATA_FOR_RAG/` 가 아무리 더러워도
**run 기록엔 「깨끗」으로 남는다.** 지금도 `.claude/_gpu_nag.json` 이 수정 상태인데 안 보인다.

- 다행인 쪽: 프롬프트를 만드는 `assemble_context.py`·`orchestrate.py` 는 `scripts/` 안이라
  **B0 오염은 이 필드가 잡는다.** 오늘 우리가 제일 겁낸 사고는 커버된다
- 나쁜 쪽: 필드 이름이 `dirty` 라 **저장소 전체로 읽힌다.** 「dirty 없음 = 재현 가능」이
  아니다. 코퍼스 데이터나 골든셋 픽스처가 바뀐 run 을 이 필드로는 못 가른다

**재현 근거는 필드 «넷» 을 같이 봐야 한다** (ai-36 확인 · `eval_e2e.py:1017~1019` 주석):
```
dirty          scripts/ 의 미커밋 변경만          ← 프롬프트 오염을 잡는다
코퍼스버전       chunks 수·embedding·rules·doc 해시  ← 코퍼스 교체를 잡는다
rules수         corpus.rules 총/verified          ← 🔴 코퍼스버전이 «안» 보는 축
적용대상분포      chunks.적용대상 분포               ← 🔴 코퍼스버전이 «안» 보는 축
```
🔴 뒤의 둘은 **검색 필터와 룰 경로의 «입력»** 이라 판정을 직접 바꾼다. 넷 다 `설정` 에
이미 박혀 있다 — 없는 건 «읽는 규칙» 이다. run 끼리 수치를 뺄 때 넷을 대조하고 시작한다.

### 1-9. 계측 사각지대 — 「값이 0」이 아니라 「못 쟀다」

```
판정 종료이유        {None: 100, 'stop': 195}   ← 잘림(length) 0건. 잘림은 이 run 의 원인이 아니다
판정 reasoning 길이  0건   🔴 계측이 «비어 있다». 「사고를 안 했다」가 아니다
정규화 계측          없음  🔴 `정규화메타` 자체가 안 실렸다
```
정규화가 지연의 큰 몫(p50 53.2초)을 먹는데 **토큰도 사고 길이도 한 건도 안 남았다.**
Run B 에서 J2① 계측(`23a40c3`)으로 처음 잰다.

### 1-10. 지연 (ms)

```
정규화    n=295  p50  53,227   p90 120,306   max 121,326    ← 120초 = 60초 × 2회 시도
검색      n=213  p50     354   p90     586   max  42,260
조립      n=213  p50       4   p90       8   max      23
판정LLM   n=213  p50  75,081   p90 234,286   max 253,010    ← 251초 = 125.5초 × 2회 시도
검증      n=191  p50      92   p90     112   max     161
총        n=201  p50 123,424   p90 192,710   max 616,535
```

### 1-11. 치명 오답 1건

```
gold_id=353  사업=예비창업패키지  정답=조건부  예측=가능
요약: "창업기업 사업비 비목은 지침 제33조부터 제42조까지 정하는 바에 따른다(S02)."
```
근거는 «닿았는데» 조건을 안 읽고 「가능」으로 단정했다. ④ 판정오류 계열이다.

---

## 2. Run B — 진행 중 (채워질 자리)

조건: `RunB_전량295_스트리밍ON_A100동시6` · V0 · 폐포 ON · **동시 6** ·
**스트리밍 ON** (`SUDDOE_LLM_STREAM=1`) · 정규화 타임아웃 60→300 ·
팟 `n41bny9uuppbdl` (A100 80GB PCIe · US-KS-2)

Run A 대비 **바뀐 변수는 «전송» 하나다.** 프롬프트(V0·`b0_sha1`)·코퍼스·룰·골든셋 불변.

```
(run 종료 후 `python scratchpad/판정분해.py <run_id>` 출력을 여기에 붙인다)
```

### 미리 못박아 두는 읽는 법

- **①이 0 에 가까워야 이 run 이 성공이다.** 일치율이 아니다. 일치율은 그 다음에 읽는다
- **①이 줄면 판단불가가 «저절로» 줄고 일치율이 오른다.** 그건 판정이 좋아진 게 아니라
  «측정이 된 것» 이다. 이 run 의 일치율을 Run A 와 빼면 그 차이의 대부분이 전송 수복이다
- **처음으로 «판정 실력» 을 읽을 수 있는 run 이다** — ③④ 의 비율과 1-5 의 쏠림이
  실패경로에 안 가려진 채 나온다
- 🔴 **Run A 와 빼도 되는 것 / 안 되는 것**
  - 되는 것: ① 실패경로 건수, 지연 분포 (전송만 바뀌었다)
  - 안 되는 것: 근거적중 — Run A 는 191건 분모, Run B 는 실패가 줄어 분모가 커진다.
    **분모를 반드시 같이 적는다**
