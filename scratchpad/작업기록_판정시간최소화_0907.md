# 작업기록 — 판정 시간 최소화 (2026-09-07 · ai-fe)

## 1. 무엇을 진행하나
오너 지시: 「판정 시간 최소화 작업을 오류 없이 지금 최적화」. ai-3f 의 요청서는 아직 안 옴 —
코드·실측을 먼저 읽고 계획을 세웠다.

## 2. 어떻게 진행했나 — 현재 실측 (Q5 실경로 통과, 2026-09-07, 로컬 콜드 1건)

| 단계 | ms | 지금 상태 |
|---|---:|---|
| 정규화(①) | 43,168 | 이미 thinking off → 4.1초 (기록 2026-09-07) |
| 비목확정 | 31,847 | **콜드** — `rule_lookup` 이 KURE-v1 을 «따로 한 벌 더» 올린다 (`rule_lookup.py:463`) |
| 검색 | 11,976 | **콜드** — `retrieve.모델()` 로드 + kiwi. warm 이면 <300ms 예산 |
| 조립·검증·전제해소 | <200 | 문제 없음 |
| **판정LLM(④)** | **59,273** | qwen3.7-plus · thinking ON · completion 3,335 토큰(추론 7,689자) · prompt 14,140 토큰 |
| 총 | 146,469 | |

warm 상태 운영 추정: 정규화 4 + 판정LLM ~55~59 ≈ **60초**. 판정LLM 이 거의 전부다.

### 근거 실측 (같은 프롬프트 계열)
- Q3: qwen3.7-plus, 입력 14,017 토큰, **출력 824 토큰 → 16.4초** (추론 없음으로 보임)
- Q4/Q5: 같은 모델, 출력 2,906~3,335 토큰(추론 6.7~7.7k자) → 52.6~59.3초
- ⇒ 판정 지연의 ~70% 가 «사고(thinking) 토큰 생성» 이다. 프롬프트 길이는 부차.

## 3. 레버와 계획

| # | 레버 | 기대 | 위험 | 필요한 것 |
|---|---|---|---|---|
| A | 콜드 이중 모델 제거: `rule_lookup.임베딩` 이 `retrieve.모델()` 을 재사용 + 서버 기동 시 워밍업(lifespan) + `/admin/warmup` 에 비목확정 포함 | 첫 요청 -30~40초, RSS -2.4GB | 없음(정확도 무관) | 코드만 |
| B | 판정 ④ thinking 제어 — `SUDDOE_JUDGE_THINKING=on\|off\|<budget>` (DashScope `enable_thinking` / `thinking_budget`) | off 59→~16초 · budget 1024 면 ~25~30초 추정 | **정확도** — A/B 필수 | 유료 호출 |
| C | 폐포(B3) off — 이미 `--폐포 off` 스위치 있음 | 입력 14k→~4k 토큰, 몇 초 + 비용 | 정확도 A/B | 유료 호출 |

A/B 설계: 같은 문항(튜닝52·held-out41 구분 유지, 각 20건) × {기준 on / off / budget} → 일치율·치명오답·판단불가율·p50 지연. 판정 채택 기준(사전 선언): 일치율 -2문항 이내·치명오답 안 늘면 채택.

## 4. 결과 — 오너 결정: «측정 없이 A 만» (B·C 유료 A/B 는 안 함)

수정 2파일 (커밋 안 함 — 중앙 ai-33 이 한다):
- `scripts/rule_lookup.py` `warmup()` — `retrieve.모델()` 재사용, 실패 시 종전 자체 로드
- `server/main.py` — `_워밍업_전부()`(검색+비목확정), `startup` 훅에서 백그라운드 스레드 워밍업
  (MOCK·`SUDDOE_WARMUP_ON_START=0` 이면 건너뜀), `/admin/warmup` 이 같은 함수를 씀

실측 (로컬):
| 항목 | 전 | 후 |
|---|---:|---:|
| `rule_lookup.warmup()` | ~30초(별도 모델 로드) | **0.03초** (`_모델 is retrieve.모델()` = True) |
| 품목 임베딩 1건 | — | 96ms |
| retrieve 워밍업(모델 로드) | 28.4초 | 28.4초 — 기동 시 백그라운드로 옮김 |

⇒ 데워진 뒤 첫 판정에서 비목확정 31.8초 + 검색 12초가 사라진다. 판정LLM 59초는 그대로다
(thinking 이 원인 — 오너가 오늘은 A/B 안 하기로 함).

테스트: `test_contract·test_rule_lookup·test_e2e_flow·test_계약_키집합·test_gpu_watchdog` 통과.
🔴 `test_contract.py::test_health_vocab_programs_profile_키` 는 **내 수정과 무관하게** 실패한다
(stash 후 재현) — `/api/programs` 항목에 `우선순위규칙목록` 키가 늘었는데 계약표가 안 따라감. 다른 레인 몫.

남은 레버(미실행): B thinking 스위치(59→~16초 추정) · C 폐포 off — 둘 다 골든셋 A/B 가 먼저다.

## 5. 후속 (같은 세션)
- v27 백그라운드 워밍업이 Cloud Run 에서 안 돎(cpu-throttling) → v28 동기 워밍업: 기동 로그 44.4초, 이후 `/admin/warmup` 0.2초
- 프론트 대기 문구 「보통 5초」→「보통 1분」(fb0187a, Vercel READY). 백엔드 PR #41~#44 main 머지
- ai-33 cfc144a(문의초안 B안) 검토: SELECT 인덱스 일치·문구 3건 OK. 결함 1 — `증빙목록` 제거가 normalize `결과` 에 들어감 → 판정 `결과` 로 옮김. 로컬 판단불가 plan 26913 으로 상세 재생성 확인. v29 = 00042-6fl
