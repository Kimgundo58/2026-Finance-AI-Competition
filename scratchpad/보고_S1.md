# 보고 S1 — 배포 패키징 (2026-09-03)

## ① 만든 파일
`requirements-api.txt`·`requirements-gpu.txt`·`Dockerfile`·`.dockerignore`·`.env.example`·
`docs/8_운영/8-5_배포_레시피.md`(98줄 · 중앙이 8-3 충돌을 재번호해 옮김).
`.gitignore` 의 `.env.*` 예외는 중앙이 처리 → `git check-ignore -v .env.example` 무출력(정상) 확인.

## ② 실측 (로컬 Docker Desktop 29.7.2 · linux/amd64)
| 항목 | 값 |
|---|---|
| 콜드 빌드 `--no-cache` / requirements 한 줄 수정 후 재빌드 | **339초** / **340초** |
| └ 그중 모델 bake (캐시 마운트 전 → 후) | **105초 → 20초** |
| CONTENT SIZE(푸시·풀 전송량) / DISK USAGE | **2.45GB** / 6.44GB |
| 컨테이너 안 `du -sx /` | **3.7GiB** (HF 2.2GB + venv 1.5GB) |
| 모델 로드+첫 인코딩 (`--network none`) | **7.7~8.1초**(초기 n=3) / **14.8~20.8초**(최종 n=6) |
| 임베딩만 돌린 프로세스 peak RSS | **2.4GB** |

🔴 「KURE-v1 ~1.1GB」는 틀렸다 — **실측 2.2GB(fp32)**. fp16 이면 반이지만 임베딩 수치가 바뀌어
안 건드렸다. `--index-url .../whl/cpu` 도 그대로 못 썼다 — **그 인덱스엔 fastapi 가 없다(실측)**.
`--extra-index-url`+`torch==2.11.0+cpu` 로 대체(cuda None 확인). torchvision 은 grep 0건 → 뺐다.

## ③ 빈 venv import
빌드 안 새 venv 에 requirements 만으로 설치 → `import server.main` **ok**. 지연 import 라 그것만
으론 안 보이는 사슬을 네트워크 차단 상태로 따로 태웠다 — `orchestrate·l3_parse·retrieve·
normalize_run·rule_lookup·stage0_extract·hwp/hwpx/docx_extract·pdftext·stage2_bm25·adapter` 전부
ok, 늦게 부르는 `lxml.etree·pypdf·docx·pdfplumber·olefile·kiwipiepy·multipart` 도 직접 확인.
`SUDDOE_MOCK=1` 기동 → `/api/health` **200** (비루트 사용자 suddoe 로).

## ④ main.py 변경 — 없음 (제안만, 수정 안 함)
$PORT 는 CMD 가 먹는다. 다만 `MOCK` 기본이 `1` 이라 환경변수를 빠뜨리면 «조용히 목업»이 뜬다:
```python
if MOCK and os.environ.get("K_SERVICE"):   # app = FastAPI(...) 뒤. K_SERVICE = Cloud Run 이 준다
    _log.error("🔴 배포 환경인데 SUDDOE_MOCK=1 이다 — 목업 응답이 나간다")
```
startup 워밍업 훅은 넣지 말자는 쪽 — min-instances=1 + `/admin/warmup` 로 덮인다.

## ⑤ 확신 없는 지점
- **Cloud Run 에 올려본 적 없다** — 4~7단계는 미검증(Cloud SQL 커넥터·CORS·SSE 타임아웃). 「빌드·기동 성공」은 「배포하면 돈다」의 증거가 아니다
- 4GB: 임베딩만 2.4GB 다. 남는 1.5GB 로 uvicorn·kiwi 사전·동시 4건이 버티는지는 **추론**
- `vllm==0.28.0` 은 PyPI 최신을 실측했을 뿐 Qwen3-32B-AWQ 로 뜬 걸 본 적 없다(로컬 GPU 없음).
  `scripts/` 는 통째로 넣었다(1.6MB) — 판정 경로만 추리려면 소유 세션 확인이 필요

## ⑥ 추가분 — 중앙 배정 W-SEED (2차)
- **과제1(시연 계정 시드) 반송.** `db/init/` 는 내 소유 밖, `scripts/seed_demo_account.py` 는 신규라도
  scripts/ 쓰기 = 헌장 금지. DB 는 읽지도 않았다
- **과제2(.env.example) 완료.** 목록을 믿지 않고 server/+scripts/ 전수 grep(`_int환경`·`_비밀()` 헬퍼
  경유 포함) → **코드가 읽는 39개 전수** 수록, 「코드엔 있는데 이름표에 없는 것 0건」 스크립트로 확인.
  중앙 목록 6개 외에 `SUDDOE_JWKS_TTL·JWT_AUD·DEMO_TTL·RUNPOD_API_KEY·RUNPOD_REST` 가 더 있었다.
  목록에 있는데 코드엔 없던 것은 없다. 값·키는 0건(이름표뿐)
- 🔴 **`server/auth.py:120` 의 `import jwt` 가 requirements 에 없었다.** 지연 import 라 기동은 되고
  «첫 로그인»에서 죽는다. `PyJWT[crypto]==2.13.0` 추가 후 이미지에서 RS256/ES256 available 실측
- Dockerfile: 모델 bake 를 캐시 마운트로 pip 레이어와 분리(위 표). 런타임 다운로드는 여전히 0.
  중앙이 「BuildKit 전용이라 CI 에서 깨진다·차가운 CI 엔 이득 0」을 문서에 덧댄 것 확인했다 — 맞다
- 🔴 **워밍업이 이미지 재빌드 후 8초 → 15초대로 갈렸다.** 모델 블롭 해시는 같고(내용 차이 아님)
  순차읽기는 3.6초(628MB/s)뿐이다. **원인 미확정** — 호스트 상태가 후보지만 확인 못 했다.
  계획엔 느린 쪽(~20초)을 쓰기로 하고 문서에 관측/추론을 갈라 적었다