# 판정 결과 기록 — Run A (2026-09-05)

> 🔴 **진행 중 문서다.** 아래 수치는 137/295 시점의 «중간값» 이고 최종이 아니다.
> run 종료 후 이 문서 맨 아래 「최종」 절을 채운다. 그 전까지 어떤 수치도 인용하지 마라.

## run 조건 — 다른 run 과 «빼지 마라»

| 항목 | 값 |
|---|---|
| 라벨 | `RunA_전량295_폐포ON_A100동시6` |
| 분모 | 295문항 (골든셋 315 중 채점대상) |
| 폐포 | **ON** (프롬프트 26,115토큰) |
| 동시 | 6 (A100 SXM 80GB · `b49dvjw7br1xvf`) |
| 코퍼스 | 20,551청크 |
| 브랜치 | `work/지연최적화-0905` |
| 시작 | 2026-09-05 05:50 UTC · 26.1초/문항 |

🔴 **run 191(2026-09-02)과 비교 금지.** 분모 93·코퍼스 20,525·동시1 이고, 그 사이에
룰 검수(74/74)·BM25 누락 복구(53→0)·참고3 표 복구·골든셋 38문항 재고정이 들어갔다.

## 중간 수치 (137/295)

```
일치율      42.3%  (58/137)      ← 네트워크 오염 5건 제외 시 약 43.9%
치명 오답    1건                  ← 채택 기준 「치명 0」의 첫 관문은 거의 통과 중
판단불가율   약 31%               ← 판단불가가 «기본값» 처럼 쓰이고 있다
근거적중    약 57%
```

## 오답 78건의 분해

| 유형 | 건수 | 성격 |
|---|---|---|
| ① 실패경로 | 30 (버그후보 25 · 네트워크 5) | 파이프라인이 넘어짐. **원인 미확정** |
| ④ 판정 오류 | 24 | 근거○ 인데 결론 틀림 |
| ③ 검색 미도달 | 나머지 | 근거✗ |

**모델 탓 상한**: 오답 78건 중 근거를 받고도 틀린 건 24건. 모델을 무엇으로 바꿔도
**상한 59.9%**(= (58+24)/137). 47·51·58·93건 시점에서 각각 68.1·68.6·68.6·65.6% 였고
계속 내려가는 중 — 표본이 늘수록 「모델 밖 원인」의 비중이 커진다.

## 판정 분포가 쏠려 있다 — 이 run 의 핵심 발견

```
              정답            예측
 불가        74 →           37     («불가»라고 말하지 못한다)
 조건부      30 →           43     (부풀었다)
 가능        20 →            1     🔴 사실상 죽은 선택지
 판단불가    10 →           39     (부풀었다)
```

**모델이 「가능」과 「불가」라는 단정을 못 하고 「조건부·판단불가」로 뭉갠다.**
치명 오답이 1건뿐인 이유가 여기 있다 — 안전한 쪽으로만 틀린다. 안전하지만 쓸모가 없다.
지식 문제가 아니라 **결론 내리는 문턱(캘리브레이션)** 문제다.

주요 혼동 (정답→예측): 불가→판단불가 27 · 가능→조건부 10 · 조건부→판단불가 9 ·
가능→판단불가 9 · 불가→조건부 8 · 판단불가→조건부 6.

## 검색 — 청크 길이별 근거적중률 (133문항)

```
① ~500자        23/40   57.5%
② 500~1000      17/28   60.7%
③ 1000~1500     23/43   53.5%
④ 1500~2500      5/22   22.7%   🔴 여기서 절벽이 있다
```

**1,500자를 넘으면 적중률이 반토막 난다.** 그 아래에서는 길이와 적중률의 관계가 거의
없다 — 즉 「청크가 길수록 나쁘다」가 아니라 **「1,500자가 임계」** 다.
코퍼스 평균은 491자, TIPS 문서 평균은 1,186자.

### 적용대상별 — 구조적 불가가 있다

```
창업기업    61/110   55.5%
공통         9/22    40.9%
주관기관     0/13     0.0%   🔴 검색 필터가 «적용대상 IN ('창업기업','공통')» 이라 원천 배제
```

주관기관 13문항은 **무엇을 고쳐도 안 나온다.** `docs/9_미결.md` #14(주관기관 화자를
지원할 것인가)가 예비창업 17문항으로 열려 있었는데, **TIPS 에서도 나온다**는 게 오늘 새로
확인된 부분이다. 이건 검색 튜닝이 아니라 **제품 정의 결정**이다.

### 문서별 근거적중률

```
재도전성공패키지 세부관리기준(11차)    5/6    83.3%
L1 통합관리지침 제14차               38/60   63.3%
창업중심대학 세부관리기준 2025         2/3    66.7%
예비창업패키지 세부관리기준(2025)      10/19   52.6%
모두의 창업 프로젝트 세부관리기준      14/27   51.9%
붙임1. 2026년 TIPS 총괄 운영지침       3/21   14.3%   🔴 최악
```

**TIPS 가 압도적으로 나쁘다.** 청크 평균 길이(1,186자)가 코퍼스 평균의 2.4배이고,
주관기관 조항이 섞여 있으며, `precedence_rules` 도 룰도 없는 사업이다(TIPS 는 「룰을
만들지 않는다」로 2026-08-31 결정). 세 요인이 겹쳐 있어 **아직 어느 것이 주범인지 못
가른다.**

## ⑥ 검증층 — 강등코드를 판정에 배선할 수 있나

```
                        정답에 붙음   오답에 붙음
 PREMISE_UNMAPPED           11           15
 CITE_HANG_MISMATCH         10            5
 VLM_DOWNGRADE               2            3
```

🔴 **`CITE_HANG_MISMATCH` 를 판단불가로 배선하자던 제안은 이 표로 기각된다.**
오답 5건 중 정답이 되는 건 1건뿐인데(gold 428), **맞은 답 10건을 판단불가로 망가뜨린다.
순증 −9.** 4건만 보고 「가장 깨끗한 배선」이라 했던 것은 표본이 작아서 생긴 착시였다.

`PREMISE_UNMAPPED` 도 마찬가지로 정답 11건에 붙어 있어 단순 배선은 안 된다.

**남는 것**: 강등코드는 «판정을 뒤집는 스위치»가 아니라 «가중치» 로 쓰거나, 다른 조건과
결합해야 한다. 단독 배선은 전부 순손실이다.

## 게이트 C — 4/4 오답 (LLM 2배 비용)

```
399 · 419 · 427 · 474 · 476   전부 「선택필요」로 되물었고 전부 오답
 └ 정답은 각각 불가 · 가능 · 판단불가 · 불가 · 조건부
 └ 474 · 476 은 근거○ 인용○ 였다 — 근거를 제대로 찾고도 되물었다
```

비목이 «갈렸다»는 판단 자체가 틀리고 있을 가능성. `docs/9_미결.md` #7(비목 신뢰도
임계)이 정답셋으로 판가름 난다고 적힌 항목이고, 이 run 이 그 데이터다.

## 지연 — 병목이 프롬프트가 아니었다

```
vLLM 실측 (호출 172건 · A100 · 동시6)
 TTFT (프롬프트 읽기)   평균  2.41초
 요청 전체              평균 50.9초
 차이 ≈ 48초            = 토큰 «생성» (약 512토큰 → 약 10 tok/s/스트림)
 prefix 캐시 적중률     30.8%
 preemption · 대기열    0 · 0        ← 동시6 은 A100 에 부담이 아니다
```

**기존 문서의 「판정 66초 중 55초가 프롬프트 26k 읽기」와 맞지 않는다.**
🔴 두 측정을 그대로 빼지 마라 — 55초는 A6000·동시1 값이다. 확정된 것은
**「이 구성에서 프롬프트는 병목이 아니다」** 하나다.

**따라서 Run B(폐포 OFF)의 지연 기대값은 죽었다.** 남은 값은 «정확도 A/B» 뿐 —
폐포가 프롬프트의 76%인데 도움인지 방해인지는 아직 모른다.

## GPU 경제성 — 실측

```
문항당 단가 = 시급 ÷ 시간당 처리량

               시급     초/문항   295건 소요   295건 GPU비   문항당
 A6000 동시3   $0.53     51.7      4.24h        $2.25      $0.0076
 A100  동시6   $1.59     26.1      2.14h        $3.40      $0.0115
```

A100 은 **2시간을 $1.15 에 산 것**이지 싸진 게 아니다.
H100 NVL($3.19/h · US-KS-2 재고 있음 · 볼륨 붙는 유일한 H100)은 손익분기가 **2.01배**
인데 기대치가 1.6~2.0배라 턱걸이·미달이고, **콜드스타트 12~15분**(GPU 가 빨라져도 안
줄어든다)을 넣으면 22% 더 비싸진다. → **전환 안 함.**

## 사고 — 네트워크 전환 오염

```
06:35:01 UTC · 98/295 시점에 사용자가 인터넷을 바꿨다
 └ 이후 2분간 실패 5건 (451·452·453·456·454) — 평소 밀도의 6배
 └ 그 뒤 12문항은 실패 1건 — 원래 밀도로 복귀
 └ 이 5건은 트레이스에 URLError·timeout 계열이 찍히면 버그 집계에서 «뺀다»
```

## 내가 틀린 것 — 지우지 않고 남긴다

1. **「실패 문항은 사업명이 비어 있다」** — 초반 5건만 그랬다. 16건이 되자 9건이 사업명
   있는 문항이었다. 정답셋 속성 전부와 교차해도 **아무것과도 상관없다**
2. **「실패 문항도 LLM 2회를 다 썼다 → 832 예외가 유력」** — 근거로 쓴 호출 수 산술이
   진행 중 문항을 안 뺀 계산이었다. 1회로도 맞아떨어진다. **어느 쪽인지 모른다**
3. **「CITE_HANG_MISMATCH 배선이 가장 깨끗하다」** — 4건 기준이었다. 15건이 되자
   10건이 정답에 붙어 있어 **순증 −9** 로 뒤집혔다
4. **「PREMISE_UNMAPPED 은 방향이 완전히 하나」** — 8건일 때였다. 12건에서 반대
   방향(조건부→불가)이 나왔다
5. **「동시6 과부하로 실패한다」** — vLLM preemption·대기열이 0 이라 GPU 는 무죄다

6. **「게이트C 가 0/6 이니 임계를 조여야 한다」** — 정답셋에 「선택필요」 행이 0건이라
   게이트C 는 «구조적으로» 맞을 수 없다. 측정할 수 없는 것을 근거로 고치려 했다

공통 실패 모양: **작은 표본에서 본 패턴을 원인으로 확정했다.** 표본이 2~3배가 되면
전부 흐려졌다. 6번은 결이 다르다 — 표본 크기가 아니라 **채점표에 그 답이 없다는 것을
안 확인했다.**

## 아직 모르는 것 — run 종료 후 확정

- 🔴 **실패경로 25건의 `실패단계`·`트레이스`** — 이게 오답의 최대 덩어리이고 유일한
  「코드 한 곳을 고치면 크게 회복」 후보다. DB 에 스택 1,200자가 저장돼 있다
- 게이트 C 전수 적중률 (지금 0/5)
- TIPS 저조의 주범 — 청크 길이 / 주관기관 / 문서 자체 중 무엇인가
- 세트별(본세트·적대적·공식·보강) 분해 — 뒤쪽 구간에 적대적·공식이 몰려 있어
  일치율이 더 떨어져도 「모델이 나빠진 것」이 아니다

## 추가 관측 (137~166건 사이)

**`TASK_NUMBER_UNSOURCED` 발견** — `llm_validate.py:291`. 「해야할일」 문장에 프롬프트에
없는 숫자가 들어가면 그 설명을 폐기한다. 앞의 코드들(불확실성 표시)과 달리 **모델이 숫자를
지어낸 환각을 실제로 잡아 걷어낸 것**이다. gold 514 는 세 코드가 동시에 붙었고
근거○ 인용○ 였다 — **근거가 닿아도 안심할 수 없다.**

⑥ 검증층 평가를 한 겹 수정한다: 오답의 82%를 «알아채지 못하는» 것은 맞지만, 알아챈
자리에서는 환각을 실제로 걷어낸다. 판정을 못 바꿀 뿐 무용하지 않다.

**「가능 → 조건부」의 강등코드가 갈린다** — `PREMISE_UNMAPPED` 9건 · `VLM_DOWNGRADE` 2건 ·
없음 다수. 원인이 무엇이든 «가능» 이 «조건부» 로 흘러내린다. 강등코드는 원인이 아니라
«불확실성의 출처 표시» 이고, 처방은 코드별이 아니라 **「불확실하면 조건부로 간다」는 기본
동작 자체**를 겨눠야 할 수 있다. 🔴 해석이고 미검증 — 전수에서 「가능→조건부」 전건의
강등코드 분포로 확인한다.

## 🔴 이번 run 의 진단 한계 — 트레이스가 저장 안 된다

`orchestrate.py:832` 는 `트레이스=traceback.format_exc()[-1200:]` 를 응답에 담지만
`eval_e2e._채점항목` 이 그 키를 **안 베낀다.** 따라서 `eval.run_items.원출력` 에 스택이 없다.

```
이번 run 에서 얻는 것   실패단계(5자리 중 하나) · 강등사유("예외이름: 메시지 200자")
못 얻는 것              어느 «줄» 에서 났는가
```

예외 «종류» 는 알 수 있으므로 진단은 가능하나, 내가 앞서 「스택 1,200자가 저장돼 있다」고
말한 것은 **틀렸다.** 다음 run 부터 저장되도록 `_채점항목` 에 `"트레이스"` 를 추가해 뒀다
(실행 중인 프로세스에는 미반영 — Run B 부터 적용).

## 원인 규명 — ai-eb 분석 + 중앙 검증 (2026-09-05)

전문: `scratchpad/성능고도화_분석_ai-eb_0905.md`. 아래 셋은 **중앙(ai-35)이 코드로 직접
재확인**했다.

### ⑴ 프롬프트 B0 한 문서가 두 병을 동시에 만든다  [확인됨]

```
assemble_context.py
 :44-48  네 판정값 정의는 «있다»   ← 「3분 기준이 없다」는 내 추정은 틀렸다
 :52     "근거가 없거나 모호하면 판단불가다. 추측해서 «가능» 을 만들지 않는다."
 :56     "가장 위험한 오답은 «불가한 것을 가능이라 답하는 것». 확신이 없으면 판단불가다."
 「조건부」·「불가」를 경계하는 대응 문장  0개
```

| 관측 | 원인 |
|---|---|
| 정답 「가능」 26건 중 1건만 맞힘 (3.8%) | `:52`·`:56` 의 «가능» 이중 경계 |
| 판단불가율 31% (정답 분포 5%) | `:56` 이 판단불가를 **명시적 기본값**으로 지정 |

문제는 기준의 **부재가 아니라 비대칭**이다. 비대칭 자체는 치명 0 제약을 떠받치는 의도된
설계이므로 없애지 않는다 — **「가능」의 진입 조건을 명시**하는 방향으로만 연다.

### ⑵ PREMISE_UNMAPPED 26건의 근본 원인  [확인됨]

```
🔴 «허용경로»(F축 매핑 목록)가 프롬프트에 «전혀 안 나간다»
 └ assemble_context.py 에 허용경로·f_경로집합 참조 0건
 └ f_경로집합() 은 llm_validate.py:353 · orchestrate.py:51 — «사후 검증»에서만 쓰인다
 └ ⇒ 모델은 유효 경로를 모른 채 매핑을 채운다. 미스매핑이 나는 게 당연하다
```

지금까지 나온 것 중 **가장 값싼 수리** — 프롬프트에 목록을 넣기만 하면 된다.

### ⑶ 실패경로 32건 — 최유력 후보 1개  [미확정]

```
llm_validate.py  f_경로집합() · s번호_메타()
 └ 호출마다 «캐시 없이 새 DB 커넥션» · try/except 없음
 └ 문항당 커넥션 최소 4개 × 295 ≈ 1,180개
 └ (4)판정LLM «이후» 단계 → 실패 시 orchestrate:827 포괄 except → 실패단계="예외"
 └ 「3~7문항마다 균일」과 맞는 유일한 후보
```

**배제 확정**: `_포획`/`_꽂을청크`(RAW+동시>1 이면 즉시 `sys.exit(1)`) · `adapter._URL캐시`
(orchestrate 가 import 안 함) · 임베딩 지연초기화 레이스(실재하나 초반 1회성이라
「전 구간 균일」과 안 맞음).

**검증 방법**: 트레이스가 저장 안 되므로 `강등사유` 의 예외 «이름» 으로 판정한다.
커넥션 고갈이면 `psycopg.OperationalError` 계열로 몰린다.

### ⑷ 지연 — 계측 자체가 사각지대였다  [확인됨]

`eval.runs` 에 run 192/193 「Q4_처방1_출력토큰실측」이 이미 있는데 `사고흔적있음=False`
로 전부 찍혀 있다. **이건 「생각을 안 한다」가 아니라 계측 도구의 사각지대다** —
`사고흔적_걷기()` 는 `<think>` 가 본문에 섞인 경우만 잡는데, `--reasoning-parser qwen3` 가
정상 동작하면 사고는 `reasoning_content` 로 깔끔히 갈라져 그 함수에 안 잡힌다.

```
필요한 «추론content있음/길이» 는 normalize_run.llm_호출 이 이미 계산(:129-130)하는데
orchestrate.py:797-817 이 모델 딕셔너리 조립 때 «빼먹는다» → 코드 2줄이면 다음 run 부터 잡힌다
```

run192 표본 9건: 요약 20~171자(상한 300 훨씬 아래) · 해야할일 0개 · 전제 0~3개.
completion 은 259~1447 로 4배 넘게 벌어지는데 JSON 크기는 그만큼 안 벌어진다 →
**사고(reasoning)가 변동 대부분을 설명할 개연성**. max_tokens 3000 캡은 안 걸렸다.

### 내 추론이 또 하나 뒤집혔다

「vLLM 완료수 = 문항×2 → 두 단계 다 성공」에 구멍이 있다 — `재시도=1` 이 파싱 실패 시
같은 단계를 두 번 완료 처리해도 카운트는 똑같이 2다. (ai-eb 지적)

## 🔴 게이트 C 는 이 정답셋으로 «평가할 수 없다» — 앞선 결론 철회

```
골든셋 315문항 정답 분포
 불가 178 · 조건부 78 · 가능 42 · 판단불가 17 · «선택필요 0»
```

게이트 C 가 발동하면 예측이 「선택필요」로 나오는데 **정답이 「선택필요」인 문항이 0건**이다.
즉 게이트 C 는 발동하는 순간 무조건 오답으로 채점된다 — **구조적으로 맞을 수 없다.**

| 앞서 적은 것 | 정정 |
|---|---|
| 「6/6 오답 → 임계를 조인다」 | 성립하지 않는다. 0/6 은 게이트C 가 나쁘다는 증거가 아니라 **정답셋이 그 기능을 못 잰다**는 뜻이다 |

```
이 run 이 말할 수 있는 것        이 run 이 말할 수 «없는» 것
 ✅ 발동 횟수와 LLM 2배 비용      ❌ 게이트C 판단이 틀렸다
                                 ❌ 임계를 조여야 한다
```

🔴 **`docs/9_미결.md` #7(비목 신뢰도 임계)을 이 run 으로 닫으면 안 된다.** 「정답셋으로
판가름 난다」고 적혀 있으나 **지금 정답셋에 판가름할 문항이 없다.** 선행 과제는 임계 조정이
아니라 **정답이 「선택필요」인 행을 정답셋에 넣는 것**이다.

## 최종 — run_id 194 (2026-09-05 · 295문항 · 7,124초 · A100 동시6)

```
전체 295    일치율 44.7%   다수결기준선 55.9%   초과 -11.2   치명 1
            판단불가 33.2% (모델선택 «4» · 실패경로 나머지)
            근거적중 51.3%   인용적중 49.5%
```

🔴 **일치율이 다수결 기준선보다 11.2%p 낮다.** 「무조건 불가」라고 답하는 상수 모델이
55.9% 인데 우리는 44.7% 다. **이 수치는 판정 실력의 증거가 아니다.**

### 구간별

```
구간           문항  일치율  기준선   초과  치명  판단불가  실패경로  근거적중
세트:공식        11   72.7   72.7    0.0    0     0.0        0     81.8
세트:본세트     228   45.6   57.0  -11.4    1    34.2       76     50.7
세트:적대적      28   42.9   78.6  -35.7    0    35.7        8     35.7
세트:보강        28   28.6   42.9  -14.3    0    35.7       10     61.9
묶음:held-out   239   46.9   57.7  -10.9    1    32.6       76     52.2
묶음:튜닝52      56   35.7   48.2  -12.5    0    35.7       18     46.9
```

`세트:공식` 11문항만 기준선과 같고(근거적중 81.8%) 나머지는 전부 기준선 미달이다.

## 🔴 실패 96건의 원인이 확정됐다 — 코드 결함이 아니라 «타임아웃» 이다

```
실패단계          강등사유(예외 이름)                                건수
 정규화 (74)      TimeoutError: The read operation timed out          71
                  RemoteDisconnected                                   2
                  URLError: getaddrinfo failed                         1
 판정LLM (22)     HTTP 524 (Cloudflare 프록시 타임아웃)                19
                  URLError: getaddrinfo failed                         2
                  RemoteDisconnected                                   1
```

**90건이 순수 타임아웃이다.** `getaddrinfo failed` 3건은 06:35 네트워크 전환 때 난 DNS
실패다(예고한 오염이 이 형태로 확인됐다).

```
경로 대응
 └ 경로="실패" 72건            = 실패단계 「정규화」  (LLM 1회차)
 └ 경로="…+4조립" 22건          = 실패단계 「판정LLM」 (LLM 2회차 · 6검증을 못 탄 이유가 이것)
```

### 내 진단이 한 바퀴 돌아 처음으로 돌아왔다

```
① 「동시6 과부하」            처음 가설
② vLLM 계측(preemption 0 · 대기열 0)을 보고 «기각»
③ 「커넥션 고갈」 로 갈아탐 (ai-eb 분석)
④ 실제 원인 = 타임아웃  →  ①이 «효과로는» 맞았다
```

**②의 오독이 핵심이다.** vLLM 이 요청을 «떨어뜨리지 않았다» 는 것과 «제때 끝냈다» 는
것은 다른 말이다. GPU 는 멀쩡히 서빙하고 있었고, **클라이언트와 Cloudflare 프록시가
먼저 포기했다.** 계측이 가리키는 층을 착각했다.

### 고치는 법 — 셋 다 싸다

```
⑴ 클라이언트 타임아웃 상향        adapter/normalize_run 의 타임아웃 값
⑵ 🔴 HTTP 524 는 상향으로 못 푼다  RunPod 프록시(Cloudflare)의 고정 한계다
     └ 동시성을 낮춰 요청당 지연을 줄이거나, 프록시를 우회(SSH 터널)해야 한다
⑶ 동시 6 → 4 로 낮춘다            처리량은 줄지만 96건이 살아난다
```

**동시성을 올려 시간을 줄이려던 계획은 폐기한다** — 지금 상태에서 동시 8로 올리면
타임아웃이 더 난다.

## 강등코드 배선 — 전부 기각

```
                        오답에 붙음   정답에 붙음
 PREMISE_UNMAPPED           29           29
 CITE_HANG_MISMATCH         11           17     ← 정답에 더 많이 붙는다
 VLM_DOWNGRADE               7            6
```

**어느 코드도 오답을 선별하지 못한다.** 정답·오답에 거의 반반씩 붙는다. 판정을 뒤집는
스위치로 쓰면 손해다. 특히 `CITE_HANG_MISMATCH` 는 정답에 더 많이 붙어 **순손실**이다.

## 검색 — 길이 절벽은 전수에서도 유지된다

```
정답청크 «최장» 길이 구간별 근거적중률 (295문항)
 ① ~500자      48/76   63.2%
 ② 500~1000    33/51   64.7%
 ③ 1000~1500   57/121  47.1%
 ④ 1500~2500   10/45   22.2%
```

```
문서별 근거적중률
 재도전성공패키지(11차)        12/16   75.0%
 창업도약패키지(2025)          13/19   68.4%
 L1 통합관리지침 제14차        73/112  65.2%
 모두의 창업 프로젝트           14/27   51.9%
 예비창업패키지(2025)          17/33   51.5%
 붙임1. TIPS 총괄 운영지침       3/21   14.3%   🔴 최악
```

## 4-way 혼동 (정답→예측 · 상위)

```
 57  불가 → 판단불가      ← 실패 96건이 여기 대부분 들어간다
 21  불가 → 조건부
 20  가능 → 조건부
 18  조건부 → 판단불가
 17  가능 → 판단불가
 13  조건부 → 불가
  9  판단불가 → 조건부
  1  조건부 → 가능        ← 유일한 치명
```

## 🔴 이 수치를 어떻게 읽어야 하나

**44.7% 를 「판정 성능」으로 인용하면 안 된다.** 295문항 중 96건(32.5%)이 타임아웃으로
판정을 아예 못 받았다. 그 96건을 뺀 199문항 기준으로 다시 재야 «모델의 판정력» 이 나온다.

```
다음 run 이 잴 것
 └ 타임아웃을 고친 뒤의 «진짜 기준선»
 └ 그때 비로소 프롬프트 A/B 의 +3문항이 잡음 위로 올라온다
```

