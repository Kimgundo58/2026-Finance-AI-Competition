# 효민님께 — 회신 초안 (2026-09-06, ai-d9 작성 · 발송은 오너)

🔴 이 파일까지가 이 세션 몫입니다. 실제 전달은 오너가 합니다.

기준 커밋 — 효민님 문서는 `6921512` 기준이고, 저희 쪽 HEAD 는 그 뒤로 **43커밋** 더
있습니다(`git log --oneline 6921512..HEAD | wc -l` 로 직접 셌습니다). 아래 항목 중
"이미 고쳤다"는 전부 그 43커밋 안에서 됐습니다 — 문서 기준 시점엔 안 보이던 것들입니다.

---

## 이미 고쳤습니다

### 5-1. 협약총액

먼저 정정합니다 — 효민님 문서에서 "한 줄짜리 문제"로 보셨을 텐데, 실제로는 그게
아니었습니다. `tenant.f_profile` 조회 코드가 **DB 에 없는 컬럼**(`협약총액`)을 물었고,
Postgres 에서 없는 컬럼을 물으면 그 트랜잭션이 그 자리에서 **abort** 됩니다. 저희 판정
경로가 그 뒤로 같은 트랜잭션에서 검증·전제해소·증빙 조회·판정 기록을 전부 이어서
하는 구조라, 이 한 군데 실패가 **그 뒤 전부를 조용히 빈 값으로** 만들고 있었습니다.
각 함수가 자기 예외를 삼켜서 밖에서는 "그냥 값이 없나 보다"로 보였습니다.

고친 것: 실제 컬럼(협약시작일·협약종료일·정부지원_현금·자기부담_현금·과업범위요약)으로
바꾸고, 실패해도 트랜잭션을 그 자리에서 롤백해 뒤가 안 죽게 했습니다. 이제 협약기간·
정부지원액이 판정 LLM 에 정상적으로 실립니다. (커밋 `99c333f`)

### 5-2. 전제해소 f값

이것도 관련된 결함이었습니다 — 판정 전 "이미 아는 정보는 다시 안 묻는" 로직(즉시검증)에
정작 그 정보(f값)를 넘기는 코드가 빠져 있어서 **즉시검증이 항상 0건**이었습니다. 사용자가
이미 등록해 둔 정보까지 매번 다시 물었다는 뜻입니다. 이것도 5-1 과 같은 원인(없는 컬럼→
트랜잭션 abort)에 걸려 있어서, 5-1 을 고치기 전에는 이 수정 하나만으론 안 붙는 상태였습니다
— 둘을 같이 고쳐야 실제로 동작했습니다. (커밋 `bdd6d25`, 파급이 `99c333f`)

### CORS

열렸습니다 — 배포 리비전 00018 에서 `OPTIONS` 요청에 200 과 `Access-Control-Allow-Origin`
헤더가 나오는 것을 curl 로 직접 확인했습니다.

---

## 곧 갑니다

### 2-3. 심층질문 축소판

레인 A·B 가 진행 중입니다. **하위항목 분리는 하지 않습니다** — 이건 저희 쪽 오너 결정입니다
(세목 단위까지 쪼개는 건 이번 범위 밖으로 정했습니다).

참고로 하나 짚어두겠습니다 — 지금 서버 정본 비목 어휘는 "기계장치"이고 프론트 쪽에서
"기계장치비"로 쓰시는 자리가 있는 걸로 압니다. 이미 별칭 흡수 로직(`corpus.item_vocab`
기반 + 공백 접기)이 배선돼 있어서 지금 당장 터지는 건 아니지만, 2-3 작업하시면서 새
필드를 만드실 때는 **서버 쪽 enum(`비목_ENUM`, 10종 고정)을 기준으로 계약**해 주세요 —
프론트 표기를 새로 하나 늘리면 그 별칭 표에 없는 이상 다시 같은 문제가 날 수 있습니다.

---

## 마감 후로 미룹니다

5-3 · 5-4 · 2-1 · 2-2 — 이번 마감 안에는 손 못 댑니다. 순서만 이렇게 잡아뒀다는 것만
먼저 알려드립니다.

---

## 오너가 결정할 부분

### 2-4. 회원가입 계정

"자동 등록이 정답은 아니다"라고 하신 지적, 저희도 그대로 동의합니다 — 이 판단이 맞습니다.
이건 정책 결정이 필요한 사안이라 오너가 직접 정하기로 했습니다. 결정되는 대로 다시
알려드리겠습니다.

---

산출: 이 문서. 발송(효민님께 전달)은 오너 몫입니다 — 저는 여기까지입니다.
