# 인수인계 — 새 중앙 세션이 «이것만» 읽으면 된다 (2026-09-06)

작성: ai-36 (전 관리세션). 아래는 전부 이 날 DB·코드에서 «직접 재서» 나온 것이다.
추측은 「추정」이라고 표시했다.

## 1. 지금 성능

```
run 195 (2026-09-05 22:54 저장) — 최신. 전량 295문항 · V0 · 스트리밍 ON
   판단불가  19 / 295      (직전 run 194 는 98)
   적중     181 / 295 = 61.4%   (run 194 는 132 = 44.7%)
   짝지은 비교  개선 59 · 악화 10 · McNemar 정확검정 p = 1.38e-9
   -> 96건 실패는 모델 탓이 «아니라» 125.8초 Cloudflare 프록시 단두대였다. 증명됨
```
```
남은 오답의 모양 (run 195 혼동행렬)
   정답\예측   불가  조건부  가능
   불가        114    34     0
   조건부       11    57     1
   가능          1    29     8    <- 정답이 「가능」인 42건 중 8건만 맞음
   -> 「조건부」로 새는 것이 74건. 이게 가장 큰 덩어리다
```

## 2. 🔴 오늘 뒤집힌 진단 — 어제 계획은 폐기됐다

```
어제 가설   "룰이 «불가» 를 말할 어휘가 없어서 조건부로 쏠린다 -> 룰 74행을 채우자"
오늘 실측   틀렸다. 근거 셋:

 ① 룰이 «안 붙은» 문항도 똑같이 조건부로 쏠린다
    조건부 예측 비율  룰 있음 49.2%(n=240) vs 룰 없음 36.1%(n=36) · p≈0.13 유의하지 않음
 ② rules.허용 은 «칸당 값 하나» 인데 (사업x비목) 81칸 중 62칸에서 정답이 갈린다
    -> 채점대상 258 중 229건(89%)에 대해 답을 담을 그릇이 아니다
 ③ 금지예시는 이미 «차 있다» — 72/74행 · 294개(무조건 262)
    그런데 scripts/_work/_B_금지매칭_감사.json: 자기재현 264/264(100%) · 골든셋 0/77(0%)
    -> 데이터 부족이 아니라 «매칭» 이 안 붙는다
```

```
🔴 진짜 원인 (코드 두 곳에서 찾았다)
   scripts/assemble_context.py:41   B0 정의
       "조건부 = 허용되나 조건·한도·사전승인·«증빙» 이 붙는다"
   scripts/rule_lookup.py:746       B4문장
       매 문항에 "증빙 N종을 갖춰야 한다" 를 넣는다. 증빙은 rules 74/74행에 있다
   => 프롬프트가 매번 「조건부」를 지시하고 있다. 모델은 시킨 대로 했다

   정답지의 실제 기준은 다르다
       「해야할일」 보유율: 조건부 96% · 가능 26% · 불가 86% · 판단불가 56%
       즉 조건부 = «사용자가 더 해야 할 일이 남았다» 이지 «규정에 조건이 있다» 가 아니다
```

## 3. 확정된 원칙 (오너 지시 · 재논의 금지)

```
🔴 만들 때는 corpus.doc_articles / _tables.json 만이 재료다
   잴 때만 eval.golden_set 을 쓴다. 절대 섞지 않는다
   작업 «범위» 도 코퍼스가 정한다 (골든셋이 안 물어본 규정도 사용자는 묻는다)
🔴 rules.허용 칸은 «채우지 않는다» (낟알이 틀렸다)
🔴 상식 별칭(맥북->기계장치)은 만들지 않는다. LLM 이 이미 안다 (실측 65.1%)
🔴 세목/item_vocab.하위항목 은 만들지 않는다 (제품이 세목에 관여 안 함)
🔴 TIPS 는 우리 비목 10종으로 «매핑하지 않는다». 자기 비목 그대로 싣는다
🔴 골든셋을 «고치지 않는다». 의심 목록만 낸다. 고칠지는 오너 결정
   고치면 그 전후를 «다른 run» 으로 기록한다
```

## 4. 오늘 바뀐 것

```
CLAUDE.md:94   「스캔 판독본 신뢰등급 A 인용 금지」를 «해제» 했다 (오너 승인)
               재도전 현행판이 vlm 인데 판독이 깨끗하고, 금지를 지키면 그 사업이
               통째로 검색에서 빠진다 (retrieve.py:41 이 parse_quality='high' 만 본다)
「선택필요」     채점·골든셋에서 «제거» 한다 (오너 결정)
               제품은 「AI 가 비목 추천 -> 사용자가 최종 클릭」 구조다
               제품 기능(orchestrate.py:627 이 두 갈래를 각각 판정)은 «정상»,
               채점기가 못 따라갔던 것이다. eval_e2e 채점을 1순위 갈래 기준으로 고친다
               (골든셋에 정답='선택필요' 는 0건이라 지울 «행» 은 없다)
```

## 5. 발견했으나 «아직 안 고친» 결함

```
🔴 recheck_queue 미처리 355건 (2026-08-31 적재 · 처리자 없음)
   BASIS_AMENDED rule 186 · check_item 75 / ITEM_VOCAB_RECHECK item_alias 57
   BASIS_RENUMBERED 9 / STRICTER_L3 7 / UPSTREAM_LAW_AMENDED 15
   -> 룰 74행 중 «71행» 이 「근거 조가 개정됐다」로 대기 중
   -> 생산자(scripts/archive/agents/agent_a4.py)는 정상 작동. 소비자가 없다
   🔴 룰 작업의 «선행조건»: 186건이 진짜 개정인지 표본 확인부터

🔴 어휘 재료 6개 중 5개가 «표가 뭉개진 채» doc_articles 에 있다
   창업중심대학 참고3 만 마크다운 표 60행으로 정상. 나머지는 표행 0
   🟢 재파싱 불필요 — _tables.json(22문서·469표)에 셀 단위로 이미 있다
   table_splice.py 로 넣기만 하면 된다 (0단계)

🔴 item_alias 228행 중 25행이 «골든셋에서» 만들어졌다 (출처='골든셋NN')
   맥북·항공권·월급 같은 상식 매핑이고, CLAUDE.md 의 정답 유출 금지 위반
   오염 크기는 쟀다: 걸리는 33문항 87.9% vs 나머지 225문항 81.8% (약 2문항어치)
   나머지 203행도 출처가 «칸 이름»(허용예시·비목명종합)이라 원문 대조가 불가능
   -> 전면 재작성 대상. 단 새 표가 82.6% 를 넘는지 확인한 «뒤» 교체

🔴 정답지 315행 중 verified=false 가 202건 (64%)
   본세트 228건 중 194건이 «한 번도 검수 안 됨» -> 전수검사 표적
   그중 104건은 근거가 표 조항이라 0단계 후에 봐야 한다 (나머지 90건은 지금 가능)

🔴 xref_mismatch 0행 — 표만 만들고 채우는 코드를 «안 붙였다»
   (audit_db.py:71 "build_refs 가 기록하지 않는다 (미적용)")
   0행은 「불일치 없음」이 아니라 「검사 안 함」이다

🔴 parse_quality 태깅이 재적재하면 초기화된다
   load_db.py 가 extraction 을 계산하지 않고 받은 값을 넘긴다 -> 전부 native 로 복귀
🔴 index_target=True 인데 parse_quality='low' 라 영원히 검색 안 되는 문서 7건
   (retrieve.py:41 은 index_target 을 «보지 않는다»)

🔴 잡음 바닥이 0 이 아니다 — >= 4/295 = 1.4%
   194 vs 195 에서 경로·근거적중·게이트값(소수점 16자리)이 «완전히 같은데» 판정이 뒤집힌
   문항 4건 (gold 370·393·542·555). vLLM 연속배칭은 결정적이지 않다
   -> 부분셋 측정을 접고 전량 295 로 가는 근거. m 이 작으면 대부분 잡음일 수 있다
```

## 6. 만들어 둔 도구 (전부 돌려서 확인함)

```
scratchpad/룰검산.py           제안 JSON 의 원문발췌가 조 본문에 «문자 그대로» 있는지 대조
                               + rule_id 실재 · 값 형식 · 단위 12자 · 「불가」 근거 문구
                               --계수 로 골든 도달 n 과 「n<=5 면 못 잰다」 경고
                               🔴 이 도구에 ✅ 는 없다. 최선이 ⚠️ 다 (반대는 증명 불가)
scratchpad/어휘슬라이스.py      골든 295를 층화분할 -> 어휘_문항_{A..D}.json + 채점키 별도
scratchpad/어휘채점.py          붙인 라벨 vs 정답. 재현율 + 혼동행렬
scratchpad/어휘_문항_A~D.json   정답 «가린» 문항집 (시험 전용)
scratchpad/어휘_채점키_A~D.json 🔴 작업 세션은 열면 안 된다
```

## 7. 계획 문서

```
scratchpad/최종계획_0906.md      전체 계획 (레인·순서·의존)
scratchpad/어휘재료_확정_0906.md  어휘 재료를 «조번호까지» 확정한 목록
scratchpad/세션지시_0906.md      각 세션에 붙여넣을 프롬프트   <- 여기부터 쓰면 된다
```

## 8. 중앙이 «직접» 할 일 (세션 안 띄우고)

```
① recheck_queue BASIS_AMENDED 186건 표본 확인   <- 룰E 의 선행조건
② 골C 구조 결함 목록 (쿼리로 끝난다)
   비목 NULL 56 · 정답근거 없음 3 · 근거원문 없음 4 · 실패-우연정답 6
   TIPS 비목 태깅 23건 (지급수수료·광고선전비는 TIPS 에 «없는» 비목이다)
③ 산출물 전부 룰검산.py 로 원문 대조 -> 교차 확인 -> DB 반영 (여기서만 쓰기)
④ 마지막: 같은 조건 2회로 «잡음 영점» 을 먼저 재고, 그 위에서 변경분을 잰다
   채택 기준은 돌리기 «전» 에 못박는다: 일치율 +3문항 · 치명 0 · 판단불가율 != 0%
```

## 9. DB 접속
```bash
PYTHONIOENCODING=utf-8 python -c "
import sys; sys.path.insert(0,'scripts/_lib'); import db
with db.connect() as c, c.cursor() as cur: cur.execute('...')"
```


---

# 10. 0906 실행 결과 (인수인계 이후 추가)

```
0단계 표 복구  조 8개 · 표행 0 -> 258 · 골든 축자적중 60 -> 69 · 잃은 것 0건
               검산 파일: scratchpad/0단계_검산.json
어C 어휘       scratchpad/어휘제안_어C.json  12건 (지급수수료 11 · 광고선전비 1)
               검산: scratchpad/어휘검산.py  (룰검산.py 와 스키마가 달라 «별도 파일»)
골B 라벨정의   scratchpad/골B_라벨정의.md
               🔴 「조건부 = 해야할일이 남았다」는 «틀렸다». 해야할일 유무는 결과지 원인이 아니다
                  가능   = 요건이 질문의 사실로 이미 충족 · 남은 건 증빙 formality
                  조건부 = 요건이 사용자 사실에 따라 성립/불성립이 갈리는 이분 판정
인F            폐기. L3_시연적재_안내.md §5-3 에 HWP 변환이 이미 검증돼 있다
```

## 새로 찾은 결함
```
🔴 창업도약 제30조: "비목은 «지침 제33조제1항»을 따른다"
   현행 통합관리지침 제33조 = 「보조사업점검평가단」 (전혀 다른 조)
   진짜 비목 조 = 제36조.  recheck_queue BASIS_RENUMBERED 의 실사례
🔴 gold 353: 지출건이 아니라 "비목 규정이 뭐냐" 는 메타 질문. 라벨이 성립하지 않는다
🔴 L3 층 혼동 주의: 「창업중심대학 세부관리기준」은 창진원 발행 = L2 다.
   그걸 L3 로 적재하면 같은 규범이 두 층에 앉아 precedence(L2>L3) 가 무의미해진다
   L3 는 «주관기관(대학) 자체» 규정이어야 한다
```
