# 문서 반영 «후보» (2026-09-06 · 중앙 ai-4a) — 아직 고치지 않았다

CLAUDE.md 의 새 규칙(「작업이 끝나면 오너에게 md 최신화할까? 를 묻는다」)을 탄다.
여기 적힌 것은 «제안» 이고, 오너 승인 전에는 docs/ 를 건드리지 않는다.

## 1. docs/6_LLM/6-3_평가와_정답셋.md — 한 줄 추가

> **`설정.정답고정.지문` 이 다른 두 run 은 hit@k·일치율을 빼면 안 된다.**
> 코퍼스가 같아도 정답 청크 집합이 다르면 같은 것을 잰 게 아니다.
> 지문이 «없는» run(≤195)은 이 지문이 생기기 전 run 이다 — 소급해 채우지 않는다.

근거 — 0단계 표복구 실측
```
run 194 정답고정 = "eval.golden_chunks(D3)"
run 195 정답고정 = "eval.golden_chunks(D3)"    글자 하나 안 다르다
그 사이/이후 golden_chunks 404행 -> 399행, gold 380·389 가 조번호 -> 원문일치
= 정답 청크 집합이 달라졌는데 라벨은 그대로였다
run 196 정답고정 = {"표":"eval.golden_chunks(D3)", "지문":"g399-f6-q315-144be751"}
```

## 2. 코드 변경 기록 (커밋 메시지에 넣을 근거)
```
scripts/eval_store.py   골든고정(cur) 신설 — (총행, 실패행, gold_id 종수, max gc_id) + 매칭방법 분포 해시
scripts/eval_e2e.py     2줄 — 지문 계산 1줄(conn 열려 있을 때) · 설정.정답고정 1줄
검증                    run 196 (--dry --limit 3 --라벨 지문검증_dry) 저장된 행을 다시 읽어 확인
                        194·195 는 안 건드렸다 (그날의 기록)
```

## 3. 아직 «안 고친» 같은 결함 — 판단 필요
```
scripts/archive/eval/sweep_retrieval.py:191
    "정답고정": "eval.golden_chunks(D3)"   <- 여기도 라벨뿐이다
    이쪽이 재는 것이 바로 hit@k 라 같은 위험이 있다.
    이번 지시 범위(eval_e2e 한 줄)에 없어서 손대지 않았다
    🔴 이 미처리가 «지금» 만드는 위험:
       sweep_retrieval 로 잰 hit@k 는 정답고정 지문이 없으므로 0906 전후를 비교할 수 없다
```

## 4. 미결로 올린 것 (docs/9_미결.md 후보)
```
· scope.범위밖_조() 가 모두의창업에서 오작동 — 「제3편 로컬트랙」 마지막 매치가
  제52조(해석) 본문 «속 언급» 이라 컷이 별지1·2·5 를 범위 밖으로 만든다.
  공용 컷(tag_apply_target·build_refs·build_precedence)이라 단독 수정 불가
· 모두의창업 별지3·4 가 doc_articles 에 없다 — gold 387·388 영구 실패
· 모두의창업 별지5 / TIPS 별지1·별첨1~3 표 복구 미적용 (이번 판 «미룸», 뺀 게 아니다)
· extract_tables.run() 이 _tables.json 을 통째로 덮어쓴다 (--doc 으로 좁히면 나머지가 날아간다)
· extract_tables 가 4분면 조판에서 표를 읽는 순서로 안 담는다 (0단계에서 5문서만 교정)
· eval.golden_chunks CHECK 가 청크 DELETE 를 막는다 — 재청킹 절차에 핀 걷기/재고정이 필요
```
