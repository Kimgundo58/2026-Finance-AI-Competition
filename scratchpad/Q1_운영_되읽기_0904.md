# Q1 — 운영 실사용자 흐름 실측 (2026-09-04, ai-d7)

중앙(ai-c4) 승인 하 실행. 스크립트는 세션 scratchpad(레포 밖)에만 있다.
DSN: `suddoe-dsn-run`(비특권 `suddoe_app`)을 `/cloudsql/...` 유닉스소켓 대신
`34.64.37.177:5432?sslmode=require` 로 바꿔 이 PC에서 직접 붙였다. psql 없음 — psycopg 로만.

## 관측 — 비특권 롤 저장 되읽기 (item4, 됐다)
3라운드(plan_id 23268/23269/23270) 전부: `POST /api/plans`(실 Supabase 토큰) 201 →
**새 연결**(suddoe_app · GUC=org) 재조회 before=0→after=1, 행 내용 일치 → 정확히 그
plan_id 로 DELETE → 새 연결 재조회 0건 복원. rowcount·200 아니라 새 트랜잭션 재조회로 확인.

## 관측 — 운영 정규화·판정 LLM 이 둘 다 실패한다 (item6 블로커, GPU/Q2 소관)
`/api/gpu/status` = `가동`(팟 컨테이너는 떠 있다는 뜻)인데:
- `POST /api/normalize` → `event: 오류 {"메시지":"정규화에 실패했습니다","종류":"LLM실패"}`
- `POST /api/judge`(정규화 dict 수동 채움, ④만 시험) → `판정:판단불가`
  `요약:"판정 모델 호출에 실패해 결론을 내리지 않았습니다."`
- `tenant.decisions` 새 연결 재조회 3회 전부 NULL — **저장은 정직하게 0건으로 닫혔다**
  (`저장:false 사유:"decision_id 없음"`) → 가짜 판정 없음. 긍정적이지만 item6(decisions
  1행+강등코드) 자체는 vLLM 복구 전까진 못 채운다.

## 관측 — org_id 자기신고 폴백 누수 (감시 ai-a7 제기, 음성)
경상국립대 org 에 plan 이 실재하는 상태에서 무토큰 `GET /api/plans?org_id=cfeba091-...`
→ `{"건수":0,"항목":[]}`. 원인: `server/auth.py:356` 이 자기신고(param 출처)엔
GUC(`app.org_id`)를 일부러 안 세우고, RLS(`org_id=current_org()`)가 GUC=NULL 이라
WHERE 절 일치와 무관하게 전부 막는다. → **지금 코드로는 이 경로로 안 샌다** (실데이터로 확인).

## 참고 (내 소유 밖, Q5/데모 쪽에 남긴다)
`tenant.orgs`/`org_programs` 의 cfeba091(경상국립대 **창업중심대학**사업단) 사업명이
`['모두의 창업 프로젝트']` 뿐이다 — org 이름과 등록 프로그램이 어긋난다.

## 이미 닫혀 있던 것 (오늘 재확인만)
`_질의_저장`(9b97694, 503→401/403/503/500 분해) · `test_교차점_비특권_판정저장.py`
(23b05e1, 비특권롤×판정경로 교차) — 로컬 실 DB·suddoe_app 롤로 `10 passed`.
