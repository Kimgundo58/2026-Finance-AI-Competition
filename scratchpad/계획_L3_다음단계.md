# 다음 주력 = L3 축 (2026-09-06 오너 결정 ③)

## 오너 결정 반영
```
① 주관기관 화자 17문항  «제외한다. 문서에 따로 명시하지 않는다» (오너: 쓸모없는 문항)
   실행: eval.golden_set 의 해당 17행을 평가범위='범위밖' 으로. 각주·주석 달지 않는다
   🔴 다만 «기계가 읽는 자리 한 곳» 에는 남긴다 — eval.runs.설정 의 분모 필드.
      안 남기면 run 195(295) 와 다음 run 이 «다른 자» 로 재진다. 문서 각주가 아니라 데이터다
② GPU  지금 안 연다. 고칠 것 다 고치고 «돌릴 때» 연다 (오너 확정)
③ 다음 주력  L3. 재료는 2024 창업중심대학 경상국립대 실물 2건
```

## 🔴 시작하기 전에 — 실측 (2026-09-06 · 내가 직접 떴다)

```
tenant.l3_articles  총 224조각 / 27,384자 / 평균 122자 / 최대 277자
  4add2b34  203조각  🔴 조제목이 «203개 전부 NULL»   <- 사업비 사용 안내문 (본문)
  5a896636    4조각  🔴 4개 전부 NULL                <- 사업비 변경신청서 «양식»
  72747055   11조각  조제목 있음                      <- 시험용
  9a4cac53    6조각  조제목 있음                      <- 시험용

🔴 eval.golden_set 의 L3 문항 = «0건»
   => 우리는 L3 판정 품질을 «한 번도 잰 적이 없다».
      지금까지의 정확도 숫자는 전부 «우리가 조달한 L1·L2» 로만 잰 값이다
```

## 왜 이 순서인가

```
자를 먼저 만들지 않으면, 고쳐도 «좋아졌는지» 를 말할 수 없다.
그리고 이 문서는 조문 형식이 «아니다» — 정답 축을 조번호로 둘 수 없다.
그래서 「골든셋 만들기」가 단순 노가다가 아니라 «설계» 다.
```

## 레인 분해 (병렬 가능)

```
[1군 — 서로 안 겹친다. 동시에 돈다]

L3-A  «재는 자» 만들기 — 골든셋 L3 문항
   재료: 4add2b34 (사업비 사용 안내문 · 203조각)
   🔴 정답 축을 «조번호로 두지 마라». 이 문서는 자기 조가 0개다
      -> 정답근거 = article_id (단락 단위). 근거원문은 그 조각 본문 «축자»
   🔴 「[첨부1~4] 비목별 집행기준·증빙서류」 구간에서 «우선» 뽑아라.
      판정이 실제로 쓰는 구간이고, 09-03 파서 수정으로 «되살아난» 자리다
   목표 30~40문항. 판정 갈래를 골고루 (불가·조건부·가능)
   산출 {질문, 정답판정, 정답근거:article_id, 근거원문, 비목, 검수메모}
   🔴 골든셋에 «넣지 마라». 파일로만 낸다. 적재는 오너 승인 후 중앙이 한 번에

L3-B  청킹 단위 재검토
   실측: 203조각 · 평균 122자 · 최대 277자 -> «너무 잘다»
   물음: 이 단위로 검색·통째로드가 성립하나. 조각 하나가 판정 근거로 «충분한가»
   🔴 L3 는 «검색 없이 통째 로드» 가 원칙이다(CLAUDE.md). 그럼 청킹이 왜 필요한지부터
      -> 통째 로드면 조각 수는 무관하고 «순서와 누락» 만 문제다. 그걸 재라
   산출: 원본 HWP 대비 «순서가 맞나 · 빠진 구간이 있나» 전수 대조

L3-C  서식 파일 판별
   5a896636 = 「변경신청서 양식」. 조 0개가 «정상» 인데 파서가 규정 문서로 취급한다
   물음: 규정 / 서식 / 참고자료 를 «업로드 시점에» 가르는 신호가 무엇인가
   🔴 지어내지 마라. 실물 2건을 열어 «실제로 다른 것» 을 찾아라
   산출: 판별 규칙 후보 + 각 규칙이 실물 2건을 맞히는지

L3-D  파싱품질을 사용자에게 «보인다»
   GET /api/l3/{doc_id} 응답에 파싱품질이 «안 실린다» (미결 문서)
   -> 16% 손실 때 화면엔 「35개 조항 인식됨」으로 보였다. 아무 예외도 안 났다
   산출: 응답 스키마 + 화면 문구안. 🔴 「깨졌다」가 아니라 «무엇이 빠졌는지» 를 말해야 한다

[2군 — 1군이 끝나야 시작한다]

L3-E  레인 Q 설계 -> 구현 (필터·자르기)
   Q 산출: 최악결합 42,856 -> 28,785토큰(70.3%) · 발동조건 못박음
   🔴 레인 S 발견을 반드시 반영 — 조제목이 None 이라 «제목 매치 안전판이 안 켜진다».
      이 org 에선 위험 완화가 «한 겹 얇다»
   🔴 태깅이 틀리면 제약이 «조용히 사라진다» = 「틀린 가능」. 이 제품에서 제일 나쁜 방향

L3-F  L3-A 골든셋으로 «측정»
   기준선부터. 고치기 «전» 값을 먼저 남긴다
```

## 순서

```
지금 큐   R확인 -> 룰반영 -> PK -> alias칸 -> case드랍 -> 영점 2회 -> 측정 1회
          🔴 이 측정은 «L1·L2 축» 이다. L3 와 섞지 않는다

그 다음   L3-A ∥ L3-B ∥ L3-C ∥ L3-D        <- 4레인 동시
그 다음   L3-A 오너 검수 -> 골든셋 적재
그 다음   L3-F 기준선 측정 (고치기 «전»)
그 다음   L3-E 구현 -> 재측정
```

## 🔴 이 계획이 «안 하는» 것

```
· L1·L2 룰을 더 파지 않는다 — 검색 미스에서 고칠 수 있는 건 9.5% 뿐이다
· A12 프롬프트 6변형 A/B 를 지금 하지 않는다 — 잡음 바닥 1.4% 라 부분셋 차이가 잡음에 묻힌다
· 주관기관 화자를 지원하지 않는다 (오너 결정 ①)
· GPU 를 지금 열지 않는다 (오너 결정 ②)
```
