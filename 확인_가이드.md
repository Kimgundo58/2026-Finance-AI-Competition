# 확인 가이드 — 무엇을 어떻게 점검하나

작성 2026-08-13 · **개정 2026-08-27** · 개발 지식 없이 따라 할 수 있게 씀

> # 🔴 먼저 읽을 것 — 이 문서의 숫자는 지금 기준이 아니다
>
> 2026-08-24 에 **데이터셋이 통째로 교체됐다.** 구 74파일(`규정문서/`, `법령 PDF/L2_`~`L4_`)은
> `archive/` 로 갔고, 현행 코퍼스는 `2026_Finance_DATA_FOR_RAG/` **109건** + 법령 XML 1,349건이다.
>
> **그런데 DB(§2)와 검색 평가(§3-2-1)의 숫자는 전부 교체 이전에 만든 것이다.**
> 그 안에는 지금은 인덱싱 대상이 아닌 문서(타 대학 규정 24건 등)가 섞여 있다.
>
> | 지금 이 문서로 할 수 있는 것 | 할 수 없는 것 |
> |---|---|
> | 점검 **절차**를 익히는 것 (SQL·스크립트 사용법은 그대로 유효) | 표의 숫자를 "정상값"으로 믿는 것 |
> | 신규 코퍼스 재인덱싱 후 **같은 절차로 다시 재는 것** | 지금 DB 상태를 합격/불합격으로 판정하는 것 |
>
> **다음 큰 작업은 신규 코퍼스 재인덱싱이고, 그 전에 청킹 단위(조 vs 항)를 확정해야 한다**
> (`구현.md` §1-1). 재인덱싱이 끝나면 §2-2·§3-2-1 의 숫자를 새 값으로 갈아끼운다.

이 문서는 **"제대로 됐는지 내가 어떻게 아나?"**에 답한다. 네 부분이다.

1. [GitHub에서 확인할 것](#1-github)
2. [DB에서 확인할 것](#2-db)
3. [RAG 품질 확인](#3-rag-품질)
4. [지금 완성된 것 / 아직 안 된 것](#4-현재-완성도)

---

## 1. GitHub

**저장소:** https://github.com/Kimgundo58/2026-Finance-AI-Competition

### 폴더 구조로 보는 법

| 위치 | 무엇 |
|---|---|
| `프로젝트설명.md` | **가장 먼저 읽을 것** — 배경·도메인·레이어(L1/L2/L3) 정의·데이터 자산 |
| **`서비스 아키텍쳐.md`** | 🔴 **시스템 구조·요청 흐름. 충돌 시 이 문서가 우선** (2026-08-27 창업팀 전용 전환) |
| **`작업 현황.md`** | 일자별 작업 로그 · 맥락 변화 이력 |
| `구현.md` | RAG 설계와 기각 사유. §8-5 에 현재 구현 상태 |
| `법령_크롤링_현황.md` | L1 법령 코퍼스 상태 · law.go.kr API 함정 |
| `2026_Finance_DATA_FOR_RAG/` | **현행 데이터셋 109건** (중기부 9 · 창진원 44 · PMS 42 · 사례집 11 · 골든셋 1 · FAQ) |
| └ `DATASET_STRUCTURE.md` | **코퍼스 신분증** — 파일별 내용·쪽수·파싱 주의사항 |
| `법령 PDF/L1_법령/` | 법령·행정규칙 XML 267 + 연혁 944 + 첨부 35 + 별표 103 |
| `건국대학교 레퍼런스/` | L3 샘플 (주관기관 규정 실물) |
| `RAG.md` / `rule_base.md` / `LLM.md` / `Agent.md` | 축별 설계 명세 (2026-08-28 신설). 구 `Rag_Agent구현 파이프라인.md` 를 대체 |
| `룰테이블_검수가이드.md` | **D단계 할 일** — 룰 19행 검수 절차 |
| `db/` | 로컬 DB 구성 (compose + 스키마 9테이블 + 사용법) |
| `scripts/` | 파이프라인 코드 (24개) |
| `archive/` | 구 데이터셋·구 문서. 이동 사유는 `_이동내역.json` |
| `_hwp변환/` | HWP → PDF 한컴 변환본 33건 |
| ~~`manifest.json`~~ | ⚠️ **쓰지 말 것.** 구 데이터셋(405파일·구 L1~L4 스킴) 기준으로 동결됐다 |

### 커밋 이력으로 보는 법

**Commits** 탭을 누르면 무엇이 언제 들어갔는지 보인다. 각 커밋 메시지에 **무엇을 왜 고쳤는지**가 적혀 있다.

| 커밋 | 날짜 | 내용 |
|---|---|---|
| Initial commit | 08-13 | — |
| 데이터셋 + 파이프라인 문서 | 08-13 | 74파일 + 설계 명세 |
| 로컬 개발 DB 구성 | 08-13 | Docker Compose + 스키마 9테이블 |
| Stage 0 파서 + Stage 2 인덱서 + D단계 도구 | 08-13 | 실제 코드 |
| 파서 정확도 수정 + 검색 평가 | 08-13 | pdfplumber 전환, 평가 스크립트 |

> ## ⚠️ 8월 13일 이후 작업이 GitHub 에 없다
>
> 8월 20~24일에 한 작업(법령 크롤링 전량, 신규 데이터셋 109건, 구 데이터셋 폐기, HWP 변환,
> 사례집 OCR, 스크립트 13개 추가)이 **전부 미커밋 상태**다.
> 워킹트리 기준 신규 **309** · 삭제 **70** · 수정 **7**.
>
> 지금 GitHub 저장소만 보면 **8월 13일에서 멈춘 프로젝트로 보인다.** 커밋이 필요하다.
> (커밋 전에 확인: 법령 XML 만 500MB 가 넘는다 → `.gitattributes` 의 LFS 설정 또는 `.gitignore` 검토)

### 확인 포인트

- [ ] 최상위에 `.md` 문서 11개가 보이는가
- [ ] `scripts/` 안에 파이썬 파일 24개가 있는가
- [ ] `2026_Finance_DATA_FOR_RAG/` 가 있고 그 안에 `DATASET_STRUCTURE.md` 가 있는가
- [ ] `법령 PDF/` 안에 `L1_법령/` 이 있는가 (L2~L4 폴더는 `archive/` 로 옮겼으므로 **없는 게 정상**)
- [ ] 커밋 메시지가 한글로 무슨 작업인지 설명하는가

> ⚠️ 저장소가 **public** 이다. 비공개로 바꾸려면 Settings → 맨 아래 Danger Zone → Change visibility.

---

## 2. DB

### 2-1. 켜져 있나 — Docker Desktop

**Containers** → `suddoe` 그룹 → `suddoe-db`, `suddoe-pgweb` 둘 다 **Running**(초록)이면 정상.

> 2026-08-27 확인 시점에는 **Docker 데몬 자체가 꺼져 있었다.** `localhost:8081` 이 안 열리면
> 먼저 Docker Desktop 이 실행 중인지 본다.

꺼져 있으면 ▶ 재생 버튼. 또는:

```powershell
cd "C:\Users\dogun\...\금융 AI공모전\db"
docker compose up -d
```

### 2-2. 데이터가 들어왔나 — http://localhost:8081

브라우저에서 열면 로그인 없이 바로 연결된다. 왼쪽에서 **`v_적재현황`** 클릭:

> 🔴 **아래 값은 "정상값"이 아니라 2026-08-13 구 코퍼스 스냅샷이다.**
> 구 74파일 데이터셋으로 만든 값이고, 그 안에 **타 대학 규정 24건 등 지금은 인덱싱 대상이 아닌 문서**가 섞여 있다.
> 신규 코퍼스로 재인덱싱하면 전부 바뀐다. 재인덱싱 후 이 표를 새 값으로 교체할 것.

| 테이블 | 구 스냅샷 (08-13) | 뜻 |
|---|---|---|
| 1. documents | 49 | 원본 문서 |
| 2. doc_articles | 1,640 | 조(條) 단위 원문 |
| 3. chunks | 2,374 | 검색용 조각 + 임베딩 |
| 4. case_chunks | 273 | 사례 Q&A |
| 5. rules | 19 | 판정 룰 (D단계 검수 대상) |
| 6. item_alias | 0 | 아직 미구현 |
| 7. decisions | 0 | 판정 로그 (LLM 붙인 뒤 쌓임) |
| 8. golden_set | 0 | 골든셋 (직접 작성 필요) |
| 9. xref_mismatch | 124 | 조문 상호참조 기록 |

**재인덱싱 후 값이 커야 정상인 이유**: 신규 코퍼스는 창진원 8개 사업 44건 + 법령 XML 267건 +
행정규칙 별표 103건이 새로 들어오고, 구 코퍼스의 타 대학 규정 24건은 빠진다.
`documents` 가 49 근처에 머물면 **오히려 재인덱싱이 안 된 것**이다.

### 2-3. 품질 점검 SQL

`Query` 탭에 붙여넣고 실행:

```sql
SELECT 'L2 조 개수(80↑ 정상)' AS 항목, count(*)::text AS 값
  FROM doc_articles WHERE doc_id LIKE 'L2_%제14차%'
UNION ALL SELECT '붙임2 존재(2 정상)', count(*)::text
  FROM doc_articles WHERE 조번호='붙임2'
UNION ALL SELECT '임베딩 누락(0이어야 함)', count(*)::text
  FROM chunks WHERE embedding IS NULL
UNION ALL SELECT '판정검색 대상 청크', count(*)::text
  FROM chunks WHERE status='active' AND parse_quality='high' AND layer IN ('L1','L2','L3')
UNION ALL SELECT '검수 완료 룰', count(*)::text
  FROM rules WHERE verified;
```

| 항목 | 합격 | 불합격이면 |
|---|---|---|
| 통합관리지침 조 개수 | 80 이상 | PDF 추출 실패 — pdfplumber 확인 |
| 붙임2 존재 | 2 | 섹션 분리 실패 |
| **임베딩 누락** | **0** | 0이 아니면 `build_index.py` 재실행 |
| 검수 완료 룰 | (D 진행에 따라) | 19가 되면 D 완료 |

> ⚠️ 위 SQL 의 `doc_id LIKE 'L2_%제14차%'` 는 **구 계층 표기**다. 통합관리지침은 중기부 발행이라
> 현행 기준으로는 **L1** 이다. 재인덱싱 시 `doc_id` 규칙이 바뀌면 이 SQL 도 같이 고쳐야 한다.
> `layer` 필드는 파일명이 아니라 **발행주체 기준**으로 부여한다 (`프로젝트설명.md` §2-3).

### 2-4. 원문이 안 깨졌나 (제일 중요)

```sql
SELECT 조번호, 조제목, left(text, 120) AS 본문앞부분
FROM chunks
WHERE doc_id LIKE 'L2_%제14차%' AND 조번호 IN ('제39조','제45조')
LIMIT 4;
```

**사람이 읽을 수 있는 문장**이어야 한다. 아래처럼 나오면 실패다:

```
❌ "...집기 가구 기구 등은 집행할 수 없으며 노트북 및 , , , PC("
✅ "제39조(기계장치, 공구·기구) ① 사업계획서 상의 사업화를 위해..."
```

쉼표·괄호·숫자가 문장 끝으로 밀려 있으면 PDF 추출이 잘못된 것이다. §원칙 4(인용은 원문 그대로)가 무너지므로 그 상태로는 쓸 수 없다.

### 2-5. 껐다 켜기 / 초기화

| 하고 싶은 것 | 명령 |
|---|---|
| 끄기 (데이터 유지) | `docker compose stop` |
| 켜기 | `docker compose up -d` |
| **전부 초기화** | `docker compose down -v` → `up -d` |

> 스키마(`db/init/01_schema.sql`)를 고쳤으면 **반드시 `down -v`** 해야 반영된다. 최초 1회만 실행되기 때문이다.

---

## 3. RAG 품질

> ⚠️ **2026-08-27 아키텍처 전환 반영 필요.** `서비스 아키텍쳐.md` 가 판정 흐름을
> LLM 3단계(① 정규화 → ④-a 포섭 스크리닝 → ④-b 조립)로 바꿨고, 인용을 **문장번호(S08) 선택**
> 방식으로 바꿨다. 아래 파이프라인 그림은 구 2단계 기준이다.

### 먼저 — 지금 잴 수 있는 건 "검색"뿐이다

```
질문 → [① LLM 정규화] → [② L3 룰 + 게이팅] → [③ 근거 수집] → [④ LLM 판정] → 답변
         미구현            룰 초안만            일부만          미구현

     ③ 근거 수집 세부
       ③-a L3 통째 로드     ⬜ 미구현 (L3 인덱싱 자체가 아직)
       ③-b L1·L2 검색       ✅ 벡터만 (BM25 미구현)
       ③-c 참조 폐포 수집    ⬜ 미구현 (refs 테이블 비어 있음)
```

"판정이 맞는가"는 LLM을 붙인 뒤에야 측정된다. 지금은 **"근거 조항을 찾아오는가"**만 잰다.

그리고 **아직 벡터 검색만이다.** BM25 + RRF가 안 붙었으므로 지금 점수는 **하한선**이고, 붙이면 올라간다.

### 3-1. 눈으로 — 5문항

```powershell
cd "C:\Users\dogun\...\금융 AI공모전"
python scripts/smoke_search.py
```

질문 5개에 대해 상위 3개 조항을 보여준다. **판단 기준 하나:**

> **"내가 이 조항만 보고 사용자에게 답할 수 있나?"**

| 결과 | 판정 |
|---|---|
| 1등이 정확히 그 비목 조항 | ✅ 좋음 |
| 1~3등 안에 있음 | ✅ 합격 (실제로는 top-8을 씀) |
| 관련 비목인데 다른 조항 | ⚠️ 청킹 문제 가능성 |
| 엉뚱한 비목 | ❌ 문제 |
| **텍스트가 깨져 있음** | 🔴 치명적 |

### 3-2. 숫자로 — 15문항 자동 채점

```powershell
python scripts/eval_retrieval.py
```

| 지표 | 의미 | 합격선 |
|---|---|---|
| **Recall@8** | 정답 조항이 상위 8개(LLM에 넘길 개수) 안에 있는 비율 | **0.85 이상** |
| Recall@3 | 상위 3개 안 | 참고 |
| Recall@1 | 1등으로 맞힘 | 참고 |
| MRR | 정답이 평균 몇 등 (1등=1.0) | 0.6 이상 |

**Recall@8이 왜 상한선인가** — 여기서 못 찾은 조항은 ④ LLM의 컨텍스트에 아예 안 들어간다. 모델을 아무리 좋은 걸 써도 **없는 근거로는 답할 수 없다.**

실패한 문항은 무엇이 대신 나왔는지까지 찍어주므로 원인 진단이 된다:

| 나온 것 | 진단 | 처방 |
|---|---|---|
| 같은 비목 다른 조항 | 청킹 경계 문제 | `MAX_CHARS` 3000 → 1500 |
| 다른 비목 | 임베딩이 도메인에 약함 | bge-m3와 비교 |
| 정확 용어를 못 찾음 | **BM25 부재** | BM25 + RRF 붙이기 |
| 구버전·타사업 섞임 | 필터 버그 | `status='active'` 확인 |

### 3-2-1. 실측 결과 (2026-08-13, 벡터 검색만) — **구 코퍼스 기준, 현재 무효**

| 지표 | 값 | 판정 |
|---|---|---|
| **Recall@8** | **1.00** (15/15) | ✅ 합격선 0.85 크게 상회 |
| Recall@3 | 0.93 (14/15) | ✅ |
| Recall@1 | 0.93 (14/15) | ✅ |
| MRR | **0.947** | ✅ |

15문항 중 14개가 **1위**로 정답 조항을 물어왔다. 나머지 1개("해외 전시회 출장 여비")도 5위 안에 들어왔다.
BM25 없이 벡터만으로 낸 점수이므로 그 시점 기준으로는 **하한선**이었다.

> 🔴 **이 점수는 코퍼스 교체로 무효가 됐다.** 구 74파일 기준이고, 당시 코퍼스에는
> 지금 들어올 법령 XML 267건·행정규칙 별표 103건·창진원 8개 사업 44건이 없었다.
> 후보 문서가 10배 넘게 늘면 Recall 은 거의 확실히 떨어진다.
> **재인덱싱 후 같은 15문항으로 다시 돌려 새 baseline 을 잡을 것.** 그때 이 표를 교체한다.
> (정답 조항 라벨도 `doc_id` 가 바뀌면 같이 고쳐야 한다 — `scripts/eval_retrieval.py` 확인)

### 3-3. 이 15문항의 쓸모

지금 점수는 **기준선(baseline)**이다. 나중에 BM25를 붙이거나 청킹을 바꿨을 때 **같은 15문항으로 다시 돌려 비교**하면 개선 여부가 숫자로 나온다.

§4 운영 규칙: **"뭐든 바꾸면 재실행. 점수 없이 '좋아진 것 같다'로 머지 금지."**

### 3-4. 정식 평가는 골든셋 30문항

이 15문항은 검색만 재는 축소판이다. 정식 평가는 §8의 지표 3개이고 골든셋이 필요하다.

| 출처 | 문항 | 상태 | 위치 |
|---|---|---|---|
| 별첨4 사업비 문항 (창진원 공식 답변) | 7 | ✅ 확보 | `2026_Finance_DATA_FOR_RAG/_골든셋/` |
| 직접 작성 | 23 | ⬜ **해야 함** | — |
| 연구재단 QA 홀드아웃 | 10~15 | 인용 정확도 채점 전용 | `사례집/사례집_한국연구재단_정부연구비사용QA사례집_2025개정.pdf` |

> ⚠️ **별첨4 는 인덱스에 절대 넣지 않는다** (정답 유출). 그래서 `_골든셋/` 폴더로 분리해 둔 것이다.

작성 요령은 `룰테이블_검수가이드.md` §9 참조.

---

## 4. 현재 완성도 (2026-08-27)

**"코드가 있다"와 "지금 코퍼스로 돌아갔다"는 다르다.** 두 칸으로 나눠 본다.

| 단계 | 코드 | 산출물 상태 | 확인 방법 |
|---|---|---|---|
| 데이터 수집 — 법령 | ✅ | ✅ 전량 수집·검증 통과 | `_verify_report.json`, `법령_크롤링_현황.md` §8 |
| **우선순위 조항** | ✅ `build_precedence.py` | ✅ 7건 추출 (`verified=false`) | `_precedence_rules.json` |
| **참조 그래프** | ✅ `build_refs.py` | ✅ 44문서 **1,136 엣지** (resolved 920 / dangling 181 / **shifted 35**) | `_refs.json` |
| **스캔 문서 조사** | ✅ `scan_inventory.py` | ✅ 226건 중 스캔전용 45건 — **A등급은 2건뿐** | `_scan_inventory.json` |
| **PDF 문자중복 대응** | ✅ `pdftext.py` | ✅ 제14차 지침·별첨4·도약2022 자동 해소 | — |
| **골든셋 초안** | — | ⚠️ **50문항** 초안 (`verified=false`) — 창업기업 33 / 주관기관 16 + 별첨4 7문항 | `_골든셋/_골든셋_초안.json` |
| 데이터 수집 — 데이터셋 | ✅ | ✅ 109건 정리 완료 | `DATASET_STRUCTURE.md` |
| HWP 변환 | — | ✅ 본문 34건 + **별표 103건 완료** (2026-08-27) | `_hwp변환/_변환로그.json` |
| 스캔 PDF 판독 | — | ✅ 부정행위 사례집 **42건**(창업기업 30 + 주관기관 12) / 재도전 2026판은 제3조만 | `사례집/_부정행위사례집_OCR.json` |
| Stage 0 — 문서 파싱 | ✅ | 🔴 **구 코퍼스 기준 — 재실행 필요** | `doc_articles` 1,640건(구) |
| Stage 1 — 룰 테이블 | ✅ | ⚠️ 초안 19행, `verified=false` → **D단계 검수 필요** | `rules` |
| Stage 2 — 인덱싱 (벡터) | ✅ | 🔴 **구 코퍼스 기준 — 재실행 필요** | `chunks` 2,374건(구) |
| Stage 2 — BM25 | ⬜ 미구현 | — | — |
| Stage 3 — 쿼리 파이프라인 | ⬜ 미구현 | LLM 미연결 | — |
| 평가 — 검색 | ✅ | 🔴 구 코퍼스 baseline, 재측정 필요 | `eval_retrieval.py` |
| 평가 — 골든셋 | ⬜ | 7/30문항 확보 (별첨4) → 23문항 작성 필요 | `_골든셋/` |
| Agent (A2/A4) | ⬜ 미구현 | — | — |
| 배포 | ⬜ | 5주차 | — |

### 다음에 할 일 (의존 순서)

0. **커밋** — 8월 20~24일 작업 전부가 아직 GitHub 에 없다 (§1 경고)
1. 🔴 **적용대상 태깅** — 모든 조문에 `주관기관 | 창업기업 | 공통`. **창업팀 전용 전환으로 1순위가 됐다**
   (`서비스 아키텍쳐.md` §2-2). 실측상 인덱싱 대상의 절반이 창업팀에 무관하다 —
   예비창업 세부관리기준은 주관기관 언급 102 vs 창업기업 65. 이 필터 없이는 Recall 을 올려도 무의미하다
1-a. **청킹 단위 확정 (조 vs 항)** — `구현.md` §1-1. 재인덱싱이 여기 걸려 있다 ⬅ **판단 필요**
1-b. **스키마 재적용** — `layer` CHECK 수정 + `precedence_rules`·`refs` 테이블 신설(2026-08-27).
   반드시 `docker compose down -v` 후 `up -d` (init 은 최초 1회만 실행됨)
2. **신규 코퍼스로 Stage 0 → Stage 2 재실행** — 구 74파일 산출물을 신규 109건 + 법령 XML 로 교체
   - 인덱싱 대상 판단: `DATASET_STRUCTURE.md` + `_law_sources.json` 의 `index` 플래그
   - ⛔ **`_골든셋/` · `archive/` · `_테스트_L3/` 는 절대 넣지 말 것** — 경로 블랙리스트를 코드로 강제
   - 특히 `archive/구_데이터/L4_기관규정/` 24건(타 대학)은 **테스트 입력 전용**이다. 인덱스에 들어가면
     남의 학교 규정이 판정 근거로 인용된다 — 이건 버그가 아니라 사고다
3. **§2-2·§3-2-1 숫자 갱신** — 재인덱싱 후 같은 절차로 다시 재서 새 baseline 기록
4. **D단계 룰 검수** (사람, 반나절) — `python scripts/review_rules.py`
5. **골든셋 검수** (사람) — 24문항 초안이 `_골든셋/_골든셋_초안.json` 에 있다. `verified=false` 라
   그대로 쓰면 안 된다. 검수 항목: 판정이 원문에서 도출되는가 / 대상이 주관기관인지 창업기업인지 /
   근거_원문이 PDF 와 문자열 일치하는가. 작성 요령은 `룰테이블_검수가이드.md` §9
6. ~~참조 그래프 구축~~ ✅ **완료** — `_refs.json` (44문서 **1,136엣지**, dangling 320→181 개선). 남은 것:
   - `_law_delegation_paths.json`(법령 간 68 규범)·`xref_mismatch`(124건)와 통합
   - DB `refs`·`precedence_rules` 테이블 적재 (지금은 JSON)
   - 잔여 dangling 181건은 대부분 R&D 계열(과학기술기본법·출연연법)로 판정 인덱스 밖이라 정상
7. **우선순위 조항 추출 (`precedence_rules`)** — 창진원 8개 사업의 제3조 부근. 사람 검수 대상
8. BM25 + RRF 구현 → 검색 재평가
9. Stage 3 쿼리 파이프라인 (LLM 2회 연결) — L3 통째 로드 + 게이팅 + 폐포 수집
10. **타 대학 24건으로 L3 파이프라인 자동 테스트** — 파싱 게이트 통과율 · 참조 해소율 ·
    **기관 간 누수 0건**(대학A 질문에 대학B doc_id 가 인용되면 즉시 FAIL). 사람 채점 불필요
11. 골든셋 평가 → 튜닝
12. 웹 UI + 배포

---

## 부록. 문제가 생기면

| 증상 | 조치 |
|---|---|
| `localhost:8081` 안 열림 | Docker Desktop에서 `suddoe-db`가 Healthy인지 먼저 확인, 30초 후 새로고침 |
| `python` 명령이 없다고 나옴 | PowerShell을 새로 열거나 `py`로 시도 |
| `psycopg` 모듈 없음 | `pip install "psycopg[binary]"` |
| 임베딩이 너무 느림 | 정상이다. CPU로 2,300건은 40분 걸린다 |
| 스크립트가 DB에 연결 못 함 | `docker compose ps`로 켜져 있는지 확인 |
| `too many clients` | 잠시 후 재시도 (블로킹된 연결이 정리되면 해소) |
