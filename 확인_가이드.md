# 확인 가이드 — 무엇을 어떻게 점검하나

작성: 2026-08-13 · 개발 지식 없이 따라 할 수 있게 씀

이 문서는 **"제대로 됐는지 내가 어떻게 아나?"**에 답한다. 네 부분이다.

1. [GitHub에서 확인할 것](#1-github)
2. [DB에서 확인할 것](#2-db)
3. [RAG 품질 확인](#3-rag-품질)
4. [지금 완성된 것 / 아직 안 된 것](#4-현재-완성도)

---

## 1. GitHub

**저장소:** https://github.com/Kimgundo58/2026-Finance-AI-Competition

### 폴더 구조로 보는 법

| 위치 | 무엇 |
|---|---|
| `Rag_Agent구현 파이프라인.md` | **설계 명세** — 청킹·임베딩·DB 스키마·Agent까지 전부 |
| `룰테이블_검수가이드.md` | **D단계 할 일** — 룰 19행 검수 절차 |
| `확인_가이드.md` | (이 문서) |
| `db/` | 로컬 DB 구성 (compose + 스키마 + 사용법) |
| `scripts/` | 파이프라인 코드 |
| `규정문서/`, `법령 PDF/` | 원본 데이터셋 74파일 |
| `manifest.json` | 코퍼스 신분증 — 인덱서는 이 파일만 신뢰 |

### 커밋 이력으로 보는 법

**Commits** 탭을 누르면 무엇이 언제 들어갔는지 보인다. 각 커밋 메시지에 **무엇을 왜 고쳤는지**가 적혀 있다.

| 커밋 | 내용 |
|---|---|
| 데이터셋 + 파이프라인 문서 | 74파일 + 설계 명세 |
| 로컬 개발 DB 구성 | Docker Compose + 스키마 9테이블 |
| Stage 0 파서 + Stage 2 인덱서 + D단계 도구 | 실제 코드 |
| 파서 정확도 수정 + 검색 평가 | pdfplumber 전환, 평가 스크립트 |

### 확인 포인트

- [ ] 최상위에 `.md` 문서 5개가 보이는가
- [ ] `scripts/` 안에 파이썬 파일 8개가 있는가
- [ ] `법령 PDF/` 안에 L1~L4 폴더가 다 있는가
- [ ] 커밋 메시지가 한글로 무슨 작업인지 설명하는가

> ⚠️ 저장소가 **public** 이다. 비공개로 바꾸려면 Settings → 맨 아래 Danger Zone → Change visibility.

---

## 2. DB

### 2-1. 켜져 있나 — Docker Desktop

**Containers** → `suddoe` 그룹 → `suddoe-db`, `suddoe-pgweb` 둘 다 **Running**(초록)이면 정상.

꺼져 있으면 ▶ 재생 버튼. 또는:

```powershell
cd "C:\Users\dogun\...\금융 AI공모전\db"
docker compose up -d
```

### 2-2. 데이터가 들어왔나 — http://localhost:8081

브라우저에서 열면 로그인 없이 바로 연결된다. 왼쪽에서 **`v_적재현황`** 클릭:

| 테이블 | 정상값 | 뜻 |
|---|---|---|
| 1. documents | **49** | 원본 문서 |
| 2. doc_articles | **1,640** | 조(條) 단위 원문 |
| 3. chunks | **2,374** | 검색용 조각 + 임베딩 |
| 4. case_chunks | **273** | 사례 Q&A |
| 5. rules | **19** | 판정 룰 (D단계 검수 대상) |
| 6. item_alias | 0 | 아직 미구현 |
| 7. decisions | 0 | 판정 로그 (LLM 붙인 뒤 쌓임) |
| 8. golden_set | 0 | 골든셋 (직접 작성 필요) |
| 9. xref_mismatch | **124** | 조문 상호참조 기록 |

### 2-3. 품질 점검 SQL

`Query` 탭에 붙여넣고 실행:

```sql
SELECT 'L2 조 개수(80↑ 정상)' AS 항목, count(*)::text AS 값
  FROM doc_articles WHERE doc_id LIKE 'L2_%제14차%'
UNION ALL SELECT '붙임2 존재(2 정상)', count(*)::text
  FROM doc_articles WHERE 조번호='붙임2'
UNION ALL SELECT '임베딩 누락(0이어야 함)', count(*)::text
  FROM chunks WHERE embedding IS NULL
UNION ALL SELECT '판정검색 대상 청크', count(*)::text
  FROM chunks WHERE status='active' AND apply_mode='apply' AND layer IN ('L1','L2','L3')
UNION ALL SELECT '검수 완료 룰', count(*)::text
  FROM rules WHERE verified;
```

| 항목 | 합격 | 불합격이면 |
|---|---|---|
| L2 조 개수 | 80 이상 | PDF 추출 실패 — pdfplumber 확인 |
| 붙임2 존재 | 2 | 섹션 분리 실패 |
| **임베딩 누락** | **0** | 0이 아니면 `build_index.py` 재실행 |
| 검수 완료 룰 | (D 진행에 따라) | 19가 되면 D 완료 |

### 2-4. 원문이 안 깨졌나 (제일 중요)

```sql
SELECT 조번호, 조제목, left(text, 120) AS 본문앞부분
FROM chunks
WHERE doc_id LIKE 'L2_%제14차%' AND 조번호 IN ('제39조','제45조')
LIMIT 4;
```

**사람이 읽을 수 있는 문장**이어야 한다. 아래처럼 나오면 실패다:

```
❌ "...집기 가구 기구 등은 집행할 수 없으며 노트북 및 , , , PC("
✅ "제39조(기계장치, 공구·기구) ① 사업계획서 상의 사업화를 위해..."
```

쉼표·괄호·숫자가 문장 끝으로 밀려 있으면 PDF 추출이 잘못된 것이다. §원칙 4(인용은 원문 그대로)가 무너지므로 그 상태로는 쓸 수 없다.

### 2-5. 껐다 켜기 / 초기화

| 하고 싶은 것 | 명령 |
|---|---|
| 끄기 (데이터 유지) | `docker compose stop` |
| 켜기 | `docker compose up -d` |
| **전부 초기화** | `docker compose down -v` → `up -d` |

> 스키마(`db/init/01_schema.sql`)를 고쳤으면 **반드시 `down -v`** 해야 반영된다. 최초 1회만 실행되기 때문이다.

---

## 3. RAG 품질

### 먼저 — 지금 잴 수 있는 건 "검색"뿐이다

```
질문 → [① LLM 정규화] → [② 룰 조회] → [③ 검색] → [④ LLM 판정] → 답변
         미구현            룰만 존재      ✅ 완성     미구현
```

"판정이 맞는가"는 LLM을 붙인 뒤에야 측정된다. 지금은 **"근거 조항을 찾아오는가"**만 잰다.

그리고 **아직 벡터 검색만이다.** BM25 + RRF가 안 붙었으므로 지금 점수는 **하한선**이고, 붙이면 올라간다.

### 3-1. 눈으로 — 5문항

```powershell
cd "C:\Users\dogun\...\금융 AI공모전"
python scripts/smoke_search.py
```

질문 5개에 대해 상위 3개 조항을 보여준다. **판단 기준 하나:**

> **"내가 이 조항만 보고 사용자에게 답할 수 있나?"**

| 결과 | 판정 |
|---|---|
| 1등이 정확히 그 비목 조항 | ✅ 좋음 |
| 1~3등 안에 있음 | ✅ 합격 (실제로는 top-8을 씀) |
| 관련 비목인데 다른 조항 | ⚠️ 청킹 문제 가능성 |
| 엉뚱한 비목 | ❌ 문제 |
| **텍스트가 깨져 있음** | 🔴 치명적 |

### 3-2. 숫자로 — 15문항 자동 채점

```powershell
python scripts/eval_retrieval.py
```

| 지표 | 의미 | 합격선 |
|---|---|---|
| **Recall@8** | 정답 조항이 상위 8개(LLM에 넘길 개수) 안에 있는 비율 | **0.85 이상** |
| Recall@3 | 상위 3개 안 | 참고 |
| Recall@1 | 1등으로 맞힘 | 참고 |
| MRR | 정답이 평균 몇 등 (1등=1.0) | 0.6 이상 |

**Recall@8이 왜 상한선인가** — 여기서 못 찾은 조항은 ④ LLM의 컨텍스트에 아예 안 들어간다. 모델을 아무리 좋은 걸 써도 **없는 근거로는 답할 수 없다.**

실패한 문항은 무엇이 대신 나왔는지까지 찍어주므로 원인 진단이 된다:

| 나온 것 | 진단 | 처방 |
|---|---|---|
| 같은 비목 다른 조항 | 청킹 경계 문제 | `MAX_CHARS` 3000 → 1500 |
| 다른 비목 | 임베딩이 도메인에 약함 | bge-m3와 비교 |
| 정확 용어를 못 찾음 | **BM25 부재** | BM25 + RRF 붙이기 |
| 구버전·타사업 섞임 | 필터 버그 | `status='active'` 확인 |

### 3-2-1. 실측 결과 (2026-08-13, 벡터 검색만)

| 지표 | 값 | 판정 |
|---|---|---|
| **Recall@8** | **1.00** (15/15) | ✅ 합격선 0.85 크게 상회 |
| Recall@3 | 0.93 (14/15) | ✅ |
| Recall@1 | 0.93 (14/15) | ✅ |
| MRR | **0.947** | ✅ |

15문항 중 14개가 **1위**로 정답 조항을 물어왔다. 나머지 1개("해외 전시회 출장 여비")도 5위 안에 들어왔다.

BM25 없이 벡터만으로 낸 점수이므로 **하한선**이다.

### 3-3. 이 15문항의 쓸모

지금 점수는 **기준선(baseline)**이다. 나중에 BM25를 붙이거나 청킹을 바꿨을 때 **같은 15문항으로 다시 돌려 비교**하면 개선 여부가 숫자로 나온다.

§4 운영 규칙: **"뭐든 바꾸면 재실행. 점수 없이 '좋아진 것 같다'로 머지 금지."**

### 3-4. 정식 평가는 골든셋 30문항

이 15문항은 검색만 재는 축소판이다. 정식 평가는 §8의 지표 3개이고 골든셋이 필요하다.

| 출처 | 문항 | 상태 |
|---|---|---|
| 별첨4 사업비 문항 (창진원 공식 답변) | 7 | ✅ 확보 |
| 직접 작성 | 23 | ⬜ **해야 함** |
| 연구재단 QA 홀드아웃 | 10~15 | 인용 정확도 채점 전용 |

작성 요령은 `룰테이블_검수가이드.md` §9 참조.

---

## 4. 현재 완성도

| 단계 | 상태 | 확인 방법 |
|---|---|---|
| Stage 0 — 문서 파싱 | ✅ | `doc_articles` 1,600여 건 |
| Stage 1 — 룰 테이블 | ⚠️ **초안만** | `rules` 19행, `verified=false` → **D단계 검수 필요** |
| Stage 2 — 인덱싱 (벡터) | ✅ | `chunks` 2,300여 건 |
| Stage 2 — BM25 | ⬜ 미구현 | — |
| Stage 3 — 쿼리 파이프라인 | ⬜ 미구현 | LLM 미연결 |
| 평가 — 검색 | ✅ | `eval_retrieval.py` |
| 평가 — 골든셋 | ⬜ | 23문항 작성 필요 |
| Agent (A2/A4) | ⬜ 미구현 | — |
| 배포 | ⬜ | 5주차 |

### 다음에 할 일 (의존 순서)

1. **D단계 룰 검수** (사람, 반나절) — `python scripts/review_rules.py`
2. **골든셋 23문항 작성** (사람)
3. BM25 + RRF 구현 → 검색 재평가
4. Stage 3 쿼리 파이프라인 (LLM 2회 연결)
5. 골든셋 평가 → 튜닝
6. 웹 UI + 배포

---

## 부록. 문제가 생기면

| 증상 | 조치 |
|---|---|
| `localhost:8081` 안 열림 | Docker Desktop에서 `suddoe-db`가 Healthy인지 먼저 확인, 30초 후 새로고침 |
| `python` 명령이 없다고 나옴 | PowerShell을 새로 열거나 `py`로 시도 |
| `psycopg` 모듈 없음 | `pip install "psycopg[binary]"` |
| 임베딩이 너무 느림 | 정상이다. CPU로 2,300건은 40분 걸린다 |
| 스크립트가 DB에 연결 못 함 | `docker compose ps`로 켜져 있는지 확인 |
| `too many clients` | 잠시 후 재시도 (블로킹된 연결이 정리되면 해소) |
