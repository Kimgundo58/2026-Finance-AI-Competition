# 프론트엔드 데이터 요구서 — 화면별 · 요소별

「써도돼요 / CHECKUMAIT」 · 프론트 담당 → **인프라 담당자 전달용**
2026-09-01 · 마감 **2026-09-07 (D-6)** · **최종본**

> 프로토타입 16화면 실측 + **2026-08-30 백·프론트 매칭 회의 결과** 반영.
> 회의에서 정해진 것은 전부 확정으로 적었고, **남은 문의는 맨 뒤 2건뿐**입니다.

---

## 표기

| 기호 | 뜻 |
|:--:|---|
| 🟢 | 담당자 DB |
| 🔵 | 프론트 계산 (서버 불필요) |
| 🟣 | 프론트가 씀 |
| 🔴 | AI 판정엔진 응답 |
| **신설** | 아직 없는 테이블·컬럼 |

## 상태 어휘 — 전 화면 공통

| `decisions.판정` | 배지 | 설명 문구 |
|---|---|---|
| `가능` | 🟢 **특이사항 없음** | 현재 기준으로 집행 가능합니다. |
| `조건부` | 🟡 **확인 필요** | 아래 조건을 충족하면 집행할 수 있습니다. |
| `판단불가` | 🟡 **확인 필요** | 규정만으로는 결론을 낼 수 없습니다. 주관기관에 문의가 필요합니다. |
| `불가` | 🔴 **위험** | 현재 계획으로는 집행할 수 없습니다. 계획 수정이 필요합니다. |
| (판정 없음) | ⬜ **점검 전** | 판정이 아니라 진행 상태 |

**배지는 3종, 설명만 갈립니다.** 백엔드는 4-way 그대로 주시면 됩니다.
`plan_tasks.상태`(`준비필요·집행예정·완료`)는 판정과 **별개 축**입니다.

---

# 화면 1. 랜딩

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 로고·헤드라인·기능 4카드 | `사업비, 쓰기 전에 AI가 먼저 확인해드려요.` | — | — | 🔵 |
| 예시 데모 카드 | `UI 디자인 외주 제작 / 3,000,000원 / 외주용역비` | — | — | 🔵 고정 |
| 데모 상태 라벨 | `확인 필요` | — | 상태 어휘표 적용 | 🔵 |

**API 불필요.**

---

# 화면 2. 온보딩 ① — 사업 선택

> 스텝퍼 **1 사업 선택** — 2 주관기관 선택 — 3 기관 기준 등록 — 4 설정 완료

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 타입 | 출처 |
|---|---|---|---|---|:--:|
| 셀렉트 | `2026 초기창업패키지` | `corpus.precedence_rules.사업명` | `사업명` | text | 🟢 |
| 카드 — 사업명 | `2026 초기창업패키지` | 〃 | `사업명` | text | 🟢 |
| 카드 — 발행처 | `중소벤처기업부 · 창업진흥원` | `corpus.documents` **신설** | `발행처` | text | 🟢 |
| 카드 — 안내 | `최초에는 주로 사용하는 사업 1개만 설정합니다.` | — | — | — | 🔵 |
| 룰 준비 배지 | `규정 데이터 준비 중` | `corpus.rules` count | `룰_준비됨` | bool | 🟢 |

```
GET /api/programs
→ [{ "사업명":"2026 초기창업패키지",
      "발행처":"중소벤처기업부 · 창업진흥원",
      "룰_준비됨": true }]
```

🔑 **사업 목록 정본은 `precedence_rules`** (8사업 전부 있음). `rules` 는 2사업뿐이라 `룰_준비됨` 으로 구분합니다.
룰 없는 6개 사업은 **선택은 되게 하고 배지만** 붙입니다.

---

# 화면 3. 온보딩 ② — 주관기관 선택

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 타입 | 출처 |
|---|---|---|---|---|:--:|
| 검색 input | `학교·기관 이름을 입력하세요` | — | 쿼리 | text | — |
| 결과 — 기관명 | `건국대학교` | `tenant.orgs` **0행** | `기관명` | text | 🟢 |
| 결과 — 주소 | `서울특별시 광진구` | `tenant.orgs` **ALTER** | `주소` | text | 🟢 |
| 결과 — 부서 | `창업지원단` | `tenant.orgs` **ALTER** | `부서` | text | 🟢 |
| (내부) 키 | | `tenant.orgs` | `org_id` | uuid PK | 🟢 |

⚠️ **`orgs` 가 0행입니다.** 시연 전에 **건국대 1행**이 반드시 들어가야 온보딩이 돕니다.
전국 기관 목록은 없으니 **MVP 는 데모 1건**으로 갑니다. 검색은 전체를 받아 **프론트에서 필터**하겠습니다.

---

# 화면 4. 온보딩 ③ — 기관 기준 등록

## ① 요약 카드

| 표시 요소 | 화면 예시 | 테이블 | 출처 |
|---|---|---|:--:|
| 선정 사업 | `2026 초기창업패키지` | `f_profile.사업명` | 🟣 |
| 주관기관 | `건국대학교` | `orgs.기관명` | 🟣 |
| 적용 기준 | `사업 공통 규정 우선 적용` | — | 🔵 |

## ② 파일 업로드

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 타입 | 출처 |
|---|---|---|---|---|:--:|
| 허용 형식 안내 | `PDF · HWP · HWPX · DOC · DOCX · 최대 20MB` | — | 상수 | — | 🔵 |
| 업로드 결과 | | `tenant.l3_documents` | `doc_id` `원본파일명` | | 🟢 |
| 시행일 · 버전 | `2026.03.01` | `l3_documents` | `시행일` `version` | | 🟢 |
| 파싱 결과 | `정상` / `주의` / `실패` | `l3_documents` | `파싱품질` | `pass\|warn\|fail` | 🟢 |
| 끊긴 참조 | `3건은 원문을 찾지 못했습니다` | `l3_documents` | `dangling수` | int | 🟢 |
| 추출 조항 수 | `42개 조항을 읽었습니다` | `tenant.l3_articles` | count | int | 🟢 |
| 건너뛰기 안내 | `파일 없이 계속하면 사업 공통 규정을 우선 적용하며…` | — | — | — | 🔵 |

## ③ 실패 안내 — 프론트가 문구를 씁니다

| 조건 | 문구 |
|---|---|
| `파싱품질 = fail` | `PDF 로 변환 후 다시 올려주세요` (DRM HWP) |
| `파싱품질 = warn` | `일부 조항을 읽지 못했습니다` |
| `dangling수 > 0` | `{n}건은 참조된 원문을 찾지 못했습니다` |

✅ **확장자 — 문서 형식은 전부 가져갑니다.** `PDF · HWP · HWPX · DOC · DOCX` 그대로 유지합니다.
**최종 목록은 담당자님이 추후 통보**해 주시고, **그 목록에 없는 형식은 프론트에서 예외 처리**합니다.
`l3_documents.extraction` 에 DOC·DOCX 값이 아직 없으니, **통보해 주실 때 그 값도 함께** 알려주세요.

✅ **용량 제한(20MB)은 DB 공간 절약 목적**이라 프론트에서도 업로드 전에 차단합니다.

⚠️ `l3_documents` 에 진행 상태 컬럼이 없어, **파싱이 10초 이내면 동기 응답**으로 만들겠습니다.

---

# 화면 5. 온보딩 ④ — 설정 완료

| 표시 요소 | 화면 예시 | 테이블 | 출처 |
|---|---|---|:--:|
| 요약 카드 | `2026 초기창업패키지 / 건국대학교 · 기관 세부기준 미등록` | `f_profile` + `orgs` + `l3_documents` | 🟢 |
| 예시 데이터 안내 | `시연용 예시 데이터가 준비되어 있어요.` | — | 🔵 |
| `예시 데이터로 둘러보기` | | **시드 데이터** | 🟣 |
| `첫 계획 작성하기` | | — | 🔵 |

🔑 **예시 데이터는 프론트가 시드 SQL 로 넣습니다.** 서버 작업 없습니다.

---

# 화면 6. 홈

## ① 인사말 · 헤더

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 타입 | 출처 |
|---|---|---|---|---|:--:|
| 인사말 | `안녕하세요! 체쿠메이트님.` | `tenant.accounts` **ALTER** | `팀명` | text | 🟣 |
| 헤드라인 | `지출 계획을 점검하고 필요한 준비를 미리 확인해보세요.` | — | — | — | 🔵 |
| `+ 새 지출 계획` | | — | — | — | 🔵 |

✅ **이름은 표시하지 않습니다. 팀명만.** (회의 확정)

## ② 사업 카드

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 타입 | 출처 |
|---|---|---|---|---|:--:|
| 배지 | `진행 중인 사업` | — | — | — | 🔵 |
| D-day | `사업 종료까지 D-129` | `f_profile` | `협약종료일` | date | 🔵 계산 |
| 사업명 | `2026 초기창업패키지` | `f_profile` | `사업명` | text | 🟢 |
| 기관 · 유형 | `건국대학교 창업지원센터 · 사업화 자금` | `orgs.기관명` + `f_profile` **ALTER** | `지원유형` | text | 🟢 |
| 사업기간 | `2026.03.01 – 2026.12.31` | `f_profile` | `협약시작일` `협약종료일` | date | 🟢 |

✅ `기관 최신 집행 안내 확인` 링크는 **삭제**합니다 (원문 뷰어 없음).

## ③ 전체 지출 계획 현황

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 타입 | 출처 |
|---|---|---|---|---|:--:|
| 건수 | `전체 13건` | `tenant.expense_plans` **신설** | count | int | 🔵 |
| 예상금액 합계 | `134,056,911원` | `expense_plans` | `금액` 합계 | numeric | 🔵 |

## ④ 통계 카드 4개 — 하나씩

| # | 카드 | 숫자 | 테이블 | 세는 기준 | 출처 |
|:-:|---|---|---|---|:--:|
| 1 | **전체** | `13건` | `expense_plans` | 전체 count | 🟣 |
| 2 | **확인 필요** | `10건` | `decisions` (via `latest_decision_id`) | `판정 ∈ (조건부, 판단불가)` | 🔴 |
| 3 | **위험** | `1건` | `decisions` | `판정 = 불가` | 🔴 |
| 4 | **특이사항 없음** | `1건` | `decisions` | `판정 = 가능` | 🔴 |

🔑 **통계 전용 API 불필요.** `expense_plans JOIN decisions ON latest_decision_id` 한 번이면 다 나옵니다.
⚠️ 13 = 10+1+1 **+1(점검 전)** 입니다. `점검 전` 카드를 5번째로 넣을지는 프론트에서 정하겠습니다.

## ⑤ 「지금 확인해주세요」 — 행 하나당

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 타입 | 출처 |
|---|---|---|---|---|:--:|
| 색 점 | 🔴🟡🟢 | `decisions` | `판정` → 색 | — | 🔵 |
| 지출명 | `시장조사 인터뷰 회의비` | `expense_plans` **신설** | `제목` | text | 🟣 |
| 비목 | `창업활동비` | `expense_plans` | `확정비목` | text | 🟣 |
| 금액 | `120,000원` | `expense_plans` | `금액` | numeric | 🟣 |
| 상태 배지 | `위험` / `확인 필요` / `점검 전` | `decisions` | `판정` | — | 🔴 |
| 클릭 → 상세 | | `expense_plans` | `plan_id` | bigserial | 🟣 |

**정렬**: `불가` → `판단불가` → `조건부` → 최신순 (프론트)

## ⑥ 「다가오는 일정」 — 행 하나당

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 타입 | 출처 |
|---|---|---|---|---|:--:|
| 날짜 | `09.09` | `tenant.plan_tasks` **신설** | `due_date` | date | 🟣 |
| 제목 | `회의비 단가 확인` | `plan_tasks` | `항목` | text | 🟣 |
| 일정 유형 | `기타` / `계약` / `비교견적` | `plan_tasks` **ALTER** | `유형` | text | 🟣 |
| 연결 지출계획 | `시장조사 인터뷰 회의비` | `expense_plans` | `제목` | text | 🟣 |
| 상태 | `완료` | `plan_tasks` | `상태` | text | 🟣 |

## ⑦ 사이드바

| 표시 요소 | 화면 예시 | 테이블 | 출처 |
|---|---|---|:--:|
| 메뉴 5개 | `홈` `지출 계획` `AI CHAT` `집행 일정` `마이페이지` | — | 🔵 |
| 참여 중인 프로젝트 | `2026 초기창업패키지` | `f_profile.사업명` | 🟢 |
| 기관 · D-day | `건국대학교 창업지원센터 · D-129` | `orgs` + 계산 | 🔵 |
| 계정 | `팀 체쿠메이트` | `accounts.팀명` **ALTER** | 🟣 |

---

# 화면 7. 지출 계획 목록

## ① 통계 4개 · ② 탭

화면 6 ④ 와 동일. 탭 = `전체` · `확인 필요` · `위험` · `특이사항 없음`

## ③ 도구 줄

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 검색 | `지출명 검색` | `expense_plans` | `제목` | 🔵 |
| `필터` · `금액 범위` · `최근 수정순` | | `expense_plans` | `금액` `updated_at` | 🔵 |
| `선택 항목 CSV 출력` | | `expense_plans` | 전체 행 | 🔵 **브라우저 생성** |
| `선택 항목 AI 점검` | | — | `plan_id[]` | 🔴 |

✅ **거래처 검색은 제거**했습니다 (회의 확정).
✅ **일괄 점검은 직렬 처리** — 한 건씩 순차 호출 + 진행률. 선택 모드 · 완료 모달 유지.

## ④ 표 — 열 하나씩

| 열 | 화면 예시 | 테이블 | 필요한 값 | 타입 | 출처 |
|---|---|---|---|---|:--:|
| 체크박스 | | — | 선택 상태 | — | 🔵 |
| 지출명 | `개발용 노트북 구매` | `expense_plans` **신설** | `제목` | text | 🟣 |
| 예상 비목 | `기계장치` | `expense_plans` | `확정비목` | text | 🟣 |
| 예상 금액 | `1,000,000원` | `expense_plans` | `금액` | numeric | 🟣 |
| AI 점검 상태 | `확인 필요` / `점검 전` | `decisions` | `판정` | — | 🔴 |
| 예상 지출일 | `2026.09.03` | `expense_plans` | `집행예정일` | date | 🟣 |
| 최근 수정일 | `2026.08.24 방금 전` | `expense_plans` | `updated_at` | timestamptz | 🟣 |
| 더보기 ⋮ | 수정 / 삭제 | — | API 제작 예정 | — | 🟣 |

## ⑤ CSV 출력 — 열 구성

| 열 | 출처 |
|---|---|
| 지출명 · 비목 · 예상금액 · 예상지출일 · 상태 | `expense_plans` + `decisions` |
| **산출 근거** | 정규화 `용도` |
| 현금 / 현물 / 합계 | `f_exec` 4분면 집계 |

✅ **세목 · 세세목 열은 뺐습니다** (룰 키가 `사업 × 비목` 단일 계층).
✅ `계획서 업로드` 버튼은 **삭제**합니다.

---

# 화면 8. 새 지출 계획 ① — 기본 정보

> 스텝퍼 **1 기본 정보** — 2 비목 확인 — 3 추가 확인

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 타입 | 필수 |
|---|---|---|---|---|:--:|
| 지출 항목 | `예: 개발용 노트북 구매` | `expense_plans` | `제목` | text | ✅ |
| 사용 목적 | `사업 수행에 왜 필요한지 작성해주세요.` | `expense_plans` | `질문원문` | text | ✅ |
| 예상 금액 | `부가세 포함` | `expense_plans` | `금액` | numeric | ✅ |
| 예상 지출일 | `2026-09-30` | `expense_plans` | `집행예정일` | date | |
| 거래처 | `미정인 경우 비워둘 수 있어요.` | `expense_plans` **신설** | `거래처` | text | |
| 첨부파일 | `견적서·과업자료 등` | `expense_plans` **신설** | `첨부파일` jsonb | | |
| `임시저장` | | `expense_plans` | `상태 = draft` | — | |

## 전송 방식

```
화면      항목 · 목적 · 금액 · 날짜 · 거래처          (폼 유지)
             ↓
전송      한 문장으로 합성  →  POST /api/normalize
          + 사용자가 입력한 원래 숫자·날짜도 함께
```

🔴 **요청 본문 형태는 문의 1번 참조** (맨 뒤)

---

# 화면 9. 새 지출 계획 ② — 비목 확인

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 안내 | `가장 가까운 비목을 자동 선택했습니다. 직접 변경할 수 있습니다.` | — | — | 🔵 |
| 비목 목록 | 그리드 | `corpus.rules.비목` | `비목[]` | 🟢 |
| AI 추천 배지 | `✦ AI 추천` | `decisions.정규화` jsonb | `비목후보[].신뢰도` | 🔴 |
| 선택 결과 | | `expense_plans` | `확정비목` | 🟣 |

## 비목 라벨 — 프론트가 교정합니다

| 화면 표시 | → 정본 enum |
|---|---|
| `기계장치비` | `기계장치` |
| `특허권 등 무형자산 취득비` | `특허권등무형자산취득비` |
| **`회의비`** | 🔴 enum 에 없음 → **`창업활동비` 로 교체** (데모 시나리오 포함) |

✅ **`code` / `label` 분리** — 표시는 라벨, 통신은 enum. 하드코딩 없이 `GET /api/vocab` 으로 받습니다.
✅ **지급수수료 세부유형 16종은 비목 목록에 통합**됩니다 (회의 확정). "어떤 지급수수료인가요?" 분기 UI 제거, 그리드를 26칸까지 확장.

```
GET /api/vocab?사업명=2026 초기창업패키지
→ { "비목": [{ "code":"기계장치", "label":"기계장치비" }, … ] }
```

---

# 화면 10. 새 지출 계획 ③ — 추가 확인

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 안내 | `외주용역비 판단에 필요한 정보만 확인합니다.` | — | 비목별 | 🟢 |
| 질문 문장 | `업체의 사업자등록 업종이 이번 과업과 관련되어 있나요?` 외 5 | `corpus.check_items` ❓ | `질문` | 🟢 |
| 답변 3택 | `예` `아니오` `확인 필요` | `expense_plans` **신설** | `추가확인답변` jsonb | 🟣 |
| 하단 안내 | `답변을 반영해 공통 기준과 비목별 기준을 함께 점검합니다.` | — | — | 🔵 |
| `저장하고 AI 점검` | | → `POST /api/judge` | | 🔴 |

✅ **질문 뱅크는 유지되고 답변이 판정엔진 인풋으로 들어갑니다** (회의 확정).
✅ **`확인 필요` 를 고르면 그 항목이 결제 전 태스크로 추가**됩니다 → 화면에 인라인 안내를 붙이겠습니다.

---

# 화면 11. 지출 상세

## ① 헤더 · ② 정보 표

| 칸 | 화면 예시 | 테이블 | 필요한 값 | 타입 |
|---|---|---|---|---|
| 제목 | | `expense_plans` | `제목` | text |
| 최근 수정 | `2026.08.24 방금 전` | `expense_plans` | `updated_at` | timestamptz |
| 관련 비목 | `외주용역비` | `expense_plans` | `확정비목` | text |
| 예상 금액 | `2,134원 (부가세 포함)` | `expense_plans` | `금액` | numeric |
| 예상 지출일 | `2026.09.30` | `expense_plans` | `집행예정일` | date |
| 거래처 | `거래처 미정` | `expense_plans` **신설** | `거래처` | text |
| 사업 | `2026 초기창업패키지` | `f_profile` | `사업명` | text |
| 사용 목적 | | `expense_plans` | `질문원문` | text |
| 상세 설명 | `…에 필요한 산출물과 검수 기준을 포함합니다.` | `decisions.정규화` jsonb | `용도` | jsonb |
| 첨부 파일 | `견적서.pdf 245KB` | `expense_plans` **신설** | `첨부파일` jsonb | |

## ③ AI 점검 결과 헤더

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 상태 배지 | `확인 필요` | `decisions` | `판정` | 🔴 |
| **헤드라인** | `외주용역비 기준으로 추가 확인이 필요합니다.` | `decisions` **신설** | `요약` | 🔴 |
| 신뢰등급 | `A` | `decisions` | `신뢰등급` | 🟢 코드 산정 |
| 버전스탬프 | `제14차, 2025.12.23 기준` | `corpus.documents` | `version` + `시행일` | 🟢 |

## ④ AI 해설

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 본문 | `사업계획과의 연관성은 확인됐습니다. 다만 계약 전에 과업 범위, 결과물, 검수 기준을 문서로 명확히 하고…` | `decisions` **신설** | `해설` | 🔴 |

⚠️ **`요약`(헤드라인)과 `해설`(본문)은 서로 다른 두 문장입니다.** 화면이 둘을 따로 씁니다.

## ⑤ 결제 전 확인 — 3칸 카드

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 제목 | `과업 범위 확정` | `plan_tasks` **신설** | `항목` | 🔴 |
| 설명 | `화면 수, 산출물 형식, 수정 횟수를 계약서에 기재하세요.` | `plan_tasks` | `설명` | 🔴 |

## ⑥ 적용 근거 — 항목 하나당

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 제목 | `사업 목적과의 직접 관련성` | `decisions.인용` jsonb | `조제목` | 🔴 |
| 설명 | `외주 결과물이 사업계획의 제품·서비스 개발에 직접 활용되는지 확인합니다.` | `decisions.인용` jsonb | `원문` 요약 | 🔴 |
| 출처 문서명 | `2026 초기창업패키지 사업화 자금 공통 기준` | `corpus.documents` **신설** | `제목` | 🟢 |

✅ `자세히 보기` / `원문 보기` 는 **삭제**합니다 (뷰어 없음).

## ⑦ 판단불가일 때 추가 표시

```
확인되지 않은 사항
· 사업운영위원회 승인 여부       ← decisions.전제[].사실

[문의 초안 복사]
```

문의 초안 텍스트가 없으면 `전제[]` 를 나열하고 복사 버튼만 붙입니다.

## ⑧ 집행 준비 — 3단계

🔑 **`plan_tasks.구분` = `결제전 | 집행 | 결제후` 와 정확히 일치합니다.**

### 1단계 — 결제 전 확인 `1/3`

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 진행률 | `1/3` | `plan_tasks` | `상태=완료` count | 🔵 |
| 체크박스 | ☑ | `plan_tasks` | `상태` | 🟣 |
| 항목 · 설명 | `금액 적정성 확인` | `plan_tasks` | `항목` `설명` | 🔴 |
| **출처 배지** | `AI 제안` / `내가 답한 확인 필요` / `위험 해소` | `plan_tasks` | `출처` + 생성 경로 | 🔵 |
| `일정에 추가` | | `plan_tasks` | `due_date` 채움 | 🟣 |

**태스크가 추가되는 3가지 경로** (회의 확정)

```
① 판정이 나올 때      LLM 이 만든 해야할일[]  →  plan_tasks(구분='결제전')
② step3 '확인 필요'   그 항목이 결제 전 태스크로
③ 판정이 '위험'       위험 걸린 항목이 결제 전 태스크로
```

→ `위험 해소` 태스크는 **상단 고정 + 빨강**으로 표시하겠습니다.

### 2단계 — 실제 집행 정보

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 타입 | 출처 |
|---|---|---|---|---|:--:|
| 안내 | `누적 집행액 계산에 반영되는 정보` | — | — | — | 🔵 |
| 실제 지출일 | `2026-09-30` | `tenant.f_exec` | `귀속월` | date | 🟢 |
| 실제 지출액 | `부가세 포함` | `tenant.f_exec` | `금액` | numeric | 🟢 |
| 재원 | (조건부 노출) | `f_exec` | `재원` | text | 🔵 자동 |
| 형태 | (조건부 노출) | `f_exec` | `형태` | text | 🔵 자동 |
| 비목 | (자동) | `f_exec` | `비목` | text | 🔵 자동 |
| 누적 집행액 | `0원` | `f_exec` | `금액` 합계 | numeric | 🟢 |
| `실제 지출 등록` | | → `f_exec` INSERT | | | 🟢 |

## 🔑 `재원` · `형태` — 협약에 값이 있는 칸만 물어봅니다

| 협약 F1 상태 | 집행 완료 화면 |
|---|---|
| **정부지원 현금만** | **아무것도 안 물음** — 재원=정부지원, 형태=현금 자동 |
| 자기부담도 있음 | `재원` 셀렉트 1개 노출 |
| 현물도 있음 | `재원` + `형태` 2개 노출 |

✅ **시연 계정은 `정부지원 현금 5,000만` 하나만 넣으면 날짜·금액 2칸으로 끝납니다.**

### 3단계 — 결제 후 필요 증빙 `0/4`

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 진행률 | `0/4` | `plan_tasks` | 완료 count | 🔵 |
| 서류명 | `계약서` | `corpus.rules.증빙[]` | | 🟢 |
| 설명 | `과업 범위와 검수 조건 포함` | `corpus.evidence_sources` **신설** | `세부정보` | 🟢 |
| 발급처 | `홈택스 →` | `corpus.evidence_sources` **신설** | `발급처` | 🟢 |
| 파일 업로드 | | `expense_plans.첨부파일` | jsonb | 🟣 |

⚠️ `rules.증빙` 49종 ↔ CSV 124종 문자열 일치가 47% 라, **별칭 매핑 전까지 발급처 링크는 숨깁니다.**

## ⑨ 수정 모달

| 칸 | 테이블 |
|---|---|
| 지출 항목 · 사용 목적 · 예상 금액 · 예상 지출일 · 거래처 · 비목 · 첨부파일 | `expense_plans` |

✅ **수정하면 재판정이 원칙** → `latest_decision_id` 를 비우고 `점검 전` 으로 되돌립니다.
✅ 삭제 시 `연결된 집행 일정도 함께 삭제됩니다` 문구 유지 (일정 테이블 생성 후 유효).

---

# 화면 12. AI CHAT

✅ **구현 확정** (회의 결과). 화면 디벨롭 필요.

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 안내 | `사업비·비목·규정·증빙을 현재 사업과 작성한 계획을 바탕으로 질문하세요.` | — | — | 🔵 |
| 참조 범위 — 사업 | `2026 초기창업패키지` | `f_profile.사업명` | text | 🟢 |
| 참조 범위 — 계획 수 | `작성한 지출 계획 14건` | `expense_plans` | count | 🔵 |
| 참조 범위 — 규정 | `최신 규정 안내` | `l3_documents` | 존재 여부 | 🟢 |
| 추천 칩 3개 | `노트북 구매 전에 뭘 준비해야 해?` 외 2 | — | — | 🔵 고정 |
| 입력창 | | → `POST /api/chat` | | 🔴 |

⚠️ **대화 이력 저장 여부**가 정해지면 알려주세요. 저장 안 하면 **브라우저 세션에만** 두겠습니다.

---

# 화면 13. 집행 일정

✅ **DB 테이블 추가 예정** (회의 결과) — 서버 연동으로 갑니다.

## ① 통계 카드 4개

| # | 카드 | 숫자 | 테이블 | 세는 기준 |
|:-:|---|---|---|---|
| 1 | 이번 달 예정 | `16건` | `plan_tasks` | 이번 달 `due_date` |
| 2 | 준비 일정 | `2건` | `plan_tasks` | `상태 = 준비필요` |
| 3 | 집행 | `0건` | `plan_tasks` | `상태 = 집행예정` |
| 4 | 완료 | `14건` | `plan_tasks` | `상태 = 완료` |

## ② 캘린더 · ③ 다가오는 일정

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 셀 칩 | `실사용자 확인` | `plan_tasks` | `항목` `due_date` | 🟣 |
| 칩 색 | 초록/보라/회색 | `plan_tasks` | `상태` | 🔵 |
| 날짜 + D-day | `9/3 D-5` | `plan_tasks` | `due_date` | 🔵 |
| 연결 계획 | `개발용 노트북 구매` | `expense_plans` | `제목` | 🟣 |
| 탭 | `전체` / `지출계획별` | — | — | 🔵 |

```
체크리스트  →  WHERE plan_id = ?
캘린더      →  WHERE due_date IS NOT NULL
```

🔑 **같은 행을 두 각도에서 봅니다.** `일정에 추가` 버튼은 `due_date` 를 채우기만 하고 새 레코드를 만들지 않습니다.

## ④ 일정 추가 모달

| 칸 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 일정 제목 | `예: 외주계약서 검토` | `plan_tasks` | `항목` | 🟣 |
| 일정 유형 | `사전 확인` | `plan_tasks` **ALTER** | `유형` | 🟣 |
| 관련 지출 계획 | 셀렉트 | `plan_tasks` | `plan_id` | 🟣 |
| 일정일 | `2026-09-09` | `plan_tasks` | `due_date` | 🟣 |
| 상태 | `준비 일정` | `plan_tasks` | `상태` | 🟣 |
| 알림 | `3일 전 알림` | `plan_tasks` **ALTER** | `알림_오프셋일` | 🟣 |
| **체크 항목 N개** | `지출 가능 여부 최종 확인` 외 2 | `plan_tasks` **ALTER** | `parent_task_id` 자기참조 | 🟣 |
| 메모 | `0/500` | `plan_tasks` **ALTER** | `메모` | 🟣 |

⚠️ **일정 하나에 체크 항목이 여러 개**입니다. `parent_task_id` 자기참조를 넣으면 테이블 하나로 해결됩니다.
⚠️ **알림은 실제 발송하지 않고 D-day 배지로만** 표시하겠습니다 (푸시 인프라 없음).

---

# 화면 14. 마이페이지

## ① 계정 카드

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 아바타 | `체` | — | 팀명 첫 글자 | 🔵 |
| 팀명 | `체쿠메이트` | `accounts` **ALTER** | `팀명` | 🟣 |
| 이메일 | `team@startup.kr` | `accounts` | `email` | 🟣 |

### 6칸 그리드

| 칸 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 팀 이름 | `체쿠메이트` | `accounts` **ALTER** | `팀명` | 🟣 |
| 이메일 | `team@startup.kr` | `accounts` | `email` | 🟣 |
| 선정 사업 | `2026 초기창업패키지` | `f_profile` | `사업명` | 🟢 |
| 주관기관 | `건국대학교 창업지원센터` | `orgs` | `기관명` | 🟢 |
| 사업 중복 수혜 여부 | `X` | `f_profile` **ALTER** | `중복수혜여부` | 🟢 |
| 적용 기준 | `사업 공통 규정 + 기관 세부기준` | — | 파생 | 🔵 |

✅ **이름 · 연락처는 없습니다. 팀명만.** (회의 확정)

## ② 사업 정보

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 사업명 | `2026 초기창업패키지` | `f_profile` | `사업명` | 🟢 |
| 기관 | `건국대학교 창업지원센터` | `orgs` | `기관명` | 🟢 |
| 협약 시작·종료일 | `2026.03.01` / `2026.12.31` | `f_profile` | `협약시작일` `협약종료일` | 🟢 |
| 사업 지원비 | `50,000,000원` | `f_profile` | 4분면 **합계** | 🔵 |

## ③ 공통 관리 기준 — 항목 하나당

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 배지 | `자동 반영` | `documents` | `layer ∈ (L1,L2)` | 🔵 |
| **제목** | `2026년 초기창업패키지 창업기업 모집공고·사업화 자금 범위` | `documents` **신설** | `제목` | 🟢 |
| **발행처** | `중소벤처기업부·창업진흥원` | `documents` **신설** | `발행처` | 🟢 |
| **설명** | `현재 사업 기준` | `documents` **신설** | `설명` | 🟢 |

⚠️ `documents` 에 사람이 읽는 제목·발행처·설명이 없습니다. **없으면 화면에 파일명이 그대로 노출됩니다.**

## ④ 주관기관 세부 안내

| 표시 요소 | 화면 예시 | 테이블 | 필요한 값 | 출처 |
|---|---|---|---|:--:|
| 배지 | `사용자 등록` | `l3_documents` | 존재 | 🔵 |
| 파일명 | `2026 건국대학교 초기창업패키지 사업비 집행 안내.pdf` | `l3_documents` | `원본파일명` | 🟢 |
| 등록일 | `2026.03.02 등록` | `l3_documents` **ALTER** | `created_at` | 🟢 |
| `파일 교체` | | `l3_documents` | `status → superseded` | 🟣 |

✅ `원문 보기` · `파일 보기` 는 **삭제**합니다 (뷰어 없음).

## ⑤ 내 정보 수정 — F 프로필

| F축 | 어디서 채우나 | 테이블 |
|---|---|---|
| **F1 협약** 4분면 + 기간 | 이 화면 (현물 2칸은 접어둠) | `f_profile` |
| **F3** `귀속월` `금액` | **집행 완료 처리할 때** | `f_exec` |
| **F4** `역할` `고용형태` `타사업참여율` `소속기관유형` `겸직` | **인건비 비목 질문으로 필수 수집** | `f_personnel` |
| PMS 붙여넣기 | 브라우저 파싱 · **원문 서버 전송 없음** | — |

🔴 **F4 인력 이름 입력칸은 만들지 않습니다.**
🔑 별도 "F 프로필 탭"이 필요 없어졌습니다 — **지출 흐름 안에서 자연스럽게 모입니다.**

---

# 부록. 담당자 작업 목록

## 신설 테이블

| 테이블 | 용도 |
|---|---|
| `tenant.expense_plans` | 지출 계획 (draft 포함) |
| `tenant.plan_tasks` | 결제 전/후 체크 + 일정 |
| `corpus.check_items` | 확인 항목 · 질문 폐쇄 목록 |
| `corpus.evidence_sources` | 증빙 발급처 (CSV 124행 준비됨) |

## ALTER — 대부분 0행이라 5분

| 대상 | 추가할 것 | 근거 화면 |
|---|---|---|
| `tenant.expense_plans` | **`첨부파일` JSONB** | 새 계획 ① · 상세 ②⑧ |
| `tenant.expense_plans` | **`거래처`** | 새 계획 ① · 상세 ② |
| `tenant.expense_plans` | **`추가확인답변` JSONB** | 새 계획 ③ |
| `tenant.plan_tasks` | `유형` · `알림_오프셋일` · `메모` · **`parent_task_id`** | 일정 추가 모달 |
| `tenant.accounts` | **`팀명`** | 홈 인사말 · 마이페이지 |
| `tenant.f_profile` | **`중복수혜여부`** | 마이페이지 |
| `tenant.f_profile` | **`지원유형`** | 홈 사업 카드 |
| `tenant.l3_documents` | **`created_at`** | 마이페이지 등록일 |
| `tenant.orgs` | `주소` · `부서` + **데이터 1행** | 온보딩 ② |
| `tenant.decisions` | `plan_id` · **`요약`** · **`해설`** | 상세 ③④ |
| `corpus.documents` | **`제목`** · `발행처` · `설명` | 마이페이지 ③ · 상세 ⑥ |

**`corpus.documents` 만 실제 작업입니다** (283행 UPDATE). 급하시면 현재 사업 L1·L2 몇 건만 먼저 채워 주셔도 화면이 돕니다.

## 부탁 2건

> **A.** `tenant` 를 **Exposed schemas + RLS** — 테이블이 생기는 대로 붙이겠습니다.
> **B.** `corpus.documents` · `doc_articles` · `rules` **셋만 먼저 Supabase 에** — 마이페이지와 적용 근거가 즉시 실데이터가 됩니다.

## 프론트가 전담 (요청 없음)

첨부파일 Storage 버킷·RLS·압축·해시 · CSV 생성 · D-day/진행률/잔액 계산 · 일괄 점검 직렬 큐 · 비목 라벨 교정 · 상태 문구 · 예시 시드 데이터

---

# 🔴 문의 — 2건

## 1. `POST /api/normalize` 요청 본문

화면은 폼이라 사용자가 **금액·날짜를 숫자로 정확히** 입력합니다. 그런데 백엔드 계약은 자연어 한 줄이라, 합쳐서 보내면 **AI 가 금액을 다시 뽑는 과정에서 어긋날 수 있습니다.**

```
사용자 입력    1,000,000원
합성 문장      "노트북 2대, 100만원"
AI 가 읽으면   대당 100만원?  총 100만원?
```

| | 보내는 것 | 결과 |
|---|---|---|
| (A) | 합성 문장만 | AI 가 전부 추출 — 틀리면 입력값이 바뀜 |
| **(B)** | 문장 + 사용자가 입력한 원래 값 | AI 는 품목·비목만, **금액·날짜는 입력값 그대로** |

> **(B) 로 가도 될까요?** 요청 본문에 `금액` · `집행예정일` 을 함께 실으면 됩니다.

## 2. 거래처 최종 처분

회의록 두 줄이 서로 다릅니다.

```
§2   "거래처명 받는걸로, AI 판정 반영은 불가"  →  일단 받기는 하기
§33  "거래처 관련 내용 삭제 결정 필요"
```

| | |
|---|---|
| **(A)** | `expense_plans.거래처` 컬럼 신설 — 저장·표시하되 판정에는 안 실음 |
| (B) | 화면에서 완전 제거 |

> **현재는 (A) 로 만들어 두었습니다.** 컬럼 하나라 비용이 없고, 사용자가 나중에 "그때 어디랑 계약했더라"를 볼 수 있습니다.
> **(B) 로 결정되면 지우겠습니다.**
