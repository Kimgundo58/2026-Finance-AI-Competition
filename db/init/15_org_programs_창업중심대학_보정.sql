-- ════════════════════════════════════════════════════════════════════════════
-- 15_org_programs_창업중심대학_보정.sql — 데모 org 의 사업 등록 누락 보정 (Q5, 2026-09-04)
--
-- 🔴 **발견 경위 — Q1(ai-d7) → Q5 인계, `scratchpad/Q1_org_programs_이상_Q5용_0904.md`.**
--    데모 org `cfeba091-251a-5ae4-8cc9-88c6e6679440`(경상국립대학교 창업중심대학사업단)의
--    `tenant.org_programs` 실측 행은 **「모두의 창업 프로젝트」(멘토기관·2026) 하나뿐**이었다
--    — 기관명에 「창업중심대학」이 들어가는데 정작 그 사업 등록이 없었다.
--
-- 🔴 **왜 데모 시나리오를 안 바꾸고 데이터를 보정하는가.**
--    오너 결정(`scratchpad/QA_레인_프롬프트_0904.md`)이 이미 「데모 기관 = 경상국립대학교
--    창업중심대학사업단. 사업 = 창업중심대학」으로 못박혀 있다(재논의 대상 아님).
--    실물 L3 문서 2건(`2026_Finance_DATA_FOR_RAG/_테스트_L3/`)도 이 기관의 **창업중심대학**
--    2024년 사업비 사용 안내문·변경신청서다 — 사업 자체는 실재한다. `org_programs` 가
--    이 사실을 놓친 것이지, 데모 시나리오가 틀린 게 아니다.
--
-- 🔴 **`org_programs` 는 지금 어디서도 실행 시점에 검증되지 않는다** (server/·scripts/
--    전수 grep, 2026-09-04 — `load_org_programs.py` 적재기 하나뿐). 그래서 이 행이
--    없어도 `POST /api/judge` 가 "창업중심대학" 을 422 로 막지는 않는다(확인함) —
--    이 보정은 **기능 블로커가 아니라 데이터 정합성**이다. 다만 이 표를 프론트가 나중에
--    "이 기관이 참여하는 사업 목록"으로 노출하면 그때는 실제 블로커가 된다.
--
-- 🔴 기존 「모두의 창업 프로젝트」 행은 **지우지 않는다** — 이 기관이 실제로 그 사업의
--    멘토기관이라는 사실 자체는 유효하다. 이 파일은 추가만 한다.
-- ════════════════════════════════════════════════════════════════════════════

INSERT INTO tenant.org_programs
       (org_id, "사업명", 기준연도, 역할, 구분, 연도출처, 출처표기, 출처파일)
VALUES ('cfeba091-251a-5ae4-8cc9-88c6e6679440', '창업중심대학', 2024, '주관기관', '',
        -- 🔴 연도출처='파일명' — 정식 기관명부 크롤링 산출이 아니라 L3 업로드
        --    파일명(「L3_2024창업중심대학_…」)에서 되짚은 값이다. 다른 사업 행들처럼
        --    본문 대상년도로 검증된 게 아니라는 걸 이 값이 정직하게 남긴다.
        '파일명', '경상국립대학교 창업중심대학사업단',
        '2026_Finance_DATA_FOR_RAG/_테스트_L3/L3_2024창업중심대학_경상국립대_사업비사용안내문.hwp')
ON CONFLICT DO NOTHING;

-- ════════════════════════════════════════════════════════════════════════════
-- 검증 — 친 뒤 반드시 이걸로 확인
--   SELECT "사업명", 기준연도, 역할 FROM tenant.org_programs
--     WHERE org_id='cfeba091-251a-5ae4-8cc9-88c6e6679440';
--   → 「모두의 창업 프로젝트」(기존) · 「창업중심대학」(신규) 두 행이 나와야 한다
-- ════════════════════════════════════════════════════════════════════════════
