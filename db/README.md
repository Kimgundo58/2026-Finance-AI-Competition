# 로컬 개발 DB 사용법

개발 지식이 없어도 따라 할 수 있게 쓴 문서입니다. **명령어를 외울 필요 없습니다** — Docker Desktop 앱에서 버튼으로 대부분 됩니다.

---

## 1. 지금 무엇이 돌아가고 있나

컨테이너(= 작은 가상 컴퓨터) **2개**가 켜져 있습니다.

| 이름 | 하는 일 | 주소 |
|---|---|---|
| **suddoe-db** | 데이터가 실제로 저장되는 곳 (Postgres + pgvector) | `localhost:5432` |
| **suddoe-pgweb** | 그 데이터를 **눈으로 보는 화면** | http://localhost:8081 |

`suddoe`는 두 컨테이너를 묶은 그룹 이름입니다.

---

## 2. Docker Desktop 앱에서 보는 법

1. **Docker Desktop** 실행
2. 왼쪽 메뉴에서 **Containers** 클릭
3. **`suddoe`** 라는 묶음이 보입니다. 왼쪽 화살표(▶)를 누르면 펼쳐집니다

```
▼ suddoe                          ← 이 프로젝트 그룹
    suddoe-db       Running   5432:5432
    suddoe-pgweb    Running   8081:8081
```

### 각 칸에서 할 수 있는 것

| 클릭할 곳 | 결과 |
|---|---|
| **`8081:8081`** 파란 링크 | 브라우저가 열리면서 **데이터 보는 화면**이 뜹니다 |
| 컨테이너 **이름** | 상세 화면 (Logs / Inspect / Stats / Exec 탭) |
| **Logs** 탭 | 무슨 일이 일어나는지 실시간 기록. 문제 생기면 여기부터 봅니다 |
| **Stats** 탭 | CPU·메모리 사용량 |
| ■ **정지 버튼** | 컨테이너 끄기 (데이터는 남습니다) |
| ▶ **재생 버튼** | 다시 켜기 |
| 🗑 **휴지통** | 컨테이너 삭제 — **데이터는 Volumes에 남아 있어서 안 지워집니다** |

### 저장된 데이터는 어디에?

왼쪽 메뉴 **Volumes** → **`suddoe_pgdata`**

여기가 진짜 데이터가 사는 곳입니다. 컨테이너를 지워도 이게 남아 있으면 데이터는 살아 있습니다. **이걸 지우면 데이터가 완전히 사라집니다.**

---

## 3. 데이터 눈으로 보기 — http://localhost:8081

브라우저에서 여세요. **로그인 없이 바로 연결됩니다.**

| 화면 위치 | 설명 |
|---|---|
| 왼쪽 목록 | **스키마 3개**(`corpus` · `tenant` · `eval`)에 나뉜 표 **32개** + 뷰 1개 |
| 표 이름 클릭 | 그 표의 내용이 오른쪽에 나옵니다 |
| 위쪽 **Rows / Structure** 탭 | 데이터 / 표 구조 전환 |
| 위쪽 **Query** 탭 | 직접 SQL 실행 (몰라도 됩니다) |

### 제일 먼저 볼 것: `v_적재현황`

왼쪽에서 **`v_적재현황`**을 누르면 표별로 데이터가 몇 건 들어왔는지 한눈에 보입니다.

```
01. corpus.documents      0
02. corpus.doc_articles   0
03. corpus.chunks         0
...
```

지금은 전부 **0** — 표만 만들어 놓고 데이터는 아직 안 넣었으니 정상입니다.
앞으로 파서를 돌리면 이 숫자가 올라갑니다. **작업이 잘 됐는지 확인하는 계기판**이라고 생각하면 됩니다.

---

## 4. 표가 세 묶음으로 나뉜 이유부터

2026-08-31 에 표를 **세 묶음(스키마)** 으로 갈랐습니다. 이름이 `corpus.documents` 처럼
`묶음이름.표이름` 으로 보입니다.

| 묶음 | 무엇이 들어가나 | 왜 갈랐나 |
|---|---|---|
| **corpus** | 법령·지침 같은 **공개 규범**과 룰 | 누구에게나 같은 내용. 사용자별 구분이 필요 없다 |
| **tenant** | 사용자가 올린 **기관 규정(L3)**, 계정, 협약·집행 정보, 판정 기록 | 남의 것이 보이면 안 된다 — 여기만 접근 제어(RLS)를 건다 |
| **eval** | **채점용 정답지** | 나중에 클라우드(Supabase)로 옮길 때 **아예 안 올린다** |

> 🔴 **왜 중요한가.** 나중에 옮겨 갈 Supabase 는 `public` 이라는 묶음을 **자동으로 인터넷에
> 공개**합니다. 예전처럼 전부 `public` 에 두면 주소창에 `/rest/v1/golden_set` 을 치는 것만으로
> 정답지가 밖으로 나갑니다. 그래서 `public` 은 **비워 두고**, 정답지는 아예 올리지 않습니다.

### 표 32개가 각각 뭔지

**corpus — 공개 규범·룰**

| 표 이름 | 무엇이 들어가나 |
|---|---|
| `documents` | 원본 문서 목록 (어느 계층인지, 현행인지 구버전인지, **검색 진입점인지**) |
| `doc_articles` | 문서를 **조(條) 단위**로 쪼갠 원문 |
| `chunks` | 검색용으로 가공한 조각 + **1024개 숫자(임베딩)** ← 검색의 핵심 |
| `case_chunks` | R&D 사례집 Q&A (참고용, 판정 근거로는 못 씀) |
| `rules` | 비목별 허용/한도/사전승인/증빙 ← 빠른 판정의 핵심 |
| `precedence_rules` | **어느 계층이 이기는가** (8개 사업 중 6개가 "관리기준이 기관규정을 이긴다") |
| `refs` | 조문끼리의 **참조 그래프** ("제33조에 따른다" 를 따라가는 길) |
| `chunk_terms` · `chunk_len` | 단어 기반 검색(BM25)용 색인 |
| `item_alias` | "맥북" → "기계장치" 같은 상품명 매핑 사전 |
| `xref_mismatch` | 문서끼리 조문 번호가 안 맞는 사례 기록 |
| `check_items` 🆕 | "결제 전/후 확인" 항목의 **정해진 목록**. 자유 문장이면 재판정 때 사용자가 체크해 둔 게 날아갑니다 |
| `evidence_sources` 🆕 | 증빙서류를 **어디서 떼는지** (홈택스·IRIS 등). 판정에 안 쓰고 안내에만 씁니다 |
| `item_vocab` | 비목 어휘집 — 판정이 고를 수 있는 비목 10종의 정본 이름 |
| `programs` | 8개 사업 목록 |
| `recheck_queue` | 법령이 개정되면 **다시 봐야 하는 룰** 대기줄 (A4 개정 대응 에이전트가 채웁니다) |

**tenant — 사용자 것 (남의 것이 안 보이게 잠겨 있음)**

| 표 이름 | 무엇이 들어가나 |
|---|---|
| `orgs` | 기관 (○○대학교 창업지원단 등) |
| `l3_documents` · `l3_articles` | 사용자가 올린 **기관 규정** 원문 |
| `accounts` | 계정 |
| `f_profile` | 협약 정보 (기간·금액) |
| `f_exec` | 지금까지 쓴 돈 (비목별·거래처별) |
| `f_personnel` | 인력 (**이름은 저장하지 않습니다** — 역할과 숫자만) |
| `unmapped_premise` | "물어봐야 하는데 물어볼 칸이 없는 것" 모음 |
| `decisions` | 판정한 기록 (나중에 "왜 그렇게 판정했나" 를 설명하는 근거) |
| `incidents` | 사고 로그 — 남의 기관 규정이 인용되려 한 것 같은 **막아낸 사건** |
| `expense_plans` 🆕 | **지출 계획.** 홈·목록·상세·새 계획 4화면이 여기 묶입니다. 판정 전 임시저장도 여기 |
| `plan_tasks` 🆕 | **할 일** = 체크리스트와 캘린더가 같은 표를 다르게 보는 것 (`due_date` 가 있으면 캘린더에도) |

**eval — 채점**

| 표 이름 | 무엇이 들어가나 |
|---|---|
| `golden_set` | 채점용 정답지 77문항 — **절대 검색 대상에 넣으면 안 되고, 클라우드에도 안 올립니다** |
| `golden_chunks` | 각 문항의 정답이 **어느 청크인지** 고정해 둔 것 (검색 점수를 재는 기준) |
| `runs` | 채점을 **언제 어떤 조건으로 돌렸나** — 조건이 다른 회차끼리 점수를 빼면 안 됩니다 |
| `run_items` | 그 회차의 **문항별** 결과 |

### 지금 들어 있는 양 (2026-09-01 실측)

```
corpus.documents        283      corpus.chunks        20,525
corpus.doc_articles  26,301      corpus.chunk_terms  1,301,584
corpus.refs          44,855      corpus.rules             72
corpus.precedence_rules   8      corpus.case_chunks      193
tenant.decisions         73      tenant.l3_articles       17
eval.golden_set          77      eval.runs               188
```

---

## 5. 껐다 켜는 법

### 방법 A — Docker Desktop (쉬움)

`suddoe` 그룹 옆의 **■ 정지** / **▶ 재생** 버튼을 누르면 됩니다.

### 방법 B — 명령어

PowerShell을 열고 `db` 폴더로 이동한 뒤:

```powershell
cd "C:\Users\dogun\Downloads\Desktop\Desktop\Desktop\Desktop\김건도\3-1 여름방학\금융 AI공모전\db"

docker compose up -d      # 켜기
docker compose stop       # 끄기 (데이터 유지)
docker compose ps         # 상태 확인
docker compose logs -f    # 실시간 기록 보기 (Ctrl+C 로 나감)
```

### ⚠️ 전부 초기화 (데이터까지 삭제)

```powershell
docker compose down -v
```

`-v`가 **볼륨(데이터)까지 지운다**는 뜻입니다. 다시 `up -d` 하면 표가 새로 만들어집니다.

> **스키마를 고쳤을 때는 이걸 해야 합니다.** `init/01_schema.sql`은 **컨테이너를 처음 만들 때만** 실행됩니다. 파일만 고치고 재시작하면 반영되지 않습니다.

---

## 6. 접속 정보

| 항목 | 값 |
|---|---|
| 호스트 | `localhost` |
| 포트 | `5432` |
| 데이터베이스 | `suddoe` |
| 사용자 | `postgres` |
| 비밀번호 | `devpw` |
| 접속 문자열 | `postgresql://postgres:devpw@localhost:5432/suddoe` |

> 이 비밀번호는 **내 컴퓨터 안에서만** 쓰는 개발용입니다. 인터넷에 열려 있지 않습니다.
> 나중에 Supabase/Neon에 올릴 때 쓰는 진짜 비밀번호는 `.env` 파일에 넣습니다 (`.gitignore`에 있어서 GitHub에 안 올라갑니다).

비밀번호를 바꾸고 싶으면 `db` 폴더에 `.env` 파일을 만들고:

```
POSTGRES_PASSWORD=원하는비밀번호
```

그다음 `docker compose down -v` → `docker compose up -d`.

---

## 7. 문제가 생기면

| 증상 | 원인 / 해결 |
|---|---|
| `suddoe-db`가 빨간색(Exited) | **Logs 탭**을 먼저 볼 것 |
| `port is already allocated` | 5432를 다른 프로그램이 쓰는 중. 기존 Postgres를 끄거나 compose의 `"5432:5432"`를 `"5433:5432"`로 변경 |
| localhost:8081이 안 열림 | `suddoe-db`가 먼저 Healthy 여야 pgweb이 뜸. 30초 기다린 뒤 새로고침 |
| 표가 하나도 없음 | 스키마가 적용 안 됨 → `docker compose down -v` 후 `up -d` |
| 한글이 깨져 보임 | pgweb에서는 안 깨집니다. PowerShell에서 깨지면 화면 문제이지 데이터 문제가 아님 |

---

## 8. 구성 파일

```
db/
├─ docker-compose.yml     ← 컨테이너 2개 정의
├─ init/
│   ├─ 01_schema.sql      ← 스키마 3개 + 표 21개 생성 (최초 1회 자동 실행)
│   ├─ 02_frontend.sql    ← 프론트용 표 4개 신설 + 기존 2개 수정 (2026-08-31)
│   └─ 03_input_fields.sql ← 입력 필드 3건 (2026-08-31)
│                            지출계획에 "사업명" 칸 / 인력 "역할" 을 3종으로 제한 /
│                            골든셋에 "비목"·"입력필드" 칸
└─ README.md              ← 이 문서
```

스키마의 설계 근거는 `../RAG.md` §2에 있습니다.

---

## 9. 확인된 환경 (2026-08-13)

| 항목 | 값 |
|---|---|
| PostgreSQL | 17.10 |
| pgvector | 0.8.6 |
| pgweb | v0.17.0 |
| 로케일 | `C.UTF-8` (한글 가나다순 정렬 정상) |
| 한글 입출력 | 테스트 통과 |

> `ko_KR.utf8`은 이 이미지에 없어서 initdb가 실패합니다. `C.UTF-8`을 쓰는 이유이며, 현대 한글은 유니코드 코드포인트 순 = 가나다 순이라 정렬에 문제가 없습니다.
