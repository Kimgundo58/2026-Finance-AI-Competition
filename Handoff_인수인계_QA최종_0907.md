# Handoff — QA 최종 마감 (2026-09-07) · 중앙 ai-33

## 목표
Vercel 실화면 QA 결함을 전부 닫고, Qwen API 경로를 새 코드로 붙이고, 성능지표를 확정한다.

## 완료된 일
- 룰 82행·TIPS 8행 Cloud SQL 동기화 완료(신규 2 + 수정 67, 실패 0). 로컬=실서버.
- 실화면 판정 1회 관통: 판정(조건부)·비목추천·AI추천배지·결제전 5건은 «정상».
- 계약 테스트 326 passed. API 스키마 변경은 전부 완화/값이라 프론트 무영향.

## 진행 중인 일
- 중앙(ai-33): AI 해설(요약)이 조문 나열로 나오는 근본결함 — B0 프롬프트 + llm_schema 수정
- 레인 5개 아래 분배. 커밋·배포·DB쓰기는 «중앙만» 한다. 각 레인은 diff + md 로 보고.

## 다음 행동
레인별 지시는 아래 표. 끝나면 SendMessage 로 ai-33 에 보고.

## 주의점
- 프론트 레포는 `../2026-Finance-AI-Competition_Frontend` (owner wori1206, 배포 브랜치 main).
- 「서버가 만든다 ≠ 화면이 쓴다」가 이번 QA 의 지배적 실패형이다. 계약만 보고 「됐다」 하지 마라 — 실화면/실응답으로 확인.
- git 커밋 금지. DB 쓰기 금지.

---

## 레인 F1 (ai-4e) — 심층질문 배선  🔴 최우선
**결함**: 프론트 전체에 `심층질문` 문자열 0건. 화면 질문 6개는 `checkumait-app.tsx:437 CATEGORY_QUESTIONS` 하드코딩.
서버가 만든 심층질문(check_items 기반, 근거 조항 딸림)이 «한 번도» 화면에 안 실린다.
또 `Question` 컴포넌트(3490행)가 답을 `useState` 로만 들고 부모로 안 올린다 → 「확인 필요」를 눌러도 아무 데도 안 간다.
**해야 할 것**
1. `/api/normalize` 응답의 `심층질문[]`(code·질문문·유형·선택지·근거)을 step 3 에 렌더. 서버가 비면 기존 상수로 폴백.
2. 답을 부모 state 로 올려 `/api/judge` 요청의 `답변: [{code, 값}]` 에 실어 보낸다 (서버 `models.py:113 답변항목`, `main.py:1306 _사용자F값_조립` 이 이미 받는다).
3. 「확인 필요」는 값 `"미상"` 으로 보낸다 — 판정이 조건부+해야할일로 떨어지는지 실화면 확인.
4. 질문 옆에 `근거.조번호` 를 작게 노출(왜 묻는지). doc_id 원문 노출 금지.

## 레인 F2 (ai-36) — 적용 근거 표시
**결함**(실화면 확인)
- doc_id 가 raw 로 노출: `L1_중소기업창업_지원사업_통합관리지침_제14차개정_20251223`. `lib/adapt.ts 문서명보기()` 가 있는데 이 화면에서 안 먹는다 — 어느 컴포넌트가 우회하는지 찾아라.
- 인용 3건 중 1·2번이 같은 「제38조 외주용역비」 통짜 원문 중복. 실제로 걸리는 건 ③·④항인데 조 전체(10개 항)를 다 보여준다.
**해야 할 것**: 항호 단위로 잘라 보여주고, 같은 조 중복은 항호를 합쳐 1건으로. 문서명은 사람이 읽는 이름으로.

## 레인 F3 (ai-5c) — 결제 후 / 비목 설명
**결함**
- 실화면에 「결제 전 확인」만 있고 «결제 후» 섹션이 안 뜬다. 서버 `할일.구분` 은 결제전/결제후/집행 3종인데 화면이 결제후를 안 그리는지, 서버가 결제후를 안 만드는지 «양쪽» 확인.
- 프론트가 `비목후보[].설명` 을 읽는데(`checkumait-app.tsx:3041`) 서버 `비목후보` 모델은 `{비목, 신뢰도}` 뿐 → 설명 줄이 영원히 안 그려진다. 서버에 `설명` 을 채울지, 프론트에서 뺄지 판단해 제안.

## 레인 Q (ai-db) — Qwen API 연동 «새 코드»
기존 vLLM 경로 코드는 **건드리지 않는다**. 새 어댑터 파일을 판다.
1. `scripts/llm_qwen.py` (신규) — DashScope 국제판 OpenAI 호환 엔드포인트, `response_format={"type":"json_schema", "strict":true}`. 스키마는 `llm_schema.py` 것을 그대로 재사용.
2. 모델명은 **버전 고정**만. `SUDDOE_QWEN_MODEL` 기본 `qwen3.7-plus`, 폴백 `qwen3.8-flash`. 맨 `qwen-plus` 별칭은 배선 금지.
3. 스위치: `SUDDOE_LLM=vllm|qwen` 환경변수 하나로 갈아끼우게. 기본값은 vllm(현행 유지).
4. 판정 1건을 실제로 통과시켜 원출력을 md 에 붙일 것. 유료 호출 10회 이내.

## 레인 P (ai-50) — 성능지표 확정
1. 룰 82행 확정본 기준으로 골든셋 342건 평가 재실행(가능하면 held-out 분리 유지).
2. 지표: 판정 일치율 · 치명오답률 · 인용정확도 · 판단불가율 · 도달률. 공모전 제출 문구까지.
3. `scratchpad/P_성능지표_제출본.md` 갱신. **부풀리지 마라** — 튜닝셋/held-out 구분해 병기.

## 레인 W (ai-3f) — 「결제 전 확인」 문구 쉽게 (오너 지시 2026-09-07)
**오너 말**: "결제전 확인 이것들 너무 설명이 어려워. 글자도 너무 많고. 적용근거만 확실하면 결제전확인은 좀 더 쉽게."
**사실관계**: 이 문장들은 LLM 생성물이 아니라 `corpus.check_items` 의 `항목`·`설명` 컬럼이다. LLM 은 code 만 고른다(`llm_schema.py:130` 주석). 그러니 **DB 문안을 다시 쓰는 작업**이다.
**규칙**
- `항목`(제목) = 명령형 한 줄, **20자 이내**. 예: "2천만원 넘는 외주면 사업운영위원회 사전심의를 받으세요" → "사전심의 받기"
- `설명` = **한 문장, 45자 이내**. 조문 표현을 일상어로. 예: "부가세 포함 2천만원 초과 시 필수입니다. 점검항목 중 하나라도 부적정이면 집행할 수 없습니다." → "부가세 포함 2천만원이 넘으면 심의를 통과해야 집행할 수 있어요."
- **근거는 절대 바꾸지 마라** — `code`·근거 조항·`구분`·`유형` 은 그대로. 문안만 손댄다.
- 조번호·법령 기호(L1/L2/S번호)를 설명에 넣지 않는다.
**산출**: `scratchpad/W_체크항목_문안.md` 에 `code | 기존항목 | 새항목 | 기존설명 | 새설명` 표. **UPDATE 문은 만들되 실행하지 마라** — 중앙이 실행한다.
