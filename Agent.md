# Agent.md — RAG · 룰 · LLM 을 엮는 법

작성 2026-08-28

> **이 문서의 범위**: 오케스트레이션 순서 · 게이트와 분기 · 전제 해소 · 검증과 강등 ·
> 지연 예산 · 재현성 · 실패 경로.
> 각 축의 내부는 [`RAG.md`] · [`rule_base.md`] · [`LLM.md`].

## 0. 오케스트레이터는 LLM 이 아니다

**코드가 순서를 지휘한다.** 에이전트가 스스로 다음 행동을 고르는 구조가 아니다.

```
멀티 에이전트 오케스트레이션   기각
agentic multi-hop              기각
function calling               미개방
```

호출 수가 고정되므로 지연이 예측되고, 같은 입력에 같은 출력이 나온다.
**이 도메인에서 재현성은 편의가 아니라 요건이다** — 판정 이력을 나중에 설명해야 한다.

---

## 1. 요청 1건의 전 과정

```
"디자이너 쓸 맥북 250만원 사도 되나요?"
  |
  v  ------------------------------------------------------------ 0ms
(1) 정규화                                        LLM (로컬)
    -> {품목, 금액, 용도, 비목후보[{비목,신뢰도}],
        결제수단, 구매명의, 신청일, 비교견적}
    스트리밍 -> 화면 4 가 즉시 그려짐
    F5(거래처)는 이 단계에서 체크박스로 수집
  |
  v  ------------------------------------------------------------ ~800ms
(2) 룰 조회  ||  (3) 검색                         코드 (병렬)
    (2) rules 조회: 사업 X 기관유형 X 비목
         금지목록 적중 -> 여기서 끝. LLM 없이 즉답 "불가"      [빠른 경로]
         한도 있음     -> 코드가 금액 비교 -> 결과를 (4) 에 주입
    (3) 벡터 + BM25 -> RRF -> 필터 -> top-8 + 참조확장
    F 프로필 로드 (F1 / F3 / F4)
  |
  v  ------------------------------------------------------------ ~300ms
  [게이트] 최고 스코어 < 임계치  ->  (6) 판단불가로 직행
  |
  v
(4)-a 포섭 스크리닝 (선택 / 미결)                 LLM x8 병렬
    조항별 {해당 | 비해당 | 불확실}
  |
  v
(4)-b 판정 조립                                   LLM
    입력 = 문장번호 붙인 원문 + 참조확장분 + 룰 결과 + F 프로필
    출력 = 스키마 강제 JSON
  |
  v  ------------------------------------------------------------ ~900ms
(5) 전제 해소                                     코드
  |
  v
(6) 검증 / 강등                                   코드
  |
  v
(7) decisions 로그                                코드
  |
  v                                        총 ~3.2s (p50)
화면 5 판정 / 6 할일 / 7 근거
```

### 1-1. 단계별 주체와 예산

> 2026-08-28 `Rag_Agent구현 파이프라인.md` §6 에서 이관.

| # | 단계 | 주체 | 입력 -> 출력 | 예산 |
|---|---|---|---|---|
| (1) | 입력 정규화 | **LLM** | 자연어 -> `{품목, 금액, 용도, 비목후보[]}` | 스트리밍 |
| (2)-a | 비목 확정 | 코드 | `item_alias` 정확 조회 -> 실패 시 벡터 유사도 | ~150ms |
| **(2)-b** | **L3 룰 조회 (먼저)** | 코드(SQL) | org+비목 -> L3 룰 유무 | <10ms |
| **(2)-c** | **게이팅 판정** | 코드 | 4갈래 분기 (§3-2) | ~0 |
| (2)-d | L1·L2 룰 + 효력 결정 | 코드(SQL) | `precedence_rules` -> `effective_rule` | <20ms |
| (2)-e | 금액 비교 | 코드 | 금액 vs 한도 | ~0 |
| **(3)-a** | **L3 통째 로드** | 코드(SQL) | **검색 없음.** 사업비 관련 장 전체 | <20ms |
| (3)-b | 필터 확정 (L1·L2만) | 코드(SQL) | 후보 `chunk_id` 집합 | ~10ms |
| (3)-c | dense | pgvector | `ORDER BY <=>` top-50 | <200ms |
| (3)-d | sparse | BM25 (SQL) | 같은 집합 마스킹 top-50 | <50ms |
| (3)-e | RRF 결합 | 코드 | -> **top-5** (L3 가 별도로 들어오므로 8->5) | ~0 |
| **(3)-f** | **참조 폐포 수집** | 코드(재귀 CTE) | `refs` 따라 깊이 3까지 | <30ms |
| (4) | 판정 조립 | **LLM** | L3 전문 + 룰 + 폐포 -> 구조화 JSON | 스트리밍 |
| (5) | 전제 해소 | 코드 | 3갈래 (§4) | ~0 |
| (6) | 검증 / 강등 | 코드 | 문장번호 치환 + 강등 규칙 (§5) | ~0 |
| (7) | 로깅 | 코드 | `decisions` insert | ~0 |

**(2)-b · (3)-a · (3)-f 가 L3 우선 구조의 신설분이고, LLM 호출 수는 여전히 2회다.**
(3)-a 와 (3)-b~e 는 서로 독립이라 **병렬 실행**한다 — L3 를 먼저 보는 것이 지연을 늘리지 않는다.

> **왜 L3 부터인가** — 근거는 L3 문서 자체다. 건국대 「사업비 관리 운영 지침」 실측:
> *"이 지침에서 **정하지 아니한 사항**에 대해서는 「국가연구개발혁신법」 … 이 정하는 바에 따름"*
> **사용자 문서가 스스로 "나부터 보고, 없으면 상위로 가라" 고 지시한다.**

---

## 2. 세 축의 접점 — 누가 무엇을 넘기나

| 경계 | 넘어가는 것 | 넘어가지 **않는** 것 |
|---|---|---|
| (1) -> (3) | JSON 필드 (코드가 쿼리 조립) | LLM 이 만든 검색어 |
| (3) -> (4) | 조문 **원문** + 문장번호 | 요약본 |
| (2) -> (4) | **비교가 끝난** 룰 결과 | 원시 한도값 (LLM 이 계산하게 두지 않는다) |
| (4) -> (6) | 문장번호 인용 | 원문 문자열 |
| (6) -> 화면 | DB 원문으로 치환한 인용 | LLM 이 쓴 인용문 |

**핵심: LLM 에 도달하기 전에 숫자는 이미 결론이 나 있다.**

---

## 3. 게이트와 분기 (판정이 닫히는 지점)

```
A. 금지목록 적중        (2) 에서 즉답 "불가"           LLM 0회
B. 검색 스코어 미달     게이트에서 판단불가            LLM 1회 ((1)만)
C. 비목 신뢰도 갈림     두 경로를 모두 판정해 보여줌   LLM 2배
D. 정상 경로            (4)-b 까지                     LLM 2~3회
```

### C — 비목이 갈릴 때

```
신뢰도 >= 임계치 (단일 후보)
   "기자재비로 봤습니다"   [맞아요] [바꿀게요]

갈림 (예: 0.7 / 0.3)
   기자재비로 보면 -> 조건부 (사전승인 필요)
   재료비로 보면   -> 불가
              어느 쪽인가요?
```

**"비목을 고르세요" 는 문제를 사용자에게 되돌려주는 것이고,
각 선택의 결과를 보여주고 고르게 하는 것은 정보를 주는 것이다.**

부수효과: 컨펌 로그가 `item_alias` 를 자동으로 채운다 -> 파인튜닝 없이 어휘 갭이 메워진다.

### 3-2. L3 게이팅 — "L3 만으로 답이 되는가" 를 코드가 4갈래로 가른다

위 A~D 와 **다른 축**이다. 위가 "판정이 어디서 닫히나" 라면 이건 "상위를 봐야 하나" 다.
**LLM 에게 묻지 않는다.** 룰 조회 결과의 모양만 보면 안다 — 물으면 호출이 3회가 되고
`구현.md` §1-5 가 기각한 agentic 루프가 된다.

```python
l3 = SELECT * FROM rules WHERE layer='L3' AND 기관ID=:기관 AND 비목=:비목

match l3:
    case None:                          # (1) L3 에 규정 없음
        # = 문서가 말한 "정하지 아니한 사항"  -> 상위로
        need_upper = True

    case r if r.참조만:                  # (2) "지침 제N조에 따른다" 만 있음
        need_upper = True; seed_refs = r.근거     # 참조 그래프로 따라간다

    case r if r.허용 in ('불가','조건부'):  # (3) L3 가 막거나 조건을 건다
        # 상위가 더 관대해도 결론이 안 바뀐다. 틀려도 "틀린 불가" = 안전
        need_upper = False               # 단, 근거 보강용으로 상위도 같이 싣는다

    case r if r.허용 == '가능':           # (4) L3 가 "가능" 이라고만 함
        need_upper = True                # 강제. 아래 참조
```

#### 🔴 왜 (4) 가 강제인가 — L3 는 단독으로 완결되지 않는다

```
L3 제15조: "기자재는 100만원 초과 시 총장 승인"
```

이것만 보고 답하면 -> *"총장 승인만 받으면 됩니다"* -> **틀린 "가능"** 이다.
**그 기자재를 애초에 살 수 있는지**(범용성 기자재 소명 필요 여부)는 L1 통합관리지침
제39조에만 있다.

**주관기관 규정은 국가 지침이 있다고 전제하고 그 위에 얹는 문서다. 구조적으로 자족하지 않는다.**
그래서 "불가" 는 단독으로 안전하지만 **"가능" 은 절대 단독으로 확정하지 않는다.**
원칙 1(오답 비대칭)을 실행 순서에 그대로 옮긴 것이다.

> ⚠️ 예외: (3) 으로 갈렸으면 검색 스코어가 임계치 미달이어도 판단불가로 보내지 않는다.
> L3 에 명시적 근거가 이미 있어 인용할 조항이 있다. 검색 실패는 상위 근거 보강에
> 실패한 것일 뿐이다.

#### (2)-d 효력 결정 — `precedence_rules` 가 병합 규칙을 정한다

**L3 가 항상 이기는 게 아니다.** 알고리즘 전문은 [`rule_base.md`] §3-1. 요약:

| `precedence_rules` | 병합 |
|---|---|
| `범위='unspecified_only'` (건국대형: "정하지 아니한 사항은 상위로") | L3 에 있으면 L3, 없으면 상위 |
| `범위='all'`, 우선=L2 (8개 사업 중 6개) | **L2 가 이긴다.** L3 는 "참고" 로 병기 |
| 조항 없음 | 폴백 — 상위 우선 + 같은 층에서 갈리면 엄격한 쪽 |

---

## 4. (5) 전제 해소 — 3갈래

LLM 이 낸 `전제[]` 의 `매핑` 을 코드가 본다.

```
a. F 있음 + 값 있음  ->  즉시 검증 -> 전제 소멸
b. F 있음 + 값 없음  ->  인라인 입력 요청 (화면에서 30초)
c. F 없음            ->  unmapped_premise 로깅 -> 화면 9 문의
```

**c 가 쌓이는 게 F축 설계의 피드백 루프다.** 빈도 높은 unmapped 는 다음 F 필드 후보다.
채울 수 없는 전제도 있다 — 그건 판단불가로 정직하게 닫는다.

---

## 5. (6) 검증과 강등 — 마지막 방어선

| 조건 | 조치 | 왜 |
|---|---|---|
| 인용 문장번호 -> DB 원문 치환 | 항상 | 인용 위조 차단 |
| 인용 0개 | -> **판단불가** | 근거 없는 판정은 판정이 아니다 |
| "가능" 인데 인용이 B등급뿐 | -> **조건부로 강등** | 사례는 단독 근거가 될 수 없다 |
| (4)-a "불확실" >= 임계 | -> **판단불가** | |
| 근거조항 없는 전제 | -> **폐기** | |
| `verified=false` 룰 단독 "가능" | -> **조문 인용 강제** | |
| 🔴 **인용의 `기관ID` 가 NULL 도 현재기관도 아님** | -> **즉시 폐기 + 사고 로그** | 멀티테넌시 누수. RLS 가 뚫렸을 때의 3차 방어 |
| 🔴 **판정이 "가능" 인데 인용이 L3 단독** | -> **조건부로 강등** | §3-2 (4) 의 실행판. L3 는 추가 제약만 적는다 |
| 참조 폐포에 **`dangling` 엣지**가 있었음 | -> 경고 + **신뢰등급 하향** | 근거 불완전 신호 |
| 인용 문서의 `domain != '창업지원사업'` | -> 경고 문구 강제 삽입 | `documents` 를 join 해 본다 (chunks 에는 없다) |
| `extraction='vlm'` 청크 인용 | -> **A등급 금지** | 스캔 판독본 |
| 버전 스탬프 부착 | 항상 | "제14차, 2025.12.23 기준" |

**precedence_rules 는 여기서 적용된다.** L2 > L3 가 규칙이므로,
L3 가 더 엄격해도 (4) 가 L3 를 근거로 "불가" 를 냈다면 여기서 뒤집힌다.

---

## 6. 지연 예산

| 단계 | 예산 | 비고 |
|---|---:|---|
| (1) 정규화 | ~800ms | 스트리밍 -> 체감은 1초 미만 |
| (2)+(3) 병렬 | ~300ms | 참조확장 10ms 포함 |
| (4)-a | 병렬 | 채택 시 |
| (4)-b | ~900ms | 스트리밍 |
| (5)(6)(7) | ~100ms | 코드 |
| **합계** | **~3.2s (p50)** | |

**원칙(응답은 초 단위)이 지켜지는 이유**: (4)-a 가 병렬이고 (1)·(4)-b 가 스트리밍이라
사용자는 첫 글자를 1초 안에 본다.

⚠️ **임베딩이 앱 서버 CPU 를 쓴다.** KURE-v1 이 워커당 ~1.5GB.
호스팅 사양이 곧 동시성 한계다 — `RAG.md` §2-8 참조. 동시 요청이 늘면 여기가 먼저 막힌다.

---

## 7. 재현성

| 무엇 | 어떻게 |
|---|---|
| LLM 호출 수 | 슬롯별 고정. function calling 미개방 |
| 검색 결과 | 모델 비교 시 **스냅샷으로 고정** |
| 코퍼스 버전 | `documents.version` |
| 판정 이력 | `decisions` 에 입력 · 인용 · 판정 · 버전 저장 |

✅ **BM25 가 DB 안으로 들어와 이 항목이 해소됐다** (2026-08-28, `RAG.md` §2-5-1).
`chunk_terms` / `chunk_len` / `term_df` 라 재인덱싱이 **트랜잭션 하나**가 된다.
버전 대조도 기동 거부 로직도 필요 없다.

---

## 8. 판단불가 경로 — 실패가 아니라 제품 기능

```
case_chunks 검색 (질문 임베딩 <-> 사례 질문 임베딩)
  -> 상위 3건 표시
  -> "R&D 사례 참고" 라벨 + "귀하의 사업에 적용되지 않습니다" 문구 필수
  -> 담당자 문의 메일 초안 자동 생성 (화면 9)
```

**사례는 판정에 영향을 주지 않는다.** 4-way 판정은 이미 `판단불가` 로 확정된 상태이고,
`case_chunks` 는 이 경로에서만 조회된다 — **함수 레벨 강제** ([`RAG.md`] §2-2).

---

## 9. 실패 경로 전수

```
(1) 정규화 실패        -> 재질문 (화면 3)
(2) 룰 없음            -> 정상. (3)(4) 로 진행
(3) 검색 0건 / 저스코어 -> 판단불가 + 문의 유도
(4) 스키마 위반        -> 1회 재시도 -> 실패 시 판단불가
(4) 타임아웃           -> 판단불가
(5) unmapped 다수      -> 판정은 내되 "미확인 전제 N개" 표시
(6) 인용 검증 실패     -> 판단불가
DB 연결 실패           -> 503. 판정을 추측으로 만들지 않는다
LLM API 실패           -> 어댑터 폴백 -> 전부 실패 시 503
```

**모든 실패의 기본값은 "판단불가" 다. "아마 가능" 은 어떤 경우에도 만들지 않는다.**

---

## 10. 비동기 Agent 계층 — 판정 경로 **밖**

> 2026-08-28 `Rag_Agent구현 파이프라인.md` §7 에서 이관. 구 계층 표기(L4)를 L3 로 정정했다.

**판정 1건은 결정적 파이프라인이다. Agent 는 지연 예산이 없는 비동기 작업에만 둔다.**

| Agent | 트리거 | 도구 | 산출 | 우선순위 |
|---|---|---|---|---|
| **A4 개정 대응** | 배치(주 1회) / 수동 | 조문 diff, DB 조회 | 변경 조항 + 영향받는 룰 + 재검수 큐 | ⭐ 1 |
| **A2 엄격조항 검토** | L3 업로드 시 | 청크 검색, 조항 비교 | 국가 규정보다 엄격한 조항 후보 | ⭐ 2 |
| A5 답변 축적 | 기관 공식 답변 수신 | 룰 스키마 변환 | 룰 후보 -> 검수 큐 | 3 |
| 정산 리허설 | 사용자 요청 | 판정 파이프라인 N회 | 집행내역별 판정 + 리스크 요약 | 4 |
| ~~판정 1건~~ | — | — | — | ❌ **금지** |

### 10-1. A4 개정 대응 (1순위 — 데모 킬러)

Stage 0 이 만든 조 단위 JSON 끼리 비교하므로 **추가 개발이 거의 없다.**

```
1. 조 매칭      조번호 + 조제목으로 제N차 <-> 제N+1차 대응 관계 수립
                (번호가 밀렸으므로 제목 매칭이 주, 번호는 보조)
2. 본문 diff    매칭된 조끼리 문자열 diff
3. 영향 판별    변경된 조를 근거로 삼는 rules 행 조회 (rules.근거 JSONB)
4. 재검수 플래그 해당 룰 verified = false 전환
5. 요약 생성    변경 요약 + 영향 범위 (LLM)
```

**실증 재료가 이미 있다** — 제12차 -> 제13차 -> 제14차에서 조번호가 실제로 밀렸고,
2025년판 세부관리기준이 제12차 번호를 참조한 채 남아 있다.
가상 시나리오가 아니라 **실제 사고를 재현하는 데모**다.

### 10-2. A2 엄격조항 검토 (2순위)

```
for 조항 in L3(현재 기관):
    대응조항 = chunks 검색(layer IN ('L1','L2'), 같은 비목/주제)
    비교 = LLM(L3 조항, 대응조항) -> {더엄격 | 동일 | 더완화 | 무관}
    if 더엄격: 후보 목록 + 룰 오버레이 제안
```

🔴 **시연 재료가 사라졌다.** 원래는 `apply_mode='compare'` 인 타 대학 규정 24건으로
시연할 계획이었으나, ① 그 24건은 `archive/` 로 갔고 `index_guard.py` 가 막으며
② `apply_mode` 컬럼 자체를 2026-08-28 제거했다.
**6주차 전까지 시연 방법을 다시 정해야 한다** (§10 미결).

### 10-3. 비용 방어 (공개 URL + 유료 API = 사고 구조)

| 방어 | 값 |
|---|---|
| 일일 총 호출 상한 | 하드 캡. 초과 시 "데모 한도 소진" 안내 |
| IP 당 시간당 제한 | N회 |
| 동일 질문 캐시 | "지난주에 물어본 건데 또" 대응과 겸용 |
| 공개 기간 | 심사 기간만 개방, 이후 비공개 전환 |
| 키 보호 | 서버 환경변수. 프론트 노출 금지, 반드시 백엔드 경유 |
| 콜드 스타트 | 무료 호스팅은 비활동 시 sleep. **발표 30분 전 미리 깨워둘 것** |

---

## 11. 미결

| # | 미결 | 영향 |
|---|---|---|
| 1 | **(4)-a 채택 = LLM 2회 vs 3회** | 지연 예산 · 화면 7 설계 · `구현.md` §8-2 와 충돌 |
| 2 | 게이트 임계치 값 | 골든셋 평가 후 튜닝 |
| 3 | "불확실" 임계 | 동상 |
| 4 | 비목 신뢰도 임계 | 동상 |
| 5 | 동시 요청 한계 | 임베딩 워커 수 실측 필요 |
