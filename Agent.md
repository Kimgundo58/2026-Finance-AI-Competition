# Agent.md — RAG · 룰 · LLM 을 엮는 법

범위: 오케스트레이션 순서 · 게이트 · 전제 해소 · 검증·강등 · 지연 예산 · 실패 경로 · 비동기 에이전트.
각 축의 내부는 `RAG.md` · `rule_base.md` · `LLM.md`. 상위 문서는 `서비스 아키텍쳐.md`.

## 0. 오케스트레이터는 LLM 이 아니다

**코드가 순서를 지휘한다.** 에이전트가 스스로 다음 행동을 고르는 구조가 아니다 —
멀티 에이전트 오케스트레이션 없음 · agentic multi-hop 없음 · function calling 미개방.
호출 수가 고정되므로 지연이 예측되고, 같은 입력에 같은 출력이 나온다.
**이 도메인에서 재현성은 편의가 아니라 요건이다** — 판정 이력을 나중에 설명해야 한다.

## 1. 요청 1건 — 단계별 주체와 예산

| # | 단계 | 주체 | 입력 → 출력 | 예산 |
|---|---|---|---|---|
| (1) | 입력 정규화 | **LLM** | 자연어 → `{품목, 금액, 용도, 비목후보[]}` | 스트리밍 |
| (2)-a | 비목 확정 | 코드 | `item_alias` 정확 조회 → 실패 시 벡터 유사도 | ~150ms |
| (2)-b | **L3 룰 조회 (먼저)** | 코드(SQL) | org+비목 → L3 룰 유무 | <10ms |
| (2)-c | **게이팅 판정** | 코드 | 4갈래 분기 (§3-2) | ~0 |
| (2)-d | L1·L2 룰 + 효력 결정 | 코드(SQL) | `precedence_rules` → `effective_rule` | <20ms |
| (2)-e | 금액 비교 | 코드 | 금액 vs 한도 | ~0 |
| (3)-a | **L3 통째 로드** | 코드(SQL) | 검색 없음. 사업비 관련 장 전체 | <20ms |
| (3)-b | 필터 확정 (L1·L2만) | 코드(SQL) | 후보 `chunk_id` 집합 | ~10ms |
| (3)-c | dense | pgvector | top-50 | <200ms |
| (3)-d | sparse | BM25(SQL) | 같은 집합 마스킹 top-50 | <50ms |
| (3)-e | RRF 결합 | 코드 | → top-5 | ~0 |
| (3)-f | **참조 폐포 수집** | 코드(재귀 CTE) | `refs` **깊이 1** · 조 지정된 참조만 | <30ms |
| (4) | 판정 조립 | **LLM** | B0~B6 (`LLM.md` §3-7) → 구조화 JSON | 스트리밍 |
| (5) | 전제 해소 | 코드 | 3갈래 (§4) | ~0 |
| (6) | 검증·강등 | 코드 | S번호 치환 + 강등 규칙 (§5) | ~0 |
| (7) | 로깅 | 코드 | `decisions` insert (전제·검색스냅샷·코퍼스버전) | ~0 |

- (3)-a 와 (3)-b~e 는 독립이라 **병렬** — L3 를 먼저 보는 것이 지연을 늘리지 않는다
- LLM 호출은 (1)·(4) 의 **2회 고정** ((4)-a 채택 시 3회 — A/B 미결)
- **왜 L3 부터인가** — 주관기관 규정 스스로가 "이 지침에서 정하지 아니한 사항은 상위에
  따른다"는 구조다. 사용자 문서가 "나부터 보고, 없으면 상위로" 를 지시한다

총 ~3.2s (p50). (1)·(4) 스트리밍이라 첫 글자는 1초 안.

## 2. 세 축의 접점 — 누가 무엇을 넘기나

| 경계 | 넘어가는 것 | 넘어가지 **않는** 것 |
|---|---|---|
| (1) → (3) | JSON 필드 (코드가 쿼리 조립) | LLM 이 만든 검색어 |
| (3) → (4) | 조문 **원문** + S번호 | 요약본 |
| (2) → (4) | **비교가 끝난** 룰 결과 | 원시 한도값 (LLM 이 계산하게 두지 않는다) |
| (4) → (6) | S번호 인용 | 원문 문자열 |
| (6) → 화면 | DB 원문으로 치환한 인용 | LLM 이 쓴 인용문 |

**LLM 에 도달하기 전에 숫자는 이미 결론이 나 있다.** 블록 순서·S번호 사양은 `LLM.md` §3-7.

## 3. 게이트와 분기

```
A. 금지목록 적중        (2) 에서 즉답 "불가"           LLM 0회
B. 검색 스코어 미달     게이트에서 판단불가            LLM 1회 ((1)만)
C. 비목 신뢰도 갈림     두 경로를 모두 판정해 보여줌   LLM 2배
D. 정상 경로            (4) 까지                       LLM 2~3회
```

**C — 비목이 갈릴 때**: "비목을 고르세요" 는 문제를 사용자에게 되돌려주는 것이다.
각 선택의 판정 결과를 나란히 보여주고 고르게 한다. 컨펌 로그는 `item_alias` 를 채운다.

### 3-2. L3 게이팅 — "상위를 봐야 하는가" 를 코드가 가른다

LLM 에게 묻지 않는다 — 룰 조회 결과의 모양만 보면 안다.

```python
l3 = SELECT * FROM rules WHERE layer='L3' AND 기관ID=:기관 AND 비목=:비목

match l3:
    case None:                            # (1) L3 에 규정 없음 = "정하지 아니한 사항"
        need_upper = True
    case r if r.참조만:                    # (2) "지침 제N조에 따른다" 만
        need_upper = True; seed_refs = r.근거
    case r if r.허용 in ('불가','조건부'):   # (3) L3 가 막거나 조건을 건다
        need_upper = False                 # 상위가 관대해도 결론 불변. 근거 보강용으로만 상위 동반
    case r if r.허용 == '가능':             # (4) L3 가 "가능" 이라고만 함
        need_upper = True                  # 🔴 강제
```

**(4) 가 강제인 이유** — 주관기관 규정은 국가 지침이 있다고 전제하고 그 위에 얹는 문서라
구조적으로 자족하지 않는다. L3 의 "총장 승인만 받으면 됩니다"만 보고 답하면, 상위의
"범용성 기자재 소명 필요"를 놓친 **틀린 "가능"** 이 된다. "불가"는 단독으로 안전하지만
**"가능"은 절대 L3 단독으로 확정하지 않는다** — 오답 비대칭을 실행 순서에 옮긴 것.

예외: (3) 으로 갈렸으면 검색 스코어 미달이어도 판단불가로 보내지 않는다 —
L3 에 인용할 명시 근거가 이미 있다.

### 3-3. 효력 결정 — precedence 가 병합을 정한다

**L3 가 항상 이기는 게 아니다.** 알고리즘 전문은 `rule_base.md` §3. 요약:

| `precedence_rules` | 병합 |
|---|---|
| `범위='all'`, 우선=L2 (8개 사업 중 6개) | **L2 가 이긴다.** L3 는 "참고" 로 화면 7 병기 |
| `범위='unspecified_only'` ("정하지 아니한 사항만 상위로") | L3 에 있으면 L3, 없으면 상위 |
| 조항 없음 | 폴백 — 상위 우선 + 같은 층에서 갈리면 엄격한 쪽 |

## 4. (5) 전제 해소 — 3갈래

```
a. F 있음 + 값 있음  →  즉시 검증 → 전제 소멸
b. F 있음 + 값 없음  →  인라인 입력 요청 (화면 안에서 30초, 대부분 LLM 재호출 없이 재계산)
c. F 없음            →  unmapped_premise 로깅 → 화면 9 문의
```

c 가 F축 설계의 피드백 루프다 — 빈도 높은 unmapped 는 다음 F 필드 후보.
채울 수 없는 전제는 판단불가로 정직하게 닫는다.

## 5. (6) 검증과 강등 — 마지막 방어선

신뢰등급은 여기서 **코드가 산정**한다 (인용 청크의 `extraction` 등 실제 속성 —
LLM 자칭 금지, `LLM.md` §3-4 [2겹]).

| 조건 | 조치 |
|---|---|
| 인용 S번호 → DB 원문 치환 | 항상. 매핑표는 `decisions.검색스냅샷` 에 저장 |
| 매핑표에 없는 S번호 | 인용 폐기 (환각 차단) |
| 인용 0개 | → **판단불가** |
| "가능" 인데 인용이 B등급(vlm 등)뿐 | → **조건부 강등** |
| (4)-a "불확실" ≥ 임계 (채택 시) | → **판단불가** |
| 근거조항 없는 전제 | → 폐기 |
| `verified=false` 룰 단독 "가능" | → 조문 인용 강제 |
| 인용의 `기관ID` 가 NULL 도 현재기관도 아님 | → **즉시 폐기 + 사고 로그** (멀티테넌시 3차 방어) |
| "가능" 인데 인용이 L3 단독 | → **조건부 강등** (§3-2 (4) 의 실행판) |
| 폐포에 dangling 엣지 있었음 | → 경고 + 신뢰등급 하향 |
| 인용 문서 `domain != '창업지원사업'` | → 경고 문구 삽입 (`documents` join) |
| `extraction='vlm'` 청크 인용 | → A등급 금지 + "판독본 기반" 경고 |
| 버전 스탬프 부착 | 항상 |

precedence 는 여기서도 적용된다 — L3 근거의 "불가"가 L2 우선 규칙에 걸리면 뒤집힌다.

## 6. 재현성

| 무엇 | 어떻게 |
|---|---|
| LLM 호출 수 | 슬롯별 고정 |
| 검색 결과 | `decisions.검색스냅샷` (S번호 매핑 + top-k). 모델 비교 시 스냅샷 고정 |
| 코퍼스 버전 | `decisions.코퍼스버전` · `documents.version` |
| BM25 | DB 내 테이블 — 재인덱싱이 트랜잭션 하나 |

## 7. 판단불가 경로 — 실패가 아니라 제품 기능

```
case_chunks 검색 (질문 ↔ 사례 질문 임베딩)
  → 상위 3건 표시 + "귀하의 사업에 적용되지 않습니다" 라벨
  → 담당자 문의 메일 초안 자동 생성 (화면 9, LLM (5))
```

**사례는 판정에 영향을 주지 않는다** — 판정이 이미 `판단불가` 로 확정된 뒤에만
`case_chunks` 를 조회한다. 함수 레벨 강제 (`RAG.md` §2-2).

## 8. 실패 경로 전수

```
(1) 정규화 실패        → 재질문 (화면 3)
(2) 룰 없음            → 정상. (3)(4) 로 진행
(3) 검색 0건/저스코어   → 판단불가 + 문의 유도
(4) 스키마 위반        → 1회 재시도 → 실패 시 판단불가   (guided_json 으로 구조적 방지)
(4) 타임아웃           → 판단불가
(5) unmapped 다수      → 판정은 내되 "미확인 전제 N개" 표시
(6) 인용 검증 실패     → 판단불가
DB 연결 실패           → 503. 판정을 추측으로 만들지 않는다
LLM 서빙 실패          → 어댑터 폴백(F축 슬롯 제외) → 전부 실패 시 503
```

**모든 실패의 기본값은 판단불가다. "아마 가능"은 어떤 경우에도 만들지 않는다.**

## 9. 비동기 에이전트 — 판정 경로 밖

판정 1건은 결정적 파이프라인이다. 에이전트는 지연 예산 없는 비동기 작업에만 둔다.

| Agent | 트리거 | 산출 | 우선순위 |
|---|---|---|---|
| **A4 개정 대응** | 배치(주 1회) / 수동 | 변경 조항 + 영향 룰 + 재검수 큐 | 1 |
| **A2 엄격조항 검토** | L3 업로드 시 | 국가 규정보다 엄격한 조항 후보 | 2 (시연 방법 미결) |
| A5 답변 축적 | 기관 공식 답변 수신 | 룰 후보 → 검수 큐 | 3 |
| 정산 리허설 | 사용자 요청 | 집행내역 전건 판정 + 리스크 요약 | 4 |

**A4 절차**: 조 매칭(제목 주·번호 보조) → 본문 diff → 변경 조를 근거로 삼는 `rules` 조회
(`근거` JSONB) → 해당 룰 `verified=false` 전환 → 변경 요약(LLM). 비목 어휘집 재검수도 트리거.

**비용 방어 (공개 URL + GPU 서빙)**: 일일 총 호출 하드 캡 · IP 시간당 제한 · 동일 질문 캐시 ·
심사 기간만 개방 · 키는 서버 환경변수(백엔드 경유 필수) · 콜드 스타트 대비 발표 30분 전 워밍업.
