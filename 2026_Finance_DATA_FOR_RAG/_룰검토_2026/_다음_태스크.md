# 다음 태스크 — 골든셋·룰 팀이 남긴 것

2026-09-04 작업에서 손 안 댄 것들이다. `docs/9_미결.md` 에 옮길 내용이고,
그 파일은 ai-c4 소유라 여기에 먼저 적어 둔다.

각 항목은 무엇을 · 왜 · 어디까지 됐나 순이다.

## 1. ✅ 참고3 재파싱 — 완료 (2026-09-05)

**무엇**: `창업중심대학 세부관리기준2025년 개정` 의 참고3 을 다시 파싱해 `corpus.chunks` 에 재적재한다.

**왜**: 본문에 표의 세로 라벨 글자가 한 자씩 섞여 있다. "기프티콘"이 "사기프티콘"으로,
"현물로"가 "현사물로"로 저장돼 있다. 그래서 AI 가 근거에 있는 예외 조항을 못 읽는다.
🔴 **룰을 고쳤는데도 판정이 여전히 틀린다.** rule 458 을 고친 뒤 실서버로 태웠는데 여전히 불가가 나왔다.
문서는 `extraction='native'`·`parse_quality='high'` 라 품질 표시로는 안 걸러진다.

**어디까지**: `1451e46`(무조건 교체) 을 "오염된 조만" 으로 좁혀 PR #29 로 커밋(main `6be6279`).
참고2 우려도 이걸로 같이 풀렸다 — 오염 스캔(`\n[가-힣]\n`)이 조 단위로 자동 걸러서, 사람이
"이 라벨은 안전한가" 를 미리 판단할 필요가 없어졌다(오염 없으면 그 조는 손 안 댐).
재적재는 전체(`load_db.py`/`stage2_chunk.py` TRUNCATE)가 아니라 바뀐 8개 조만 스코프를 좁혀
로컬·운영(Cloud SQL) 양쪽에 반영했다. 상세: `실제 구현.md` "2026-09-05 · 참고3 재파싱".

**검증점**: `창업중심대학.json` 의 CCU-16·23·25 — `라벨제거후 3` → **0** 확인(로컬).
운영은 `eval.golden_set` 이 없어 같은 스크립트로 못 돌린다 — 오염 문자열 직접 대조로 확인함.

```bash
python "2026_Finance_DATA_FOR_RAG/_골든셋_2026/_tools/중앙_검증_ai-c5.py" \
       "2026_Finance_DATA_FOR_RAG/_골든셋_2026/창업중심대학.json"
```

## 2. eval run

**무엇**: 확장된 골든셋으로 채점 run 을 돌린다.

**왜**: 202문항을 새로 넣었는데 점수를 안 재봤다.

**어디까지**: 적재만 끝났다. GPU 가 필요하고 ai-c4 소관이다.
🔴 **분모가 93 → 295 로 바뀐다** (2026-09-05, `eval_store.평가대상(cur)` 직접 호출 실측 — 315 는
`golden_set` 전체 행수고 범위밖 20건이 빠져 295 다). 기존 점수와 비교 불가다.
`eval.runs.설정` 에 골든셋 판(113 원본 + 202 신규 · 표식 `검수메모 like '골든셋2026%'`)을 박아야 한다.
🔴 참고3 재적재가 선행이다. 오염된 근거로 채점하면 그 점수를 못 믿는다.

## 3. 룰 스키마 — 칸 넷이 없다

**무엇**: `corpus.rules` 에 칸 넷을 더한다. `docs/5_룰/5-1` §5 미결 #1 에 붙는다.

**왜**: 오늘 "제안은 있는데 담을 칸이 없다"로 막힌 게 전부 여기다.

| 칸 | 없으면 |
|---|---|
| 한도 / 심의 임계 구분 | "1인 5만원까지"(넘으면 불가)와 "500만원 넘으면 심의"(심의받으면 가능)를 못 가른다 |
| 부분 제외 표시 | "이 지출의 일부만 막는다"를 못 적어서 금지예시를 짧게 못 줄인다 |
| 위반 확정 여부 | "이미 발주했다"와 "지금 발주하면"을 못 가른다 |
| 사전승인을 품목 단위로 | 지금은 비목 전체에 걸려서 rule 453·462 를 못 고친다 |

**어디까지**: 제안만 있다. `_룰수정_최종.md` §3 에 보류 9건과 각각의 이유가 있다.

## 4. 룰 수정 보류분 9건

**무엇**: 오늘 안 고친 룰들이다.

**왜**: 닻이 갈렸거나(439) 담을 칸이 없거나(453·462·477) 범위를 안 재봤다(445).

**어디까지**: `_룰수정_최종.md` §3 에 대상·이유가 표로 있다.
🔴 **"검토했는데 안 고쳤다"와 "고치고 싶은데 못 고친다"가 섞여 있으니 그 표를 보고 골라라.**

## 5. 문체 정리 잔여

**무엇**: `docs/부록` 5벌 + `docs/` 본문 하 18벌.

**왜**: 시간이 없었다. 반쯤 고쳐 놓은 것은 없다. 전부 시작 전이다.

**어디까지**: `_문체정리_docs_목록.md` 에 파일별 측정값과 작업량(상/중/하)이 있다.
규칙은 `_문체_규칙.md`, 검사기는 `_tools/문체검사.py` 다.

🔴 **인용 손상을 조심해라.** 오늘 14건이 깎였다가 복원됐다.
인용은 손으로 옮겨 적지 말고 코퍼스에서 잘라 온다. 규칙 1-6·1-7 을 먼저 읽어라.

## 6. 검증 세션 미완분

**무엇**: TIPS 21문항 독립 재검증(ai-2e). 그다음 초격차 30% 표본.

**왜**: TIPS 는 교차검토가 제일 약했다. 검토자가 지침 16. 을 안 읽고 재도출했다고 스스로 밝혔다.

**어디까지**: 배정만 했다. 산출 예정 경로는 `_골든셋_검증_2026/TIPS.md` 다.

## 7. 표기 통일 — 낫표

**무엇**: 저장소 안에서 낫표(`「」`) 표기가 갈려 있다. 한 판으로 정리한다.

**왜**: 오늘 한 세션은 56곳을 되돌렸고 다른 세션은 5곳을 그대로 뒀다.
둘 다 근거가 있었다. 안쪽 문자열이면 두고, 바깥 따옴표면 바꾸는 게 기준이다.

**어디까지**: 안 했다. 🔴 **오늘 인용을 건드려 14건이 깨졌기 때문에, 표기를 맞추려고
인용 근처를 또 만지는 걸 미뤘다.** 문체 작업이 아닌 별도 판에서 하는 게 안전하다.

## 8. 작은 것 둘

- `audit_db.py:186` — 골든셋 미검수 기대값이 0인데 지금 202가 나온다.
  `verified` 를 false 로 둔 게 사실이라서 그렇다. 기대값을 조정할지 정해야 한다
- `docs/8_운영` 에 「8-5」가 둘이다(무인운전·배포레시피). 번호 충돌이라 참조가 애매하다
- `docs/0-2` 가 110줄로 100줄 상한을 넘는다. 쪼갤지 면제할지 미정
