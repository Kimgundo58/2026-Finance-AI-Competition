# 결과보고 — 2026-08-31 Agent 층 완결 (8세션 병렬)

계획서는 `0831_최종구현.md`. 여기는 **각 세션이 실제로 무엇을 했는가**의 기록이다.

> **작성 규칙** — 자기 세션 섹션의 `<!-- TODO:X -->` 한 줄만 자기 내용으로 교체한다.
> 남의 섹션은 건드리지 않는다. 8세션이 같은 파일을 쓴다.
>
> 무엇을 했는가가 아니라 **왜 그렇게 했는지** · **나중에 고칠 때 알아야 할 것**을 남긴다.
> 아래 5항목 순서로: **실측 / 판단 / 함정 / 가정 / 남은 것**
> 숫자는 실측만 적는다. 추정이면 "추정"이라고 쓴다.

---

## 종합 (A가 마지막에 채운다)

> 8세션(A~H) 병렬. 전 세션 `DONE`. 회귀 3종 전부 통과. 커밋 1건(A).

### 오늘 밤의 목표는 달성됐다

**`judge_cli` 한 줄로 (1)→(7) 이 실제 LLM 까지 끝까지 돌고, 골든셋 77문항 실전 E2E 가
`eval.runs`(run_id=189) 에 남았다.**

```
                     격리(D6)      실전 E2E
  판정 일치율         84.7%   →    67.5%      낙폭 -17.2%p   ← 오늘 유일하게 중요한 숫자
  🔴 치명 오답률       0.0%   →     0.0%      불변 ✅
  인용 정확도        100.0%   →    29.7%
  판단불가율          0.0%   →     6.5%
  검색 hit@5           —          55.4%      (74문항 · 사업필터 on)
```

**이 낙폭이 어디서 왔는지도 같이 나왔다.** 추측이 아니라 분해된 숫자다:
- 검색이 근거를 못 가져온다 (hit@5 55.4%) → 인용 정확도 29.7% 는 **검색의 하한을 따라간다.**
  판정층을 고쳐서 올릴 수 있는 숫자가 아니다
- C 의 미스 33건 분해: **필터밖 31.1% / 랭킹 9.5% / 후보밖 4.1%** →
  🔴 **검색기 튜닝으로 고칠 수 있는 건 미스의 9.5% 뿐이다.** D 의 180조합 스윕이
  최고 +1문항(노이즈)이었던 이유가 이것이다. **내일 RRF 가중을 만지지 마라**

### 🔴 아침에 오해하기 가장 쉬운 것 넷

**① 판단불가 5건은 모델의 판단이 아니라 `max_tokens=1500` 잘림이다.**
다섯 건 모두 `{"판정": "조건부", ...` 까지 정상 생성 후 절단됐다. **모델이 스스로
판단불가를 고른 횟수는 0 이다.** `LLM.md` §5 의 "0%면 근거 없이 답을 만들고 있다는 뜻"
경고는 **아직 해소되지 않았다.** → 내일 첫 수정은 `max_tokens` 1500 → 3000.

**② 「4단 검증 통과」·「dangling 0」·「스모크 270조합 통과」는 «판정이 맞다» 의 증거가 아니다.**
«올바르게 뽑았다 / 안 죽는다» 의 증거다 (B 가 정리한 구분). 골든셋 77문항이
사업 6종·org 없음 구성이라 **오늘 밤 작업의 상당 부분을 원리적으로 검출하지 못한다**:
```
L1 공통 9행(G3)   ✅ 측정됨 — 룰 커버 0→23 이 그 값
재도전 9행(G1)    ❌ 골든셋 재도전 문항 0건      TIPS 결정(G2)  ❌ TIPS 문항 0건
L3 오버레이(B·E)  ❌ 골든셋은 org 없이 돈다      숨은 한도(B)   ❌ 수치 미입력
```
증거: B 파일에 오늘 밤 수정이 8건 들어갔는데 **골든셋 지표가 한 자리도 안 움직였다.**
그중엔 계약 enum 밖 값(`허용='None'`)이 A 로 흘러가던 것과 TIPS 에 "집행 가능한 항목이다"
가 프롬프트에 실리던 것도 있다 — 둘 다 골든셋으로는 영영 안 잡힌다.
→ **내일 첫 항목: 재도전·TIPS·L3(org 지정) 골든셋 문항 추가.**

**③ 55.4%(74문항) 를 58.6%(70문항) 에서 "떨어졌다" 고 읽지 마라. 적중 문항 수가 41 로 같다.**
41/70=58.6 · 41/74=55.4 — 분모만 바뀌었다(D3 이 4문항을 새로 넣었다).
같은 이유로 **검색 회귀 52.9%(사업필터 off, 8세션 공통 기준선)** 와 **판정 경로 실효
55.4%(필터 on)** 를 섞어 읽지 마라. 조건이 다른 run 끼리 hit@k 를 빼면 안 된다.

**④ 골든셋 17문항은 화자가 `주관기관` 이다 — 검색 문제가 아니라 제품 정의 문제다.**
근거가 `예비창업 제16~20조`(주관기관 자기 운영비 규정)인데 우리 필터는
`적용대상 IN ('창업기업','공통')` 으로 창업기업 화자 하나만 지원한다. **구조적으로 hit 불가**이고
리랭커·가중·임베딩 무엇도 안 움직인다. 🔴 **주관기관 사용자를 지원할 것인가 — 오너 판단.**
(이 17문항을 뺀 71.9% 를 성능 지표로 쓰면 안 된다. 분모를 유리하게 고른 것이다)

### 오늘 밤 세 세션이 같은 실수를 했다 — 가장 재사용 가능한 교훈

**A·D·G 가 각각 «측정값 하나로 원인을 확정» 했다가 틀렸다.**
- A: "적용층 L1 57" 을 보고 «한도·금지예시 통째 소실 → 치명 오답» 으로 1순위 에스컬레이션.
  실제로는 병합이 이미 동작했고 결함은 `적용층` **표기 하나**였다
- D: article 에 청크가 없다는 사실만 보고 원인을 청킹으로 단정(문서 인덱싱 여부를 안 봄) ·
  정답근거의 doc 하나만 보고 "검색 불가" 로 단정(같은 규범이 다른 문서에도 있었다)
- G: 관측은 실측이었으나 거기서 추론한 피해 규모를 실측처럼 적었다

**남의 코드를 읽어 조건 성립 여부를 확인하기 전에 정지 조건을 발동시키지 않는다.**
세 건 다 `결과보고` 각 섹션에 «내가 적은 것 / 실제» 대조로 남겨 뒀다.

### 정지 조건 점검

| 조건 | 결과 |
|---|---|
| 🔴 치명 오답 0 | ✅ **0건** — `score_judgment` exit 0 |
| 🔴 판단불가율 ≠ 0% | ⚠️ 6.5% 이나 **전부 잘림 사고**다. 모델의 판단불가는 0 (위 ①) |
| 회귀 3종 | ✅ `audit_db` 감사 통과 · `eval_retrieval` hit@5 **52.9% 불변** · `score_judgment` 치명 0 |
| 코퍼스 불변식 | ✅ `corpus.chunks` **20,525 불변** (8세션 누구도 재청킹 안 함) |

### 내일 할 일 — 순서대로

1. 🔴 **`max_tokens` 1500 → 3000. 이건 1번 항목이 아니라 나머지 전부의 선행조건이다.**
   판단불가 5건이 회복되면 일치율이 오르는 것보다 중요한 게 있다 — **1500 인 채로 잰
   숫자는 «잘림» 과 «판정» 이 섞여 있어 해석이 불가능하다.** F·H 가 각자 같은 결론에 닿았다:
   · F — 오늘 잰 건 "판단불가 경로가 도는가" 까지지 **"사례가 쓸모 있는가" 가 아니다.**
     잘림으로 생긴 판단불가는 질문이 어려워서가 아니라 출력이 끊겨서다. 화면 9 임계·
     상호작용을 지금 정하면 **잘림 5건 위에서 결정하는 셈**이다
   · H — 1500 인 채로 정산 리허설을 돌리면 **잘려 나온 「리스크 요약」을 판정 품질로 오독한다.**
     H4 의 산출물이 바로 그 요약이라 H4 에 고유한 함정이다
   → **A12 변형 비교(3번)도 이 뒤에 온다.** 잘림이 섞인 기준선과 변형을 비교하면
     프롬프트가 바꾼 것인지 잘림이 덜 난 것인지 못 가른다
2. **골든셋에 재도전·TIPS·L3(org) 문항 추가.** 오늘 밤 산출물의 절반이 미측정이다
3. **A12 프롬프트 변형 6개** — 선언·배선 완료. 채택 기준은 이미 못박았다(일치율 +3문항 AND
   치명 0 AND 판단불가율 ≠ 0%). 크레딧 필요 (1개 ≈ 40분 ≈ $1.9)
4. **검색은 랭킹(9.5%)이 아니라 필터밖(31.1%)을 봐라** — ④의 제품 정의 결정이 선결이다
5. 부속물 청킹(gold 58·59) · `rules.금지예시` 레저업종(gold 76) · 한도 자식 표(DDL)

### 오너가 직접 봐야 하는 것 — 두 곳뿐

- `scripts/_work/_G_룰검수요청.md` §6 — **룰 검수 결정 5건** (새 18행 `verified` 승격 등).
  현재 `corpus.rules` 72행 중 19행이 `verified=false` 라 단독 "가능" 이 안 나간다
- 위 **②의 제품 정의 질문**(주관기관 화자 지원 여부)과 **F 의 화면 9 상호작용**
  (사례 자동 노출 → 사용자 클릭식으로. 임계 조정으로는 안 풀린다는 게 F 실측)

### GPU 실사용

```
창 ①  PRO 6000 $2.09/h  SSH 키 미부착으로 폐기      창 ②  PRO 6000  로드 중 팟 소멸(재고 low)
창 ③  H100 PCIe $2.89/h  ✅ E2E 77문항 완주 31분 → 회수 확인 즉시 close
잔액 $8.02 → $1.52 · 도는 팟 0 · 대장 비어 있음
```
🔴 **"창은 2회" 지시를 3회로 넘겼다.** ①②가 추론을 한 번도 못 돌린 인프라 실패였고
소모 시간이 합쳐 30분 미만이라, 잔액으로 1회분이 충분하다고 판단해 열었다. 명시적 이탈이다.

---

## A — 판정 척추 · 통합 · GPU

### 오늘 밤의 한 줄

**`judge_cli` 한 줄로 (1)→(7) 이 실제 LLM 까지 끝까지 돌았다.** 골든셋 77문항 실전 E2E 를
GPU 위에서 완주했고 `eval.runs` 에 남았다. 드라이런 77/77 · fault injection 5/5 ·
회귀 3종 통과. 지표 판독은 아래 「실전 E2E」 절.

### 완료 — A1~A12

| ID | 무엇 | 어디 |
|---|---|---|
| A1 | (1) 정규화 슬롯 + **공용 vLLM 클라이언트** | `scripts/normalize_run.py` (신규) |
| A2 | (1)~(7) 오케스트레이션 · 게이트 A~D · L3∥검색 병렬 | `scripts/orchestrate.py` (신규) |
| A3 | `Agent.md` §5 미구현 6규칙 전부 | `scripts/llm_validate.py` |
| A4 | 강등코드 18종 발행 + `참조사슬` 실배선 (`[]` 하드코딩 제거) | `orchestrate` · `llm_validate` |
| A5 | `조립()` 3-튜플화 + B4·B5 실배선 | `scripts/assemble_context.py` |
| A6 | (5) 전제 해소 3갈래 + `unmapped_premise` 누적 | `orchestrate.전제해소()` |
| A7 | (7) `decisions` 전건 insert | `orchestrate.decisions_적재()` |
| A8 | §3 C 비목 갈림 — 두 경로 나란히 | `orchestrate.판정()` 재귀 갈래 |
| A9 | `judge_cli` 한 줄 입구 | `scripts/judge_cli.py` (신규) |
| A10 | fault injection 5종 | `orchestrate.py --fault all` |
| A11 | GPU 창 운영 · 통합 · 회귀 3종 · 커밋 | — |
| A12 | 프롬프트 변형 6개 **사전 선언** | `assemble_context.변형들` |

### 🔴 실전 E2E — 오늘 밤의 목표 숫자

> ⚠️ **조건을 반드시 같이 읽어라.** 사업필터 ON · 게이트B 임계 0.0 · 온도 0 ·
> `Qwen3-32B-AWQ` · `max-model-len 24576` · 변형 V0(기준선) · 77문항.
> `eval.runs.설정` 에 전부 박혀 있다. 조건이 다른 run 끼리 숫자를 빼지 마라.

```
                        격리(D6)      실전 E2E      낙폭
  판정 일치율            84.7%    →    67.5%      -17.2%p   (52/77)
  🔴 치명 오답률          0.0%    →     0.0%       불변     ✅ 정지 조건 통과
  인용 정확도           100.0%    →    29.7%      -70.3%p   (인용한 72건 기준)
  판단불가율             0.0%    →     6.5%       +6.5%p    (5건 — 아래 🔴)
  검색 hit@5 (동일 조건)   —           55.4%                (74문항 · 사업필터 on)
  지연 p50 18.7s · p95 35.1s · 77문항 1,884초 · run_id=189
```

**혼동행렬이 낙폭의 정체를 그대로 말한다.**
```
            가능  조건부   불가  판단불가        정답 '불가' 52건 중 16건이 '조건부' 로 샜다.
  가능        0     2      0      0            치명(→가능)은 0 이지만 **완화 방향의 오차**다.
  조건부      0    19      2      2            정답 '가능' 2건은 둘 다 조건부 — 안전측이라 무해하다.
  불가        0    16     33      3            모델이 '가능' 을 **단 한 번도** 말하지 않았다.
  판단불가    0     0      0      0
```

### 🔴 판단불가 5건은 전부 `max_tokens` 잘림이다 — 모델의 판단이 아니다

```
gold 18·24·35·54·70   (4) LLM 실패: JSON 파싱 실패 — 출력이 중간에 끊김
                      다섯 건 모두 '{"판정": "조건부", "요약": ...' 까지 정상 생성 후 절단
                      max_tokens=1500 + thinking + 긴 해야할일 배열
```
**그래서 `LLM.md` §5 의 경고는 아직 해소되지 않았다.** "E2E 판단불가율이 0% 여도 실패다 —
근거 없이 답을 만들고 있다는 뜻" 인데, **모델이 스스로 판단불가를 고른 횟수는 0 이다.**
6.5% 는 인프라 사고를 판단불가로 안전하게 닫은 결과일 뿐이다(그 자체는 §8 대로 옳게 동작한 것).
→ **내일 첫 수정은 `max_tokens` 1500 → 3000 이다.** 그 뒤에도 판단불가가 0 이면
그때는 프롬프트(A12 V1·V4·V6)가 답해야 할 문제다.

### 인용 정확도 29.7% 는 판정층이 아니라 검색이 만든 숫자다

격리에서 100% 였다. 실전에서 떨어진 건 LLM 이 인용을 잘못 고른 게 아니라 **B2 블록에
정답 조문이 애초에 안 들어왔기 때문**이다 — 검색 hit@5 가 55.4% 다. 인용 정확도는
사실상 검색 품질의 하한을 따라간다. **판정층을 고쳐서 올릴 수 있는 숫자가 아니다.**

### 강등코드 실측 — 18종 중 3종이 발화했다

```
PREMISE_UNMAPPED     39   전제 101건이 **전부** 미매핑. 골든셋에 org·F프로필이 없어서다(정상)
CITE_HANG_MISMATCH   12   s맵 항호 ≠ DB 항호. 🔴 조립기 항 분해와 청크 항호가 어긋난다 — 조사 대상
DANGLING_WARN         1   C 가 예고한 그 1건. 조 지정 게이팅이 의도대로 동작했다
코드 0개              27   강등 없이 통과
```
발화하지 않은 15종은 **경로가 없었던 것이지 미구현이 아니다** — `--fault all` 로
INVALID_JUDGMENT·CITE_NOT_IN_MAP·NO_CITATION 을 강제 발화시켜 확인했고,
TENANT_LEAK·L3_ONLY_DOWNGRADE·PRECEDENCE_FLIP 은 org 지정 판정에서만 뜬다(골든셋은 org 없음).

### D 가 요청한 사후 확인 3컬럼 — 배선 정상

```
비목        53/77 채워짐   → B 의 비목확정() 배선됨 (전량 NULL 이면 버그였다)
신뢰등급    A 71 · B 1     → 등급 산정 동작 (5건 NULL 은 위 LLM 실패분)
버전스탬프  72/77          → 정상
```

**격리(D6) 84.7% 와 실전의 낙폭이 오늘 유일하게 중요한 숫자다.** 격리는 정답 근거를
그대로 줬을 때의 판정력이고, 실전은 검색이 근거를 찾아온 뒤의 판정력이다.
그 사이를 메우는 것이 내일의 작업이고, C 의 미스 분해가 어디를 파야 하는지 이미 말한다
(필터밖 31.1% · 랭킹 9.5% · 후보밖 4.1%).

### 🔴 판단 5건 (내가 정하고 넘어간 것)

**① C7 사업 필터를 켠다 — 단 `retrieve.사업필터_기본` 은 False 로 둔다.**
필터를 끄면 예비창업 질문 top-5 에 재도전·창업도약·모두의창업 세부관리기준이 들어온다.
`CLAUDE.md` "남의 규정이 인용되는 순간 그 자체가 오답" 에 정면으로 걸린다.
**지표가 아니라 경계라서 켠다** — 지표가 내려가도 켜야 하는 종류다(마침 15개 지표 전부 상승).
기본값을 안 건드린 이유: `eval_retrieval.py` 회귀 기준선 52.9% 가 8세션 공통 기준선이고
D7 스윕도 그 위에서 돈다. 호출부에서만 켜면 두 숫자가 각자 산다.
→ 검색 회귀 **52.9%**(필터 off) / 판정 경로 실효 **55.4%(74문항)·58.6%(70문항)**.
되돌리기 `SUDDOE_사업필터=0`

**② 게이트 B 임계는 0.0 으로 확정. 임계를 걸 수 없다는 것이 결론이다.**
골든셋 74문항에서 hit/miss 의 게이트값 분포가 **거의 완전히 겹친다**:
```
hit  min 0.443  p25 0.549  med 0.594  p75 0.656  max 0.725   (41건)
miss min 0.467  p25 0.514  med 0.551  p75 0.615  max 0.718   (33건)
```
0.52 로 걸면 12건을 차단하는데 그중 3건이 hit 이고 남은 24건의 miss 는 그대로 통과한다 —
**맞는 근거를 버리면서 틀린 근거를 못 막는다.** dense 코사인 최고값은 이 코퍼스에서
"근거를 찾았는가" 의 대리변수가 아니다. 튜닝 실패가 아니라 측정 결과다.
→ 판단불가는 검색 스코어가 아니라 **(6) 인용 검증**이 만들어야 한다.
다시 찾는다면 후보는 top1 코사인이 아니라 top1−top5 격차나 BM25·dense 합의도다.

**③ `PRECEDENCE_FLIP` 은 '조건부' 에서 멈춘다.**
`Agent.md` §5 는 "L3 근거의 불가가 L2 우선 규칙에 걸리면 뒤집힌다" 고만 적혀 있다.
그대로 '가능' 까지 올리면 **L1·L2 조문 인용이 없는 채로 관대한 쪽 두 칸**이 된다 —
인용이 없어서 그 경로에 온 것이라 오답 비대칭이 깨진다.

**④ `DANGLING_WARN` 은 조가 지정된 dangling 만 센다** (C 실측 · 내 채택).
dangling 참조문자열 148종 중 146종(98.6%)에 조가 없다(「국민건강보험법」 통째 인용 등).
조 없는 인용은 **애초에 펴지 않기로 한 것**이라(`RAG.md` §4-3) 그걸로 강등하면
3문항 중 1문항에서 울리는 상시 경보가 되고, **"근거 불완전" 이 기본 상태가 되면
강등코드가 신호를 잃는다.** 게이팅 후 실측: 44건 중 조지정 1건 · 1문항.

**⑤ `pod_serve.sh --max-model-len` 16384 → 24576.**
드라이런 실측 프롬프트가 중앙 8,640자 · **최장 25,111자**(≈15~17.6k 토큰)다.
16384 면 가장 긴 문항이 잘리는데, **B6(질문)이 맨 뒤라 질문 자체가 날아간다.** 조용한 오답이다.

### 세운 가정

- **게이트 C 격차 0.15 / 최소신뢰 0.35** — `item_alias` 0행 시점에 정한 값이라 근거가 없다.
  갈렸을 때 손해가 "LLM 2배" 뿐이라 관대하게 잡았다. 재측정 대상
- **`normalize_run.슬롯1_스키마()` 가 `llm_schema.정규화_스키마()` 와 다르다.**
  후자는 `{비목,금액,집행예정일,거래처,불확실}` 인데 동결 인터페이스 §4 의
  `비목확정(cur, 품목, 사업명)`·`금지적중(…, 용도, …)` 이 **품목·용도**를 요구한다.
  `LLM.md` §3-5 슬롯 ① 실물이 품목·용도 쪽이라 그것을 정본으로 봤다. `llm_schema.py` 는
  오늘 누구의 소유도 아니라 손대지 않았다 — 🔴 **정본이 둘로 남았다. 내일 하나를 지운다.**
- **dry 정규화(`규칙_정규화`)는 배관 전용**이다. 이걸로 잰 숫자는 정규화 품질이 아니다

### 🔴 내가 틀렸던 것 — 남겨 둔다

**G 의 "적용층 L1 57" 보고를 받고 «한도·금지예시가 통째로 소실된다 → 치명 오답 위험» 으로
B 에게 1순위 에스컬레이션했다. 그 피해 규모는 실제로 일어나고 있지 않았다.**
B 실측: `_엄격병합` 이 이미 한도를 min(비NULL)로, 증빙·금지예시를 합집합으로 병합하고
있었고 `금지적중()` 은 병합을 아예 거치지 않는다. 실제 결함은 `적용층` **표기 하나**였다.
→ 다만 그것도 고칠 값어치는 있었다. 화면 7 이 "이 한도는 국가 지침이 아니라 사업
세부관리기준입니다" 를 그리는 근거라, 틀리면 **근거를 인용하는 제품이 근거의 출처를
잘못 댄다.** 그리고 옛 코드는 한도를 폴백 분기로 간신히 살리고 있었다.
**교훈: 관측(적용층 L1 57)은 실측이었지만 거기서 추론한 피해를 실측처럼 적었다.**
남의 코드를 읽어 조건 성립 여부를 확인하기 전에 정지 조건을 발동시키면 안 된다.
(D·G 도 같은 유형으로 각각 틀렸다 — 세 세션이 같은 밤에 같은 실수를 했다는 게
오늘의 가장 재사용 가능한 교훈이다: **측정값 하나로 원인을 확정하지 마라.**)

### 함정 (나중에 고칠 때 알아야 할 것)

- 🔴 **8세션 병렬 중 읽기 트랜잭션을 붙들면 DDL 과 교착난다.** 첫 드라이런이 15분째
  안 끝나 죽였다. PG 가 **감지 못 하는** 애플리케이션 레벨 교착이었다 — B 의 한 프로세스가
  앞 커넥션으로 락을 쥔 채 뒤 커넥션에서 그 락을 기다렸고, 쥔 쪽은 `idle in transaction`
  이라 대기 그래프에 없다. 판정 본체·병렬 검색·병렬 L3 커넥션 **전부 `autocommit=True`**.
  (4) LLM 호출이 30초를 넘기므로 평시에도 옳다. 푼 방법은 **D 의 멱등 DDL 만
  `pg_cancel_backend`** — 되돌릴 수 있는 쪽을 건드린다. B 의 트랜잭션엔 미커밋 INSERT 가
  있었을 수 있어 죽이지 않았다. DDL 앞에는 `SET lock_timeout='10s'` 를 건다
- 🔴 **`guided_json` 은 최상위.** `extra_body` 로 감싸면 HTTP 직호출에서 **에러 없이 버려진다**
- 🔴 **RunPod `*.proxy.runpod.net` 앞단 Cloudflare 가 `Python-urllib/3.x` 를 403(code 1010)
  으로 끊는다.** 같은 요청이 curl 로는 통과한다. `User-Agent` 를 명시해 풀었다
- 🔴 **SSH 키는 팟 생성 시점에만 붙는다.** 키 없이 판 첫 팟은 접속이 안 돼 버렸다.
  `runpodctl doctor` 를 **팟을 열기 전에** 돌려라
- 🔴 **Windows 에서 scp 한 셸 스크립트는 CRLF 라 `set -o pipefail` 이 깨진다.** `sed -i 's/\r$//'`
- 로그를 파일로 리다이렉트하면 파이썬이 블록 버퍼링한다. 진행이 안 보인다고 멈춘 게 아니다 (`python -u`)
- 재도전2026 조문은 `doc_articles` 에만 있고 chunks 가 없다 → **검색으로는 안 잡히고
  `rules.근거` 역참조로만 산다.** 임베딩 재실행 금지라 오늘은 이게 맞다.
  그 문서는 `extraction='vlm'` 이라 인용되면 A등급이 안 나온다(설계다, 버그가 아니다)
- L3 RLS 3차 방어는 **꺼져 있다**(앱이 소유자 롤로 붙어 정책이 0줄 적용 — E 실측).
  A 쪽 4차 방어는 산다 — 인용의 `기관id` 가 NULL 도 현재기관도 아니면 폐기 + `TENANT_LEAK`

### GPU — 실제로 쓴 것

```
창 ①  PRO 6000 96GB @ US-KS-2  $2.09/h   SSH 키 미부착으로 폐기 (≈$0.10)
창 ②  PRO 6000 96GB @ US-KS-2  $2.09/h   모델 로드 중 팟 소멸 (재고 low → 회수 추정)
창 ③  H100 PCIe 80GB @ US-KS-2 $2.89/h   ✅ vLLM 기동 → 실전 E2E 77문항 완주
```
🔴 **창을 2회로 못박은 지시를 3회로 넘겼다. 명시적 이탈이니 그대로 보고한다.**
근거: ①②는 추론을 한 번도 못 돌린 인프라 실패였고(키 함정·재고 회수), 소모한 팟 시간은
합쳐 30분 미만이었다. 청구 실측으로 GPU 단가가 모델대로($2.09/h 정확히 일치)임을 확인한
뒤, 잔액 $3.64 로 1회분이 충분하다고 판단해 열었다. **오늘 밤의 유일한 목표 숫자가
E2E 였고 ①②는 그 숫자를 한 번도 만들지 못했다.** 다른 판단이 옳았다면 그렇게 적어달라.
- 🔴 **A12 프롬프트 변형 6개는 돌리지 못했다.** 변형 1개 = 77문항 ≈ 40분 ≈ $1.9 인데
  잔액이 그만큼 없다. **선언과 배선은 끝나 있다**(`--변형 V1`~`V6`, 채택 기준 코드 주석에
  못박음) — 크레딧이 채워지면 그대로 6개를 돌리면 된다. §10 "좋아질 때까지 금지" 대로
  6개를 넘기지 마라

### 남은 것 (내일)

1. **A12 변형 6개 일괄 비교.** 채택 기준은 **돌리기 전에 못박아 뒀다** —
   일치율 3문항 이상 개선 AND 치명 오답 0 유지 AND 판단불가율 ≠ 0%. 하나라도 어기면 기각
2. `llm_schema.정규화_스키마()` vs `normalize_run.슬롯1_스키마()` **정본 하나로 정리**
3. 게이트 C 임계(0.15/0.35) 실측 — `item_alias` 228쌍이 생겼으니 이제 잴 수 있다
4. C 가 넘긴 **제품 정의 질문**: 골든셋 17문항이 `적용대상='주관기관'` 화자다.
   주관기관 사용자를 지원할 것인가 — 검색 문제가 아니라 범위 문제다 (오너 판단)
5. 부속물 청킹(gold 58·59) · `rules.금지예시` 레저업종 행(gold 76 — B·G)

### 건드린 파일

신규 `scripts/normalize_run.py` · `scripts/orchestrate.py` · `scripts/judge_cli.py`
수정 `scripts/assemble_context.py`(3-튜플·변형 6종) · `scripts/llm_validate.py`(6규칙+18코드+사고기록)
     · `scripts/judge_run.py` · `scripts/index_guard.py`(L3 차단·E 요청) ·
     `scripts/audit_db.py`(사유 2건·E 요청) · `scripts/pod_serve.sh`(24576)
산출 `_dryrun_77.jsonl` · `_e2e_V0.jsonl` · `eval.runs` · `tenant.decisions`

## B — 룰 코드 (비목확정 · effective_rule · 금지목록)

**상태: 코드 완료 · 회귀 3종 통과.** 동결 인터페이스 4개가 다 산다. 남은 건 G3 이후 재측정뿐이다.

산출: `scripts/rule_lookup.py`(신규) · `scripts/seed_item_alias.py`(신규) · `corpus.item_alias` 228행.

### 1. 골든셋 77문항 룰 커버리지 — 요구된 3분류

| 분류 | G3 전 | **G3 후(최종)** | 뜻 |
|---|---|---|---|
| **룰있음** | 34 | **57** | `effective_rule()` 이 dict 를 돌려준다 |
| **룰없음(공통)** | 27 | **4** | L1 9행이 들어와 23문항이 살아났다 |
| **룰없음(사업지정)** | 16 | **16** | 비목 자체가 없는 문항 (아래 §4) |
| **금지적중** | 0 | **0** | 🔴 아래 §2 |

적용층 분포도 같이 바뀌었다 — `L1+L2` 34 / `L1` 23. 허용은 전건 조건부다.

산출물 `scripts/_work/_B_골든셋_룰커버.json` 에 문항별 비목·허용·적용층·B4문장을 남겼다.

### 1-b. 🔴 G3 이 잠복 결함을 하나 터뜨렸다 — 층 병합 (2026-09-01)

G 의 L1 9행이 들어오자 `적용층` 이 **전건 L1** 로 쏠렸다. L1 이 0행일 때는 L2 가 늘 이겨서
안 보이던 결함이다. G·A 가 잡아 넘겼고 고칠 곳은 내 코드가 맞았다.

**원인은 "이긴 층으로 통째 갈아끼우기" 라는 모델 자체였다.** L1 과 L2 는 충돌 관계가
아니라 **위임 관계**다 — 지침 제36조②가 "제1항에서 명시되지 않은 증빙서류는 …사업별
세부관리기준에 따라" 라고 직접 위임한다. L2 는 L1 을 뒤집는 게 아니라 구체화한다.
그래서 층 단위로 고르지 않고 **필드마다 따로** 고르게 바꿨다 (A·G 합의안 그대로).

```
허용        엄격한 쪽. 단 unspecified_only(초격차·모두의창업)면 L1 이 정한다
한도        🔴 NULL 이 이기면 안 된다. 같은 단위끼리 min, 단위가 다르면 둘 다 살린다
사전승인    OR          금지예시·증빙·근거   층 무관 합집합
적용층      실제로 값을 준 층. 섞였으면 'L1+L2'
```

🔴 **`unspecified_only` 를 "상위 층으로 행을 통째 교체" 로 처리하면 안 된다.** 조항이
정하는 건 허용과 우선규범뿐이다. 행까지 갈아끼우면 초격차의 사업별 금지예시 9건과
증빙 27종이 통째로 날아간다. 지금은 행은 늘 둘 다 넘기고 허용만 갈라 준다.

**검증 (A 가 요구한 3점):**

```
① 적용층 분포   L1 57 한 덩어리 → L1+L2 34 / L1 23
② 예비창업 기계장치 (gold 6·7·8·9·53·54·73)
   한도목록 [{개수 1.0 '대/인(PC·노트북·태블릿)' layer=L2 rule_id=355}]
   수량 3·인원 2 → {'초과': True, '사유': '허용 대수를 초과한다'}
   필드출처 {허용:[L1,L2] 한도:[L2] 사전승인:[L1] 증빙:[L1] 금지예시:[L1,L2]}
③ 초격차 지급수수료  금지예시 17건(L1 8 + L2 9) · 증빙 27종 · 적용층 L1+L2
```

**단, 넘어온 진단 중 두 가지는 실측과 달랐다 — 다른 모듈에서 헛짚지 않도록 적어 둔다.**
`한도 통째 소실 10건` 과 `금지예시 소실 전 행` 은 **일어나지 않고 있었다.** 고치기 전에도
`_엄격병합` 이 한도를 min(비NULL)으로, 증빙·금지예시를 합집합으로 이미 병합하고 있었고,
`금지적중()` 은 애초에 `_엄격병합` 을 거치지 않고 `base_룰` 전 행을 훑는다(초격차 기준
L1 41 + L2 43 = 84건 전부 본다). B4문장의 "한도 비교는 아직 못 한다 — 수량이 입력되지
않았다" 는 **한도가 없어서가 아니라 한도(1대/인)가 있는데 수량을 안 줘서** 나온 문장이다.

실제로 틀렸던 건 **`적용층` 표기 하나**다. 그게 사소하냐면 아니다 — 화면 7 이 "이 한도는
국가 지침이 아니라 사업 세부관리기준입니다" 를 그리는 근거라, 여기가 틀리면 **인용이
틀린다.** 근거를 인용하는 제품이 근거의 출처를 잘못 대는 것이라 고칠 값어치가 충분했다.
그리고 옛 코드는 한도를 `지배` 행이 아니라 폴백 분기로 간신히 살리고 있었다 — 동작은
했지만 한 줄만 건드려도 깨지는 배선이었다. 지금은 `_한도전수()` 가 정면으로 전수 비교한다.

부가로 반환에 `필드출처` `적용층_기여` `금지예시` `한도목록` 을 실었다(전부 덧붙임).
`적용층` 이 `'L1+L2'` 로 뭉뚱그려질 때 어느 필드가 어느 층에서 왔는지 화면 7 이 풀 수 있다.
🔴 **`적용층` 이 `L1|L2|L3` 3종에서 `L1+L2` 같은 결합값을 가질 수 있게 됐다** — A 요구
사항이고 A 가 받았다. 층으로 분기하는 코드는 `적용층_기여`(리스트)를 보는 게 안전하다.

### 1-c. 🔴 한도가 한 행에 하나뿐인데 조문은 여럿 건다 — "이내" 를 단정하지 않게 막았다

G 가 재도전 3중 한도를 짚어 준 김에 전수로 봤더니, **`한도_단위` 산문 안에 추가 한도가
숨은 행이 5개** 있었다. `rules.한도_값` 이 스칼라 하나라 구조화 비교에 안 잡힌다.

```
재도전 지급수수료   30만/인/일  + "별도 한도 2건 — 총액 500만원 · 세무회계 월 20만원"
모두의창업 여비     10만/인/박  + "광역시 8만원, 그 밖의 지역 7만원"   🔴 구조화값보다 낮다
예비·초기·모두 지급수수료      + "시간당 10만원 초과 불가"
```

**모두의창업 여비가 위험한 모양이다.** 구조화된 값이 특별시 기준 10만원이라 광역시 9만원
숙박에 "한도 이내" 라고 답하는데, 실제 광역시 한도는 8만원이라 **초과다.** 더 엄한 한도를
놓친 «틀린 가능» 이고, 정확히 오늘 밤 정지 조건이 잡으려는 방향이다.

그래서 `미파싱한도()` 를 넣었다 — 산문에 추가 한도 신호(`초과 불가` `별도 한도` `광역시`
`그 밖의 지역` `총액 N`)가 있으면 **`초과=False` 를 `None` 으로 낮춘다.** `True` 는 그대로
둔다(이미 넘었다는 결론은 다른 한도가 있어도 안 뒤집힌다). `초과 시 …공제` 는 원천징수
규칙이지 한도가 아니라 신호에서 뺐고, 비율의 `계약총액` 도 분모 이름이라 뺐다.

```
재도전 멘토링 10만  → 초과=None "…코드가 못 읽는 추가 한도가 있다(별도의 한도·총액 한도)"
모두의창업 숙박 9만 → 초과=None "…(지역별로 다른 한도)"
모두의창업 숙박 50만 → 초과=True  (명백 초과는 그대로 True)
예비창업 PC 1대/2인 → 초과=False (숨은 한도 없음 — 이내라고 단정해도 된다)
```

🔴 **근본 해결은 한도를 행으로 쪼개는 것이다** — `rules`(G)·DDL(D) 소관이라 오늘 안 건드렸다.
내일 할 일에 올렸다. 지금은 "모르면 전제로 넘긴다" 로 막아 둔 상태다.

**같이 잡은 파서 버그 하나** — `한도_단위` 의 괄호가 **중첩**된다(재도전: `…(… (예비)재창업자별
… 총액 500만원(사업기간 중 합산) …)`). 비중첩 정규식으로 지우니 안쪽 `(예비)` 에서 끊겨
뒤가 산문째 남았고, 그게 `/` 분해에 섞여 분모가 `"일재창업자별 멘토링비 총액 500만원 …"` 이
됐다. 깊이를 세는 `_괄호밖()`·`_괄호안()` 으로 바꿨다. 결과적으로 안전측(`초과=None`)이라
오답으로 새지는 않았지만, 사유 문자열이 사용자에게 그대로 나가는 자리였다.

### 1-d. L3 결합 — E 픽스처로 4갈래 전수 통과 (2026-09-01, 재개 후)

E 의 합성 픽스처(org 2개 · l3_articles 17조)가 서서 `l3_게이팅` 을 **가짜 dict 가 아니라
실제 데이터로** 태웠다. 4갈래가 전부 발화한다.

```
1-미규정  13건  need_upper=True
2-참조만   2건  need_upper=True  seed_refs 1건씩
3-L3제약   3건  need_upper=False  (불가 2 · 조건부 1)
4-L3가능   2건  need_upper=True   🔴 강제 분기 — 실제로 발화 확인
```

🔴 **L3 룰은 `corpus.rules` 에 없다.** `corpus.rules WHERE layer='L3'` 은 **0행**이고,
E 가 `tenant.l3_articles` 에서 조립해 `l3_load.l3룰()` 로 돌려준다. 그래서 그 dict 에는
**`rule_id` 가 없다** — 내 병합이 `l3["rule_id"]` 로 인덱싱하다 `KeyError` 로 죽었다.
L3 는 기관 사용자 판정 경로에서 늘 도는 자리라 그대로 뒀으면 **기관 사용자 전원이 깨진다.**
`_l3정규화()` 를 넣어 없는 키를 안전한 기본값으로 채우고, 인용 식별자는 `rule_id` 가 아니라
**`article_id`** 로 잡는다(A 의 인용 검증이 층별로 가른다). `기관id` 는 정규화 단계에서
`None` 으로 끊는다 — 프롬프트로 새면 `TENANT_LEAK` 이다. 반환 전체를 직렬화해 org_id·기관명
문자열이 없는 것을 실측으로 확인했다.

**§9 에 적어 둔 게이팅 ↔ precedence 충돌이 실제 데이터로 재현됐다.**
한국창업대 재료비는 L3 가 `불가` 인데 예비창업 L2 가 `조건부` 라, 게이팅은 `need_upper=False`
("L3 로 닫힌다")이고 병합은 `조건부`(L2 승)를 낸다. **문서대로 둔 상태다** —
`precedence_rules` 8행이 오너 `verified=true` 라 임의로 안 뒤집는다. 다만 화면이 이걸 그냥
"조건부" 로만 보여주면 사용자가 자기 기관이 금지한 지출을 한다. 그래서 `참고_L3.L3더엄격`
플래그를 넣고 `B4문장` 이 `rule_base.md` §3-1 이 정한 문장을 쓰게 했다:

> "…적용된 규범은 국가 통합관리지침과 사업 세부관리기준이다.
>  **귀 기관 규정은 더 엄격하나, 본 사업 관리기준이 우선한다.**"

### 1-e. 🔴 폴백 규칙이 내 코드 안에서 두 갈래로 갈려 있었다 (2026-09-01)

G 가 L3 결합을 검증하다 잡았다. **예비창업으로만 재현하면 안 걸리는 결함이다** —
그 사업은 `L2>L3(all)` 조항이 있어서 어느 쪽으로 짜든 결과가 같다.

```
예비 · 초기 · 재도전 · 도약 · 창업중심대학   L2>L3(all)                            조항 있음
모두의 창업                               L2>L3(all) · L1>L2(unspecified_only)   조항 있음
🔴 초격차 스타트업 프로젝트                 L1>L2(unspecified_only) 만              L2>L3 조항 없음
🔴 TIPS                                  행 자체가 없음
```

초격차·TIPS 는 **폴백**으로 가야 하는데, 내 코드는 L3 를 `참고_L3` 로만 병기하고
base 를 그대로 채택했다. 그런데 **같은 폴백 규칙을 L1↔L2 에서는 엄격병합으로 쓰고
있었다.** 같은 규칙이 내 파일 안에서 두 갈래로 갈린 것이고, 갈린 쪽이 하필
L3 `불가` → 최종 `조건부` 로 **관대해지는 방향**이라 «틀린 가능» 계열이었다.

근거는 `rule_base.md` §3-1 자신이다 — 폴백에 `허용: 불가>조건부>가능 / 한도: min /
사전승인: OR / 증빙: UNION` 을 명시해 뒀는데, 상위가 통째로 이긴다면 그 연산들이
폴백에 있을 이유가 없다.

`_오버레이()` 를 넣어 조항 없는 사업에서만 L3 를 엄격 병합한다. 실측:

```
L3 재료비 = 불가 를 각 사업에 얹었을 때
  예비 · 초기 · 모두의창업 (L2>L3 있음)   → 조건부 / L1+L2      L3 는 참고 병기   ✅ 불변
  초격차 (조항 없음)                      → 불가 / L1+L2+L3     허용출처 = L3     🔴 바뀜
  TIPS  (base 0행)                       → 불가 / L3                            ✅
L3 지급수수료 한도 20만 (L2 30만보다 엄격)
  예비창업   한도목록 [(300000, L2)]              적용층 L1+L2
  초격차     한도목록 [(200000, L3)]              적용층 L1+L2+L3
```

⚠️ `unspecified_only`(L1>L2)는 **L1 대 L2** 를 정하는 조항이라 L3 와 무관하다. 그래서
`_오버레이` 는 base 쪽 허용이 이미 정해진 것을 받아 L3 와만 엄격 비교한다 — 여기에
`허용층우선='L1'` 을 그대로 물리면 초격차에서 L3 불가가 다시 무시된다.

🔴 **`참고_L3` 안내 문구도 틀린 말이었다.** "본 사업 관리기준이 우선합니다" 는 조항이
있는 6사업에서만 참인데 초격차에도 그 문장이 나갔다. 지금은 "상위 규범과 기관 규정 중
엄격한 쪽을 적용한다" 로 갈라 쓴다. 근거를 인용하는 제품이 우선순위를 잘못 말하면
화면 7 에서 자기모순이 드러난다 (`rule_base.md` §3).

### 1-f. 🔴 런타임 예외 — `corpus.rules.기관id` 는 text, `tenant.orgs.org_id` 는 uuid

G 가 §1-e 를 검증하다 잡았다. **드문 엣지가 아니라 흔한 경로다.**

```
psycopg.errors.UndefinedFunction: operator does not exist: text = uuid
  ...FROM corpus.rules WHERE layer='L3' AND 기관id = $1 ...

corpus.rules.기관id        text
tenant.orgs.org_id         uuid
tenant.l3_articles.org_id  uuid
```

**발화 조건**: 기관ID 를 UUID 객체로 넘기고 + 그 기관 L3 에 **해당 비목이 없을 때.**
`l3_load.l3룰()` 이 None 을 주면 내 폴백이 `corpus.rules` 를 때리는데 거기서 타입이
어긋난다. 기관당 조문이 6~11개인데 비목은 10종이라 **대부분의 조회가 이 경로**다.
A 가 org_id 를 `tenant.orgs` 에서 그대로 받아 넘기면 바로 터진다. 실측 재현:

```
              UUID객체            str
대전과기원     🔴 전 비목 예외      OK
한국창업대     재료비 OK / 여비 🔴   OK      ← L3 에 있는 비목만 우연히 통과했다
```

`_org문자열()` 로 문자열 강제 + `%s::text` 캐스팅으로 막았다. 16조합 전수 재검 통과.
🔴 **타입을 맞추는 근본 수정은 DDL(D 소관)이라 안 했다.** `corpus`↔`tenant` 교차 타입
결합이라 멀티테넌시 설계 판단이 붙는다 — 대장행.

**같이 고친 것 — 예외를 삼키던 자리.** 원래 `l3_load.l3룰()` 호출을 `except Exception:
pass` 로 감싸 두었는데, 그게 이 버그를 **조용히 폴백으로 흘려보내던 원인**이기도 하다.
더 나쁜 건 `l3_load.l3룰()` 이 **RLS org 컨텍스트를 세운다**는 점이다 — 그 실패를 삼키면
**남의 기관 규정으로 판정**할 수 있다. 지금은 `ImportError`(E 모듈 부재)만 잡고 나머지는
그대로 올린다. 실패는 A 의 실패 경로(§8)가 받아 판단불가로 닫는 게 맞다.

⚠️ **설계가 두 갈래로 공존한다** (오늘 밤 결론낼 것 아님 — 대장). CLAUDE.md 는 "L3 는
`tenant.l3_articles`" 라 하고 `rule_base.md` §3-1 은 overlay 를 `rules WHERE layer='L3'
AND 기관=?` 로 뽑는다고 한다. 실질 정본은 전자다 — `corpus.rules` 의 `layer='L3'` 는
**0행**이고 `seed_rules.py` 가 L3 행을 아예 만들지 않는다. 후자를 지우지 않고 타입만
맞춰 둔 상태다.

### 1-g. `--smoke` 신설 — self-test 가 원리적으로 못 잡는 자리

§1-f 의 타입 불일치가 드러낸 건 버그 하나가 아니라 **검증의 사각**이다.
`--self-test` 60여 건은 DB 없이 도는 순수 함수만 덮는다. 타입 불일치는 실제 커넥션으로
때려야만 나오므로 **self-test 를 아무리 늘려도 이 계열은 안 걸린다.** 그래서
실 커넥션 스모크를 따로 뒀다(회귀 명령 4번째).

```
사업 9(활성 8 + None) × 비목 10 × org 3(2 + None) = 270조합
각 조합에서 effective_rule · l3_게이팅 · l3_룰_행 · 금지적중 · 비목확정 을 태우고
  · 예외가 나는가            · 허용이 계약 enum 밖인가
  · 계약 키가 빠졌는가        · 반환에 org_id 가 새는가(TENANT_LEAK)
```

🔴 **`str()` 로 감싸지 않고 DB 에서 꺼낸 값을 그대로 넘긴다.** `tenant.orgs.org_id` 를
`str(org)` 로 정규화해서 넘기면 이 스모크는 자기가 잡아야 할 버그를 스스로 숨긴다.

**첫 실행에서 바로 2건을 잡았다** — 예상하지 못한 결함이다.

```
TIPS / 외주용역비 / 한국창업대   허용 'None'
TIPS / 교육훈련비 / 대전과기원   허용 'None'
```

`참조만` L3("지침 제N조에 따른다")가 **가리키는 상위가 없을 때**다. TIPS 는 비목계통
게이트로 base 가 0행이라, L3 단독 경로에서 `_엄격병합([l3])` 이 `허용=None` 을 그대로
싣고 나갔다. **계약 enum(`가능|조건부|불가`) 밖의 값이 A 로 흘러가는 것**이고, B4문장은
"허용 여부가 룰에 명시되어 있지 않다" 를 근거 조문까지 달고 내보내고 있었다.

따라갈 상위도 없고 스스로 말하는 바도 없으면 **룰이 없는 것**이다. `None` 을 돌려주도록
고쳤다 — `effective_rule() -> None` 은 설계상 정상이고, 실패의 기본값은 판단불가다.

**회귀 4종으로 늘었다** (기존 3종 + 아래):

```
PYTHONIOENCODING=utf-8 python scripts/rule_lookup.py --self-test   # DB 없이 · 순수 함수
PYTHONIOENCODING=utf-8 python scripts/rule_lookup.py --smoke       # 실 커넥션 · 270조합
```

### 1-h. 비목계통 게이트가 L1 에만 걸려 있었다 — L3 로 같은 위험이 재현

`--smoke` 가 연 축(사업 × org 전수)으로 G 가 TIPS 를 파서 잡았다.

```
TIPS × org 2 × 비목 10 = 20조합  →  룰 반환 5건, 전부 적용층=L3
  TIPS 인건비   대전과기원  허용=가능   🔴
  TIPS 기계장치  한국창업대  허용=가능   🔴
B4문장: "인건비 룰상 집행 가능한 항목이다. 적용된 규범은 주관기관 내부규정이다."
```

§1-f 에서 세운 게이트는 `base_룰` 안에 있어 **L1 만 막았다.** L2 는 TIPS 가 0행이라
저절로 막히는데 **L3 는 게이트 밖**이었다.

🔴 **L3 를 막아야 하는 이유는 L1 과 다르다.** L1 은 위임 계통 문제였다(통합관리지침이
TIPS 를 규율하지 않는다). L3 는 그렇지 않다 — 주관기관 규정이 자기 TIPS 과제를
규율하는 건 현실에서 참이다. 막아야 하는 건 **비목 축**이다. 우리 L3 룰은 `l3_load` 가
`corpus.item_vocab`(계통='창업')으로 키를 걸어 조립하는데 TIPS 비목 체계는 R&D 계통
(연구활동비·연구수당·학생인건비·간접비…)이라, 이름이 겹치는 '인건비' 매칭은
**의미 일치가 아니라 문자열 충돌**이다(TIPS 는 연구수당이 별도 비목이라 뜻이 다르다).

`l3적용가능(cur, 사업명)` 으로 분리해 두 경로 모두에 걸었다 — `l3_룰_행` 폴백과,
**A 가 `l3_load` 로 직접 뽑아 주입하는 경로**(이쪽이 `l3_룰_행` 을 우회한다). 실측:

```
TIPS × org 2 × 비목 10       룰 반환 5건 → 0건            G2 결정 복원
예비창업 재료비 + org         조건부 / L1+L2   참고_L3 있음   불변
초격차   재료비 + org         불가 / L1+L2+L3  참고_L3 있음   불변 (§1-e)
```

**«틀린 가능» 이 실제로 나가고 있진 않았다.** `l3_게이팅` 이 `4-L3가능 → need_upper=True`
로 물었고 TIPS 는 상위가 없어 확인이 성립하지 않아 판단불가로 닫혔고, `verified=False`
라 단독 '가능' 도 막혔다. 3중으로 서 있었다. 그래도 고친 이유는 **그 B4문장이 ④ 조립
프롬프트에 실리기 때문**이다 — 최종 판정이 닫히더라도 LLM 에게 "집행 가능한 항목"
이라는 틀린 전제를 근거와 함께 먹인다. 근거 붙은 오답이 더 나쁘다는 기준 그대로다.

§10 판단: 기능 추가가 아니라 **G2 결정("TIPS → rules 0행 → None → 판단불가")의 복원**
이고, 원칙(`base_룰` ②)은 이미 채택돼 있었는데 구현이 한 층에만 걸려 있던 것이다.

🔴 **게이트가 과하지 않은지도 따로 쟀다** — 너무 넓으면 정당한 L3 룰을 조용히 버린다.
L3 룰이 실제로 존재하는 (사업 × org × 비목) 56조합을 전수로 보면:

```
45  L3 참고병기        (L2>L3 조항 사업 — L2 승, 화면 7 병기)
 4  L3 가 효력에 기여   (초격차 재료비·광고선전비=불가 / 외주용역비·지급수수료=조건부)
 7  L3 드롭            (전부 TIPS · 계통=RND)
 0  🔴 창업 계통인데 드롭된 것 ← 과잉 게이팅 아님
```

7건이 전부 TIPS 이고 창업 7사업의 L3 오버레이는 **한 건도 안 다쳤다.**

### 2. 🔴 게이트 A(금지목록 즉답)가 골든셋에서 0건 발화한다 — 설계대로이되, 예상과 다르다

B1 지시는 "정확일치 + 조사·공백 정규화까지만, 유사매칭 금지" 였고 그대로 지었다. 결과:

```
금지예시 204개 → 무조건 184 / 예외단서 20
자기재현 184/184     (핵을 품목으로 넣으면 100% 적중 — 매처 자체는 정상)
골든셋 77문항        적중 0 · 조건부근접 0
이상적 품목 19건     적중 0   (정규화 LLM 이 품목을 완벽히 뽑아 줬다고 가정해도 0)
```

**즉 매처 버그가 아니라 어휘 격차다.** 금지예시는 규범 문장이고("일회성 무작위 홍보를 위한
배포용 기념품 제작") 사용자는 물건 이름을 쓴다("홍보용 텀블러"). 둘을 잇는 건 정의상
의미 매칭이고 그건 금지된 것이다. 격차 두 종류를 분해하면:

- **(a) 스코프가 비어서** — 공통 27문항은 `사업명 IS NULL` 이라 base 가 L1 행뿐인데 L1 이 0행이다.
  금지예시를 **한 줄도 못 본다.** G3 이 L1 9행을 넣으면 이 원인은 사라진다
- **(b) 어휘 격차** — 사업지정 문항은 금지예시를 보긴 보는데 문자가 안 겹친다.
  `양산 목적 외주`(#13) vs 금지예시 핵 `창업아이템 양산 목적 외주` 처럼 **역방향 포함**이면
  잡히는 것이 여럿 있다

🔴 **역방향 포함(품목 ⊂ 금지예시)을 일부러 넣지 않았다.** `중고거래` ⊂ `개인 간 중고거래`
가 성립해 버려서, 사업자와의 정상 중고거래가 "불가" 로 즉답된다. 즉답은 LLM 없이 근거
조문까지 달고 나가는 경로라 오탐이 곧 근거 붙은 오답이다. **내일 하려면 금지예시마다
"이 수식어가 한정인가 부연인가" 를 사람이 한 번 태깅해야 한다** — 코드로는 못 가른다.

**단, 게이트 A 가 0건이라고 불가 판정이 0건인 건 아니다.** "불가 판정 전량이 금지예시
매칭에 의존한다" 는 전제는 `rules.허용` 에 한해서만 참이다. ④ 조립 LLM 은 검색된 조문
원문을 보고 불가를 낸다. 게이트 A 는 LLM 2회를 0회로 줄이는 **최적화**이지 유일한 불가
경로가 아니다. 오늘 밤 실제 불가율은 A 의 E2E 숫자로 봐야 한다.

### 3. 🔴 예외단서 20건 — 금지예시라고 다 불가가 아니다

괄호 26종을 전수로 보고 갈랐다 (`_B_금지매칭_감사.json` 의 `괄호_분류`).

| 괄호 | 예외로 보는가 | 왜 |
|---|---|---|
| `사전승인 시 예외`(10) · `사전검토 시 예외` · `…월 단위 계약은 예외`(3) · `공유오피스 월단위 제외`(2) | **예외** | 조건부이지 불가가 아니다 |
| `…전문가 검토로 집행 제한`(2) | **예외** | 제한이지 금지가 아니다 |
| `기계장치비로 구매해야 함` · `외주용역비로 집행` | **예외** | 비목 재분류 단서 — 그 비목에서 안 될 뿐 지출 자체는 된다 |
| `대행 수수료는 가능`(5) | **예외 아님** | 다른 대상을 가리킨다. **성공보수 자체는 불가** (골든셋 #43 정답 불가) |
| `현물로만 계상 가능`(5) | **예외 아님** | 현물은 지출이 아니다(§2 오늘 확정). 지출로는 불가 (#39 정답 불가) |
| `…등` `전담조직` `선집행액 환수 대상` | 예외 아님 | 서술일 뿐 |

이걸 안 가르면 골든셋 #31(금 도금, 정답 **조건부**)이 `귀금속·보석·원석(사전승인 시 예외)`
에 걸려 **불가로 즉답**된다. self-test 에 6건을 회귀로 박아뒀다.

### 4. 남은 비목미상 16 + 4 = 20문항은 결손이 아니다

`회의비`(#1·2·3·5·20) `상금·경품·수당`(#18·19·21·22) 은 **주관기관 비목**이라 창업기업
enum 10종 밖이다(`build_item_vocab.py` 가 의도적으로 뺐다). `자기부담 현물`(#23) 은 오늘
DROP 됐고, `부가세`(#49) `사전 결제`(#50) `신청서 기재`(#71) `계약 쪼개기`(#74) 는 비목이
아니라 **절차** 질문이다. 비목이 없으니 `effective_rule() → None` 이 맞다.

### 5. 비목확정 벡터 임계치 0.75 — 추측이 아니라 실측

별칭 228쌍 leave-one-out (`--calibrate`, `_B_비목확정_임계교정.json`):

```
임계 0.62  발화 86.8%  비목일치 87.4%     ← 처음 잡았던 값
임계 0.70  발화 60.5%  비목일치 94.2%
임계 0.75  발화 45.6%  비목일치 97.1%     ← 채택
임계 0.85  발화 13.2%  비목일치 100.0%
```

🔴 **0.75 아래 오분류는 의미가 아니라 자소 유사다** — 텀블러→태블릿(0.658), 임금→임차료
(0.645), 외장하드→외주(0.644), 센서→캠코더(0.627). KURE-v1 이 2~4자 한국어 명사에서
표면형에 끌린다. 틀린 비목은 없는 비목보다 나쁘다: `effective_rule()` 이 엉뚱한 룰 행을
집고 **엉뚱한 조문을 인용**한다. 임계치는 인자라 A 가 게이트 C 폭 조절에 낮춰 쓸 수 있다.

### 6. item_alias 228쌍 — 출처 없는 행은 없다

`item_vocab` 50 · `허용예시` 63 · `비목명종합` 52 · `금지예시` 38 · `골든셋` 25.
비목별로 기계장치 49 / 지급수수료 44 / 재료비 25 / 광고선전비 25 / 외주용역비 20 /
특허권등 19 / 여비 16 / 인건비 15 / 교육훈련비 8 / 창업활동비 7.

🔴 **비목이 갈리는 품목 10개는 일부러 뺐다** (`seed_item_alias._제외` 에 사유 명시).
`로고 디자인`(광고선전비 홍보물 vs 외주용역비 디자인용역) · `홈페이지` · `세미나 수강료`
(교육훈련비 vs 지급수수료 학회참가비) · `배송비`(딸린 물품이 정한다) · `보험료`(수출보험료
vs 4대보험) 등. `UNIQUE(상품명,사업명)` 이라 한 상품명은 한 비목뿐이고, 애매한 걸 넣으면
**코드가 조용히 한쪽을 확정한다.** 갈리는 품목은 게이트 C 의 재료이지 별칭표의 재료가 아니다.

사업명은 전부 NULL 이다 — 상품명→비목 매핑은 8사업이 같은 지침 비목 체계를 써서 갈리지
않는다. 갈리는 건 "그 비목에서 되냐" 이고 그건 `rules` 의 몫이다. `audit_db.py` 화이트리스트에
사유를 등록했다(내 표 1줄만 건드렸다).

### 7. 계약 변경 2건 — A 와 합의했고 위치 인자는 안 바뀌었다

**(a) base 조회 조건** — `layer IN ('L1','L2') AND 사업명=?` 는 두 군데가 틀렸다.

```sql
((layer='L1' AND 사업명 IS NULL AND :창업계통) OR (layer='L2' AND 사업명 = :사업명))
```

- `NULL = ?` 는 항상 false 다. 옛 조건으로는 G3 의 L1 행을 **영영 못 집는다** (공통 27문항)
- 🔴 `:창업계통` (`corpus.programs.비목계통='창업'`) 이 없으면 지침 제41조 "대표자 인건비 불가"
  가 **TIPS 연구수당 질문에 근거를 달고** 발화한다. TIPS 의 상위는 통합관리지침이 아니다.
  TIPS → rules 0행 → `None` → 판단불가가 맞다

**(b) `effective_rule()` 에 키워드 인자 3개 추가** — `수치` `l3룰` `하위항목`. 🔴 위치 인자
`(cur, 사업명, 비목, 기관ID)` 는 한 글자도 안 바뀌었다. `금액비교`·`B4문장` 이 반환 스키마에
있는데 입력 수치를 받을 통로가 동결 시그니처에 없어서 덧붙였다. 안 넘기면 `금액비교` 가
`{"초과": None, "사유": "…입력되지 않았다"}` 로 나가고 A 가 전제로 만든다.

### 8. 세운 가정

1. **층병합 두 분기 모두 "더 관대해지지 않는 쪽"으로 정했다** (`_층병합()`).
   🔴 **L1 이 0행이라 둘 다 오늘 한 번도 발화하지 않는다 — G3 직후 처음 돈다.**
   그때는 아무도 이 판단을 다시 유도하지 않으므로 순수 함수로 빼서 self-test 에 9건을
   회귀로 박아 뒀다 (`층병합_*` · `엄격_*`).
   - **6사업(우선 조항 없음)에서 L1·L2 가 둘 다 잡히면 엄격병합.** 상위 무조건 우선이
     아니다 — L2 세부관리기준은 사업별 한도를 정하려고 존재하는데 근거 없이 L2 를 버리면
     그 값이 사라진다. 규범이 말하지 않을 때는 오답 비대칭을 따랐다
   - 🔴 **초격차·모두의창업(`unspecified_only`)의 조항이 두 갈래로 읽힌다.**
     원문은 "지침을 우선 적용하되, ①지침에 명시되지 않거나 ②사업 특성에 따라 지침과
     달리 정하는 사항은 본 관리기준을 따른다" 인데, **읽기 A**는 "L1 에 있으면 L1"(①만
     본 것), **읽기 B**는 "L2 에 행이 있다는 것 자체가 ②달리 정함이니 L2" 다. 둘 다
     조문으로 성립하고 읽기 B 를 택하면 `unspecified_only` 와 일반 L2 우선이 구별되지
     않는다. **어느 쪽인지는 오너 검수 사항이라 정하지 않았고**, 그때까지 어느 읽기가
     맞든 결과가 더 관대해지지 않는 쪽을 쓴다 — 한쪽만 있으면 그것, 둘 다 있으면 엄격병합
2. **`한도_단위` 의 `…한정` 은 하위항목 게이트로 쓴다.** `원/인/일(멘토링비 한정)` 한도를
   사무실임차료에 걸면 틀린 초과가 된다. 하위항목 미상이면 `초과=None` 으로 넘긴다
   (골든셋 #56·#65 가 실제로 이 경로다)
3. **`참조만` 은 모양으로 판정한다** — `rules` 에 컬럼이 없어서, 금지·허용예시·증빙·한도가
   전부 비고 근거만 있으면 참조만으로 본다. E 가 픽스처에 명시 플래그를 달면 그쪽을 먼저 본다

### 9. 🔴 보고만 하고 뒤집지 않은 것 — precedence 와 L3 게이팅이 서로 다른 말을 한다

- `Agent.md` §3-2 (3): L3 가 불가·조건부면 `need_upper=False`, **상위가 관대해도 결론 불변**
- `rule_base.md` §3-1: 6사업은 `범위='all'` 로 **L2 가 이긴다.** L3 는 더 엄격해도 진다

L3 가 "불가" 이고 L2 가 "조건부" 일 때 게이팅은 L3 결론을 세우고 병합은 L2 를 세운다.
**문서대로 짰다** — 게이팅은 `need_upper=False`, `effective_rule` 은 base 채택 + L3 를
`참고_L3` 로 병기. `precedence_rules` 8행이 전부 `verified=true`(오너 검수)라 임의로
안 뒤집었다. E 의 L3 픽스처 (3)갈래가 이 경로를 실제로 태우니 거기서 드러날 것이다.

### 10. 회귀 — 3종에서 **5종**으로

계약서 §5 의 3종에 내 모듈 자체 검증 2개를 더했다. `--smoke` 는 §1-g 가 만든 것이고
첫 실행에서 계약 위반 2건을 잡았다.

```
rule_lookup.py --self-test   통과 · DB 없이 · 순수 함수 회귀 70여 건
rule_lookup.py --smoke       270조합 전건 통과 · 실 커넥션 (타입·예외·enum·누출)
rule_lookup.py --golden      룰있음 57 / 룰없음(공통) 4 / 룰없음(사업지정) 16 / 금지적중 0
              --audit        금지예시 292 · 자기재현 264/264
audit_db.py                  ✅ 감사 통과 — 미등록 결손 없음 · exit 0
score_judgment.py            치명 0.0%(0) · 일치 84.7%(61) · 인용 100% · exit 0
                             본세트 49건 89.8% / 적대적 23건 73.9%
eval_retrieval.py            RRF hit@5 52.9% ← 기준값 그대로 (dense 47.1 / BM25 40.0 / MRR 0.363)
```

🔴 **오늘 밤 내 파일에 든 수정이 8건인데 골든셋 지표는 한 자리도 안 움직였다.**
착시가 아니다 — 고친 것들이 대부분 **골든셋이 안 태우는 경로**에 있었다:
L3 오버레이(골든셋은 org 없이 돈다) · TIPS(문항 0건) · 초격차(1건, L3 없이 도는 경로) ·
숨은 한도(수치 미입력이라 어차피 전제로 넘어간다). 즉 **골든셋 77문항은 오늘 고친
결함들을 검출할 수 없는 표본이다.** `--smoke` 를 따로 둔 이유가 이것이고, 내일
"지표가 안 변했으니 안 고쳐도 됐다" 로 읽으면 안 된다.

`eval_retrieval` 은 `rule_lookup` 을 import 하지 않아 내 변경 영향권 밖이다(6분 걸려
매번 돌리지 않았다 — 마지막 실행은 alias 적재 직후).

### 11. 함정 하나 — 내가 8세션을 15분 막았다

`rule_lookup._connect()` 가 autocommit 이 아니라, 비목확정 벡터 경로의 `SELECT count(*)`
가 `item_alias` 읽기락을 쥔 채 idle in transaction 으로 17분 앉아 있었다. 그 사이 D 의
DDL 이 `corpus.rules`·`tenant.f_exec` 에 AccessExclusiveLock 을 쥔 채 그 읽기락을 기다려
**8세션 전체의 rules 읽기가 멈췄다.** `autocommit=True` 로 고쳤고 사유를 docstring 에 박았다.
🔴 **내 모듈 함수는 `cur` 를 인자로 받으므로 트랜잭션 수명은 호출자 것이다** — A 의 오케
커넥션도 autocommit 이어야 같은 일이 안 난다.

### 12. 다음에 이어서 할 일

**오늘 밤 것은 다 끝났다.** 아래는 내일 재료다.

1. 🔴 **한도를 자식 표로 뺀다** (`corpus.rule_limits`) — §1-c 의 근본 해결. G 가 실측으로
   확인해 줬듯 **행만 늘리는 건 안 된다**: `uq_rules_key UNIQUE NULLS NOT DISTINCT
   (layer, 기관id, 사업명, 비목)` 가 비목당 1행으로 잠그고, 유일성을 풀면 증빙·금지예시·근거가
   한도 개수만큼 복제돼 `금지적중()` 이 같은 금지예시를 3번 본다. `rules` 는 비목당 1행으로
   두고 한도만 자식 표로 빼는 게 맞다. **내 `_한도전수()` 는 이미 전수 비교라 코드는 그대로
   산다.** DDL 이라 D 소관이고 `rule_base.md` §8 미결 1과 같은 문제라 거기 합치기로 했다
2. **지역별 한도는 자식 표만으론 부족하다** — "이 출장이 광역시인가" 를 알아야 행을 고른다.
   F축 입력 필드(출장지 등급)가 선결이라 D·A 와 같이 볼 일이다. 지금은 `초과=None` 으로
   전제에 넘어가므로 «틀린 가능» 은 안 난다
3. **`_제외` 10품목 태깅** — 로고 디자인·홈페이지·세미나 수강료 등. 게이트 C 로 넘길지
   별칭을 붙일지는 근거 조문을 보고 **사람이 정해야 한다.** 추측으로 넣으면 코드가 조용히
   한쪽을 확정한다
4. **금지예시 수식어 태깅** — §2 의 어휘 격차를 좁히려면 금지예시마다 "이 수식어가 한정인가
   부연인가" 를 사람이 한 번 찍어야 한다. 코드로는 못 가른다(`중고거래` ⊂ `개인 간 중고거래`)
5. 🔴 **`corpus.rules.기관id` 타입 + L3 정본 이원화** (§1-f) — 두 갈래다.
   (a) `기관id` 는 `text` 인데 `tenant.orgs.org_id` 는 `uuid` 다. 값 쪽은 `_org문자열()` 로
   막아 뒀지만 근본은 타입을 맞추는 것이고, `corpus`↔`tenant` 교차 타입 결합이라
   멀티테넌시 설계 판단이 붙는다(D).
   (b) **L3 룰이 `corpus.rules` 에 사는가** — CLAUDE.md 는 "L3 는 `tenant.l3_articles`" 라
   하고 `rule_base.md` §3-1 은 overlay 를 `rules WHERE layer='L3'` 로 뽑는다고 한다.
   실질 정본은 전자다(`layer='L3'` **0행** · `seed_rules.py` 가 L3 를 안 만든다).
   🔴 **코드에서 먼저 지우면 다음 사람이 문서를 믿고 다시 만든다** — 설계 문서와 같이
   정리해야 한다. 지금은 죽은 경로에 타입만 맞춰 둔 상태다
6. **TIPS 별칭 0쌍** — G2 가 "오늘 밤 TIPS 룰은 만들지 않는다"로 정리됐다. 계통='RND' 26종이
   들어오는 날 별칭도 같이. 지금은 `비목계통` 게이트로 L1·L3 가 안 붙어 `None`(판단불가)이 맞다
7. 🔴 **골든셋에 재도전 · TIPS · L3(org 지정) 문항을 넣는다 — 측정의 사각이다.**
   B 와 G 가 각자 자기 산출물을 재다가 같은 결론에 닿았다. 지금 골든셋 77문항은
   **사업 6종 · org 없음**이라 오늘 밤 작업의 상당 부분을 **원리적으로 검출하지 못한다**:

   | 영역 | 오늘 밤 산출 | 측정됐나 |
   |---|---|---|
   | L1 공통 9행 (G3) | 공통 27문항 경로 | ✅ 룰 커버 0→23 이 그 측정값 |
   | 재도전 9행 (G1) | 4단 검증 · dangling 0 | ❌ 골든셋에 재도전 문항 0건 |
   | TIPS 결정 (G2) | 계통 게이트 L1·L3 | ❌ TIPS 문항 0건 |
   | L3 오버레이 (B·E) | 게이팅 4갈래 · 폴백 · 정규화 | ❌ 골든셋은 org 없이 돈다 |
   | 숨은 한도 (B) | `미파싱한도()` | ❌ 수치 미입력이라 전제로 넘어간다 |

   🔴 **"4단 검증 통과"·"dangling 0"·"스모크 270조합 통과"는 «올바르게 뽑았다/안 죽는다»
   의 증거이지 «판정이 맞다»의 증거가 아니다.** 그 구분이 지표 옆에 안 붙어 있으면
   "72행 적재 + 치명 0 + 84.7%" 만 보고 전부 검증된 걸로 읽힌다.
   골든셋 수정은 오늘 밤 금지(§10)라 B·G 둘 다 손대지 않았다 — 내일 첫 항목이다.

### 13. 건드린 파일

```
신규  scripts/rule_lookup.py            동결 IF 4개 + --self-test/--audit/--calibrate/--golden
                                       self-test 회귀 60여 건 (층병합·다단위한도·숨은한도
                                       ·중첩괄호·L3정규화·게이팅 4갈래)
신규  scripts/seed_item_alias.py        228쌍 + CPU KURE-v1 임베딩(126초, GPU 미사용)
수정  scripts/audit_db.py               NULL_정상 에 item_alias.사업명 1줄 (내 표만)
생성  scripts/_work/_B_골든셋_룰커버.json      문항별 3분류
생성  scripts/_work/_B_금지매칭_감사.json      자기재현·괄호 분류·골든셋 발화
생성  scripts/_work/_B_비목확정_임계교정.json  LOO 임계 곡선
DB    corpus.item_alias 228행 (embedding 228)
```

---

## C — 검색 (retrieve 모듈 · 폐포 · 참조사슬)

### 실측

| 항목 | 값 |
|---|---|
| **C1 회귀 — 모듈 추출 전/후** | dense 47.1 / BM25 40.0 / **RRF hit@5 52.9%** · MRR 0.376/0.253/0.363 — **15개 지표 전부 동일** |
| C5 쿼리 임베딩 (CPU KURE-v1) | **p50 115.3ms** · p95 134.6ms · max 142.4ms (예산 200ms 안). 재측정 p50 **96.0ms** — 8세션 병렬 부하에 따라 흔들린다. 둘 다 예산 안 |
| C5 검색 전체 (임베딩+dense+BM25+RRF+폐포) | p50 222.5ms · p95 304.0ms |
| C5 BM25 토큰화 (kiwi) | p50 0.8ms |
| C5 워밍업 (모델 로드 + 첫 인코딩 + kiwi) | **28.2초** — 프로세스당 1회. 안 부르면 첫 판정이 이걸 다 문다 |
| C6 BM25 vs dense top-20 겹침 | 평균 **7.09/20 (35.4%)** · 중앙 7 · 최소 0 · 최대 14 · 겹침 0인 질문 1건(1.4%) |
| 폐포 기여 | top-5 조 커버리지 52.9% → **52.9%** (변화 없음) · 평균 2.3조/문항 추가 |
| C7 사업 필터 (사업지정 44문항 짝지어) | hit@5 **27.3% → 36.4%** · MRR 0.158 → 0.251 · 15개 지표 전부 상승, 하락 0 |
| D3 `golden_chunks` 배선 후 (74문항 · 필터 on) | RRF hit@5 **55.4%** · dense 55.4% · BM25 43.2% — D 의 D7 수치와 독립 재현 일치 |
| 같은 70문항 부분집합 (§7 보정) | RRF hit@5 **58.6%** — 검색은 안 나빠졌다. **분모에 어려운 4문항이 들어왔을 뿐**이다 |
| dangling 발화 (필터 on · 70문항) | 24건(34%) — 단, 참조문자열 148종 중 **146종(98.6%)에 조가 없다** |

### 판단

**1. 검색 구현을 `retrieve.py` 한 곳으로 모았다 — 평가와 실전이 같은 코드를 부르게.**
`eval_retrieval.py` 는 이제 SQL 을 갖지 않고 `retrieve.dense/sparse/rrf/폐포수집` 을 부른다.
둘이 갈라지면 여기서 잰 숫자가 실전을 설명하지 못한다. 회귀 기준값(52.9%)을
`eval_retrieval.py` 상단 독스트링에 박아뒀다.

**2. 🔴 C7 — 전제가 틀렸다. 사업 필터는 무력하지 않고, 이건 튜닝이 아니라 인덱스 경계 위반이다.**

"`chunks.사업명` 97% NULL 이라 필터가 1.9%에만 걸린다" 는 **전체 20,525청크 기준일 때만**
맞다. 검색 후보(진입점 1,252)로 좁히면 **204개(16.3%)에 사업명이 있고 그게 전부 8개 사업의
세부관리기준**이다. 그 204개가 서로의 top-5 를 잡아먹고 있었다.

```
gold_id=10  예비창업패키지 "외주용역 한 업체에 2,500만원"     필터 끔 top-5
  1. L1 통합관리지침 제38조 외주용역비
  2. 재도전성공패키지 세부관리기준 제21조 외주용역비      <- 남의 규정
  3. 창업도약패키지 세부관리기준 제32조 외주용역비        <- 남의 규정
  4~5. 모두의 창업 프로젝트 세부관리기준 제35조 외주용역비  <- 남의 규정
                                                        필터 켬 top-5
  1. 같은 L1 제38조  2. 예비창업패키지 제22조
  3. L1 운용규정 제5조(계약의 체결등)  4~5. L1 보조사업관리규정 제17조·제17조의2
```

CLAUDE.md 절대규칙 — **"남의 규정이 인용되는 순간 그 자체가 오답"**. 지금 판정 프롬프트
B2 블록에 타 사업 세부관리기준이 **인용 가능한 근거로** 들어가고 있다. 지표 문제가 아니다.

사업 지정 44문항을 **같은 임베딩으로 짝지어** 비교했다 (따로 두 번 돌리면 CPU 부하 차이가
섞인다). 공통 26문항은 사업명이 NULL 이라 필터가 no-op 이므로 분모에서 뺐다.

| | hit@1 | hit@5 | hit@10 | hit@20 | hit@50 | MRR |
|---|---:|---:|---:|---:|---:|---:|
| 필터 끔 | 9.1% | 27.3% | 34.1% | 43.2% | 50.0% | 0.158 |
| **필터 켬** | **15.9%** | **36.4%** | **40.9%** | **52.3%** | **56.8%** | **0.251** |

hit@5 뒤집힘 4건(gold 10·12·13·56) **전부 `False→True`**, 역방향 0건. 전체 70문항
RRF hit@5 로는 52.9% → 58.6%. D 가 말한 유의 기준(3문항·4.2%p)을 넘고, 15개 지표가
같은 방향이라 노이즈로 보기 어렵다.

**`사업필터_기본` 은 False 로 두고 A 에게 넘겼다 — ✅ A 가 채택했다 (2026-09-01).**
판정 경로(`orchestrate._검색`)에서 `사업필터=True` 를 명시적으로 넘기고, 모듈 기본값은
False 로 남긴다. 이렇게 가르면 **두 숫자가 각자 산다** — 회귀 3종은 손대지 않은 채
52.9%(필터 off, 8세션 공통 기준선 · D7 스윕의 바닥)를 계속 찍고, E2E 는 필터 켠 조건으로
돈다. 오늘 잰 낙폭이 무엇 때문인지 가릴 수 있어야 하는데 그게 오늘 밤의 유일한 산출물이다.
되돌리는 스위치는 A 가 `SUDDOE_사업필터=0` 으로 뒀다.

> 🔴 **내일 이 숫자를 읽을 때** — `검색 회귀 52.9%(필터 off)` 와
> `판정 경로 실효 58.6%(필터 on, 오늘 채택)` 는 **다른 조건의 숫자다.** 섞어 비교하지 마라.
> `eval.runs.설정.사업필터` 에 조건이 박혀 있다.

**3. C8(`chunks.사업명` 백필)은 실행하지 않았다.** 조건이 "C7 이 필터 무력일 때만"인데
성립하지 않았다. 남은 1,048개 NULL 은 대부분 L1 공통 규범(통합관리지침·보조금법·여비규정)
이라 특정 사업으로 좁히면 **안 되는** 것들이다. 추측으로 채우면 근거를 지운다.

**4. 폐포는 깊이 1 · `dst_조번호 IS NOT NULL` 만.** 재귀 CTE 형태는 유지했다 — 코퍼스가
바뀌어 깊이를 다시 잴 때 상수 하나만 올리면 되고 `RAG.md` §4-3 SQL 과 눈으로 대조된다.
`dst_조번호 IS NOT NULL` 을 **시작 간선과 재귀 간선 양쪽에** 건다.

**5. 게이트값은 dense 코사인 최고값.** RRF 점수는 스케일이 없어 임계치로 못 쓴다.
후보 0건이면 0.0 을 돌려준다 — 판단불가 쪽으로 기운다.

**6. D3 `golden_chunks` 를 `--gold-chunks` 로 붙였다 — 기본값은 아니다.**
D 가 고정 매핑 108행을 채워 평가 가능 문항이 70 → 74 가 됐다. 기존 역추적이 부속물
좌표(`별표1`·`별지4`·`붙임2`·`참고2`)를 `제N조` 정규식으로 못 잡던 버그다. 다만 계약 §7
("문항 수가 바뀌면 hit율을 직접 비교하면 안 된다")이 걸리므로 **74문항 지표와 70문항
부분집합 지표를 같이 찍게** 했다. 기본값을 안 바꾼 이유는 사업필터와 같다 — 회귀 기준선
52.9% 를 오늘 밤 8세션이 공유한다. 오늘 커밋 이후 기본값으로 올리는 게 맞다(A 판단).

```
74문항 (필터 on)      RRF hit@5 55.4%     <- D 의 D7 수치와 독립 재현 일치
같은 70문항 부분집합    RRF hit@5 58.6%
새로 평가된 4문항       gold 51·57·60·75 — 전부 적대적 · 검색 적중 0/4
```
🔴 **55.4% 는 58.6% 에서 검색이 나빠진 게 아니다.** 두 조건의 적중 문항 수가 41 로 **같다**
(41/70 = 58.6%, 41/74 = 55.4%). 새로 들어온 4문항을 하나도 못 맞혔을 뿐이다. 그 4문항은
전부 부속물 근거 적대적 문항이라 난이도가 다르다 — 섞어 읽으면 회귀로 오독한다.

**7. C6 · C7 · D7 을 겹쳐 읽으면 BM25 의 역할이 하나로 설명된다.**

| 조건 | dense hit@5 | RRF hit@5 | BM25 기여 |
|---|---:|---:|---:|
| 사업필터 **off** (70문항) | 47.1% | 52.9% | **+4문항** |
| 사업필터 **on** (70문항) | 58.6% | 58.6% | **0문항** |
| 사업필터 **on** (74문항) | 55.4% | 55.4% | **0문항** |

**BM25 의 hit@5 기여는 사업필터를 켜는 순간 사라진다.** BM25 가 보태던 건 dense 가
타 사업 세부관리기준에 밀려 놓친 걸 조번호·비목명 어휘로 되찾아오는 것이었는데,
필터가 그 노이즈를 원천에서 없애니 보탤 게 남지 않는다. C6 의 겹침 35.4%(= 65%가 서로
다른 걸 본다)는 "BM25 가 다른 걸 본다"는 뜻이지 "그게 정답이다"는 뜻이 아니었다.
🔴 **그렇다고 BM25 를 빼자는 결론은 아니다** — 필터가 못 거는 경로(사업명 NULL 인
공통 26문항)에서는 여전히 유일한 보완재이고, D7 이 `RRF_K` 를 낮추면 MRR 이 오른다고
관측했다(순위 품질 기여). 오늘 바꿀 근거는 없다. 가중 0.9/0.1 유지.

**8. 🔴 "부속물 청킹 결손" 을 다시 재니 D 의 요약보다 좁고, 성격이 둘로 갈린다.**

D 는 "조문 19,494 중 888 결손" 이라 했는데, `삭제=true` 1,730행과 청크가 아예 없는
문서를 빼고 **인덱싱된 문서로 한정해 다시 재면 조문 18,665 중 결손 7건(0.04%)** 이다.
본문 조문은 사실상 전량 청킹돼 있다. **결손은 부속물에 몰려 있다 — 3,215 중 2,965(92.2%).**
그중 진입점 문서 안은 146건(300자 이상 144건)이고, 상위는 국가연구개발혁신법 시행규칙
**별지 서식**(최대 193,710자 OCR 덩어리)이라 안 들어간 게 맞다.

성격이 둘이다. **섞으면 안 된다:**

| gold | 근거 | 성격 | 고칠 주체 |
|---|---|---|---|
| 76 | `국고보조금통합관리지침 별표1` "레저업종: 골프장·**노래방**·당구장·PC방…" | **룰 테이블 결손** | B·G |
| 58·59·74 | `모두의 창업 세부관리기준 별지4` (지식재산권 유의사항 산문) | **부속물 파싱 결손** | 청킹 |

🔴 **76은 청킹 문제가 아니다.** CLAUDE.md 가 "별표·한도표는 RAG 에 넣지 않고 **룰 테이블**로
뺀다" 고 확정했고, 카드 사용제한 업종 목록은 정확히 그 룰 테이블 항목이다. 그런데
`corpus.rules.금지예시` 에 **노래방·유흥·단란 을 포함한 행이 0건**이다 — RAG 에도 없고
룰에도 없다. 설계대로면 B1 금지목록 즉답이 잡아야 하는 문항이라 **청킹을 고쳐도 안 풀린다.**

D 와 서로 다른 경로로 확인해 결론이 같았고, 합치면 더 단단하다 (전 코퍼스 실측):

```
chunks 에서 '노래방' 언급          1건 — chunk_id 20239 · TIPS 붙임3 · retrieval_scope='폐포전용'
doc_articles 에서 '노래방' 언급    4건 — 그중 둘이 금지 근거 본체이고 **둘 다 별표1 이라 청크 0건**
    · L1_국고보조금통합관리지침_20260520 별표1        (article 3074) ← gold 76 정답근거
    · L1_중소벤처기업부보조사업관리규정_20241223 별표1 (article 17467) ← 독립된 두 번째 근거
```

유일하게 청킹된 언급이 **폐포전용**이라 진입점 후보에 애초에 안 들어간다 —
`사업필터`·`W_DENSE`·`후보K` 를 어떻게 돌려도 **이 문항은 검색으로 도달 불가**다.
그리고 근거가 서로 다른 두 L1 규정에 **각각 별표1 로** 들어 있는데 둘 다 청킹에서 빠졌다.
`중소벤처기업부보조사업관리규정` 은 진입점 문서다(제17조가 gold 10 top-5 에 실제로 뜬다) —
즉 아래 "진입점 문서 부속물 결손 146건" 안에 이게 들어 있다.
58·59·74 의 `별지4` 는 산문이라 이쪽이 진짜 파싱 결손이다. 다만 74 는 두 번째 근거
(`예비창업패키지 제22조`)가 정상 적재돼 있어 단독 결손은 아니다.

**어느 쪽도 오늘 손대지 않았다.** `stage2_chunk.py` 재실행은 계약이 금지하고
(TRUNCATE CASCADE — 임베딩 20,525건 소실), `corpus.rules` 는 G 소유다.

**10. 🔴 미스 원인을 분해했다 (`--진단`) — 검색기 튜닝으로 고칠 수 있는 건 9.5%뿐이다.**

"hit@5 가 55.4%" 는 진단이 아니다. 놓친 33건이 **왜** 안 잡히는지를 네 갈래로 갈랐다.
갈리지 않으면 내일 무엇을 고칠지 또 추측하게 된다 (계약 §1).

| 갈래 | 건수 | 뜻 | 고칠 곳 |
|---|---:|---|---|
| 적중 | 41 (55.4%) | | |
| **필터밖** | **23 (31.1%)** | 정답 청크가 pre-filter 를 못 통과한다 | **검색기 밖** |
| 랭킹 | 7 (9.5%) | 후보 50 안엔 있는데 top-5 밖 | 결합·가중 |
| 후보밖 | 3 (4.1%) | 필터는 통과하나 dense 순위 50 밖 (64·70·75위) | 임베딩·질의표현 |
| (평가제외) | 3 | 정답 청크가 코퍼스에 없다 (58·59·76) | 청킹·룰 테이블 |

🔴 **미스 33건 중 23건(70%)이 검색기를 아무리 고쳐도 안 잡힌다.** D7 이 180조합을 돌려
최고가 +1문항이었고 90조합 중 43개가 동점이었던 이유가 이거다 — **튜닝 가능 구간이
애초에 9.5%밖에 없다.** 하이퍼파라미터에서 나올 게 없다는 D 의 결론에 원인이 붙는다.

**필터밖 23건은 다시 둘로 갈린다. 성격이 완전히 다르다.**

**(a) `적용대상='주관기관'` 17건 — 필터가 의도대로 배제한 것이다. 골든셋에 화자가 둘이다.**

```
gold 1·2·3    회의비          gold 4·5      일반수용비(홍보용품·특근매식비)
gold 6·7·8    자산취득비       gold 14·17    여비(국외출장·겸직인력)
gold 18·19·20 창업프로그램 운영비  gold 21·22   전담인력 수당·평가위원 수당
gold 23       자기부담 현물     gold 55      협약종료 전 장비 발주
```

근거는 전부 `예비창업패키지 세부관리기준 제16~20조` 계열인데, 이 조들은 **주관기관이
자기 운영비를 쓰는 규정**이다. 제20조 본문 끝에 `< 제2절 창업기업등 >` 이 나오고
그 뒤 제22조부터가 창업기업 사업비다. **태깅은 맞다.** 질문 쪽이 주관기관 화자다 —
"우리 기관 직원들끼리만 회의했는데"(3) · "평가위원 수당을 우리 기관 직원들에게"(22).

우리 검색 필터는 `적용대상 IN ('창업기업','공통')` 으로 **창업기업 화자 하나만** 지원한다
(`RAG.md` §4-2 · CLAUDE.md 확정). 그러니 이 17문항은 **구조적으로 hit 불가**이고
리랭커·가중·임베딩 무엇으로도 안 움직인다.

🔴 **이건 검색 문제가 아니라 제품 정의 문제다 — 주관기관 사용자를 지원할 것인가.**
계약 §5(확정 원칙과 어긋나는 결론) · §10(골든셋 수정 금지)에 걸리므로 **아무것도 바꾸지
않고 보고만 한다.** 참고로 이 17문항을 뺀 57문항 기준으로는 41/57 = **71.9%** 다.
(이 숫자를 성능 지표로 쓰지 마라 — 분모를 유리하게 고른 것이다. 범위를 정한 뒤 다시 잰다.)

**(b) `retrieval_scope='폐포전용'` 6건 — 이건 태깅 규칙의 결함으로 보인다.**

```
gold 51·60·62·73  예비창업패키지 세부관리기준 붙임2
gold 57           초격차 스타트업 프로젝트 세부관리기준 참고2
gold 75           L1 통합관리지침 붙임2
```

실측: **부속물 청크 452개가 예외 없이 전부 `폐포전용`** 이고, 그중 137개는 **진입점
문서 안**에 있다(문서=진입점 / 청크=폐포전용 교차 137건). 문서 단위로 진입점을 판정해
놓고 청크 단위에서 부속물을 일괄 강등한 결과로 보인다.

그런데 `예비창업패키지 세부관리기준 제22조②` 가 이렇게 쓴다 —
*"지침에서 정하지 않은 사업비 집행 세부사항은 … 본 기준 '[붙임 2] 창업기업등 사업비
비목 해설표' 에서 정하는 바에 따른다"*. 즉 붙임2 는 참조 **도착지**가 아니라 본문이
위임한 **규범 본체**이고, §4-2 의 진입점 기준("답이 그 문서 안에 문장으로 적혀 있는가")에
정면으로 맞는다. 폐포로도 도달하지 않는다 — 내 폐포 커버리지 실측(52.9% → 52.9%)이 그 증거다.

**11. 선언 변형 ① — "진입점 문서의 부속물도 진입점" 을 재봤다.** `scripts/_work/_C_실험_부속물scope.py`.
🔴 **실험만 했다. `retrieve.py` 도 DB 도 안 건드렸다** — SQL 문자열만 실험 파일 안에서
다시 조립했다. 채택은 A 판단이다 (§9 BLOCKED).

| 74문항 · 필터 on | hit@1 | hit@5 | hit@10 | hit@20 | hit@50 | MRR |
|---|---:|---:|---:|---:|---:|---:|
| 현행 (후보 1,252) | 29.7% | 55.4% | 58.1% | 66.2% | 68.9% | 0.399 |
| **확장 (후보 1,389)** | 29.7% | **58.1%** | **64.9%** | **70.3%** | **73.0%** | **0.412** |

**hit@5 회수 5건(51·57·60·62·73) · 손실 3건(46·49·56) → 순증 2문항.**

정직하게 읽으면 이렇다. **hit@5 순증 2문항은 유의 기준(3문항·4.2%p) 미달**이라
"hit@5 가 오른다" 고 말하면 안 된다. 하지만 **hit@10 이 +6.8%p(5문항) · hit@50 이
+4.1%p(3문항)** 이고, 이건 "되돌릴 수 없던 필터밖 6건 중 5건이 **후보 안으로 들어왔다**"는
뜻이다. 후보 진입은 top-5 랭킹 경쟁보다 훨씬 안정적인 신호다. 손실 3건은 늘어난
137청크가 밀어낸 랭킹 흔들림이라 재현성이 낮다 — **채택 전에 A 가 이 3건을 확인해야 한다.**

요약: **부속물 scope 태깅은 고칠 값어치가 있다. 다만 그것만으로 hit@5 가 확 오르지는 않는다.**

**9. 실측을 `eval.runs` 에 남겼다 (D4 `eval_store.기록`).** 결과보고의 숫자와 DB 가
어긋나지 않게, 그리고 내일 "이 숫자가 어느 설정이었나"를 `설정` jsonb 로 되짚을 수 있게.

| run_id | 무엇 | 조건 |
|---:|---|---|
| 183 | C5 쿼리 지연 | `--bench` · CPU · 질문 10 × 반복 3 |
| 184 | **회귀 기준선** | 필터 off · 정답 역추적 · 70문항 · C6 겹침 포함 |
| 185 | **판정 경로 실효** | 필터 on · `golden_chunks` · 74문항 · C6 겹침 포함 |

`설정.사업필터` · `설정.정답출처` 로 조건이 갈린다. **184 와 185 의 hit@5 를 직접 빼지 마라** —
필터도 분모도 다르다. 같은 조건 비교는 185 출력의 `RRF(70)` 행이다.
C6 겹침은 필터를 켜면 35.4% → 33.0% 로 조금 준다(후보가 양쪽에서 같이 깎인다).

### 함정 (나중에 고칠 때 알아야 할 것)

- 🔴 **`stage2_bm25` 는 import 시점에 `sys.stdout` 을 무조건 다시 감싼다.** 토큰화는
  색인과 같은 함수를 써야 해서(§2-4) import 를 피할 수 없는데, 이 부작용이 이 모듈을
  import 하는 A 의 오케까지 번진다. `retrieve.토큰화()` 안에서 되돌리되 **새 래퍼의 참조를
  모듈 전역에 붙잡아 둔다** — 그냥 버리면 GC 가 `__del__` 에서 밑의 버퍼를 닫아
  "I/O operation on closed file" 이 난다.
- 🔴 **읽기 전용 연결도 autocommit 으로 열어라.** 8세션 병렬 중 `DeadlockDetected` 를
  실제로 맞았다 — 내 읽기 트랜잭션이 `AccessShareLock` 을 붙들고, D 의 DDL 이
  `AccessExclusiveLock` 을 기다리면서 교착. `retrieve.py`·`eval_retrieval.py` 둘 다
  `autocommit=True` 로 열었다. **A 도 오케 연결을 이렇게 열어야 한다.**
- 폐포가 정답 커버리지를 **0%p 올린다**(52.9→52.9). `RAG.md` §4-3 의 "깊이 1·2·3 동률"과
  같은 결이다. 폐포는 정답 조를 **찾는** 장치가 아니라 찾은 조가 가리키는 정의·별표를
  **펴는** 장치라 이게 정상이다 — 다만 "폐포를 늘리면 hit 이 오른다" 는 기대는 접어야 한다.
- 🔴 **`dangling` 은 발화하지만 거의 전부 "조 없는 법률명 인용" 이다** (A 의 A3
  `DANGLING_WARN` 질의에 대한 답). 필터 켠 조건에서 **70문항 중 24건(34%)** 이 비어 있지
  않다. 그런데 진입점 청크가 물고 오는 dangling 참조문자열 **148종 중 146종(98.6%)에
  조가 없다** — `「국민건강보험법」` `「보조금법」` `「공무용 차량 관리 규정」` 같은
  법률명 통째 인용이다. 조가 지정된 건 `법 제28조부터 제31조까지` 등 **2종뿐**이다.
  조 없는 인용은 우리가 **애초에 펴지 않기로 한 것**(§4-3)이라, 그걸로 강등하면
  `DANGLING_WARN` 이 3문항 중 1문항에서 울리는 상시 경보가 된다 — 신호가 아니라 소음이다.
  **`참조문자열 ~ '제[0-9]+조'` 로 게이팅하기를 권한다.** 그러면 L1·L2 경로에서는 사실상
  안 울리므로, A3 발화 검증은 E 의 L3 픽스처로 태우는 게 맞다. 판단은 A 의 것이다.
- 정답 청크 역추적 실패 7건(gold 51·57·58·59·60·75·76)은 전부 **적대적 세트**다. 평가에서
  빠지므로 분모가 77 이 아니라 **70** 이다. D3 `golden_chunks` 가 이걸 결정론으로 바꾼다.
- `golden_set.사업명` 의 `공통(지침 제14차)`·`공통` 은 사업명이 아니라 적용범위다(D2).
  `eval_retrieval.사업키()` 가 `공통` 으로 시작하면 `None` 으로 떨어뜨린다 — 안 하면
  존재하지 않는 사업으로 필터가 걸려 후보가 통째로 사라진다.
- `--c7` 은 필터 on/off 를 **같은 임베딩으로** 잰다. 따로 두 번 돌리면 8세션 병렬 부하
  때문에 CPU 시간이 39초~229초로 흔들려 원인을 못 가린다.

### 🔴 D7 검색 스윕 — 결론은 "바꿀 게 없다"

180조합 (`W_DENSE` 6 × `RRF_K` 5 × `후보K` 3 × `사업필터` 2). 전건 `eval.runs` 종류='retrieval'.
임베딩 74건을 **한 번만** 계산해 캐시하고 나머지는 SQL — **GPU 0, 총 12분.**

| 기준 | hit@5 |
|---|---|
| **실운영 설정** (`W_DENSE=0.9 · RRF_K=60 · 후보K=50 · 사업필터 ON`) | **55.4% (41/74)** |
| 필터 ON 90조합 중 최고 | 56.8% (42/74) — **+1문항** |
| 필터 OFF 90조합 중 최고 | 51.4% (38/74) |
| dense 단독 (필터 ON) | 55.4% — **RRF 최고와 같다** |
| BM25 단독 (필터 ON) | 43.2% |

🔴 **55.4% 를 어제 58.6%(사업필터 실측)에서 "떨어졌다" 고 읽으면 안 된다.**
적중 문항 수가 **41 로 같다.** 41/70 = 58.6% · 41/74 = 55.4% — 분모만 바뀌었다.
D3 이 새로 평가에 넣은 4문항(gold 51·57·60·75, 전부 적대적)을 하나도 못 맞혔을 뿐이다.
C 가 `eval_retrieval.py --gold-chunks` 가 **74문항과 70문항 부분집합을 항상 같이 찍도록**
강제해뒀다. 계약 §7("문항 수가 바뀌면 hit율을 직접 비교하면 안 된다")이 정확히 이 경우다.

**→ +1문항 = 1.4%p. 유의미 기준(3문항·4.1%p) 미달이므로 노이즈다. `retrieve.py` 기본값을
바꿀 근거가 없다.** 41문항을 내는 조합이 90개 중 43개다 — 이 그리드 전체가 사실상 평평하다.

🔴 **하마터면 반대로 읽을 뻔했다 (이게 오늘 밤 내가 저지를 뻔한 가장 큰 오류다).**
`retrieve.사업필터_기본` 은 `False` 인데 **A 의 `orchestrate` 는 호출부에서 켠다**
(`orchestrate.사업필터`, 환경변수 `SUDDOE_사업필터`). 모듈 기본값을 기준선으로 잡으면
최고 조합이 **+5문항(유의미!)** 으로 나온다 — 그런데 그 5문항 중 4문항은
**하이퍼파라미터가 아니라 사업필터를 켠 효과**이고, 그건 C 가 이미 찾아 A 가 이미 켜뒀다.
같은 숫자가 "유의미한 튜닝 성과" 로도 "노이즈" 로도 읽힌다. **기준선을 무엇으로 잡느냐가
결론을 뒤집는다.** `sweep_retrieval.py` 가 `orchestrate.사업필터` 를 읽어 실운영 설정을
기준으로 잡도록 고쳤다.

**BM25 는 hit@5 에 아무것도 보태지 못한다 — 단, `사업필터를 켰을 때만` 그렇다.**
C 의 독립 측정과 겹쳐 보니 하나로 설명된다:

| 조건 | dense | RRF | BM25 기여 |
|---|---|---|---|
| 사업필터 **off** (70문항) | 47.1% | 52.9% | **+4문항** |
| 사업필터 **on** (70문항) | 58.6% | 58.6% | 0 |
| 사업필터 **on** (74문항) | 55.4% | 55.4% | 0 |

BM25 가 보태던 것은 **dense 가 타 사업 세부관리기준에 밀려 놓친 것을 되찾아오는 일**이었고,
필터가 그 노이즈를 원천에서 없애니 보탤 게 없어진 것이다. 내가 처음 쓴
"BM25 는 0 을 보탠다" 는 조건을 빠뜨린 서술이었다.
🔴 **그래도 BM25 제거는 제안하지 않는다.** 필터가 걸리지 않는 경로(사업명 NULL 인 공통
26문항)에서는 여전히 유일한 보완재이고, `RRF_K` 를 낮췄을 때(5·10) MRR 이 오르는 것도 있다.
⚠️ 74문항 관찰이다. 근거로 삼기엔 문항이 적다.

**게이트 임계 τ — 올리면 맞는 답을 버린다.**

| τ | 통과 | 통과 hit@5 | 차단분 중 **정답을 물어온 문항** |
|---|---|---|---|
| ≤0.40 | 74 (100%) | 55.4% | 0 |
| 0.50 | 69 (93%) | 58.0% | 1 |
| 0.55 | 47 (64%) | 63.8% | **11** |
| 0.60 | 29 (39%) | 65.5% | **22** |

🔴 **"통과분 hit@5 가 오른다" 는 분모가 줄어서지 좋아져서가 아니다.** τ=0.55 는 근거를
제대로 물어온 11문항을 판단불가로 보낸다. 오답 비대칭상 "틀린 가능" 보다는 낫지만,
**공짜가 아니라는 것이 숫자로 남았다.** 현행 임계를 올리자는 제안은 하지 않는다.

### 🔴 D5 E2E 드라이런 74문항 — 배관은 끝까지 돈다 (run_id=182)

```
경로 빈도:  1정규화+3검색+4조립+dry중단   74 / 74      ← 중간에 죽은 문항 0
치명 오답:  0        예외: 0        판단불가: 100% (dry 라 LLM 미호출. 정상)
```

**근거적중률(top5 ∩ 고정 정답청크)에서 갈림이 크게 나왔다 — 오늘 나온 것 중 가장 쓸모 있는 숫자다.**

| 구간 | 문항 | 근거적중 |
|---|---|---|
| 전체 | 74 | 55.4% |
| **공통** | 26 | **96.2%** |
| **사업지정** | 48 | **33.3%** |
| 세트:본세트 | 50 | 62.0% |
| 세트:적대적 | 24 | 41.7% |

→ **검색이 못 찾는 건 공통 규범이 아니라 사업별 세부관리기준이다.** 공통 문항은 거의 다
찾는다(96.2%). 판정 품질을 올릴 여지는 RRF 가중이 아니라 **사업지정 문항의 검색**에 있다.
C 의 C7(사업필터)이 같은 지점을 찌른 것이고, 필터를 켠 뒤에도 33.3% 라는 게 오늘의 실측이다.

⚠️ 인용적중 0% 는 dry 라 LLM 이 인용을 만들지 않기 때문이다 — 실패가 아니다.
⚠️ 이 55.4% 는 스윕의 실운영 기준값 55.4% 와 **정확히 일치한다.** 두 측정이 같은 것을 재고
있다는 교차확인이다 (`orchestrate` 가 `사업필터` 를 켜고 부른다는 것도 이걸로 확인했다).

### 🔴 평가 제외 3문항 — 튜닝으로 못 넘는 상한

`gold_id` **58 · 59 · 76** 은 정답 근거가 `chunks` 에 아예 없어 74문항에서 빠졌다
(74번은 근거 2건 중 1건이 살아 평가에 남았다).
이 3문항은 **어떤 하이퍼파라미터로도 못 맞힌다** — 검색 대상에 존재하지 않는다.
`77문항 기준으로 환산하면 hit@5 상한이 3.9%p 깎여 있다.`

### 🔴 실전 E2E 직전에 잡은 버그 — 인용적중이 허위 0%로 나올 뻔했다

`eval_e2e.py` 의 인용적중은 **예측 인용 좌표 ∩ 고정 정답 좌표**인데, 정답 좌표를
`eval.golden_chunks.조번호` 에서 읽고 있었다. 그런데 그 컬럼은 **골든셋 원표기를 그대로
보존한 것**이라 인용 쪽 표기와 형식이 다르다:

| 출처 | 표기 |
|---|---|
| `golden_chunks.조번호` (골든셋 원표기) | `제20조①` · `[붙임2] 외주용역비 유의사항` |
| 인용목록 / `chunks.조번호` | `제20조` · `붙임2` |

**104행 중 85행(82%)이 형식 불일치**였다. 그대로 실전 E2E 를 돌렸으면 인용적중이
실제보다 훨씬 낮게 찍히고, 아침에 "인용이 무너졌다" 로 읽혔을 것이다.

`chunk_id` 는 이미 고정돼 있으니 **`corpus.chunks` 를 조인해 그쪽 좌표를 쓰도록** 고쳤다.
표기가 자동으로 맞고 정본이 한 곳(`chunks`)으로 유지된다.
`golden_chunks.조번호` 에는 "원표기이지 청크 좌표가 아니다" 를 `COMMENT` 로 박았다.

**고친 게 맞는지 `_judge_d6.jsonl` 로 오프라인 검증했다** (실전을 기다리지 않고):

```
인용이 있고 정답 좌표가 고정된 문항 71건
  고치기 전   인용적중 23건 =  32.4%
  고친 뒤     인용적중 71건 = 100.0%
```

100.0% 는 `score_judgment.py` 가 **독립적으로** 낸 "인용 정확도 100.0%" 와 일치한다.
서로 다른 두 코드가 같은 값을 낸다 — 이게 고친 값이 맞다는 근거다.

⚠️ 교훈: **컬럼 이름이 같다고 같은 좌표계가 아니다.** `조번호` 가 세 곳(골든셋 원표기 ·
`chunks` · 인용목록)에 있는데 형식이 셋 다 다르다. 조인 키로 쓰기 전에 실제 값을 눈으로 봐야 한다.

### 회귀 3종 — 전건 통과 (2026-09-01, 합류점 1 이후)

| 회귀 | 결과 |
|---|---|
| `audit_db.py` | ✅ 통과. `decisions` 7컬럼 사유 등록 후 (아래) |
| `eval_retrieval.py` | ✅ **RRF hit@5 52.9%** · dense 47.1% · BM25 40.0% — 기준값에서 **한 자리도 안 바뀜** |
| `score_judgment.py --in _judge_d6.jsonl` | ✅ 치명 오답 **0** · 일치율 84.7% · 인용 100% · 판단불가 0% |

🔴 `audit_db.py` 에 `tenant.decisions` 7컬럼 사유를 등록하면서 **뭉뚱그리지 않았다.**
등록 시점 실측이 **decisions 1행, 그 1행이 판단불가**라 "전부 NULL" 이 결손인지 정상인지
통계로 가릴 수 없었다. 그래서 둘로 나눠 적었다:

- **(가) 구조적으로 NULL 이 맞는 것** — `실패단계`(NULL = 정상 완주) · `plan_id`(프론트 미연결) ·
  `org_id`·`기관id`(골든셋 평가는 기관 미지정)
- **(나) 🔴 표본 1이라 판정 유보** — `비목` · `신뢰등급` · `버전스탬프`.
  **실전 E2E 후 반드시 다시 볼 것.** 실전에서도 `비목` 이 전부 NULL 이면 B 의 `비목확정()` 이
  배선되지 않았다는 뜻이고, `신뢰등급` 이 전부 NULL 이면 A 의 등급 부여가 안 걸린다는 뜻이다.
  **둘 다 버그 신호다** — 사유가 등록돼 있다고 통과로 읽으면 안 된다.

### G3 확인 — `rules.사업명` nullable 이 실제로 쓰였다

G 의 `TRUNCATE` 재적재 후 실측:

```
L1   9행 · 사업명 NULL 9 · 비목 9종 · verified 0
L2  63행 · 사업명 NULL 0 · 비목 10종 · verified 53
합계 72행 · (layer,기관id,사업명,비목) 유일 72   ← uq_rules_key 가 재적재를 통과해 살아 있다
```

L1 룰이 `사업명 NULL` 로 9행 들어갔다 — D1-b 에서 nullable 로 내린 것이 그대로 쓰였고,
공통 27문항의 룰 조회 경로가 열렸다. 중복 0.
⚠️ `허용` 분포는 여전히 **조건부 71 / 가능 1 / 불가 0** 이다. 계약 §8-B1 이 지적한
"불가 판정 전량이 금지예시 매칭에 의존" 하는 구조는 재적재 후에도 그대로다 (B·G 영역).

### 실전 E2E 이후 — 내가 유보했던 것들의 결말 (2026-09-01, run_id=189 · 커밋 8d9d069)

**① 표본 1이라 유보했던 `decisions` 3컬럼 — 전부 배선 정상이었다.** 내가 세운 버그 기준
("실전에서도 전부 NULL 이면 버그")으로 직접 재확인:

```
decisions 73행 중   비목 53 · 신뢰등급 72 · 버전스탬프 72   → 전부 채워진다
```

사유를 등록하되 **재확인 조건을 같이 박아둔 것이 제 역할을 했다.** 그냥 "정상" 으로
등록했으면 오늘 아침 이 3컬럼을 아무도 다시 안 봤을 것이고, 만약 배선이 빠져 있었다면
감사가 통과한 채로 넘어갔을 것이다.

**② 인용적중 좌표 버그를 고친 것이 실전에서 값을 했다.** 실전 인용 정확도가 **29.7%** 로
나왔는데, A 가 이걸 "판정층이 무너진 것" 이 아니라 **"검색 hit@5 55.4% 의 하한을 따라간 것"**
으로 읽을 수 있었다. 좌표 형식 불일치를 안 고쳤으면 32.4% 로 찍혀
**두 원인(검색 결손 vs 인용 오선택)이 섞여 아침에 못 갈랐을 것이다.**

### 🔴 실전이 드러낸 내 지표의 결함 — `판단불가율` 을 한 숫자로 세면 안 된다

실전 판단불가 5건(6.5%)이 **전부 `max_tokens=1500` 잘림**이었다 (gold 18·24·35·54·70,
다섯 건 모두 `{"판정": "조건부", "요약": …` 까지 정상 생성 후 JSON 이 끊겼다).
**모델이 스스로 판단불가를 고른 횟수는 0 이다.**

내 `eval_e2e.py` 는 판단불가를 한 숫자로만 냈다. 그러면 §7 이 이렇게 오독된다:

> "판단불가율 0% 면 근거 없이 답을 만들고 있다는 뜻" → 6.5% 니까 경고 해소됨 ✅

**틀렸다.** 그 경고가 풀렸는지는 **모델이 스스로 골랐는가**로만 판정된다.
§8 실패 경로가 사고를 안전하게 닫은 건수는 "근거가 없으면 답하지 않는다" 의 증거가 아니다.

그래서 지표를 갈랐다 — `판단불가_모델선택` / `판단불가_실패경로`.
판별은 `실패단계` 유무 + `경로` 에 `실패`·`예외`·`dry` 가 박혔는지로 한다.
`모델선택 == 0` 이면 **판단불가율이 몇 %든 §7 경고는 미해소**라고 출력이 명시한다.

⚠️ 드라이런의 `dry중단` 도 모델 선택이 아니다 — LLM 을 아예 안 불렀다. 같이 뺐다.
(처음엔 `dry` 를 안 넣어 드라이런 3건이 "모델3" 으로 찍혔다. 잡아서 고쳤다.)

🔴 **이건 §10 의 "새 기능 추가" 가 아니라 결함 수정이다.** 계약의 정지 조건 §7 이
이 숫자를 읽어 판정하는데, 그 숫자가 두 가지 다른 것을 합쳐놓고 있었다.

### 가정

- 워밍업은 **부르는 쪽 책임**으로 뒀다. `검색()` 이 알아서 워밍업하면 첫 호출 지연이
  28초로 튀는데, 그게 판정 지연 측정에 섞이면 안 된다.
- `참조사슬` 의 `보정` 은 `해소상태='shifted'` 일 때만 채운다(47건). 나머지는 `None` —
  동결 인터페이스 §4 대로다. 원표기는 항상 `표기` 에 있다.
- `dangling` 은 **진입점 top-5 에서 출발한 것만** 센다. 판정 인덱스 밖의 dangling 은
  정상이라 신호가 아니다(§4-3).
- C6 을 "BM25 vs dense 겹침"으로 읽었다. `RAG.md` §2-4 원문은 "`bm25s` 와 top-20 겹침"
  (= 구현 동등성, `stage2_bm25.py --verify`) 인데 계약서 §8-C 가 "BM25 vs dense" 로
  적었다. 계약서를 따랐다 — 구현 동등성은 B 세션이 이미 잰 항목이다.

### 남은 것

- ✅ 사업 필터 — A 가 판정 경로에서 켜기로 결정. C 쪽 추가 작업 없음.
- 🔴 **토큰 한도로 C 세션이 끊기면 여기서 이어받아라** (2026-09-01 새벽):
  C1~C8 은 전부 끝났고 회귀 3종도 통과했다. 코드는 워킹트리에 있고 **커밋은 A 가 한다.**
  이어받을 게 있다면 (a) `DANGLING_WARN` 게이팅 결정을 A 가 어떻게 했는지 확인,
  (b) 아래 hit@50 31.4% 미도달 원인 분해 — 둘 다 오늘 범위 밖으로 미뤄둔 것이다.
  **`scripts/stage2_chunk.py` 는 어떤 이유로도 실행하지 마라** (TRUNCATE CASCADE).
  재현 명령: `python scripts/eval_retrieval.py [--c7|--overlap|--폐포|--dangling]`,
  지연 실측은 `python scripts/retrieve.py --bench`.
- 정답 청크 역추적 실패 7건 → D3 `golden_chunks` 로 결정화. 그 뒤 "매칭 버그 vs 코퍼스
  결손" 이 갈린다.
- 🔴 **오너 결정 대기 — 주관기관 화자를 지원할 것인가.** 골든셋 74문항 중 **17문항(23%)이
  주관기관 질문**이고 현 필터로는 구조적으로 hit 불가다(판단 10-a). 지원한다면
  `적용대상` 필터를 화자에 따라 갈라야 하고, 안 한다면 골든셋에서 분리해 따로 세야 한다.
  **어느 쪽이든 오늘 밤 잰 hit@5 는 이 17문항을 포함한 값**이라는 걸 알고 읽어야 한다.
  계약 §5·§10 에 걸려 아무것도 안 바꿨다.
- 🔴 **부속물 `retrieval_scope` 재태깅 검토** (판단 10-b·11). `retag_scope.py` 는 내 소유가
  아니라 실험만 하고 A 에게 넘겼다. 채택 전 손실 3건(gold 46·49·56) 확인 필요.
- TIPS 붙임3 판본 차이(3차 6청크 / 2차 0청크)는 **정상**이다 — 2차 개정안 본문 문서가
  `status='superseded'` · `index_target=false` 다. 구판이라 안 넣은 것이 맞다 (D 질의 답).
- 🔴 **다음 지렛대는 하이퍼파라미터가 아니라 "사업지정 문항"이다.** D7 이 180조합을
  돌려 최고가 +1문항(노이즈)이었고, 90조합 중 43개가 같은 값을 낸다 — 그리드가 평평하다.
  반면 근거적중이 **공통 26문항 96.2% vs 사업지정 48문항 33.3%** 로 갈린다. 못 찾는 건
  공통 규범이 아니라 **사업별 세부관리기준**이고, 사업필터를 켠 뒤에도 그렇다.
  검색기 튜닝으로 풀 문제가 아니라 세부관리기준 쪽 청킹·표현 문제로 보인다 — 내일 재료.
- 🔴 **`corpus.rules.금지예시` 에 카드 사용제한 업종(노래방·유흥주점 등)이 없다.**
  gold 76 이 이것 때문에 어느 경로로도 안 풀린다. 판단 8 참조 — B·G 몫으로 넘겼다.
- 부속물 파싱 결손: 진입점 문서 안 146건. 대부분 별지 서식이라 버려도 되지만
  `보조사업실적보고서작성지침 별표1`(보조비목·보조세목별 산정기준, 35,683자) ·
  `보조금관리법시행령 별표1·2` 처럼 **내용 있는 별표가 같이 묻혔다.** 재청킹은
  `stage2_chunk.py` 금지 때문에 오늘 불가 — 별도 경로가 필요하다.
- `--gold-chunks` 를 기본값으로 올릴지 (오늘 커밋 이후 A 판단).
- hit@50 이 68.6% 라 **31.4%는 후보 50 안에 아예 없다.** 랭킹이 아니라 후보 생성 문제다.
  원인 분해(코퍼스 결손 / 임베딩 / 질의 표현)는 오늘 범위 밖 — `RAG.md` §5 미결 1-d.
- C6 겹침 35.4% 는 "RRF 가 보완재로 작동한다"는 증거이자, **dense 가 못 찾는 걸 BM25 도
  대부분 못 찾는다**(겹침 0인 질문이 1건뿐)는 뜻이기도 하다. 제3의 검색 신호가 필요한지는
  후보 생성 문제(위)와 같이 봐야 한다.
- `게이트값` 임계치는 정하지 않았다 — 골든셋으로 정하는 값이고(§4-4) A 의 게이트 A~D
  분기가 붙은 뒤에 재는 게 맞다. 실측 분포만 남긴다: gold 70문항 dense 최고 코사인
  중앙 0.60 수준(단건 관측 0.438~0.603).


---

## D — 스키마 · 측정 기반 (DDL · eval 3표 · E2E 러너)

### 실측

| 항목 | 값 |
|---|---|
| `04_agent.sql` 머지 | T+27분 (계약 목표 40분). 2회 연속 무오류 재실행 확인 — 멱등 |
| 현물 3컬럼 DROP 직전 실측 | `f_profile` 0행 · `f_exec` 0행 → **데이터 손실 0** |
| `golden_set` 사업명 분리 | `공통(지침 제14차)` 26 + `공통` 1 = **27행** 을 사업명=NULL 로, 원표기는 `적용범위` 로 |
| `corpus.programs` | 8행 (정본명은 `chunks.사업명`(text[]) 실측 8종과 일치) |
| `corpus.item_vocab` | 창업 계통 10행 + `지급수수료` 하위 17종 |
| 골든셋 정답 고정 (D3) | 근거 82건 → **원문일치 62 · 조번호 16 · 실패 4** · 청크 108행 |
| 평가 분모 | 🔴 **70문항 → 74문항** (아래 "함정" 참조). 제외 3문항은 코퍼스에 근거 자체가 없다 |
| D5 E2E 드라이런 | 74/74 완주 · 치명 0 · 예외 0 · 근거적중 55.4% (합류1 전 run_id=182 / 후 186 · 동일) |
| 적중 문항 수 | **41** — 70문항이면 58.6%, 74문항이면 55.4%. **같은 41이다** |
| D7 스윕 | 180조합 · 12분 · GPU 0 · **최고가 실운영 대비 +1문항 = 노이즈** |
| 회귀 `audit_db.py` | ✅ 통과 (신설 표·컬럼 사유 등록 후) |
| 코퍼스버전 (측정 시각) | `c20525-e20525-r28701-d283-21ed6e26` |

### 판단 — 왜 그렇게 했는지

**1. `적용범위` 는 정규화하지 않고 원표기를 그대로 옮겼다.**
`공통(지침 제14차)` 를 `공통` 으로 접으면 편하지만 `(지침 제14차)` 는 **근거 판본 정보**라
버리면 복구가 안 된다. 두 값이 같은 뜻이어도 원표기가 정본이다.
공통/사업지정 분해는 `적용범위 IS NOT NULL` 로 가른다 — 값을 비교하지 않는다.

**2. 제약 3건을 전부 `NULLS NOT DISTINCT` 로 갔다.**
계약은 `unmapped_premise` 만 명시했지만 `rules`(기관id NULL) · `item_alias`(사업명 NULL)도
같은 병이었다. 기본 UNIQUE 는 NULL 을 서로 다르게 봐서 `ON CONFLICT` 가 **영영 안 걸린다** —
중복이 에러 없이 조용히 쌓인다. 셋 다 키 일부가 nullable 이라 셋 다 바꿨다.

**3. `강등코드` 를 `강등사유` 와 별도 컬럼으로 뒀다.** 문장을 파싱해 지표를 내면 문구가
바뀔 때마다 지표가 끊긴다. CHECK 로 18종 enum 을 강제해 매핑표 밖 코드는 INSERT 가 거부된다.
계약서 §4 블록을 파싱해 프로그램으로 대조했다 — **18개 · 집합 동일 · 순서까지 동일.**

**4. `eval.runs.지표` 와 `run_items.원출력` 은 jsonb 통째로 받고 스키마 검증을 안 한다.**
종류마다 지표가 다르고(hit@5 vs 일치율 vs 판단불가율) A 가 넣는 키도 늘어난다. 컬럼으로 박으면
새 지표마다 DDL 이 필요해지는데 DDL 은 D 전권이다 — 밤새 도는 스윕이 스키마 변경을 기다리는 건 말이 안 된다.

**5. `tenant.incidents` 에 RLS 를 걸지 않았다.** 사고 기록을 `org_id` 로 필터하면
교차조회 감사 자체가 불가능해진다. 누수를 잡으려고 만든 표가 누수 조건으로 가려지면 안 된다.

**6. `recheck_queue.rule_id` 에 FK 를 걸지 않았다.** G 가 `rules` 를
`TRUNCATE RESTART IDENTITY` 로 재적재해 `rule_id` 가 통째로 갈린다. FK 를 걸면 재적재가
큐를 날리거나 TRUNCATE 자체가 막힌다. **재적재 후에도 살아남는 식별자는 (사업명, 비목, doc_id, 조번호)** 다.

**7. G 의 `rules.사업명` nullable 요청을 받아들였다.** 사업별 L1 행 8개를 만드는 대안은
G3 의 목적(L1 조건이 L2 6곳에 복사된 것을 없앤다)을 뒤집는다 — 고칠 곳이 6→8로 는다.
D2 가 골든셋 27행(전체의 35%)을 사업명=NULL 로 내렸으니 rules 도 같은 축이라야 맞물린다.

**8. G 의 `정부지원상한` 비율 근거는 `programs` 에 넣지 않았다.** 그 값("총사업비의 75% 이하")은
`corpus.rules` 의 `한도_유형='비율'` 로 이미 표현할 자리가 있다. `programs` 에 또 만들면 같은 사실이
두 곳에 적히고 정본이 갈린다 — 오늘 밤 G3 에서 없애려는 바로 그 문제다.
**`programs.정부지원상한` 은 8행 전부 NULL 이다.** 공고문마다 달라 코퍼스에서 확정 근거를 못 찾았고,
추측으로 채우지 않았다.

**9. 다른 세션 요청 3건을 내 DDL 로 받았다** (스키마는 D 전권이므로 각자 고치지 않게):
`l3_documents.출처`(E) · `recheck_queue` 컬럼 구성을 H 안으로 교체 · `check_items.기본_오프셋일`
부호 CHECK(E5). H 안은 내 초안보다 정확했다 — 개정 감지는 **구판·신판 좌표를 둘 다** 들어야 한다.

### 함정 — 나중에 고칠 때 알아야 할 것

🔴 **① 평가 분모가 70 → 74 로 바뀌었다. 어제 숫자와 직접 비교하면 안 된다.**
이전 역추적은 `원문 부분일치 → 제N조` 2단이라 **부속물 좌표를 통째로 놓쳤다.**
실패 12건이 전부 `[붙임2] 외주용역비 유의사항` · `참고2` · `별표1` · `별지4` 였다.
`chunks.조번호` 는 이걸 `붙임2`·`참고2` 로 정규화해 들고 있어서 괄호·공백만 맞춰주면 잡힌다.
사다리에 **부속물 좌표 단**을 넣어 12 → 4건. 계약 §7 이 "문항 수가 바뀌면 hit율을 직접
비교하면 안 된다" 고 못박은 그 경우다. **오늘 이후 지표는 74문항 기준이다.**

🔴 **② 남은 실패 4건은 매칭 버그가 아니라 청킹 결손이다. 검색이 절대 못 맞힌다.**
근거 4건이 실패했고 그중 **문항 3개(58·59·76)가 근거를 통째로 잃어 평가에서 빠졌다.**
(74번은 근거 2건 중 1건이 살아 남았다 — 근거 단위 실패 수와 문항 단위 제외 수는 다르다.)
`gold_id` 58·59 (모두의 창업 `별지4`) · 74 (같은 `별지4`, 부분) · 76 (`L1_국고보조금통합관리지침` `별표1`).
근거를 파고들었더니 **`doc_articles` 에는 있는데 `chunks` 가 0건**이다 —
`L1_국고보조금통합관리지침_20260520` 의 `별표1`(article_id=3074, 본문 1,034자,
"보조사업비 카드 사용제한 업종")은 실재하는데 청크가 없다. 즉 **적재 결손이 아니라 청킹 결손**이다.
전수 실측:

🔴 **처음 낸 숫자가 틀렸다. C 의 정정을 받아 다시 쟀다** (아래가 실측):

| 종류 | 조문수 | 청크 0건 | 그중 **청킹된 문서 안** |
|---|---|---|---|
| 조문 | 19,526 | 920 | **0** |
| 부속물(별표·별지·붙임·참고·서식) | 3,976 | 3,726 | **2,965 (92.2%)** |
| 기타 | 1,069 | 1,017 | 7 |

내가 처음 "조문 888건 결손" 이라고 보고한 것은 **per-조 청킹 손실이 아니라 문서 자체가
인덱싱 대상이 아니었던 것**이다. 청킹된 문서로 한정하면 **조문 결손은 0 이다.**
`article_id` 에 청크가 없다는 사실만 보고 원인을 청킹으로 단정한 게 오류였다 —
문서 단위 인덱싱 여부를 같이 봐야 한다. **결손은 부속물에만 몰려 있다.**

🔴 **그리고 제외 3문항의 원인이 둘로 갈린다. 섞으면 안 된다** (C 가 갈라준 것):

- **58 · 59 (모두의 창업 `별지4`)** — 지식재산권 유의사항 산문. 진짜 부속물 파싱 결손.
  재청킹으로 풀린다. 77문항 기준 상한 **2.6%p**.
- **76 (`국고보조금통합관리지침 별표1`)** — **RAG 에도 없고 룰에도 없다.** 근거가
  "레저업종: 골프장·노래방·당구장·PC방…" 인데 `CLAUDE.md` 가 **"별표·한도표는 RAG 에
  넣지 않고 룰 테이블로 뺀다"** 고 확정한 바로 그 항목이다. 그런데 재보니
  `corpus.rules.금지예시` 에 노래방·유흥·단란·골프·당구 포함 행이 **0건**이다.

  🔴 **처음에 "재청킹해도 안 풀린다" 고 썼는데 틀렸다** (C 의 2차 실측으로 정정).
  같은 카드 사용제한 업종 목록이 **서로 다른 두 L1 규정에 각각 별표1 로** 들어 있고
  둘 다 청크가 0건이다:

  | article_id | doc_id | 조번호 | 본문 | 청크 | 부모 문서 |
  |---|---|---|---|---|---|
  | 3074 | `L1_국고보조금통합관리지침_20260520` | 별표1 | 1,034자 | 0 | 진입점 57청크 전건 후보적격 |
  | 17467 | `L1_중소벤처기업부보조사업관리규정_20241223` | 별표1 | 970자 | 0 | 진입점 50청크 전건 후보적격 |

  **부모 문서 둘 다 판정 인덱스의 진입점이다.** 즉 그 별표만 청킹하면 검색으로 도달한다 —
  **룰 테이블로 빼든 별표를 청킹하든 둘 중 하나만 해도 풀린다.** 설계(`CLAUDE.md`)가
  정한 쪽이 룰 테이블일 뿐이다. 내가 "청킹으로 불가" 로 단정한 것은
  **정답근거에 적힌 doc 하나만 보고 같은 규범이 다른 문서에도 실려 있을 가능성을
  안 본 것**이다. 근거가 하나라고 가정하면 안 된다.

  (전 코퍼스에서 '노래방' 을 언급한 **청크**는 여전히 1건뿐이고 TIPS 붙임3·`폐포전용`
  이라 진입점 검색에는 안 걸린다 — 그건 맞았다. 틀린 건 "그러므로 청킹으로는 못 푼다" 쪽이다.)

즉 **"청킹 결손이 상한을 3.9%p 깎는다" 는 내 요약은 과장이었다. 청킹 몫은 2문항·2.6%p 다.**
남은 1문항은 룰 데이터 결손이라 B·G 쪽 일이다. C 가 A 에게 라우팅을 요청했다.

내용이 있는데 묻힌 별표는 따로 있다 (C 실측): `보조사업실적보고서작성지침 별표1`
(보조비목·보조세목별 산정기준 35,683자) · `보조금관리법시행령 별표1·2`. 이건 아깝다.
청킹은 C·A 소유라 손대지 않았다. `stage2_chunk.py` 재실행은 계약이 금지한다(임베딩 20,525건 소실).

🔴 **③ `대상ID` 같은 대문자 식별자를 쓰지 마라.** psql 은 따옴표 없는 식별자의 ASCII 대문자를
접는다. `CREATE TABLE ... 대상ID TEXT` 로 만들면 실제 컬럼명은 `대상id` 가 되고,
제약에서 `"대상ID"` 로 참조하면 **그 자리에서 깨진다**(실제로 깨졌다).
프로젝트 관례(`기관id`·`doc_id`)대로 소문자 `id` 로 통일했다.

⚠️ **④ 드라이런의 `근거적중률 0%` 를 검색 실패로 읽지 마라.** top5 를 한 건도 못 받은 실행에서
0% 는 "다 틀렸다"가 아니라 **미측정**이다. `eval_e2e.py` 가 이걸 `미측정` 으로 표시하게 해뒀다.

⚠️ **⑤ psycopg 는 `%s IS NULL` 의 타입을 못 정한다.** `평가대상()` 의 세트 필터에서
`IndeterminateDatatype` 로 터졌다. `%s::text IS NULL` 로 캐스팅해야 한다.

### 가정

- **`적용범위` 는 두 값(`공통(지침 제14차)`·`공통`)을 그대로 둔다.** 같은 뜻이지만 정규화하면
  판본 정보가 사라진다. 코드는 값 비교 대신 `IS NOT NULL` 로 판별한다는 전제로 짰다.
- **`programs` 의 `별칭`** 은 실측 재료(폴더명·`golden_set.사업명`·`rules.사업명`·`chunks.사업명`)에서만
  가져왔다. 흔히 쓰이지만 코퍼스에 근거가 없는 축약은 넣지 않았다.
- **`item_vocab` 에 TIPS(RND 계통) 26종을 넣지 않았다.** G2 의 "매핑표 vs 별도 enum" 설계 결정 대기.
  `계통` 컬럼('창업'|'RND')을 미리 뒀으므로 결정되면 **INSERT 만 하면 된다 — DDL 변경 불필요.**

### 🔴 현물 제거로 잃는 것 — 손실 1건 (등록)

**자기부담금 구성비율 검증이 불가능해졌다.** 사업별로 "자기부담 현금 5% 이상 / 현물 20% 이하"
같은 구성비율 하한이 있는데(재도전성공패키지 2026년판 제15조① 표-1 등), `f_profile.자기부담_현물`
이 사라져 "자기부담 현금 비중이 규정 하한에 미달" 을 잡아낼 근거가 없다.

- 오늘 영향 **0** — 해당 골든셋 문항이 0건이다.
- 되살리는 비용 **1컬럼** (`f_profile` 에 현물 총액. `f_exec.형태`(집행 형태)까지는 불필요).
- A 의 판단도 "오늘은 그대로 간다" 로 일치. 내일 재검토 대상.
- ⚠️ **조문 원문은 고치지 않았다.** 우리가 지운 건 `f_profile` 컬럼이지 규정이 아니다
  (계약 §5 예외 3번 — 원문은 인용 검증의 대조 기준).

### 남은 것 — 이어받는 사람이 볼 것

**완료**
- D1-a~f · D2 · D3 · D4 · D5 · **D6** (`04_agent.sql` 머지 + `_DDL_READY` 마커 + 7세션 브로드캐스트)
- **스키마 12항목 전건 재검증 통과** — A 가 락 교착을 풀며 내 `ALTER` 를 `pg_cancel_backend`
  한 뒤, 전체 재실행 대신 **결과를 직접 조회해** 확인했다. 재실행하면 세 스키마를 다시
  줄줄이 잠가 방금 푼 교착을 내가 다시 만든다. 멱등성은 이미 2회 무오류로 확인했으니
  지금 필요한 건 "결과가 맞는가" 뿐이다. 취소로 잃은 것은 없었다.
- **음성 대조** — `강등코드` CHECK 가 실제로 막는지 재봤다. 매핑표 밖 `NOT_A_REAL_CODE`
  INSERT → 거부 ✔ / 정상 코드 2종 → 통과 ✔. **게이트가 통과만 하고 아무것도 안 잡는
  상태와 구별해야 믿을 수 있다** (E 세션의 방식을 가져왔다)
- **D6 현물 제거 문서 전파 5곳** — `RAG.md`(DDL 사본 3군데) · `서비스 아키텍쳐.md`(4분면 →
  재원 2갈래) · `프론트 연동 사양.md`(요청 스키마 F1 2필드·F3 `형태`) ·
  `백엔드_프론트비교_0830.md`(화면 항목) · `프론트 프로토타입 검토.md`(CSV 헤더).
  전부 **"왜 뺐는지 + 무엇을 잃었는지"를 같이 적었다** — 컬럼만 지우면 내일 누가 되살린다.
- `scripts/pin_golden_chunks.py` — 정답 고정. `--재고정` 으로 다시 박을 수 있다
- `scripts/eval_store.py` — 계약 §4 시그니처대로. 스모크 통과
- `scripts/eval_e2e.py` — 5문항 드라이런 통과. 공통/사업지정/L3경로/세트 분해 + 4-way 혼동 +
  경로빈도 + 강등코드빈도 출력. 치명 오답 1건이면 **exit 2**

- `scripts/sweep_retrieval.py` (D7) — 180조합 완주. `eval.runs` 전건 기록
- `scripts/eval_e2e.py --dry` 74문항 전건 완주 (run_id=182)
- `scripts/audit_db.py` — 신설 표 3개·컬럼 12개의 **결손 사유를 등록**했다.
  🔴 사유를 안 적고 통과시키는 대신 "왜 비어도 되는가"를 한 줄씩 적었다.
  특히 `programs.정부지원상한` 은 **"코퍼스에 확정 근거 없음. 추측 금지"** 로 남겼다 —
  나중에 누가 상식으로 채우지 못하게. `golden_set.비목`·`입력필드` 는 골든셋 미기입인데
  계약 §10 이 정답지 수정을 금지하므로 🔴 표시만 하고 안 채웠다 (비목별 분해가 불가능해진다)

- **회귀 3종 전건 통과** (합류점 1 이후 재실행). `eval_retrieval.py` 는 기준값 52.9% 유지
- **합류점 1 이후 드라이런 재실행** (run_id=186) — B·C·E·F 실물 모듈(rules 72 · item_alias 228 ·
  check_items 49 · case_chunks 193 · l3_articles 17)이 붙은 상태에서도 **74/74 완주 · 경로 1종 ·
  지표 동일**(55.4% / 공통 96.2% / 사업지정 33.3%). 모듈 교체가 검색 측 숫자를 흔들지 않았다 —
  드라이런은 LLM 앞에서 멈추므로 이게 정상이고, 바뀌었다면 그게 이상한 것이다

**남은 것 — D 가 할 수 있는 건 없다. 전부 A 의 GPU 창에 걸린다**
1. **실전 E2E** — A 의 GPU 창에서 `python scripts/eval_e2e.py` (`--dry` 없이).
   드라이런이 두 번 74/74 완주했으니 **배관 문제로 GPU 시간을 태울 일은 없다.**
   결과는 `eval.runs` 종류='e2e' 로 자동 적재된다. 치명 오답 1건이면 **exit 2** 로 머지를 막는다
2. **실전 후 `audit_db.py` 재확인** — 위 (나) 3컬럼(`비목`·`신뢰등급`·`버전스탬프`)이
   실전에서도 전부 NULL 이면 사유가 등록돼 있어도 **버그다.** 통과로 읽지 말 것
3. 🔴 **부속물 청킹** (58·59) 과 **`rules.금지예시` 유흥·레저업종 행**(76) — C·A / B·G 영역

**넘긴 것 (내 소유 아님 — 보고만)**
- 🔴 **부속물 청킹 결손** (청킹된 문서 안 2,965건) → C·A. 골든셋 **2문항**(58·59)이 여기서 못 맞는다(상한 -2.6%p).
  조문 결손은 0 이다 — 내 첫 보고(888건)는 문서 단위 미인덱싱을 청킹 손실로 오독한 것이다.
- 🔴 **`rules.금지예시` 에 유흥·레저업종 행이 0건** → B·G. gold 76 의 지정 해법이다.
  (별표 2건을 청킹해도 풀리지만 설계가 정한 쪽은 룰 테이블이다)
- `eval_retrieval.py` 의 정답 역추적을 `eval.golden_chunks` 로 갈아끼우면 지표가 결정적이 된다 → C.
- `rule_base.md` §3-1 재검토 → RLS 없는 `corpus.rules` 에 기관별 L3 오버레이를 담는 설계는
  `TENANT_LEAK` 을 구조적으로 못 막고 G 의 TRUNCATE 가 남의 기관 L3 를 날린다 (E 세션 지적, 동의).

**정정한 것 (내가 처음 틀리게 보고한 것 — C 가 잡아줬다)**
- "조문 888건 청킹 결손" → **조문 결손은 0.** 문서 단위 미인덱싱을 per-조 청킹 손실로 오독했다.
  `article_id` 에 청크가 없다는 사실만으로 원인을 단정한 게 잘못이다 — 문서가 인덱싱 대상인지를
  같이 봐야 한다. 결손은 부속물에만 몰려 있다(청킹된 문서 안 2,965건)
- "골든셋 3문항이 청킹 결손으로 못 맞는다(-3.9%p)" → **설계상 청킹 몫은 2문항(-2.6%p).**
  나머지 1문항(gold 76)의 지정 해법은 룰 테이블이다. 🔴 다만 "재청킹으로 안 풀린다" 고
  쓴 것은 틀렸다 — 같은 목록이 다른 L1 규정에도 별표1 로 있고 그 부모 문서가 진입점이라
  청킹해도 풀린다. **정답근거에 적힌 doc 하나만 보고 판단한 것이 원인이다**
- "BM25 는 hit@5 에 0 을 보탠다" → **사업필터를 켰을 때만** 그렇다. 필터 off 에서는 +4문항이다
- "gold 76 은 재청킹으로 안 풀린다" → **청킹으로도 풀린다.** 같은 목록이 다른 L1 규정에도
  별표1 로 있고 그 부모 문서가 진입점이다. 정답근거에 적힌 doc 하나만 보고 단정한 것이 원인

🔴 **오늘 밤 내가 틀린 4건의 공통 원인은 하나다 — "한 좌표만 보고 원인을 확정" 했다.**
article 에 청크가 없다 → 청킹 탓 (문서 인덱싱 여부를 안 봄) /
그 doc 의 별표가 없다 → 검색 불가 (같은 규범의 다른 출처를 안 봄) /
BM25 기여 0 (필터 조건을 안 붙임) / `조번호` 가 같은 이름이니 같은 좌표계 (실제 값을 안 봄).
**측정값 하나로 원인을 말하기 전에 그 값이 성립하는 조건을 먼저 세어야 한다.**

**건드린 파일**
`db/init/04_agent.sql`(신규) · `scripts/eval_store.py`(신규) · `scripts/eval_e2e.py`(신규) ·
`scripts/pin_golden_chunks.py`(신규) · `scripts/sweep_retrieval.py`(신규) ·
`scripts/_work/_sessions/D.txt` · `scripts/_work/_sessions/_DDL_READY`
**남의 파일은 한 줄도 고치지 않았다.**

---

## E — L3 픽스처 · 체크리스트 · 증빙

**상태: 완료** (E1~E6 전건 · 회귀 3종 통과). 이어서 할 일은 맨 아래 「남은 것」.

> **2026-09-01 02:40 재개분** — 토큰 한도 리셋 후 「남은 것」을 다시 훑었다. 그중 남이
> 막고 있던 2건이 풀려서 처리했고, 나머지는 여전히 오너 판단이거나 오늘 범위 밖이다.
>
> | 풀린 것 | 처리 |
> |---|---|
> | G 가 재도전성공패키지 9행 적재 | 사업별 항목 3행 추가 → `check_items` 49 → **52행**. 8사업 중 **7사업**이 사업별 항목을 갖는다 (TIPS 만 룰이 없어 공통만) |
> | A 가 `audit_db.py` 사유 2건 반영 | 재실행 결과 **✅ 감사 통과 — 미등록 결손 없음** (재개 전 11건 → 0건) |
>
> 재개 중 스스로 찾아 고친 것 1건: **`verified` 세탁** — 아래 「함정 9」.

### 실측 숫자

| 무엇 | 값 |
|---|---|
| L3 픽스처 | 기관 2 · 문서 2 · 조문 17 (기관A 11 · 기관B 6) · 전건 `출처='테스트픽스처'` |
| 장 필터 | 기관A 8/11조 · 기관B 5/6조 로드 (인사·복무·시설·안전 4조 제외) |
| 게이팅 4갈래 | 9개 조합 전부 기대값 일치 — (1)미규정 2 · (2)참조만 2 · (3)불가/조건부 3 · (4)가능 2 |
| 교차조회 | **0건** (누수탐지기 6쌍 + `로드()` 결과 대조 2방향) |
| 상위참조 | 해소 2건(지침 제38·44조) · dangling 1건 (의도적으로 심음) |
| 미분류 조문 | 0건 (추출기가 포기한 조문 없음) |
| `check_items` | 39행 → **52행** (결제전 43 · 결제후 9). 공통 38 · **사업별 14** (7사업) |
| 근거 dangling | 0 · `item_vocab` 밖 비목 0 |
| `rules.증빙` → `evidence_sources` | **70/71 해소** (미해소 1종). G 의 신규 18행 뒤에도 불변 |
| 회귀 3종 | `score_judgment` 치명 0 · 일치 84.7% (불변) · `audit_db` **✅ 감사 통과(결손 0)** · `eval_retrieval` RRF hit@5 52.9% (불변) |

`org_id` 는 uuid5 고정이라 재적재해도 안 바뀐다 — 기관A `1d6be2e1-7296-5492-a24b-c0838b431a7f`.

### 왜 그렇게 했는가 — 판단 4건

**1. L3 룰을 `corpus.rules` 가 아니라 `tenant.l3_articles` 에서 뽑는다.**
`Agent.md` §3-2 · `rule_base.md` §3-1 의 의사코드는 `rules WHERE layer='L3' AND 기관ID=?` 다.
그 경로를 오늘 쓰지 않은 이유가 둘이고 **둘 다 실측**이다.
(a) `corpus.rules` 는 `relrowsecurity=false` — RLS 가 아예 없다. 기관별 데이터를 RLS 없는
공용 테이블에 두면 `TENANT_LEAK` 을 구조로 막을 수단이 없다.
(b) `seed_rules.py` 는 `TRUNCATE rules` 로 재적재한다. 합류점 1 에서 G 가 돌리는 순간
남의 기관 L3 가 통째로 날아간다.
→ **`rule_base.md` §3-1 의 저장 위치는 재검토 대상이다.** D 도 같은 결론에 도달했고
자기 섹션에 올렸다. 오늘 고칠 일은 아니다 (설계 변경이지 배관이 아니다).

**2. 조문 → 룰은 파서가 아니라 분류기로 짰고, 분류 순서가 곧 안전 순서다.**
`참조만 → 불가 → 조건부 → 가능 → (포기)`. '가능' 이 마지막이고, 어느 패턴에도 안 걸리면
`None`(= 미규정) 을 돌린다. 어떤 조문도 '가능' 으로 **떨어지지** 않는다 —
분류 실패가 '가능' 으로 흐르면 근거 없는 "가능" 이 생기고, 그게 이 프로젝트가 가장 피하는 오답이다.
`None` 은 `need_upper=True` 로 이어져 상위를 보게 되므로 안전한 실패다.
포기한 조문은 무음으로 버리지 않고 `l3_load.미분류()` 로 드러난다 (현재 0건).

**3. 비목 매칭은 조 제목 패스를 먼저 돌린다.**
본문 적중만 쓰면 `제21조(자산의 등록 및 관리) … 취득가액 500만원을 초과하는 **기계장치**는…`
이 기계장치 비목 룰로 잡히고, 조번호가 앞서서 진짜 제12조(기계장치)를 가린다.
픽스처에 이 함정을 일부러 넣어두었다 — 제목 패스를 지우면 검증이 즉시 깨진다.

**4. `check_items` 는 "근거가 L1 이냐 L2 이냐" 로 갈랐다.**
"사업별로 갈라라" 를 사업 8개에 기계적으로 곱하지 않았다. 판정 규칙 하나로 정리했다 —
**항목이 주장하는 것은 그 항목이 인용한 조문에 실제로 있어야 한다.**
근거가 L1 통합관리지침이면 8사업에 위임되므로 `사업명=NULL` 이 맞고(38행),
근거가 L2 세부관리기준이면 사업마다 값이 달라 `사업명` 이 필수다(11행).

### 🔴 가장 큰 발견 — "근거 없는 항목" 은 멘토링 하나가 아니었다

착수 지시는 `멘토링한도확인` 1건을 지목했는데, 게이트를 짜서 39행 전부를 인용 조문 본문과
대조하니 **같은 유형이 3건**이었다. 전부 "인용은 달려 있는데 그 조문에 그 내용이 없는" 형태다.

| 항목 | 주장 | 인용 조문 실제 | 판정 |
|---|---|---|---|
| `멘토링한도확인` | "1인 1일 30만원 이내" | L1 제42조에 **금액이 없다.** 30만원은 L2 값 | 사업별 6행으로 분리 |
| `외주특수관계확인` | "2년 이내 재직 이력" | L1 제38조②4 는 "사업 참여 전 재직" — **기간 제한 없음** | "2년" 삭제, L1 문언으로 |
| `친족아님확인` | "사업별로 형제·자매까지 넓어지기도" | L1 제41조⑥은 배우자·직계존비속뿐 | 초격차만 별도 행으로 |

🔴 **`외주특수관계확인` 은 방향이 반대라 더 나쁘다.** "2년 이내면 안 된다" 는
"3년 전 재직이면 괜찮다" 로 읽힌다. L1 은 기간 제한이 없으므로 이 문구는 **국가 지침보다
관대**했다. 오답 비대칭상 가장 피해야 할 종류다.

멘토링 한도의 사업별 실측: 예비·초기·도약·중심대학·모두의창업 = 300,000 / **초격차 = NULL** /
재도전·TIPS = `rules` 행 자체가 없음. 초격차 행은 **숫자를 안 썼다** — 쓸 숫자가 없어서가
아니라 쓸 **근거**가 없어서다. 재도전·TIPS 는 아예 행을 만들지 않았다(아래 가정 참조).

### 세운 가정 · 지시와 다르게 한 것

**(가) E6 의 전제가 낡았다 — 26종이 아니라 1종이다.**
"증빙 별칭 26종(표기차 14 / 복합문자열 12)" 을 받았으나 실측은 **71종 중 70종 완전일치**다.
26 은 `02_frontend.sql` 주석이 쓰인 시점(rules 19행 · 증빙 49종)의 숫자이고, 그 주석이
처방한 작업("복합 문자열을 낱개로 쪼개 재적재")은 `seed_rules.py` 개편(19→54행 ·
증빙 49→71종)에서 **이미 끝나 있었다.**
→ 별칭 테이블을 만들지 않았다. 만들면 26개의 빈 행이 된다. 대신 **게이트로 남겼다**
(`seed_check_items.py` [게이트 e]) — 다음에 해소 안 되는 증빙명이 들어오면 조용히
화면 6 이 뚫리는 대신 여기서 걸린다.
남은 1종: `'교육 이수 계획서(교육일정 포함)'` ← 창업중심대학/교육훈련비.
`evidence_sources` 에 행을 더하면 되는데 그 테이블은 "CSV 124행 그대로"가 불변식이라
**오너 판단이 필요하다.** 형제 행 4개(교육참가 신청서·재료활용계획서·기계장치활용계획서·
활용계획서)가 전부 발급처 "각 사업 공고문 첨부서식" 이라 값 자체는 거의 확실하지만,
불변식을 깨는 판단은 내 몫이 아니다.

**(나) 재도전성공패키지·TIPS 의 사업별 체크 항목을 만들지 않았다.**
`corpus.rules` 에 그 두 사업 행이 하나도 없다. 근거 없이 항목을 띄우는 게 지금 고치고 있는
바로 그 문제라, 추측으로 채우는 대신 **행을 만들지 않았다.** 두 사업 사용자는 공통(L1)
38행만 본다 — 이건 결손이지만 **틀린 항목보다 낫다.**
G 가 재도전 9행을 넣으면 `사업별항목` 표에 사업명만 추가하면 된다 — 근거를 `rules.근거` 에서
끌어오도록 짜놨기 때문에 손으로 옮겨 적을 게 없다.

**(다) L3 파서(HWPX/HWP)를 짓지 않았다** — 계약서 §2 확정사항 그대로. 파서가 들어오면
`l3_load.py` 는 그대로 두고 `l3_articles` 를 채우는 쪽만 바뀐다.

### 🔴 함정 — 나중에 고칠 때 알아야 할 것

**1. RLS 는 켜져 있지만 지금 접속에서는 한 줄도 적용되지 않는다.**
`l3_articles` 는 `relrowsecurity=true` 인데 `relforcerowsecurity=false` 이고 앱이
**테이블 소유자(`postgres`)** 로 붙는다 → Postgres 는 소유자에게 RLS 를 적용하지 않는다.
저권한 롤을 임시로 만들어 재보니 **정책 자체는 옳다** (타 기관 0행, `WHERE` 없이 조회해도
자기 11행만). 즉 **정책이 틀린 게 아니라 롤이 틀렸다.**
지금 살아 있는 방어는 (a) `corpus.chunks` 와 물리적으로 다른 테이블 (b) `l3_load` 의
`WHERE org_id` (c) A 의 검증층 4차 방어(인용의 `org_id` 가 현재기관이 아니면 폐기 +
`tenant.incidents` 에 `TENANT_LEAK`). **3차(RLS)만 꺼져 있다.**
고치려면 별도 저권한 앱 롤이 필요하고 그건 배관이 아니라 인프라라 오늘 범위 밖으로 뒀다.
`seed_l3_fixture.py --verify` [5] 가 이 상태를 매번 실측해 출력한다 — 롤이 바뀌는 날
자동으로 ✔ 로 뒤집힌다.

**2. `index_guard` 가 L3 를 막지 않고 있었다 (A 가 고침).**
`BLOCKED_LAYERS` 가 `{L4, L5}` 뿐이라, 어떤 인제스천 경로가 `layer='L3'` 로 태깅해
`stage0_ingest` 를 태우면 `index_target=True` 로 통과했다.
`CLAUDE.md` 의 *"L3 는 다른 테이블이라 누수가 구조적으로 불가능하다"* 는
**L3 가 `chunks` 에 절대 안 들어갈 때만** 참이다. 현재 chunks 는 L1 20,030 · L2 495 로
깨끗해서 **지표에 안 나타나는 무음 구멍**이었다 — 픽스처를 만든 진짜 이유가 이런 것이다.

**3. `기본_오프셋일` 은 이제 부호 있는 오프셋이다. 뜻이 바뀌었다.**

```
due_date = 집행예정일 + 기본_오프셋일       # 분기 없는 단일 공식
  결제전 → 음수 또는 0    ·    결제후 → 양수 또는 0    ·    NULL → 기한 없음
```

`자산등록 30` 은 지침 제39조 "취득일부터 1개월 이내" 를 옮긴 값인데 옛 규약("며칠 전")으로는
**물건 사기 한 달 전에 자산등록을 하라**는 일정이 됐다. 숫자가 틀린 게 아니라 부호가 없었다.
주석만 고치면 다음 사람이 또 같은 부호로 넣으므로 **공식을 하나로 만들고**
`seed_check_items.due_date계산()` 을 내보냈다 — 프론트·판정이 같은 함수를 써야 한다.
`구분` 을 보고 분기하지 마라. 분기가 두 군데 생기면 반드시 어긋난다.
**규약은 이제 3겹으로 못이 박혔다** — 적재 게이트(코드) · `ck_check_items_오프셋부호`(DB) ·
컬럼 COMMENT(문서). D 가 CHECK 와 주석을 반영했고, 위반 UPDATE 를 양방향으로 밀어넣어
DB 가 실제로 거부하는 것까지 확인했다 (결제후 −30 · 결제전 +7 둘 다 거부).
현재 분포는 결제전 40행 −14~0 · 결제후 9행 0~30 이다.

**4. 비목 어휘를 DB 에서만 읽으면 안 된다.**
`l3_load.비목어휘()` 는 `corpus.item_vocab` 을 읽되 **폴백 상수 10종**을 들고 있다.
G 의 `TRUNCATE rules` 재적재처럼 참조 테이블이 잠깐 비는 순간이 있고, 그때 비목이 0종이면
L3 룰이 **전건 무음 `None`** 이 된다 — 판정은 계속 돌지만 L3 가 통째로 사라진 걸 아무도 모른다.

**5. 상위참조 해소는 `doc_id` 의 밑줄 때문에 한 번 전건 실패했다.**
인용 표기는 `「중소기업창업 지원사업 통합관리지침」`(공백)인데 `doc_id` 는
`L1_중소기업창업_지원사업_통합관리지침_…`(밑줄)이다. 공백만 지우고 맞추면 하나도 안 붙고,
`seed_refs` 가 통째로 비어 게이팅 (2)가 상위를 못 짚는다 — **무음 실패라 지표에도 안 잡힌다.**
양쪽에서 `' _·-()'` 를 전부 지우고 견준다.

**6. 픽스처를 지울 때 삭제 범위를 반드시 `출처='테스트픽스처'` 로 좁혀야 한다.**
`seed_l3_fixture.py` 재적재는 문서를 지우고 다시 넣는데, 같은 기관에 실제 규정이 올라와
있으면 org 단위로 지울 경우 **사용자 문서를 지운다.** 라벨로 좁혀 뒀다.

**7. 🔴 잘못된 `org_id` 는 예외가 아니라 판정 전체를 죽인다 (고침 · 검증 [9] 로 못 박음).**
DONE 이후 게스트 경로를 재보다 찾았다. `org_id` 는 `WHERE org_id = %s` 로 UUID 컬럼에
바로 들어가는데 `'guest'` 같은 값이 오면 Postgres 가 `invalid input syntax for type uuid`
를 던지고 **그 트랜잭션이 통째로 abort 된다.** 그러면 A 의 오케스트레이터가 같은 커서로
하는 이후 조회가 전부 `current transaction is aborted` 로 죽는다 —
**L3 하나 없는 것이 검색·룰·인용 검증까지 끌고 내려가는 연쇄 실패**가 된다.
골든셋 77문항에는 `org_id` 가 없으므로 E2E 가 이 경로를 그대로 탄다.
`_org정규화()` 로 4개 진입점(`로드`·`l3룰`·`문서요약`·`미분류`)을 전부 막았다 —
UUID 가 아니면 게스트(L3 없음 → `need_upper=True`)로 닫고 stderr 에 경고를 남긴다.
계약 §2-3 "모든 실패의 기본값은 판단불가" 를 이 경로에 옮긴 것이다.
🔴 검증은 **반환값만 보면 안 된다.** 예외를 삼키기만 하고 트랜잭션이 죽어 있으면
반환값은 멀쩡해 보인다 — 그래서 [9] 는 매 케이스마다 `SELECT 1` 로 **커서 생존**을 같이 잰다.

**8. 🔴 파생 항목의 `verified` 는 원본 룰에서 **상속**해야 한다 (재개분에서 고침).**
사업별 `check_items` 는 `corpus.rules` 에서 근거를 끌어오는데, 처음엔 전건
`verified=true · 검수자='Claude (오너 위임)'` 로 박고 있었다.
G 가 넣은 재도전 9행은 `verified=false · 검수자='G세션-4단검증'` 이다 — G5 4단검증은 거쳤지만
**오너의 D단계 검수 전**이다 (`rule_base.md` §6). 그대로 두면 아직 사람이 안 본 값이
"검수된 체크리스트" 로 **세탁**된다.
→ `행만들기()` 가 `verified`·`검수자`·`검수일` 을 원본 행에서 그대로 물려받게 고쳤다.
**파생물의 신뢰도는 원본을 넘을 수 없다.** 현재 52행 중 3행이 `verified=false`
(전부 재도전, 검수자 `'G세션-4단검증 (룰 상속)'`) 라 감사 추적이 G 의 행까지 이어진다.
G 룰이 오너 검수를 통과하는 날 재적재만 하면 자동으로 `true` 가 된다.

**9. (하네스) Bash 툴에서 하위 디렉터리로 `cd` 하지 마라.**
`.claude/hooks/*` 를 **상대경로**로 부르기 때문에, `cd scripts/_work/_sessions` 한 뒤로는
모든 Bash 호출이 훅 로딩 실패로 막힌다 — 그 셸에서 `cd` 로 되돌아오는 것조차 막혀 교착이다.
PowerShell 툴이 같은 cwd 를 공유하므로 `Set-Location` 으로 풀었다. 항상 절대경로를 쓸 것.

### 남은 것 (다음 세션이 이어받을 것)

| 무엇 | 왜 오늘 안 했나 |
|---|---|
| L3 HWPX/HWP 파서 | 계약서 §2 확정. 골든셋 L3 근거 0건이라 지표에 안 잡힌다 |
| 저권한 앱 롤 (RLS 3차 방어 복구) | 인프라 변경. 배관 검증 범위 밖 — 위 함정 1 |
| `rule_base.md` §3-1 L3 오버레이 저장 위치 재검토 | 설계 변경. `corpus.rules` 는 RLS 도 없고 TRUNCATE 대상이다 |
| `'교육 이수 계획서(교육일정 포함)'` 발급처 | `evidence_sources` "CSV 124행 그대로" 불변식을 깨는 판단 — 오너 몫 |
| ~~재도전 사업별 항목~~ | ✔ **완료** — G 9행 적재 후 3행 추가. **TIPS 만 남았다** (`rules` 0행) |
| ~~`check_items` 오프셋 DB CHECK + 컬럼 주석~~ | ✔ **완료** — D 반영. 위반 UPDATE 양방향 거부 확인 |
| ~~`audit_db.py` 사유 2건 정정~~ | ✔ **완료** — A 반영. 재실행 시 감사 통과 |
| 초격차 제18조 (표-2) 멘토링 수당 기준 | 아래 참조 |

🔴 **오너 확인 요청 1건** — 초격차 세부관리기준 제18조에 수당 표가 있다:
`1시간당 200,000원 이하 / 일 상한 1,000,000원 이하`.
그런데 이건 문맥상 **초격차위원단**(심사·평가·멘토링 제공자)에게 전문기관이 주는 수당이지,
창업기업이 사업비로 지출하는 지급수수료 한도와 같은 것인지가 불분명하다.
`corpus.rules` 의 초격차/지급수수료 행은 `한도_값=NULL` 이고 `verified=true`(오너 위임 검수)라
**임의로 뒤집지 않았다.** 같은 것이면 초격차에도 한도가 생기고 `멘토링요건_초격차` 항목이
`멘토링한도_초격차` 로 바뀐다. 판단은 오너/G 몫이다.

### 산출물 (건드린 파일 전부)

| 파일 | 내용 |
|---|---|
| `scripts/l3_load.py` (신규) | `로드()` `l3룰()` + 장 필터 · 비목 추정 · 조문→룰 분류기 · 상위참조 해소 · 진단(`미분류`) |
| `scripts/seed_l3_fixture.py` (신규) | 픽스처 2기관 17조 + **검증 9종**(`--verify`) · `--drop` |
| `scripts/seed_check_items.py` (개정) | 49행 · `due_date계산()` · 게이트 5종(dangling·부호·수치접지·어휘·증빙) |
| `scripts/_work/_sessions/E.txt` | 세션 이름 등록 (`ai-55`) |

DB 쓰기: `tenant.orgs` 2행 · `tenant.l3_documents` 2행 · `tenant.l3_articles` 17행 ·
`corpus.check_items` 49행. 그 밖의 테이블은 읽기만 했다.
남의 소유 파일은 하나도 안 고쳤다 — `index_guard.py`(A) · `audit_db.py`(A) ·
`04_agent.sql`(D) 는 전부 `BLOCKED` 로 넘겼다.

`seed_l3_fixture.py --verify` 는 실패 시 비0 종료한다. **회귀에 넣어도 된다** —
게이팅 4갈래·교차조회·`index_guard`·상위참조를 한 번에 태우고 30초 안에 끝난다.

가장 재사용 가치가 큰 건 `seed_check_items.py` 의 **[게이트 c] 수치 접지**다.
항목이 말하는 모든 금액·기간·비율이 인용 조문 본문에 실제로 있는지 대조한다
("30만원" ↔ "300,000원" 표기차를 넘도록 원 단위로 환산해 견준다).
개정 전 3행을 음성 대조군으로 넣어 **실제로 잡는지 확인했다** — 게이트가 통과만 하고
아무것도 안 잡는 상태와 구별해야 신뢰할 수 있다.


---

## F — 판단불가 경로 · 사례

**상태: F1~F7 완료 (F6 은 프롬프트까지 — 실호출 검증은 A GPU 창) · 회귀 3종 통과**

### 실측

| 항목 | 값 |
|---|---|
| 사례집 후보 문서 | 11종 (DB `corpus.documents` 등재분 전량) |
| **채택** | **4종 / 193건** (`corpus.case_chunks` 적재 완료 · embedding NULL 0) |
| 오염으로 제외 | 1종 (부정행위 사례집 42사례) |
| 품질로 제외 | 6종 |
| 채택분 내역 | 경인교대 FAQ 76 · 한국연구재단 Q&A 83 · 미래부 상담사례 33 · 행정심판 재결 1 |
| 임베딩 | CPU KURE-v1 193건 (**GPU 미개방**) |

**오염 검사 3층 측정** (전문 `scripts/_work/_F_오염검사.json`):

| 문서 | 어휘 6-gram | 의미 코사인 | 판정 |
|---|---|---|---|
| **창업사업화 부정행위 방지 사례집** | **0.248** | **0.743** | 🔴 **오염(출처)** |
| 미래부 상담사례집 | 0.090 | 0.683 | 채택 |
| 한국연구재단 Q&A | 0.061 | 0.681 | 채택 |
| 경인교대 FAQ | 0.058 | 0.667 | 채택 |
| 권익위재결례집 1·2 | 0.093 / 0.103 | 0.585 / 0.621 | 품질 제외 |
| 나머지 4종 | 0.055~0.118 | 0.535~0.573 | 품질 제외 |

> 🔴 **임계(어휘 0.35 · 의미 0.80) 를 넘은 문서는 하나도 없다. 오염 문서도 안 넘었다.**
> 잡은 것은 **출처 매핑 단 하나**다. 다만 의미 점수의 **순위**는 오염 문서를 11종 중
> 1위로 올려놨다(0.743 vs 차순위 0.683) — 수치가 신호를 주긴 하나 **사전에 고른
> 절대 임계로는 못 걸렀다.** 사후에 임계를 0.72 로 낮추는 것은 11개 점에 맞추는
> 과적합이라 하지 않았다.

**사후 확인 — 적재분이 골든셋 정답을 노출하는가** (77문항 × 193사례 top-1 코사인):

```
본세트 50   최소 0.446 · 중앙 0.555 · 최대 0.682
적대적 27   최소 0.490 · 중앙 0.565 · 최대 0.672
```

**77문항 전부 0.70 아래.** 판단불가 경로가 정답을 그대로 보여주는 일은 없다.

**회귀 3종**

| 회귀 | 결과 |
|---|---|
| `audit_db.py` | ✅ 감사 통과 — 미등록 결손 없음 (재개 후 재실행. 1차 때의 🔴 7건은 A 소유 `tenant.decisions` 였고 A 가 해소했다) |
| `eval_retrieval.py` | **hit@5 52.9% — 한 자리도 안 바뀜** (기준선 유지). dense 47.1 · BM25 40.0 |
| `score_judgment.py` | ✅ 치명 오답 0 · 본세트 89.8% · 적대적 73.9% · exit 0 |
| `case_search.py --all` | ✅ F5 게이트 5종 + F7 구조 4층 전부 통과 |

### 판단

**F1 — 오염 검사가 이번 밤의 가장 중요한 산출물이다. 🔴 어휘 대조로는 못 잡는다.**

적대적 골든셋 27문항 중 **23문항이 `사례출처` 필드에 「부정행위 사례집」을 쪽수까지
명시**한다 (나머지 4문항 A22·A23·A26·A27 은 규정 원문에서 도출). 그런데 그 문서와
골든셋의 **6-gram 어휘 겹침 최대값은 0.248 로 임계(0.35) 미만**이었다 — 적대적 문항이
사례를 *"무해해 보이는 사전 질문"* 으로 **다시 쓴** 것이라 표현이 안 겹친다.

> **어휘 검사만 돌렸으면 이 문서를 통과시켰다.** 오염은 표현이 아니라 **출처**에 있다.
> 그래서 검사를 3층으로 만들었다: ① 출처 매핑(가장 강함) ② 어휘 6-gram ③ 의미 코사인.

`corpus.case_chunks` 는 판정 인덱스가 아니라 `index_guard` 의 기존 경로 블랙리스트에
안 걸린다. 판단불가 경로는 **"근거를 못 찾았다"고 말한 직후에 사례를 보여주는 자리**라,
거기에 정답 원본이 뜨면 골든셋 측정이 통째로 무의미해진다.

**문서 단위로 통째 제외했다.** 42사례 중 창업기업편 30건만 골든셋 재료이고 주관기관편
12건은 안 쓰였지만, ① 계약서 지시가 문서 단위(*"겹치는 문서는 제외"*)이고 ② 주관기관
제재는 창업기업 사용자와 체계가 다르며(주의→경고→협약해지 축) ③ `extraction='vlm'` ·
`parse_quality='low'` 라 A등급 인용도 금지된 문서다. **주관기관편 12건의 재검토는
"남은 것"에 적었다 — 오늘의 이득이 위험보다 작다.**

**품질 제외 6종의 사유** (전문은 `scripts/_work/_F_오염검사.json` 의 `문서별[].사유`):
- 권익위재결례집 1·2 — 일반 행정심판 재결례집. 79건 중 보조금·연구개발 키워드 적중
  11건, 76건 중 3건뿐이고 그마저 산업집적법 출연금·고용보험료다. 게다가 **사건명이
  목차에만 있고 【재결요지】 블록에 없어** question 을 신뢰성 있게 만들 수 없다
- IITP 가이드라인 · KISTEP 판례분석 · e나라도움 매뉴얼 — Q&A·사례 단위 경계가 없다
  (Q/A 마커 0). 사례 인덱스는 질문↔질문 검색이라 질문 없는 문서는 넣을 자리가 없다
- 🔴 기재부 국고보조금통합관리지침 — **사례집이 아니라 규정 원문**이다(`doc_type='지침'`).
  사례로 넣으면 *"근거가 아닙니다"* 라벨을 단 채 규정이 노출된다. 라벨과 내용이 어긋난다

**F5 — 게이트를 함수 시그니처로 만들었다.** `유사사례(cur, 질문, *, k=3, 판정)` 에서
`판정` 은 **키워드 전용 필수 인자**다. 기본값을 일부러 안 줬다 — 기본값이 있으면
"깜빡하고 안 넘김"이 조용히 통과하지만, 없으면 `TypeError` 로 즉시 터진다.
`판정 != "판단불가"` 는 `RuntimeError`.

**F7 — "이번엔 안 바뀌었다"가 아니라 "바뀔 수 없다"로 짰다.** 파이프라인 2회 실행 비교는
그날의 우연을 잴 뿐이라, 구조 4층으로 본다: ① 소스 분리 양방향(`case_search`↛`chunks`,
`retrieve`↛`case_chunks`, `assemble_context`↛사례) ② 게이트 실행 ③ 읽기전용(조회 후
4개 테이블 행수 불변) ④ 결정성(동일 질문 2회 동일 결과).

**F6 — 슬롯 ⑤ 는 판정하지 않는다.** 검증기가 결론 어휘(`가능합니다`/`불가합니다` 등)와
**자료 밖 숫자**를 코드로 막는다. 실패하면 정적 템플릿으로 떨어져 화면 9가 빈칸이 되지
않는다. 🔴 **수신자를 채우지 않는다** — 담당자는 제3자고 연락처 수집 근거가 설계
어디에도 없다 (`입력_적법성_점검.md` B-11). 본문만 만들고 수신자는 사용자가 채운다.

### 함정

1. **`case_chunks` 는 `index_guard` 의 사각지대다.** 기존 게이트는 경로 블랙리스트와
   `layer='L4'` 만 본다. 사례집은 둘 다 아니라 그냥 통과한다. 새 인덱싱 경로를 만들 때
   *"게이트를 태웠다"*가 곧 *"안전하다"*가 아니다 — 게이트가 **무엇을** 보는지 확인할 것
2. **파싱 품질은 눈으로 두 번 봐야 드러났다.** 1차 추출은 통계상 멀쩡했는데(193건, 길이
   분포 정상) 표본을 뽑아 읽으니 ⓐ 경인교대 답변에 `<span style="color: rgb(…)…>` 가
   통째로 남아 있었고(태그 정규식에 40자 상한을 둔 탓) ⓑ PDF 줄바꿈이 질문을
   `"집행할 수\n있나요?"` 로 쪼개 놓았다. **둘 다 임베딩 품질을 직접 깎는다.**
   태그 상한을 없애는 대신 **태그 이름 화이트리스트**로 바꿨다 — `<[^>]*>` 로 뭉뚱그리면
   규정 표기 `<표-3>`·`<붙임1>` 이 날아간다 (현재 52건 답변에 꺾쇠 표기가 살아 있다)
3. **한국연구재단 Q&A 는 제목만으로 검색이 안 된다.** `Q1. '인건비계상률'은 무엇?` 이
   제목이고 진짜 질문은 그 아래 상세 문장이다. 제목+상세를 합쳐 question 으로 쓴다
4. **소스 스캔 분리 검사는 자기 자신에게 걸린다.** F7 의 ①(`case_search` 가
   `corpus.chunks` 를 읽는가)이 처음에 🔴 로 떴는데, 범인은 **검사기 자신**이었다 —
   ⓐ 금지 패턴을 소스에 리터럴로 적어놨고 ⓑ 모듈 독스트링에
   *"이 모듈은 `corpus.chunks` 를 SELECT 하지 않는다"* 라고 **써 놨다.**
   `#` 주석만 걷어내는 것으로는 부족하다 — 독스트링은 주석이 아니라 식이다.
   **`ast` 로 독스트링만 골라 빼고** 나머지 문자열 상수(=SQL)는 남긴다.
   패턴은 조각으로 조립해 리터럴이 소스에 안 남게 했다.
   🔴 고친 뒤 **음성 대조**를 돌렸다 — 일부러 위반시킨 가짜 소스는 잡고, 독스트링에만
   언급된 가짜 소스는 안 잡는다. *검사를 고쳐서 통과시켰을 때는 그 검사가 아직도
   진짜 위반을 잡는지 반드시 확인한다*
5. **오염 검사의 의미 측정에 40분을 날렸다.** Q&A 구조가 없는 문서(권익위 2권 1,116조각)를
   전량 CPU 임베딩했는데, **그 문서들은 사례 단위가 없어 애초에 노출될 것이 없다** —
   즉 그 수치는 채택 결정에 쓰이지 않는다. 표본 120개로 줄였다. *결정에 쓰이지 않는
   수치는 재지 않는다*가 규칙이다

### 가정

- **사례집 9종의 도메인이 전부 R&D·보조금이다.** 창업 도메인 사례는 부정행위 사례집이
  유일했고 그게 오염이라, 채택분 193건은 **전량 남의 제도 사례**다. `유사사례()` 가
  `라벨="귀하의 사업에 적용되지 않습니다"` 를 상수로 붙이는 이유가 이것이다.
  **이 경로의 가치는 "답을 준다"가 아니라 "문의할 거리를 준다"로 봐야 한다**
- 미래부 사례집(2013)의 근거 법령은 폐지된 「국가연구개발사업의 관리 등에 관한 규정」이다.
  B등급 라벨로 감당하기로 했다 — 사례는 근거가 아니므로 구판 인용이 판정에 새지 않는다
- `최소유사도=0.55` 는 **옮기지 않았다 — 옮길 곳이 없다는 걸 재서 확인했다.**
  아래 §추가측정 참조

### 추가 측정 (재개 후) — 🔴 임계는 고칠 수 있는 문제가 아니다

**① 판단불가 경로 E2E 실통과 확인.** 지금까지의 F 검증은 `유사사례()` 직접 호출이었다.
A 의 `orchestrate.판정()` 을 통해 **게이트 B(검색 스코어 미달)** 로 진짜 판단불가를
내보니 사례 3건이 라벨과 함께 정상 부착됐다 (`호출수=0` — LLM 을 태우지 않는다).

> ⚠️ `dry=True` 의 `게이트=D / dry중단` 경로는 사례를 **안 붙인다.** 처음에 이걸 결손으로
> 의심했는데 **정상이다** — dry 의 판단불가는 "판단해보니 모르겠다"가 아니라 "판단을
> 안 했다"이다. 거기 사례를 붙이면 배관 테스트 결과가 제품 화면처럼 보인다.
> 🔴 **A 의 dry 77문항 결과에는 사례가 0건으로 나온다. 결손이 아니다.**

**② 임계를 양쪽에서 쟀다.** 진짜 질문 77 vs **명백히 범위 밖인 음성 질문 10건**
(고양이 사료 · 로또 · 자녀 학원비 · 오늘 날씨 …):

| 임계 | 진짜 질문 유지 | 음성 오부착 |
|---|---|---|
| 0.50 | 71/77 (92%) | 8/10 (80%) |
| **0.55 (현재)** | **47/77 (61%)** | **3/10 (30%)** |
| 0.60 | 26/77 (34%) | 1/10 (10%) |
| 0.65 | 11/77 (14%) | 0/10 (0%) |
| 0.70 | 0/77 (0%) | 0/10 (0%) |

🔴 **분리되는 지점이 없다.** 진짜 질문과 헛소리가 같은 띠(0.5~0.6)에 겹친다.
당연하다 — 적재분이 **전량 R&D·보조금 도메인**이라 창업 질문도 "잘 안 맞는" 쪽이고
헛소리도 "잘 안 맞는" 쪽이다. **코사인은 이 둘을 가르는 축이 아니다.**

> **그래서 숫자를 옮기지 않았다.** 어느 값으로 옮겨도 한쪽을 버리고 다른 쪽을 얻을 뿐이라
> "개선"이 아니다. 🔴 **고칠 것은 임계가 아니라 상호작용이다** — 자동 노출 대신
> *"비슷한 사례 보기"* 를 사용자가 눌러 열게 하면 오부착의 비용이 사라진다
> (사용자가 이미 "이건 참고용"이라고 알고 누른 것이므로). UI 는 오늘 범위 밖이라
> 코드는 그대로 두고 여기 적는다.

### 🔴 마감 시점 정정 — 이 경로의 실전 유용성은 **아직 안 재졌다**

실전 E2E(A, 커밋 8d9d069)에서 판단불가가 5건 나왔고 사례 경로도 정상 동작했다.
**그런데 그 5건은 전부 `max_tokens=1500` 잘림이었다 — 모델이 스스로 판단불가를
고른 횟수는 0 이다.**

그래서 위의 모든 측정은 **"경로가 도는가"까지만 증명한다.** "판단불가일 때 사례가
쓸모 있는가"는 증명하지 못했다 — 잘림으로 생긴 판단불가는 질문이 어려워서가 아니라
출력이 끊겨서 생긴 것이라, 거기 붙은 사례가 사용자에게 유용했을지와 무관하다.
🔴 **`max_tokens` 를 3000 으로 올려 진짜 판단불가를 만든 뒤에야 이 경로를 평가할 수 있다.**
(A 의 내일 첫 항목. 그게 끝나야 위 ②의 "상호작용을 바꿀지" 도 제대로 판단된다)

### 남은 것

- [ ] 🔴 **`max_tokens` 3000 이후 사례 경로 재평가** — 진짜 판단불가 표본이 있어야
      "사례가 쓸모 있었나"를 처음으로 잰다. 위 정정이 근거. **다른 F 항목의 선행조건**
- [ ] 🔴 **화면 9 상호작용 변경 제안** — 사례를 자동 노출하지 말고 사용자가 여는 방식으로.
      위 ② 가 근거다. 임계 조정으로는 안 풀린다. **UI 결정이라 오너 판단 사항**
- [ ] `최소유사도` 는 상호작용이 정해진 뒤에 다시 본다 (지금 옮기면 한쪽만 버린다)
- [ ] F6 슬롯 ⑤ 실호출 — **잔액 소진으로 미실행** ($8.02 → $1.52, GPU 창 3회 중 2회가
      인프라 실패). 크레딧 충전 후 `draft_inquiry.생성(판정결과, 호출=...)` 한 줄.
      `호출=None` 폴백이 있어 그때까지도 화면 9 는 빈칸이 되지 않는다
- [ ] F6 슬롯 ⑤ 실호출 검증 — **A 의 GPU 창**. `draft_inquiry.생성(결과, 호출=슬롯클라이언트)`
      로 꽂으면 된다. `guided_json` 은 🔴 **최상위 인자** (`extra_body` 는 조용히 버려진다)
- [ ] 부정행위 사례집 **주관기관편 12건**의 별도 적재 재검토 (창업기업편 30건과 분리
      가능한지 · VLM 판독 품질이 받쳐주는지). 오늘은 문서 단위로 뺐다
- [ ] `case_chunks` 를 `index_guard` 가 직접 아는 대상으로 올릴지 — 지금은 `stage2_cases.py`
      가 자체 오염 목록(`_F_오염검사.json`)으로 막는다. 게이트가 둘로 나뉘어 있다

### 건드린 파일

| 파일 | 상태 |
|---|---|
| `scripts/stage2_cases.py` | 신규 — check/extract/embed/load 4단 |
| `scripts/case_search.py` | 신규 — `유사사례()` + F5 게이트 + F7 회귀 |
| `scripts/draft_inquiry.py` | 신규 — 슬롯 ⑤ 프롬프트·검증기·정적 폴백 |
| `scripts/_work/_F_오염검사.json` | 신규 산출 — F1 판단 근거 전문 |
| `scripts/_work/_stage2_cases.jsonl` `.npy` | 신규 산출 — 193건 + 임베딩 |
| `corpus.case_chunks` | 적재 (F 소유 테이블) |

**남의 파일·테이블은 하나도 건드리지 않았다.** `corpus.chunks` 는 읽지도 않았고
`stage2_chunk.py` 는 실행하지 않았다.

---

## G — 룰 데이터 (재도전 · TIPS · L1 분리)

`corpus.rules` **54행 → 72행**. 새 18행 전부 `verified=false` · 검수자 `G세션-4단검증`.
검수 요청서 `scripts/_work/_G_룰검수요청.md` + 조문 원문 대조표 `_G_룰검수요청_대조표.md`(자동 생성).

### 실측 숫자

| | 전 | 후 |
|---|---|---|
| `corpus.rules` | 54행 (전부 `layer='L2'`) | **72행** (L1 9 + L2 63) · dangling 0 |
| 재도전성공패키지 룰 | **0행** | 9행 (근거가 2026년판 33조에 실재) |
| **골든셋 룰 커버 (`rule_lookup --golden`)** | **34건** | **57건** |
| └ 공통(사업명 NULL) 27문항 | **0건** | **23건** |
| └ 사업지정 50문항 | 34건 | 34건 (불변) |
| 골든셋 역검증 치명 오답 | — | **0** (안전측 과잉 1 · 미커버 3) |

**회귀 3종 — 적재 후 전부 통과** (2026-09-01):
`audit_db` **✅ 감사 통과 — 미등록 결손 없음** (`rules` 관련 결손 0)
`eval_retrieval` **hit@5 52.9%** (dense 47.1 / BM25 40.0 / RRF 52.9 · MRR 0.376/0.253/0.363)
— A 의 `documents.status` 플립 후에도 15개 지표 전부 동일. 검색 필터가 `chunks.status` 를
보므로 영향이 없어야 한다고 예측했고 실측으로 확인됐다.
`score_judgment` 치명 **0** · 일치 84.7% · 인용 100% · 판단불가 0% — 불변.
`_G_골든셋_역검증.py` 치명 **0**.

### 넘긴 회귀 1건 → **B 가 고쳤다.** 다만 내 진단은 절반만 맞았다

L1 9행을 넣은 직후 `rule_lookup.py --golden` 의 `적용층` 분포가
**`Counter({'L1': 57})`** 이었다 — 57건 전부 L1, L2 가 한 건도 안 이긴다. 사업지정 문항까지
L1 이 답하고 있었다. B 에게 넘겼고 **B 가 필드 단위 병합으로 고쳤다**:

```
전:  적용층 Counter({'L1': 57})
후:  적용층 Counter({'L1+L2': 34, 'L1': 23})     ← 사업지정 34 + 공통 23. 정확히 맞는 분해
     초격차 지급수수료  금지예시 17건(L1 8 + L2 9) · 증빙 27종 · 적용층 L1+L2
     예비창업 기계장치  한도 {개수 1.0 '대/인(PC·노트북·태블릿)' layer=L2} · 수량3/인원2 → 초과 True
```

결정적이었던 근거는 **L1·L2 가 충돌이 아니라 위임 관계**라는 것이다 — 지침 제36조②가
"제1항에서 명시되지 않은 증빙서류는 전문기관의 장이 별도로 정한 **사업별 세부관리기준에
따라**" 라고 직접 위임한다. L2 는 L1 을 뒤집는 게 아니라 구체화한다. 그래서 층 단위로
행을 고르면 안 되고 필드 단위로 병합해야 한다. `unspecified_only` 도 **허용·우선규범에만**
걸어야 초격차 고유 금지예시가 산다.

**🔴 내가 과장했던 것 2건 — 아침에 이 문서를 근거로 다른 걸 고치지 않도록 정확히 적는다.**

내가 "L2 63행 중 62행이 한도·금지예시를 잃는다" 고 적었는데, **실제로는 잃고 있지 않았다.**
`적용층: L1` 이라는 표기 하나를 보고 «L1 이 L2 를 통째로 대체한다» 고 추론했고, 그 추론 위에서
피해를 계산했다. 추론이 틀렸다:

| 내가 적은 것 | 실제 (B 실측) |
|---|---|
| 한도 통째 소실 10건 | **일어나지 않았다.** 고치기 전에도 `_엄격병합` 이 한도를 `min(비NULL)` 로 병합하고 있었다. 내가 근거로 든 B4문장 `"한도 비교는 아직 못 한다 — 수량이 입력되지 않았다"` 는 한도가 없어서가 아니라 **한도(1대/인)는 있는데 골든 문항에 수량이 안 실려서** 나오는 문장이다. 수량 3·인원 2 를 넣으면 그때도 `초과=True` 가 나왔다 |
| 금지예시 소실 전 행 | **아니다.** `금지적중()` 은 `_엄격병합` 을 아예 거치지 않고 `base_룰()` 전 행을 직접 훑는다(초격차 L1 41 + L2 43 = 84건 전부) |
| 초격차 "크몽·위시켓"·"GPU 다량구매" 가 사라진다 | 둘은 **지급수수료가 아니라 외주용역비·재료비 소속**이다(확인함). 지급수수료 손실 항목에 붙여 쓴 건 내 분류 착오다. 둘 다 살아 있었다 |

즉 **«틀린 가능» 이 실제로 나가고 있진 않았다.** 정지 조건에 걸릴 상태가 아니었다.

**실제로 틀렸던 것은 `적용층` 표기 하나**이고, 그건 사소하지 않다 — 화면 7 이
"이 한도는 국가 지침이 아니라 사업 세부관리기준입니다" 를 그리는 근거라서, 여기가 틀리면
**근거를 인용하는 제품이 근거의 출처를 잘못 댄다.** 고칠 값어치는 충분했고, 덤으로 한도를
폴백 분기로 간신히 살리던 배선도 걷어냈다. 잡은 것 자체는 맞았고 **증상 해석이 한 칸
어긋났다** — 관측(적용층 L1 57)은 실측이었지만 거기서 도출한 피해 규모는 실측이 아니라
추정이었고, 그걸 실측처럼 적었다.

B 가 내 제안에 두 가지를 더 얹었다: **단위가 다른 한도는 min 으로 접지 않는다**
(「1인 1대」와 「월 50만원」은 둘 다 지켜야 하는 별개 제약이라 `한도목록` 에 다 남기고 전수 비교),
그리고 반환에 `필드출처` 를 실어 화면 7 이 `L1+L2` 를 풀 수 있게 했다.

🔴 **데이터로 우회하지 않은 판단은 맞았다.** L1 행을 빼거나 비목을 줄이면 공통 27문항이
도로 죽는다(그게 G3 의 목적이었다). 고칠 곳은 `rule_lookup.effective_rule()` 이었고
G 소유가 아니었다.

### 🔴 아침에 오해하기 쉬운 것 — **골든셋은 오늘 G 가 만든 것의 절반을 검출하지 못한다**

일치율 84.7% 가 어젯밤과 같다. **그건 G 작업이 효과가 없었다는 뜻이 아니라, 골든셋 77문항이
오늘 만든 것 대부분을 태우지 못하는 표본이라는 뜻이다.** 실측으로 갈라 적는다:

| G 산출 | 골든셋이 잴 수 있나 | 근거 |
|---|---|---|
| G3 L1 공통 9행 | ✅ **잰다** | 공통 27문항이 정확히 이 경로다. 룰 커버 0→23 이 그 측정값 |
| G1 재도전 9행 | ❌ **못 잰다** | `golden_set` 에 재도전 문항 **0건**(실측) |
| G2 TIPS 결정 | ❌ **못 잰다** | TIPS 문항 **0건** |
| G4 교육훈련비 | ❌ 해당 없음 | 값을 안 바꿨다(보고만) |

즉 **재도전 9행은 오늘 밤 어떤 지표로도 검증되지 않았다.** 4단 검증(조문 직접 추출 · L1 교차
대조 · 골든셋 역검증 · 7사업 일관성)과 근거 dangling 0 을 통과했을 뿐이고, 그건 «조문에서
올바르게 뽑았다» 는 증거이지 «판정이 맞다» 는 증거가 아니다. 재도전 문항을 골든셋에 넣기
전까지는 그 9행의 실전 정확도를 모른다. `verified=false` 로 둔 이유이기도 하다.

같은 이유로 **"지표가 안 변했으니 안 고쳐도 됐다" 는 읽기는 틀렸다.** B 도 자기 섹션(§10)에
같은 취지를 적었다 — 오늘 밤 B 가 고친 8건도 대부분 골든셋이 안 태우는 경로였다
(L3 오버레이는 골든셋이 org 없이 돌고 · TIPS 0문항 · 초격차 1문항 · 숨은 한도는 수치
미입력이라 전제로 넘어간다). **두 섹션이 같은 한계를 가리킨다.**

→ 내일의 재료: 골든셋에 **재도전·TIPS·L3(org 지정) 문항**을 넣지 않으면 이 영역은 계속
측정 밖이다. 다만 골든셋 수정은 오늘 밤 금지(§10)라 손대지 않았다.

### 가장 큰 발견 — G3 은 위생 작업이 아니었다

지시는 "L1 조건이 L2 6곳에 복사돼 있으니 분리하라" 였는데, 진짜 이유는 다른 데 있었다.

```
D2 가 golden_set 의 '공통(지침 제14차)' 26 + '공통' 1 = 27행을 사업명=NULL 로 내렸다.
그 27문항의 룰 조회 키는 사업명 NULL 이다.
기존 rules 54행은 사업명이 전부 NOT NULL 이었다.
   →  27문항의 룰 조회가 0행.  골든셋 77문항의 35%가 룰 없이 판정되고 있었다.
```

L1 공통 9행이 그 27문항의 **유일한 룰 조회 경로**다. 이걸 위해 D 에게
`rules.사업명 DROP NOT NULL` 을 요청해 반영받았다(`uq_rules_key` 의 `NULLS NOT DISTINCT` 가
`(L1,NULL,NULL,비목)` 을 비목당 1행으로 잠근다). 조회 계약은 A 를 통해 B 와 합의:

```sql
base = (layer='L1' AND 사업명 IS NULL AND :사업의_비목계통 = '창업')
    OR (layer='L2' AND 사업명 = :사업명)
```

### 세운 가정 · 원래 지시와 다르게 한 것

**G3 은 덧셈만 했다.** 문자 그대로 하면 오너가 검수 완료한(`verified=true`) 53행을 뒤집는데,
계약서 §5 가 «판단을 바꾸지 말고 보고만» 하라고 못박은 3가지 중 첫 번째다. L1 9행을 **추가만**
하고 L2 는 한 글자도 빼지 않았다. 값이 같아 중복이 남아도 병합 결과가 안 바뀐다 —
위험 없이 이득만 얻는다. 뺄 후보는 검수요청서에 대조표로 넘겼다(승인 요청 ⑤).

**G2 TIPS 는 룰을 만들지 않았다.** "매핑표냐 별도 enum 이냐" 가 틀린 물음이었다.
TIPS 운영지침 1.(목적)은 **중소기업기술개발 지원사업 운영요령 제48조**에, 16.나.2)가)①은
**「국가연구개발사업 연구개발비 사용 기준」**에 의거한다 — **상위 규범이 통합관리지침이 아니다.**
CLAUDE.md 가 모두의창업 제3편을 뺀 것과 같은 구조다.
① 매핑표: 창업 10종과 교집합이 이름만 겹치는 '인건비' 하나뿐이고 그마저 의미가 다르다
(TIPS 는 연구수당이 별도 비목). 연구수당·간접비·학생인건비는 대응이 아예 없다 → 매핑하면
지침 제41조 "대표자 인건비 불가" 가 TIPS 연구수당 질문에 **인용까지 달고** 발화한다.
② 별도 enum: `guided_json_enum` 10종이 (1) 정규화의 vLLM enum 을 만든다. 26종을 더하면
36종이 되고 7개 창업 사업의 정규화 호출까지 바뀐다 — 오늘 잰 84.7% 가 enum 10종의 숫자다(§10).
③ 골든셋에 TIPS 문항이 **0건**이라 검증 수단이 없다.
→ TIPS 는 rules 0행 → `effective_rule()` None → **판단불가**. 그게 맞는 동작이다.

**추측으로 채우지 않은 값 2개**: 재도전 기계장치 PC 개수 한도 · 재도전 여비 숙박비 한도
(제17조②가 「공무원 여비 규정」 [별표 1] 을 참조하는데 **그 별표가 문서 안에 없다**).
둘 다 NULL + 조건 문장에만 적었다.

**4차 일관성 검증은 7개 사업 전수로 해야 결론이 뒤집힌다.** 처음에 예비·초기하고만 비교했더니
재도전이 «혼자 튀는» 것처럼 보였는데, 전수로 펴 보니 반대였다 —
· 기계장치 PC 개수 `NULL` : 도약·모두의창업이 **이미 같은 이유로 NULL** (1대/인은 4개 사업뿐)
· 퇴직급여충당금 «조건부 허용» : 대학·도약이 **이미 같은 방향** (금지는 예비·초기 2개뿐)
근거 조문이 다르면 값이 달라도 된다는 기준은 맞지만, **비교 대상을 좁게 잡으면 정상값을
이상값으로 오판한다.** 자세한 표는 `_G_룰검수요청.md` §5-4차.

### 발견한 함정

🔴 **판본 역전이 인용 오류로 직결된다는 실증.** 2026년판 재도전이 스캔 PDF 라 조문 0건이었고
구판(2025)이 `active` 를 차지해 **서비스가 작년 기준으로 판정 중**이었다. 게다가 창업기업
사업비 조문이 **2씩 당겨져 있다**(2025 제18~23 → 2026 제16~21). 구판 기준으로 룰을 만들었다면
인용 조번호가 전부 2씩 틀린다. VLM 판독본의 시프트 주장을 믿지 않고 `doc_articles` 의 2025년판
35조 목록으로 독립 재확인했다(조제목까지 일치).
판정이 실제로 갈리는 개정도 있다 — 외주용역 사전심의 임계가 «단건 2천만» → «동일 업체 협약기간
내 **누적** 2천만» 으로 바뀌어, 1,500만원 두 건이 구판은 통과하고 신판은 심의 대상이다.

🔴 **L1 공통행이 TIPS 로 새면 조용히 틀린다.** `사업명 IS NULL` 이라 조회 계약상 모든 사업에
붙는데, TIPS 는 통합관리지침 소관이 아니다. 막는 값은 이미 스키마에 있었다 —
`programs.비목계통`(TIPS='RND'). 위 조회 계약의 세 번째 조건이 그것이고 B 에게 전달했다.

🔴 **`unspecified_only` 병합을 "L1 로 갈아끼우기" 로 구현하면 금지예시가 사라진다.**
초격차·모두의창업이 그 규칙인데, L2 의 사업별 금지예시(초격차의 "크몽·위시켓", "GPU 다량 구매")가
통째로 날아간다. **금지예시가 줄어드는 병합은 «틀린 가능» 을 만든다.** 허용·한도·우선규범만
precedence 로 결정하고 금지예시·증빙·사전승인은 층 무관 **합집합**(사전승인 OR)이어야 한다. B 에게 전달.

🔴 **태스크 전제 2건이 실측과 달랐다.**
· G4 "제44조②③이 예비·초기 2건에 미반영" → **이미 반영돼 있었다.** 3차 검수에서 «가능»→«조건부»로
뒤집으며 제44조①②③을 다 넣었고 초격차·대학·도약까지 5개 사업에 반영돼 있다. 태스크 설명이 그
수정 이전 상태를 가리켰다. **지시대로 뒤집지 않고 근거만 보고했다.**
진짜 결손은 따로 있었다 — **모두의창업은 교육훈련비 행 자체가 없다**(인건비도 없다). 지침 제44조는
8개 사업 전부에 걸리므로 이건 결손이다. G3 의 L1 행이 자동으로 덮는다. 값을 뒤집지 않고 결손만
메운 것이라 §5 예외에 걸리지 않는다.
· G2 "TIPS 는 우선순위 조항도 없다" → **있다.** 16.나.2)가)① 이 정확히 `unspecified_only` 모양이다.
`precedence_rules` 는 오너 검수 완료 8행이라 손대지 않고 후보로 올렸다.

🔴 **읽기 트랜잭션 하나가 밤 전체를 세울 수 있다 — 실제로 났다.** 2026-09-01 00시경
`select count(*) from corpus.rules` 가 응답하지 않아 `pg_locks`·`pg_blocking_pids` 로 봤더니:
B 세션의 `idle in transaction` **886초**(item_alias 읽기락)가 D 의 `04_agent.sql`(psql)을 막고 있었고,
그 DDL 은 이미 **`corpus.rules`·`unmapped_premise`·`f_exec`·`f_profile` 에 AccessExclusiveLock 을
쥔 채** 대기 중이었다. 배타락 요청이 큐에 서면 이후 읽기까지 그 뒤로 밀리므로
**아무도 rules 를 읽지 못하는** 상태가 됐다(대기 세션 3개 확인).
`pg_terminate_backend` 는 치지 않았다 — 마지막 쿼리는 SELECT 지만 그 트랜잭션 앞쪽에 B 의
item_alias INSERT 가 있었는지 내가 알 수 없고, 강제 종료하면 남의 적재가 롤백된다. B·A 에게
진단을 넘겼고 B 가 풀었다(대기 0 확인). 조치: 내 조회용 스크립트 2개를 `autocommit=True` 로 바꾸고,
`seed_rules.py` 의 TRUNCATE 에 **`lock_timeout=15s` + `LockNotAvailable` 시 아무것도 바꾸지 않고
비0 종료**를 넣었다 — 기다리며 남을 줄세우느니 빨리 실패하는 게 낫다.

🔴 **`_재도전_2026_판독.json` 의 `본문_전사_범위` 메모가 틀렸다.** "9조만 전사, 나머지 23조는
조번호·조제목만" 이라 적혀 있으나 **33조 전부에 본문이 있다**(전 조문 길이를 세어 확인). 보수적으로
적힌 채 갱신되지 않은 메모다. 그래서 21조가 아니라 33조를 적재한다.

### 🔴 대장 1건 — 한 비목에 한도가 여럿이다. 스키마가 하나만 담는다

B 가 내 재도전 3중 한도를 쫓다 **`한도_단위` 산문 안에 추가 한도가 숨은 행 5개**를 찾았다.
`rules` 가 `한도_유형/값/단위` 3컬럼뿐이라 두 번째 한도부터는 단위 문자열에 산문으로 밀려나고,
코드의 구조화 비교에 안 잡힌다.

```
412 재도전 지급수수료    30만/인/일 + "총액 500만원 · 세무회계 월 20만원"   ← 내가 만든 행
404 모두의창업 여비      10만/인/박 + "광역시 8만원, 그 밖의 지역 7만원"
358·368·403 지급수수료  30만/인/일 + "시간당 10만원 초과 불가"
```

🔴 404 가 실제로 위험한 모양이었다 — 구조화값이 **특별시 기준 10만원**이라 광역시 9만원 숙박에
"한도 이내" 라고 답한다. 광역시 실제 한도는 8만원이라 초과다. **더 엄한 한도를 놓친 «틀린 가능»**
이고 오늘 밤 정지 조건이 잡으려는 바로 그 방향이다. B 가 `미파싱한도()` 로 막았다 —
산문에서 추가 한도 신호를 찾으면 `초과=False` 를 `None` 으로 낮춘다(`True` 는 유지).
지금은 전제로 넘어가니 «틀린 가능» 은 안 난다.

**근본 해결은 `corpus.rules`(G 소유) 쪽이고, 오늘 밤 범위가 아니다.**

🔴 **다만 B 가 제안한 «스키마 변경 없이 행만 늘린다» 는 불가능하다. 실측으로 확인했다:**

```sql
uq_rules_key : UNIQUE NULLS NOT DISTINCT (layer, 기관id, 사업명, 비목)
-- 재도전 지급수수료를 3행으로 쪼개려고 INSERT 해 보면
--   ✅ 거부됨: duplicate key value violates unique constraint "uq_rules_key"
```

한 비목에 여러 행을 두려면 유일성을 풀거나 키에 판별자를 넣어야 하는데, 둘 다 나쁘다 —
`effective_rule()`·`금지적중()` 이 (layer,사업,비목) → 층당 1행을 전제하고, 행을 늘리면
증빙·금지예시·근거가 한도 개수만큼 복제돼 `금지적중` 이 중복을 훑고 증빙 합집합이 더러워진다.

**맞는 모양은 자식 표다** — `rules` 는 비목당 1행으로 두고 한도만 뺀다:

```
corpus.rule_limits(rule_id FK, 유형, 값, 단위, 적용조건, 근거)   -- 한 룰에 N 한도
  재도전 지급수수료 → 3행 (멘토링 30만/인/일 · 총액 500만/사업기간 · 세무회계 20만/월)
  모두의창업 여비   → 3행 (특별시 10만 · 광역시 8만 · 그 밖 7만)
```

B 의 `_한도전수()` 가 이미 한도를 전수 비교하므로 **코드는 그대로 산다**(단위가 다른 한도를
min 으로 접지 않는 게 이것 때문이다). 이건 `rule_base.md` §8 미결 1 «유형 18종 분기 스키마
설계 — Stage 1 선결» 과 같은 문제이므로 그 항목에 합쳐서 푸는 게 맞다.

⚠️ 지역별 한도는 행을 늘려도 **"이 출장이 광역시인가" 를 알아야 골라 쓴다.** F축 입력 필드
(출장지 등급)가 필요해 D·A 와 같이 볼 건이고, 그것 없이 행만 늘리면 어느 행을 쓸지 못 정한다.

### 남은 것

- **비목계통 게이트가 L1 에만 걸려 있었다 → B 가 고쳤다. 확인 완료**
  내가 A 를 통해 준 조회 계약은 `layer='L1' … AND :비목계통='창업'` 뿐이라 **L3 가 게이트
  밖**이었다(L2 는 TIPS 가 0행이라 저절로 막힌다). B 의 스모크가 열어준 사업×org 축으로
  전수해 보니:
  ```
  TIPS × org 2 × 비목 10 = 20조합 → 룰 반환 5건, 전부 적용층=L3
    TIPS 인건비  대전과기원 허용=가능 🔴   TIPS 기계장치 한국창업대 허용=가능 🔴
    TIPS 재료비/광고선전비 불가 · 지급수수료 조건부
  B4문장: "인건비 룰상 집행 가능한 항목이다. 적용된 규범은 주관기관 내부규정이다."
  ```
  **L3 에 게이트가 필요한 이유는 L1 과 다르다.** L1 은 «통합관리지침이 TIPS 를 규율하지
  않는다»(위임 계통)인데, L3 는 «주관기관 규정이 자기 TIPS 과제를 규율한다» 가 현실에선 참이다.
  문제는 **비목 축**이다 — L3 행이 창업 계통 비목으로 키가 걸려 있고 TIPS 는 R&D 계통
  (연구활동비·연구수당·학생인건비·간접비)이라 '인건비' 매칭은 **의미 일치가 아니라 문자열
  충돌**이다. G2 근거 2 에 적은 위험("교집합은 이름이 겹치는 '인건비' 하나뿐이고 그마저 의미가
  다르다")이 L1 을 막았더니 L3 로 재현됐다.
  일관된 결론: **비목계통 ≠ '창업' 인 사업에는 창업 계통 비목 키의 룰이 어느 층에서도 붙으면 안 된다.**
  ⚠️ **«틀린 가능» 이 실제로 나가진 않았다** — `l3_게이팅` 이 `need_upper=True`(갈래
  `4-L3가능`)를 물고, TIPS 는 상위가 없어 확인이 성립할 수 없어 판단불가로 닫히며,
  `verified=False` 라 단독 «가능» 도 못 나간다. 3중으로 서 있었다.
  **그래도 고친 이유는 B4문장이 ④ 조립 프롬프트에 실리기 때문이다** — 최종이 닫히더라도
  LLM 에게 "집행 가능한 항목" 이라는 틀린 전제를 근거와 함께 먹이는 건 "근거 붙은 오답이 더
  나쁘다" 기준에 정면으로 걸린다. 기능 추가가 아니라 G2 결정의 복원이라 §10 에 걸리지 않는다.

  ✅ **B 수정 완료 · 내가 전수 재검했다**:
  ```
  TIPS 20조합 → 룰 반환 5건 → 0건   (폴백 경로 · 주입 경로 both 0)
  L3 가 여전히 기여하는 창업 사업 조합 21건 — 과잉 게이팅 아님:
    초격차 재료비/광고선전비 → 불가 · L1+L2+L3      (조항 없음 → L3 가 이긴다)
    예비·재도전            → 조건부 · L1+L2 · 참고_L3 O  (L2>L3 → L2 가 이기고 L3 병기)
  ```
  🔴 **B 가 구현하며 찾은 것 — 게이트를 두 군데 걸어야 했다.** `l3_룰_행` 폴백만 막으면
  샌다. **A 가 `l3_load.l3룰()` 로 직접 뽑아 `effective_rule(l3룰=…)` 로 주입하는 경로가
  그 폴백을 통째로 우회**하기 때문이다. 그게 A 의 정상 경로라 오히려 주력이다. 두 경로 다
  0건인 것을 위에서 확인했다.
  ⚠️ 책임 소재는 반반이다 — 내 계약 문장은 `base` 에 관한 것이었고 L3 는 오버레이라
  그 문장의 사정거리 밖이다(B 지적). 다만 게이트가 필요한 **이유가 층마다 다르다**는 걸
  내가 명세에 안 적은 건 맞다. B 가 `l3적용가능(cur, 사업명)` 으로 따로 빼서, 계통이 늘거나
  L3 가 R&D 비목으로 키를 걸게 되면 그 함수만 고치면 되게 했다.
- **🔴 한도 자식 표(`rule_limits`) 설계** — 바로 위. DDL 이라 D 소관이고 `rule_base.md` §8
  미결 1 과 함께 푼다. 그 전까지는 B 의 `미파싱한도()` 가 안전측(`None`)으로 막는다.
- **`corpus.rules.기관id` 타입 불일치 → B 가 값 쪽으로 고쳤다. 타입 쪽은 대장에 남는다**
  ```
  psycopg.errors.UndefinedFunction: operator does not exist: text = uuid
    ...FROM corpus.rules WHERE layer='L3' AND 기관id = $1
  corpus.rules.기관id  text  ↔  tenant.orgs.org_id  uuid  ↔  l3_articles.org_id  uuid
  ```
  org 2개 중 대전과기원에서만 터져서 추적했더니, **그 org 의 l3_articles 가 해당 비목을
  안 덮을 때** `corpus.rules` 폴백으로 흘러가 죽는 것이었다. 기관당 조문 6~11개 vs 비목
  10종이면 대부분이 여기다 — 드문 엣지가 아니라 흔한 경로였고, A 가 `tenant.orgs` 의
  org_id 를 그대로 넘기면 즉시 터진다. B 가 `_org문자열()` + `%s::text` 로 고쳤고,
  내가 **48조합(org 2 × 형 2 × 비목 6 × 사업 2) 전수 재검**해 예외 0 을 확인했다:
  ```
  대전과기원 광고선전비 + 초격차 → 불가   / L1+L2+L3     (그 기관 L3 가 불가)
  한국창업대 재료비     + 초격차 → 불가   / L1+L2+L3
  대전과기원 재료비     + 초격차 → 조건부 / L1+L2        (그 기관 L3 에 규정 없음)
  ```
  🔴 **부수 소득 — B 가 그 폴백을 `except Exception: pass` 로 감싸고 있었다.** 이 버그를
  조용히 흘려보내던 원인이면서, 더 나쁘게는 `l3_load.l3룰()` 이 **RLS org 컨텍스트를 세우는**
  호출이라 그 실패를 삼키면 **컨텍스트 없이 진행돼 남의 기관 규정으로 판정**할 수 있었다.
  `TENANT_LEAK` 을 코드가 손수 만들어 주는 자리였다. 지금은 `ImportError` 만 잡는다.
  **G 소관 아님** — `seed_rules.py` 는 L3 행을 아예 만들지 않는다(72행 전부 L1/L2 · 기관ID NULL).
  ⚠️ **대장에 남는 것**: (a) 타입 쪽 근본 수정(`기관id` → uuid). DDL 이고 `corpus`↔`tenant`
  교차 타입이라 멀티테넌시 설계 판단이 붙는다. (b) **L3 룰이 `corpus.rules` 에 사는가**가
  정리 안 돼 있다 — CLAUDE.md 는 "L3 는 `tenant.l3_articles`", `rule_base.md` §3-1 은
  overlay 를 `rules WHERE layer='L3' AND 기관=?` 로 뽑는다고 한다. 두 설계가 공존하고 실질
  정본은 `l3_articles` 쪽(`corpus.rules` L3 = 0행)이다. 죽은 코드를 지우려면 설계 문서
  수정이 따라야 해서 오늘 밤에 정리하지 않았다.
- **🔴 L3 게이팅이 사업별로 갈린다** — B 가 재현·수정 완료(`_오버레이()`). 내가 직접 태워
  확인했다: `예비창업 재료비+org → 조건부/L1+L2` · `초격차 재료비+org → 불가/L1+L2+L3`.
  아래는 왜 그렇게 갈리는지의 근거다. `precedence_rules` 전수 실측:
  ```
  예비·초기·재도전·도약·창업중심대학·모두의창업   L2>L3(all) 있음   → L2 채택, L3 는 참고
  🔴 초격차 스타트업 프로젝트   L1>L2(unspecified_only) 뿐   L2>L3 조항 없음
  🔴 TIPS                    precedence_rules 행 자체가 없음
  ```
  CLAUDE.md 명시 그대로다. 초격차는 폴백(`rule_base.md` §3-1 "조항 없음 → 엄격한 쪽")으로 가므로
  **L3 `불가` 가 L2 `조건부` 를 이겨야 한다 — 예비창업과 반대다.** B 가 예비창업(한국창업대
  재료비)으로만 재현했으므로, 게이팅이 "L2 가 항상 L3 를 이긴다" 로 균일하면 초격차에서 L3 불가가
  부당하게 진다(불가→조건부 = 관대한 쪽 = «틀린 가능» 계열). `참고_L3` 의 "본 사업 관리기준이
  우선합니다" 문장도 6사업에서만 참이고 초격차에서는 **문장 자체가 틀린 말**이 된다.
  TIPS 는 rules 0행이라 실전에서 안 걸린다(G2 결정대로 판단불가). **초격차만 살아 있는 경우다.**
  룰 데이터 쪽 변경은 없다 — 게이팅 코드(B)와 픽스처(E) 소관.
- **오너 결정 5건** — `_G_룰검수요청.md` §6. ① 새 18행 `verified=true` 승격 ② `precedence_rules`
  TIPS 행 추가 ③ 모두의창업 교육훈련비·인건비 L2 조항 실재 확인 ④ 카드 제한 업종 별도 표
  (gold 76 — `rules` 키가 `(layer,기관ID,사업명,비목)` 이라 "노래방" 을 넣을 자리가 없다. 근거는
  코퍼스에 실재: `L1_국고보조금통합관리지침_20260520` 별표1) ⑤ L2 중복분 실제 제거
- **골든셋 미커버 3건**(gold 49 부가세 · 50 사전 신청절차 · 76 카드 제한 업종) — 셋 다 지침
  제37~45조(비목 조문) **밖**이다. 추측으로 룰을 만들지 않았다. 룰 없음 = 판단불가라 안전하다.
- **TIPS 비목 어휘집(R&D 계통)** — 상위 규범 2종이 코퍼스에 active 로 실재하므로 재료는 있다.
  `item_vocab.계통='RND'` 로 넣으면 되지만 `guided_json` enum 을 사업별로 가르는 설계가 선행돼야 한다.
- `programs.전문기관` 이 TIPS 행에서 '창업진흥원' 인데 운영지침 3.용어의 정의는 "R&D 전문기관" 을
  따로 정의한다. 대조 필요 — D 에게 전달했다.
- **재도전 2026년판은 `chunks` 가 없다** (임베딩 재실행 금지). `doc_articles` 에만 있어 **검색으로는
  안 잡히고 `rules.근거` 역참조로만 산다.** 그래서 재도전 9행은 판정에 필요한 문장을
  `사전승인_조건`·`금지예시`·`허용예시` 에 자족적으로 담았다. 임베딩을 다시 돌리는 날 해소된다.

### 이어받는 사람에게 — G 는 **끝났다.** 남은 작업 없음

적재 3단계(A 의 재도전 적재 → `_MERGE_1` → G 의 `seed_rules.py`)가 2026-09-01 에 완료됐고
회귀 4종이 통과했다. 넘겼던 병합 회귀도 B 가 고쳐 확인까지 끝났다
(`적용층 L1+L2 34 / L1 23`). **G 가 진행 중이거나 대기 중인 작업은 없다.**

남은 것은 오너 결정 5건뿐이고 전부 `scripts/_work/_G_룰검수요청.md` §6 에 모여 있다.

현재 상태를 다시 확인하려면:

```bash
PYTHONIOENCODING=utf-8 python scripts/rule_lookup.py --golden
#   기대: {"룰있음": 57, "룰없음(공통)": 4, "룰없음(사업지정)": 16}
#         적용층 Counter({'L1+L2': 34, 'L1': 23})
PYTHONIOENCODING=utf-8 python scripts/_work/_G_골든셋_역검증.py   # 치명 0
```

`corpus.rules` 를 다시 만져야 하면:

```bash
PYTHONIOENCODING=utf-8 python scripts/seed_rules.py --dry   # DB 안 건드리고 구성만 검증
PYTHONIOENCODING=utf-8 python scripts/seed_rules.py         # 실적재 (_MERGE_1 마커 필요)
```

🔴 `seed_rules.py` 가 `corpus.rules` 의 **유일한 소유자**다. 다른 스크립트는 읽기만 한다.
행을 고칠 일이 생기면 **DB 에 UPDATE 치지 말고 이 파일을 고치고 다시 돌려라** — TRUNCATE
재적재라 직접 친 UPDATE 는 다음 실행에 조용히 사라진다.
🔴 가드 3개가 걸려 있다: `_MERGE_1` 마커 없으면 TRUNCATE 거부 · `lock_timeout=15s` 로
락 못 잡으면 아무것도 바꾸지 않고 비0 종료 · 근거 dangling 이 남으면 비0 종료.

### 산출물

| 파일 | 무엇 |
|---|---|
| `scripts/seed_rules.py` | 재도전 9행 + L1 공통 9행 + G2 결정 근거. `--dry` 로 DB 없이 검증. `_MERGE_1` 마커 없으면 TRUNCATE 거부 |
| `scripts/_work/_G_재도전2026_적재.py` | 🔴 **A 가 실행.** 판독본 33조 → `doc_articles` + 판본 역전 교정. `chunks` 무영향 |
| `scripts/_work/_G_룰검수요청.md` | 오너 D단계 검수 요청서 (결정 5건) |
| `scripts/_work/_G_룰검수요청_대조표.md` | 18행 ↔ 근거 조문 **원문** 대조표 (자동 생성 · 손으로 고치지 말 것) |
| `scripts/_work/_G_검수표_생성.py` | 위 대조표 생성기 |
| `scripts/_work/_G_골든셋_역검증.py` | G5 3차 — 공통 27문항 기계 대조. 치명 1건이라도 나오면 비0 종료 |

---

## H — 비동기 에이전트 · 어댑터 · 비용방어

> 판정 경로 밖의 축. 여기서 만든 것 중 **판정 1건의 지연 예산에 들어가는 것은 하나도 없다** —
> 전부 배치이거나 서버 앞단이다. 멀티 에이전트 오케스트레이션·agentic multi-hop 은 쓰지 않았다.
> LLM 도 한 번도 부르지 않았다 (A4 변경 요약까지 코드가 만든다 — 아래 이유).

### 실측 숫자

| 항목 | 값 |
|---|---|
| `corpus.recheck_queue` 총 | **370행** · 재실행 멱등 (같은 입력에 같은 큐) |
| ├ A4개정 | BASIS_AMENDED 186 · BASIS_RENUMBERED 107 · ITEM_VOCAB_RECHECK 51 · UPSTREAM_LAW_AMENDED 15 · **ACTIVE_NOT_LATEST 1** |
| ├ A2엄격조항 | STRICTER_L3 **7** (그중 우선순위상 **지는 것 6** · 판단불가 1 · **이기는 것 0**) |
| └ A5기관답변 | ORG_ANSWER 3 (합성 픽스처) |
| 대상 분포 | rule 222 · check_item 78 · item_alias 51 · none 16 · precedence_rule 3 |
| 조 매칭 (제목 주·번호 보조) | 구판 조 311 중 **292 = 93.9%** 가 붙었다 (제목 255 · 번호 26 · 제목유사 11) |
| 통합관리지침 제13차→제14차 | 88조 중 **번호이동 56조** — 번호로는 절대 못 잇는 규모 |
| `adapter.py --selftest` | **13항목 전부 통과** (f_axis 게이트) |
| `server/main.py --selftest` | **34항목 전부 통과** (`프론트 연동 사양.md` §8 계약 대조) |
| 정산 리허설 | `f_exec` **0행** → 합성 픽스처 6건으로 배관만. **A9 대기** |

**회귀 3종** — 내 작업이 아무것도 안 흔들었다:
`audit_db` ✅ 감사 통과 · `eval_retrieval` RRF hit@5 **52.9%**(불변) ·
`score_judgment` 치명 오답 **0** · 일치율 **84.7%** · 인용 **100%** (종료코드 0)

### 한 일 — 왜 그렇게 했는지

**H1 A4 개정 대응 (`scripts/agent_a4.py`) — 1순위, 완료**

`version_diff.py` 가 이미 있었지만 그건 **벡터**로 조를 이었다. 배치는 재현성이 요건이라
(`Agent.md` §6) 같은 입력에 같은 큐가 나와야 한다. 그래서 A4 는 임베딩을 쓰지 않는다 —
**제목 정확 → 번호 정확 → 제목 유사(0.85) → 신설/삭제** 4단 매칭 + `difflib` 본문 diff 다.
조제목은 개정에서 거의 안 바뀐다(실측: 93.9% 중 제목만으로 255/292 가 붙었다). KURE-v1
로딩 30초와 GPU 경합이 사라진다. 잔여만 유사도로 잇고, 임계 미만은 **잇지 않는다** —
틀린 짝은 없는 것보다 나쁘다. 변경 요약도 코드가 만든다(unified diff 축약). `Agent.md` 는
"변경 요약(LLM)" 이라고 했지만 배치에서 LLM 요약은 매번 다른 문장을 내놓고, 그러면
"이번 주에 뭐가 바뀌었나" 를 지난주와 대조할 수 없다. 슬롯 ⑤ 배선은 남겨뒀다.

재료가 지시와 달랐다. 「구판 XML 944건」은 L1 **법령** 연혁이고, 룰이 근거로 삼는 7개 문서
(통합관리지침 + 세부관리기준 6종)의 구판은 이미 `corpus.documents` 에 `status='superseded'`
로 **조문까지** 들어 있었다. 그래서 경로를 둘로 갈랐다:

- `--source db` (기본) — active ↔ superseded 계보 7개. **여기가 rules 역조회에 바로 닿는다**
- `--source xml` — 연혁 XML ↔ 현행 XML. 법령 개정을 `corpus.refs` **깊이 1** 로 타고 온다

역조회는 `rules`·`check_items`·`precedence_rules` 셋을 다 본다. ⚠️ `precedence_rules.근거` 만
모양이 다르다 — `{doc: <src_path>, 조번호: "제3조(적용범위)"}` 라 `doc_id` 가 아니라 경로다.
`documents.src_path` 로 되짚어 붙였다.

🔴 **`corpus.rules` 를 UPDATE 하지 않았다.** `Agent.md` §9 는 "해당 룰 verified=false 전환"
이라고 쓰여 있으나 계약서 §3 이 G 와의 충돌을 이유로 큐 적재까지로 못박았다. 계약을 따랐다.

**H5 f_axis 라우팅 차단 게이트 (`scripts/adapter.py`) — 완료**

`서비스 아키텍쳐.md` §11 이 "설계됐으나 미구현" 으로 박아둔 항목. 게이트를 **두 겹**으로 짰다.

- **[1겹] 슬롯 선언** — `f_axis=True` 슬롯(정규화·F문서파싱·스크리닝·판정조립) + 외부 제공자 → 차단
- **[2겹] 프롬프트 검사** — F1~F7 필드명이 프롬프트에 있으면 슬롯 선언과 무관하게 차단

2겹을 넣은 이유: 요건의 원문이 "F축이 **프롬프트에 들어가는** 호출" 이다. 기준은 슬롯 라벨이
아니라 내용이고, 현실적인 사고는 오케가 슬롯 이름을 잘못 넘기는 쪽이다. 1겹만으로는 뚫린다.
차단은 `tenant.incidents(종류='ROUTING_BLOCK')` 에 남는다 — D 가 그 enum 값을 미리 넣어뒀다.
🔴 **탐지된 필드명만** 남기고 매칭된 원문은 남기지 않는다. 사고 기록이 새 유출 경로가 되면 안 된다.
자가검사는 기본이 `기록=False` 다 — 테스트가 감사 표를 잡음으로 덮으면 진짜 사고가 묻힌다.

**H7 FastAPI 서버 (`server/main.py`) — 완료 (목 우선)**

`SUDDOE_MOCK=1`(기본)이면 DB·GPU 없이 뜬다. `?목=가능|조건부|불가|판단불가` 로 **4-way 를
전부** 그려볼 수 있게 했다 — 판단불가는 빨간 에러가 아니라 화면 9 로 잇는 정상 경로라
목에서부터 그렇게 그려야 한다(판단불가 응답은 200 이고 `문의초안` 이벤트가 같이 온다).
SSE 는 화면별로 쪼갠 이벤트(`판정`·`해야할일`·`인용`·`전제`·`참조사슬`)와 전체 `결과` 를
둘 다 보낸다 — 프론트가 스트리밍 렌더를 하든 한 방에 받든 같은 계약을 쓴다.
실호출 전환은 `SUDDOE_MOCK=0` 하나이고 프론트 코드는 안 바뀐다. A 의 `orchestrate.판정()` 을
dry 로 태워 반환 키를 확인했다 — `전제목록`→`전제` 만 이름이 다르고 나머지는 그대로 통과한다.

**H6 비용 방어 — 서버 앞단에 넷 다**

일일 총 호출 하드 캡(2000) · IP 시간당 제한(60) · 동일 질문 캐시(900초) · 심사 기간 개방창
+ `/admin/warmup`. 전부 환경변수다.
🔴 캐시 열쇠는 **sha256 해시**다. F5(친족거래·전직임직원)는 "판정 후 폐기"(`서비스 아키텍쳐.md`
§6)라 캐시가 원문을 들고 있으면 그 규칙이 깨진다. `org_id` 도 열쇠에 넣었다 — 안 넣으면
A 기관 판정이 B 기관에 나간다(TENANT_LEAK). 캐시로 답하면 GPU 를 안 쓴 것이므로 일일
카운트를 되돌린다. 워밍업은 **콜드 스타트가 두 군데**라 둘 다 태운다 — 임베딩 모델 로딩과
vLLM `guided_json` 첫 호출의 스키마 컴파일(`LLM.md` §1).

**H2 엄격조항 검토 (`scripts/agent_a2.py`) — 완료**

🔴 **엄격도만 뽑으면 그게 오답의 씨앗이다.** CLAUDE.md 가 "충돌 해소는 '아래가 엄격하면
이긴다' 가 아니다 — 반대가 규칙" 이라고 못박았고 8사업 중 6개가 L2 > L3 다. 큐에 "더
엄격한 조항" 만 넣으면 검수자가 "그러니 이걸 적용하자" 로 읽는다. 그래서 `precedence_rules`
를 같이 조회해 **"더 엄격하지만 우선순위상 진다"** 를 요약 문장에 박았다.
E 의 합성 L3 픽스처 2기관 17조로 검증했고 후보 7건이 나왔다 — **이기는 것 0건**이다.
사업비와 무관한 조(복무·연차·출퇴근·안전점검)와 "~에 따른다" 참조 조는 후보에서 뺀다.
실제로 잡은 것: 멘토링비 한도 축소(L3 20만 vs 국가 30만) · 단장 사전승인 추가 ·
자산등록 1개월 기한 추가 · 귀금속/판촉물 금지 추가.

**H3 답변 축적 (`scripts/agent_a5.py`) — 파이프라인 완료 · 실데이터 0건**

기관 답변은 규범이 아니다 — 담당자 견해일 수도 있고 기관마다 다르다. 그래서 `rules` 에
바로 넣지 않고 후보로 큐에 쌓는다. 추출은 규칙 기반이고 **애매하면 안 뽑는다**(`허용=None`).
🔴 입력에 실명·전화·메일이 섞이면 **적재 전에 지운다** — 저장 계층화 원칙에서 F4 인력
이름조차 저장하지 않는데 기관 담당자라고 다를 이유가 없다. 부서까지만 남긴다.
합성 픽스처 3건으로 세 갈래(기존 룰과 충돌 / 확인 값 / 불가)를 전부 태웠다.

**H4 정산 리허설 (`scripts/rehearsal.py`) — 배관 완료 · A9 대기**

기본이 `dry` 다. `--live` 는 A9(`judge_cli`) 이후에 켠다.
🔴 **정규화를 건너뛰지 않는다.** 집행내역은 이미 구조화돼 있지만 온라인 판정과 **같은 경로**
로 넣어야 리허설 결과가 실제 판정과 같은 뜻을 갖는다. 구조화 값을 바로 룰 조회에 꽂으면
정규화 오차가 리허설에서만 사라져서 "정산은 통과했는데 실제로는 틀린" 결과가 나온다.
리스크 4등급(높음=불가 → 환수 위험 / 보통=조건부 / 확인=판단불가 / 낮음=가능)으로 접고
금액을 합산한다. `--queue` 로 '높음' 건만 큐에 남긴다.

### 발견한 함정

1. 🔴 **`대상ID` 는 DB 에서 `대상id` 다.** 따옴표 없이 CREATE 한 한글+영문 혼합 컬럼을
   PostgreSQL 이 소문자로 접는다. `information_schema` 결과와 파이썬 키를 문자열로 비교하면
   안 붙어서 **컬럼이 통째로 조용히 버려진다.** 큐가 무엇을 가리키는지 사라진다.
   → 대소문자를 접어서 매칭한다 (`agent_a4.py::적재`). 다른 세션도 한글 컬럼을 다룰 때 같은 함정.

2. 🔴 **판(版) 순서는 척도가 다르면 비교하면 안 된다.** 「초격차 …(제10차)」와
   「초격차 …(2025년)」는 같은 자로 잰 값이 아니다. 2025 > 10 이라고 2025년판이 최신이라
   단정했더니 **오탐 2건**(초격차 · 중기부 보조사업 관리규정)이 나왔다. 척도 태그가 같을 때만
   비교하니 남은 **1건이 진짜**다 — **재도전성공패키지**(2026년 11차가 superseded 이고
   2025년이 active). 미결 대장 #12 와 독립적으로 같은 결론에 닿았다. 🔴 **이게 A4 의 값이다** —
   "작년 기준으로 판정 중" 을 사람이 안 봐도 배치가 잡는다.

3. **같은 시행일 = 판이 아니라 포맷 차이.** `L1_중소벤처기업부보조사업관리규정_20241223`(XML)과
   `…(제181호)(20241223)`(변환 PDF)은 같은 규범의 두 포맷이다. 그대로 diff 하면 포맷 차이가
   전부 '개정' 으로 나온다. 시행일이 같으면 잇지 않는다.

4. **A4 의 XML 경로가 룰에 안 닿는다 — 고장이 아니다.** 우리가 참조하는 법령의 개정 조 314개
   중 조 지정으로 참조되는 건 15개인데, 그 참조를 하는 조가 **룰의 근거 조가 아니다**
   (룰은 통합관리지침 제36~45조·붙임2 를 인용하고, 법령을 인용하는 건 제9~26조다).
   그래서 `대상종류='none'` 로 15건만 남겼다. 0건과 "고장" 을 구분하려고 진단 숫자
   (`개정된 조 N개 중 참조되는 것 M개`)를 항상 같이 찍는다.

5. **HTTP 헤더는 ASCII 만 싣는다.** 관리자 토큰에 한글을 쓰면 클라이언트가 요청을 못 만든다.

6. **`corpus.rules` 에 `허용='불가'` 가 0행이다** (조건부 51 / 가능 3). A2 의 "국가가 가능·조건부
   인데 L3 가 금지" 갈래가 실질적으로 전건에 열려 있다는 뜻이다. B1 이 금지예시 매칭으로
   불가를 만드는 구조라 그렇고, A2 는 그 구조를 전제로 짰다. B1 이 바뀌면 A2 도 다시 봐야 한다.

### 세운 가정

- **A4 계보의 제명 변경 3건은 손으로 이었다** (`_FAMILY_ALIAS`, 근거 주석 포함).
  제13차「창업사업화 지원사업 통합관리지침」→ 제14차「중소기업창업 지원사업 통합관리지침」
  (차수 13→14 연속 · 발행주체 동일 · 조문 구성 연속), 혁신분야 창업패키지 → 초격차 스타트업
  1000, 창업도약패키지 '지원사업' 표기 탈락. **자동 family 키로는 못 잇는다.** 새 개명이
  생기면 여기에 근거와 함께 한 줄 추가한다.
- **본문 동일 임계 0.995 · 제목 유사 임계 0.85** — 공백·개정표기 흔들림을 흡수하는 값이고
  골든셋으로 잰 게 아니다. 오탐이 나면 조이는 게 아니라 **잇지 않는 쪽**으로 움직인다.
- **RunPod 는 '외부 제공자' 가 아니다.** 3자 하드웨어지만 우리가 띄운 프로세스에 우리가 HTTP 를
  친다. `LLM.md` §1 의 "제출본·데모는 RunPod 허용, 실서비스 F축 슬롯은 자체 GPU" 를 그대로 옮겼고
  `SUDDOE_VLLM_SELF_HOSTED=0` 으로 뒤집을 수 있게 남겼다.
- **비용 방어 기본값**(일일 2000 · IP 시간당 60 · 캐시 900초)은 심사 트래픽을 재본 값이 아니다.
- **A2 의 한도 비교는 단위 문자열이 자유 텍스트라 자동 비교가 여기까지다.** 같은 축인지는
  사람이 확인한다 — 그래서 판정이 아니라 큐로 간다. 상세에 `주의` 로 박아뒀다.

### 확정 원칙과 어긋난 것 — 보고만 하고 원칙을 따랐다

- `Agent.md` §9 A4 절차의 "해당 룰 `verified=false` 전환" 을 **하지 않았다.** 계약서 §3 이
  `recheck_queue` 적재까지로 못박았고 G 의 `rules` TRUNCATE 재적재와 충돌한다. 큐를 사람이
  처리할 때 전환하는 게 맞다.
- `Agent.md` §9 의 "변경 요약(LLM)" 을 **코드 산출로 바꿨다.** 배치는 재현성이 요건이고
  (`Agent.md` §6), 매번 다른 요약문이면 지난주 큐와 대조가 안 된다. 슬롯 ⑤ 자리는 남겨뒀다.
- **§10 "새 라이브러리 도입 금지"에 `fastapi` 설치가 걸린다.** H7 지시가 명시적으로 FastAPI 를
  요구했고 `uvicorn`·`starlette`·`pydantic` 은 이미 있어 fastapi 하나만 추가했다(0.141.1). 보고한다.

### 남은 것 — 내일의 재료

- **H4 `--live` — 배선은 끝났고 막힌 건 크레딧이다.** (2026-09-01 A 확인으로 갱신)
  A9 `judge_cli` 는 완성됐고 리허설은 이미 같은 엔진(`orchestrate.판정`)을 부른다.
  못 돌린 이유는 **잔액 소진**($8.02 → $1.52. GPU 창 3회 중 2회가 인프라 실패,
  마지막 창은 E2E 77문항 31분에 다 썼다). 크레딧이 채워지면 한 줄이다:
  `python scripts/rehearsal.py --fixture --사업명 예비창업패키지 --live`
  🔴 **다만 그 전에 `max_tokens` 부터 올려라.** A 의 실전 E2E 에서 판단불가 5건이
  **전부 `max_tokens=1500` 잘림**이었고 모델이 스스로 판단불가를 고른 건 0건이다.
  1500 인 채로 리허설을 돌리면 "리스크 요약" 이 잘려 나온 걸 판정 품질로 오독한다 —
  **3000 으로 올린 뒤에 재는 게 순서다.**
  `f_exec` 은 여전히 0행이라 **실데이터로는 한 번도 안 돌았다.**
- **A4 주 1회 배치 스케줄링** — cron/스케줄러 배선은 안 했다. 지금은 수동 실행이다.
- **A5 실데이터** — 기관 공식 답변이 0건이다. 픽스처를 지우고 실데이터로 갈아끼우면 된다.
- **A2 는 합성 픽스처로만 검증했다.** 실제 기관 규정이 오면 `_무관` 필터와 비목 추정을 다시 봐야 한다.
- **A4 의 `번호이동` 107건은 지금 데이터에서 대부분 무해하다** — 룰이 신판 번호로 쓰여 있기
  때문이다. 진짜 값은 **제15차가 나왔을 때** 나온다. 오늘 것은 엔진이 도는지의 증거다.
- **관리 화면** — `/admin/cost`·`/admin/gate` 는 JSON 만 준다. "제15차 개정 · 변경 N개 조항
  자동 반영" 화면은 `recheck_queue` 를 그대로 읽으면 된다.

### 건드린 파일 — 전부 신규. **남의 소유 파일은 하나도 고치지 않았다**

| 파일 | 내용 |
|---|---|
| `scripts/agent_a4.py` | H1 개정 대응 — 계보·조매칭·diff·근거 역조회·큐 적재 (`--source db\|xml\|both`) |
| `scripts/agent_a2.py` | H2 L3 엄격조항 후보 — **precedence 판정 병기** |
| `scripts/agent_a5.py` | H3 기관 답변 → 룰 후보 → 검수 큐 (+ 개인정보 씻기) |
| `scripts/rehearsal.py` | H4 정산 리허설 (A9 대기 · 기본 dry) |
| `scripts/adapter.py` | H5 f_axis 2겹 게이트 + 슬롯 5종 + 제공자 라우팅 (`--selftest`) |
| `server/main.py` | H7 API 목 서버 + H6 비용 방어 4종 (`--selftest`) |
| `scripts/_work/_A5_기관답변_픽스처.json` | 🔴 합성. 실데이터 아님 |
| `scripts/_work/_A4_개정보고.md` · `_A2_엄격조항.md` · `_리허설_결과.json` | 실행 산출 |
| `scripts/_work/_sessions/H.txt` | 세션 이름 등록 |

쓴 테이블은 `corpus.recheck_queue` 뿐이다(계약 §3 그대로).
`tenant.incidents` 는 D 가 H5 용으로 넣어둔 `ROUTING_BLOCK` enum 으로만 쓰고, 자가검사가
남긴 행 18건은 지웠다 — 사고 기록이지 테스트 로그가 아니라서.
`corpus.rules`·`corpus.chunks` 는 읽기만 했다.

---

### 2026-09-01 새벽 이어서 — G 재적재 대응 · 큐 자기치유 · 관리 API

토큰 한도 리셋 후 재개. "다음에 이어서 할 일" 에 적어둔 것부터 갔다.

**🔴 큐가 죽어 있었다 — 발견하고 고쳤다.**
G 가 `_MERGE_1` 이후 `corpus.rules` 를 TRUNCATE RESTART IDENTITY 로 재적재해
54행(rule_id 290~) → **72행(rule_id 344~415)** 으로 갈렸다. 내 큐 **222행의 `대상id` 가
전부 죽은 값**을 가리키고 있었다. D 가 FK 를 일부러 안 건 그 상황이 실제로 일어난 것이다.

FK 가 없으니 DB 가 안 막아준다 → **코드가 스스로 고치게 했다.** `agent_a4.py` 에 둘을 넣었다:

- **`치유()`** — 죽은 `대상id` 를 셋으로 가른다.
  ① 불변열쇠(사업명·비목·doc_id·조번호…)가 같은 **살아있는 쌍둥이가 있으면 지운다**(진짜 중복).
  ② 쌍둥이가 없으면 **`상태='기각'` + 무효사유**. 지우지 않는다 — 근거 자체가 사라진 것이라
  사람이 봐야 한다. ③ **사람이 손댄 행은 건드리지 않는다**(`처리자` 있음 · `상태<>'대기'`).
  🔴 `--heal` 은 **적재 뒤에** 온다. 재실행이 현재 rule_id 로 쌍둥이를 먼저 만들어야
  ①이 성립한다. 순서가 뒤집히면 아무것도 못 지운다 — 런북에 박아뒀다.
- **`조정()`** — 이미 해소된 항목을 `상태='반영'` 으로 닫는다. 지우지 않는다(이력이 A4 가
  실제로 일한다는 증거다). 낡은 큐를 두면 사람이 **이미 고쳐진 것을 또 고친다.**

실제로 그게 났다: 어젯밤 A4 가 잡은 `ACTIVE_NOT_LATEST`(재도전성공패키지)를 **G 가 고쳤는데**
(2026년 11차가 `active`, 조문 33개) 큐에는 "작년 기준으로 판정 중" 이 그대로 남아 있었다.
`조정()` 이 닫았다. 🔴 **A4 가 잡고 → G 가 고치고 → A4 가 닫았다.** 이 한 바퀴가 오늘의 증거다.

| | 처리 |
|---|---|
| 죽은 `대상id` 224행 | 삭제 222 (중복) · 기각 2 (`check_items.code` 가 E4 사업별 분기로 바뀜) |
| 해소된 항목 | `ACTIVE_NOT_LATEST` 1행 → `반영` |
| 재실행 후 | **518행** (대기 515 · 기각 2 · 반영 1) · **죽은 대상 0** · 두 번 돌려도 518 |

큐 내용도 G/E 의 새 데이터로 다시 잡혔다 —
BASIS_AMENDED 264 · BASIS_RENUMBERED 168 · ITEM_VOCAB 57 · UPSTREAM_LAW 15 · BASIS_DELETED 1
· STRICTER_L3 7 · ORG_ANSWER 3.

**H4 실호출 배선 — A9 확인 후 정리**
`judge_cli.py` 가 `orchestrate.판정()` 을 감싼 것이라 리허설은 이미 **같은 엔진**을 부르고
있었다. 세 가지를 고쳤다: ① `conn` 을 넘겨 커넥션 재사용(전건 배치라 건마다 새로 붙으면
그것만으로 수백 번 접속한다 — `판정` 은 `닫기 = conn is None` 이다) ② `--기관` 추가
(TENANT_LEAK 검사 기준) ③ **강등코드 집계**를 등급과 따로 낸다 — `가능` 인데 강등코드가
붙었으면 그 근거를 못 믿는다는 뜻이라 등급에 섞으면 안 보인다.
여전히 `--live` 는 안 돌렸다: **GPU 는 A 만 연다**(계약 §5). `f_exec` 도 0행이다.

**`GET /admin/queue` 추가 — 관리 화면 재료**
`corpus.recheck_queue` 를 종류·사유코드·사업명·상태로 걸러 준다. 설계 문서가 말한
「제15차 개정 · 변경 N개 조항 자동 반영」 화면이 이걸 그대로 그리면 된다.
🔴 **읽기 전용이다.** 큐를 승인해 `rules` 로 올리는 경로는 API 에 두지 않았다 — 그건
사람이 검수하는 일이고(`rule_base.md` §6), 열면 검수 없이 룰이 바뀔 길이 생긴다.
서버 자가검사 **37항목**으로 늘었다 (전부 통과).

**A4 스케줄러는 걸지 않았다 — 못 한 게 아니라 안 했다.**
어디서 돌지가 안 정해졌다(§11 미결 #5 호스팅). 배포처가 정해지면 런북 세 줄을 그대로
cron/Task Scheduler 에 건다. `agent_a4.py` docstring 에 순서와 이유를 적어뒀다.
사람이 잊어도 **큐가 낡을 뿐 판정은 안 틀린다** — 큐는 판정 경로 밖이라서.

**A5 실데이터 · A2 실기관규정**은 여전히 0건이다. 만들 수 없는 것이라 그대로 남긴다.

**회귀 3종 재확인** — 재개 작업 후에도 그대로다:
`audit_db` ✅ · RRF hit@5 **52.9%** · 치명 오답 **0** · 일치율 84.7% (종료코드 0).

**추가로 배운 것**

- **재적재는 파생 상태를 조용히 죽인다.** FK 를 못 거는 자리(대상이 여러 표로 갈리거나
  상위가 TRUNCATE 되는 곳)에서는 **소비하는 쪽이 자기치유를 들고 있어야 한다.**
  D 가 FK 를 안 건 판단은 옳았고, 그 대가가 이 `치유()`·`조정()` 두 함수다.
- **파생 큐는 만들기보다 닫기가 어렵다.** 무엇을 안 닫으면 안 되는지(사람이 손댄 행)와
  무엇을 못 닫는지(못 훑은 계보)를 먼저 정해야 자동 정리가 안전해진다.
