# L3 시연용 적재 — 담당자 안내

2026-09-05 · 프론트/인프라 → 백엔드 담당자
백엔드 `origin/main` `3134ce5` 기준

---

## 0. 방향

```
프론트     L3 업로드 화면을 «붙이지 않습니다»
담당자     시연용 L3 문서를 DB에 «직접 적재» 합니다
판정엔진   L3 부터 보는 흐름 그대로 (게이팅 4갈래)
```

프론트는 이 건에 코드가 없습니다. **적재만 되면 판정이 바로 L3를 탑니다.**

---

## 1. 적재하면 판정이 이렇게 갈립니다

`rule_lookup.py::l3_게이팅()`

| L3가 이 비목에 대해 | 갈래 | 상위(L1·L2) |
|---|---|---|
| 규정 **없음** | `1-미규정` | ✅ 봄 |
| 상위 조문을 **가리키기만** 함 | `2-참조만` | ✅ 봄 + seed_refs |
| **불가 / 조건부** | `3-L3제약` | 🔴 **안 봄 — 여기서 닫힘** |
| **가능** | `4-L3가능` | 🔴 **강제로 봄** |

> 🔴 「가능」일 때 오히려 상위를 **반드시** 봅니다. 코드 주석:
> 「주관기관 규정은 국가 지침이 있다고 전제하고 그 위에 얹는 문서라 **구조적으로
> 자족하지 않는다.** "불가"는 단독으로 안전하지만 **"가능"은 절대 L3 단독으로
> 확정하지 않는다.」

**지금은 L3가 비어 있어 모든 판정이 `1-미규정`으로 갑니다.** 오류가 아니라 정상 경로이고,
적재하는 순간 위 4갈래가 실제로 갈립니다.

---

## 2. 적재 대상

```
org_id  cfeba091-251a-5ae4-8cc9-88c6e6679440   (경상국립대학교 창업중심대학사업단)
```

`tenant.orgs` 에 실재하는 값입니다. 시연 계정 `prototype@ssudo.kr` 이 이 기관에 물려
있습니다(`db/init/12_qa_accounts.sql`).

⚠️ 「경상국립대학교」·「경상국립대학교 창업지원단」이 명부에 같이 있습니다.
**이름으로 찾으면 다른 기관에 붙습니다.** 위 UUID를 그대로 쓰세요.

---

## 3. 판정이 L3를 집어오는 조건

`l3_load.py::로드()`

```sql
SELECT a.article_id, a.조번호, a.조제목, a.본문, a.장, a.조번호_int
  FROM tenant.l3_articles a
  JOIN tenant.l3_documents d ON d.doc_id = a.doc_id
 WHERE a.org_id = %s AND d.org_id = %s AND d.status = 'active'
 ORDER BY a.조번호_int NULLS LAST, a.조번호
```

그 뒤 `사업비관련장(장)` 으로 한 번 더 거릅니다.

**적재할 때 반드시 맞춰야 하는 것:**

| 조건 | |
|---|---|
| `l3_documents.status = 'active'` | 🔴 `superseded` 면 **안 실립니다** |
| `l3_articles.org_id` = `l3_documents.org_id` | 둘 다 같은 org 여야 함 (비정규화 컬럼) |
| `장` | **NULL 이어도 됩니다** — 「파서가 못 잡은 것이지 무관하다는 증거가 아니다」로 통과시킵니다 |
| `파싱품질` | `CHECK (대기/pass/warn/fail)` — NOT NULL |

---

## 4. 컬럼별로 무엇이 중요한가

`tenant.l3_articles` 로 직접 넣으실 경우 (「파싱 형태 적재」).

| 컬럼 | 쓰임 | 권고 |
|---|---|---|
| **`조제목`** | 🔴 **게이팅이 여기를 «먼저» 봅니다** | **비목명이 들어가게** — 「제12조(재료비)」의 `재료비` |
| `본문` | 프롬프트 B1 블록에 통째로 들어감 | 표는 §5 참고 |
| **`조번호`** | 🔴 **화면에 인용 라벨로 그대로 나갑니다** | 「제12조」 형태 권장. `단락047` 이면 사용자에게 무의미 |
| `조번호_int` | 정렬 | 조 순서대로 |
| `장` | 사업비 관련 여부 필터 | NULL 무방 |

**`조제목`이 왜 중요한가** — `l3_load.l3룰()` 주석:

> 🔴 **제목 패스가 먼저다.** 본문 적중은 「제21조(자산의 등록 및 관리) … 기계장치는」
> 같은 부수적 언급까지 잡아서, 조번호가 앞선 자산관리 조문이 **진짜 비목 조문을
> 가려버린다.** 제목에서 못 찾을 때만 본문으로 내려간다.

그리고:

> 🔴 조문은 있는데 분류가 안 되는 경우도 None(=미규정)이다. **'가능'으로 추정하지 않는다** —
> 분류 실패를 '가능'으로 흘리면 근거 없는 "가능"이 생긴다.

**즉 `조제목`에 비목명이 없으면 조문을 넣어도 게이팅이 안 걸리고 `1-미규정`으로 떨어집니다.**

---

## 5. 🔴 문서를 파서에 태워 적재하실 경우 — HWP 표가 깨집니다

`L3_2024창업중심대학_경상국립대_사업비사용안내문.hwp` 로 실측했습니다.

### 5-1. 증상

```
211| 재료비
212| 기
213| 준            ← 「기준」이 세로로 쪼개짐
214|  - 재료는 시제품의 부속품…
219| 유
220| 의
221| 사
222| 항            ← 「유의사항」이 네 글자로
```

**글자는 한 자도 안 빠집니다**(공백 제외 대조 결과 손실 0자). 문제는 다릅니다 —
**행·열 구분자가 없습니다**(`|` 0개·탭 0개).

문서 전체에서 비목/헤더 등장 순서:

```
434 비목 재료비   436 헤더 기준   448 헤더 유의사항
468 헤더 기준     ← 🔴 비목 없이 헤더만
551 헤더 기준     ← 🔴 또    583 ← 🔴 또    607 ← 🔴 또
```

표에서 비목 칸이 **세로 병합**돼 왼쪽에 한 번만 쓰이는데, 셀을 세로로 풀면 그 연결이
끊깁니다. **「500만원 이상 사전승인」이 어느 비목 것인지 사라집니다.**
LLM이 순서로 추측하게 되고, 틀리면 **기계장치 기준을 재료비에 적용**하는 오답이
나옵니다 — 그런데 근거 조문이 붙어 나가니 **맞는 것처럼 보입니다.**

`convert_hwp.py` 주석과 같은 얘기입니다:
> 세부관리기준은 **한도·증빙이 표에 있어서 표 손실은 치명적이다.**

### 5-2. 원인

| 추출기 | 대상 | 표 처리 |
|---|---|---|
| `hwp_extract.py` (84줄) | **구형 `.hwp`(OLE)** | 🔴 **없음** — PARA_TEXT 레코드만 훑음 |
| `hwpx_extract.py` | `.hwpx` | ✅ `_walk_table()` |
| `docx_extract.py` | `.docx` | ✅ `_walk_table()` |

`stage0_extract.extract_hwp()` 는 매직바이트로 **분기만** 하고 변환하지 않습니다.

참고로 `table_splice` 는 이 경로에 안 걸립니다 — 그 자신의 주석대로
「L3 업로드(doc_id 가 UUID 라 `_tables.json` 에 없다)는 조용히 통과」하고,
`_tables.json` 은 조달 PDF 4개 전용입니다.

### 5-3. 해법 — 검증했습니다

```bash
curl -L -o h2o.oxt \
  https://github.com/ebandal/H2Orestart/releases/download/v0.7.2/H2Orestart.oxt
unopkg add --shared h2o.oxt

cp 원본.hwp /tmp/src.hwp        # 🔴 ASCII 파일명 (아래 함정 ①)
soffice --headless --norestore --convert-to docx --outdir /tmp/out /tmp/src.hwp
```

변환본을 `stage0_extract.extract()` 로 뽑은 결과:

```
211| 재료비          ← 1행 1열
212| 기  213| 준     ← 1행 2열 헤더
214|  - 재료는 시제품의 부속품…
218| 재료비          ← 🔴 «다시 나옴» — 세로 병합 셀이 행마다 반복
219| 유 220| 의 221| 사 222| 항   ← 2행 2열 헤더
```

**218번 줄이 핵심입니다.** 세로 병합된 「재료비」가 각 행에 반복돼 나와서,
「유의사항」 블록의 주인을 텍스트만 보고 알 수 있습니다.

`docx_extract.py` 가 「알려진 한계」로 적어둔 동작이 여기선 정확히 이득입니다:
> ⚠️ 세로 병합 셀은 원본 저장 도구에 따라 **이어붙은 칸에도 같은 텍스트가** …

### 5-4. 함정 셋

**① 파일명 한글이 깨집니다**
```
java.io.FileNotFoundException: /tmp/L3_???????????.hwp
  at HwpDoc.OCFdoc.OwpmlFile.open(OwpmlFile.java:57)
```
H2Orestart Java쪽 인코딩 문제. **ASCII 임시 파일명으로 복사 후 변환**하면 됩니다.
서버는 이미 `{doc_id}.hwp`(UUID)로 저장하므로 실제 경로에선 안 걸립니다.

**② 🔴 단락 분할이 깨집니다 — 같이 고쳐야 합니다**

```
                    빈 줄(\n{2,})    조 건수
원본 .hwp                 969          203
docx 변환본                 0            1   🔴
```

`split_articles` 의 단락 폴백이 `re.split(r"\n{2,}", text)` 인데
`docx_extract` 는 문단마다 **단일 개행**만 넣습니다. 전체가 한 덩어리가 되어
조 1건(27,000자)이 됩니다.

같은 100자 누적 규칙을 **단일 개행 기준**으로 적용하면 **209건** — 원본 수준(203)으로
돌아옵니다. 「빈 줄이 없으면 단일 개행으로 자른다」 한 갈래만 추가하면 됩니다.

**③ 변환 손실 16자**
```
원본 hwp (공백 제외)  20,136
docx 변환(공백 제외)  20,389   (병합셀 반복 +269, 빠짐 −16)
```
빠진 16자는 **표지 제목**(「2024년 경상국립대학교 창업중심대학…」)입니다.
LibreOffice가 텍스트박스로 렌더한 것으로 보입니다. 판정 구간이 아니라 영향은 없지만
**0은 아니라는 점**은 남깁니다.

### 5-5. 배선 위치

```python
# scripts/stage0_extract.py :: extract_hwp()  — OLE 갈래 «직전»
kind = sniff(path)
if kind.startswith("HWP-DRM"):
    raise HwpProtectedError(f"{kind} ({path.name})")

# ── 추가 ────────────────────────────────────────────────
# 🔴 구형 .hwp 는 표 구조가 없다. LibreOffice+H2Orestart 로 .docx 를 거쳐
#    docx_extract 의 표 걷기를 태운다. 실패하면 기존 경로로 폴백.
변환 = _libreoffice_docx(path)      # ASCII 임시명 복사 후 soffice --convert-to
if 변환:
    return extract_docx(변환)
# ────────────────────────────────────────────────────────

from hwp_extract import extract
return extract(str(path))
```

받는 쪽(`docx_extract._walk_table`)이 이미 표를 걷습니다. **새로 만들 게 없습니다.**

---

## 6. 문서 선택 — 이게 제일 큰 변수입니다

시연에 쓰신 `사업비사용안내문.hwp` 는 **규정이 아니라 안내문**입니다.

```
Ⅰ. 사업개요 / Ⅱ. 사업비 사용안내 / [첨부1~4]
```

「제○조」 7건은 **전부 타 규정 인용**(「제35조 외주용역비 참고」, 「민법 제767조」 등)이고
**자기 조문이 0개**입니다. 그래서 조문 분해 5단 전략이 전부 미달하고 단락 폴백으로
떨어집니다 — 파서 버그가 아니라 **형식이 다릅니다.**

결과적으로 `조번호 = 단락047` 이 되어 §4의 두 가지가 다 무너집니다:
- 인용 라벨이 사용자에게 무의미
- `조제목`이 없어 게이팅이 안 걸림 → `1-미규정`

**「창업중심대학 세부관리기준」 원본**을 쓰시면 이 문제가 통째로 사라집니다.
이 안내문의 [첨부1][첨부2]가 그 기준을 발췌한 것이라, 원본에는 「제N조(비목명)」
구조가 있을 가능성이 높습니다.

> 원본을 주시면 같은 방식으로 돌려서 조 분해가 되는지·표가 사는지 바로 확인해드립니다.

---

## 7. 「파싱 형태 적재」를 택하실 경우 — 가장 안전한 길

문서를 파서에 태우지 않고 **조문을 손으로 정리해 `l3_articles` 에 직접 INSERT** 하는 방법.
시연 범위가 좁다면 이쪽이 확실합니다.

- 표 깨짐·단락 분할 함정을 **전부 우회**합니다
- `조번호`·`조제목`을 원하는 대로 통제할 수 있어 §4의 두 조건을 정확히 맞춥니다
- 비목 5~6개만 넣어도 게이팅 4갈래가 전부 시연됩니다
  (`불가` 하나, `조건부` 하나, `가능` 하나, 나머지는 미규정)

⚠️ 다만 `seed_l3_fixture.py` 머리말의 경고는 지켜야 합니다:
> 합성 조문이 실기관 규정과 섞이는 순간, 이 서비스가 사용자에게 **존재하지 않는 규정을**
> 말하게 된다.

**실제 문서의 조문을 그대로 옮기는 건 괜찮고, 없는 조문을 지어내면 안 됩니다.**

---

## 8. 프론트가 하는 것 / 안 하는 것

| | |
|---|---|
| L3 업로드 화면 | ❌ 안 붙입니다 |
| L3 파싱 상태 표시 | ❌ 안 붙입니다 |
| 판정 결과의 근거 표시 | ✅ 이미 붙어 있습니다 — **L3 조문이 오면 그대로 「적용 근거」에 뜹니다** |

프론트는 근거의 출처가 L1인지 L3인지 구분하지 않고 `조번호`·`조제목`·`원문` 을
그대로 그립니다. **적재만 되면 프론트 작업 없이 화면에 나타납니다.**

한 가지 정리할 게 있습니다 — 마이페이지의 「적용 기준 · 2026 경상국립대학교 …
집행 안내.pdf」는 **DB에 없는 표시용 하드코딩**입니다. 실제 적재가 끝나면 그 자리를
실제 문서명으로 바꾸거나, 그 전까지는 표시를 빼겠습니다.

---

## 9. 요청 정리

| # | 항목 | |
|---|---|---|
| 1 | 시연용 L3 문서 적재 (org `cfeba091-…`, `status='active'`) | |
| 2 | `조제목`에 비목명이 들어가게 | §4 — 없으면 게이팅이 안 걸림 |
| 3 | `조번호`를 「제N조」 형태로 | §4 — 화면 인용 라벨 |
| 4 | 문서 경로로 가실 경우 §5-5 변환 + §5-4② 단락분할 **세트로** | 하나만 하면 조 1건 |
| 5 | 가능하면 「세부관리기준」 원본 사용 | §6 |

적재되면 알려주세요. 판정 결과에 L3 조문이 실제로 인용되는지 프론트에서 확인하겠습니다.
