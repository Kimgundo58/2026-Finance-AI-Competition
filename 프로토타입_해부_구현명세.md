# 프로토타입 해부 — 백엔드 구현 명세

> 대상: `https://checkumait-user-clean.yeemmin.chatgpt.site/`
> 작성: 2026-09-01 · 근거: 실물 코드·DB 실측 (`server/main.py` · `db/init/*.sql` · `scripts/`)
>
> **이 문서의 목적은 "무엇을 만들 것인가" 를 화면 단위로 못박는 것이다.**
> 판정 표기는 `프론트 연동 사양.md` 가 정본이고, 여기는 **구현 지시**다.

---

## 0. 이 문서가 이전 판단 2건을 뒤집는다

`프론트 프로토타입 검토.md`(2026-08-31)는 프로토타입을 백엔드 계약 기준으로 전수 판정했다.
그 문서의 결론 중 **둘이 오늘 뒤집혔다.** 나머지는 그대로 유효하다.

| # | 어제 결론 | 오늘 | 뒤집은 이유 |
|---|---|---|---|
| 1 | step1 폼 -> **자연어 textarea 로 교체** | 🔴 **폼 유지** | 근거였던 "`item_alias` 학습 소실" 이 성립하지 않는다 (§1-1) |
| 2 | step3 질문뱅크 -> **전체 제거** | 🔴 **제거 아님 — 위치와 형태를 바꾼다** | 요구는 "판정 전 60문항" 이 아니라 "판정 후 맥락 기반 확인사항" 이다 (§2) |

**그대로 유효한 것** — 세목·세세목 제거 / AI 챗봇 제거 / 건별 첨부 제거 /
4-way enum 전면 교체 / 판단불가 경로 신설 / F 프로필 신설.

---

## 1. 방향 전환 (1) — 입력은 폼이다

### 1-1. 어제 근거가 왜 틀렸나

어제 검토의 ❌ 사유는 *"폼으로 다 받으면 컨펌 로그 기반 `item_alias` 학습 경로가 사라진다"* 였다.

```
corpus.item_alias (상품명, 비목, 사업명, 출처, embedding)
                                        +- seed | 사용자질문 | 센터답변
```

이 표가 배우는 건 **`상품명 -> 비목`** 이다. 그런데 폼에도 「지출 항목」칸이 있어 상품명은
그대로 들어오고, **비목은 폼에서도 묻지 않는다**(AI 추천 -> 사용자 확정). 학습쌍
`(맥북 -> 기계장치)` 는 폼에서도 똑같이 생긴다. **소실되지 않는다.**

### 1-2. 폼이 오히려 유리한 지점

하위 계약이 이미 필드 모양이다:

```python
정규화 출력  {품목, 금액, 금액_추정여부, 용도, 비목후보[], 누락필드}
rule_lookup.비목확정(cur, 품목, 사업명)
rule_lookup.금지적중(cur, 품목, 용도, 사업명, 비목)
```

폼이면 `금액_추정여부` 가 **항상 false** 다. 자연어 경로에서 "맥북 한 대 정도" 를 LLM 이
추정하다 틀리면 **한도 비교가 통째로 틀린다**. 그 오답 경로 하나가 사라진다.

### 1-3. 확정 필드 구성

| 필드 | 필수 | 형식 | 백엔드 소비처 |
|---|:--:|---|---|
| 사업명 | 🔴 | 드롭다운 8종 (`GET /api/programs`) | 룰 조회 키의 절반 |
| 지출 항목 | 🔴 | text | `비목확정(품목,…)` · `금지적중(품목,…)` |
| 금액 | 🔴 | 숫자 | `금액비교()` |
| 사용 목적 | 🔴 | text | `금지적중(…, 용도, …)` |
| 예상 집행일 | | date | 협약기간 판정 |
| 거래처 | | text | 저장만 (누적 2천만원 심의). **판정 미사용 — 라벨에 명시** |
| F5 친족거래 | | 체크 | 특수관계. 판정 후 폐기 |
| F5 전직임직원업체 | | 체크 | 동일 |
| **추가 설명** | | textarea | 🆕 `용도` 에 합류. 폼이 못 담는 예외 맥락 |
| ~~비목~~ | — | — | 여기서 묻지 않는다. step2 에서 추천 -> 확정 |

### 1-4. 세목·세세목 17종 — 제거 확정

`corpus.item_vocab` 실측상 **하위항목을 가진 비목은 지급수수료 하나뿐**이고 17종이 있다.
그리고 `금액비교(rule, 수치, 하위항목)` 에 소비처가 **실제로 있다**:

```
"이 한도는 {한정} 에만 걸리는데 하위항목이 확정되지 않았다"
  -> 멘토링비 한도를 사무실임차료에 걸면 틀린 초과가 된다
```

**그래도 폼 필드로는 만들지 않는다.** 하위항목은 사용자가 고를 것이 아니라
**`품목`·`용도` 에서 정규화가 뽑을 값**이다. 17개 중 고르라 하면 사용자가 틀리게 고른다.

> 🔴 **백엔드 할 일**: 정규화 출력에 `하위항목: str | None` 을 추가하고, 지급수수료로
> 분류될 때만 17종 매칭을 시도한다. 못 정하면 `null` 로 두고 `금액비교()` 가
> "하위항목 미확정" 으로 한도를 보류하게 둔다 — 이미 그렇게 동작한다.

---

## 2. 방향 전환 (2) — 「확인필요」 흐름을 채택한다

### 2-1. 요구사항 원문

> F축이건 사용자의 기본 세부조건에 따라 판정해줄 수 있는 요인들을 LLM이 문장들 구성해서
> 물어보는 것. 「확인필요」 버튼은 사용자가 나중에 확인하는 데 시간이 걸릴 것 같은 걸
> 눌러놓고, 홈화면에서 「조건부」로 그 건이 뜨고, 누르면 「결제 전 확인필요」에 작성된다.
>
> 설명은 *"내부 외부 참석자를 구분해주세요"* 같은 게 아니라
> **"현재 사용자 상태가 ~~니까, ~~기준에 따르면 ~~를 확인하셔야 합니다"** —
> 사용자 context + 근거 기반으로 신뢰감 있게.

### 2-2. 이 흐름은 이미 DB에 모델링돼 있다

**새 테이블이 필요 없다.**

```
corpus.check_items  52행  — 확인항목 마스터
    code · 사업명 · 비목 · 구분 · 항목 · 설명 · 기본_오프셋일 · 근거 · verified

tenant.plan_tasks         — 지출계획 1건에 붙는 확인항목 인스턴스
    출처   ai | user             <- AI 생성 / 사용자 추가
    구분   결제전 | 결제후 | 집행  <- 「결제 전 확인필요」 = 구분='결제전'
    상태   준비필요 | 집행예정 | 완료
    due_date                     <- NULL 이면 체크리스트만, 값 있으면 캘린더에도
    코드   -> check_items(code)  <- 재판정 때 같은 항목인지 알아보는 키
    날짜_사용자수정 BOOLEAN       <- true 면 재판정이 날짜를 덮지 않는다
```

스키마 주석이 이미 이렇게 적어놨다:

> 🔴 `user` 는 재판정이 절대 건드리지 않는다
> 체크리스트와 캘린더는 **같은 행을 다르게 거르는 것**이지 서로 다른 레코드가 아니다.
> 따로 두면 상세에서 체크했는데 캘린더는 "준비 필요" 로 남는 식으로 반드시 어긋난다.

### 2-3. 확인항목 마스터 실물 (52행 중 6)

```
성공보수아님      결제전  성공보수가 포함되지 않았는지 확인하세요
4대보험가입       결제전  4대사회보험에 가입돼 있는지 확인하세요
타사업중복아님    결제전  타 정부지원사업에 등재된 직원이 아닌지 확인하세요
전대차아님확인    결제전  사무실이 전대차 계약이 아닌지 확인하세요
보증금관리비제외  결제전  임차보증금과 관리비는 빼고 신청하세요
급여증빙제출      결제후  급여대장과 입금확인증을 매달 제출하세요
```

**이게 어제 검토가 「제거」하라던 60문항의 정체다.** 질문 뱅크는 없애는 게 아니라
**마스터 테이블로 이미 정규화돼 있었고**, 프론트가 물을 게 아니라 **판정이 골라주는 것**이다.

### 2-4. 🔴 진짜 결손 — 「설명」이 정적 문자열이다

```
지금  check_items.설명 = "미가입자는 인건비를 집행할 수 없습니다."     <- 고정 문구

원함  "현재 신규채용 2명 중 1명이 4대보험 미가입 상태로 보이고,
       통합관리지침 제41조가 미가입자 인건비를 금지하므로,
       해당 인력의 자격취득신고 여부를 결제 전에 확인하셔야 합니다."
```

**차이는 셋이다:** (1) 사용자 상태 인용 (2) 근거 조항 인용 (3) 구체적 행동 지시.

### 2-5. 재료는 이미 프롬프트에 들어가고 있다

```python
# scripts/orchestrate.py:542
조립(..., 룰결과=b4_문장(룰), f요약=b5_문장(cur, org_id), ...)

# scripts/assemble_context.py
B4  룰 조회 결과 (금액 비교는 이미 끝났다)
B5  협약·집행 현황          <- 여기가 「현재 사용자 상태」
```

**즉 LLM 은 이미 사용자 상태를 보고 있다.** 안 하는 이유는 능력이 아니라
**프롬프트가 그렇게 써 달라고 요구하지 않아서**다.

### 2-6. Qwen3 32B 가 할 수 있는가 — 할 수 있다. 조건 셋

| | 판단 |
|---|---|
| 능력 | ✅ **충분하다.** "주어진 상태 + 주어진 조항 -> 조건절 한 문장" 은 추론이 아니라 **재구성**이다. 32B 에게 어려운 과제가 아니다 |
| 🔴 출력 길이 | **`max_tokens` 를 다시 재야 한다.** 1500 에서 이미 77문항 중 5건이 잘렸다. 항목마다 3문장 설명을 붙이면 출력이 크게 는다. **3000 으로도 모자랄 수 있다** |
| 🔴 환각 위험 | 게스트는 **F축이 비어 있다.** 그때 "현재 사용자 상태가 ~~니까" 를 쓰면 **없는 상태를 지어낸다** |
| 🔴 스키마 | `해야할일[{항목,설명}]` 에 `코드` 가 없다. `plan_tasks.코드` 로 이어붙일 키가 없어 재판정 때 같은 항목인지 못 알아본다 |

> **가드 규칙 (프롬프트에 못박을 것)**
> B5 가 비었으면 **상태절을 쓰지 않는다.** "협약 정보가 등록되지 않아 일반 기준으로
> 안내합니다" 로 시작하고 근거 조항만 인용한다. **없는 상태를 문장으로 만들지 않는다.**
> 이건 "모든 실패의 기본값은 판단불가" 와 같은 계열의 규칙이다.

---

## 3. 화면별 해부

표기 — ✅ 지금 붙여도 됨 / 🟡 데이터는 있으나 후속 필요 / ⏸ 설계 미결 / ❌ 만들지 말 것 / 🆕 신설

### P0. 랜딩

| 컴포넌트 | | 백엔드 |
|---|:--:|---|
| 서비스 소개 · CTA | ✅ | 프론트 자체 |
| 로그인 / 게스트로 시작 | 🟡 | 게스트 경로는 확정(`org_id=NULL`). 로그인은 ⏸ 개인정보 처리방침 후 |

### P1. 온보딩 1/4 — 사업 선택

| 컴포넌트 | | 백엔드 |
|---|:--:|---|
| 사업 8종 선택 | ✅ | `GET /api/programs` — 사업명·별칭·비목계통·트랙범위 |
| | 🔴 | **필수다.** 룰 조회 키가 `사업 x 비목` 이라 없으면 한도를 못 고른다. 게스트도 반드시 |
| TIPS 선택 시 | 🔴 | **`corpus.rules` 에 TIPS 0행.** 판단불가로 닫힌다 — 프론트가 버그로 오해하지 않도록 사전 공지 (§7) |

### P2. 온보딩 2/4 — 주관기관 선택

| 컴포넌트 | | 백엔드 |
|---|:--:|---|
| 기관 검색·선택 | 🟡 | `tenant.orgs` 현재 2행(픽스처). 기관 마스터 적재 필요 |
| "기관을 모르겠어요" | ✅ | `org_id=NULL` 게스트 경로. L3 없이 L1·L2 로만 판정 |
| ⏸ 주관기관 **화자** 지원 | ⏸ | 🔴 **오너 결정 대기.** 골든셋 17문항이 주관기관 화자인데 필터가 `적용대상 IN ('창업기업','공통')` 이라 구조적으로 못 받는다. 지원하면 여기 "당신은 누구인가" 칸이 하나 는다 |

### P3. 온보딩 3/4 — 기관 세부기준 등록 (L3 업로드)

| 컴포넌트 | | 백엔드 |
|---|:--:|---|
| 파일 업로드 | 🆕 | 🔴 **업로드 API 미구현.** `POST /api/l3/upload` 신설 필요 |
| PDF | 🟡 | `pdftext.py::extract()` 경유 — 있음 |
| HWPX · HWP | 🆕 | 🔴 **파서 미구현.** HWPX 는 XML 직파싱, HWP 5.0 은 별도. DRM 배포용은 "PDF 변환 후 재업로드" 안내 |
| 20MB 제한 | ✅ | 무방 |
| "파일 없이 계속" | ✅ | 게스트 경로와 동일 |
| dangling 알림 | 🟡 | `l3_documents.dangling수` 컬럼 있음. **업로드 시점에 알린다** (판정 시점 아님) |

**저장처는 이미 있다** — `tenant.l3_documents` / `tenant.l3_articles`(현재 픽스처 17행).
소비 함수도 있다 — `l3_load.로드()` · `rule_lookup.effective_rule(…, 기관ID)`.

### P4. 온보딩 4/4 — 완료

| 컴포넌트 | | 백엔드 |
|---|:--:|---|
| 등록/미등록 분기 문구 | ✅ | 프론트 자체 |

### 공통 셸 — 사이드바

| 컴포넌트 | | 백엔드 |
|---|:--:|---|
| 네비게이션 | ✅ | 프론트 자체 |
| 게스트 배지 · 로그인 유도 | 🆕 | 신설 |

### P5. 홈 대시보드

| 컴포넌트 | | 백엔드 |
|---|:--:|---|
| 최근 지출계획 목록 | 🟡 | `tenant.expense_plans` (현재 0행) |
| **「조건부」 건 노출** | 🆕 | 🔴 **사용자 요구 핵심.** `plan_tasks.상태='준비필요' AND 구분='결제전'` 인 계획을 모은다 (§4) |
| 집행 현황 요약 | 🟡 | F1·F3 기반 — F 프로필 신설 전제 |
| 4-way 상태 뱃지 | ✅ | `가능 · 조건부 · 불가 · 판단불가` 폐쇄 enum |

### P6. 지출 계획 목록

| 컴포넌트 | | 백엔드 |
|---|:--:|---|
| 목록·검색·정렬 | 🟡 | `expense_plans` 승격 컬럼(확정비목·금액·집행예정일·거래처·사업명)이 정렬·필터용 |
| 검색 | 🟡 | `질문원문` 을 뒤진다 -> §6-1 참조 |
| 상태 필터 | ✅ | 4-way |
| CSV 출력 | 🟡 | 프론트 자체. ❌ `현물 신청 가능금액` 컬럼은 불가 (현물 필드 제거됨) |

### P7. 새 지출 계획 — 위저드

**step1 기본 정보 — 🔴 폼 유지 (§1-3 표대로)**

| 컴포넌트 | | 지시 |
|---|:--:|---|
| 지출항목/사용목적/금액/집행일 개별 칸 | ✅ | **유지.** 어제 ❌ 를 뒤집는다 |
| 거래처 칸 | 🟡 | 유지하되 **"판정에는 쓰이지 않습니다"** 를 helper text 로 |
| 첨부파일 (견적서 등) | ❌ | **제거.** 사전승인 판정이라 견적서가 판정을 바꾸지 않는다. 정산은 스코프 밖 |
| F5 체크 2문항 | ✅ | 유지 |
| **추가 설명 textarea** | 🆕 | 신설. 폼이 못 담는 맥락 |

**step2 비목 확인**

| 컴포넌트 | | 지시 |
|---|:--:|---|
| AI 비목 추천 + 직접 변경 | ✅ | `비목후보[{비목,신뢰도}]` · **최종 선택은 사용자** |
| 비목 목록 | 🟡 | **enum 10종 문자열 그대로.** 프로토타입의 "기계장치비"·"회의비" 표기 교정 |
| 창업활동비 | 🟡 | **예비창업패키지에만 노출** (`GET /api/vocab?사업명=`) |
| 비목 갈릴 때 두 경로 병렬 | 🟡 | 컴포넌트는 짓되 **플래그로 끈다.** 임계치 실측 대기 |
| ~~세목·세세목 17종~~ | ❌ | **제거 확정** (§1-4) |

**step3 — 🔴 「예/아니오 질문뱅크」를 「확인필요 체크리스트」로 교체**

| 컴포넌트 | | 지시 |
|---|:--:|---|
| ~~60문항 예/아니오~~ | ❌ | 판정 **전에** 묻지 않는다 |
| **확인필요 체크리스트** | 🆕 | 판정 **후에** 나온다. `해야할일[]` + `전제[]` 렌더 (§4) |
| **「확인필요」 토글** | 🆕 | 🔴 신설. `plan_tasks.상태` 를 바꾼다 |
| 저장하고 AI 점검 | ✅ | `normalize -> (컨펌) -> judge` |

### P8. 지출 상세

| 컴포넌트 | | 백엔드 |
|---|:--:|---|
| 계획 정보 카드 | ✅ | 정규화 결과 + 확정비목 |
| **상태 뱃지** | 🔴 | 4-way 폐쇄 enum 으로 **전면 교체**. 프로토타입엔 판단불가·불가가 없다 |
| **판단불가 -> 화면 9** | 🆕 | 판단불가는 에러가 아니다. 빨간 화면 금지, 문의 초안으로 |
| AI 해설 | ✅ | `요약` |
| **결제 전 확인 카드** | 🆕 | `해야할일[구분='결제전']` + **맥락 기반 설명** (§4) |
| 적용 근거 카드 | ✅ | `인용[{조번호,조제목,원문,doc_id}]` — **원문까지 그린다** |
| 신뢰등급 A/B | 🆕 | 코드가 산정 |
| 버전스탬프 | 🆕 | "제14차, 2025.12.23 기준" |
| 참조사슬 | 🆕 | `참조사슬[{from,표기,관계,to,보정}]` |
| 전제[] | 🆕 | `[{사실, 근거조항, 매핑, 미충족시}]` — F 필드에 있으면 **인라인 입력** |
| ⏸ 조항별 해당/비해당 | ⏸ | (4)-a 미결. **레이아웃만 잡고 비운다** |
| 필수/권장 구분 | ⏸ | 스키마에 없음 |
| 확률·% 표시 | ✅ | **없음 — 만들지 말 것** |
| 면책 문구 | ✅ | 유지 |
| 첨부 업로드/다운로드 | ❌ | 제거 |
| 증빙 파일 업로드 | ❌ | 정산 스코프 밖 |
| 결제 후 증빙 목록 | 🟡 | `rules.증빙` -> `evidence_sources` 발급처. 룰 19행 미검수 |

### P9. AI 챗봇 — ❌ 화면 전체

확정 원칙 위반이다. 판정 1건 = LLM 2회 고정, function calling 미개방,
agentic multi-hop 미채택(재제안 금지). **자유 QA 슬롯은 존재하지 않는다.**

대체 — 추천 질문 칩은 **화면 3의 예시 칩**으로 옮기고, 누르면 판정이 시작되게.

### P10. 집행 일정 (캘린더)

| 컴포넌트 | | 백엔드 |
|---|:--:|---|
| 캘린더 뷰 | 🟡 | 🔴 **어제 ❌ 였으나 재검토 대상.** `plan_tasks.due_date` + 부분 인덱스 `ix_tasks_calendar` 가 **이미 있다.** 「확인필요」에 날짜를 붙이는 순간 캘린더는 같은 테이블의 다른 필터일 뿐이다 |
| 알림(3일 전) | ❌ | 발송 인프라 없음 |

### P11. 규정·지침

| 컴포넌트 | | 백엔드 |
|---|:--:|---|
| 규정 목록 | 🟡 | `corpus.documents`. 단 **판정 인덱스 경계**를 넘지 말 것 |
| 원문 뷰어 | ⏸ | `doc_id` 는 주나 뷰어 미설계 |

### P12. 내 정보

| 컴포넌트 | | 백엔드 |
|---|:--:|---|
| 계정 정보 | 🟡 | `tenant.accounts` |
| **F 프로필 탭** | 🆕 | 🔴 **신설.** F1 협약(현금 2칸+기간) / F3 집행내역 / F4 인력 |
| | 🔴 | **이름 입력칸 금지.** 역할·수치만 (`f_personnel.역할` CHECK 3종) |
| PMS 붙여넣기 파서 | 🆕 | 브라우저 정규식. **PMS 원문은 서버로 보내지 않는다** |

---

## 4. 「확인필요」 흐름 — 전용 명세

### 4-1. 생애 주기

```
[판정]   judge 응답의 해야할일[] 이 온다
           {코드, 구분, 항목, 설명, 기본_오프셋일}
             |
             v
[적재]   POST /api/plans/{plan_id}/tasks:sync
           check_items 마스터와 대조해 plan_tasks 에 출처='ai' 로 적재
           🔴 출처='user' 행은 건드리지 않는다
             |
             v
[사용자] 「확인필요」 토글 -> 상태='준비필요' 유지 + due_date 지정
           날짜를 직접 고치면 날짜_사용자수정=true
             |
             v
[홈]     plan_tasks.상태='준비필요' AND 구분='결제전' 인 계획을
         「조건부」로 모아 보여준다
             |
             v
[상세]   클릭 -> 「결제 전 확인필요」 섹션에 맥락 기반 설명과 함께 렌더
             |
             v
[재판정] 입력이 바뀌면 재판정. 코드로 대조해 ai 행만 갱신
```

### 4-2. 🔴 재판정 규칙 (여기가 제일 틀리기 쉽다)

```
출처='user'            절대 건드리지 않는다 (스키마 주석에 명시)
출처='ai' + 코드 일치   설명을 갱신한다. 상태는 유지
출처='ai' + 코드 소멸   삭제한다 (더 이상 해당 없는 항목)
날짜_사용자수정=true    due_date 를 덮지 않는다
상태='완료'             재판정이 '준비필요' 로 되돌리지 않는다
```

### 4-3. 설명 생성 계약 — LLM (4) 슬롯

**입력 (이미 프롬프트에 있음)**

```
B4   룰 조회 결과 — 한도·사전승인·증빙. 금액 비교는 끝나 있다
B5   협약·집행 현황 — 「현재 사용자 상태」의 출처
인용  S번호        — 「~~기준에 따르면」의 출처
```

**출력 형태**

```
[상태절]  현재 <B5에서 확인된 사실>이고,
[근거절]  <S번호 조항>이 <내용>을 규정하므로,
[행동절]  <무엇을> 결제 전에 확인하셔야 합니다.
```

**🔴 가드 3개 (프롬프트에 못박는다)**

1. **B5 가 비면 상태절을 쓰지 않는다.** "협약 정보가 등록되지 않아 일반 기준으로
   안내합니다" 로 대체. **없는 상태를 지어내지 않는다**
2. **근거절은 S번호로만.** 원문을 옮겨 쓰지 않는다 (기존 원칙 3과 동일)
3. **행동절은 반드시 사용자가 할 수 있는 행위.** "확인하세요" 가 아니라
   "무엇을 어디서 확인하세요"

### 4-4. 스키마 변경 1건 (필수)

```
지금  해야할일: [{항목, 설명}]
필요  해야할일: [{코드, 구분, 항목, 설명, 기본_오프셋일}]
```

`코드` 없이는 `plan_tasks.코드` 로 이어붙일 수 없고, **재판정 때 같은 항목인지 못 알아본다.**
`구분` 없이는 결제전/결제후를 못 가른다.

> 🔴 **둘 다 `check_items` 에 이미 있는 값이라 LLM 이 만드는 게 아니라 코드가 붙여 준다.**
> LLM 은 `설명` 만 쓴다. `코드`·`구분` 을 LLM 에게 생성시키면 enum 밖 값이 나온다 —
> 어제 B 세션이 `허용='None'` 이 흘러가던 걸 잡은 것과 같은 유형의 사고다.

---

## 5. API 명세

### 5-1. 불변 (지금 프론트에 넘겨도 됨)

```
GET  /api/health                    판정_enum · 모드
GET  /api/vocab?사업명=             비목 10종 + 별칭
GET  /api/programs                  사업 8종
GET  /api/profile?org_id=           F 프로필
PUT  /api/profile
POST /api/judge                     SSE. 응답 스키마 동결
       진행* -> 판정 -> 해야할일 -> 인용 -> 전제 -> 참조사슬 -> 결과 -> 완료
       판단불가면 문의초안 추가
       ?목=가능|조건부|불가|판단불가 로 4-way 전부 렌더 가능
```

**목 서버가 기본값이다** (`SUDDOE_MOCK=1`). DB·GPU 없이 뜬다 — 프론트는 오늘 착수 가능.

### 5-2. 변경

```
POST /api/normalize
  지금  {질문: str, 사업명, f5}
  변경  {품목, 금액, 용도, 집행예정일?, 거래처?, 추가설명?, 사업명, f5}
        + 질문: str  (자연어 경로 병행 유지 — 골든셋 재현용)
        검증: 질문 XOR 폼 필드
  출력  기존 + 하위항목: str|null
```

### 5-3. 신설

```
POST   /api/l3/upload                 L3 규정 업로드 (multipart)
                                      -> l3_documents + l3_articles, dangling 즉시 보고
POST   /api/plans                     지출계획 저장
GET    /api/plans?상태=&사업명=        목록
GET    /api/plans/{id}                상세
POST   /api/plans/{id}/tasks:sync     판정 결과 -> plan_tasks 적재 (ai 행만)
PATCH  /api/plans/{id}/tasks/{tid}    상태·due_date 변경 (「확인필요」 토글)
POST   /api/plans/{id}/tasks          사용자 직접 추가 (출처='user')
GET    /api/tasks?상태=준비필요        홈 「조건부」 집계
```

---

## 6. DB 변경 대장

### 6-1. 🔴 필수 1건 — `expense_plans.질문원문 NOT NULL`

폼 경로에는 "질문 원문" 이 없다. 두 방법 중 **(b) 권장**:

| | 방법 | 평가 |
|---|---|---|
| (a) | NOT NULL 해제 + `입력방식` 컬럼 신설 | 스키마 변경 2건. 목록 검색이 빈 값을 만난다 |
| **(b)** | **폼 값을 문장으로 합성해 저장** | **스키마 변경 0.** 검색이 살고 자연어 경로와 비교 가능 |

```
품목=맥북 금액=2500000 용도=디자이너 작업용 사업명=초기창업패키지
  -> "초기창업패키지에서 디자이너 작업용 맥북 250만원을 사도 되나요?"
```

⚠️ **합성 문장을 다시 LLM 입력으로 쓰지 않는다.** 필드 -> 문장 -> 필드 왕복은 정보를 잃는다.
저장·검색·표시 전용이다.

### 6-2. 선택

```
expense_plans.용도       컬럼 승격 (지금 정규화 JSONB 안). 목록 필터링용이면
expense_plans.추가설명   신설 (자유 텍스트 칸을 만들면)
expense_plans.하위항목   신설 (지급수수료 한도 판정 추적용)
```

### 6-3. 이미 있어서 안 만들어도 되는 것

```
expense_plans.사업명            어제 추가
golden_set.비목 · 입력필드      어제 추가 (폼 경로 정본용)
l3_documents · l3_articles      L3 저장처
check_items 52행                확인항목 마스터
plan_tasks                      「확인필요」 상태 저장처 + 캘린더
f_personnel.역할 CHECK          3종 폐쇄
```

---

## 7. 프론트에 반드시 미리 알릴 것

실서버(`SUDDOE_MOCK=0`)에 붙는 날 **버그로 오해할 것이 확실한 지점 2개**:

1. **「가능」 판정이 거의 안 나온다.** `corpus.rules` 72행 중 **19행이 미검수**라
   단독 「가능」이 안 나가게 막혀 있다 (안전장치, 의도된 동작).
   미검수 19행 = 공통(L1) 9 + 재도전 9 + 모두의창업 1 — 전부 어제 새로 만든 행이다
2. **TIPS 를 고르면 판단불가로 닫힌다.** `rules` 에 TIPS 0행

> **UI 개발은 목으로, 실서버는 연동 스모크용으로.**

---

## 8. 구현 순서

| # | 작업 | 규모 | 선행 |
|---|---|---|---|
| 1 | `max_tokens` 1500 -> 3000 | 한 줄 | — |
| 2 | 폼 정규화 계약 (`정규화요청` + `_실_정규화` 분기) | 반나절 | — |
| 3 | **연동 사양 v1.0 동결 -> 프론트 전달** | — | 2 |
| 4 | `해야할일[]` 에 `코드`·`구분` 추가 (코드가 붙임) | 반나절 | — |
| 5 | (4) 프롬프트에 맥락 설명 요구 + 가드 3개 | 반나절 | 1, 4 |
| 6 | `golden_set.입력필드` 77문항 채우기 | 1일 | 2 |
| 7 | plan_tasks API 4종 (sync/patch/post/list) | 1일 | 4 |
| 8 | 지출계획 CRUD API | 1일 | 2 |
| 9 | L3 업로드 API + HWPX 파서 | 2일 | — |

🔴 **1번은 순서가 아니라 선행조건이다.** `max_tokens=1500` 인 채로 잰 숫자는
「출력 잘림」과 「판정」이 섞여 해석이 불가능하다. 5번은 출력을 더 늘리므로 특히 그렇다.

---

## 9. 미결 — 오너 결정 대기

| # | 안건 | 왜 지금 정해야 하나 |
|---|---|---|
| 1 | **주관기관 화자 지원 여부** | 지원하면 온보딩에 칸이 하나 는다. 화면 다 만든 뒤면 재작업 |
| 2 | **룰 검수 19행** | `_G_룰검수요청.md` §6. 검수 전엔 단독 「가능」이 안 나간다 |
| 3 | **TIPS 0행** | 의도된 판단인지 결손인지 미확인 |
| 4 | 비목 갈림 임계치 | 화면 4 두 경로 병렬 표시 여부. 실측 대기 |
| 5 | 화면 7 조항별 해당/비해당 | (4)-a 스크리닝 채택 미결 |
