# 법령 크롤링 현황

정리 2026-08-24. 「써도돼요」 RAG 코퍼스 중 **법령 파트**(L1)의 현재 상태.

---

## 1. 한눈에

| 자산 | 수량 | 용량 | 위치 |
|---|---|---|---|
| 현행 법령·행정규칙 XML | **270** | 106.4 MB | `법령 PDF/L1_법령/` |
| 구판(시점본·연혁) XML | **944** | 414.6 MB | `법령 PDF/L1_법령/연혁/` |
| 첨부 (별표·본문공백 보완) | **35** | 21.0 MB | `법령 PDF/L1_법령/첨부/` |
| **행정규칙 별표** | **103** | 2.2 MB | `법령 PDF/L1_법령/별표/` (전량 PDF 변환 완료 → `_hwp변환/`) |
| | | **544.2 MB** | |

수집 경로는 국가법령정보 OPEN API(`law.go.kr/DRF`), 포맷은 XML.

| 스크립트 | 역할 |
|---|---|
| `scripts/Law_Crawling.py` | 창진원(L2) 배치 — 현행 + 시점본 + 위임 추적 |
| `scripts/mss_crawl.py` | 중기부(L1) 배치 — 같은 구조, 출처만 다름 |
| `scripts/fetch_admrul_history.py` | 행정규칙 구판 (`nw=2`) |
| `scripts/fetch_admrul_attachments.py` | 별표 참조 행정규칙의 첨부 |
| `scripts/fetch_admrul_appendix.py` | **행정규칙 별표** (`target=admbyl`) |
| `scripts/build_delegation_paths.py` | 위임 계통 복원 |
| `scripts/build_law_sources.py` | 출처 통합 인덱스 + 판정 인덱스 편입 판정 |
| `scripts/build_mss_doc.py` | `중기부_법령_링크모음.md` 생성 |
| `scripts/verify_corpus.py` | **마스터 ↔ 디스크 전수 대조** |

## 2. 두 배치

| 배치 | 마스터 | 수집 | 미등재 | 위임 추가 | 마스터 문서 |
|---|---|---|---|---|---|
| **창진원 (L2)** | 51 (L01~L39 + R01~R12) | 45 | 6 (수동수집) | 64 | `법령 연계 모음/문서_법령_링크모음.md` |
| **중기부 (L1)** | 84 | 79 | 5 | 135 | `중기부_법령_링크모음.md` |

**파일은 규범당 한 벌만 둔다.** 두 배치가 같은 법을 함께 참조하는 경우가 많아
파일을 복제하면 개정 시 어긋난다. 출처는 `_law_sources.json` 의 `sources` 배열로 구분한다.

| `sources` | 규범 수 |
|---|---|
| 중기부 단독 | 159 |
| **중기부 + 창진원** | **55** |
| 창진원 단독 | 46 |
| 범위밖(인용법령 경유) | 4 |

### 중기부 미등재 5건

| # | 명칭 | 조달 |
|---|---|---|
| 71 | 중소기업창업 지원사업 통합관리지침 (제14차) | **PDF 보유** — `2026_Finance_DATA_FOR_RAG/중기부/` |
| 72 | 창업사업화 지원사업 통합관리지침 (제13차) | **PDF 보유** — 〃 |
| 73 | 창업사업화지원사업 통합관리지침 (2022-6호) | 미확보 — K-Startup 과거 공고 아카이브 |
| 77 | 예산 및 기금운용계획 집행지침 | 미확보 — 기획재정부(moef.go.kr) |
| 84 | 정부지원금 부정사용자에 대한 고발기준 | 미확보 — 정식 명칭 미확정 |

실질 미확보는 **3건**(#73·#77·#84).

## 3. 판정 인덱스 편입 여부 — 264개 규범 중 45건 제외

위임 추적은 근거 사슬을 완성하려고 넓게 따라가지만, 그렇게 딸려온 것 중에는
창업지원금 판정에 절대 쓰이면 안 되는 규범이 섞인다.

| 제외 사유 | 건수 | 예시 |
|---|---|---|
| 타부처 보조금 규정 | 20 | 교육부·고용노동·국토교통·법무부 국고보조사업 관리규정 |
| 복지·수당 계열 | 16 | 아이돌봄 지원법, 기초연금법, 국민기초생활 보장법 |
| 타부처 R&D 전문기관 지정 고시 | 5 | 경찰청·식약처·조달청·지식재산처·질병관리청 |
| 위임 아님(인용법령 경유) | 4 | 산업교육진흥법 계열 3건, 산업기술혁신사업 공통 운영요령 |

**「교육부 국고보조사업 관리규정」이 창업지원금 판정 근거로 인용되면 그게 최악의 사고다.**
파일은 보관하되(사슬 완성용) `_law_sources.json` 의 `index: false` 로 표시했다.

> 판정 시 주의: `index` 는 **직접참조 여부로 결정**한다. 위임으로 딸려온 것(`ref_id` 가 `D-`)을
> 직접참조로 세면 타부처 규정이 전부 통과해 필터가 무력화된다.

## 4. 위임 계통

`_law_delegation_paths.json` — 68개 규범(창진원 배치). 중기부 배치는 `_mss_report.json` 의
각 레코드에 `delegated_from`/`delegate_via`/`depth` 가 직접 들어 있다.

```json
"창업기업및국외창업기업범위에관한규정": {
  "chain": ["중소기업창업지원법시행령", "창업기업및국외창업기업범위에관한규정"],
  "delegated_from": "중소기업창업 지원법 시행령",
  "delegate_via": "제2조제5항", "kind": "위임행정규칙", "depth": 1,
  "구판제명": "창업 및 창업기업 범위에 관한 규정"
}
```

위임 추적은 3단계(법률 → 시행령·시행규칙 → 위임 행정규칙). `위임구분=인용법령`은 제외.
범위 밖으로 뺀 위임은 창진원 705건 / 중기부 1,068건이 각각
`_law_delegated_dropped.json` · `_mss_delegated_dropped.json` 에 남아 있다.

## 5. 별표 — 세 갈래로 갈린다

| 대상 | 별표가 어디 있나 | 수집 방법 |
|---|---|---|
| **법령** | XML `별표단위/별표내용` 에 **ASCII 박스표**(`┌─┬─┐`) | 자동 (본문에 포함) |
| **행정규칙 (본문 참조)** | 본문엔 "별표 1에 따른다"만, 표는 없음 | `target=admbyl` |
| **행정규칙 (첨부)** | 첨부 HWP/PDF | `첨부파일링크` |

법령은 공무원 여비 규정 별표 2의 숙박비 상한액(서울 100,000 / 광역시 80,000 / 그 밖 70,000)까지
텍스트로 확인된다. RAG 투입 시 괘선은 마크다운 표나 문장으로 변환할 것.

### `target=admbyl` — 2026-08-24 확보

「중소벤처기업부 보조사업 관리규정」은 본문이 *"별표 1의 업종에는 보조사업비를 사용할 수 없다"*고만
하고 정작 그 목록인 **「보조사업비 카드 사용제한 업종」**은 본문·첨부 어디에도 없었다.
**비목 적격성 판정에 직결되는 표**다.

`lawSearch.do?target=admbyl` 이 별표를 독립 레코드로 준다(`별표서식파일링크` 포함).
다만 `query` 는 **별표명만 매칭**하므로 행정규칙명으로는 못 찾는다 → 전체 목록(83,986건)을
페이지네이션으로 훑어 `_admbyl_index.json` 캐시를 만들고 `관련행정규칙명` 으로 거른다.

- 인덱스 편입 행정규칙 25건 → 매칭 별표 **103개, 전량 수집 성공**
- 확보: 보조사업비 카드 사용제한 업종(중기부·기재부판), 포상금 지급신청서,
  창업지원자금 지원중단·회수기준, 학생인건비 통합관리 서식류 등
- 파일은 **HWP v5 … 가 아니었다.** 103건 중 **67건이 확장자만 `.hwp` 이고 실제로는 HWPX**
  (`mimetype=application/hwp+zip`)다. 한컴 COM 에 포맷을 틀리게 주면 Open 이 조용히 실패한다
  → `scripts/convert_hwp.py::sniff()` 가 매직바이트로 판별한다
- 다운로드 간헐 실패로 **HTML 오류페이지 5건**이 섞여 있었다(크기 검사만으로는 통과).
  재수집 결과 5건 전부 정상 수신 → **103/103 PDF 변환 완료** (2026-08-27)

## 6. 구판 보관

| 대상 | 방법 | 수량 |
|---|---|---|
| 법령 | `target=eflaw` + `efYd={연도}0101~{연도}1231` | **631** |
| 행정규칙 | `lawSearch.do?target=admrul&**nw=2**` | **313** |

행정규칙은 `eflaw` 대상이 아니라 시점본을 못 받는다. `nw=2` 가 현행+연혁 전체를 준다(`nw=1`은 현행만).
**구판은 제명이 다를 수 있다** — 「창업 및 창업기업 범위에 관한 규정」(2022 제정) → 현행
「창업기업 및 국외 창업기업 범위에 관한 규정」. 묶는 키는 개정 불변인 **행정규칙ID**.

구판은 **판정 인덱스 편입분만** 받는다(`--all` 로 전량 가능). 제외분까지 받으면 노이즈가 수백 건 는다.

`DOC_YEARS = (2022, 2023, 2024, 2025, 2026)` — 신규 데이터셋에 「창업도약패키지 세부관리기준(2022년)」이
있어 2022 를 포함한다.

## 7. 청킹 방침

- 청크 단위 = **항**. 계층은 편 > 장 > 절 > 조 > **항** > 호 > 목
- 호는 항에 합친다 — 떼면 "다음 각 호의 어느 하나" 문맥이 사라져 근거로 못 쓴다
- **항이 없는 조는 조 단위로 폴백**, **행정규칙은 조 단위 고정**(XML에 계층이 없음)
- 스키마는 `paragraph_no` 가 null 일 수 있어야 한다

> 신규 데이터셋 `DATASET_STRUCTURE.md` §5-4 는 법령을 **조 단위**로 잡았다. 통일 여부 결정 필요.

### 행정규칙 XML 은 본문 모양이 셋이다

```
(a) 법령          →  .//조문단위
(b) 행정규칙       →  최상위에 조문내용 반복
(c) 행정규칙 변종   →  <조문> 래퍼 안에 조문번호/조문제목/조문내용 삼중주
```

(c)는 코퍼스에 1개(「훈령ㆍ예규 등의 발령 및 관리에 관한 규정」)뿐이다.
`stage0_extract.py::extract_xml()` 은 `.//조문내용` descendant 검색이라 (c)도 정상 처리한다 — 실측 확인함.

## 8. 검증 — `scripts/verify_corpus.py`

마스터 목록 기준으로 파일 실재 + **인용된 조가 그 법에 실재하는지**까지 본다.
2026-08-24 기준 **전 항목 통과**.

| 점검 | 결과 |
|---|---|
| 창진원 마스터 51건 | 현행·시점본 누락 0 / 인용 조 검증 실패 0 |
| 중기부 마스터 84건 | 현행·시점본 누락 0 / 인용 조 검증 실패 0 |
| 위임 경로 68개 규범 | 파일 누락 0 |
| XML 무결성 | 1,214개 파싱 실패 0 |

### 검증이 잡아낸 것

1. **「산업교육진흥 및 산학연협력촉진에 관한 법률 시행규칙」 누락** — 법률·시행령은 있는데
   시행규칙만 없었다. 수집 완료
2. **고용정책 기본법 제13조의3** — 보조금법 제26조의8①9가 인용하나 현행법엔 제13조의2까지만 있다.
   해당 통합정보전산망은 **제15조의5**로 이동했다. **현행 보조금법 XML 도 구 조번호를 그대로 쓰는
   입법상 dangling reference** — 수집 누락이 아니다. 마스터의 인용 조를 제15조의5로 교정

## 9. 남은 과제

1. **참조 법령 목록을 신규 데이터셋 기준으로 재도출** — 창진원 배치 51건의 범위는 폐기한 구 PDF에서
   나온 것이다. `2026_Finance_DATA_FOR_RAG/창진원/` 42건에서 다시 뽑아야 한다
2. 재크롤링은 대부분 불필요 — 제외해둔 위임(창진원 705 / 중기부 1,068)에서 차집합만
3. 언더스코어 유무만 다른 중복 파일 6쌍 정리 (3쌍은 내용 완전 동일)
4. `_law_citations.json` 폐기 (구 PDF 인용 매핑)
5. ~~별표 HWP 103개 한컴 변환~~ ✅ **2026-08-27 완료** (98건 1차 + 재수집 5건). `_hwp변환/법령 PDF/L1_법령/별표/`

## 10. 산출물 파일

| 파일 | 내용 |
|---|---|
| `_law_report.json` / `_mss_report.json` | 배치별 수집 결과 + 조문 검증 |
| `_mss_master.json` | 중기부 마스터 84건 (인용 조항·등장 문서·비고) |
| `_law_sources.json` | **규범별 출처 + 판정 인덱스 편입 여부** |
| `_law_delegation_paths.json` | 위임 계통 68개 규범 |
| `_law_delegated_dropped.json` / `_mss_delegated_dropped.json` | 범위 밖 제외 위임 |
| `_admbyl_index.json` | 행정규칙 별표 전체 목록 83,986건 (캐시) |
| `_admrul_history_log.json` | 행정규칙 구판 수집 로그 |
| `_verify_report.json` | 대조 검증 결과 |
| `_law_cache.json` | 시점본 캐시. `hist:{MST}` -> 시행일자. 재실행 시 이미 받은 시점본을 다시 내려받지 않는다. **파일명 규칙을 바꾸면 이 캐시를 비워야 한다** |

## 11. 재실행 명령어

```powershell
python scripts/Law_Crawling.py --dry-run          # 창진원(L2) 배치 대상 확인
python scripts/mss_crawl.py --dry-run             # 중기부(L1) 배치 대상 확인
python scripts/fetch_admrul_history.py            # 행정규칙 구판 (nw=2)
python scripts/fetch_admrul_attachments.py        # 별표 참조 행정규칙의 첨부
python scripts/fetch_admrul_appendix.py           # 행정규칙 별표 (target=admbyl)
python scripts/build_delegation_paths.py          # 위임 계통 복원
python scripts/build_law_sources.py               # 출처 통합 인덱스
python scripts/build_mss_doc.py                   # 중기부_법령_링크모음.md 재생성
python scripts/convert_hwp.py                     # HWP/HWPX → PDF (한컴 COM, 매직바이트 판별)
python scripts/build_precedence.py                # 우선순위 조항 → _precedence_rules.json
python scripts/build_refs.py                      # 참조 그래프 → _refs.json
python scripts/verify_corpus.py                   # 마스터 ↔ 디스크 전수 대조
```

OC 키는 **환경변수 `LAW_GO_KR_OC`** 로 넣는다 (공개 저장소에 개인 ID를 두지 않으려고
2026-08-29 에 상수에서 뺐다). 비어 있으면 `Law_Crawling.require_oc()` 가 즉시 죽는다 —
OC 가 틀리면 API 는 HTTP 200 에 빈 결과를 주므로(§12 함정 참조) 조용한 0건으로 새면 안 된다.

```powershell
$env:LAW_GO_KR_OC = "<law.go.kr 신청 ID>"   # PowerShell
export LAW_GO_KR_OC='<law.go.kr 신청 ID>'    # bash
```

포맷은 XML (`stage0_extract.py::extract_xml()` 재사용).

## 12. 검증된 API 함정 14 — 재크롤링 시 반드시 반영

> 2026-08-28 에 `CLAUDE.md` 에서 이관. 크롤링은 사실상 끝난 작업이라 매 세션
> 컨텍스트에 올릴 이유가 없다. **law.go.kr 을 다시 칠 때만 이 절을 편다.**

| # | 함정 | 대응 |
|---:|---|---|
| 1 | JSON 은 단일 원소가 배열이 아닌 **객체**로 와서 `for` 순회가 조용히 깨진다 | XML 채택으로 회피 |
| 2 | `lsDelegated` 가 주는 위임 일련번호는 **구판**이다 | `행정규칙ID`/`법령ID`(개정 불변 키)로 현행판 재조회 |
| 3 | 행정규칙 본문은 `MST` 가 아니라 **`ID`** 파라미터 | `MST` 로 치면 HTML 껍데기가 온다 |
| 4 | `위임구분=인용법령` 은 위임이 아니라 참조다 | 따라가면 전체 법령으로 번진다 → 제외 |
| 5 | **다른 법으로 넘어가는 위임도 시행령/시행규칙으로 표시**된다 | 제명 접두 일치로 필터 (고등교육법 → 한국교원대학교 설치령) |
| 6 | 행정규칙 구판은 `eflaw` 가 아니다 | `lawSearch.do?target=admrul&nw=2` (`nw=1`은 현행만). 구판은 제명이 다를 수 있어 묶는 키는 `행정규칙ID` |
| 7 | 일부 판본은 조문내용 대신 "자세한 내용은 상단 메뉴 …" 안내문만 온다 | 본문은 첨부에만 있다 |
| 8 | `·`(U+00B7)와 `ㆍ`(U+318D)는 **다른 문자**다 | 초ㆍ중등교육법, 훈령ㆍ예규… 문서와 API 표기가 갈린다 |
| 9 | 행정규칙 별표가 **본문에도 첨부에도 없는** 경우가 있다 | `target=admbyl`. 단 `query` 가 별표명만 매칭 → 전체 목록 83,986건을 `_admbyl_index.json` 캐시로 만들어 `관련행정규칙명` 으로 거른다 (§5 참조) |
| 10 | `admbyl` 목록 페이지네이션 중 **빈 응답이 간헐적**으로 온다 | 재시도·부분저장 필수 |
| 11 | 행정규칙 XML 본문 모양이 셋이다 | `.//조문단위` / 최상위 `조문내용` 반복 / `<조문>` 래퍼 삼중주. descendant 검색(`.//조문내용`)이면 셋 다 잡힌다 (§7 참조) |
| 12 | `admbyl` 별표는 확장자가 `.hwp` 인데 **실제로는 HWPX** 가 다수 (103건 중 67건) | 한컴 COM 에 `format="HWP"` 를 주면 Open 이 조용히 False. **매직바이트 판별** (OLE `D0CF11E0`=HWP / `PK`=HWPX) |
| 13 | 다운로드 간헐 실패 시 수 KB 짜리 **HTML 오류페이지**가 온다 | 크기 검사(`<512바이트`)만으로는 5건이 통과했다. **매직바이트 검증 + 재시도** |
| 14 | **다단 레이아웃 PDF 는 단어 *사이* 로 다른 컬럼이 끼어든다** | 창업중심대학: `"창"` + 다른 문장 + `"업중심대학의"`. 공백 제거로는 안 풀린다 — 정규식 앵커를 **꼬리**에 건다 |
