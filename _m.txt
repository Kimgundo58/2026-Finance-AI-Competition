마감 후 정밀 검토 — 부록 시점 경고 · 갈래 재귀 오답 표기 제거 · 낡은 수치 3곳

## 고친 것

1. docs/부록/ 12개 배너에 **시점 경고**를 달았다
   전처리_파이프라인_전문.md 가 refs 28,701(실제 44,865)을 담고 있었다. 「참고 자료」라고만
   적혀 있으면 현재값으로 읽힌다. 🔴 수치 자체는 안 고쳤다 — 이관은 옮기는 일이지 고치는 일이 아니다.
   대신 「여기 수치는 이관 시점 원문 그대로다. 지금 값은 0_현황.md 와 DB 가 기준이다」를 박았다.

2. orchestrate.py 갈래 재귀에서 **오답 표기 경로**를 없앴다
   _비목고정 이 후보에 없으면 후보[:1] 로 물러났는데, 재귀 안에서 (1) 정규화를 다시 돌기
   때문에 품목이 미세하게 달라져 후보가 안 겹칠 수 있다. 그때 다른 비목으로 판정하고
   호출부가 r[비목] = c[비목] 로 고정 비목 이름표를 덮어쓴다 — 판정 제품에서 오답 표기다.
   고정 비목으로 후보를 새로 세우게 고쳤다.

3. orchestrate.py 비목후보 자료형 방어
   dict 를 넘기면 키가 비목이 됐다. list/tuple 만 순회하고 단건은 감싼다.

4. chunk_terms 1,148,718 -> 1,149,877 (0_현황·4-5·README) — DB 실측과 어긋나 있었다
5. 4-5_적재_지표 의 max_tokens=1500 에 시점 명시 — run 189 조건인데 현재값처럼 읽혔다
6. _레인_W1.md 코드펜스 이스케이프 — 본문 속 백틱3 언급을 렌더러가 펜스로 읽어 문서가 깨졌다

## 정정 (BE 가 마감 후 재감사해 잡았다)

🔴 「할일 org_id 배선으로 격리를 닫았다」는 과장이었다.
org_id 는 인증된 신원이 아니라 쿼리 파라미터, 자기신고다. 남의 uuid 를 알면 그 기관 계획을
읽고 그 계획에 할일을 쓴다(200/200/201 실측). 만든 것은 스코핑 필터이지 접근 제어가 아니다.
9-2_백엔드_프론트_미결 에 BE4(org_id 자기신고) · BE5(L3 업로드 미감사) 로 올렸다.

## 확인했고 문제 없던 것

지워진 md 15개의 사실 보존율 88~100% (수치·식별자·조문·상수 기준) · 위키링크 215 깨짐 0 ·
DB vs 문서 일치 · 아카이브 77개 참조 3건 전부 무해 · 훅 6종 배선 확인 ·
핵심 규칙 반대 서술 0건 · _lib 반쯤 전환 0 · dry e2e 93/93 실패 0 · pytest 140/2

## 감사 자체에서 내가 두 번 틀렸다

줄 단위 대조로 「보존율 1.3%」가 나왔다 — 요약본에 줄 단위 잣대를 댄 것이라 무의미했다.
그다음 사실 단위 스크립트도 git ls-files 가 한글 경로를 8진 이스케이프로 내놔
한글 경로 문서를 하나도 안 읽었다(「12%」). 둘 다 허위 경보였고 실제는 88~100% 다.
잣대가 대상에 안 맞으면 숫자가 거짓말을 한다.

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
Claude-Session: https://claude.ai/code/session_01PGkVQmm5PW89hFDGdrDxMd
