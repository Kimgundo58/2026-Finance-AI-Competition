# 프론트 연동 사양

「써도돼요」 화면별·컴포넌트별 백엔드 지원 여부.

> 판정 응답 스키마는 `LLM.md` §3-4, F축 저장 스키마는 `RAG.md` §2-3,
> 화면 흐름은 `서비스 아키텍쳐.md` §4 가 정본이다. 충돌하면 그쪽이 이긴다.

## 표기

| 표기 | 뜻 |
|:--:|---|
| ✅ | **지원 확정.** 지금 붙여도 된다 |
| 🟡 | **조건부.** 데이터는 있으나 검수·후속 단계가 남았다. 목으로 개발하고 나중에 교체 |
| ⏸ | **결정 대기.** 설계가 안 닫혔다. 자리만 잡고 비워둘 것 |
| ❌ | **미지원.** 만들지 말 것. 사유가 있다 |

화면 번호는 0·1·2·8 이 결번이다. 센터 관리자 화면을 뺀 흔적이고 재번호하지 않는다.

---

## 0. 먼저 — 화면 전체 흐름

```
화면 3 묻기 ──▶ 화면 4 확인 ──▶ 화면 5 판정 ──▶ 화면 6 할일 ──▶ 화면 7 근거
 자연어 한 줄     비목 컨펌        4-way          해야할일        조항 원문
                                     │
                                     └── 판단불가 ──▶ 화면 9 문의 초안

           [ F 프로필 ] 로그인 경로. 게스트도 판정은 된다
```

**스트리밍은 두 군데다** — 화면 3→4(정규화), 화면 4→5(판정 조립).
첫 글자가 1초 안에 나오는 설계라 스켈레톤보다 **스트리밍 렌더**가 맞다.

---

## 1. 화면 3 — 묻기

| 컴포넌트 | 지원 | 백엔드가 주는 것 / 비고 |
|---|:--:|---|
| 자연어 입력 textarea | ✅ | `POST /api/normalize` 로 문자열 그대로 전송 |
| 예시 질문 칩 | ✅ | 프론트 자체. 백엔드 불필요 |
| F5 체크박스 — 친족 거래인가 | ✅ | 건별 수집. **판정 후 폐기**, 저장 안 함 |
| F5 체크박스 — 전직 임직원 업체인가 | ✅ | 동일 |
| **사업 선택** (예비창업패키지 / 초기창업패키지 …) | 🔴 | **아래 §8 확인 필요 #1** — 룰 조회 키가 `사업 × 비목` 이라 사업을 모르면 한도를 못 고른다 |
| 비목 선택 UI | ❌ | 여기서 묻지 않는다. 화면 4 에서 추천 후 사용자가 최종 선택 |
| 금액·집행일·거래처 입력칸 | ❌ | 문장에서 정규화가 뽑는다. 화면 4 에서 확인받는다 |
| 영수증·견적서 파일 업로드 | ❌ | 판정에 불필요. 증빙 **목록**만 안내한다 |
| 스크린샷 업로드 | ❌ | 지원하지 않는다. 텍스트 붙여넣기만 |

**왜 비목을 안 묻나** — 사용자는 비목을 모른다. 「맥북」이 기계장치인지 재료비인지 아는 사람이면
이 서비스가 필요 없다. 그리고 화면 4 의 컨펌 로그가 `item_alias`(맥북 → 기계장치)를 자동으로
채우는 구조라, 처음부터 물으면 그 학습 경로가 사라진다.

---

## 2. 화면 4 — 확인 (스트리밍)

`POST /api/normalize` 의 SSE 응답을 그대로 렌더한다.

| 컴포넌트 | 지원 | 백엔드가 주는 것 / 비고 |
|---|:--:|---|
| 품목 카드 | ✅ | `품목` |
| 금액 | ✅ | `금액` |
| **"추정값입니다" 배지** | ✅ | `금액_추정여부` — true 면 사용자에게 확인받는다 |
| 용도 | ✅ | `용도` |
| **비목 추천 + 최종 선택** | ✅ | `비목후보[{비목, 신뢰도}]`. **최종 선택은 사용자가 한다** |
| 전체 비목 목록에서 직접 고르기 | ✅ | enum 10종 (§6) |
| 비목이 갈릴 때 두 경로 병렬 표시 | 🟡 | 설계 확정. **분기 임계치가 골든셋 실측 대기** |
| 결제수단 / 구매명의 / 신청일 / 비교견적 | ✅ | 정규화가 문장에서 뽑는다. 없으면 `null` — 빈 값 UI 필요 |
| 스트리밍 진행 표시 | ✅ | SSE 이벤트 도착 순서대로 |
| 되묻기(clarifying question) | ❌ | 되묻지 않는다. **추정 후 이 화면에서 확인**받는 설계다 |

---

## 3. 화면 5 — 판정

`POST /api/judge` 의 SSE 응답.

| 컴포넌트 | 지원 | 백엔드가 주는 것 / 비고 |
|---|:--:|---|
| **4-way 판정 배지** | ✅ | `판정` — `가능 \| 조건부 \| 불가 \| 판단불가` 폐쇄 enum |
| 요약 한 문장 | ✅ | `요약` |
| **신뢰등급 A / B** | ✅ | `신뢰등급` — **LLM 이 아니라 코드가 산정**한다. 인용 청크의 추출 방식·인덱스 등급 기준 |
| 버전스탬프 | ✅ | `버전스탬프` — 예: `"제14차, 2025.12.23 기준"` |
| 근거 조항 개수 | ✅ | `인용[]` 길이 |
| 확률 / 점수 / % 표시 | ❌ | **제공하지 않는다.** LLM 자칭 신뢰도는 검증 불가라 스키마에서 제거했다 |
| "AI가 n% 확신" 류 문구 | ❌ | 동일. 만들지 말 것 |

🔴 **판단불가는 에러가 아니다.** 4-way 중 하나이고 정상 경로다. 빨간 에러 화면이 아니라
화면 9 로 이어지는 안내로 그려야 한다.

---

## 4. 화면 6 — 할일

| 컴포넌트 | 지원 | 백엔드가 주는 것 / 비고 |
|---|:--:|---|
| **해야할일 체크리스트** | ✅ | `해야할일[{항목, 설명}]` |
| 사전승인 필요 여부 배지 | 🟡 | `rules.사전승인` + `사전승인_조건`. **룰 19행 D단계 검수 전** |
| 증빙서류 목록 | 🟡 | `rules.증빙 TEXT[]`. 동일하게 검수 전 |
| **증빙 발급처 링크** (홈택스·특허로·IRIS·4대사회보험) | 🟡 | 팀 반입 자료(증빙서류 종합 124종)에 있으나 **아직 DB 미적재**. 목으로 개발 |
| 체크 상태 저장 | ❌ | 미설계. 로컬 상태로만 |
| 서류 업로드 / 제출 | ❌ | 정산 서류 자동 제출은 스코프 밖 |

---

## 5. 화면 7 — 근거

| 컴포넌트 | 지원 | 백엔드가 주는 것 / 비고 |
|---|:--:|---|
| **인용 조항 원문** | ✅ | `인용[{조번호, 조제목, 원문, doc_id}]` — S번호를 코드가 DB 원문으로 치환한 값 |
| **참조사슬** ("이 규정이 왜 나에게 적용되나") | ✅ | `참조사슬[{from, 표기, to, 보정}]` |
| 전제 목록 | ✅ | `전제[{사실, 근거조항, 매핑, 미충족시}]` |
| **조항별 해당 / 비해당 / 불확실** | ⏸ | **④-a 스크리닝 채택이 미결.** 기각되면 재료 산출 방식을 다시 설계해야 한다 |
| "비해당 조항도 보여준다" | ⏸ | 방침은 확정. 재료가 위 항목에 묶여 있다 |
| 참고 사례 위젯 | 🟡 | `case_chunks`(B등급) 준비됨. **Stage 2 인덱싱 후** 붙는다. **판정에 영향 없음 — 라벨 필수** |
| 원문 전체 보기 / 원문 링크 | 🟡 | `doc_id` 는 준다. **원문 뷰어는 미설계** |

⏸ 항목 때문에 이 화면은 **레이아웃만 잡고 내용은 비워두는 것**을 권한다.

---

## 6. 화면 9 — 문의 초안

| 컴포넌트 | 지원 | 백엔드가 주는 것 / 비고 |
|---|:--:|---|
| 판단불가 안내 | ✅ | `판정 = "판단불가"` + `요약` |
| 무엇이 없어서 못 정했는지 | ✅ | `전제[]` 가 그대로 근거 |
| 기관 문의 초안 텍스트 | 🟡 | LLM (5) 슬롯. **판단불가 경로 전용**이고 프롬프트 미설계 |
| 복사 버튼 | ✅ | 프론트 자체 |
| 기관 담당자 연락처 | ❌ | 데이터 없다 |

---

## 7. F 프로필 (로그인 경로)

`GET / PUT /api/profile`. **게스트도 판정은 된다** — `전제[]` 가 늘어날 뿐이다.

| 컴포넌트 | 지원 | 백엔드가 주는 것 / 비고 |
|---|:--:|---|
| **F1 협약 — 4분면 4칸** | ✅ | `{정부지원, 자기부담} × {현금, 현물}` + 협약시작일 / 협약종료일 |
| **F3 집행내역** | ✅ | 비목 · 재원 · 형태 · 거래처 · 인력역할 · 귀속월 · 금액 |
| **F4 인력** | ✅ | 역할 · 고용형태 · 타사업참여율 · 소속기관유형 · 겸직 |
| **PMS 붙여넣기 파서** | ✅ | **브라우저에서 정규식 파싱.** 서버와 무관 — PMS 화면 텍스트 샘플만 있으면 된다 |
| 로그인 유도 문구 | ✅ | *"협약 정보를 입력하면 미확인 전제 2개가 사라집니다"* — 개수는 `전제[]` 로 계산 |
| **F4 인력 이름 입력칸** | ❌ | 🔴 **만들지 말 것.** 역할·수치만 저장한다. 저장 계층화 원칙이라 나중에 못 바꾼다 |
| F2 사업계획서 업로드 | 🟡 | 파싱 후 **원문 폐기**, 구조화 결과만 저장. MVP 후순위 |
| F6 기업정보 / F7 수혜이력 | ❌ | 후순위. MVP 는 F1·F3·F4 |
| 파일 업로드로 F 입력 | ❌ | 텍스트 붙여넣기만 |
| 판정 이력 조회 | 🟡 | `decisions` 테이블은 있으나 **조회 API 미설계** |

🔴 **PMS 원문은 서버로 보내지 않는다.** 브라우저에서 파싱해 **확인된 숫자만** 전송한다.
실명·과제번호가 서버에 도달하면 안 된다.

---

## 8. API 계약 (제안 — 확정 전)

응답 스키마는 확정이나 **HTTP 계약은 아직 합의 전**이다. 아래대로 목 서버를 만들면
프론트가 백엔드 없이 끝까지 갈 수 있다.

```
POST /api/normalize        SSE      화면 3 -> 4
  req  { "질문": "디자이너 쓸 맥북 250만원 사도 되나요?",
         "사업명": "예비창업패키지",
         "f5": { "친족거래": false, "전직임직원업체": false } }
  evt  { "품목": "맥북", "금액": 2500000, "금액_추정여부": false,
         "용도": "디자이너 작업용",
         "비목후보": [{ "비목": "기계장치", "신뢰도": 0.82 }],
         "결제수단": null, "구매명의": null, "신청일": null, "비교견적": null }

POST /api/judge            SSE      화면 4 -> 5 / 6 / 7
  req  { "정규화": { ... }, "확정비목": "기계장치", "사업명": "예비창업패키지" }
  evt  { "판정": "가능 | 조건부 | 불가 | 판단불가",
         "요약": "1인 1대 한도 내에서 구매 가능합니다.",
         "해야할일": [{ "항목": "", "설명": "" }],
         "인용":   [{ "조번호": "제39조", "조제목": "", "원문": "", "doc_id": "" }],
         "전제":   [{ "사실": "", "근거조항": "S14",
                     "매핑": ["F1.정부지원.현금"], "미충족시": "불가" }],
         "신뢰등급": "A",
         "버전스탬프": "제14차, 2025.12.23 기준",
         "참조사슬": [{ "from": {}, "표기": "", "to": {}, "보정": "" }] }

GET  /api/vocab                     비목 enum 10종 (아래 §9)

GET  /api/profile
PUT  /api/profile
  { "f1": { "정부지원_현금": 0, "정부지원_현물": 0,
             "자기부담_현금": 0, "자기부담_현물": 0,
             "협약시작일": "", "협약종료일": "" },
    "f3": [{ "비목": "", "재원": "정부지원|자기부담", "형태": "현금|현물",
             "거래처": "", "인력역할": "", "귀속월": "", "금액": 0 }],
    "f4": [{ "역할": "", "고용형태": "", "타사업참여율": 0,
             "소속기관유형": "", "겸직": false }] }
```

---

## 9. 비목 enum — 확정 10종

프론트 라벨을 **이 문자열 그대로** 맞춘다. 정규화 출력·룰 조회·골든셋이 같은 목록을 쓴다.

```
재료비 · 외주용역비 · 기계장치 · 인건비 · 지급수수료 ·
여비 · 교육훈련비 · 광고선전비 · 특허권등무형자산취득비 · 창업활동비
```

- **창업활동비는 예비창업패키지에만 있다.** 사업에 따라 노출 목록이 달라진다
- 주관기관 비목(창업프로그램 운영비 등)은 **enum 에 넣지 않는다** — 우리 사용자는 창업기업이다
- 별칭(맥북 → 기계장치)은 어휘집이 아니라 `item_alias` 의 몫이다. 프론트는 신경 쓰지 않아도 된다

---

## 10. 확인 필요 / 결정 대기

| # | 항목 | 왜 막혀 있나 |
|:-:|---|---|
| 1 | 🔴 **게스트는 사업을 어떻게 아는가** | 룰 조회 키가 `사업 × 비목` 인데 게스트는 F 프로필이 없다. **화면 3 에서 사업을 물어야 할 수도 있다** — 결정 필요 |
| 2 | ⏸ 화면 7 조항별 판단 구조 | ④-a 스크리닝 채택 여부 미결 |
| 3 | ⏸ 로그인 · 동의 화면 | 개인정보 처리방침 확정 후. 게스트 경로는 이미 확정이니 그것부터 |
| 4 | ⏸ 응답 지연 기반 UX 튜닝 | p50 3.2초는 설계값이고 실측 전 |
| 5 | 🟡 룰 기반 필드(증빙·사전승인) | 19행 D단계 사람 검수 전. `verified=false` 인 룰만으로 "가능" 판정 금지 |

---

## 11. 지금 시작해도 되는 것 — 우선순위

| 순위 | 작업 | 왜 안 막히나 |
|:--:|---|---|
| 1 | **API 계약 + 목 서버 합의** | 병렬화의 열쇠. 응답 스키마가 확정이라 지금 못 박을 수 있다 |
| 2 | 화면 5 · 6 | 스키마 확정. 목 JSON 으로 완성 가능 |
| 3 | 화면 3 · 4 | 입력이 textarea + 컨펌뿐. 스트리밍 렌더 배관 포함 |
| 4 | 판단불가 UX + 화면 9 | 에러가 아니라 정상 경로로 그린다 |
| 5 | F 프로필 폼 | DDL 확정. 필드가 안 바뀐다 |
| 6 | PMS 붙여넣기 파서 | 브라우저에서만 돈다. PMS 화면 텍스트 샘플만 필요 |
| 7 | 디자인 시스템 · 반응형 · 접근성 · 에러/재시도 | 전부 백엔드 무관 |
