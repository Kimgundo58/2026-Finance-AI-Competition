# 백엔드 ↔ 프론트 프로토타입 비교 — 가능 / 불가 전수 판정

- **대상 프론트**: https://checkumait-user-clean.yeemmin.chatgpt.site/
  (번들 `checkumait-app-BT3CoerM.js` 실측 — 한글 UI 문자열 700건 전수 추출, 2026-08-30)
- **대상 백엔드**: `서비스 아키텍쳐.md` §4·§6·§7 · `프론트 연동 사양.md` §8 API 계약 ·
  `LLM.md` §3-4 출력 스키마 · `rule_base.md` §2·§4 룰 스키마 · `RAG.md` §2-3 tenant DDL ·
  `db/init/01_schema.sql` (정본 DDL)
- **관련 문서**: 화면별 상세 수정 지시는 `프론트 프로토타입 검토.md`,
  AI/코드/프론트 분업은 `프로토타입_기능구분.md`. **이 문서는 그 둘을 가능/불가 2값으로 압축한 것**

---

## 0. 판정 기준 — 무엇이 "가능"인가

| 판정 | 뜻 |
|:--:|---|
| **가능** | 백엔드가 그 화면 요소를 채울 **계약(스키마·테이블·룰·API)이 존재**한다. 프론트 자체로 끝나는 것도 포함 |
| **불가** | 채울 계약이 **없거나**, 확정 원칙에 어긋나 **만들지 않기로 한 것**, 또는 설계가 안 닫혀 **지금은 줄 수 없는 것** |

불가는 이유가 넷이라 성격이 다르다. 2값 판정은 그대로 두고 `사유`로 구분한다.

| 사유 | 뜻 | 되살아날 수 있나 |
|:--:|---|---|
| **원칙** | 확정 원칙 위반 — 판정 1건 = LLM 2회 고정 · function calling 미개방 · 저장 계층화 · 인덱스 경계 | ❌ 재제안 금지 |
| **스코프** | 서비스 범위 밖 (정산 서류 관리·일정 관리 등) | ❌ MVP 밖 |
| **미설계** | 자리는 맞는데 백엔드에 대응 필드/API가 없다 | ⭕ 설계하면 |
| **대기** | 설계 결정이 안 닫혔다 (④-a·부가세 등) | ⭕ 결정되면 |

**가능**에도 온도차가 있다. `조건` 열에 적었다.

- `즉시` — 지금 붙여도 된다
- `목` — 스키마는 확정, HTTP API가 미구현이라 목 서버로 개발 후 교체
- `검수` — 데이터는 있으나 사람 검수 전 (`verified=false` 룰 단독으로 "가능" 판정 금지)
- `F전제` — F 프로필(F1·F3·F4) 입력 화면이 신설돼야 실데이터가 된다

---

## 1. 한 장 요약

```
프로토타입 12화면 + 공통 셸
        ||  백엔드가 채울 수 있다            ||  백엔드가 채울 수 없다
========||===================================||===================================
코어     ||  비목 추천 + 사용자 확정          ||  자유 질의 챗봇          (원칙)
        ||  판정 4-way + 요약 + 해야할일     ||  일괄 N건 동시 점검      (원칙)
        ||  적용 근거 조항 + 원문            ||  파일 업로드 3종         (원칙/스코프)
        ||  필요 증빙 목록 · 사전승인 플래그  ||  집행 일정 · 캘린더 · 알림 (스코프)
--------||-----------------------------------||-----------------------------------
구조     ||  L3 업로드 → 조 단위 적재         ||  세목·세세목 계층        (미설계)
        ||  기관 기준 오버레이 + 우선순위     ||  지급수수료 세부유형 16종 (미설계)
        ||  D-day · 한도 잔액 (F1·F3)        ||  ~60문항 질문 뱅크       (원칙)
--------||-----------------------------------||-----------------------------------
어휘     ||  사업명 8종 — 스코프 정확히 일치   ||  상태 4어휘 전부         (원칙)
        ||                                   ||  비목 라벨 4건 불일치     (미설계)
```

**한 줄**: 프로토타입의 **판정 코어는 거의 그대로 살아남고**, 그 위에 얹힌
**관리도구 껍데기(계획 CRUD · 일정 · 챗봇 · 파일함)는 백엔드가 받쳐주지 않는다.**
반대로 **백엔드가 이미 주는데 프론트에 자리가 없는 것이 14건**이다 (§4).

### 집계

§2 화면별 판정 **132행** (그중 10행은 프로토타입에 없는 항목 — 신설/금지 지시).
§3 어휘 대조 14행은 따로 센다.

| 판정 | 행 | 세부 |
|:--:|--:|---|
| **가능** | **73** | 즉시 25 · 목(API 미구현, 스키마 확정) 40 · 검수(룰 D단계 전) 3 · F전제(F 프로필 신설 전제) 5 |
| **불가** | **59** | 원칙 28 · 미설계 19 · 스코프 9 · 대기 3 |

| 화면 | 가능 | 불가 | 성격 |
|---|--:|--:|---|
| P0 랜딩 | 3 | 2 | 문구는 전부 사실과 일치. 상태 라벨·거래처만 교정 |
| P1 사업 선택 | 4 | 0 | 🔴 **미결 #1을 이 화면이 해소한다** — 그대로 채택 |
| P2 주관기관 | 2 | 0 | 데모 기관 적재만 남음 |
| P3 L3 업로드 | 7 | 1 | DOC·DOCX만 제거 |
| P4 완료 | 3 | 0 | F1 입력 단계 신설 |
| 공통 셸 | 5 | 3 | 챗봇 CTA·일정 메뉴·이름 표시 제거 |
| P5 홈 | 7 | 6 | 집계는 살고, draft·일정·일괄 배너가 죽는다 |
| **P6 목록** | 8 | **12** | 서버에 "계획" 엔티티가 없다 — 컬럼 절반이 못 채워진다 |
| **P7 새 계획** | 5 | **9** | step1·step3 구조째 교체. **step2만 그대로 산다** |
| P8 상세 | 14 | 12 | 코어는 살고 파일함·일정·상태 어휘가 죽는다 |
| **P9 챗봇** | 1 | 4 | 🔴 화면 전체 불가 (칩만 화면 3으로 이동) |
| **P10 일정** | 0 | 6 | 🔴 서버 기능 전체 불가 |
| P11 규정·지침 | 9 | 2 | 원문 뷰어만 없다 |
| P12 내 정보 | 5 | 2 | F 프로필 탭 신설이 본체 |

---

## 2. 화면별 전수 판정

### P0. 랜딩

| 프로토타입 요소 | 판정 | 사유/조건 | 백엔드 대응 |
|---|:--:|:--:|---|
| 시작하기 (헤더·hero CTA) · "처음으로" | **가능** | 즉시 | 프론트 자체 |
| 주요 기능 4카드 (비목 자동 판단 / 규정·기준 점검 / 필요 증빙 안내 / 사전절차 체크) | **가능** | 즉시 | 4문구 모두 실제 기능과 일치 |
| AI 점검 결과 예시 데모 카드 (레이아웃) | **가능** | 즉시 | 정적 |
| 데모 카드 상태 라벨 "확인 필요" | **불가** | 원칙 | 4-way enum 밖 → `조건부`로 교체 (§3-1) |
| 데모 카드 "거래처 ㈜디자인랩" 행 | **불가** | 원칙 | 거래처명은 판정 스키마·`decisions` 어디에도 없다 |

### P1. 온보딩 1/4 — 사업 선택

| 프로토타입 요소 | 판정 | 사유/조건 | 백엔드 대응 |
|---|:--:|:--:|---|
| 사업 선택 드롭다운(`<select>`) | **가능** | 즉시 | 🔴 룰 조회 키가 `사업 × 비목`이라 **필수**. 미결 #1(게스트는 사업을 어떻게 아나)을 이 화면이 해소한다 |
| 사업 목록 8종 | **가능** | 즉시 | 예비·초기·창업도약·창업중심대학·재도전성공·모두의 창업 **일반・기술**·초격차·민관공동(TIPS). **사업 스코프와 정확히 일치** — 로컬트랙 제외 표기까지 맞다. 룰 조회 키 매핑만 붙이면 된다 |
| "1개만 설정" 제약 | **가능** | 즉시 | 판정 인덱스가 기관 1벌 전제라 오히려 맞다 |
| "중소벤처기업부 · 창업진흥원" 출처 표기 | **가능** | 즉시 | L1·L2 발행주체와 일치 |

### P2. 온보딩 2/4 — 주관기관 선택

| 프로토타입 요소 | 판정 | 사유/조건 | 백엔드 대응 |
|---|:--:|:--:|---|
| 주관기관 검색 + 선택 | **가능** | 목 | `tenant.orgs(org_id, 기관명, 사업명[])` — 테이블은 있다. **전국 기관 목록 데이터가 없다** → MVP는 데모 기관 1건 사전 적재 |
| "일치하는 주관기관이 없습니다" 상태 | **가능** | 즉시 | 공통 규정만 적용(게스트성 경로)으로 안내 — 설계와 일치 |

### P3. 온보딩 3/4 — 기관 세부기준 등록 (L3 업로드)

| 프로토타입 요소 | 판정 | 사유/조건 | 백엔드 대응 |
|---|:--:|:--:|---|
| 기관 지침 파일 업로드 | **가능** | 목 | `tenant.l3_documents` + `l3_articles` 조 단위 적재. **셀프서비스 업로드 API 미구현** |
| 확장자 **PDF** | **가능** | 즉시 | `pdftext.py::extract()` 경유 |
| 확장자 **HWPX · HWP** | **가능** | 목 | 2026-08-30 채택. HWPX는 XML 직파싱(최우선), HWP 5.0은 `hwp_extract.py` 계열. `extraction ∈ {hwpx, hwp}` 이미 DDL에 있다 |
| 확장자 **DOC · DOCX** | **불가** | 미설계 | 파서 없음. **셀렉터에서 제거** |
| 최대 20MB | **가능** | 즉시 | 제약 없음 |
| "파일 없이 계속" (공통 규정 우선 적용) | **가능** | 즉시 | 게스트 경로 원칙과 정확히 일치 |
| "등록 문서는 금액 기준·사전승인·증빙 판단에 사용" 문구 | **가능** | 즉시 | 사실과 일치 |
| (프로토타입 없음) **DRM 배포용 HWP 실패 안내** | **가능** | 목 | `파싱품질='fail'` 반환 → "PDF 변환 후 재업로드" UI 필요 |

### P4. 온보딩 4/4 — 설정 완료

| 프로토타입 요소 | 판정 | 사유/조건 | 백엔드 대응 |
|---|:--:|:--:|---|
| 완료 안내 2문구 분기 (기관 기준 등록/미등록) | **가능** | 즉시 | 프론트 자체 |
| 홈으로 가기 / 첫 계획 작성하기 | **가능** | 즉시 | 프론트 자체 |
| (프로토타입 없음) **F1 협약 입력 단계** | **가능** | 목 | `f_profile` 4분면+협약기간. **이게 없으면 홈 D-day·한도 잔액이 전부 가짜다** (§4-1) |

### 공통 셸 — 사이드바

| 프로토타입 요소 | 판정 | 사유/조건 | 백엔드 대응 |
|---|:--:|:--:|---|
| 네비 "홈" · "지출 계획" | **가능** | 목 | 아래 P5·P6 컬럼별 판정 참조 |
| 네비 "규정·지침" | **가능** | 목 | P11 참조 |
| 네비 **"집행 일정"** | **불가** | 스코프 | P10 전체 미지원 — 메뉴째 제거 |
| 참여 프로젝트 카드 (사업명 · 기관 · **D-129**) | **가능** | F전제 | `f_profile.협약종료일` 날짜 계산 |
| 사이드바 펼침/접힘 · 모바일 메뉴 닫기 | **가능** | 즉시 | 프론트 자체 |
| 계정 메뉴 · 내 정보 · 로그아웃 | **가능** | 목 | `tenant.accounts` 존재. 단 로그인·동의 화면은 미결 #3 — **게스트 경로부터** |
| 프로필 이름 "홍길동" · 팀 "팀 루멘" 표시 | **불가** | 원칙 | `accounts`는 `email`·`pw_hash`뿐. 저장 계층화 — 이름 컬럼을 만들지 않는다 |
| **플로팅 "AI에게 물어보기"** | **불가** | 원칙 | P9 참조. **"새 판정 시작" CTA로 교체** |

### P5. 홈 대시보드

| 프로토타입 요소 | 판정 | 사유/조건 | 백엔드 대응 |
|---|:--:|:--:|---|
| 새 지출 계획 (주 CTA) | **가능** | 즉시 | 판정 진입점 |
| 진행 중인 사업 카드 · 사업기간 · "사업 종료까지 D-129" | **가능** | F전제 | `f_profile.협약시작일·협약종료일` |
| "기관 최신 집행 안내 확인" 링크 | **불가** | 미설계 | `doc_id`는 주지만 **원문 뷰어가 없다** |
| 전체 지출 계획 현황 — 건수 집계 | **가능** | 목 | `decisions` COUNT. **조회 API 미설계** |
| 전체 지출 계획 현황 — 예상금액 집계 | **가능** | 목 | `decisions.금액` SUM |
| 상태별 분류 탭 (전체/…) | **가능** | 목 | `decisions.판정` GROUP BY — **어휘는 4-way로** |
| "지금 확인해주세요" (확인 필요 지출 목록) | **가능** | 목 | `WHERE 판정='조건부'` |
| "확인이 필요한 지출 N건이 더 있어요" | **가능** | 목 | 동일 |
| **"점검 전 / 임시저장" 상태의 계획** | **불가** | 미설계 | 서버에 **"계획" 엔티티가 없다**. `decisions`는 판정이 끝난 건만 남는다 — draft 저장처 없음 |
| 다가오는 일정 · 전체 캘린더 보기 | **불가** | 스코프 | P10 참조 |
| **"AI가 지출 계획 N건을 점검하고 있어요" 배너** | **불가** | 원칙 | 일괄 점검 미설계 (GPU 직렬 큐) |
| AI 점검 로딩 3단계 스피너 (기본 정보 확인→비목 분류→확인 항목 정리) | **불가** | 원칙 | 첫 글자 1초 설계 — **SSE 스트리밍 렌더로 교체**. 단계 스피너가 오히려 느려 보이게 만든다 |
| "보통 5~10초 정도 소요됩니다" 문구 | **불가** | 원칙 | 설계값 p50 **3.2초**와 불일치 |

### P6. 지출 계획 목록

**전제**: 서버에 "지출 계획" 테이블이 없다. 대응물은 판정 로그 `tenant.decisions` 하나뿐이고,
여기엔 **판정이 끝난 건만** 들어간다. 그래서 컬럼별로 갈린다.

| 목록 컬럼 | 판정 | 사유/조건 | 백엔드 대응 |
|---|:--:|:--:|---|
| **지출명** | **불가** | 미설계 | `decisions`에 `질문원문`은 있어도 제목 필드가 없다. 정규화 `품목`으로 대체하거나 컬럼 신설 |
| 예상 금액 | **가능** | 목 | `decisions.금액` |
| AI 점검 상태 | **가능** | 목 | `decisions.판정` — **4-way enum으로** |
| 다음 행동 | **가능** | 목 | `해야할일[0].항목` |
| **최근 수정일** | **불가** | 미설계 | `created_at`만 있다. "수정" 개념이 없다 — 수정하면 **재판정**이 원칙 |

| 그 외 요소 | 판정 | 사유/조건 | 백엔드 대응 |
|---|:--:|:--:|---|
| 상태 필터 · 정렬(최근 수정순/최근 작성순) · 페이지네이션(10개씩) | **가능** | 목 | 프론트 자체 + 조회 API |
| **검색 "지출명, 거래처로 검색"** | **불가** | 미설계 | 두 필드 모두 `decisions`에 없다. `질문원문` 검색으로 교체 |
| **계획서 업로드** | **불가** | 원칙 | 입력은 텍스트뿐 — 파일로 판정을 시작하는 경로가 없다 |
| **"선택한 N건 AI 점검" (일괄)** | **불가** | 원칙 | 판정 1건 = LLM 2회 고정 + GPU 직렬 큐 |
| "N건의 점검이 완료됐어요" 모달 | **불가** | 원칙 | 상동 |
| 선택 모드 (전체 선택/선택 해제/N건 선택됨) | **불가** | 원칙 | 일괄 점검이 사라지면 존재 이유가 없다 |
| 행 메뉴 수정 / 삭제 · 삭제 confirm | **불가** | 미설계 | 서버 저장·수정·삭제 API 없음. 로컬 상태로만 |
| 삭제 confirm "연결된 집행 일정도 함께 삭제됩니다" | **불가** | 스코프 | 일정 자체가 없다 |
| "점검 전" 뱃지 (임시저장 상태) | **불가** | 미설계 | draft 저장처 없음 |

**CSV 출력** — 프론트 자체 생성은 가능하나 컬럼별로 재료가 갈린다.

| CSV 컬럼 | 판정 | 사유/조건 | 백엔드 대응 |
|---|:--:|:--:|---|
| 비목 | **가능** | 목 | `decisions.비목` — enum 10종 |
| **세목 · 세세목** | **불가** | 미설계 | 룰 조회 키는 `사업 × 비목` **단일 계층**. 하위 계층이 백엔드에 없다 |
| **산출 근거** | **불가** | 미설계 | 대응 필드 없음. 정규화 `용도`가 가장 가깝다 |
| 집행일 | **가능** | 목 | 정규화 `신청일` |
| 현금 / ~~현물~~ / 합계 | **가능** | F전제 | `f_exec(재원, 금액)` 재원별 집계. 🔴 현물은 2026-08-31 제거 — 현물 계상은 지출이 아니라 판정 대상이 아니다 (`04_agent.sql` D1-a) |
| 현금신청 가능 금액 / 현물 신청 가능금액 | **가능** | F전제 | `f_profile` 한도 − `f_exec` 누적 (코드 계산) |

### P7. 새 지출 계획 — 3단계 위저드

🔴 **step1·step3은 구조째 교체 대상.** 백엔드 계약은 자연어 1건(`POST /api/normalize`)이고,
품목·금액·용도·날짜는 정규화가 뽑아 **화면 4에서 컨펌**받는다.

| 프로토타입 요소 | 판정 | 사유/조건 | 백엔드 대응 |
|---|:--:|:--:|---|
| **step1 지출 항목 입력칸** | **불가** | 원칙 | 자연어 textarea 1개로 교체. 폼으로 다 받으면 컨펌 로그 기반 `item_alias` 학습 경로가 사라진다 |
| **step1 사용 목적 입력칸** | **불가** | 원칙 | 동일 — 정규화 `용도` |
| **step1 예상 금액 입력칸** | **불가** | 원칙 | 동일 — 정규화 `금액` + `금액_추정여부` |
| **step1 예상 지출일 입력칸** | **불가** | 원칙 | 동일 — 정규화 `신청일` |
| **step1 거래처 입력칸** ("거래처 미정") | **불가** | 원칙 | 거래처명은 판정 스키마에 없다. **F5 체크박스 2문항으로 교체** |
| **step1 첨부파일** (견적서·과업자료, 파일 첨부) | **불가** | 원칙 | 판정 입력에 파일 없음 — 증빙 **목록**만 안내 |
| **step2 AI 비목 추천 배지 + 직접 변경** | **가능** | 즉시 | `비목후보[{비목, 신뢰도}]` + **최종 선택은 사용자** — 설계와 정확히 일치. 프로토타입 최대 자산 |
| step2 비목 목록 | **가능** | 즉시 | 단 **라벨 4건 교정 필요** (§3-2) |
| **step2 "어떤 지급수수료인가요?" 세부유형 16종** (기술이전·학회·전시회·시험인증·멘토링·기자재 임차·사무실 임차·운반·보험·보관·회계감사·세무기장·법인설립·기술보호·수리·법률컨설팅·기타) | **불가** | 미설계 | 룰 조회 키에 세부유형 축이 없다. 세부 구분은 정규화 `용도`가 담당 |
| **step3 비목별 예/아니오 질문 뱅크 (~60문항)** | **불가** | 원칙 | **되묻기 없는 설계.** 결핍은 판정 응답의 `전제[]`로 돌아온다 — 질문 뱅크 전체 제거, **전제[] 렌더러로 교체** |
| step3 중 **특수관계 2문항** (대표자·임직원이 최근 2년 내 해당 업체 근무 / 배우자·직계존비속) | **가능** | 즉시 | F5. **step1 체크박스로 이동.** 건별 수집 → 판정 후 폐기 |
| 취소 / 임시저장 | **불가** | 미설계 | 서버 draft 없음. 로컬 상태로만 |
| **저장하고 AI 점검** | **가능** | 목 | `normalize → (컨펌) → judge` 배선 |
| (프로토타입 없음) **정규화 컨펌 화면 (화면 4)** | **가능** | 목 | 백엔드가 다 준다 (§4-2) |

### P8. 지출 상세

| 프로토타입 요소 | 판정 | 사유/조건 | 백엔드 대응 |
|---|:--:|:--:|---|
| 지출 계획 정보 카드 — 항목·금액·사업 | **가능** | 목 | 정규화 결과 |
| 정보 카드 — **관련 비목** | **가능** | 목 | 확정비목 |
| 정보 카드 — **상세 설명** | **가능** | 목 | 정규화 `용도` |
| 정보 카드 — **거래처** | **불가** | 원칙 | 스키마에 없다 |
| 정보 카드 — **"(부가세 포함)"** | **불가** | 대기 | 부가세 처리 기준 미확인 — 룰 검수와 함께 확정 전 표기 보류 |
| **AI 점검 상태 배지 4종** (특이사항 없음 / 확인 필요 / 위험 / 준비 필요) | **불가** | 원칙 | 🔴 4-way 폐쇄 enum으로 전면 교체 (§3-1) |
| 파생 상태 어휘 (준비 완료 / 위험 가능성 / 사전조치 필요 / 점검 필요 / 단가 기준 확인 / 조건 확인 / 추가정보 확인) | **불가** | 원칙 | 동일 — 전부 enum 밖 |
| **AI 해설** (요약 문장) | **가능** | 목 | `요약` — ④-b가 직접 쓴다 |
| **결제 전 확인 카드들** | **가능** | 목 | `해야할일[{항목, 설명}]` |
| **필수 / 권장 구분** | **불가** | 대기 | 스키마에 우선순위 필드 없음 — 구분 없이 렌더하거나 스키마 확장 안건 |
| **적용 근거 카드** (규정 제목 + 출처) | **가능** | 목 | `인용[{조번호, 조제목, 원문, doc_id}]` — **원문까지 준다** (§4-3) |
| 자세히 보기 / 원문 보기 | **불가** | 미설계 | `doc_id`는 주지만 원문 뷰어 없음 |
| 확률 · % · "n% 확신" 표시 **없음** | **가능** | 즉시 | 프로토타입에도 없다 — **그대로 유지. 만들지 말 것** |
| 면책 문구 ("참고용, 최종 판단은 담당기관…") | **가능** | 즉시 | 유지 |
| 첨부 파일 목록·다운로드·삭제·추가 (최대 10개/20MB) · 파일 미리보기 | **불가** | 원칙 | 판정 입력에 파일 없음 |
| 집행 준비 — 결제 전 완료 항목 | **가능** | 목 | `해야할일` 재사용 |
| 집행 준비 — **체크 상태 저장** | **불가** | 미설계 | 저장처 없음. 로컬 상태로만 |
| 집행 준비 — **"일정에 추가" / 일정 등록 모달 / N일 전 알림** | **불가** | 스코프 | P10 참조 |
| **결제 후 필요 증빙 목록** | **가능** | 검수 | `rules.증빙 TEXT[]` — 룰 19행 D단계 검수 전 |
| 증빙 **발급처 링크** | **가능** | 목 | 팀 반입 자료(증빙서류 124종)에 있으나 **DB 미적재** |
| 증빙 **파일 업로드** | **불가** | 스코프 | 정산 서류 관리는 범위 밖 |
| 필요 증빙 PDF 다운로드 | **가능** | 검수 | 프론트 자체 생성 — 데이터는 룰 검수 후 |
| 지출 계획 수정 / 삭제 | **불가** | 미설계 | 서버 저장 없음. 수정 시 **재판정**이 원칙 (판정은 입력에 종속) |
| (프로토타입 없음) **신뢰등급 A/B · 버전스탬프 · 참조사슬 · 전제[]** | **가능** | 목 | 전부 코드가 부착해 준다 (§4-3) |
| (프로토타입 없음) **판정 = 불가 · 판단불가 상태** | **가능** | 목 | 4-way 중 2개가 통째로 없다 (§4-4) |
| (프로토타입 없음) 조항별 해당/비해당/불확실 | **불가** | 대기 | ④-a 스크리닝 채택 미결 — 레이아웃만 |

### P9. AI에게 물어보기 (챗봇) — **화면 전체 불가**

| 프로토타입 요소 | 판정 | 사유/조건 | 백엔드 대응 |
|---|:--:|:--:|---|
| **자유 질의 챗 전체** (메시지 전송/전송 중/전송 완료/다시 전송) | **불가** | 원칙 | 🔴 판정 1건 = LLM 2회 고정 · function calling 미개방 · agentic multi-hop 미채택. **자유 QA 슬롯은 존재하지 않는다** |
| "작성한 지출 계획 N건 · 최신 규정 안내를 함께 확인" 참조 범위 | **불가** | 원칙 | 해당 컨텍스트 주입 경로 없음 |
| "현재 지출 우선 참조" 모드 | **불가** | 원칙 | 동일 |
| 응답 내 "가장 먼저 할 일" / "필요 증빙" 퀵액션 | **불가** | 원칙 | 판정 응답(`해야할일`·`rules.증빙`)에 이미 있다 — 챗으로 다시 물을 필요가 없다 |
| 추천 질문 칩 ("노트북 구매 전에 뭘 준비해야 해?" / "회의비 단가를 확인해줘" / "외주 계약 증빙이 궁금해") | **가능** | 즉시 | ⚠️ 챗이 아니라 **화면 3의 예시 질문 칩으로 이동** — 누르면 판정이 시작되게 |

**대체안**: ① 판정 흐름 자체가 질문-답이다 → "AI에게 물어보기" = **새 판정 시작**
② 판단불가 건은 **화면 9 문의 초안**(LLM ⑤ 슬롯, 2회 셈 밖)이 담당한다.

### P10. 집행 일정 (캘린더) — **서버 기능 전체 불가**

| 프로토타입 요소 | 판정 | 사유/조건 | 백엔드 대응 |
|---|:--:|:--:|---|
| 일정 CRUD (추가/수정/삭제) · 캘린더 뷰 · 리스트 뷰 · 달 이동 | **불가** | 스코프 | 일정 관리는 백엔드 설계에 없다 |
| 알림 (알림 없음 / 1일·3일·7일 전) | **불가** | 스코프 | 푸시·알림 인프라 없음 |
| 일정 체크 항목 · 메모 | **불가** | 스코프 | 저장처 없음 |
| 일정 유형 "사전승인" | **불가** | 미설계 | 개념은 `rules.사전승인` 플래그와 연결되지만 **일정으로 저장할 곳이 없다** |
| 일정 유형 계약·견적·증빙·집행 | **불가** | 스코프 | 상동 |
| 관련 지출 계획 연결 | **불가** | 미설계 | 계획 엔티티 자체가 없다 |

**처분 권고**: MVP는 화면째 제거. 데모용으로 남기려면 **전부 localStorage 명시** —
서버 연동 기대를 만들지 말 것.

### P11. 규정·지침

| 프로토타입 요소 | 판정 | 사유/조건 | 백엔드 대응 |
|---|:--:|:--:|---|
| 현재 적용 사업 카드 + 사업기간 | **가능** | F전제 | 온보딩 + `f_profile` |
| 공통 관리 기준 카드 (모집공고 · 통합관리지침) | **가능** | 목 | L1·L2 코퍼스 실재 — `documents` 메타(제목·차수·시행일) 노출 |
| "자동 반영" 배지 | **가능** | 목 | 인덱스에 이미 들어있다는 뜻으로 사실과 일치 |
| 원문 보기 (공통 기준 · 통합관리지침) | **불가** | 미설계 | 원문 뷰어 없음 |
| 기관 지침 파일 카드 + **"87개 점검 조항 연결"** | **가능** | 목 | `SELECT COUNT(*) FROM tenant.l3_articles WHERE doc_id=?` — 적재 결과 그대로 |
| 파일 보기 | **불가** | 미설계 | 뷰어 없음 |
| 파일 교체 / 기관 등록 | **가능** | 목 | P3와 동일 (업로드 API 미구현) |
| (프로토타입 없음) **재업로드 dangling 리포트** | **가능** | 목 | `l3_documents.dangling수` — "업로드 시점에 알린다"가 설계 원칙 |
| 기관 기준 추가 확인 항목 표 (회의비 1인 단가 / 범용성 기자재 / 외주용역 계약) | **가능** | 검수 | 콘셉트가 L2>L3 우선순위 구조와 부합. **정적 하드코딩 → 룰 테이블 조회로 교체** |
| "공통 기준만으로 결론내리지 않고 기관 안내를 함께 확인합니다" 문구 | **가능** | 즉시 | `precedence_rules` 병합 원칙과 일치 |
| 면책 문구 | **가능** | 즉시 | 유지 |

### P12. 내 정보

| 프로토타입 요소 | 판정 | 사유/조건 | 백엔드 대응 |
|---|:--:|:--:|---|
| 이메일 | **가능** | 목 | `accounts.email` |
| 선정사업 | **가능** | 목 | `f_profile.사업명` |
| 정보 수정 / 로그아웃 | **가능** | 목 | `accounts` 존재. 로그인·동의 화면은 미결 #3 |
| **이름 · 연락처 · 소속** | **불가** | 원칙 | 저장 계층화 표에 없는 컬럼. **최소 수집 원칙** — 개인정보 방침 확정 전 만들지 않는다 |
| (프로토타입 없음) **F 프로필 탭** (F1 4분면+협약기간 / F3 집행내역 / F4 인력) | **가능** | 목 | DDL 확정 — 필드가 안 바뀐다 (§4-1) |
| (프로토타입 없음) **PMS 붙여넣기 파서** | **가능** | 즉시 | 브라우저 정규식. **서버와 무관** — PMS 원문은 서버로 보내지 않는다 |
| (만들면 안 되는 것) **F4 인력 이름 입력칸** | **불가** | 원칙 | 🔴 역할·수치만 저장. 나중에 못 바꾼다 |

---

## 3. 어휘 대조 — 문자열이 안 맞으면 배선이 안 된다

### 3-1. 판정 상태 — 프로토타입 어휘는 전부 불가

백엔드 `판정`은 **4-way 폐쇄 enum**이고 vLLM `guided_json`이 토큰 레벨에서 강제한다.
enum 밖 문자열은 백엔드가 **만들어낼 수 없다**.

| 프로토타입 어휘 | 판정 | 매핑 대상 |
|---|:--:|---|
| 특이사항 없음 | **불가** | → `가능` |
| 확인 필요 | **불가** | → `조건부` |
| 준비 필요 · 사전조치 필요 · 조건 확인 · 추가정보 확인 · 단가 기준 확인 | **불가** | → `조건부` |
| 위험 · 위험 가능성 | **불가** | → `불가` |
| 준비 완료 · 점검 필요 · 점검 전 | **불가** | 판정이 아니라 진행 상태 — 분리하거나 제거 |
| — | — | 🔴 **`불가`에 대응하는 어휘가 없다** ("위험"은 경고지 금지가 아니다) |
| — | — | 🔴 **`판단불가`가 통째로 없다** — 에러가 아니라 정상 경로 |

**커버리지 2/4.** 오답 비대칭 원칙("최악은 틀린 가능")상 없는 쪽 둘이 하필 안전한 쪽이다.

### 3-2. 비목 라벨 — 4건 불일치

정본은 `rule_base.md` §1-b 어휘집 enum 10종. 정규화 출력·룰 조회·골든셋·`f_exec.비목`이
전부 같은 문자열을 쓴다.

| 프로토타입 라벨 | 판정 | 정본 enum |
|---|:--:|---|
| 재료비 · 외주용역비 · 인건비 · 지급수수료 · 여비 · 교육훈련비 · 광고선전비 | **가능** | 그대로 일치 |
| 기계장치비 | **불가** | → **`기계장치`** (공구·기구·비품·SW 포함) |
| 특허권 등 무형자산 취득비 | **불가** | → **`특허권등무형자산취득비`** (띄어쓰기 없음) |
| **회의비** | **불가** | 🔴 **enum에 없다.** 창업기업 법정 비목이 아니다 — 룰 조회가 0행을 돌려준다 |
| (프로토타입 없음) **창업활동비** | **가능** | 월 50만원 한도. **예비창업패키지에만 노출** — 사업에 따라 목록이 달라진다 |

⚠️ "회의비"는 프로토타입 데모 3건 중 1건(시장조사 인터뷰 회의비)의 근간이라
**데모 시나리오까지 같이 바꿔야 한다.**

### 3-3. 사업명 — 8종 전부 가능

프로토타입 라벨이 사업 스코프와 **정확히 일치한다.** 특히 "모두의 창업 **일반・기술**"은
로컬트랙 제외를 이미 반영하고 있다. 표시 라벨은 유지하고 룰 조회 키만 매핑한다.

| 프로토타입 표시명 | 판정 | 룰 조회 키 |
|---|:--:|---|
| 2026 예비창업패키지 / 초기창업패키지 / 창업도약패키지 / 창업중심대학 / 재도전성공패키지 | **가능** | 동명 |
| 2026 모두의 창업 일반・기술 | **가능** | `모두의 창업 프로젝트` (제3편 로컬트랙 제외) |
| 2026 초격차 스타트업 1000+ | **가능** | `초격차 스타트업 프로젝트` |
| 2026 민관공동 창업자 발굴·육성 | **가능** | `TIPS(민관공동 창업자 발굴·육성)` |

---

## 4. 역방향 — 백엔드는 주는데 프론트에 자리가 없는 것

여기가 진짜 손해다. **14건 전부 "가능"인데 그릴 곳이 없다.**
(P3 DRM 실패 안내 · P11 dangling 리포트는 §2 화면표 안에 있어 아래 셈에서 뺐다.)

### 4-1. F 프로필 (F1 · F3 · F4) — 화면이 통째로 없음

`f_profile` · `f_exec` · `f_personnel` DDL이 확정인데 입력 화면이 어디에도 없다.
이게 비면 홈의 **D-129**, CSV의 **현금/현물 신청 가능 금액**, 한도 비교(1인 1대·월 50만원·
선급금 50%)가 전부 계산되지 않는다. **PMS 붙여넣기 파서**는 브라우저에서만 돌아
백엔드 대기 없이 지금 만들 수 있다.

### 4-2. 정규화 컨펌 화면 (화면 4)

`POST /api/normalize` SSE가 `품목·금액·금액_추정여부·용도·비목후보[]·결제수단·구매명의·
신청일·비교견적`을 준다. 프로토타입은 step2에서 **비목만** 확인한다.

- **"추정값입니다" 배지** (`금액_추정여부=true`) — 없음
- **빈 값 UI** (결제수단·구매명의·신청일·비교견적은 각각 `null` 가능) — 없음
- **비목이 갈릴 때 두 경로 병렬 판정 표시** — 없음 (분기 임계치는 골든셋 실측 대기)

### 4-3. 판정의 신뢰 근거 4종 — P8에 자리 없음

⑥ 단계에서 **코드가** 부착하는 값들이다. LLM 자칭이 아니라 검증 가능한 값이라
오히려 이게 제품의 방어선이다.

| 백엔드가 주는 것 | 프로토타입 |
|---|---|
| `신뢰등급` A / B (인용 청크의 `extraction` 속성으로 코드 산정) | 없음 |
| `버전스탬프` ("제14차, 2025.12.23 기준") | 없음 |
| `참조사슬[{from, 표기, to, 보정}]` — "이 규정이 왜 나에게 적용되나" | 없음 |
| `전제[{사실, 근거조항, 매핑, 미충족시}]` — 무엇이 없어서 못 정했나 | 없음 |
| `인용[].원문` — 조항 **원문**까지 준다 | 제목·출처만 표시 |

### 4-4. 판단불가 경로 + 화면 9 문의 초안

`판정="판단불가"`는 **에러가 아니라 4-way 중 하나**이고, 검색 게이트(최고 스코어 < 임계치)와
투표 불일치가 여기로 떨어진다. 빨간 에러 화면이 아니라 문의 초안 안내로 그려야 한다.
문의 초안 텍스트는 LLM ⑤ 슬롯(2회 셈 밖) — **프롬프트 미설계**라 이것만 조건부다.

---

## 5. 불가 총괄 — 사유별로 묶어서

### 5-1. 원칙 위반 (재제안 금지) — 되살아나지 않는다

```
자유 질의 챗봇 (P9 전체)          판정 1건 = LLM 2회 고정 · function calling 미개방
일괄 N건 동시 점검 (P6)           GPU 직렬 큐 · 동시 배치 미설계
step1 구조화 폼 4칸 (P7)          자연어 1줄이 계약 · item_alias 학습 경로가 사라진다
step3 질문 뱅크 ~60문항 (P7)      되묻기 없는 설계 — 결핍은 전제[] 로 돌아온다
파일 업로드 3종 (P6·P7·P8)        판정 입력에 파일 없음 (계획서·견적서·증빙)
거래처명 입력·표시·검색            판정 스키마에 없다 (F5 체크박스 2문항으로 대체)
이름·연락처·소속 저장              저장 계층화 — 최소 수집
F4 인력 이름 입력칸                🔴 만들면 나중에 못 바꾼다
상태 어휘 4종 + 파생 7종           4-way 폐쇄 enum 밖 — 백엔드가 만들어낼 수 없다
단계 스피너 · "5~10초"             SSE 스트리밍 렌더 (첫 글자 1초, p50 3.2초)
```

### 5-2. 스코프 밖 — MVP에서 안 만든다

```
집행 일정 · 캘린더 · 알림 · 체크항목 · 메모 (P10 전체)
증빙 파일 업로드 (정산 서류 관리)
"일정에 추가" 연동 (P5·P8)
```

### 5-3. 미설계 — 설계하면 살아난다

```
지출 계획(draft) 엔티티 · 수정/삭제 API      서버에 계획 테이블이 없다
판정 이력 조회 API                           decisions 테이블은 있다
지출명 · 최근 수정일 컬럼                     decisions에 대응 필드 없음
원문 뷰어 (P5·P8·P11 4곳)                    doc_id는 준다
세목·세세목 / 지급수수료 세부유형 16종        룰 키가 사업 × 비목 단일 계층
CSV "산출 근거" 컬럼                          대응 필드 없음
DOC·DOCX 파서                                PDF·HWPX·HWP만
```

### 5-4. 결정 대기 — 지금 논쟁하지 말 것

```
조항별 해당/비해당/불확실 (P8)    ④-a 스크리닝 채택 미결 — 레이아웃만 잡아둘 것
필수/권장 구분 (P8)               해야할일 스키마에 우선순위 필드 없음
"(부가세 포함)" 표기 (P8)         부가세 처리 기준 미확인 — 룰 검수와 함께
```

⚠️ **④-a를 기각하면 화면 7(조항별 판단 구조)의 재료 산출 방식을 재설계해야 한다.**

---

## 6. 프론트에 넘길 순서

백엔드 대기 없이 지금 시작할 수 있는 것부터.

| 순위 | 작업 | 왜 안 막히나 |
|:--:|---|---|
| 1 | **상태 어휘 4-way 전면 교체** + 판단불가 UI | enum이 확정이다. 문자열만 바꾸면 된다 |
| 2 | **P9 챗봇 · P10 일정 · 파일 업로드 3종 · 일괄 점검 제거** | 되살아나지 않는다 — 빨리 걷어낼수록 이득 |
| 3 | **P7 step1 → 자연어 textarea + F5 체크 2개**, step3 → 전제[] 렌더러, **화면 4 신설** | 입력 계약이 확정 |
| 4 | **P8에 신뢰등급·버전스탬프·인용 원문·참조사슬 자리 신설** | 스키마 확정. 목 JSON으로 완성 가능 |
| 5 | **F 프로필 화면 + PMS 붙여넣기 파서** | DDL 확정 + 파서는 브라우저에서만 돈다 |
| 6 | 비목 라벨 4건 교정 · 지급수수료 세부유형 제거 · 창업활동비 조건부 노출 | 어휘집이 정본 |
| 7 | 스피너 → SSE 스트리밍 렌더 2군데 (normalize · judge) | 목 SSE로 배관 가능 |
| 8 | 목록·현황·이력은 로컬 상태 + 목 API | 이력 조회 API 설계 대기 |
| 9 | 온보딩 확장자에서 DOC·DOCX 제거 + DRM HWP 실패 안내 UI | 파서 경로 확정 |

---

## 7. 근거 문서

| 판정 근거 | 문서 |
|---|---|
| 확정 원칙 (LLM 2회 · function calling · 인덱스 경계) | `CLAUDE.md` · `서비스 아키텍쳐.md` §1·§4 |
| API 계약 · 화면별 지원 여부 | `프론트 연동 사양.md` §1~§9 |
| 판정 출력 스키마 [1겹]/[2겹] | `LLM.md` §3-4 |
| 비목 enum · 룰 스키마 · 우선순위 병합 | `rule_base.md` §1-b·§2·§3 |
| tenant DDL (F축·L3·decisions) | `RAG.md` §2-3 · `db/init/01_schema.sql` |
| 화면별 상세 수정 지시 | `프론트 프로토타입 검토.md` |
| AI/코드/프론트 분업 | `프로토타입_기능구분.md` |

**충돌 시 `서비스 아키텍쳐.md`가 이긴다. 문서와 코드가 어긋나면 코드를 믿는다.**
