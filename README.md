# 써도돼요

**창업지원금 지출 사전승인 판정 서비스.** 창업기업이 “이거 사도 돼요?”를 물으면
규정을 찾아 **가능 · 조건부 · 불가 · 판단불가** 를 근거와 함께 답한다.

금융 AI 공모전 6주 MVP · 창업진흥원 8개 사업 대상.

---

## 이 제품의 요점 넷

**① 판정 1건 = LLM 2회로 고정한다.**
검색·룰 조회·금액 비교·효력 결정은 **코드가** 한다. LLM 은 ① 정규화와 ④ 조립만 한다.
function calling 을 열지 않는다 — 호출 수가 고정돼야 지연을 예측하고 재현할 수 있다.

**② 인용은 생성하지 않고 «추출» 한다.**
LLM 은 원문을 쓰지 않는다. 조립 단계에서 근거마다 `S01…Sn` 번호를 붙이고, LLM 은
**S번호만** 고른다. 출력 스키마가 2겹인 이유가 이것이다 — [1겹] LLM 이 채우는 칸,
[2겹] 코드가 채우는 칸(원문·조번호·doc_id).

**③ 모든 실패의 기본값은 판단불가다.**
근거가 모자라면 “아마 가능”을 만들지 않는다. 사용자는 담당자에게 물어보면 되지만,
틀린 “가능”은 돈을 잘못 쓰게 만든다.

**④ 판정 인덱스의 경계를 «구조로» 강제한다.**
프롬프트로 부탁하지 않는다. 타 기관 규정·정답셋은 인덱스에 물리적으로 안 들어간다.

---

## 판정 흐름 — 어디서 빠져나가고, 그때까지 얼마를 썼는가

```
입력 (품목 · 금액 · 사업 · 기관)
 └ ① 정규화 [LLM 1회]                      품목·용도·금액·비목후보
 └ 비목확정 (룰 테이블 · 별칭 326)
     └ 게이트 C  비목이 갈리면 → 두 갈래 판정 → "선택필요"
 └ L3 룰 조회 + 게이팅 · effective_rule(한도)
     └ 게이트 A  금지목록 «문자» 적중 → 즉답 "불가"   [LLM 총 1회로 끝]
 └ ③ L3 통째로드  ∥  검색: dense + BM25 → RRF → top-5 → 참조 깊이1 확장
     └ 게이트 B  dense 최고값 < 임계 → "판단불가"     [LLM 총 1회로 끝]
 └ ④ 조립  B0지시 B1(L3) B2(검색) B3(참조) B4(룰문장) B5(기관사실) + S번호
 └ ④ 판정 [LLM 2회, guided_json]            S번호만 인용
 └ ⑥ 검증·강등  →  ⑤ 전제해소  →  응답
```

`⑥ 검증`이 `⑤ 전제해소`보다 먼저인 것은 오타가 아니라 실제 실행 순서다.

---

## 규범 세 층과 효력

```
L1  중소벤처기업부   법률·고시·훈령·통합관리지침     우리가 조달
L2  창업진흥원       사업별 세부관리기준             우리가 조달
L3  주관기관         기관 규정·협약 특약             사용자가 «업로드»
```

**충돌 해소는 「아래가 엄격하면 이긴다」가 아니다 — 반대가 규칙이다.**
8개 사업 중 6개가 적용범위 조에서 `L2 > L3` 를 명시한다(주관기관 규정이 더 엄격해도 진다).
초격차·모두의창업·TIPS 는 `L1 > L2`(지침에 없는 것만 세부관리기준)도 갖는다.
이 관계는 `corpus.precedence_rules` 9행에 **조문 근거와 함께** 들어 있다.

L3 는 검색하지 않고 **통째로 읽는다**. `chunks` 가 아니라 `tenant.l3_articles` 라
다른 테이블이고, 그래서 누수가 **구조적으로 불가능**하다.

---

## 판정 인덱스의 경계

```sql
-- scripts/retrieve.py
FILTER = "embedding IS NOT NULL AND status='active' AND parse_quality='high'
          AND retrieval_scope='진입점' AND layer IN ('L1','L2')"
```

이 한 줄이 경계다. 여기서 나오는 성질 셋:

- **타 기관 규정(L4)은 안 들어간다.** 남의 규정이 인용되는 순간 그 자체가 오답이다.
- **정답셋은 안 들어간다.** 정답 유출.
- **검수 전 문서는 안 보인다.** 자동 수집분은 `status='staged'` 로 들어오고, 사람이
  검수해 `'active'` 로 올린다. 필터를 깜빡할 자리가 없다.

새 인덱싱 경로는 `scripts/archive/eval/index_guard.py` 를 반드시 통과한다.

---

## 규정집이 낡지 않게 하는 경로

```
매주 월요일 10:00 KST (Cloud Scheduler)
 └ 발행처에서 현행판 수집 → 우리 판과 비교
     └ 같으면 «아무것도 안 한다»
 └ 파싱 (pdftext · hwp · hwpx) → 🔴 «표 검산»
     └ fail → corpus 에 «안 들어간다». 사람 확인 대기
     └ warn → status='staged' + 표시
     └ pass → status='staged'
 └ 사람이 검수 → 'staged' → 'active'  (옛 판은 'superseded')
 └ 바뀐 조를 근거로 쓰는 룰·체크항목 → recheck_queue
```

**표 검산이 관문인 이유**: 별표·한도표가 뭉개진 채 들어가면 규정이 깨진 채 판정에 쓰이고
아무도 모른다. 실측으로 어휘 재료 6개 중 5개가 그 상태로 DB 에 있던 적이 있다.

사용자 업로드(L3)는 이미 자동이다 — `POST /api/l3/upload` 가 202 를 먼저 주고
배경에서 파싱·적재·품질판정까지 한다.

---

## 지금 코퍼스

| | |
|---|---|
| 활성 문서 | 245 |
| 조문 | 24,574 |
| 검색 청크 | 진입점 1,455 · 폐포전용 19,101 |
| 조문 간 참조 | 44,865 |
| 룰 | 74행 (금지예시 358 · 허용예시 225) |
| 우선순위 룰 | 9 |
| 체크항목 | 52 |
| 비목 | 창업 10 · RND(TIPS) 8 · 별칭 326 |
| 정답셋 | 315문항 (검증 307) |
| L3 (업로드분) | 224조각 |

---

## 이 프로젝트가 지키는 규율

**측정값 하나로 원인을 확정하지 않는다.** 함수를 실제로 태워 보기 전에 원인을 정하지 않는다 —
스키마를 보는 것과 함수를 부르는 것은 다른 일이다.

**관측과 추론을 갈라 쓴다.** 둘을 같은 문단에 같은 어조로 적으면 읽는 사람이 못 가른다.

**조건이 다른 run 끼리 지표를 빼지 않는다.** 프롬프트·코퍼스·정답지가 바뀌면 같은 것을 잰 게
아니다. 그래서 정답지에 지문(fingerprint)을 붙이고 경계선을 문서에 남긴다.

**「없다」와 「못 봤다」를 가른다.** 부재는 읽어서 안 나온다 — **있으면 보일 조건**을 만들어
확인한다. 검사기가 0을 내면 「같다」가 아니라 「못 맞췄다」를 먼저 의심한다.

**「검증 통과」는 「판정이 맞다」의 증거가 아니다.** 「올바르게 뽑았다 / 안 죽는다」의 증거다.

---

## 읽는 순서

`docs/README.md` → 지금 상태 `docs/0_현황.md` → 결정 대기 `docs/9_미결.md`

판단이 필요한 원칙은 `CLAUDE.md` 에 있다. 기계가 검사할 규칙은 `.claude/hooks/` 와
`scripts/archive/eval/index_guard.py` 에 있다.
