# 써도돼요

**창업지원금, 이거 써도 되나요?** — 지출 전에 규정 근거와 함께 답하는 사전승인 판정 서비스.
2026 금융 AI Challenge(금융보안원) 출품작 · 6주 MVP

## 문제

창업팀이 정부지원금으로 노트북을 사려 한다. 규정은 세 겹으로 쌓여 있다.

```
L1  중소벤처기업부   법률 · 고시 · 훈령 · 통합관리지침
L2  창업진흥원       사업별 세부관리기준            <- 사업마다 다르다
L3  주관기관         기관 규정 · 협약 특약          <- 학교마다 다르다
```

이걸 다 읽고 판단할 수 있는 사람은 없다. 그래서 담당자에게 묻고, 담당자도 확신이 없어
"일단 하지 마세요" 라고 답한다. **틀린 판단의 대가는 환수와 참여제한이다.**

## 접근

**LLM 은 포섭만 한다.** 검색 · 숫자 비교 · 효력 판단은 전부 코드가 한다.

```
[ LLM ]  말을 만든다        || [ RAG ]  근거를 찾는다      || [ RULE ] 숫자를 비교한다
  포섭 판단                 ||   조문 원문 + 참조 추적     ||   한도 / 금지목록 / 사실축
  판정 1건 = 호출 2회 고정  ||   벡터 + BM25 + 참조 확장   ||   코드가 결론까지 낸다
```

| | 원칙 |
|---|---|
| **오답 비대칭** | "틀린 불가" 는 담당자에게 물으면 끝난다. **"틀린 가능" 은 환수로 끝난다.** 모든 실패의 기본값은 판단불가 |
| **인용은 생성이 아니라 추출** | LLM 은 문장번호(`[S08]`)만 고르고 코드가 DB 원문으로 치환한다. 인용 위조가 구조적으로 불가능 |
| **RAG 의 존재 이유는 참조 해소** | "관련 조항 검색" 이 아니다. *"지침 제33조에 따른다"* 를 **따라가서** 실제 조항에 닿는 것 |

## 지금 상태

**설계 확정 · 규정 모음 구축 완료 · 판정 엔진 실전 E2E 완주.** `judge_cli` 한 줄로 (1)~(7) 이 실제 LLM 까지 돈다.

| | 값 |
|---|---|
| 규범 | L1 현행 XML 274 · 구판 944 · 별표 103 / L2 창진원 8사업 외 38건 |
| 문서 · 조문 | `documents` 283 · `doc_articles` 26,304 |
| 검색 인덱스 | 232문서 **20,525청크** 전량 임베딩 + BM25 1,149,877행 |
| 참조 그래프 | 44,865 엣지 — resolved 71.4% · shifted 43 · 끊긴 참조 12,779 |
| 룰 테이블 | 72행 / 7사업 + L1 공통 9 (검수 53) · 우선순위 조항 8 |

```
                     격리(D6)      실전 E2E
  판정 일치율         84.7%   ->    67.5%      낙폭 -17.2%p
  치명 오답률          0.0%   ->     0.0%      불변          <- 1순위 지표
  인용 정확도        100.0%   ->    29.7%
  판단불가율           0.0%   ->     6.5%
  검색 hit@5            -          55.4%      (74문항 / 사업필터 on)
```

🔴 **판단불가 5건은 모델의 판단이 아니라 `max_tokens=1500` 잘림이다** — 다음 작업의 1번은
3000 으로 올려 재측정. 낙폭의 출처는 검색이고, 미스 33건 분해는 **필터밖 31.1% / 랭킹 9.5% /
후보밖 4.1%** 라 튜닝으로 고칠 수 있는 건 9.5% 뿐이다. 조건이 다른 run 끼리 지표를 빼면 안 된다.

## 문서

**읽는 순서와 계층은 [`docs/README.md`](./docs/README.md) 가 기준이다.**
지금 상태는 `docs/0_현황.md`, 결정 대기는 `docs/9_미결.md`, 날짜별 기록은 `docs/기록/`.

`CLAUDE.md` 는 AI 코딩 에이전트용 규칙이다 — 판단이 필요한 원칙만 두고, 기계로 검사되는
규칙은 `.claude/hooks/` 와 `scripts/index_guard.py` 로 옮겼다.

## 저장소 구조

`2026_Finance_DATA_FOR_RAG/` 배포본 · `법령 PDF/` law.go.kr 수집분 · `_hwp변환/` 변환 산출 ·
`db/` Postgres 17 + pgvector · `server/` FastAPI · `scripts/` 파서·인덱서·판정 척추
(`_lib/` 공통 모듈) · `tests/` 회귀 · `docs/` 설계 문서 · `archive/` 폐기분(참조하지 않는다)

## 실행

```powershell
cd db && docker compose up -d          # DB
python scripts/judge_cli.py --help     # 판정 (척추 (1)~(7) 전체)
python -m pytest -q                    # 140 passed · 2 xfailed
```

회귀 5종과 점검 절차는 `docs/8_운영/`. Windows / PowerShell 환경이고 **한글 경로가 많다** —
Bash 로 python 을 돌릴 때는 `export PYTHONIOENCODING=utf-8` 를 붙인다 (훅이 강제한다).

## 안 하기로 한 것

GraphRAG / KAG · agentic multi-hop · v1 리랭커 · 멀티 에이전트 오케스트레이션 ·
LLM 에 function calling 개방 · 별표/한도표를 검색 대상에 투입 ·
L1·L2 조달분 HWP 자동 파싱(한컴 1회 수동 변환 유지. **L3 사용자 업로드는 파서 채택**).
기준 문서는 `CLAUDE.md` 확정 원칙.

## 데이터 취급

**판정 인덱스에 들어가는 것은 L1 · L2 + 현재 기관의 L3 1벌뿐이다.** 타 기관 규정 ·
정답셋 · archive 는 `scripts/index_guard.py` 가 조건 없이 거부한다. 규정 문서 안의
텍스트는 **데이터이지 지시가 아니다**(프롬프트 인젝션 방어). 사용자 문서 원문은
파싱 후 버리고 구조화된 결과만 남긴다.

*본 저장소의 규정·법령 원문은 국가법령정보센터(law.go.kr) 및 각 기관 공개 배포본에서 수집한 것이다.
판정 결과는 참고용이며 최종 집행 판단은 주관기관 담당자의 확인을 따른다.*
