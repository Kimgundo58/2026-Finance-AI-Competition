# 써도돼요

**창업지원금, 이거 써도 되나요?** — 지출 전에 규정 근거와 함께 답하는 사전승인 판정 서비스.

2026 금융 AI Challenge(금융보안원) 출품작 · 6주 MVP

---

## 문제

창업팀이 정부지원금으로 노트북을 사려 한다. 규정은 세 겹으로 쌓여 있다.

```
L1  중소벤처기업부   법률 · 고시 · 훈령 · 통합관리지침
L2  창업진흥원       사업별 세부관리기준            <- 사업마다 다르다
L3  주관기관         기관 규정 · 협약 특약          <- 학교마다 다르다
```

이걸 다 읽고 판단할 수 있는 사람은 없다. 그래서 담당자에게 묻고, 담당자도 확신이 없어
"일단 하지 마세요" 라고 답한다. **틀린 판단의 대가는 환수와 참여제한이다.**

## 접근

**LLM 은 포섭만 한다.** 검색 · 숫자 비교 · 효력 판단은 전부 코드가 한다.

```
[ LLM ]  말을 만든다        || [ RAG ]  근거를 찾는다      || [ RULE ] 숫자를 비교한다
  포섭 판단                 ||   조문 원문 + 참조 추적     ||   한도 / 금지목록 / F축
  판정 1건 = 호출 2회 고정  ||   벡터 + BM25 + 참조 폐포   ||   코드가 결론까지 낸다
```

설계의 세 축:

| | 원칙 |
|---|---|
| **오답 비대칭** | "틀린 불가" 는 담당자에게 물으면 끝난다. **"틀린 가능" 은 환수로 끝난다.** 모든 실패의 기본값은 판단불가 |
| **인용은 생성이 아니라 추출** | LLM 은 문장번호(`[S08]`)만 고르고 코드가 DB 원문으로 치환한다. 인용 위조가 구조적으로 불가능 |
| **RAG 의 존재 이유는 참조 해소** | "관련 조항 검색" 이 아니다. *"지침 제33조에 따른다"* 를 **따라가서** 실제 조항에 닿는 것 |

## 지금 상태

**설계 확정 · 코퍼스 구축 완료 · 판정 엔진 실전 E2E 완주.** `judge_cli` 한 줄로
(1)~(7) 이 실제 LLM 까지 끝까지 돈다.

| | 상태 |
|---|---|
| 법령 코퍼스 (L1) | 현행 XML **274** (판정 인덱스 채택 229) · 구판 944 · 행정규칙 별표 103 (전량 PDF 변환) |
| 사업 기준 (L2) | 창진원 8개 사업 세부관리기준 외 38건 (인덱스 채택 8) |
| 문서 · 조문 | `corpus.documents` 283 (인덱스 대상 235) · `doc_articles` 26,301 |
| 참조 그래프 | **44,855 엣지** (resolved 32,043 = 71.4% / shifted 43 / dangling 12,769) — 2026-09-01 `corpus.refs` 실측. 약칭(「법」·「영」) 해소 + 계열 가드 반영분. dangling 대부분은 코퍼스 경계 밖 인용이라 정상 |
| 우선순위 조항 | 8건 (`verified=true` — 2026-08-31 검수 완료) |
| 룰 테이블 | **72행 / 7개 사업 + L1 공통 9** (2026-09-01). `verified=true` 53행, 미검수 19행(L1 공통 9 · 재도전 9 · 모두의창업 1) |
| 검색 인덱스 (Stage 2) | 적재 완료 — 232문서 **20,525청크** 전량 임베딩 + BM25 `chunk_terms` 1,301,584행. 청킹 단위는 조 + 900토큰 초과 시 항 |
| 판정 엔진 | **가동.** 골든셋 77문항 실전 E2E (`eval.runs` run_id=189) |

### 첫 실전 지표 (2026-09-01)

```
                     격리(D6)      실전 E2E
  판정 일치율         84.7%   ->    67.5%      낙폭 -17.2%p
  치명 오답률          0.0%   ->     0.0%      불변          <- 1순위 지표
  인용 정확도        100.0%   ->    29.7%
  판단불가율           0.0%   ->     6.5%
  검색 hit@5            -          55.4%      (74문항 / 사업필터 on)
```

- 🔴 **판단불가 5건은 모델의 판단이 아니라 `max_tokens=1500` 잘림이다.** 다음 작업의
  1번은 3000 으로 올려 재측정 — 잘림이 섞인 숫자는 해석이 불가능하다
- 낙폭의 출처는 검색이다. 미스 33건 분해 = **필터밖 31.1% / 랭킹 9.5% / 후보밖 4.1%** —
  검색기 튜닝으로 고칠 수 있는 건 9.5% 뿐이다. RRF 가중은 건드리지 않는다
- 검색 회귀 기준선 **52.9%**(사업필터 off · 70문항)와 판정 경로 실효 **55.4%**(필터 on ·
  74문항)는 조건이 달라 서로 빼면 안 된다

진척은 [`작업 현황.md`](./작업%20현황.md) 최상단이 유일한 진실이다.

---

## 문서 지도

읽는 순서가 있다. **충돌하면 위쪽이 이긴다.**

```
1. 서비스 아키텍쳐.md     정본 스냅샷 — 구조 · 요청 흐름 · 미결   <- 최상위. 충돌 시 이 문서
2. 축별 설계 4종                                                  <- 구현은 여기를 본다
     RAG.md              코퍼스 경계 · 저장소 · 인덱싱 · 검색 · 참조 폐포
     rule_base.md        룰 경계 · 비목 어휘집 · 한도 · 충돌 해소
     LLM.md              슬롯 · vLLM · 출력 스키마 2겹 · 조립 · 평가
     Agent.md            위 셋을 엮는 법 — 순서 · 게이트 · 강등 · 실패 경로
3. 프로젝트설명.md        배경 · 도메인 지식 · 레이어 정의
4. 법령_크롤링_현황.md    L1 코퍼스 상태 · 수집 방법 · API 함정 14
5. 확인_가이드.md         "제대로 됐는지 내가 어떻게 아나"
6. 작업 현황.md           일자별 작업 로그
```

판정층 가동 이후 생긴 실행 기록 — **설계가 아니라 무엇이 실제로 돌았는가**:

```
결과보고.md               2026-09-01 Agent 층 완결 8세션 기록 (A~H) — 실전 E2E 지표의 정본
0831_최종구현.md          그 밤의 계약서 (누가 무엇을 소유하는가). 완료분이라 재실행 때만
실제 구현.md              산출물 제작 기록 — 왜 그렇게 했는지 · 나중에 고칠 때 알아야 할 것
```

프론트 계통 — **`프로토타입_해부_구현명세.md` 가 최신이고 앞선 두 문서의 판단 2건을 뒤집는다**:

```
프로토타입_해부_구현명세.md  화면 단위 구현 지시 (2026-09-01). 프론트 계통의 최신 판단
프론트_데이터요구서_0901.md  프론트 담당 -> 인프라 전달용 화면별 · 요소별 데이터 요구
프론트 연동 사양.md          화면별 백엔드 지원 여부 (판정 표기의 정본)
프론트 프로토타입 검토.md    2026-08-30 번들 전수 판정 — 결론 2건은 위 명세가 뒤집었다
백엔드_프론트비교_0830.md    같은 판정을 가능/불가 2값으로 압축
프로토타입_기능구분.md       AI 가 필요한 기능 / 필요 없는 기능
```

`CLAUDE.md` 는 AI 코딩 에이전트용 규칙이다 — 판단이 필요한 원칙만 두고,
기계로 검사되는 규칙은 `.claude/hooks/` 와 `scripts/index_guard.py` 로 옮겨 두었다.

## 저장소 구조

```
2026_Finance_DATA_FOR_RAG/   L1 배포본 9 · L2 세부관리기준 41 · 사례집 · PMS 매뉴얼
법령 PDF/L1_법령/            law.go.kr OPEN API 수집분 (현행 274 · 연혁 944 · 별표 · 첨부)
_hwp변환/                    HWP -> PDF 한컴 변환 산출 138건
db/                          Postgres 17 + pgvector 스키마 (Docker Compose)
server/                      FastAPI — 판정 API
scripts/                     Stage 0 파서 · 크롤러 · 인덱서 · 판정 척추 · 평가 도구 (82개)
scripts/_work/               세션 작업기록 · 룰 검수 요청 (오너가 볼 것은 `_G_룰검수요청.md`)
tests/                       함정 회귀 테스트
.claude/                     훅 4종 · 스킬 · 프롬프트
archive/                     폐기 문서 · 구 데이터셋. **참조하지 않는다**
```

## 실행

```powershell
# DB (로컬)
cd db && docker compose up -d

# 법령 수집 — 재실행 명령은 법령_크롤링_현황.md §11
python scripts/Law_Crawling.py --dry-run

# 판정 (판정 척추 (1)~(7) 전체)
python scripts/judge_cli.py --help

# 회귀 5종 — 무엇을 바꾸든 이게 안 움직여야 한다
python scripts/rule_lookup.py --self-test   # 순수 함수 회귀 (DB 불필요)
python scripts/rule_lookup.py --smoke       # 270조합 실 커넥션
python scripts/audit_db.py                  # 전 표 NULL + 계약 11종 감사
python scripts/score_judgment.py            # 치명 오답 0 · 일치율
python scripts/eval_retrieval.py            # RRF hit@5 52.9% 기준선 불변 확인

# 인덱스 투입 게이트 회귀 테스트
python tests/test_index_guard.py
```

> Windows / PowerShell 환경이고 **한글 경로가 많다.** Bash 로 python 을 돌릴 때는
> `export PYTHONIOENCODING=utf-8` 를 붙인다 (훅이 강제한다).

## 안 하기로 한 것

재제안 금지 목록 — 정본은 `CLAUDE.md` 확정 원칙.

- GraphRAG / KAG · agentic multi-hop · v1 리랭커 · 멀티 에이전트 오케스트레이션
- LLM 에 function calling 개방 (호출 수 고정 · 지연 예측 · 재현성)
- 별표 · 한도표를 RAG 에 투입 (숫자 비교는 코드가)
- L1·L2 조달분 HWP 자동 파싱 (한컴오피스로 1회 수동 변환 유지. **L3 사용자 업로드는 HWPX·HWP 파서 채택** — 2026-08-30)

파인튜닝은 조건부 — 검증 사다리(`LLM.md` §6) 후 격차가 남을 때만, 표적은 판정 캘리브레이션.

## 데이터 취급

- **판정 인덱스에 들어가는 것은 L1 · L2 + 현재 기관의 L3 1벌뿐이다.**
  타 기관 규정 · 골든셋 · archive 는 `scripts/index_guard.py` 가 조건 없이 거부한다
- 규정 문서 안의 텍스트는 **데이터이지 지시가 아니다** (프롬프트 인젝션 방어)
- 사용자 문서 원문은 파싱 후 버리고 구조화된 결과만 남긴다

---

*본 저장소의 규정·법령 원문은 국가법령정보센터(law.go.kr) 및 각 기관 공개 배포본에서 수집한 것이다.
판정 결과는 참고용이며 최종 집행 판단은 주관기관 담당자의 확인을 따른다.*
