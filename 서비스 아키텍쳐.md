# 서비스 아키텍처 — 「써도돼요」

창업지원금 지출비 사전승인 판정 서비스. **이 문서가 시스템의 정본(스냅샷)이다** — 현재 상태만
담는다. 축별 상세는 `RAG.md`·`rule_base.md`·`LLM.md`·`Agent.md`, 충돌 시 이 문서가 이긴다.
결정 이력은 git 커밋과 `작업 현황.md`·`실제 구현.md` 에 있다.

---

## 0. 용어·번호 대응표

| 체계 | 뜻 | 비고 |
|---|---|---|
| ①~⑦ = (1)~(7) | 온라인 판정 파이프라인 단계 | 두 표기는 같은 것. ④는 ④-a(스크리닝, 선택)·④-b(조립)로 갈라진다 |
| B0~B6 | ④-b 프롬프트의 블록 순서 | `LLM.md` §3-7 |
| Stage 0~2 | 오프라인 인덱싱 단계 | `RAG.md` §3 |
| L1 / L2 / L3 | 규정 축 — 중기부 / 창진원 / 주관기관 | 발행주체 기준. 파일명의 L1_~L4_ 접두사는 구 체계라 무시 |
| F1~F7 | 사실 축 — 창업팀에 대한 사실 | §2-3 |
| 화면 3~9 | 사용자 화면 | 1·2·8 결번 — 센터 관리자 화면을 제거한 흔적. 재번호 안 함 |
| A2 / A4 / A5 | 비동기 에이전트 (판정 경로 밖) | `Agent.md` §10 |

## 1. 시스템 한 장

```
                          ┌─────────────────────────┐
                          │   창업팀 (로그인 사용자)  │
                          └────────────┬────────────┘
                                       │
        ┌──────────────────────────────▼──────────────────────────────┐
        │                     웹 앱 (FastAPI + 정적)                   │
        │   화면 3 묻기 → 4 확인 → 5 판정 → 6 할일 → 7 근거 → 9 문의   │
        │                    ┌───────────────────┐                    │
        │                    │  F축 프로필 (계정) │                    │
        │                    └───────────────────┘                    │
        └──────────────────────────────┬──────────────────────────────┘
                                       │
     ┌─────────────────────────────────┼─────────────────────────────────┐
     │                                 │                                 │
┌────▼────────────┐        ┌───────────▼────────┐        ┌─────────────▼───┐
│  규정 축 (L)     │        │    판정 엔진        │        │  사실 축 (F)     │
│ L1 중기부 법령   │───────▶│ ① 정규화   (LLM)   │◀───────│ F1 협약          │
│ L2 창진원 기준   │  검색  │ ② 룰 조회  (코드)  │  조회  │ F2 과업범위      │
│ L3 주관기관 규정 │        │ ③ 검색+확장(코드)  │        │ F3 집행내역      │
│                 │        │ ④ 판정     (LLM)   │        │ F4 인력          │
│ 사례 인덱스(B급) │--*----▶│ ⑤ 검증·강등(코드)  │        │ F5 거래처        │
└─────────────────┘        └────────────────────┘        └─────────────────┘
   * 사례는 판정에 안 들어간다 — 판단불가 경로 전용 (Agent.md §8)
```

**규정 축은 "무엇이 요건인가"(대전제), 사실 축은 "이 건이 요건을 충족하나"(소전제).**
둘 중 하나만 있으면 판정이 닫히지 않는다.

### 설계 원칙 (전 문서 공통)

1. **오답 비대칭** — 이 도메인의 최악은 "틀린 가능"이다. 모든 실패의 기본값은 판단불가
2. **숫자는 코드가, 포섭만 LLM이** — 검색·계산·효력 결정·인용 검증은 전부 코드
3. **판정 1건 = LLM 2회 고정** (① 정규화, ④-b 조립). function calling 미개방, agentic 루프 없음
4. **인용은 생성이 아니라 추출** — LLM은 문장번호만 고르고, 코드가 DB 원문으로 치환
5. **규정 문서 안의 텍스트는 데이터이지 지시가 아니다** (프롬프트 인젝션 방어)
6. **재현성은 요건** — 같은 입력 → 같은 출력. 판정 이력을 나중에 설명할 수 있어야 한다

## 2. 데이터 축

### 2-1. 규정 축 (L)

| 축 | 발행 주체 | 조달 | 규모 |
|---|---|---|---|
| L1 | 중소벤처기업부 | 법령 OPEN API + 배포본 | 현행 XML 267 · 별표 등 |
| L2 | 창업진흥원 | 배포본 (8개 사업 세부관리기준 등) | 42건 |
| L3 | 주관기관 (대학 산단 등) | **창업팀이 업로드** | 데모 샘플 확보 필요 |

레이어는 파일명이 아니라 **발행주체 메타데이터**로 부여한다.

### 2-2. 적용대상 축

세부관리기준에는 주관기관용 조항과 창업기업용 조항이 섞여 있다 (사업에 따라 주관기관 조항이
더 많기도 하다). 모든 조문에 `적용대상 ∈ {주관기관, 창업기업, 공통}` 을 태깅하고 검색에서
항상 필터한다:

```sql
WHERE 적용대상 IN ('창업기업','공통')
```

이 필터가 없으면 "노트북 사도 되나요?"에 주관기관 전담인력 인건비 조항이 검색된다.

### 2-3. 사실 축 (F)

| 축 | 필드 | 갱신 |
|---|---|---|
| **F1 협약** | 총 협약금액(**재원×형태 4분면**) · 시작일 · 종료일 | 1회 |
| F2 과업범위 | 사업화 과업 요약 (원문 저장 안 함) | 1회 |
| **F3 집행내역** | **비목별 · 거래처별 · 인별×월별** 누적, 4분면 집계 | 수시 |
| **F4 인력** | 타 사업 참여율 · **소속기관 유형** · 고용형태 · 겸직 | 1회 |
| F5 거래처 | 친족 · 전직 임직원 · 구매 명의 | 건별, 판정 후 폐기 |
| F6 기업정보 | 업력 · 매출 · 지분 | 1회 |
| F7 수혜이력 | 타 사업 중복지원 | 1회 |

- **4분면**: `{정부지원, 자기부담} × {현금, 현물}`. 재원제한·구성비율 룰이 이 축을 요구한다
- **F3 집계 축 3개**: 비목별 한도 · 동일 거래처 누적(2천만원 심의) · 인별월별 상한이 각각 요구
- **F4 소속기관 유형**: 참여율 상한이 기관 유형에 따라 100% / 130% 로 갈린다
- MVP(예비/초기창업패키지)의 핵심은 **F1·F3·F4**. F5~F7은 후순위
- 건별 사실(품목·금액·시기)은 F축이 아니라 ① 정규화가 그때그때 받는다
- 저장 스키마: `RAG.md` §2-3 (`tenant.f_profile` · `f_exec` · `f_personnel`)
- **F축은 열린 목록이다.** 부족은 `unmapped_premise` 루프(§5)가 드러내고, 빈도로 승격한다

### 2-4. F1·F3 입력 — PMS 텍스트 붙여넣기

K-Startup PMS 「사전승인 신청」 화면에 집행협약한도액(=F1)·신청가능금액이 현금/현물 분리로
이미 표시된다. 사용자가 그 표를 **드래그 복사 → textarea 붙여넣기 → 브라우저에서 정규식
파싱(서버 전송 없음) → 숫자만 폼에 자동 입력 → 확인 후 확인된 숫자만 서버 전송.**
원문(실명·과제번호 포함)은 서버에 도달하지 않는다. 스크린샷 업로드는 지원하지 않는다.

### 2-5. 입력 최소화 원칙

입력 경로를 추가할 때마다 이 5개 질문을 통과시킨다:

```
1. 판정에 필요한 최소 단위가 무엇인가?   문서 / 필드 / 체크박스 하나
2. 그 단위보다 큰 것을 받고 있지 않은가?
3. 사용자가 "무엇을 주는지" 알 수 있는가?
4. 값이 틀렸을 때 책임이 누구에게 남는가?   ← 사용자 입력이어야 방어선이 산다
5. 원문 없이 구조화 결과만으로 기능이 유지되는가?
```

## 3. 배포 토폴로지

```
╔══════════════════════════════════════════════════════════════════════╗
║  브라우저 — 정적 프론트                                               ║
║   게스트: 화면 3~9 판정 가능 (F축 없이, 전제[] 다수)                  ║
║   로그인: F축 프로필 저장 · 판정 이력 · 전제 자동 해소                ║
╚═══════════════════════════════│══════════════════════════════════════╝
                                │  HTTPS / SSE (스트리밍)
╔═══════════════════════════════▼══════════════════════════════════════╗
║  앱 서버 (FastAPI)                                                   ║
║   인증·SSE / 판정 오케스트레이터 / 룰 엔진 / 전제 해소기 /            ║
║   인용 검증기 / LLM 어댑터                                           ║
║   임베딩 KURE-v1 (CPU, 워커당 ~1.5GB — 사양이 동시성 한계)           ║
║   kiwipiepy (쿼리 토큰화)                                            ║
╚══════════╤══════════════════════════════╤════════════════════════════╝
           │ 데이터 경계 ①                │ 데이터 경계 ②
╔══════════▼════════════════╗  ╔══════════▼════════════════════════════╗
║  Supabase (관리형)         ║  ║  LLM 서빙 — vLLM (RunPod / 자체 GPU)  ║
║  corpus: 규정·룰·refs      ║  ║  전 슬롯 오픈소스 (Qwen3)             ║
║  tenant: 계정·F축·L3·      ║  ║  F축 포함 슬롯은 외부 API 라우팅 금지  ║
║   판정로그 (RLS+코드 이중) ║  ║  (어댑터 f_axis 게이트)               ║
║  public 은 비운다 · 골든셋 ║  ╚═══════════════════════════════════════╝
║   은 올리지 않는다(eval)   ║
╚═══════════════════════════╝
```

- 데이터가 밖으로 나가는 경계는 **둘**이다(LLM 서빙 + Supabase 저장). 원문·실명이 안 나가는
  것은 §6 저장 계층화 덕분이지 인프라 덕분이 아니다
- 호스팅 후보: HF Spaces Docker(PRO, 2vCPU/16GB) 또는 VM. 미결 (§11)

## 4. 요청 1건 — 온라인 판정 파이프라인

```
  "디자이너 쓸 맥북 250만원 사도 되나요?"
   │
   ▼ ────────────────────────────────────────────────────────── 0ms
│ ① 정규화                                   LLM (로컬)        │
│   → {품목, 금액, 용도, 비목후보[{비목,신뢰도}], 결제수단,     │
│      구매명의, 신청일, 비교견적}   ⚡스트리밍 → 화면 4        │
│   F5(거래처)는 이 단계에서 체크박스로 수집                    │
   │                                                    ~800ms
   ▼ ──────────────────────────────────────────────────────────
│ ② 룰 조회 + ③ 검색                    코드 · 병렬            │
│   F 프로필 로드 ── 룰 조회(사업×기관유형×비목) ── 금액 비교   │
│   L3: 사업비 관련 장 통째 로드 (검색 없음)                    │
│   L1·L2: 임베딩+BM25 → RRF → 필터 → top-5                    │
│   참조 폐포: refs 깊이 3 재귀 CTE (~10ms, LLM 0회)            │
   │                                                    ~300ms
   │  ✂️ 게이트: 최고 스코어 < 임계치 → ⑥ 판단불가 직행
   ▼ ──────────────────────────────────────────────────────────
│ ④-a 포섭 스크리닝 (선택 · 미결)      LLM × N 병렬            │
│   조항별 독립 판정 {해당|비해당|불확실} — A/B 실측 후 채택    │
   ▼ ──────────────────────────────────────────────────────────
│ ④-b 판정 조립                              LLM               │
│   입력 = B0~B6 블록 (원문 + S번호, 요약본 금지)               │
│   출력 = 스키마 강제 JSON (LLM.md §3-4 [1겹])                 │
│   인용은 S번호만 — 원문 복사 금지                             │
   │                                                    ~900ms
   ▼ ──────────────────────────────────────────────────────────
│ ⑤ 전제 해소                                코드              │
│   ① F 있음·값 있음 → 즉시 검증 → 소멸                        │
│   ② F 있음·값 없음 → 인라인 입력 요청                        │
│   ③ F 없음        → unmapped_premise 로깅 → 화면 9           │
   ▼ ──────────────────────────────────────────────────────────
│ ⑥ 검증 · 강등                              코드              │
│   S번호 → DB 원문 치환 · 강등 테이블 (Agent.md §5)            │
│   신뢰등급·버전스탬프·참조사슬은 여기서 코드가 부착           │
   ▼ ──────────────────────────────────────────────────────────
│ ⑦ decisions 로그 (전제·검색스냅샷·코퍼스버전 포함)            │
   ▼                                             총 ~3.2s (p50)
  화면 5 판정 · 6 할일 · 7 근거
```

①·④-b가 스트리밍이라 사용자는 첫 글자를 1초 안에 본다.

### 4-2. 비목 확정 — 자동 추정 + 사용자 컨펌

화면 3에서는 비목을 묻지 않는다(자연어 한 줄). LLM이 추정하고 화면 4에서 컨펌받는다.

```
신뢰도 ≥ 임계치 (단일 후보)   "기자재비로 봤습니다"  [맞아요] [바꿀게요]
갈림 (예: 0.7/0.3)           두 경로를 모두 판정해 결과를 보여주고 고르게 한다
```

컨펌 로그는 `item_alias` 승격 후보로 적재된다 — 어휘 갭이 운영으로 메워진다.

### 4-3. 화면 7 — 판단 구조가 곧 설명이다

사후에 "왜 그렇게 판단했는지"를 LLM에게 다시 묻지 않는다(지어낸 서사가 나온다).
④의 조항별 출력을 그대로 그린다 — **비해당 조항도 보여준다** ("왜 이건 안 걸리나"가
소명에서 더 중요할 때가 많다). 참조사슬("이 규정이 왜 나에게 적용되나")과
"참고 사례" 위젯(판정 무영향)이 함께 렌더된다.

## 5. 결핍 처리 루프 — 전제[] 와 unmapped_premise

- **LLM은 F 스키마를 확장하지 못한다.** 고정 스키마 안에서 결핍만 지적한다
- 전제에도 근거 조항(S번호)을 강제한다 — 근거 없는 전제는 ⑥이 폐기
- `매핑`은 F필드 경로의 **참조 목록**이다 (`["F1.정부지원.현금", ...]`). 계산식은 룰에서 코드가 도출
- 주간으로 `unmapped_premise` 빈도를 집계해 상위 항목을 F 필드로 승격한다
- 채울 수 없는 전제(예: 거래처 임직원 명부)는 억지로 묻지 않고 화면 9로 넘긴다.
  **판단불가는 실패가 아니라 기능이다**

## 6. 인증 · 개인정보 — 저장 계층화

| 데이터 | 저장 | 비고 |
|---|---|---|
| 계정 (이메일·해시) | ✅ | |
| F1 협약 · F3 집행내역 (숫자) | ✅ | 민감도 낮음 |
| F4 인력 | ✅ | **이름 저장 ✕** — 역할·수치만 |
| F5 거래처 | ⚠️ 건별, 판정 후 폐기 | 체크박스 결과만 |
| F2 사업계획서 원문 · PMS 원문 | 🔴 **저장 ✕** | 파싱 후 폐기, 구조화 결과만 |

- 개인정보처리방침·수집 동의·파기 정책·컬럼 암호화 필요. 저장 위탁(Supabase) 명시
- **F축이 프롬프트에 들어가는 LLM 호출은 자체 통제 하드웨어 강제** — 어댑터 `f_axis` 게이트
- **게스트 경로 유지**: 로그인은 관문이 아니라 정확도 다이얼이다
  ("협약 정보 입력하면 미확인 전제 2개가 사라집니다")

## 7. LLM 슬롯

| 슬롯 | F축 | 모델 | 실행 |
|---|---|---|---|
| ① 정규화 | ✅ | Qwen3 8B | 🔒 로컬(vLLM) |
| F 문서 파싱 | ✅ | Qwen3 8B | 🔒 로컬(vLLM) |
| ④-a 스크리닝 (선택) | ⚠️ | 미정 | 채택 자체가 미결 |
| ④-b 판정 조립 | ✅ (B5 = F 프로필) | **Qwen3 32B thinking + 투표 N=3~5** | RunPod·vLLM |
| ⑤ 문장 생성 | ✕ | Qwen3 8B | 동일 서버. 판단불가 경로 전용 — 2회 셈 밖 |

- **전 슬롯 오픈소스.** `AnthropicAPI` 어댑터는 비활성 폴백으로만 유지(F축 슬롯 제외).
  배치 유지 조건 = 아래 검증 사다리 통과 (치명 오답 0 제약)
- 마감 제출본·데모는 골든셋·테스트 데이터만 다루므로 RunPod(3자 하드웨어) 서빙 허용.
  **실서비스의 F축 실데이터 슬롯은 자체 GPU가 전제**
- vLLM 운영: guided_json(스키마 토큰 강제) · reasoning parser(thinking 뒤 문법 적용) ·
  awq_marlin · prefix caching · 가중치는 Network Volume — 상세 `LLM.md` §1

### 검증 사다리 (품질 관문)

```
1. Qwen3 thinking baseline → 2. 자기일관성 투표 N=3~5 → 3. 금지목록 룰 분리
→ 4. 프론티어와 격차 측정(측정 비교군) → 5. 격차 시 캘리브레이션 QLoRA
→ 6. 배포 양자화(AWQ 4bit) 그대로 골든셋 재실행   ← 배치 유지의 최종 관문
```

투표에서 불일치는 판단불가로 보낸다 — 틀림이 드러나는 구조가 1회 호출보다 안전하다.
파인튜닝 표적은 규정 지식이 아니라 **판정 캘리브레이션**이다 (기관별 수치 학습 금지).

## 8. 오프라인 인덱싱 (요약 — 상세는 RAG.md §3)

```
원문 (XML·PDF·HWP→PDF · L3 업로드는 HWPX/HWP 직파싱 — RAG.md §3-1)
  ├─ Stage 0    파싱·조 분해 (pdftext 경유 · 섹션분리 · 4단 fallback · 게이트 V1~V4)
  ├─ Stage 0.5  적용대상 태깅 {주관기관|창업기업|공통}
  ├─ Stage 0.7  참조 그래프 → refs (명시 참조만 파싱 — 관계 추론 없음)
  ├─ Stage 0.8  우선순위 조항 → precedence_rules
  ├─ Stage 1    룰 컴파일 → rules (숫자·목록만. 자연어 요건은 룰화 금지)
  └─ Stage 2    청킹(900토큰)·컨텍스트 헤더·임베딩·BM25 → 판정/사례 인덱스 물리 분리
```

- 모든 인덱싱 경로는 `scripts/index_guard.py` 게이트를 태운다
  (archive·골든셋·테스트L3·범위밖·L4/L5 조건 없이 거부)
- 별표는 대부분 서식이다 — 판정용 4건만 룰화. 룰의 주재료는 **L2 세부관리기준 본문·[붙임2] 표**

## 9. 평가 체계

| 세트 | 수량 | 재는 것 |
|---|---|---|
| 검색 | 15문항 | Recall@5 |
| 골든셋 | **70± 목표 (현 68)** = 별첨4 7 + 직접작성 33+1 + 적대적 27 | 판정 일치율 · 인용 정확도 |
| 적대적 | 27 (부정행위 사례집 변환, `_골든셋_스테이징/`) | **치명 오답률** |
| KCL | 283+169 (외부 벤치마크) | 법률 추론 능력 |

- **치명 오답률** = 정답이 불가/조건부인데 "가능"으로 답한 비율 — 1순위 지표
- **선택 기준: 치명 오답 0 제약 하에서 판단불가율 최소.** 기준은 결과를 보기 전에 정한다
- 골든셋은 인덱스 투입 금지(정답 유출). 무엇이든 바꾸면 골든셋 재실행 — 점수 없이 머지 금지
- 모델 비교 시 검색 결과를 스냅샷으로 고정한다. 적대적 세트에는 사업별로 정답이 갈리는
  문항(A9)이 있다 — 사업 필터 검증용

## 10. 구현 순서

**UI보다 CLI 판정기가 먼저다.** 판정 품질이 골든셋을 못 넘으면 UI는 의미가 없다.

```
 1. 판정층 격리 테스트 — 골든셋 근거_원문으로 검색 없이 판정층만 잰다 (지금 가능)
 2. Stage 0 표 추출 ([참고N]·[붙임2] 표 → 룰 재료)
 3. 골든셋 검수 (68문항 verified 전환 · 적대적 세트 _골든셋/ 이동)
 4. 룰 테이블 — 유형 18종 분기 스키마 → 사업별×기관유형별
 5. 청킹 단위 확정 (조 vs 항) → 재인덱싱 (0 → 0.5 → 0.7 → 2)
 6. baseline 재측정 → BM25+RRF 검증 (bm25s 동등성)
 7. 쿼리 파이프라인 + 프롬프트 문구 + CLI 판정기
 8. F1·F3·F4 폼 + 전제 해소 루프
 9. 모델 검증 사다리 (RunPod)
10. 웹 UI + 배포
```

## 11. 미결 대장

### 사람이 결정하거나 구해와야 하는 것

| # | 항목 |
|---|---|
| 1 | 데모용 L3 샘플 — 건국대가 주관하는 **중기부 사업** 규정 확보 (기존 레퍼런스는 과기부 계통이라 위임 사슬이 안 닿음. F축 검증용으로는 유효) |
| 2 | ~~청킹 기본 단위 (조 vs 항)~~ → **조 기준 + 900토큰 초과 시 항 으로 확정** (2026-08-31) |
| 3 | 수익 모델 |
| 4 | 골든셋 68문항 검수 (`verified=false` 전량) · 룰 19행 D단계 검수 |
| 5 | 호스팅 (HF Spaces vs VM) · L3 원본 보관 여부 |

### 실측으로 판가름 나는 것 — 지금 논쟁하지 말 것

| # | 항목 | 판단 근거 |
|---|---|---|
| 6 | ④-a 채택 (LLM 2회 vs 3회) | ④-b 단일 호출과 A/B — **모델별로 따로** |
| 7 | 임계치 대장: 검색 게이트 · ④-a 불확실 · 비목 신뢰도 · 투표 N · RRF 가중 | 전부 골든셋 |
| 8 | Stage 0.5 태깅의 실제 필요성 | 판정층 테스트에서 검색 오염 여부 |
| 9 | ④ 오픈소스 배치의 품질 | 사다리 1~4·6단계 |
| 10 | 1만 청크 초과 시 ANN 재검토 | 실측 |

### 설계됐으나 미구현

어댑터 `f_axis` 라우팅 차단 게이트 · Stage 0 표 추출 · 비목 어휘집 파일(`_비목_어휘집.json`) ·
tenant 스키마 마이그레이션 · ⚠️ **④-a를 기각하면 화면 7(조항별 판단 구조)의 재료 산출 방식을
재설계해야 한다** — 이 의존을 잊지 말 것.

---

## 부록. 문서 지도

```
서비스 아키텍쳐.md   (이 문서) 정본 스냅샷. 충돌 시 우선
RAG.md               코퍼스 경계 · 스키마(DDL) · 인덱싱 · 검색 · 참조확장
rule_base.md         룰 경계 · 비목 어휘집 · 스키마 · precedence 병합 · 검수
LLM.md               슬롯 · vLLM · 출력 스키마 2겹 · B0~B6 조립 · 평가
Agent.md             오케스트레이션 · 게이트 · 검증강등 · 실패 경로 · 비동기 에이전트
프로젝트설명.md      배경 · 도메인 지식
법령_크롤링_현황.md  L1 조달 기록 (완료 작업 — 재실행 시에만)
작업 현황.md         일자별 로그   /   실제 구현.md  산출물 제작 기록
```

**코드가 정본인 것** — 문서와 어긋나면 코드를 믿는다:
`scripts/pdftext.py` · `db/init/01_schema.sql` · `scripts/index_guard.py` ·
`_refs.json` · `_precedence_rules.json`
