# 적재 지표 — 규정 모음 규모와 평가 실측

이 문서가 답하는 질문:
- 규정 모음은 지금 얼마나 크고 어떻게 나뉘는가
- ANN(근사 검색)은 왜 안 쓰는가
- 정답셋 실전 E2E 결과는 얼마인가

## 규정 모음 적재 실측 (2026-09-06 재확인 — DB 직접 조회, 함수 호출 병기)

| 항목 | 값 | 2026-09-04 값 |
|---|---|---|
| `documents` | 283 (인덱스 대상 235, 불변) | 283 (235) |
| `doc_articles` | 26,304 (불변) | 26,304 |
| `chunks` | **20,556** (232문서 · L1 20,031 / L2 525 · 진입점 1,455 / 폐포전용 19,101) | 20,525 |
| `chunk_terms` | 1,148,541 | 1,149,877 |
| `refs` | 44,865 (불변) | 44,865 |
| `rules` | **74 (검수 74/74 · 미검수 0, 불변) · 금지예시 358 · 허용예시 225(전부 배열)** | 74 |
| `precedence_rules` | **9** (+1 — TIPS 조항 추가, CLAUDE.md 2026-09-05) | 8 |

🔴 이 표는 `orchestrate.코퍼스버전()`="docs245/chunks20556" · `eval_store.코퍼스버전()`
="c20556-e20556-r44865-d283-…"(둘 다 2026-09-06 직접 함수 호출로 재확인, 두 축이
여전히 다른 형식이라는 결론은 [[9-3_실측에서_나온_것]] 참조)와 맞춰 놓은 값이다.

> 🔴 `rules` 는 행 수만 찼지 **판정을 못 낸다는 우려가 있었다** — 2026-09-04 실측
> 시점 허용 분포는 조건부73·가능1·불가0 이었다. 그 뒤 룰 123건 append·스칼라43건
> 반영·G1~G3 재작성을 거쳤고, **이 분포를 오늘 재집계하지 않았다 — 확인불가로
> 남긴다**(옛 숫자를 최신인 것처럼 옮겨 적지 않는다). → [[9-4_판정이_조건부로_수렴한다]]

🔴 **판정 검색이 실제로 보는 후보 풀은 1,250청크다**(2026-09-06 재확인, retrieve.py:41
FILTER 그대로 실행 — `embedding IS NOT NULL AND status='active' AND parse_quality='high'
AND retrieval_scope='진입점' AND layer IN ('L1','L2') AND 적용대상 IN ('창업기업','공통')`,
2026-09-04 값은 1,275 였다).
`retrieval_scope` 가 `documents` 와 `chunks` 양쪽에 있고 검색 필터는 **청크 쪽**을 읽는다 —
문서 기준으로 세면 실제보다 크게 나온다. 진입점 전체(1,455)와도 다르다: 적용대상 필터가
205청크를 더 깎는다. **셋을 섞어 적지 마라** — 20,556(적재) ≠ 1,455(진입점) ≠ 1,250(후보 풀).
🔴 오늘 확정된 정책 — **L1·L2 자동 적재는 앞으로 `status='staged'` 로 들어온다**(검색에
바로 안 보임, 검수 후 `active` 전환이 있어야 후보 풀에 들어온다). 위 표의 235/245 는
이 정책 «이전» 에 이미 적재된 행 기준이다.

L1: 현행 XML 274(판정 인덱스 채택 229)·구판 944·별표 103. L2: 38건(인덱스 채택 8).
🔴 L3: `tenant.l3_articles` **224조각**(2026-09-06 재확인, DB `count(*)` 직접 조회) —
「17조(E 픽스처)」는 낡은 값이다. 224조각 중 203개는 조제목 NULL(사업비 사용 안내문 본문),
나머지는 시험용 조제목 있는 조각이다(레인 S 실측, `docs/9-2_백엔드_프론트_미결`도 같은 값).

## 룰 컴파일(Stage 1) 표 추출 (2026-08-31)

`scripts/archive/indexing/extract_tables.py` 실측: 22문서 표 원본 578 → 병합 469 · 실패 0. 산출
`_tables.json` 900KB. 이 표가 [[5-1_룰_테이블]]의 `rules` 컴파일 원재료다.

## ANN(HNSW/IVFFlat) — 미사용 확정 (2026-08-31)

`scripts/archive/eval/bench_ann.py` 실측: 20,055청크(현 20,525)에서 전량스캔 134ms·recall 100%로
예산(top-50 < 200ms) 안. HNSW는 켜지는 구간(ef≤100)의 recall이 82~91%(최악 30%),
안 켜지는 구간(ef≥200)에서는 플래너가 인덱스를 버린다 — 중간이 없다.
규정 모음이 크게 늘면 같은 스크립트로 재측정.

## DB 용량 (2026-08-31 실측)

로컬 DB 478MB · `corpus.chunks` 302MB(20,055행 시점). 무료 티어(500MB)를 거의 채운다.

> **각주(2026-09-03) — 위 문장의 「무료 티어 500MB」 제약은 사라졌다.**
> 저장은 GCP Cloud SQL(SSD 20GB)로 갔고 Supabase 는 인증만 쓴다. 같은 날 실측 총 335MB
> (corpus 323 / tenant 2.2 / eval 1.4) → 60배 여유. → [[2_구조/2-4_배포와_인증]]

## 평가 세트

| 세트 | 수량 | 재는 것 |
|---|---|---|
| 검색 | 15문항 | Recall@5 |
| 정답셋 | 77 (본세트 50 + 적대적 27, 전량 `verified=true`) | 판정 일치율 · 인용 정확도 |
| 적대적 | 27 (부정행위 사례집 변환) | 치명 오답률 |
| KCL | 283+169 (외부 벤치마크) | 법률 추론 능력 |

## 실전 E2E 실측 (2026-09-01, `eval.runs`, run_id=189)

| 지표 | 격리(D6) | 실전 E2E | 낙폭 |
|---|---:|---:|---|
| 판정 일치율 | 84.7% | 67.5% | -17.2%p |
| 치명 오답률 | 0.0% | 0.0% | 불변 (1순위 지표, 통과) |
| 인용 정확도 | 100.0% | 29.7% | |
| 판단불가율 | 0.0% | 6.5% | |
| 검색 hit@5 | — | 55.4% (74문항·필터 on) | |

낙폭의 출처는 검색이다 — 분해는 [[4-4_검색_지표]] 참조. 판단불가 5건은 모델의
판단이 아니라 `max_tokens=1500` 잘림이다(모델이 스스로 판단불가를 고른 횟수 0).

> 🔴 위는 **2026-09-01 run 189 의 조건**이다. 2026-09-02 run 191 에서 `max_tokens` 를
> 3,000, `max-model-len` 을 40,960 으로 올려 **프롬프트 초과 0건 · 잘림 0**이 됐다.
> 지금 값은 [[0_현황]] 이 기준이다.

- **치명 오답률** = 정답이 불가/조건부인데 "가능"으로 답한 비율 — 1순위 지표
- **선택 기준: 치명 오답 0 제약 하에서 판단불가율 최소**(기준은 결과를 보기 전에 정한다)
- hit@k는 조건(분모·필터)이 같은 run끼리만 비교
- 정답셋 재실행 없이 머지 금지 — 무엇이든 바꾸면 재실행

**정답셋 77문항은 절반을 원리적으로 검출하지 못한다** — 사업 6종·org 없음 구성이라
재도전 룰 9행·TIPS 결정·L3 오버레이·숨은 한도가 전부 미측정이다.

## 관련 문서

[[4-4_검색_지표]] · [[3-1_검색_범위]] · [[2-1_시스템_한장]]
