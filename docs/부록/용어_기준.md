# 용어 기준

> 참고 자료. 통독 대상이 아니다 — 100줄 상한 면제.
> 2026-09-02 에 문서·주석·식별자를 이 표대로 바꿨다. **앞으로 새 글도 이 표를 따른다.**
> 「바꾸지 않는다」 표가 더 중요하다 — 거기 걸리면 코드가 깨진다.

## 바꾼다

| 현재 | → | 비고 |
|---|---|---|
| 골든셋 | **정답셋** | |
| 폐포 | **참조 확장** | 「폐포 수집」 → 「참조를 따라 수집」 |
| 코퍼스 | **규정 모음** | 「인덱싱 코퍼스의 경계」 → 「검색 대상의 경계」 |
| 하네스 | **자동 점검** | |
| 슬롯 | **호출 자리** | 「전 슬롯」 → 「전 호출 자리」 |
| 레인 | **작업 갈래** | |
| 정본 | **기준 문서** | |
| 스냅샷 | **현재 상태** | |
| 게이트 | **통과 조건** | 「검증 게이트」 → 「검증 통과 조건」 |
| 대장 | **목록** | 「미결 대장」 → 「미결 목록」 |
| 어휘집 | **용어 사전** | |
| 볼륨 | **분량** | |
| 판정층 | **판정 단계** | |
| dangling | **끊긴 참조** | |
| 폴백 / fallback | **대체 경로** | |
| 인덱스 투입 | **검색 대상에 넣기** | |
| 상설화 | **상시 실행** | |
| 적대적 골든셋 | **함정 문항** | |

## 🔴 바꾸지 않는다

무엇을 바꾸지 않는지가 더 중요하다. 아래를 건드리면 코드가 깨진다.

| 대상 | 예 |
|---|---|
| **DB 스키마·테이블·컬럼·인덱스 이름** | `corpus` · `tenant` · `eval` · `eval.golden_set` · `ix_refs_dangling` · `index_target` · `적용대상` |
| **파일·경로명** | `_골든셋_재적재.py` · `_B_골든셋_룰커버.json` · `.claude/_lanes.json` |
| **코드 식별자** (변수·함수·클래스·키) | `dangling` 을 키로 쓰는 JSON · `retrieval_scope` |
| **인용된 로그·출력 원문** | 「그때 이렇게 찍혔다」는 기록이다 |
| **`docs/기록/` 전체** | 그 시점의 관측이다. 소급 수정 금지 |
| **`archive/` 전체** | 훅이 쓰기를 막는다 |
| **SQL 문자열 안** | `WHERE layer IN ('L1','L2')` 같은 리터럴 |

## 왜 스키마 이름은 두는가

`corpus`/`tenant`/`eval` 은 AI 조어가 아니라 DB 표준 네이밍이다. 개명하면 60개 파일의
SQL 문자열을 동시에 고쳐야 하고, 라이브 DB(청크 20,525 · 참조 44,865)의 DDL 마이그레이션이
된다. 얻는 것보다 잃을 게 크다.

## 2026-09-02 에 적용한 순서와 결과

1. 문서(`docs/**` — `기록/` 제외, 루트 md) — 산문·표·제목
2. 코드 주석·docstring (`scripts/**` · `server/**` · `tests/**`)
3. 한글 식별자 (변수·함수명) — 실측 약 119건. 바꾼 뒤 반드시 `pytest` 통과 확인

각 단계마다 치환 건수를 세어 보고한다. 「전부 바꿨다」는 보고가 아니다.

## 안 바꾼 한글 식별자 (교차 참조 있음)

`dangling`(65회·14파일) · `게이트`(33·8) · `게이트값`(6·2) · `게이트임계`(1·1) ·
`슬롯`(5·3) · `슬롯표`(2·1) · `정본`(8·4) · `정본비목`(2·2) · `코퍼스`(18·8) ·
`코퍼스버전`(4·2) · `폐포`(17·3) · `폐포수집`(2·1) · `폴백`(5·5)

바꾸려면 **호출부를 전부 같이** 고쳐야 한다. 지금은 그대로 두는 쪽을 택했다.
🔴 `orchestrate.py` 의 「게이트」는 응답 계약이다 (`게이트="A"/"B"/"C"` · `게이트값` ·
`SUDDOE_GATE_B`). 그 파일에서는 산문도 안 바꾼다 — 주석이 코드와 어긋나는 게 더 나쁘다.
