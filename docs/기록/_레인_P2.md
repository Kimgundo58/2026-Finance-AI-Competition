# 레인 P2 — 룰 경로 (금지예시 적중 0 · 허용=가능 후보)

2026-09-03 · 워크트리 `금융 AI공모전-agent` / 브랜치 agent-perf-2026-09-03 · 중앙 ai-e8.
DB 는 한 행도 쓰지 않았다(커넥션 `read_only=True`). 아래는 **관측**이고, 추론은 절로 갈라 적는다.

## 1. 어디까지 왔나

### 관측 A — 금지예시 적중 기준선 (확정. 다시 재지 않는다)

하네스 `scratchpad/P2_금지예시_적중.py`. 진입점은 P4 와 같은
`orchestrate.판정(질문, 사업명, dry=True, 기록=False, conn=<read_only>)` — `eval_e2e.실행()` 은
`eval.runs` 에 행을 써서 안 쓴다. 문항은 `eval_store.평가대상(cur)` = **93**
(정답 불가 49 · 조건부 21 · 가능 16 · 판단불가 7). 경과 100초.

| 적중 | 참불가 | 🔴 틀린불가 | 놓침(정답=불가∧미적중) | 허용충돌 | 조건부근접 |
|---:|---:|---:|---:|---:|---:|
| 0 | 0 | 0 | **49 / 49** | 0 | 0 |

게이트 분포 D 91 · C 2 — **게이트 A 는 한 번도 안 열렸다.** 비목확정 실패 43/93.

### 🔴 관측 B — 위 0 과 run 191 의 0 은 «다른 0» 이다

dry 의 `규칙_정규화()` 산출을 93문항 전수로 열었더니 **용도가 93/93 전부 빈 문자열**이고
품목은 「출장 갈」·「사무실 놓을」·「주는 직원」·「사업비 산」 같은 조각이었다.
금지매칭 본문은 «품목+용도» 다 — 그러니 dry 의 적중 0 은 「금지예시가 안 맞는다」가 아니라
**「본문이 비어 있다」의 증거**다.

그래서 닻을 바꿔 다시 봤다. `eval.run_items(run_id=191)` 93행의 `경로` 전수:

```text
'1정규화+3검색+4조립+6검증'  89
'1정규화+3검색+4조립'         4
→ 「A금지적중」이 든 경로 0건
```

**실제 LLM 정규화로 돈 run 191 에서도 게이트 A 는 0/93 이다.** 두 0 이 우연히 같아서
하마터면 한 말로 읽을 뻔했다. `docs/9-4` ②의 「0건 적중」은 이 닻으로 살아 있다.

**추론** — 별칭(할 일 3)의 효과는 dry 로 재면 쓰레기 품목을 상대로 재는 것이다.
효과 크기는 P4 의 실물 run 에서만 잰다. dry 표는 「배관이 돈다 · 플래그 off 에서 안 변한다」
의 증거로만 쓴다.

### 관측 C — 「조문 인용체 87%」를 대리지표 없이 다시 셌다

`scratchpad/P2_핵_분류.py`. 상호배타 4갈래, **적용 순서 C2 → C1 → C3 → C4**(순서가 결과를 바꾼다).

```text
C2 규범참조형  핵에 `제\d+조` · 「…」 · `법 제` · 민법 · 고용노동부 · 전문기관의 장 ·
               사업운영위원회 · 중소벤처기업부 · 창업진흥원
C1 조건서술형  핵에 않은|않는|아닌|없는|없이|되지|하지|못한|초과|미만|이상|이하|미가입|
               미소진|미구입|미제출|벗어난|남는|지난|이내|이전|이후|전까지|경우|받지|
               허위|임의\s*처분|부적정|연관성이|부족
C3 복합명사구  공백 어절 3개 이상 (C1·C2 아님)
C4 단순지출명  어절 2개 이하
```

금지예시 294 = 무조건 266 · 예외단서 28. 중복 뺀 고유핵(무조건) 155.

| 분모 | C1 | C2 | C3 | C4 |
|---|---:|---:|---:|---:|
| 무조건 266 | 96 | 15 | 132 | **23 (8.6%)** |
| 고유핵 155 | 62 | 9 | 74 | **10 (6.5%)** |

C4 고유핵 10개 전량: 국내 운반비 · 기술평가 비용 · 기프티콘 · 문헌구입비 · 사무실 관리비 ·
사무실 임차보증금 · 유니폼 제작 · 임대 보증금·관리비 · 퇴직급여충당금 ·
창업자의 직계존비속·형제·자매·배우자.

🔴 **규칙의 한계를 같이 적는다.** 마지막 항목은 원문이 「·」 나열을 붙여 써서 어절 2개로
세어졌을 뿐 지출명이 아니다. C4 의 실질은 10 이 아니라 **9** 다. 규칙을 손대 9 로 만들지 않고
규칙과 예외를 둘 다 적는 쪽을 골랐다 — 수를 맞추려고 규칙을 고치면 다음 사람이 못 믿는다.

**추론** — 9-4 의 「약 87%」와 방향이 같고 값이 조금 크다(91.4%). 대리지표가 틀렸던 게
아니라 느슨했다.

### 관측 D — 매칭 「방향」. 느슨하게 세면 3배로 보인다

`금지후보()` 조건은 `핵n in 본문n` 이다. 반대 방향(사용자 표현 ⊂ 금지예시)을 두 규칙으로 갈랐다.

```text
역방향_전체 (제약 없는 부분문자열)                        10문항
역방향_경계 (품목n 4자 이상 ∧ 핵의 한 어절과 정확히 일치)   3문항
```

**10 대 3.** 부분문자열로만 세면 없는 신호가 보인다. 그 3건은
362(정답 불가 · 품목 '외주 업체') · 436(정답 불가 · 품목 '중고 거래') ·
**416(정답 «가능» · 품목 '협약 시작일')**.

**추론** — 방향을 뒤집는 안은 참불가 2 · **틀린불가 1**. 순증 +1 이지만 채택기준 ① 치명 오답 0
을 깬다. **매칭 정책은 안 건드린다**(§B1)는 원래 방침이 수로 확인됐다.
포함가능 88/93 · 품목n 길이 중앙 5자 — 길이가 병목이 아니라 **어휘가 병목**이다.

### 관측 E — 허용 감사는 (다) 자동 계수까지만 갔다 (마감으로 중단)

`scratchpad/P2_허용_감사.py`. 조건부 73행 중 **(다) 못 정함 6행**(근거 문서의
`corpus.documents.extraction='vlm'`). 나머지 67행은 `갈래=None` — 사람이 읽어야 한다.
곁수: 사전승인=true 37 · 한도값 있음 19 · 「명문표지만 있고 조건표지 없음」 14
(이건 (가) 후보 «자리»이지 (가) 가 아니다. 자동으로 '가능' 을 만들지 않는다).

🔴 **처음엔 (다) 가 0 으로 나왔다.** `chunks.parse_quality == 'vlm'` 으로 짰는데 그 컬럼 값은
`high`/`low` 뿐이라 **규칙이 아예 발화할 수 없었다** — 틀린 조건이 아니라 «안 도는» 조건이었다.
vlm 태그는 `corpus.documents.extraction` 에 있다. 0 이 너무 깨끗하면 규칙이 실제로 도는지부터 본다.

## 2. 다음 한 걸음 — **닫혔다**

~~할 일 4) 허용 감사의 (가)(나) 를 사람이 읽고 채운다.~~
→ 2026-09-03 완료. **(가) 는 0행**이다([[_레인_P2_허용감사]]). 67행 전부가 사전승인=True(33)
이거나 금지예시를 갖는다(34). 오버레이 파일은 만들지 않았다 — 적을 행이 없다.
원인은 시딩이 아니라 **스키마**다: 비목 알갱이에서는 「가능」도 「불가」도 표현이 안 되고
조건부만 남는다. 다음 걸음은 룰 테이블 «밖» 이다 — 가능 25.0% 의 원인은 여기가 아니었다.

## 3. 안 닫힌 물음

- **run 191 의 실제 LLM 품목·용도는 어디에도 없다.** `run_items.원출력` 키에 `정규화` 가 없고
  (`s맵·top5·강등사유·강등코드·게이트값·경로·근거적중·사업명·세트·신뢰등급·실패경로·실패단계·
  요약·인용목록·인용적중·적용범위·전제목록·지연ms·치명·판정·프롬프트길이`), `eval_e2e` 는
  `판정(..., 기록=False)` 로 불러 `tenant.decisions` 에도 안 남는다 → **복원 불가.**
  다음 run 에 `원출력.정규화`(품목·용도·금액·비목후보·누락필드·_출처) · `비목` · `게이트` ·
  `금지근거`(게이트 A 일 때만)를 요청했다(ai-e8 경유 → P4). 문항당 200바이트 안팎.
  이게 없으면 금지예시·비목 축은 GPU 를 다시 켜야만 열린다.
- **비목확정 실패 43/93** 이 dry 정규화의 쓰레기 품목 탓인지 `item_alias` 의 커버리지 탓인지
  아직 못 가른다. 위 필드가 들어와야 갈린다.
- (가)(나) 미분류 67행.

## 4. 2일차 — 의미 대조 측정 (별건 문서)

「단어 대조 → 의미 대조」로 바꿨을 때의 임계값 표·음성 대조·오탐의 «모양» 은
[[_레인_P2_의미매칭]] 에 따로 적었다(이 문서 100줄 상한). 한 줄 결론 —
**오탐 0 을 지키면 0/113 이 축2(유리한 축) 5건 · 축1(실제 배관) 1건**이 되고,
오탐은 잡음이 아니라 **정답이 「가능」인 이웃 문항**이다(부정·조건을 임베딩이 못 읽는다).

## 5. 금액비교 미발화의 몫 (별건 문서)

ai-a3 가 잡은 `_effective()` 의 `수치=` 누락을 ai-e8 이 고치기 전에 run 191 로 쟀다 —
[[_레인_P2_한도쏠림]]. 한 줄 결론 — **한도 있는 비목과 없는 비목의 쏠림률이 안 갈린다**
(0.30 대 0.38, 닻 둘이 방향까지 반대). 좁혀서 「한도 ∧ 질문에 금액」 7문항만 보면 3/3 이
조건부로 갔고 셋 다 정답 「가능」이지만, **`수치=` 만으로 닫히는 건 그중 1건**이다.

## 6. 대상/조건 분해 — 안 된다 (별건 문서)

앞 측정 뒤 내가 낸 안(대상만 의미 대조 · 조건은 코드)을 C1 62핵에 재 봤다 —
[[_레인_P2_대상조건분해]]. **오탐 0 에서 0건.** 62 중 32 가 지출명을 안 담아 못 닿고,
남은 30 의 대상 19종은 사실상 비목 어휘라 `비목확정()` 을 다시 하는 것이다.
음성 대조가 진짜보다 나았다 — **변별력을 갖고 있던 건 조건 쪽**이었다.

## 7. 불가 경로 — 왜 0행인가 · 상한은 얼마인가 (별건 문서)

[[_레인_P2_불가경로]]. **불가 0행은 시딩 실수가 아니라 스키마가 강제한 것**이다 —
행 키가 `UNIQUE (layer, 기관id, 사업명, 비목)` 이라 비목 통째가 금지인 경우에만 적을 수
있는데 그런 비목이 없다. 조문 전수에서 금지 문장 3,345개 중 비목 통째 금지는 **0건**.
무조건 금지는 전부 비목 아래 한 단계(세목)에서 나는데 **세목 컬럼이 없어** 금지예시
문자열로 갔고, 그게 안 걸린다. 사용자가 쓸 법한 금지예시는 **155 중 20 안팎(13%)**.

## 8. 세목·조건 축 — **오너가 안 하기로 정했다**

설계 제안서는 2026-09-03 커밋 전에 **폐기**됐다(오너 결정 · ai-e8 전달). D-4 에 스키마를
여는 방향을 닫은 것이다. 여기서 **처방은 더 이상 안 낸다.** 제안서에 있던 사실 중
살아남는 것만 옮겨 둔다:

- 🔴 **C1 조건서술형은 62 가 아니라 57 이다**(고유핵 155 기준 36.8%). `_C1` 의 `이전` 이
  「기술**이전**」·「권리**이전**」(移轉)에 걸렸다 — 「以前」이 아니다. 5핵(기술이전 평가비 2 ·
  권리이전 발명보상금 3)이 잘못 세어졌고 실제로는 C3 다. C3 는 74 → 79.
  **경계 없는 부분문자열이 수를 부풀린 세 번째 사례**다(vlm · 한도_값 · 이것).
  [[_레인_P2_의미매칭]]·[[_레인_P2_대상조건분해]]의 결론은 안 바뀐다 — 5핵 전부 어절 4
  이상이라 애초에 후보 밖이었다
- C1 57핵이 요구하는 **조건의 종류는 9가지**(T0 미분류 1). 핵당 1종 27 · 2종 29 · 3종 1 —
  절반이 두 조건의 AND 다. `scratchpad/P2_조건종류.py`+`.json`
- run 191 혼동표: **불가 81.6% · 조건부 81.0% · 가능 25.0% · 판단불가 0.0%.**
  불가는 이 시스템에서 제일 잘 되는 축이고, 손실은 가능·판단불가에 있다.
  이 표가 이 레인의 우선순위를 뒤집었다

## 9. 허용 감사 — 닫았다 (별건 문서)

[[_레인_P2_허용감사]]. **「74행 중 가능 1행」은 맞다.** 기준은 자연 대조(같은 비목이 한쪽은
가능·한쪽은 조건부인 창업활동비 2행)에서 뽑았다 — 사전승인이 있으면 조건, **한도는 조건이
아니다**(유일한 가능 행이 한도 50만원/월을 갖고도 가능이다). 금지예시가 있으면 (나)로 둔 건
관측이 아니라 안전 판단이다(게이트 A 0/93 이라 올리면 「틀린 가능」이 근거를 달고 나간다).
🔴 기준이 74행 전수에서 rule 435 하나만 정확히 집지만 **가능 부류의 n 이 1이라 순환이다** —
결론은 「67행 전부가 사전승인 또는 금지예시를 갖는다」는 관측에서만 나온다.

## 10. 가능 25.0% · 판단불가 0.0% 를 문항으로 열었다 (별건 문서)

[[_레인_P2_가능_판단불가]]. 가능 16건은 **(ㄱ) 조건 충족이 질문에 적혀 있다 13 : (ㄴ) 없다 3**,
잃은 11건 중 **9건이 (ㄱ)** 이다. 🔴 그런데 (ㄱ) 13 중 12 가 보강 세트라 **문항 문체의
산물일 수 있다** — 13 은 이 문체에서의 상한이다.
판단불가 7건은 6건이 「규정이 답을 안 한다」는 부재다. **TIPS 2건은 룰 경로가 이미
`None`(=판단불가)을 내는데 파이프라인이 안 쓴다** — 「사업 룰 0행 → 판단불가」로 닫으면
**순증 +2 · 손실 0**(강등코드 경로 -21 과 부호가 반대). 모두의창업 4건은 룰이 조문보다
넓어서(제33조① 진출자 비목 5개 밖) 안 잡힌다.

## 참고

[[_레인_P2_의미매칭]] · [[_레인_P2_한도쏠림]] · [[_레인_P2_대상조건분해]] · [[_레인_P2_불가경로]] · [[_레인_P2_허용감사]] · [[_레인_P2_가능_판단불가]] · [[9-4_판정이_조건부로_수렴한다]] · [[9-5_룰_테이블_구조]] · [[5-1_룰_테이블]] · [[5-3_검수_절차]]
