# 레인 W3 — 작업 기록

## 2026-09-02 · 1차 태스크 — 공통 모듈 추출(`scripts/_lib/`)

### 한 일
- 실측(grep) → «공통 모듈 인터페이스 설계안» 을 ai-ae 에게 SendMessage 로 보고 → 승인 받음
  → `scripts/_lib/db.py`·`scripts/_lib/paths.py` 생성 → ai-ae 가 지정한 20개 파일 +
  (Q2 승인분) DSN 체이닝 3개 전환
- `scripts/_lib/db.py` : `DSN`(환경변수 `SUDDOE_DSN` 우선) · `connect(dsn=None, *,
  autocommit=False, connect_timeout=None)` · `borrow(conn=None, **kwargs)`
  (`eval_store.py` 의 `닫기 = conn is None` 패턴을 컨텍스트매니저로 흡수)
- `scripts/_lib/paths.py` : `ensure_on_path()` — repo root 와 scripts/ 를 sys.path 에
  중복 없이 넣는다. 자기 위치(`scripts/_lib/paths.py`) 기준으로 계산해 호출자 파일의
  깊이(scripts/ 인지 scripts/_work/ 인지)와 무관하다
- 전환 20개: orchestrate·retrieve·eval_store·rule_lookup·llm_schema·seed_rules·load_db·
  l3_load·build_refs·stage2_chunk·stage2_bm25·normalize_run·llm_validate·build_index·
  assemble_context·agent_a4·agent_a2·case_search·judge_run·adapter
- 추가 승인분(Q2, 20개 목록 밖) — `DSN = 다른모듈.DSN` 체이닝 3건을 `_lib.db.DSN` 직접참조로:
  eval_e2e.py·sweep_retrieval.py·eval_retrieval.py

### 판단
- **호출은 항상 모듈 접두어**(`db.connect(...)`) — `from _lib.db import connect` 로 이름만
  들여오지 않는다(ai-ae Q3 승인). 지역변수 `conn`·함수 `연결()` 과 겹칠 자리가 많아서다
- **`connect()` 의 `autocommit` 기본값은 False 유지**(ai-ae 확인). 읽기 전용 병렬 경로
  (retrieve·eval_retrieval·orchestrate 등, 2026-08-31 DDL 교착 실측)만 명시적으로
  `autocommit=True` 를 넘긴다 — 쓰기 경로까지 기본값으로 끌어올리면 문장 단위 원자성이 깨진다
- **sys.path 조작은 "닭-달걀"이라 완전히 없애지 못한다.** `_lib` 자체가 scripts/ 밑
  서브패키지라 `from _lib import paths` 를 하려면 그 전에 scripts/ 가 이미 sys.path 에
  있어야 한다. 그래서 각 파일에 최소 부트스트랩 한 줄(`sys.path.insert(0,
  os.path.dirname(os.path.abspath(__file__)))` 류)은 남기고, 나머지(repo root 계산·
  중복삽입 가드·추가 경로)만 `paths.ensure_on_path()` 로 옮겼다
- **ROOT 변수가 sys.path 말고 다른 용도로도 쓰이는 파일은 ROOT 계산 자체를 안 건드렸다**
  (agent_a2·agent_a4·stage2_chunk — `ROOT / "scripts/_work/..."` 로 데이터 경로를 만드는 데도
  쓰고 있었다). sys.path.insert 줄만 `paths.ensure_on_path()` 로 바꾸고 ROOT 정의는 그대로 둠
- **`import psycopg` 는 `.connect()` 외 다른 용도로 쓰는 파일에서만 남겼다** — `seed_rules.py`
  가 유일하다(`psycopg.errors.LockNotAvailable` 를 잡는다). 나머지는 전부 제거
- `case_search.py` 의 중복삽입 가드(`if ROOT not in sys.path`)는 ai-ae 지시대로 제거 —
  `paths.ensure_on_path()` 가 이미 가드를 갖고 있다. 단 `ROOT` 자체는 `_F_오염검사.json` 경로
  조합에도 쓰여서 변수 정의는 남겼다
- 🔴 `adapter.py`·`judge_run.py` 는 함수 시그니처·이름을 안 건드렸다(ai-ae 지시) — DSN·
  sys.path·connect 호출부만 교체

### 자가검증
① 전환 23개 파일(20개 + Q2 승인 3개) 전부 `import` 성공 확인
   (`sys.path.insert(0,'scripts'); __import__('<파일명>')` 으로 개별 실행 — 전부 OK)
② `python -m compileall -q scripts/` 통과 (exit=0)
③ 전환 전/후 동작 대조 — 5개 스크립트를 실제로 돌려 확인
   - `rule_lookup.py --smoke`(실 커넥션 스모크): "스모크 270조합(사업 9×비목 10×org 3) — 전건 통과"
   - `case_search.py --selftest`(F5 게이트, DB 조회 포함): "✅ 전부 통과"
   - `judge_run.py --help` / `orchestrate.py --help`: argparse 정상 출력(전체 import 체인 검증 —
     assemble_context·llm_schema·llm_validate·normalize_run·_lib 전부 물림)
   - `llm_schema.py --slot 1`: 스키마 JSON 정상 출력
   - 🔴 **엄밀한 git-stash 기반 전/후 바이트 대조는 하지 않았다** — 6세션이 같은 워킹트리를
     공유 중이라 `git stash` 가 다른 세션의 미커밋 변경(문서 재구성·서버 라우트 등)을 건드릴
     위험이 있었다(ai-ae 의 git 금지 지시와 같은 이유). 대신 위 5개를 **전환 후 버전으로
     실제 실행**해 DB 연결·출력이 정상임을 확인했다 — DSN 값과 connect() kwarg 는 기계적
     1:1 치환이라 grep 스윕(아래)으로 누락·오타 여부를 교차 확인함
④ 전체 pytest 는 돌리지 않음(ai-ae 지시 — DB 공유, ai-ae 가 돌린다)
⑤ 잔존물 grep 스윕 — 20+3개 파일에 `psycopg`(seed_rules.py 의 `psycopg.errors` 제외)·
   `SUDDOE_DSN` 리터럴이 하나도 안 남았음을 확인

### 함정
- `llm_validate.py` 세 번째 connect 전환 때 `import psycopg` 줄을 지우지 않고 connect 콜만
  바꿔서 남겨뒀었다 — grep 스윕에서 잡아 제거. 콜 사이트 교체 후엔 반드시 그 위 로컬
  `import psycopg` 도 같이 지워야 한다(둘이 붙어 있지 않은 경우가 있다)
- `stage2_bm25.py`·`case_search.py` 는 전환 후 `DSN`/`ROOT` 전역이 아무 데도 안 쓰이거나
  (stage2_bm25) 딴 용도로 쓰이거나(case_search) 해서 기계적 일괄 치환이 아니라 파일마다
  실제 참조를 grep 해서 확인해야 했다 — "`DSN = os.environ.get(...)` → `DSN = db.DSN`" 한
  줄 치환이 전부인 파일은 소수였고, 나머지는 로컬 함수 스코프 변형이 파일마다 달랐다

### 남은 것
- 2차 태스크(한글 식별자 119건 + DB 2건 용어 치환) — ai-ae 사전 대기, 승인 전 착수 금지
- stage0_extract.py·stage0_ingest.py·scan_tables.py(2차 목록, repo root 를 path 에 넣는
  다른 성격) — 지금 건드리지 않음. ai-ae 가 2차에서 다룬다
- 전체 pytest 는 ai-ae 가 돌린다 (기준선: 134 passed · 2 skipped · 6 xfailed)

### 산출물
`scripts/_lib/__init__.py` · `scripts/_lib/db.py` · `scripts/_lib/paths.py` (신규) ·
전환 23개: `scripts/{orchestrate,retrieve,eval_store,rule_lookup,llm_schema,seed_rules,
load_db,l3_load,build_refs,stage2_chunk,stage2_bm25,normalize_run,llm_validate,build_index,
assemble_context,agent_a4,agent_a2,case_search,judge_run,adapter,eval_e2e,sweep_retrieval,
eval_retrieval}.py`

## 2026-09-02 · 2차 태스크 (A) — 용어 치환, 주석·docstring 만

### 한 일
- `docs/_용어_치환사전.md` 16개 단어 + 4개 구문을 `scripts/` 전체(orchestrate.py 제외 —
  ai-ae 전담 · `_work/` 포함)의 **주석(#)·docstring 안에서만** 치환. 코드 식별자·SQL·
  JSON 값·에러 메시지·CLI 플래그·파일 경로는 전부 손대지 않음(2차 (B) 몫)
- `ast`(docstring 스팬) + `tokenize`(주석 스팬) 로 스팬을 뽑아 그 구간에서만 정규식 치환하는
  스크립트를 직접 짜서 돌렸다. 스캔→검증→적용을 파일 하나씩 순서대로(Read 후 Edit 부분수정)

### 판단 — 실측 없이 밀었으면 났을 사고들
- 🔴 **1차 시도(`\b단어\b`)가 대량으로 못 바꿨다.** 한국어 조사가 공백 없이 명사에
  바로 붙어서("슬롯이다") `\b` 가 명사-조사 경계를 못 잡는다. 받침 유무로 이/가·은/는·
  을/를·과/와·으로/로 를 교정해 붙이는 로직으로 다시 짰다(모든 경우 "이다/이었다" 완전형은
  유지 — 축약형보다 항상 안전)
- 🔴 **`ast` 의 `col_offset`/`end_col_offset` 은 UTF-8 *바이트* 오프셋이다** — 문자
  오프셋으로 잘못 쓰면 한글이 많은 줄에서 docstring 스팬이 밀려 **바로 뒤 실제 코드**(변수
  `어휘집_경로`)를 깨뜨린다. `python -m compileall` 로 처음 잡았다. 그 줄만 바이트→문자로
  변환하는 헬퍼를 넣어 고쳤다 — tokenize 의 컬럼은 원래 문자 단위라 안 섞어야 한다
- 파일명·경로 인용 보호: `_비목_어휘집.json`·`_골든셋_재적재.py` 같이 백틱 인용된 실제
  파일명 9곳이 처음엔 깨졌다(파일은 안 바뀌는데 주석의 인용만 바뀌어 서로 어긋남). "`_` 로
  바로 이어지거나 뒤에 `.확장자` 가 오면 건드리지 않는다" 가드를 추가
- CLI 플래그 보호: `--dangling`·`--폐포`·`--cpu-fallback` 처럼 실제 argparse 플래그 이름이
  된 자리는 앞이 `--`/`-` 로 시작하면 스킵. **argparse 의 `help=` 문자열 자체는 애초에
  스코프 밖**(주석·docstring 아님)이라 손대지 않음 — 화면에 뜨는 한국어 설명이 코드와
  어긋나는 자리는 원래도 없다
- 파일별 다의어 예외: `pick_gpu.py`·`runpod_pod.py` 의 "볼륨" 은 전부 RunPod 네트워크
  스토리지를 가리킨다(분량 개념이 아니다) — 이 두 파일만 "볼륨" 치환에서 통째로 제외
- 겹말 방지: 원어와 새말이 겹치는 4개 구문(`참조 폐포`→`참조 확장`, `슬롯 자리`→`호출 자리`,
  `폴백 경로`→`대체 경로`, `dangling 참조`→`끊긴 참조`)은 단어별로 각각 바꾸면
  "참조 참조 확장" 류 겹말이 나서 구문째 먼저 접고 그 다음 단어치환을 돌렸다. 그래도
  `_work/_G_재도전2026_적재.py` 하나는 **다른 세션이 독립적으로 손댄 파일**이라 "끊긴
  참조 참조" 겹말이 남아 있던 것을 최종 grep 스윕에서 잡아 고쳤다(내 스크립트를 안 거친
  자리라 놓칠 뻔함 — scripts/ 전체를 매번 재스캔해야 하는 이유)
- 동사 활용 예외: "폴백한다" 처럼 `**폴백**한다` 로 마크다운 볼드가 명사와 조사 사이에 끼면
  가드가 못 잡는다(공백/언더스코어 가드만 있었음). 자동 치환 후 결과를 눈으로 다시 훑어
  "고정 문구로 대체한다" 로 손으로 고쳤다(대체 경로 는 명사라 "-한다" 를 못 받는다)
- 알려진 잔여 흠 하나 — `rule_lookup.py` 의 "L1 통과 조건(`base_룰` ②)와" : 원문 "게이트(…)와"
  에서 조사(와)가 명사 바로 뒤가 아니라 괄호 삽입구 뒤에 붙어 있어 자동 교정이 못 미쳤다.
  조사 하나만 어색하고 뜻은 그대로라 방치 — 눈에 띄면 손으로 고칠 것

### 자가검증
① `python -m compileall -q scripts/` 통과(exit=0) — 49개 파일 전부
② 파일마다 적용 직후 재스캔 0건 확인, 전체 완료 후 scripts/ 전체 재스캔 **0건**
   (겹말·조사 오류·파일명 오염·CLI 플래그 오염 4종 그레프 전부 0건)
③ 실제 실행 대조 — `rule_lookup.py --smoke`(270/270 통과) · `case_search.py --selftest`
   (F5 게이트 전부 통과, 실 DB) · `seed_rules.py --dry`(생성 72행, 전환 전과 동일) ·
   `judge_run.py --help` / `orchestrate.py --help`(전체 import 체인) · `llm_schema.py --slot 1`
④ 전체 pytest 는 안 돌림(ai-ae 몫)

### 용어별 전체 등장 수(전/후) — scripts/ 전체, 코드 식별자 포함 원값
| 용어 | 전 | 후 | 줄어든 만큼 = 주석·docstring 치환분 |
|---|---:|---:|---:|
| 골든셋→정답셋 | 158 | 94 | 64 |
| 게이트→통과 조건 | 166 | 107 | 59 |
| 정본→기준 문서 | 143 | 98 | 45 |
| 코퍼스→규정 모음 | 93 | 47 | 46 |
| 슬롯→호출 자리 | 85 | 48 | 37 |
| dangling→끊긴 참조 | 101 | 71 | 30 |
| 폐포→참조 확장 | 106 | 76 | 30 |
| 어휘집→용어 사전 | 56 | 30 | 26 |
| 폴백→대체 경로 | 45 | 23 | 22 |
| 대장→목록 | 34 | 22 | 12 |
| 판정층→판정 단계 | 5 | 2 | 3 |
| fallback→대체 경로 | 3 | 1 | 2 |
| 하네스→자동 점검 | 1 | 0 | 1 |
| 레인→작업 갈래 | 2 | 1 | 1 |
| 볼륨→분량 | 18 | 18 | 0(전부 보호 — 아래) |
| 스냅샷→현재 상태 | 3 | 3 | 0(주석·docstring 안에 아예 없었음) |
합계 치환 약 378건 (구문 우선치환 포함 — `_lib/db.py`·`_lib/paths.py` 등 신규 파일은 대상
아님, `orchestrate.py` 는 ai-ae 전담이라 이 표에서 뺌)

### 바꾸지 않은 것 (판단 근거)
- **코드 식별자 전부** — `class 슬롯`(adapter.py) 등 프로그램 이름과 겹치는 자리. 위 표의
  "후" 잔존치가 그 실측이다. 2차 (B)(식별자 치환)가 승인되면 그때 같이 간다
- **DB 컬럼·JSON 키·SQL 리터럴** — `코퍼스버전`(eval.runs 컬럼)·`게이트값`·`게이트임계`·
  `게이트분해`·`슬롯표`·`슬롯이름`·`정본비목`·`정본증빙`·`정본명`·`정본값`·`대장행`·
  `볼륨되는_DC`·`골든셋측`·`폐포전용`·`폐포깊이`·`dangling수`·`_dangling검사` 등 확인된
  합성 식별자는 뒤에 붙어도 안 건드리는 방어 목록에 올려 보호
- **argparse `help=` 문자열** — 화면에 뜨는 실행 시 텍스트라 주석·docstring 스코프 밖
- **CLI 플래그 이름** — `--dangling`·`--폐포`·`--cpu-fallback`
- **파일 경로 인용** — 백틱 안의 실제 파일명(`_비목_어휘집.json` 등)
- **`pick_gpu.py`·`runpod_pod.py` 의 "볼륨" 전부** — RunPod 네트워크 스토리지를 가리키는
  같은 철자 다른 뜻이라 파일 단위로 통째 제외
- **`stage2_cases.py` 등 SQL/print 인자 안의 "코퍼스"·"dangling"** — 사용자에게 찍히는
  런타임 문자열이라 주석·docstring 이 아님(범위 밖)

### 남은 것
- 2차 (B) — 한글 식별자 119건 + DB 2건. 승인 전 착수 금지, 지금 대기 중
- (B) 승인되면 위 "바꾸지 않은 것" 의 합성 식별자 목록(게이트값·슬롯표·정본비목 등)이
  실제 변경 후보가 된다 — 이번에 이미 위치를 다 찾아뒀으니 (B) 착수 시 재탐색 불필요

### 산출물
scripts/ 전체 49개 파일 주석·docstring 치환(orchestrate.py 제외) + `_work/` 2개 중
`_G_재도전2026_적재.py` 겹말 1건 수정. 신규 파일 없음(순수 치환)

---

## 2026-09-02 · 2차 태스크 (B) — 한글 식별자 치환, 교차 참조 0인 것만

ai-ae 가 승인하며 범위를 좁혔다: 🔴 **교차 참조가 0인 식별자만 바꾼다.** 다른 파일이
부르는 것은 이름을 그대로 두고 목록에만 올린다. (A)에서 이미 찾아둔 합성 식별자
후보(게이트값·슬롯표·정본비목 등, 36건)에 대해 아래 6단계를 그대로 적용했다.

### 한 일
① `find_identifiers.py`(AST 스캔) — 후보 36건 확정 (16개 용어 사전에 물리는 함수명·
   변수명·클래스명, `scripts/ server/ tests/ db/` 전체 대상)
② `check_refs.py` → 틀렸다. `grep -rlP` 의 한글 `\b` 가 신뢰 불가라 폐기
   `check_refs2.py` 로 교체 — 파이썬 `re` 자체의 유니코드 인식 lookaround
   (`(?<![\w가-힣])이름(?![\w가-힣])`)로 다시 셈. 최종 재확인(이번 보고 직전 재실행,
   숫자 전부 재현됨):
   - **교차 참조 0건 → 22건**: 이 중 `볼륨되는_DC`(④에서 제외)를 뺀 21건이 원래 대상,
     그중 `orchestrate.py` 소속 4건은 파일 경계 사고(아래)로 갈렸다
   - **교차 참조 있음 → 13건**: dangling(65회, 14파일) · 게이트(33회, 8파일) ·
     게이트값(6) · 게이트임계(1) · 슬롯(5) · 슬롯표(2, `server/main.py`) · 정본(8) ·
     정본비목(2) · 코퍼스(18) · 코퍼스버전(4) · 폐포(17) · 폐포수집(2) · 폴백(5)
     — **바꾸지 않았다.** 목록만 올린다
③ 나머지 20건(orchestrate.py 4건 제외)은 각각 사전 `grep -n` 으로 정의부·사용부가
   자기 파일 안에서만 나오는지 재확인 후 `Edit(replace_all)` 로 개별 치환 — 14개
   파일에 걸쳐 18건 (아래 표)
④ 제외 판단 2건 (교차 참조는 0이지만 바꾸지 않음):
   - `확정본`(`_work/_골든셋_재적재.py`) — "정본"을 글자로 포함하는 우연한 부분일치.
     실제 의미는 "확정된 원본 파일" 이지 사전이 노리는 "정본"(기준 문서) 개념이 아니다
   - `볼륨되는_DC`(`pick_gpu.py`) — (A)에서 이미 확인한 "볼륨=RunPod 스토리지" 뜻이라
     "분량"으로 바꾸면 뜻이 깨진다. 파일 단위 제외를 식별자 단위로 확장 적용
⑤ 🔴 **경계 사고** — (B) 스캔에서 (A) 때 쓰던 제외 파일 목록을 재사용하며
   `orchestrate.py` 재제외를 빠뜨렸다. 교차 참조 0으로 나온 그 안의 4건에
   `replace_all` 을 적용했고, 첫 Edit 에서 "파일이 디스크에서 바뀌었다" 경고가 떴다
   (ai-ae 가 그 사이 실제로 수정 중이었다). 되돌리지 않고 — 사고 1에서 배운 대로
   되돌리기 자체가 더 위험하다 — 상태(컴파일 통과·이름 일관성·`"게이트B임계"`
   무밑줄 프론트 계약 키는 안 건드림)를 확인만 하고 ai-ae 에 그대로 보고했다.
   ai-ae 판정(커밋 완료): 3건(`통과조건B_임계`·`통과조건C_격차`·`통과조건C_최소신뢰`)은
   원복 — 이유는 내가 틀려서가 아니라 이 파일에서 "게이트"가 산문이 아니라 응답
   필드명·환경변수(`SUDDOE_GATE_B/C`)·검색 반환 dict 키까지 걸치는 **외부 계약어**라
   상수만 바꾸면 같은 파일 안에서 같은 낱말이 다른 걸 가리키게 되기 때문. 1건
   (`_규정모음버전` ← `_코퍼스버전`)은 사설 전역이라 파일 밖에서 안 쓰여 유지.
   재발 방지: **스캔 스크립트 자체에 `orchestrate.py` 하드코딩 제외**를 걸었다
   (제외 목록 "재사용"에 의존하지 않는다)
⑥ 14개 파일 전부 `compileall` + 실제 CLI 실행으로 재확인 (아래 자가검증)

### 바꾼 것 (18건 · 14파일 · 전부 교차 참조 0 확인)
| 파일 | 이전 | 이후 |
|---|---|---|
| seed_check_items.py | `_dangling검사` | `_끊긴참조검사` |
| l3_load.py | `_비목_폴백` | `_비목_대체경로` |
| load_evidence.py | `_어휘집` | `_용어사전` |
| load_evidence.py | `정본_비목_enum` | `기준문서_비목_enum` |
| rule_lookup.py | `_어휘집_직결` | `_용어사전_직결` |
| sweep_retrieval.py | `게이트_그리드` | `통과조건_그리드` |
| sweep_retrieval.py | `게이트값들` | `통과조건값들` |
| sweep_retrieval.py | `게이트분해` | `통과조건분해` |
| normalize_run.py | `슬롯1_스키마` | `호출자리1_스키마` |
| adapter.py | `슬롯이름` | `호출자리이름` |
| llm_schema.py | `어휘집_경로` | `용어사전_경로` |
| agent_a4.py | `어휘집_트리거` | `용어사전_트리거` |
| seed_rules.py | `정본_비목_enum` | `기준문서_비목_enum` |
| seed_rules.py | `정본증빙` | `기준문서증빙` |
| llm_validate.py | `조지정_dangling` | `조지정_끊긴참조` |
| retrieve.py | `폐포SQL` | `참조확장SQL` |
| eval_retrieval.py | `폐포res` | `참조확장res` |
| retag_scope.py | `폐포전용_집합` | `참조확장전용_집합` |

### 안 바꾼 것 + 이유 (더 중요한 쪽)
**교차 참조가 있어 그대로 둔 13건** — 다른 파일이 실제로 부르므로 이름만 바꾸면
호출부가 조용히 깨진다. (횟수·파일 수는 `check_refs2.py` 재실행 재확인치)
| 식별자 | 정의 파일 | 참조 횟수 | 참조 파일 수 |
|---|---|---:|---:|
| dangling | 여러 곳(arg/var) | 65 | 14 (`db/init` 포함) |
| 게이트 | sweep_retrieval.py 등 | 33 | 8 |
| 게이트값 | orchestrate.py | 6 | 2 |
| 게이트임계 | orchestrate.py | 1 | 1 |
| 슬롯 | adapter.py(class) | 5 | 3 |
| 슬롯표 | adapter.py | 2 | 1 (`server/main.py`) |
| 정본 | 여러 곳 | 8 | 4 (`db/init` 포함) |
| 정본비목 | load_evidence.py | 2 | 2 |
| 코퍼스 | sweep_retrieval.py 등 | 18 | 8 |
| 코퍼스버전 | eval_store.py | 4 | 2 |
| 폐포 | assemble_context.py/retrieve.py | 17 | 3 |
| 폐포수집 | retrieve.py | 2 | 1 |
| 폴백 | draft_inquiry.py | 5 | 5 |

**다른 이유로 제외한 2건**
- `확정본` — "정본"을 문자로 포함하는 우연한 부분일치일 뿐, 사전이 노리는 개념이 아니다
- `볼륨되는_DC` — "볼륨"이 RunPod 스토리지를 가리키는 이 파일 특유의 뜻이라 "분량"으로
  바꾸면 오역이 된다 ((A)의 `pick_gpu.py`/`runpod_pod.py` 파일 단위 제외와 동일 판단)

**`orchestrate.py` 4건 — 경계 사고 후 ai-ae 판정**
- `통과조건B_임계`·`통과조건C_격차`·`통과조건C_최소신뢰` → **원복**(응답 필드·환경변수·
  검색 dict 키까지 걸치는 외부 계약어라 상수만 바꾸면 파일 안에서 같은 말이 갈린다)
- `_규정모음버전`(← `_코퍼스버전`) → **유지**(사설 전역, 파일 밖 미사용)

### 자가검증
① `python -m compileall -q scripts` 통과(exit 0) — 사고 직후 1회, 이번 마감 전 1회
② 14개 파일 전부 실제 CLI 실행으로 재확인 (`--help` 만으로 끝내지 않음):
   `seed_check_items.py --check`(게이트 b/c 통과) · `l3_load.py --org <실제 UUID>`
   (비목 8종 표 출력, `_비목_대체경로` 경로 포함) · `load_evidence.py --diff`
   (125종/71종 완전일치) · `rule_lookup.py --smoke`(270/270) · `normalize_run.py --q`
   (실 LLM 호출 성공 — adapter.호출(호출자리이름=…) 종단 확인) ·
   `retrieve.py --q "여비 한도"`(실 검색 결과 3건 + 참조확장 정상) ·
   `eval_retrieval.py --k 5 --사업필터`(hit@5 수치 정상 출력) ·
   `retag_scope.py --dry-run`(집계 정상) · `llm_validate.py --self-test`(회귀 전건 통과) ·
   `agent_a4.py --dry`(458건 dry 처리) · `seed_rules.py --dry`(이전과 동일)
   `sweep_retrieval.py` 는 `--help` 만 확인(그리드 서치 자체는 비용 문제로 실행 안 함 —
   치환된 3개 식별자는 사전 `grep` 으로 자기 파일 내부 전용임을 이미 확인)
③ 전체 pytest 는 안 돌림(ai-ae 몫)

### 함정
- **경계 사고 재발** — (A) 때 만든 파일 제외 목록을 (B) 스캔에 그대로 재사용하다
  `orchestrate.py` 재제외를 빠뜨렸다. "제외 목록 재사용"은 그 자체로 위험 신호 —
  이번에 스캔 스크립트에 파일명을 하드코딩해 재발을 원천 차단했다
- **교차 참조 체커 자체의 버그** — 1차 `grep -rlP` 의 한글 `\b` 신뢰 불가(잘못된
  0건 판정 위험) → 파이썬 `re` 로 교체. 그 파이썬 버전에도 자기 파일 제외 로직에
  경로 구분자(백/정슬래시) 불일치 버그가 있어 `확정본` 이 자기 파일 안에서도
  "교차 참조 2건"으로 잘못 잡혔다 — 다만 이 건은 애초에 의미상 제외 대상이라
  결론에는 영향 없음. 체커 코드 자체는 안 고쳤다(스크래치패드 도구, 결론 재확인만
  하고 정확도가 결론을 안 바꾸는 걸 확인)

### 남은 것
- 없음 — (B) 종료. DB 컬럼명 치환(사전에 있던 "DB 2건")은 스키마 변경이라
  범위 밖(코드 식별자 리네이밍이 아니라 마이그레이션 필요) — 진행 안 함, ai-ae 판단 필요시 별도 요청

### 산출물
14개 파일 18건 치환 + `orchestrate.py` 관련 4건(3원복·1유지, ai-ae 확정·커밋
완료). 신규 파일 없음(순수 치환)

---

## 2026-09-02 · 후속 검토 — 「뜻이 안 바뀌었나」 역검증 (오너 지시, ai-ae 하달)

`compileall`·pytest·잔존 0건은 «안 죽는다» 의 증거일 뿐 «뜻이 맞다» 의 증거가
아니라는 지적을 받고, 치환 49개 중 8개 파일을 원본과 나란히 눈으로 대조했다.

### 한 일
① 표본 선정 — 위험순: 변경량 top5(seed_rules·rule_lookup·build_refs·adapter·
   llm_validate) + 판정 경로(retrieve·assemble_context·judge_run, 위 5개와 중복분 제외) +
   「게이트=계약」 의심 파일(dict 키·env 변수로 게이트가 박힌 곳을 grep 으로 먼저 찾음:
   retrieve·eval_e2e·sweep_retrieval·stage0_run·draft_inquiry·stage2_chunk, orchestrate.py 는
   읽기 전용)
② 내 대조 도구 자체를 먼저 점검(오너가 그날 밤 자기 스크립트에서 겪은 한글 경로
   8진 이스케이프 함정을 나도 확인하라고 함) — `git diff --name-only` 가 이 저장소에서
   실제로 한글 파일명을 이스케이프한다(`scripts/_work/` 34개 확인). 내 표본 8개는
   전부 영문 파일명이고 리터럴 경로를 직접 `git diff -- <경로>` 로 넘겼지 그 출력을
   파싱하지 않아 함정에 안 걸렸다는 것까지 확인
③ `git diff baseline-0902-정리전 -- <8개 파일>` 을 사람 눈으로 정독, project-wide grep 으로
   "치환어+괄호삽입구+조사" 패턴 전수 조사

### 발견 (4종)
① **뜻이 바뀐 곳(=④ 과잉 치환과 동일 건)** — `build_refs.py` 모듈 docstring "해소 상태
   3종" 범례. `resolved`·`shifted`는 실제 `해소상태` 컬럼 리터럴이라 그대로 남았는데
   셋째 `dangling`만 "끊긴 참조"로 바뀌어 병렬이 깨짐. DB CHECK 제약(`01_schema.sql:262`)이
   지금도 문자열 `dangling`을 강제한다 — 산문이 아니라 열거값 이름을 적은 자리였다
② **조사 깨짐** — "치환어+괄호삽입구+조사" 패턴 3건(뜻은 안 바뀜, 이미 알려진
   rule_lookup.py 1건 포함): `case_search.py:18`("…가"→"…이") · `seed_rules.py:1667`
   ("…과"→"…와") 신규 2건. project-wide grep 으로 이 유형은 scripts/ 전체에 딱 3건뿐임을
   확인(orchestrate.py 엔 없음)
③ **코드와 어긋난 주석** — `stage2_chunk.py`·`sweep_retrieval.py` 에서 "통과 조건"으로
   바뀐 주석 바로 다음 줄이 보호된 `print("게이트: …")` 리터럴이라 두 낱말이 붙어 읽힘.
   + 참고 관찰(내 작업 범위 밖): `orchestrate.py` 주석에도 같은 문제가 있었음 —
   ai-ae 가 직접 확인, 자기 실수로 인정하고 6개 파일(orchestrate·sweep_retrieval·
   stage2_chunk·stage0_run·judge_cli·draft_inquiry) 21곳을 "게이트"로 일괄 재통일함
④ 과잉 치환 — ①과 동일 건 외 추가 없음(CLI 플래그·파일명·SQL 리터럴·dict 키 전수
   재확인, 위반 0건)

### 판단
①·②(신규 2건)를 ai-ae 승인 받아 고쳤다. ③은 ai-ae 가 자기 파일들에서 직접 일괄
정리(내 담당 아님, orchestrate.py 는 계속 읽기 전용 유지).

### 자가검증
`python -m compileall -q scripts` exit 0 · `case_search.py --selftest` 전부 통과
(분리·오염 체크 포함) · `seed_rules.py --dry`(uq_rules_key 중복 0 · 비목 집합
정본10/사용10 일치 · 근거 빈 행 0, 수정 전과 동일)

### 함정
DB CHECK 제약처럼 **리터럴 열거값 3개를 나란히 적은 범례**는, 그중 하나만 한글로
바꾸면 "이것도 프로그램이 실제로 쓰는 이름이다"라는 신호가 사라진다 — `resolved`/
`shifted`/`dangling`처럼 형제 항목이 있으면 그 형제와 대칭을 맞추는 게 판단 기준.
ai-ae 가 `docs/부록/용어_기준.md` 에 이 규칙을 일반형으로 박아뒀다.

### 산출물
3개 파일 3건 수정(build_refs.py·case_search.py·seed_rules.py). 신규 파일 없음

---

## 2026-09-02 · 사업명 표기 대조 + `비목계통()` 폴백 제거 (오너 지시, ai-ae 하달)

### 한 일
① 사업명 표기 5벌(프론트 번들·corpus.rules·precedence_rules·폴더명·golden_set/chunks)
   전수 대조표 작성 — 읽기 전용. `corpus.programs`(8행, 별칭 배열 있음)가 이미
   정본으로 존재하는 걸 확인, ai-ae 의 "CLAUDE.md 가 precedence_rules 를 정본이라
   한다"는 전제가 문서 근거 없음을 확인(grep 0건) — 실제 선언은 `db/init/04_agent.sql:263`
   에 있고 대상은 `corpus.chunks.사업명`. ai-c7(BE) 협업으로 프론트 번들 원문 확정,
   U+30FB/U+00B7 가운뎃점 코드포인트 차이를 직접 `unicodedata` 로 재확인
② "터질 자리 세기" — scripts/server/db/tests 전수(250여 줄) grep. 다운스트림은
   전부 파라미터화 쿼리라 실패 양상이 "조용히 0행" 하나로 통일됨을 확인하고,
   `rule_lookup.비목계통()` 의 "못 찾으면 창업으로" 폴백이 TIPS 를 창업 계통으로
   오분류해 "근거 붙은 오답" 을 만드는 경로를 코드로 추적해 보고
③ ai-ae 승인 — `rule_lookup.py` 의 `비목계통()`·`base_룰()`·`l3적용가능()` 3개 함수 수정
   (server/main.py 정규화는 BE 담당, corpus.programs 별칭 추가는 ai-ae 담당 — 안 건드림)

### 판단
`사업명 이 None(미지정)` 과 `사업명을 줬는데 못 찾음` 을 갈랐다 — 전자는 기존대로
"창업"(정답셋 공통 27문항 처리 유지), 후자는 `None`을 돌려주고 호출부(`base_룰`·
`l3적용가능`)가 그걸 받으면 조회 자체를 접어(`[]`/`False`) `effective_rule()` 이
판단불가(`None`)로 닫히게 했다. `corpus.programs` 쿼리 자체가 예외(테이블 없는
워킹트리)일 때는 기존 "창업" 폴백을 그대로 유지 — 이건 표기 불일치가 아니라
스키마 미비 상황이라 다른 문제다.

### 자가검증
① `python -m py_compile scripts/rule_lookup.py` 통과
② `--self-test` 통과 · `--smoke` 270조합 전건 통과(기존과 동일)
③ **함수를 실제로 태워 확인**(스키마만 읽고 끝내지 않음) — 실제 프론트 TIPS 문자열
   "2026 민관공동 창업자 발굴·육성"으로:
   `비목계통()` → `None`(정본 'TIPS' 로는 'RND', `None` 입력은 그대로 '창업' — 3갈래 다 확인)
   `base_룰(..., "인건비")` → 0행(수정 전 SQL을 그대로 재현해 대조한 결과 **1행이 붙었을
   것**을 확인함 — rule_id 421, 조건부, 근거 `L1_...통합관리지침 제36조·제41조`.
   제41조가 바로 코드 주석이 경고하던 "대표자 인건비 불가" 조항이다)
   `l3적용가능()` → `False` · `effective_rule()` → `None`(판단불가)
④ 🔴 **기존 pytest 는 이 경로를 아예 커버하지 않는다** — `tests/` 전체에 `rule_lookup`
   을 import 하는 파일이 0개(확인함). "테스트가 안 깨졌다"가 아니라 "애초에 아무 테스트도
   이 함수를 안 불렀다"는 뜻이라 정직하게 그렇게 보고한다. 전체 pytest 는 안 돌림(ai-ae 몫)

### 함정
없음 — 사고 없이 끝남. `except Exception` 분기(`corpus.programs` 테이블 자체가 없는
워킹트리)와 "쿼리는 성공했는데 0행"을 갈라야 했던 게 유일하게 조심할 지점이었고,
전자는 기존 "창업" 유지·후자만 `None` 으로 바꿔 처리했다.

### 남은 것
- `server/main.py` 진입점 정규화 — BE(ai-c7) 담당
- `corpus.programs.별칭` 에 "민관공동 창업자 발굴·육성"(U+00B7) 추가 + 모두의창업
  표기 결정 — ai-ae 담당

### 산출물
`scripts/rule_lookup.py` 1개 파일, 함수 3개(비목계통·base_룰·l3적용가능) 수정.
신규 파일 없음

### 정정 (ai-ae 지적, 커밋 dc39f28 이후)
"tests/ 에 rule_lookup 을 직접 import 하는 파일이 0개다" 를 "이 경로는 커버 안
된다" 로 넘겼는데 **틀렸다** — 전체 pytest 를 돌려보면 `test_e2e_flow`·
`test_persist` 가 간접적으로 이 경로를 탄다. "직접 import 안 한다"는 사실 그대로고,
"커버 안 된다"는 그 사실에서 한 칸 넘어간 추론이었다 — 이 프로젝트가 반복해서
틀린 바로 그 유형(측정은 맞고 추론이 넘어감). 후속으로 `tests/test_rule_lookup.py`
를 신설해 직접 회귀를 박았다(아래).

## 2026-09-02 · `test_rule_lookup.py` 신설 (ai-ae 지시)

### 한 일
`비목계통()`·`base_룰()`·`l3적용가능()`·`effective_rule()` 의 새 분기를 직접 재는
회귀 파일을 만들었다. 읽기만 한다 — `corpus.rules`·`corpus.programs` 기존 행만
쓰고 INSERT/UPDATE/DELETE 없음, 픽스처 생성/삭제 자체가 필요 없어 8세션 동시
DB 사용과 안 부딪힌다.

테스트 8개: `비목계통()` 세 갈래(미지정→창업 · 정본 TIPS→RND · 모르는 표기→None,
`!= "창업"` 도 명시적으로 단언) · `base_룰()` 두 갈래(모르는 사업명→`[]` · 정본→
예외 없음, 예비창업패키지로 행수>0 대조) · `l3적용가능()` False 갈래 · **회귀
1건**(프론트 TIPS 표기+인건비 → `effective_rule()` is None, docstring 에 "깨지면
rule_id 421(제36조·제41조)이 돌아온 것" 명시) · `corpus.programs` 표 자체가 없는
워킹트리에서 "창업" 유지(가짜 커서로 DB 없이 검증).

### 자가검증
`pytest tests/test_rule_lookup.py -v` — 8 passed
`pytest tests/test_rule_lookup.py tests/test_tenant_isolation.py tests/test_contract.py -q`
(다른 실DB 테스트와 파일 순서 충돌 확인) — 79 passed, 1 xfailed
전체 pytest 는 안 돌림(ai-ae 몫). 테스트 통과시키려고 프로덕션 코드는 안 고쳤다 —
8개가 한 번에 통과해서 고칠 필요가 없었다.

### 산출물
`tests/test_rule_lookup.py` 신설(8개 테스트). 기존 파일 수정 없음
