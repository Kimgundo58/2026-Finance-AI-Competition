# 2026-09-05

`docs/9_미결.md` 예정 항목 1번(2026-09-04 팀이 코드만 커밋하고 못 끝낸 것) 마무리.
단독 세션 — 병렬 레인 없음.

---

## 14:00 · table_splice.py 정리 커밋 + PR #29 머지

`docs-style-2026-0904` 브랜치 워킹트리에 커밋 안 된 수정본(`1451e46` "라벨 있으면
무조건 교체" → "오염된 조만 교체")이 남아 있었다. 그 브랜치는 이미 main 에 병합된
뒤(PR #28)라, main 에서 새 브랜치를 따 stash 로 옮겨 재적용 — 문체 정리 커밋들과
섞이지 않게 했다.

- PR #29 머지, main `6be6279`
- 검증(로컬 dry-run): 창업중심대학 2025 재분해 → 참고1~5 오염 신호 0,
  "사기프티콘"·"현사물로"(오염형) 사라지고 "기프티콘"·"현물로"만 남음

## 14:40 · GPU 팟 정리

훅이 열려 있던 팟(`5ofl00dpzidchp`, L40, $0.82/h)을 지목 — 이번 작업에 GPU 불필요라 닫음.
잔액 $17.31 · 시간당 $0.005 로 정상화.

## 15:10 · 스코프 좁힌 재적재 — 로컬

전체 재적재(`load_db.py`/`stage2_chunk.py`) 는 `corpus.chunks` 20,525건을 TRUNCATE
하고 임베딩을 CPU 로 처음부터 계산하면 4시간이다(`stage2_chunk.py` 주석 실측). 이번
변경은 4문서뿐이라 전용 스크립트(`scripts/_work/_참고3_scoped_reload.py`)로 좁혔다.

- 바뀐 조: 8건 — 초격차 참고3·참고6 / TIPS 별첨1·붙임3·붙임5 / 창업도약 별지서식 /
  창업중심대학 참고3·참고5 (사람이 지목한 게 아니라 재분해 후 DB 대조로 코드가 찾음)
- 로컬 청크 39건 삭제 → 53건 신규, 최대 944토큰(게이트 1024 통과), CPU 임베딩 178.2초
- CCU 검증(`_골든셋_2026/_tools/중앙_검증_ai-c5.py` 창업중심대학.json):
  "라벨제거후" 대조 실패 3건 → **0건**

## 15:40 · 같은 재적재 — 운영 Cloud SQL

`gcloud secrets`(`suddoe-pg-root`)로 postgres 루트 계정을 받아 운영 DB(34.64.37.177)에
직접 연결. 같은 스크립트로 dry-run → 로컬과 동일한 8개 조 확인 → apply.

- 운영 청크 39건 삭제 → 53건 신규, CPU 임베딩 73.2초(가중치 캐시 재사용)
- `eval.golden_set` 이 운영에 없어(정답셋 미탑재, 의도된 것) CCU 스크립트는 못 돌림 —
  대신 "기프티콘" 있음·"사기프티콘" 없음 직접 대조로 확인
- 컨테이너 재배포 안 함 — 데이터만 바뀐 변경이라 Cloud Run 이 다음 요청부터 바로 반영

## 16:00 · md 최신화

- `docs/QA/서비스_현황_인수인계_0904.md` 의 "파싱 오염 수정 중" 알려진 이슈 → 해결로 갱신
- `2026_Finance_DATA_FOR_RAG/_룰검토_2026/_다음_태스크.md` 항목 1 완료 처리
- `실제 구현.md` 에 산출물 기록 추가

---

## 산출물

```text
PR #29 · main 6be6279                              table_splice 오염조만 교체
scripts/_work/_참고3_diff_점검.py                    1회성 진단(DB 안 씀)
scripts/_work/_참고3_scoped_reload.py                재적재 본체. SUDDOE_DSN 으로 로컬/운영 전환
```

## 남은 것

`_다음_태스크.md` 항목 2~8 그대로 — eval run 재측정(분모 315) · 룰 스키마 칸 4개 ·
룰 수정 보류 9건 · 문체 정리 잔여 · TIPS/초격차 표본 검증 · 낫표 표기 통일 등.
이번 세션에서 손 안 댔다.
