# 레인 P4 — GPU 실측 (2026-09-03)

중앙 ai-e8 · 세션 ai-e6 · 워크트리 `금융 AI공모전-agent` · 브랜치 agent-perf-2026-09-03.
🔴 **오늘 GPU 는 한 번도 안 켰다. 팟 0개. 비용 0원.** 할 일 0번(eval_e2e 개조 + dry 배관)만 했다.

## ① 지금 어디까지 됐나

`scripts/eval_e2e.py` 개조 완료 · 컴파일 통과 · dry 93문항 2회 완주(오류 0).
**소유 밖 파일은 0줄 고쳤다.**

### 왜 래퍼 같은 걸 썼나 — 나중에 묻는 사람을 위해

run 191 이 리플레이 불가였던 것은 eval 의 결손이 아니라 **`orchestrate.판정()` 의 반환 계약
결손**이다. 관측:

- `프롬프트길이` 는 dry 분기에만 있고 실전 update 에 없다 → DB 실측 run 191 **non-null 0/93**
- 검증 «전» LLM 원 JSON · `룰들` · 폐기된 인용·전제는 함수 밖으로 아예 안 나온다
  (`llm_validate.검증` 은 `(d, 사유)` 만 돌려준다 — 폐기분은 사유 «문자열» 로만 남는다)
- `dangling` 은 응답에 있는데 eval 이 안 베꼈다 → run 191 원출력에 `검색` 키 **0/93**

추론: 정답은 orchestrate 에 관측 훅을 다는 것(B안)이다. 그런데 그 파일은 백엔드 레인이 도는
본 트리와 같은 파일이라 이 레인이 못 고친다. 그래서 **모듈 전역을 감싸 계약을 우회했다(A안).**
**이건 임시방편이다.** 언젠가 B안으로 걷어내라.

### 들어간 것

| | |
|---|---|
| `SUDDOE_EVAL_RAW=1` | 검증 전 1겹 원본 포획. 기본 off |
| `SUDDOE_EVAL_RAW_PROMPT=1` | 프롬프트 전문까지. 기본 off (크기 때문) |
| 감싸는 자리 4곳 | `orchestrate.조립` · `orchestrate.정규화` · `orchestrate.검증` · `orchestrate.llm_호출`(④) · `normalize_run.llm_호출`(①) |
| 정체성 가드 3단 | ①감싸기 전 `is` 정본 ②감싼 후 `is not` ③run 끝 문항 단위 불변식. 어긋나면 **저장 전에 예외로 죽는다** |
| `--변형` `--세트`(다중) `--부분집합` `--gold-ids` `--max-model-len` | |
| `설정` 스탬프 | 변형·부분집합·max_model_len·관측 최대토큰(①④ 따로)·코퍼스버전·rules수·적용대상분포·GPU·VLLM_URL·RUNPOD_POD_ID·켜진 `SUDDOE_*` 전부 |
| 항상 남기는 원출력 키(플래그 무관) | `모델`(종료이유·토큰) · `dangling` · `후보수` · `정규화` · `비목` · `게이트` · `금지근거` |

🔴 **`orchestrate.llm_호출` 만 감싸면 호출 ①이 통째로 샌다.** ①의 실제 호출은
`normalize_run.py:280` 이 **자기 모듈 전역**을 부르는 것이라 orchestrate 쪽 바인딩을 갈아도
안 걸린다. 두 자리를 따로 감싼다. (P1·ai-e8 과 독립 도달)

🔴 **B 블록 경계 정의는 P3(`assemble_context.블록자수/블록해시`)를 부른다.** 여기에 정규식을
따로 두지 않는다 — 정의가 둘이면 P3 의 표와 이 run 이 안 맞는다. 검산 `합 == len(프롬프트)`.

### 고친 계측 결함 하나

프롬프트 예산 줄의 `MML` 이 **24576 으로 하드코딩**돼 있었다. run 191 은 40960 으로 떴는데
이 줄은 24576 기준으로 「초과 14건 🔴」을 찍는다 — 표와 서버가 다른 수를 본다.
`--max-model-len` 으로 받게 했고 **기본값은 24576 그대로**라 안 주면 현행과 같은 줄이 나온다.

## ② 다음 한 걸음

1. 🔴 `pod_serve.sh` 에 `--reasoning-parser qwen3` 추가 (ai-98 실측: Qwen3 thinking ×
   guided_json 이 공백만 뱉으며 토큰을 태우는 루프에 빠진다). **P4 다음 세션 첫 항목.**
   ①정규화·④조립이 같은 guided_json 을 쓴다 — run 191 이 통과한 건 프롬프트가 운 좋게
   루프를 안 밟은 것이지 구조적 안전이 아니다.
2. ai-e8 의 신호를 받고 팟을 연다(**신호 전에는 열지 않는다**). `--max-model-len 40960` 확정
   (ai-43 승인). 32768 로 내리면 run 191 기준선(최장 44,926자)이 깨진다.
3. 앵커 V0 · 튜닝52 · 라벨 `P4-V0-52` · `SUDDOE_EVAL_RAW=1`.

## ③ 아직 안 닫힌 물음

- 🔴 **run 191 의 max_tokens 가 1500 인지 3000 인지 모른다.** 관측: 라벨과 `docs/0_현황.md`
  는 3000 이라 적혀 있고, `설정` JSON 에는 그 축이 아예 없으며, 직전 커밋 `03a891e` 의
  orchestrate 는 `최대토큰=1500` 이었고 그때 워킹트리는 `+dirty` 였다.
  → **어느 쪽으로도 고쳐 읽지 마라. 재측정으로만 닫힌다.** (ai-e8 지시)
- `코퍼스버전` 해시는 (chunks, embedding, refs, documents, max chunk_id)만 본다.
  오늘 `corpus.rules` 72→74 · `chunks.적용대상` 이 바뀌었는데 **해시는 run 191 과 글자 하나
  안 다르다**(`c20525-e20525-r44865-d283-107daa68`). 그 둘은 검색 필터와 룰 경로의 입력이라
  판정을 바꾼다 → `설정` 에 `rules수`·`적용대상분포` 를 따로 박아 뒀다. 해시만 믿지 마라.
- `finish_reason` 은 골든셋 채점 경로(`eval_e2e`·`judge_run`)에서만 보인다.
  `server/main.py:970` 이 명시 화이트리스트라 **`/api/judge` 실서버 경로로는 영영 못 본다**(ai-e8 정정).
- 원출력 키가 플래그 off 에서도 7개 는다(위 표). 「기본값 현행 유지」에서 의도적으로 벗어난
  지점이다 — 종료이유를 플래그 뒤에 두면 안 켠 run 이 또 나와 run 191 을 반복하기 때문.

## dry 배관 검증 — 93문항 (2026-09-03)

조건: `--dry --no-log --max-model-len 40960 SUDDOE_EVAL_RAW=1` · 코퍼스버전
`c20525-e20525-r44865-d283-107daa68` · 오류 0 · 116초(첫 run 129초, 차이는 임베딩 가중치 로드).
출력 원본 `scratchpad/P4_dry_raw_0903.out` · 개조 전 기준선 `scratchpad/P4_dry_baseline_0903.out`.

```
프롬프트 자수   중앙 15,221 · p90 34,247 · 최장 44,926 · n=93
입력 예산      54,228자 (max-model-len 40,960 - max_tokens 3,000, 0.7토큰/자) → 초과 0건
경로 빈도      1정규화+3검색+4조립+dry중단 91 · 1정규화+C비목갈림 2
근거적중       81.4% (전체) · 보강 95.2% · 적대적 58.3%
```

🔴 **이 표의 일치율(7.5%)은 판정 성능이 아니다.** dry 는 LLM 을 안 부르므로 93문항이 전부
판단불가로 닫힌다. 「배관이 끝까지 도는가」의 증거일 뿐이다.
🔴 **dry 문항과 실패경로 문항에는 래퍼에 ④·검증 포획이 안 남는다. 고장이 아니라 정상이다.**
가드 ③이 「4조립을 지났으면 조립 포획이 있다 / 6검증을 지났으면 ④·검증 포획이 있다」는
문항 단위 불변식으로 세기 때문에 dry 에서도 통과한다.
