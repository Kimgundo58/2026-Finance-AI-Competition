# 레인 P1 — 검증층 재설계 · 다음 run 스키마 (2026-09-03)

세션 ai-2c · 워크트리 `금융 AI공모전-agent` · 브랜치 agent-perf-2026-09-03 · 중앙 ai-e8.
**코드 변경 누계 1건** — `llm_validate.py:29~31` **주석만**(+5/-3, 동작 변경 0, ai-e8 사후 보고).
`llm_schema.py` 무변경 · 동결 2곳 무변경 · DB 쓰기 0 · git 무변경.

| 문서 | 무엇 |
|---|---|
| [[_레인_P1_1일차]] | 다음 run 필드 명세(P4 인계) · 래퍼 구멍 · 오답 해부 · 넓은 규칙 2개 기각 |
| [[_레인_P1_2일차_A_스키마와컨텍스트]] | S번호 폭증 내성 · 컨텍스트 상한 · 행분해는 무해 |
| [[_레인_P1_2일차_B_인용오조준]] | 「엉뚱한 걸 인용」이 아니었다 · 인용 축 규칙 4개 전부 음수 |
| [[_레인_P1_2일차_C_검색진단]] | (a)/(b)/(c) 갈래 · 도달은 적중의 거의 필요조건 · BM25 |
| [[_레인_P1_2일차_D_강등코드_발화]] | **22종 중 4종만** 울린다 · L3 0/93 · 못 태우는 0 과 건강한 0 |
| [[_레인_P1_2일차_E_폐쇄열거_규모]] | 폐쇄 열거는 조 1개 · 문항 3건 · 상한 5.8%p — 축이 작아진다 |

하네스(전부 읽기 전용 · DB 무쓰기 · 튜닝 52 안에서만):
`scratchpad/P1_오답해부.py` · `P1_S번호_내성.py` · `P1_정답조_어디서_빠지나.py`

## 한 눈에 — 2일차에 뒤집힌 것 셋

1. **「인용 오조준 12건」은 오조준이 아니다.** 정답 조가 s맵에 아예 없었다 (ai-e8 전제 정정)
2. **「run 191 이 컨텍스트에 잘렸다」는 성립 안 한다.** 한도 40,960 · 환산 0.73 토큰/자 → 초과 0
3. 🔴 **내가 낸 「닿아도 안 맞는다」가 틀렸다.** 도달 판정을 오늘 검색기로 다시 돌려
   run 191 의 적중과 붙였다. 저장 top5 로 다시 세니 **밀림 33.3%(추측의 바닥) vs 닿음 58.8%** —
   도달은 적중의 **거의 필요조건**이다 (ai-a3 와 분자·분모 전건 일치)

## ③-b 내 파일에 걸린 외부 제약 (P3 · ai-1e 와 합의, 2026-09-03)

P3 의 B3 행분해 **안 A**(문법 안 벗어남)가 `llm_validate` 의 두 성질에 걸려 있다.
**다음 P1 세션이 `_항_추출`·`s번호_메타` 를 고치면 그 근거가 같이 죽는다 — 고치기 전에 P3 에 알려라.**

1. `s번호_메타` 가 article 갈래를 `항호_DB=None` 으로 세운다(:150) → `_항_추출(본문, None)` = `(본문,"조전체")`,
   그리고 `CITE_HANG_MISMATCH` 는 `항호_DB and 항호` 둘 다 참일 때만 깎으므로 행이 늘어도 새로 안 울린다
2. 🔴 **내가 보탠 단서**: `신뢰등급` 의 `any(extraction=='vlm')` 은 개수가 아니라
   «vlm 문서가 인용 집합에 **처음 들어오는가**»다. 행 분해가 인용 **문서 집합**을 넓히면 A→B 강등이 새로 울린다.
   안 A 는 같은 조를 쪼개기만 해서 집합이 안 변해야 맞지만 — **그건 논증이지 실측이 아니다**(ai-1e 인정).
   → P3 이 `docs/기록/_레인_P3.md` §5-3 에 「ROWSPLIT on 의 전/후 인용 doc 집합 대조」를 검사 항목으로 박았다

---


## ⑦ 2일차에 안 닫힌 것

- 🔴 **묶음별 상한 두 개**(58.8% 튜닝 / 94.3% held-out) — 격리 run 2판 없이는 못 닫는다.
  ai-e8 승인 · P4 GPU 첫 판. 입력 사양은 아래 §⑨
- `guided_json` 실제 내성 — GPU 박스 몫. 로컬엔 xgrammar/outlines 가 없다
- (c) 필터컷 1건의 경계 물음 — 오너 결정 대기 ([[_레인_P1_2일차_C_검색진단]])
- 「없다」로 답하는 **폐쇄 열거 조문의 검색** — 규모는 쟀다(조 1개 · 문항 3건 · 상한 5.8%p ·
  [[_레인_P1_2일차_E_폐쇄열거_규모]]). **처방은 안 냈다**(ai-e8 지시). 축 배정 대기
- 닿음 34건의 예측에 **판단불가가 0건**인 것(정답엔 3건) — 관측만 있고 기전은 모른다
- 426·427(TIPS)은 정답근거 필드가 비어 있어 어느 축으로도 설명이 안 된다. [[_레인_P1_1일차]] 의 물음 그대로
- 🔴 **정답셋의 L3 공백** — L3 를 지키는 방어 코드 **셋 전부**를 원리적으로 못 태운다
  (`PRECEDENCE_FLIP` · `L3_ONLY_DOWNGRADE` · `TENANT_LEAK`).
  정답셋 설계라 내 것이 아니다. 오너 결정 대기 ([[_레인_P1_2일차_D_강등코드_발화]])
- `UNVERIFIED_RULE`(동결)이 run 191 에서 0건 — ai-43 에 전달할지 ai-e8 결정 대기
- ~~`eval.runs.설정` 에 코퍼스버전 키가 없다~~ → **내가 틀렸다.** `eval_e2e.py:769` 에 1일차부터
  있고 run 191 이 6키인 건 그때 코드에 없었기 때문이다(P4 정정).
  🔴 대신 **코퍼스버전 해시가 `rules수`·`적용대상분포` 를 «안 본다»** — 그 둘이 바뀌어도 해시가
  같다. 「오늘 top5 가 run 191 과 5개 중 1.88개만 겹친다」의 원인이 거기일 수 있다
  (`적용대상` 이 바뀌면 `retrieve.py:43` pre-filter 가 통째로 움직인다). P4 가 `설정` 에 따로 박았다

**자진 신고**: 프롬프트 복원 집계의 argmax 가 held-out 문항 번호를 한 번 출력했다.
본 것은 «프롬프트 자수» 뿐이고 정답·본문은 안 봤다. 하네스에 가림을 넣어 막았다.


## ⑨ P4 에 넘긴 격리 run(D6) 입력 사양

🔴 **지금의 `--isolated` 를 그대로 뜨면 2판을 해석할 수 없다.** 격리 모드는 B2·B3 를 통째로
갈아치워서(`assemble_context.py:346`) 「정답 조가 있다」와 「방해물 40개가 사라졌다」가 안 갈린다.

| | 자수 중앙 / 최대 | S번호 중앙 / 최대 |
|---|---:|---:|
| 격리 `조립(…, 격리근거=)` | 2,115 / 10,517 | 9 / 42 |
| 실전 B1~B3 (run 191 복원) | 14,258 / 44,077 | 42 / 120 |

**격리 프롬프트는 실전의 1/7 이다.** 그래서 팔을 셋으로 제안했다 —
1판 기준선(실전 검색) · **2판 «꽂기»**(실전 top-5 를 그대로 두고 정답 청크를 s맵에 **추가**) ·
3판 격리(현 `--isolated`, 상한 참고용). **2판이 ai-e8 의 물음에 답하는 팔**이고,
방해물이 남아 실전과 조건이 같다. 2판 ≈ 1판 이면 병목은 검색이 아니라 판정이다.

- 문항 범위 **48건** (튜닝 52 중 `격리_근거` 가 조문을 찾는 것). 세 판 **전부 같은 48건**으로 채점
- 제외 4건: 426·427(정답근거 빔) · 380·391(조번호 라벨 `'[붙임2] …'` 라 `제N조` 추출에 안 걸림)
- 🔴 미사용 41 에는 쓰지 마라 — 정답근거를 프롬프트에 넣는 짓이라 held-out 이 아니게 된다
- 반드시 저장: `finish_reason` · 요청/실제 토큰 · 프롬프트 자수(블록별) · 판단불가는 **모델선택/실패경로** 분리
- 3판은 (a)(b)(c)를 한꺼번에 지워 갈래별 기여가 안 나온다. 2판은 갈래별로 쪼갤 수 있다
- 🔴 **세 판 모두 「보강 / 적대적 / 본세트 / 공식」 묶음별로 채점**해라. 합산 하나는 부풀림이다
  — 닿음 적중이 튜닝 58.8% vs held-out 94.3% 로 35포인트 갈린다 (ai-a3 집계)


## ④ 이 기록이 «판정이 맞다» 의 증거가 아닌 것

여기 있는 것은 전부 run 191 **한 run 의 사후 해부**다. 규칙을 채택한 것이 0건이고
동작을 바꾼 코드는 0줄이다(고친 건 사실이 틀린 주석 하나뿐). 일치율은 1%도 안 움직였다.

2일차도 같다. 과제 A 의 「검증층이 안 깨진다」는 **«안 죽는다» 의 증거지 «판정이 맞다» 의
증거가 아니다** — 오히려 2일차가 보탠 것은 정반대다: 인용 축에서 검증층이 고칠 수 있는
최대치가 튜닝 52 안에서 **1건**이라는 관측이다.
