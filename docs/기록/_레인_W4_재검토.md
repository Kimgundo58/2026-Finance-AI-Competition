# W4 재검토 (2026-09-02 밤, 오너 지시 — 읽기 전용)

규칙대로 git 쓰기·파일 이동 없음, DB 는 읽기만. 옮기지 않았다 — 목록만 올린다.

## ① 아카이브 77개 재검토 — 되살릴 후보

### 🔴 A. «완료된 1회용» 이 아니라 «아직 답이 안 온 요청» — 강한 근거로 확인함

**`_G_룰검수요청.md` + `_G_룰검수요청_대조표.md`(2,761줄)** — §6 "오너가 결정할 것 6건"이
문서 안에 명시돼 있고, DB 로 전부 미해결 확인:

| # | 요청 | DB 실측 (지금) |
|---|---|---|
| ① 새 18행 verified 전환 | `corpus.rules` **53/72** verified — 그대로 |
| ② TIPS `precedence_rules` 1건 추가 | `precedence_rules` 에 TIPS/민관 행 **0건** — 안 됨 |
| ④ 카드 사용제한 업종(노래방 등) `금지예시` 등재 | `rules.금지예시` 에 "노래방" 포함 행 **0건** — 안 됨 |
| ⑥ 재도전 재료비 «불가»로 내릴지 | 여전히 `허용='조건부'·verified=false`, 임시 완화 문구만 적용 |

**6건 중 확인한 4건이 전부 미해결.** 「완료된 검수 자료」가 아니라 「오너 응답을 6일째
기다리는 결정 대기열」이다. 지워지면 이 6개 결정 자체가 존재했다는 사실이 사라진다.

**`_큐잔여9행.json`** — 이전에 "네 판단이 맞다"로 유지했는데, 이번에 DB 로 직접 확인:
`재도전성공패키지` 룰 **9행 중 verified 0건**. "잔여 9행"은 지금도 문자 그대로 잔여다 —
가짜 경보 아니었다.

### 🟠 B. 실측 근거가 여기에만 있는 것 — docs 결론의 유일한 출처

- **`_별칭유출_실측.json` / `_별칭유출_실측_0901.md`** — `item_alias` 228행 중 정답셋 유래
  25건의 «기여도» 측정(트랜잭션 DELETE→재실행→ROLLBACK 방식, 커밋 안 함). **끝에 미해결
  구멍이 있다**: "골든셋 51·52·54·60·62·72·75·77 이 현재 `eval.golden_set` 에서 매칭 안 됨
  — 구 77문항 번호 체계로 보이나 신·구 대응표가 없어 채점 대상인지 판정 못 함." 이 자체가
  다음 사람이 풀어야 할 미결이다
- **`_column_verify.json` / `_split_verify.json` / `_extract_verify.json`** — Stage 0
  파싱 품질 실측(조 개수·gutter·dedupe·validate 등, 문서별). `RAG.md` §3-1 의 4단 fallback·
  gutter 로직 서술의 근거 데이터로 보이는데, 문서 쪽에 이 원본 수치가 재수록돼 있는지
  확인 안 됨
- **`_검수재료_0901.json`** — `_G_룰검수요청.md` §5 "4차 골든셋 역검증·기존 6사업 일관성
  대조"의 원재료(rule_id 별 근거 조문·문장수·평균/최저 유사도). 요청서 §6 결정이 아직
  안 났으니 이 재료도 같이 산다

### ✅ C. 재검토해도 판단 유지 — 근거 보강

**`.bak` 7개** — `_build_refs·_load_db·_seed_rules(×2)·_stage2_chunk·_stage2_chunks.jsonl·
_골든셋_확정본.json`. 대응하는 라이브 스크립트 4종(`build_refs.py`·`load_db.py`·
`seed_rules.py`·`stage2_chunk.py`) 전부 `git log` 에 다중 커밋 이력이 있는 것 확인 —
git 이 이미 그 히스토리를 담당한다. **버려도 된다는 판단 그대로 유지.**

### 판단 안 한 것 — 골든셋 보강후보는 오히려 반대 사례

**`_골든셋_보강후보_0901.json`**(요청 잔존 의심 대상이었으나) — DB 대조 결과 이미 반영됨.
후보 29건(재도전6·TIPS2 포함) = 지금 `eval.golden_set` 의 재도전6·TIPS2 와 **정확히 일치**.
「확정본 아님」이라는 파일 안 문구는 지금 기준 stale — **이미 답이 온 요청**이라 되살릴
필요 없다. 다만 부수 발견: 이 병합으로 정답셋이 77→113행이 됐는데, `LLM.md`(원본, 손 안
댐)·내 `6-3_평가와_정답셋.md`·`docs/9_미결.md` #14/#7 이 인용하는 "77문항"·"재도전·TIPS
0건" 수치가 지금 DB 실측과 어긋난다. **삭제 판단과는 별개 문제**라 여기 기록만 하고
고치지 않았다 — 수치 갱신은 실측 소유 세션 몫이다.

---

## ② `scripts/_work/` 잔류 17개 재확인 — 다 살아있다

**mtime 전수 확인**: 가장 오래된 것도 `_rules_검증.json`(2026-08-31 15:13)로 **어제**다.
"몇 달 전"에 해당하는 파일 0개 — 전부 최근 36시간 안에 쓰였다.

**대응 스크립트 존재 확인**: 오늘 밤 커밋(`c5f66ad` 코드 용어 치환·`02b2fb4` scripts/_lib
전환)이 있었길래 개명·이동 여부를 따로 확인했다 — `rule_lookup.py`·`judge_run.py`·
`verify_rules.py`·`rehearsal.py`·`agent_a2/a4/a5.py`·`stage2_cases/chunk/bm25/embed.py`·
`seed_rules.py`·`load_db.py`·`case_search.py` **전부 그 이름 그대로 `scripts/` 에 있다.**

**`_큐잔여9행.json`** — 위 ①-A 에서 DB 로 재확인: 재도전 9행 verified 0/9, 잔여 그대로.

**결론: 17개 중 죽은 것 0개.**

---

## ③ `docs/5·6·8` 9개 — 원본 대조로 찾은 빠진 사실

베이스라인(`git show baseline-0902-정리전:<파일>`)과 대조. **산문 재현 여부는 안 봤다 —
수치·조번호·식별자만.**

| 원본 | 빠진 것 | 내 파일 |
|---|---|---|
| `rule_base.md` §5 | 표 추출 실측 **22문서 578표 → 병합 469, 실패 0**(2026-08-31) | 5-1 (없음) |
| `rule_base.md` §7 | **우선규범** 표 — 모두의창업="「중소기업창업 지원사업 운영요령」", 초격차="요령 및 지침(규범명 없음)" | 5-1/5-2 (없음) |
| `Agent.md` §1 | 단계별 개별 지연 예산 — `<10ms`·`<20ms`·`~150ms`·`<200ms`·`<50ms`·`<30ms` 를 두 묶음(`<200ms`·`<300ms`)으로 합쳐 개별 수치가 안 보인다 | 6-1 (뭉갬) |
| `Agent.md` §0-a | 강등코드 실측 — **18종 중 3종 발화**: `PREMISE_UNMAPPED 39` · `CITE_HANG_MISMATCH 12` · `DANGLING_WARN 1` · 코드0개 27 | 6-2 (없음) |
| `LLM.md` §7 | 미결 #3 **"투표 N·각종 임계치"**(정답셋 §11 임계치 대장) | 6-3 (없음) |
| `LLM.md` §7 | 미결 #6 "자체 GPU(실서비스 F축 슬롯) — 마감 후" | 6-3/8-3 어디에도 없음 |
| `LLM.md` §3-5 | "(1) 정규화 출력" JSON 예시(`{품목,금액,금액_추정여부,용도,비목후보,누락필드}`) | 6-1/6-2 (없음, (4)-b 출력 예시만 있음) |
| `확인_가이드.md` §3-2 | 검색 실패 진단표(같은비목다른조항→청킹경계 / 다른비목→임베딩열세 / 정확용어못찾음→BM25문제 / 구버전등→필터버그) | 8-2 (없음) |
| `확인_가이드.md` §3-2b | 회귀 기준선 수치 **hit@5 52.9%** — "기준선"이라고만 쓰고 숫자를 뺐다 | 8-2 (숫자 누락) |
| `확인_가이드.md` §3-3 | 코퍼스버전 식별자 **`r28701`→`r44855`**(비교 불가 근거) | 8-2/6-3 어디에도 없음 |
| `GPU Guideline.md` §6 | 비용표 — A100 SXM 정답셋 1회 **$4 내외**(2~3시간) · 볼륨 **$0.07/GB·월**(100GB=월$7) · HIGH_PERFORMANCE 티어 존재 | 8-3 (없음) |
| `프로젝트설명.md` §3-4 | 재매칭 예시 문장("귀 기관 규정은 제33조라 하지만 현행 기준 제39조입니다") · B등급 "R&D 사례 참고" 라벨 문구 | 6-2 (원칙만, 예시 없음) |

**대체로 스코프 판단이었지 실수는 아니었던 것**: `rule_base.md` §4 값표(→ 부록 예정)·
`db/README.md` 표32개+실측량(→ 부록 `DB_운영.md` 예정)·`Agent.md` §4·6·7·8·9(전제해소·
재현성·판단불가경로·실패경로·비동기에이전트 — 애초에 6-1/6-2 스코프 밖으로 잡았다.
`docs/2_구조/2-2_판정_흐름.md` 가 이미 다뤘을 가능성이 높은데 확인은 안 했다)는 의도적
분리라 여기 올리지 않았다.

**고치지 않았다** — 오너 판단이 지나면 그때 반영한다.
