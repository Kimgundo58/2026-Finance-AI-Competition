# 레인 P3 — 프롬프트 크기 / B3 부속표 행 단위 항분해

> 2026-09-03 · 워크트리 `금융 AI공모전-agent` · 브랜치 `agent-perf-2026-09-03` · 세션 ai-1e · 중앙 ai-e8.
> 기록이라 100줄 면제. **소급 수정하지 않는다** — 낡으면 머리에 배너를 단다.

## 0. 🔴 레인 목표가 바뀌었다 (ai-e8 승인 2026-09-03)

들어올 때의 목표는 「프롬프트가 커서 잘린다 → 크기를 줄인다」였다. **첫 측정이 그 가설을 기각했다.**
93문항 dry 에서 추정 토큰 40,960 초과 **0건**, 30,000 초과 **1건**이다. 크기는 지금 위험이 아니라 낭비다.

→ 새 목표: **거대 블록 하나가 S번호 하나로 붙어 인용이 조준되지 않는 것을 푼다.**
   B3 최대 조각 11,641자, 창업중심대학 참고3 10,515자가 통째로 S번호 1개다. LLM 이 그 표의
   어느 행을 근거로 삼았는지 말할 방법이 없고, 우리도 그것을 채점할 방법이 없다.

## 1. 측정 1 — 93문항 dry 분해 (완료)

**조건** — 이 조건이 다른 run 과는 빼지 않는다.

| | |
|---|---|
| 하네스 | `scratchpad/P3_프롬프트_분해.py` (읽기 전용 · DB 무쓰기) |
| 문항 | `eval_store.평가대상()` 채점 93문항 (범위밖 제외) · dry · top_k=5 |
| 코퍼스버전 | `c20525-e20525-r44865-d283-107daa68` — 출처 **`eval_store.코퍼스버전()`** |
| 소요 | 129초 · 오류 0 · 원본 `scratchpad/P3_분해_전.json` |

🔴 스탬프 꼴이 두 벌이라 출처를 같이 적는다. 판정 쪽 `orchestrate.코퍼스버전()` 은 `docs245/chunks20525` 꼴이고
서로 비교가 안 된다 ([[../9-3_실측에서_나온_것]]). **같은 꼴끼리만 대조한다.**

### 관측 (자수 = 파이썬 문자 수)

```
구간        n   총평균   총최대   B3평균  B3최대  표평균 표최대  표자수평균  S평균 S최대
전체       93  17,113  44,926  10,310  36,917   1.1    4      7,085    45  120
공통       26  28,585  44,926  21,185  36,917   1.8    4     15,298    61  103
사업지정   67  12,661  26,840   6,089  18,482   0.8    3      3,898    39  120
본세트     34  25,427  44,926  18,182  36,917   1.6    4     12,868
보강       28  11,843  26,840   5,598  18,482   0.7    3      4,332
적대적     24  12,714  24,460   5,891  18,282   0.8    2      3,457
공식        7  12,895  16,733   6,070   9,869   0.6    1      2,453
```

- 표 ≥1 문항: 공통 24/26(92%) · 사업지정 41/67(61%)
- 표 ≥2 문항: 공통 19/26(73%) · 사업지정 **8/67(12%)**
- B3/총: 공통 74% · 사업지정 48% — 표자수/총: 공통 54% · 사업지정 31%
- 추정 토큰(자수×0.7) 40,960 초과 **0건** · 30,000 초과 **1건**(gold_id=377 · 44,926자 ≈ 31,448)
- 표 인스턴스 98건 / **고유 16종**. 상위 4종이 79건(81%):
  예비창업 붙임2(27·4,398자) · 창업중심대학 참고3(23·10,515) · 초격차 참고2(16·9,771) · 초기창업 붙임2(13·4,293)

### 추론 — 위 관측에서 나온 것 (관측과 갈라 쓴다)

1. **[[../9-3_실측에서_나온_것]] 의 미측정 항목에 답이 나왔다.** 「사업 지정 문항에서도 같은 패턴인가」 →
   **표는 걸린다(61%)지만 «여러 개 동시»는 공통 특유다**(73% vs 12%). gold_id=377 의 「표 4개 동시」는
   대표값이 아니라 상단이다. 그래도 사업지정에서도 프롬프트의 31% 가 표다.
2. 고유 표가 16종뿐이고 상위 4종이 81% 라 **대표화(DEDUP)의 사정권이 넓다.**
3. 크기 가설 기각은 위 §0.

## 2. 부속표는 갈래가 둘이다 — 별표는 정상 (ai-e8 질문에 대한 답)

`+---` 가 26,304행에서 0건인 것은 **변환이 안 도는 게 아니라 검사 문자열이 틀린 것**이었다(내 실수, 정정).

| 갈래 | 출처 | 실물 | P3 사정권 |
|---|---|---|---|
| **별표** | 법령 XML(L1) | 유니코드 괘선 박스표. `L1_공무원여비규정 별표4` 표본에 `│`88 `─`216 `┼`24 | 12/98 인스턴스뿐. **파싱 정상 — 안 건드린다** |
| **붙임·참고** | 조달분 HWP→한컴 1회 수동변환 PDF(L2 세부관리기준) | 괘선 0. 열이 줄 단위로 섞인 PDF 흐름 텍스트 | 79/98(81%) — **여기가 병목** |

흐름 텍스트 실물(창업중심대학 참고3): 열이 `재료비 재료비 재료비` 처럼 한 줄에 붙고, 좌측 머리글이
`기\n준\n` 으로 세로로 쪼개지며, 페이지 꼬리 `- 23 -` 와 머리글 `비 목 주요 내용` 이 반복된다.

`│` 3,217행의 정체도 갈렸다: **표 구분자가 맞다.** 125문서에 │ 644,986개인데 한 문서에 수만 개씩 몰려 있다
(지방세기본법시행규칙 445행·100,113개 · 소득세법시행규칙 206행·68,497 등). 다만 시행규칙 별지·별표라
판정 B3 에는 거의 안 걸린다 → **앵커 후보로는 별표 갈래에만 유효하다.**

## 3. 항호 문법 — 「안 벗어나는 안」 채택 (ai-e8 승인)

**행 분해는 프롬프트 텍스트에서만 하고 s맵의 항호는 article 갈래 그대로 `None` 을 유지한다.**
`llm_validate`(P1) · `eval_e2e`(P4) · `server/main.py` 변경 0줄. 코드에서 확인한 근거 셋:

1. `_항_추출(본문, None)` → `(본문, "조전체")`. 이 표들이 **이미** 그 경로다 → 인용 원문 치환이 현행과 바이트 동일
2. `CITE_HANG_MISMATCH` 는 `항호_DB and 항호` 둘 다 참일 때만 깎는데 `s번호_메타` 가 article 을 `항호_DB=None`
   으로 세운다(`llm_validate.py:150`) → 행이 늘어도 새로 울릴 수 없다
3. 등급·판정 로직에 인용 **개수** 기반 규칙이 없다(전부 any/all) → 같은 표의 여러 행을 인용해도 부작용 없음

ai-2c(P1) 확인(2026-09-03): 오늘 `_항_추출`·`s번호_메타` 를 한 줄도 안 고쳤으므로 근거 1·2 는 유효하다.
🔴 ai-2c 가 보탠 검사 항목 하나 — `신뢰등급` 은 `any(extraction=='vlm')` 이라 **개수는 무관하지만
「vlm 문서가 인용에 처음 들어오는가」는 바뀔 수 있다.** 같은 조를 쪼개기만 하면 문서 집합이 안 변해 무해하나,
**전/후 인용 doc 집합이 같은지 ROWSPLIT 구현 뒤에 확인해야 한다** (아직 미확인 · §5 에 포함).

이 제약은 **양쪽 기록에 대칭으로 박혀 있다** — [[_레인_P1]] §3-b 「내 파일에 걸린 외부 제약」에 안 A 의 근거
1·2 와 vlm 단서가 적혀 있고, 다음 P1 세션이 `_항_추출`·`s번호_메타` 를 고치기 **전에 P3 에 알린다**가 명시돼 있다.
한쪽에만 있으면 다음 사람이 못 찾는다(ai-2c 제안, 2026-09-03).

🔴 **버린 중간안이 이 결정의 핵심이다.** 행 시작 문자열을 지금 문법 그대로 항호에 넣으면 `본문.find()` 는
맞게 서지만 끝을 `_항마커`(①~⑳)로만 잡는데 표엔 그 마커가 없다 → **끝이 조문 끝까지 흘러서 범위를 「항」이라
적으면서 실제로는 여러 행을 담는다.** 조용히 절반만 성공하는 경로라 배제했다.
문법을 넓히는 안(안 B)은 인용 원문이 달라져 run 191 과의 기준선 비교가 끊긴다 — 이득을 수로 보일 때만 연다.

## 4. 코드 변경 — 블록 경계 정의 한 곳 (완료 · 부작용 없음)

`scripts/assemble_context.py` 에 순수 함수 3개를 추가했다. **`조립()` 은 한 글자도 안 바뀌었다.**
P3 의 측정표와 P4 의 `eval.runs` 가 같은 숫자를 쓰게 하려는 것이다(ai-e8 지시: 정의가 둘이면 안 맞는다).

```
블록분해(프롬프트) -> {블록: 원문}   블록자수(...) -> {블록: 문자수}   블록해시(...) -> {블록: sha1 12자}
```

경계 규칙 = 줄머리 `## B<숫자>.` 매치 시작 위치 · B0 은 첫 매치 앞 전부 · 사이의 `\n\n` 은 **앞 블록**에 든다 ·
같은 이름 두 번이면 합산(V3 이 B4 를 앞으로 옮긴다) · 자수는 문자 수(바이트도 토큰도 아니다) ·
🔴 값들의 합 == `len(프롬프트)` 가 항상 성립한다 → **부르는 쪽에서 검산으로 쓴다.**
정의를 ai-e6(P4)에 보냈고, 내 하네스도 이 함수를 부르도록 바꿔 재실행해 **같은 표가 나오는 것을 확인**했다.

프롬프트를 잡는 자리: orchestrate 가 `from assemble_context import 조립` 로 이름을 들여왔으므로
**`orchestrate.조립` 을 감싸야** 잡힌다. `assemble_context.조립` 을 갈면 안 잡힌다.

## 5. 다음 한 걸음 (다음 세션이 여기서 이어받는다)

1. **ROWSPLIT 앵커 구현** — `SUDDOE_B3_ROWSPLIT=1` (기본 off). 앵커 후보 넷:
   ① 반복 머리글 `비 목 주요 내용` ② 페이지 꼬리 `- N -` ③ 비목 어휘 사전 31종 ④ `※`·`-` 항목 머리.
   🔴 **넷으로 못 자르면 자르지 않고 통째 1항으로 둔다**(ai-e8 강한 승인: 「반쯤 자른 표는 안 자른 표보다 나쁘다」).
   별표 갈래는 괘선(`┠─`)이 있어 훨씬 쉽지만 12/98뿐이라 우선순위가 아니다.
2. **DEDUP** — `SUDDOE_B3_DEDUP=1` (기본 off). 같은 비목 해설표류 다중 적중 시 질문 사업 우선, 없으면 RRF 상위 1개.
   🔴 **가드를 바꿨다**(ai-e8 승인): `eval_e2e` 의 근거적중은 **top5 청크**만 보므로 B3 를 잘라도 안 움직인다 —
   가드로 못 쓴다. 대신 「정답 근거 좌표가 B3 폐포에 남아 있나」를 하네스에서 따로 센다.
3. **플래그 off 에서 93문항 조립 출력이 바이트 단위로 현행과 같음을 증명** (생략 불가 · `블록해시` 로 잰다).
   같은 자리에서 **ROWSPLIT on 의 전/후 인용 doc 집합이 같은지**도 센다 — 집합이 늘면 `VLM_DOWNGRADE` 가
   새로 울릴 수 있다(ai-2c 지적, §3).
4. 전/후 표 + `docs/9-3` 프롬프트 병목 절 갱신안. 판정 효과는 P4 가 잰다.

## 6. 안 닫힌 물음

- **행 분해가 판정을 좋게 하는지는 아직 «가정»이다.** 이 레인이 낸 것은 전부 프롬프트 쪽 실측이고,
  「S번호가 조준되면 인용적중·일치율이 오른다」는 P4 의 GPU run 전에는 증거가 없다. 순증 b−c 로 재야 한다.
- 붙임2 를 검색 진입점에서 빼는 것과의 충돌(hit@5 55.4→60.8)은 **여전히 아무도 안 정했다.** P3 은 그 충돌을
  건드리지 않고 크기만 다뤘다 — 진입점은 그대로다.
- 표 고유 16종 중 상위 4종 밖 12종을 아직 눈으로 안 봤다. 앵커가 그쪽에서도 서는지는 미확인.
- 별표(괘선 갈래)에 행 분해를 넣을 값이 있는지 — 12/98 이라 지금은 안 넣기로 했을 뿐, 재지는 않았다.

## 7. 표 행분해 구현 — 합격 기준 4개 전부 섰다 (2026-09-03 · ai-e8 승인)

플래그 `SUDDOE_ROWSPLIT` (기본 off). 🔴 이름에 B3 을 안 박았다 — `원문블록()` 을 지나는
**B1·B2·B3 전부**에 걸린다. 표가 B3 에 몰려 있을 뿐이다(ai-e8 결정).
별칭 `SUDDOE_B3_ROWSPLIT` 은 P레인 프롬프트 표기라 **2026-09-04 에 지운다.**

### 관측 (조건: `scratchpad/P3_행분해_검증.py` · 채점 93문항 · dry · top_k=5 · 85초 ·
### 코퍼스버전 `c20525-e20525-r44865-d283-107daa68` · 출처 `eval_store.코퍼스버전()`)

| 합격 기준 | 값 |
|---|---|
| ① off 에서 기존과 동일 | **93/93** — 문자열·s맵 둘 다 |
| ② on 최대 S 원문자수 | **2,612** (off 12,121) |
| ③ S마커수 == len(s맵) | **93/93** |
| ④ dry 완주 | **93/93 · 예외 0** |
| 비용 — 프롬프트 자수 | +중앙 570 · 최대 +2,430 |
| 비용 — S번호 | +중앙 10 · 최대 +40 · **최대 s맵 120 → 120** |
| 인용 후보 `{(종류,id)}` 집합 | **93/93 동일** |

🔴 ①의 닻은 자수가 아니다. `git show HEAD:scripts/assemble_context.py` 를 별도 모듈로 올려
**같은 인자로 조립을 두 번 태워** 문자열을 대조했다 — 내 수정 «전» 코드가 곧 기준선이다.
두 번 돌린 사이에 `orchestrate.py` 가 두 번 바뀌었고(`051b5fd`·`67ef75f`) off 최대마디가
12,126 → 12,121 로 달랐다. ①은 **같은 run 안의 대조**라 영향을 안 받는다.

표 16종: 못 쪼갠 것 2 (970자·679자 — `_표_최소=1200` 미만이라 애초에 손 안 댐), 3,000 초과 0.
별지9 11,573 → 최대 1,436 · 창업중심대학 참고3 10,515 → 1,635 · 예비창업 붙임2 4,398 → 1,216.

코퍼스 전체 26,304 조문 중 분해가 달라진 것 3,208(12.2%) — 부속표 2,976 · 그 밖 232.
232 중 표본 5건을 실제로 열었다: 소득세법시행령 제155조·제167조의3, 부가세법시행령 제101조,
팁스 「12. 협 약」, 계약예규 별첨4 — **전부 조문 안에 괘선표가 들어 있거나 쪽바닥이 찍힌 것**이었다.

### 추론 — 위 관측에서 나온 것 (관측과 갈라 쓴다)

- 게이트를 **이름표가 아니라 내용**으로 건 것이 232건의 성격을 정한다고 본다. 문서명이
  「붙임1. 2026년 팁스TIPS 총괄 운영지침…」인 문서가 실재해서, `머리`(=doc_id 조번호)로 걸면
  그 문서의 평범한 조문이 전부 표로 잡힌다. 그래서 조각 «안»의 표 자취만 본다.
- 「닻이 하나도 안 걸리면 안 자른다」가 기본값이라, 못 잡는 표는 **현행과 같아질 뿐** 나빠지지 않는다.
- 🔴 **인용 후보 집합 93/93 동일은 「신뢰등급이 안 바뀐다」의 증거가 아니다**(ai-2c 지적).
  `신뢰등급` 은 s맵이 아니라 **LLM 이 실제로 인용한 목록**에서 `any(extraction=='vlm')` 로 잰다.
  s맵 id 집합이 같아도 LLM 이 다른 행을 고르면 인용 doc 집합이 달라져 A→B 강등이 새로 울릴 수
  있다. 내 검사는 「사고가 났나」를 잡을 뿐이고, 그 물음은 **P4 의 GPU run 전/후로만 닫힌다.**

### 🔴 이 작업의 «이유» 가 바뀌었다 (ai-e8, 2026-09-03)

  옛 이유 — LLM 이 표의 특정 행을 가리키게 한다 → **무너졌다.**
  P1 실측: s맵에 있는데 딴 걸 골랐다 **0/9** · 정답 조가 s맵에 아예 없었다 **7/9**.
  「조준 실패」가 아니라 **「검색 미회수」**였다. 인용 조준을 고쳐 얻을 최대치는 +1 이다.
  새 이유 — **행 단위로 «고를» 수 있게 하는 전제**다. 쪼개 놓지 않으면 뺄 수도 없다.

## 8. 「질문과 무관한 행을 뺀다」 — 설계만 남기고 보류 (ai-e8 지시 · 2026-09-03)

### 관측 (조건: `scratchpad/P3_행제거_예상.py` · 93문항 · dry · top_k=5 · `SUDDOE_ROWSPLIT=1` ·
### 코퍼스버전 `c20525-e20525-r44865-d283-107daa68`)

| 물음 | 값 |
|---|---|
| 표가 뜬 문항 | 65/93 |
| 표 행 총수 / 비목 라벨이 붙은 행 | 1,280 / **211 (16%)** |
| 라벨 붙은 자수 / 라벨 없는 자수 | 113,167 / **602,920 (84%)** |
| 정규화 비목이 채워진 문항 | **23/93** |
| 라벨 ≠ 질문비목 인 행(= 뺄 수 있는 최대치) | 평균 265자 · 최대 5,288자 · **표자수 대비 2%** |
| 🔴 정답 근거 문장이 표 «행»에서 발견된 문항 | **1** (그중 빼는 쪽 0) |

※ 첫 시도는 `정규화["비목"]` 을 읽어 0/93 이 나왔다. 실제 키는 `비목후보[{비목,신뢰도}]` 다
(`normalize_run.호출자리1_스키마`). **내 필드 이름이 틀린 것**이었고 고쳐 다시 쟀다.
원소가 dict 인 run 과 str 인 run 이 둘 다 있다 — 둘 다 받게 했다.

### 추론 — 왜 보류인가 (관측과 갈라 쓴다)

- 「무엇을 근거로 뺀다고 말할 수 있나」에 지금 답이 없다고 본다. 뺄 근거가 되는 라벨은 행의
  16% 에만 있고, 질문 쪽 비목은 4문항 중 1문항만 채워지며, 둘이 다 맞아떨어져도 **2%** 준다.
- 🔴 더 결정적인 것은 ④다. **행 단위 정답이 1건뿐이라 「잘못 뺐다」를 잴 자가 없다.**
  `eval_e2e._인용좌표`·`eval.golden_chunks` 는 (doc_id, 조번호) 라 행을 못 가른다.
  되살리려면 **행 단위 정답셋이 먼저** 있어야 한다 — 그게 이 설계의 선행조건이다.
- 컨텍스트 쪽 근거도 약해졌다: run 191 서버는 **40,960 으로 떠 있었고**(P4 기록 반박),
  실측 자/토큰 중앙 1.366 에서 **40,960 초과 0/95**. 안 터지는 걸 미리 고치는 일이 된다.

## 9. 갈래 재귀의 S번호 — 확인 결과 (읽기만 함 · 2026-09-03)

### 관측

- `orchestrate.py:564` 갈래 재귀는 `판정()` 을 2번 부르고 `조립()` 은 호출마다 `n=0` 에서
  다시 매긴다 — **두 갈래가 같은 S01 을 서로 다른 조문에 붙이는 건 사실이다.**
- 인용은 각 갈래 «안에서» `llm_validate.검증()` 이 s맵으로 풀어 `인용목록=[{s번호, doc_id,
  조번호, …}]` 로 내보낸다. **좌표가 나가므로 S번호 충돌이 표시를 깨뜨리진 않는다.**
- `응답 = dict(결과[0])` 이라 최상위 인용목록·s맵은 **갈래0의 것**이다.
- `_마무리` 는 `tenant.decisions` 에 최상위 한 행만 넣는다(`갈래` 칼럼 없음) —
  **갈래1의 인용은 DB에 안 남는다.**
- `갈래` 를 읽는 코드가 **0곳**이다. `server/`·`eval_e2e.py`·`llm_validate.py` 전부 없고
  `_실_판정`(server/main.py:921) 반환 화이트리스트에도 없다.

### 🔴 곁가지로 밟은 것 — 실경로에서 `인용` 이 항상 빈 배열이다

`llm_validate.검증()` 을 **실제로 태워** 반환 키를 봤다. `인용` 키가 없다:
`['강등사유','강등코드','미매핑전제','버전스탬프','신뢰등급','요약','인용목록','전제목록',
'참조사슬','판정','해야할일']`. `최종응답.to_dict()` 가 `asdict()` 라 필드명 그대로 `인용목록`
이고, `orchestrate` 도 `인용` 키를 안 만든다. 그런데 `server/main.py:925` 는
`"인용": r.get("인용", [])`, `:658` 은 `yield _sse("인용", out.get("인용", []))` 다 —
**바로 옆 `전제` 에는 `r.get("전제") or r.get("전제목록")` 폴백이 있는데 `인용` 에만 없다.**
`SUDDOE_MOCK=1` 경로(main.py:227·256·275)는 `"인용"` 을 직접 채운다.

추론: **목이 실 경로를 가린 자리**라고 본다 — 검사할 것은 통과 여부가 아니라 목/실경로의
키집합이 같은가다. 🔴 `server/main.py` 는 내 소유가 아니라 **한 글자도 안 고쳤다.**
ai-e8 에 넘겼고 백엔드가 볼 일이다. 계약(`프론트 연동 사양.md` §8)의 원소 모양이
`{조번호,조제목,원문,doc_id}` 인데 `인용목록` 원소와 키가 같은지는 **내가 안 쟀다.**

## 10. 컨텍스트 — P4 토크나이저 실측으로 닫았다 (2026-09-03)

### 관측 (P4 ai-e6 · dry 93문항 · top_k=5 · `8c8098f` · 실제 Qwen3-32B-AWQ 토크나이저 ·
### 조립 95회 — 게이트C 재귀 2회 포함)

| | off | on |
|---|---|---|
| 40,960 초과 문항 | **0/95** | **0/95** |
| 32,768 초과 문항 | 1/95 | **3/95** |
| 최장 프롬프트 | 44,926자 = 33,139토큰 | 47,356자 = 35,032토큰 |
| 최대 s맵 | 120 | **120** (안 늘었다) |

입력 예산 40,960 − 3,000(출력) = 37,960토큰 · 남는 여유 **2,928토큰**.
🔴 최장 프롬프트 문항과 최대 증가 문항이 **같은 문항이다 — gold_id 377.**
자수 증가 상위 5: 377(+2,430) · 366(+2,164) · 370(+1,832) · 365(+1,780) · 355(+1,763).
블록별 증가는 **B3 +41,477 · B2 +9,040 · B6 +5** (전체 +50,522 = +3.2%) —
`원문블록()` 을 지나는 블록 전부에 걸린다는 것이 P4 의 독립 분모에서도 보인다.

### 추론

- **여유가 넉넉해서 안전한 게 아니라 한도가 40,960 이라서 안전하다.** 증가가 가장 큰 문항이
  하필 가장 긴 문항이라, 여유는 계산으로 넉넉해 보여도 실제로는 한 문항이 다 쓴다.
- 🔴 **그래서 이 기능의 선행조건은 「40,960 으로 뜬다」이다.** 32,768 로 내려가면
  off 1 → on 3 으로 **내 변경이 먼저 깨진다.** `scripts/pod_serve.sh:38` 은
  `MAX_MODEL_LEN="${MAX_MODEL_LEN:-40960}"` 로 고쳐져 있으나,
  `docs/8_운영/8-5_배포_레시피.md:74` 는 아직 **32768**, `8-3_GPU.md:79` 는 **20000** 이다.
  문서대로 띄우면 깨진다. 🔴 두 파일 다 내 소유가 아니라 **안 고쳤다** — ai-e8 에 넘겼다.
