# 레인 W4 기록

담당: W4 (Sonnet) · 중앙: ai-ae

## 1차 — scripts/_work/ 아카이브 이관 (2026-09-02)

- 실측: `scripts/_work/` 94개 (오너 지시 97개는 오차 — 실제 카운트로 진행)
- 방식: `mv` (git mv 아님 — 동시 세션 6개, git 인덱스 잠금 충돌 회피. git add -A 는 ai-ae 가 페이즈
  종료 시 일괄)
- 목적지: `archive/_work_2026-09-02/` 평평하게(원본 구조 재현 안 함)

### 분류

| 구분 | 개수 | 처리 |
|---|---:|---|
| A. 라이브 스크립트 기본 입출력 경로 | 16 | 유지 |
| B-유지. 이름상 미완료(`_큐잔여9행.json`) | 1 | 유지 |
| B-이관. 백업(.bak) 7 + `emb.npy`(경로 미특정) | 8 | 이관 |
| C. 1회용 조사·세션기록, 코드참조 0 | 69 | 이관 |
| **합계** | **94** | 유지 17 · 이관 77 |

### A — 유지 16개 (경로·역할)

| 파일 | 역할 |
|---|---|
| `_A2_엄격조항.md` | agent_a2.py write target |
| `_A4_개정보고.md` | agent_a4.py write target |
| `_A5_기관답변_픽스처.json` | agent_a5.py `--in` 기본 read 입력 |
| `_B_골든셋_룰커버.json` | rule_lookup.py write target |
| `_B_금지매칭_감사.json` | rule_lookup.py write target + 코드주석 근거자료 |
| `_B_비목확정_임계교정.json` | rule_lookup.py write target |
| `_F_오염검사.json` | case_search.py read, stage2_cases.py write+게이트(없으면 exit) |
| `_G_재도전2026_적재.py` | seed_rules.py 가 재도전 9행 적재 도구로 참조 |
| `_재도전_2026_판독.json` | 위 스크립트 입력데이터 |
| `_judge_result.jsonl` | judge_run.py `--out` 기본값 |
| `_rules_검증.json` | verify_rules.py 사용예 기본 출력 |
| `_stage2_cases.jsonl` / `.npy` | stage2_cases.py write target |
| `_stage2_chunks.jsonl` | stage2_chunk.py write / stage2_bm25.py·stage2_embed.py read |
| `_골든셋_재적재.py` | load_db.py 가 골든셋 단독 갱신 도구로 참조 |
| `_리허설_결과.json` | rehearsal.py `--out` 기본값 |

### B-이관 8개 (백업·경로미특정)

`.bak` 7개(`_build_refs`·`_load_db`·`_seed_rules`×2·`_stage2_chunk`·`_stage2_chunks.jsonl`·
`_골든셋_확정본.json`) — 코드참조 0, git 이 롤백 담당. `emb.npy` — 코드참조 0, stage2_embed
재실행 시 재생성됨.

### C 69개

독립 1회용 스크립트 17 · 작업기록 .md 9 · 세션재개 기록(2026-09-01 완료 8/8) 4 ·
실험 전/후 스냅샷 9 · E2E/드라이런/스모크 로그 8 · 검증·감사 산출물 6 ·
`_0901` 날짜접미 검수 산출물 6 · 기타 데이터 스냅샷 10 (붙임2·예비창업/초기창업 txt 포함)

### 자가검증

| 항목 | 결과 |
|---|---|
| `python -m compileall scripts/` | 통과 (exit 0) |
| 이관 77개명 `scripts/ server/ db/ tests/` grep | 참조 3건 발견 → 전부 주석/docstring/일반 CLI 기본값
  (`_G_룰검수요청.md`·`_증빙_별칭후보.json` : seed_rules.py 서술형 주석, `emb.npy` : stage2_embed.py
  `--out` 기본값이 bare 파일명이라 실경로 미고정) — 실제 파일시스템 read/write 아님, 이관해도 무해 |
| 개수 정합 | 유지 17 + 이관 77 = 94 ✓ |

## 버린 것 (원본 삭제 없음 — 전부 `archive/_work_2026-09-02/`에 있음)

이관된 77개 전체 파일명은 위 A/C 표 참조. 삭제된 파일 없음, 전량 이동.
