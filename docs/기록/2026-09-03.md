# 2026-09-03

배포 세션. 6세션 병렬 (중앙 ai-43 · S1 배포 패키징 · S2 인증·격리 · S3 판정 경로 · S4 GPU 워치독 ·
S5 문서 정합성). 브랜치 `cleanup/docs-py-2026-09-02`.

---

## 한 장 요약

| | 전 | 후 |
|---|---|---|
| API 실행 위치 | 로컬 uvicorn | **Cloud Run `suddoe-api`** · asia-northeast3 |
| DB | 로컬 PG | **Cloud SQL `suddoe-db`** · 덤프 321MB 적재 오류 0 · 12개 표 행수 일치 |
| 컨테이너 | 없음 | `Dockerfile` · `requirements-api/gpu.txt` · `.dockerignore` · `.env.example` |
| 인증 | `?org_id=` 자기신고 하나 | Supabase JWT + 데모 토큰 + `OrgId주입` 미들웨어 (실서버 미배선) |
| GPU 과금 | 수동 stop | 유휴 30분 자동 정지 `server/gpu_watchdog.py` |
| 실서버 판정 화면 7 | 근거 빈 배열 | 채워짐 |

---

## 🔴 오늘 제일 큰 것 — 실서버 판정 전수가 근거 없이 나갔다

`server/main.py:_실_판정` 이 오케 반환키 `인용목록` 을 `인용` 으로 읽었다. 실서버 판정 **전수**가
화면 7(근거) 무지로 나갔다. `scripts/rehearsal.py:140` 에 같은 결함. 둘 다 중앙이 고쳤다.

🔴 **목 응답에 `인용` 이 직접 박혀 있어 테스트 241건이 전부 통과했다.**
목이 실 경로와 다른 키를 쓰면 테스트는 실 경로를 안 본다. 목 분기의 하드코딩 `"인용"` 은
`main.py:230·259·278·293` 에 지금도 그대로 있다 — 그 자리가 원인이 아니라 가림막이었다.

지금 트리 상태 (방금 실행):
```bash
$ grep -n "인용목록" server/main.py scripts/rehearsal.py
server/main.py:978:        "인용": r.get("인용") or r.get("인용목록") or [],
scripts/rehearsal.py:142:            "인용": [c.get("조번호") for c in (r.get("인용") or r.get("인용목록") or [])
```

## 인프라 — 중앙 실측

- Cloud SQL `suddoe-db` 신설 (asia-northeast3 · `--edition=enterprise`). 로컬 덤프 321MB 적재
  오류 0 · 12개 표 행수 일치. 🔴 **eval 스키마는 안 올렸다** (인덱스 투입 금지 규칙과 별개로 미적재)
- Cloud Run `suddoe-api` 배포 → `https://suddoe-api-1081277785480.asia-northeast3.run.app`
- `/api/health` 200 `"모드":"live"` 0.27초 · `/api/orgs` 413건 · CORS PATCH 200 + ACAO

## 룰 — 중앙 실측

`corpus.rules` 검수 74/74 · 미검수 0 · 강등 0/80조합. 480 재도전 재료비 정정 ·
421/424/478 verified · 489/490 신규(모두의창업 인건비·교육훈련비) · `corpus.chunks.적용대상` 2행 정정.

## 세션별 산출 (각 보고서에서 실측이 붙은 것만)

| | 한 일 | 실측 |
|---|---|---|
| **S1** 배포 패키징 | 이미지 6파일 신설 · 모델을 빌드 단계에서 굽는다(런타임 다운로드 0) | 콜드 빌드 339초 · CONTENT 2.45GB · 컨테이너 안 3.7GiB · 임베딩 peak RSS 2.4GB |
| **S2** 인증·격리 | `server/auth.py`·`routes_orgs.py` 신설 · 데모 세션 토큰 · `slug()` HMAC | 공격 55건 실행 / 통과 55 · 실패 0 · `/api/orgs` 413행 UUID 노출 0건 |
| **S3** 판정 경로 | `_실_판정` 에 `_비목고정` 전달 · 후보 밖 비목 경고 · 폼 `비목후보` LLM 1회 | SSE 이벤트열 목·실 양쪽 계약과 일치(빠짐 0·늘어남 0) · 정규화 11키/판정 8키 동일 |
| **S4** GPU 워치독 | `server/gpu_watchdog.py` 하나 · `/api/gpu/status·keepalive` · 중앙이 `bf2485f` 로 배선 | 가짜팟·가짜시계로 14 시나리오 발동 / 전부 통과 · 새 SSE 이벤트 이름 0개 |
| **S5** 문서 정합 | `2-4_배포와_인증.md` 전면 개정(Supabase 저장 → Cloud SQL, 인증만 Supabase) · 9_미결 #5·#12 처리 | DB 총 335MB(corpus 323·tenant 2.2·eval 1.4) — SSD 20GB 대비 60배 |

### 🔴 헌장 밖 배정 2건은 실행하지 않고 돌려보냈다

- S5 → D4 프론트 인증 (헌장 「docs/** 만·코드 수정 금지」)
- S1 → 시연 계정 시드 `db/init/`·`scripts/` 신규 (헌장 「server/·scripts/ 수정 금지」)

동료 세션은 다른 세션의 제약을 풀 수 없다. 둘 다 오너·중앙이 재배정할 자리다.

## 🔴 관측과 추론을 가른 자리

**관측**
- 이미지 안 모델 로드+첫 인코딩: **7.7~8.1초**(초기 이미지 n=3) / **14.8~20.8초**(최종 이미지 n=6).
  모델 블롭 해시 동일(`c18156e8…` · 2,271,064,456 B) · 순차읽기 3.6초(628MB/s)
- 임베딩만 돌린 프로세스 peak RSS 2.4GB. Cloud Run 배정은 8Gi(오너 결정 D3)
- 워치독 14 시나리오 전부 가짜팟에서 발동했다

**추론 — 아직 사실이 아니다**
- 워밍업 8초→15초대의 **원인은 못 박았다.** 호스트 상태가 후보지만 확인 못 했다.
  계획에는 느린 쪽(약 20초)을 쓴다
- 「타이머가 돈다」는 「비용이 준다」의 증거가 아니다. 절감은 청구서로만 확인된다
- 「빌드·기동 성공」은 「배포하면 판정이 돈다」의 증거가 아니다

---

## 오늘 드러난 함정 셋

| 무엇 | 어떻게 드러났나 | 어디에 박았나 |
|---|---|---|
| `server/auth.py:120 import jwt` 가 requirements 에 없었다 | 지연 import 라 기동은 되고 **첫 로그인**에서 죽는다. 컨테이너 안 `python -c "import jwt"` 로 확인 | `requirements-api.txt` 에 `PyJWT[crypto]==2.13.0` |
| `--index-url .../whl/cpu` 를 그대로 쓰면 빌드가 깨진다 | `pip index versions fastapi --index-url …` → `No matching distribution found`. 그 인덱스엔 fastapi 가 없다 | `--extra-index-url` + `torch==2.11.0+cpu` |
| `.gitignore` 의 `.env.*` 가 `.env.example` 을 삼켰다 | `git check-ignore -v .env.example` 이 규칙을 찍었다. 지금은 무출력 | 중앙이 예외 추가 |

## 안 끝난 것

- **W-RLS — 실서버는 아직 「읽기 전용」이다.** RLS 는 2026-09-02 기록대로 아무것도 막지 않는다
- GPU 팟 처분 미정 · 데모 계정 시드 미작성 · 프론트 PR #1 미머지
- RunPod REST 스키마 미검증(실물 stop→start 왕복 미실행) · Qwen3-32B-AWQ 로 vLLM 이 뜬 것을 본 적
  없다 · Cloud SQL 커넥터·SSE 타임아웃은 Cloud Run 에서 미검증
- `docs/8_운영/8-5_배포_레시피.md` **115줄** — 100줄 상한 초과. 분할은 중앙 판단 대기

## 커밋

`13ffdc3` · `7f05526` · `2b8b2e8` · `bf2485f`(워치독 배선)
