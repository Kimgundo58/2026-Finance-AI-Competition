# 레인 BE 기록

## 2026-09-02 (수) 야간 — 미결 3건

세션: BE(ai-c7) 중앙 · B1(ai-31) 워커 · 협의 ai-ae. 산출은 `docs/7_백엔드/`.

### 지시서 전제가 하나 낡아 있었다

지시서: 「`POST /api/guest` 가 계약에는 동결인데 서버에 없다」.
**실측**: 계약 v1.0.1 은 이미 §2-1 을 「미결 — 시도했다가 접었다」로 내렸고 §1 표의
행도 뺐다. 전용 xfail 테스트도 그때 지워졌다. 즉 **선택지 ②는 이전 세션이 이미 집행**했다.
남은 건 `tests/test_contract.py` 의 `계약_보류` 집합 잔재뿐 — 그것만 걷었다.

### 스키마를 바꿔 무는 길은 안 갔다 — 근거

읽지 않고 **태웠다** (`tenant.expense_plans` 실제 INSERT 후 롤백):

| `org_id` | 결과 |
|---|---|
| `guest_<hex>` | `InvalidTextRepresentation` (컬럼이 uuid) |
| `orgs` 없는 uuid | `ForeignKeyViolation` |
| `NULL` | OK |

**추론**: 남은 길은 게스트마다 `orgs` 행(브라우저 수만큼 증식) 또는 타입·FK 변경인데,
후자는 5개 테이블의 RLS(`org_isolation USING (org_id = tenant.current_org())`)와 묶인다.
→ **로그인 설계와 분리 불가.** 오늘 밤 스키마는 안 건드렸다.
제3안(`guest_key TEXT` 부축)은 `docs/7_백엔드/계약_정정_0902.md` §4 에 근거와 함께 올려뒀다.

### 고친 것

| 자리 | 전 | 후 |
|---|---|---|
| `routes_l3._실_상태` | 실 경로에서 **주인에게도 항상 404** | 5케이스 정상 |
| `routes_tasks` 쓰기 3경로 | `org_id` 를 아예 안 받음 | 9케이스 정상 |
| `persist.py:78` | `동기화()` 위치인자 2개 | `org_id` 동반 |

### 인수인계 기록을 한 곳 정정했다

`백엔드_동결상태_0901.md` 는 L3 고장을 「쿼리 자체가 매번 죽는다」로 적었다.
**실측하면 갈린다** — `org_id` 를 `uuid.UUID` 객체로 넘기면 **성공**하고, **str** 로
넘기면 죽는다. HTTP 쿼리 파라미터는 항상 str 이라 **결론(실서버 100% 404)은 같지만**,
원인을 「파라미터 타입 문맥」으로 좁혀야 고칠 자리가 보인다.

고칠 길이 둘이었다. 캐스트(`%s::text IS NULL`)도 실측으로 되살아나지만
**그 길은 게스트가 남의 기관 L3 를 읽는 구멍을 연다**(태워서 확인). 그래서
`_org조건()` 파이썬 분기로 갔다 — `l3_documents.org_id` 가 NOT NULL 이라 게스트는 0행이다.

### 숫자

- `SUDDOE_MOCK=0 pytest tests/test_tenant_isolation.py tests/test_contract.py -q`
  → **71 passed · 1 xfailed**
- xfail 4건 걷음 (L3 조회 1 + 쓰기 3). L3 skip 가드 2건이 실제로 돌기 시작했다
- 엔드포인트 **경로 18 · 메서드 20** 유지 (파라미터만 늘었다)

⚠️ 위는 **두 파일만** 돌린 값이다. 전체 기준선(134/2/6) 대조는 ai-ae 가 한다.

### 안 닫힌 것

- `test_저장_이벤트에_decision_id가_실제로_채워진다` — xfail 유지.
  🔴 이 줄은 처음에 「GPU 팟이 있어야 참이 된다」로 적었는데 **틀렸다.** 실제 원인은
  모듈 경계다 — 아래 「마감 · 안 닫은 것」에 실측 경위를 적었다. (이 줄은 그 시점의
  기록이라 지우지 않고 정정만 붙인다)
- `test_contract.py:180` `받은값` — 의도된 계약 xfail
- 게스트끼리 서로 보이는 결함 — 로그인 대기. 못박은 테스트는 그대로 둔다
- `PUT /api/profile` 쓰기 — 스텁 그대로. 오늘 범위 밖

### 덤으로 나온 것 — 빈 DB 재현 (B1)

「알려진 함정」 2건은 **지금 파일 기준으로 재현된다** (별도 DB `suddoe_verify` 로 실측,
3종 차집합 양방향 0). 대신 **새 함정**이 하나 드러났다 —
`04_agent.sql:14` 의 `\set ON_ERROR_STOP on` 은 psql 전용이라 psycopg 로 돌리면
04 가 통째로 안 먹고, 그 상태로 대조하면 「스키마가 안 맞는다」는 잘못된 결론이 난다.
실 배포 경로(psql)는 정상이라 **파일은 안 고쳤다.** 상세는 `docs/7_백엔드/DB_재현_0902.md`.

### 프론트 7벌 요약 (2,729줄 → 168줄)

산출: `docs/7_백엔드/프론트_인계_0902.md`(94) · `프론트_화면별_지원표.md`(74).
원자료 추출은 B1(ai-31)에 맡기되 **판단·종합을 금지**했다 — 뒤집힌 결론이 섞인 문서
뭉치에서 워커가 합치면 폐기된 결론이 되살아난다.

**요약하다 나온 것 — 이게 요약보다 값이 크다:**

1. **화면 번호 체계가 셋이다** (A 3·4·5·6·7·9 결번 있음 / B 1~14 정본 / C P0~P12).
   같은 숫자가 다른 화면이다 — A화면4=정규화컨펌, B화면4=L3업로드.
   **대응표가 원본 어디에도 없어서 내가 유도했다**(지원표 §0). 근거 문장이 없으니 틀리면 거기를 고친다
2. **계약의 화면 번호 오기** — `/api/profile` 이 「12 내 정보」였는데 12는 AI CHAT,
   마이페이지는 14다. ai-ae 가 계약 이관하면서 고쳤다
3. 🔴 **「회의 확정」 5건이 동결 계약과 반대다** (AI CHAT · 집행일정 서버연동 ·
   지급수수료 16종 · DOC/DOCX · 20MB). 낱개가 아니라 한 갈래다 —
   `프론트_데이터요구서_0901.md` 하나가 계약 반대편에 서 있고 나머지 3문서는 계약 편이다.
   **내가 판정하지 않았다.** 오른쪽 열(서버 실측)만 태워서 채우고 오너 판단으로 올렸다
4. **P11 규정·지침은 B체계에 대응 화면이 없다.** 누락인지 폐기인지 미확인 — 표에 남겨뒀다

### 용어 정리 (사전 적용)

`server/`·`tests/` 주석 **79건** (기계 14 + 소유 서술 65) · `docs/7_백엔드/` 7건.
🔴 **치환 전후 pytest 가 같다**(전체 140 passed · 2 xfailed 불변) — 주석만 건드렸다는 증거다.

- 「레인 A/B/C 소유」는 기계 치환하면 **낡은 뜻이 보존된다.** 그 구조가 회수됐으므로
  「지출계획 계통」·「공용 계약 파일이다. 필드를 늘리려면 먼저 합의할 것」처럼
  **지금 실행 가능한 문장**으로 다시 썼다
- 「깨지면 조율 세션에 보고한다」는 **가리키는 대상이 없는 지시**라 행동으로 바꿨다
- 🔴 **기술적 사실은 하나도 안 건드렸다** — 지연 import 이유 · `persist.py` UPDATE 를
  걷어내면 안 되는 이유 · 위치인자 함정 · `_org조건` 무력화 테스트 취지 · IDF 임계값 0.50 근거
- 안 바꾼 것과 **그 사유**: `슬롯표`(식별자) · `"슬롯"`(JSON 키) · `_골든셋/`(경로) ·
  「프록시·게이트웨이」(다른 낱말) · 「자산관리대장」(도메인어) · `dangling` 18건(컬럼·키) ·
  테스트 픽스처 문자열 3건(데이터 값)
- 「Phase 0」 2건은 **날짜를 지어내지 않고 실측했다** — 문서 어디에도 없어서
  `git log --diff-filter=A` 로 `models.py`·`_common.py`·`routes_plans.py` 가 전부
  `9502411`(2026-09-01)에 생긴 걸 확인하고 「(2026-09-01 동결/분해)」로 바꿨다

---

## 마감 (2026-09-02 야간)

### 닫은 것

| 항목 | 증거 |
|---|---|
| `GET /api/l3/{doc_id}` 실 경로 404 | 5케이스 실측 · 온보딩 스피너 풀림(조 11건) |
| 할일 쓰기 3경로 `org_id` 배선 | 9케이스 실측 · `보존_user=1` 로 재판정 규칙 미파손 |
| `persist.py` 위치인자 사고 | 같이 안 고쳤으면 기관 계획 저장이 통째로 깨질 자리였다 |
| xfail 4 · skip 2 | 전체 6 xfail·2 skip → **2 xfail·0 skip** (총 142 불변) |
| 빈 DB 재현 | `suddoe_verify` 3종 차집합 양방향 0 |
| 프론트 문서 7벌 | 2,729줄 → 168줄 · §7 개인정보 규칙 유실 직전 회수 |
| 용어 정리 | 86건 · pytest 불변 |

### 🔴 안 닫은 것 — 닫았다고 적지 않는다

- **`decisions.plan_id` 실측** (`test_저장_이벤트에_decision_id가_실제로_채워진다`) — xfail 유지.
  🔴 **사유가 세 번 틀리게 적혀 있었다. 마지막 오기는 내가 냈다.**
  「레인 A 대기」(그 파일은 0901 에 들어왔다) → 「**GPU 판정 필요**」(내가 인수인계 문서의
  「GPU 팟 미가동」을 그대로 옮겼다 — 이 테스트는 `_실_판정` 을 monkeypatch 해서 **LLM 을
  아예 안 부른다**) → 「실 `plan_id=1` 행 없음」(행을 넣고 태웠다. **여전히 xfail**).
  **실제 원인**: `monkeypatch.setattr(main, "MOCK", False)` 가 `main` 만 뒤집는데
  `persist.py` 는 import 시점에 자기 `MOCK` 을 따로 들고 있어 목 분기(`decision_id: 9001`)를
  탄다. 실패 메시지가 `assert 9001 == 777` 이다. GPU 와 무관하다.
  → 닫으려면 `persist.MOCK` 도 뒤집고 계획 행을 만들면 된다. **고치는 건 오너 판단이다.**
- **`PUT /api/profile` 쓰기** — `_실_프로필_저장` 이 스텁 그대로. 응답 모양만 동결
- **게스트끼리 서로의 지출계획이 보인다** — 로그인 대기. 구조적이다.
  못박은 테스트(`test_게스트끼리는_지금_서로_보인다_…`)를 **그대로 뒀다** — 로그인이
  닫히면 그게 빨개지는 게 목적이다
- **화면 11 조항별 해당/불해당** — 판정 출력에 필드 자체가 없다
- **P11 규정·지침** — B체계에 대응 화면이 없다. 누락인지 폐기인지 미확인

### 🔴 정밀검토에서 나온 정정 (2026-09-02 마감 후)

**내가 밤중에 «격리 구멍을 닫았다»고 보고한 것은 과장이었다.** 실측으로 확인했다:

`org_id` 는 인증된 신원이 아니라 **쿼리 파라미터, 즉 자기신고**다. 아무나
`GET /api/plans?org_id=<A의 uuid>` 를 치면 **A 의 계획과 금액이 200 으로 나오고**,
`POST /api/plans/{A계획}/tasks?org_id=<A>` 도 **201 로 들어간다.**

- **내가 만든 것**: 스코핑 필터 (정직하게 신고한 호출자에게 남의 것이 안 섞인다)
- **내가 만들지 않은 것**: 접근 제어 (사칭을 막는다) — 그건 인증의 몫이다
- 「9케이스 실측 통과」는 **호출자가 자기 org 를 정직하게 밝힐 때만** 성립하는 값이다
- 지금 사칭을 어렵게 하는 건 org_id 가 122비트 UUID 이고 **이를 노출하는 엔드포인트가
  0개**라는 사실뿐이다(6개 엔드포인트 + orgs 목록 API 부재 실측)
- 🔴 **화면 3(기관 선택)용 기관 목록 API 가 org_id 를 실어 내보내면 그 방어가 사라진다**

**공개 문서(`계약_정정_0902.md`·`프론트_인계_0902.md`)는 「빼면 404 다」로만 적혀 있어
사실관계는 틀리지 않았다.** 틀린 건 내가 보고에 쓴 「격리」라는 낱말과
`test_tenant_isolation.py` 의 「경계가 막혀 있는가를 본다」였다 — 둘 다 정정했다.

**같은 뿌리의 미감사 1건**: `POST /api/l3/upload` 도 `org_id` 를 그대로 믿는다.
남의 기관 이름으로 문서를 올릴 수 있다(202 + 그 org 로 행 생성 실측).
오늘 밤 나는 L3 **읽기**와 할일 **쓰기**만 봤고 L3 쓰기는 안 봤다.

**→ 후속 감사 2건을 B1(ai-31)·W1(ai-e5)에 돌렸다. 정본은 `docs/7_백엔드/격리감사_0902.md`.**
거기서 내 이해가 **한 번 더 뒤집혔다**:

- 🔴 **RLS 는 지금 아무것도 안 막는다.** tenant 11개 표에 정책이 있지만
  `relforcerowsecurity=False` 이고 앱이 **소유자(`postgres`)로 붙는다** → 전량 우회.
  실질 방어는 애플리케이션 `WHERE` 하나뿐이고, **그 `WHERE` 가 자기신고 org_id 를 믿는다** —
  두 약점이 직렬이 아니라 **같은 지점**이다.
  내가 「스키마를 못 바꾸는 이유는 RLS 와 묶여서」라고 쓴 문장은 마이그레이션 비용으로는
  맞지만 「RLS 가 지키고 있다」로 읽힐 자리였다. 지키고 있지 않다
- 🔴 **쓰기 IDOR 이 3곳이다** — 내가 찾은 2곳(`tasks`·`l3/upload`)에 더해
  **`POST /api/plans` 가 body 의 `org_id` 를 검증 없이 쓴다**(B1 발견). 나는 못 봤다
- ✅ **판정 «안쪽»은 깨끗하다** — L3 로드 org 스코프 교차 0건(양방향) · 검색 layer/적용대상
  필터 하드코드 · 골든셋 0 · L4 0 (전부 실행 확인, 골든셋은 양성대조까지)
- 🟡 사업 스코프는 상수가 아니라 **env 토글**(`SUDDOE_사업필터`). 지금 기본값이라 안전하지만
  `0` 이 들어가면 타 사업 세부관리기준이 근거로 섞인다(재현됨). 격리가 아니라 **판정 정확도** 사안

### 🔴 오너 판단으로 올린 것 (아침에 볼 것)

1. **게스트 격리 제3안** — `expense_plans` 에 nullable `guest_key TEXT` 부축.
   uuid·FK·RLS 를 안 건드리는 대신 **테넌시 축이 둘**이 되고 RLS 가 그 축을 안 본다.
   실측 3행(uuid 거부 / FK 위반 / NULL OK)은 `docs/7_백엔드/계약_정정_0902.md` §4
2. **「회의 확정」 5건** — 데이터요구서 하나가 동결 계약 반대편에 서 있다.
   4문서 중 3이 계약 편. **어느 쪽이 이기는지 내가 판정하지 않았다** (`프론트_인계_0902.md` §3)
3. **`04_agent.sql:14` 의 `\set ON_ERROR_STOP on`** — psql 전용이라 psycopg 로 돌리면
   04 가 통째로 안 먹는다. 실 배포 경로(psql)엔 결함이 없어 **안 고쳤다** (`DB_재현_0902.md`)

### 다음 사람이 알아야 할 것

- 쓰기 경로에 `org_id` 를 실으려면 **읽기·쓰기 양쪽에 다** 실어야 한다. 한쪽만 하면 404 다
- `_실_동기화` 안에서 DB 가 준 org 값을 `계획org` 로 받는다 — 파라미터 `org_id`(str)와
  **다른 값이다.** INSERT 에는 `계획org`(uuid)가 들어가야 한다. 여기서 이름을 겹치면 조용히 깨진다
- **「고쳤다」와 「지금 파일에 있다」는 다르다.** 오늘 밤 다른 갈래에서 수정이 덮여 사라졌다.
  커밋 전에 `grep` 으로 확인한다
- **「측정했다」와 「같은 조건에서 측정했다」도 다르다.** 용어 건수가 안 맞았을 때
  빼지 않고 조건을 물은 것이 맞았다 — 상대 값은 `__pycache__` 를 센 것이었다
