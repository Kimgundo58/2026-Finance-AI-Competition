<!-- 실제 구현.md 1~1367행 (416~419행 제외 — 파일 제목 아티팩트, 대조표 참조) -->
## 2026-09-01 · 레인 A — 테스트 픽스처 누수 17건 정리 + 재발 원인 수정

`audit_db.py` 가 `tenant.expense_plans` 이상(15→17건, 06:45~06:56 UTC)을 잡았다.
전부 이 레인 `tests/test_persist.py` 의 픽스처 버그가 원인 — 고치고, 지우고, 재실행으로
확인했다.

### 입력
지시: ai-14 감사 결과 통보(제목='테스트 품목'·질문원문 고정 서명 · 상세 SELECT 결과 포함).

### 판단
- **삭제 전에 서명 5개 컬럼(제목·질문원문·금액·사업명·상태) 전부 + `org_id IS NULL`
  로 SELECT 먼저 걸어 대상을 확정했다.** `제목` 단독 조건은 기각 — 지시대로 나중에
  실 사용자가 같은 제목을 쓸 수 있다
- **DELETE 는 SELECT 로 확정한 plan_id 리스트로만 걸었다** (WHERE 서명 재사용 아님) —
  두 쿼리 사이 다른 세션이 새 행을 만들어도(TOCTOU) 그 행은 안 지워지게
- 원인은 `test_persist.py::test_없는_plan_id_는_저장False_decisions_안건드림` —
  `_실_생성()` 으로 계획을 실제로 만들어 놓고 `_, decision_ids = 정리` 로 `plan_ids` 를
  버렸다. 주석("plan_id 자체를 안 만들었으니 없다")이 틀렸다 — 만들었다. `정리` 픽스처
  자체(yield 뒤 정리)는 pytest 표준 teardown이라 테스트 실패에도 돈다 — **버그는
  fixture 설계가 아니라 이 한 테스트가 반환값을 버린 것**

### 실측
삭제 전 SELECT: 17건, 전부 서명 일치·`org_id IS NULL`·`latest_decision_id IS NULL`.
`plan_tasks`·`decisions.plan_id` 모두 0건 걸림 — CASCADE 로 딸려 나갈 게 없었다.
`DELETE ... WHERE plan_id = ANY(그 17개)` → rowcount 17. 전후 `tenant.decisions`
73 → 73 (안 건드림). 수정 후 `pytest tests/test_persist.py tests/test_plans.py
tests/test_e2e_flow.py -q` 를 **연속 2회** 돌려 `expense_plans` 가 매번 0으로
돌아오는 것 확인(누수 재발 안 함). `pytest tests/ -q` → 118 passed.

### 함정
`_실_생성()` 처럼 부작용(DB INSERT)이 있는 헬퍼를 "존재확인용 부산물"로만 쓸 때
반환값을 무심코 버리기 쉽다 — 이번 버그가 그 패턴. 앞으로 실 DB 테스트에서 `_실_*`
생성 함수를 부르면 그 반환값의 id 를 정리 리스트에 담았는지 그 줄에서 바로 확인한다.

### 남은 것
레인 B(`test_tasks.py`)도 실 DB 를 쓴다고 ai-14 가 언급 — `expense_plans` 가 이 정리
이후 다시 늘면 그쪽이 원인일 가능성이 있다(내 소유 파일 밖이라 확인만 하고 ai-14 에 넘김).

### 산출물
`tests/test_persist.py`(버그 수정 1건, 주석 정정) · DB 삭제 17행(`tenant.expense_plans`,
CASCADE 없음).

---

## 2026-09-01 · 골든셋 재구성 — 채점 분모 65 → 93 · 다수결 기준선 67.7% → 52.7%

측정 도구가 실력을 못 재던 것을 고쳤다. 코드 변경 6파일 · DB 변경 4건.

### 입력
- `scripts/_work/_골든셋_보강후보_0901.json` (ai-ba 생성 29건)
- `2026_Finance_DATA_FOR_RAG/_골든셋_스테이징/_골든셋_확정본.json` (84문항)
- ai-40 의 적재 반려 3건 · ai-14 의 스키마 갭 질의 2건

### 판단
- **보강후보 29건 전건 편입.** 남의 「42건 전건 통과」를 믿지 않고 독립 감사를 다시 돌렸다
  (`/tmp/aud.py`). ai-ba 가 안 본 축을 하나 더 걸었다 — 그 조가 **검색 필터를 통과하는
  진입점 청크로 실재하는가**. 주관기관 16문항에서 이미 당한 「구조적으로 hit 불가능한
  문항이 분모에 있다」를 재발시키지 않기 위해서다. 결함 0.
- 🔴 **`eval_store.평가대상()` 의 `EXISTS(golden_chunks)` 가 판단불가 문항을 구조적으로
  배제**하고 있었다. 규범에 답이 없다는 게 정답인 문항은 고정할 청크가 애초에 없다.
  「판단불가 정답 0건」은 작성자의 취향이 아니라 **채점 규약에 박힌 구조**였다.
  → `정답판정='판단불가' OR EXISTS(...)` 로 열었다. 기각안: 골든셋에 억지 근거를 달아
  청크를 고정 — 없는 근거를 만드는 것이라 문항의 의미가 사라진다.
- 판단불가는 **인용·근거 적중 분모에서 뺀다.** 맞힐 수 없는 문항을 섞으면 인용적중률이
  구조적으로 깎인다. 판정일치율에는 남긴다. `인용채점분모` 를 따로 찍는다.
- N8(초격차 L3 충돌) 만 `평가범위='범위밖(L3 업로드 전제)'`. `eval.golden_set` 에
  `org_id` 컬럼이 없어 L3 경로가 평가되지 않는다. 문항은 남기고 분모에서만 뺐다.
- `pin_golden_chunks` 의 **「🔴 코퍼스 결손」 라벨 삭제.** 스크립트가 아는 것은
  「청크에 없다」 하나뿐인데 원인을 단정하고 있었다. 이 라벨을 믿고 적재를 지시한 3건이
  전부 오조치였다(①별표라 설계대로 컷 ②범위 밖 로컬트랙 ③같은 규범 두 벌). 관측만 적고
  원인은 사람이 문서를 열어 가르게 바꿨다.
- `check_items.유형` 을 **표로 옮겼다.** 서버가 항목 텍스트를 코드로 분류하고 있었는데,
  값은 맞았지만 마스터가 52→60 으로 늘 때 갱신이 빠진다. `DEFAULT '기타'` 라 새 코드가
  늘어도 조용히 틀리지 않는다. `seed_check_items` 의 `ON CONFLICT DO UPDATE` 목록에
  없어서 재시드에도 안 날아간다.
- `l3_documents.파싱품질` 에 `'대기'` 추가. 접수 시점에 `'warn'` 을 쓰면 「평가했는데
  미심쩍다」와 「아직 안 봤다」가 같은 값이 되어 구분이 영영 사라진다.
- `해야할일[].구분` 은 **출력에 안 싣는다.** 마스터 데이터지 LLM 이 고를 값이 아니다.
  실으면 모델이 뒤집을 여지가 생겨 due_date 정본이 둘이 된다. `code` 로 조인한다.

### 실측
```text
                 이전        지금
eval.golden_set   84   →   113행
채점 분모         65   →    93
다수결 기준선   67.7%  →  52.7%     ← 15%p 하락
정답 분포
  불가         44 67.7% → 49 52.7%
  조건부       17 26.2% → 21 22.6%
  가능          4  6.2% → 16 17.2%
  판단불가      0  0.0% →  7  7.5%   ← 0 이 아니게 됐다
사업 커버리지   5개    →   8개 전부
골든청크 고정   81/84  →  107/113
corpus.chunks  20,518 → 20,525 (ai-40 별지①②⑤ 적재분. 내 스크립트는 무변경)
근거 감사       42/42 통과 · doc·조·NFKC원문·진입점청크 4축
```

🔴 2026-09-06 현재: `eval.golden_set` **315행**(verified 307 · 미검수 8) · 채점 분모 **295**.
정답 분포 불가182·조건부75·가능41·판단불가17. 위 84→113 은 이 날짜 시점의 성장 기록이고,
그 뒤로도 두 번(202건 추가·5건 라벨 정정 등) 더 바뀌었다 — 최신은 [[9_미결]] 참고.

```text
tenant.decisions 해야할일 146항목 중 code 있음 146 (100%)
check_items.유형  기타 50 · 비교견적 1 · 계약 1
```

### 함정
- **ai-14 의 「판정이 `code` 를 안 준다」가 틀렸다.** `code` 는 처음부터 enum 으로 닫혀
  실려 있었고(146/146), 소비자의 실경로도 이미 `h.get("code")` 를 우선했다. 역추정이
  도는 건 MOCK 경로 하나뿐이었다. 「역추정이 오답을 낸다」(관측·맞음)에서 「생산자가 안
  준다」(추론·틀림)로 건너뛰고 **그 함수가 불리는 조건을 안 센** 것이다.
- 오늘 같은 모양의 오류가 나·ai-14·ai-40·ai-ba 에서 각각 나왔다. 공통 원인은 하나다 —
  **측정값 하나로 원인을 확정하고, 그 값이 성립하는 조건을 안 셌다.**
- `--세트` 의 `choices` 가 손으로 적혀 있어 `공식`·`보강` 을 못 골랐다. 목록을 늘렸다.

### 남은 것
- **최종 실전 E2E 미실행.** GPU 팟을 안 열었다. 새 분모 93 의 기준선 52.7% 초과 여부가
  판정 기준이다. 🔴 구 run(분모 77·일치율 67.5%)과 **같은 줄에 놓으면 안 된다** —
  정답 분포가 바뀌어 같은 실력이어도 숫자가 내려간다. 비교 가능한 축은 기준선 초과폭뿐.
- 정답 판단불가 7건이 **모델선택인지 실패경로인지 갈라 세는 칸이 없다.** 지금은
  예측 쪽만 갈라 센다. LLM 이 죽어서 얻은 점수가 실력으로 보인다 (ai-ba 지적).
- `rule 486`(재도전 여비 숙박비) 진단 오류 — `L1_공무원여비규정_20260701` 별표2 제2호에
  상한이 실재한다. 「근거 부재」가 아니라 2홉이면 선다. **오너 서명 대상.**
- TIPS 조번호 표기(`16.` 형식) 미결. 지금 바꾸면 refs·golden_chunks·retrieve 좌표가
  한꺼번에 흔들린다. TIPS 2문항은 둘 다 판단불가라 좌표 없이 분모에 들어간다.
- 미고정 6건: gold387·388(별지4 로컬트랙) · gold405(별표1 룰테이블) · 426·427·433(판단불가)

### 산출물
- `scripts/eval_store.py` — `평가대상()` 에 판단불가 예외
- `scripts/eval_e2e.py` — 인용·근거 분모 분리 · `인용채점분모` · `--세트` choices
- `scripts/pin_golden_chunks.py` — 「코퍼스 결손」 라벨 → 관측만 기술
- `scripts/orchestrate.py` — `판정(..., plan_id=)` (405·413·713·719 + 호출부 4곳)
- `db/init/01_schema.sql` — `l3_documents.파싱품질 IN ('대기','pass','warn','fail')`
- `db/init/02_frontend.sql` — `check_items.유형 TEXT NOT NULL DEFAULT '기타'`
- `2026_Finance_DATA_FOR_RAG/_골든셋_스테이징/_골든셋_확정본.json` — 113문항

---

## 2026-09-01 · 레인 A — E2E: 판정 저장이 화면(탭·통계·상세)에 실제로 반영되는가

`tests/test_e2e_flow.py` 신규. ai-14 후속 배정. `persist.py`(직전 항목)가 이었다는
"증거가 단위 테스트뿐"이라는 지적에 대한 응답 — 실 DB 로 4-way 전부를 태워
계획 생성부터 목록 탭·통계·상세까지 한 번에 확인한다. `test_contract.py`(레인 D)는
계약 표면만 보고 이 흐름은 안 본다.

### 입력
지시: ai-14 2026-09-01(persist.py 후속) · `server/_common.py`(`탭_판정`) ·
`corpus.check_items`(실측 52종 — 비교견적·계약 유형 각 1종, 실 code 사용).

### 판단
- **LLM 미호출.** `persist.판정_저장()` 을 직접 부르고, `decisions` 행은 orchestrate 가
  이미 만들어 둔 것을 흉내내 SQL 로 직접 INSERT — 판정 자체를 검증하는 테스트가 아니라
  "저장 배선이 화면에 닿는가"만 본다
- **해야할일에 실 `code` 를 실었다.** 2026-09-01 실측(`tenant.decisions` 73행·해야할일
  146건 전수)이 판정 출력의 100%가 code 를 싣는다고 확인했으므로, code 없는 픽스처는
  현실을 반영하지 않는다는 지시를 그대로 따름
- **탭 판정을 하드코딩(`_기대_탭`)하고 `_common.탭_판정` 과 별개로 실제 API 응답 5개
  탭 전부를 순회해 대조했다.** `탭_판정` 딕셔너리 값만 assert 하면 매핑 상수가 스스로를
  검증하는 동어반복이 된다 — API 를 실제로 쳐서 그 계획이 기대한 탭에만 뜨고 나머지엔
  안 뜨는지를 봐야 회귀를 잡는다
- **유형은 값을 단정하지 않고 `할일유형_ENUM` 소속만 확인.** 레인 B 가 `유형` 소스를
  하드코딩 맵(`_common.유형_추정`)에서 `corpus.check_items."유형"` 컬럼으로 옮기는
  중이라 값 자체를 고정하면 레인 B 작업과 충돌한다

### 실측
`pytest tests/test_e2e_flow.py -v` 1 passed. 4-way → 탭 매핑(전부 실 API 응답으로 확인):
| 판정 | 탭 |
|---|---|
| 가능 | 특이사항없음 |
| 조건부 | 확인필요 |
| 불가 | 위험 |
| 판단불가 | 확인필요 |

`pytest tests/ -q` → **118 passed, 2 xfailed**(레인 C 소유 xfail, 이 레인 무관). 테스트
전후 `tenant.decisions` 행수 73 → 73 (증감 없음, 기존 골든셋 분모 안 건드림 확인).
통계(전체/확인필요/위험/특이사항없음/점검전/금액합계)가 탭 파라미터를 바꿔도 동일함을
5개 탭 전부에서 확인. 같은 `decision_id` 로 두 번 저장해도 `plan_tasks` 행수 불변(멱등).
알 수 없는 탭 `?탭=없는탭` → 422 (직전 항목에서 고친 회귀 방지 확인).

### 함정
없음 — persist.py·routes_plans.py 가 이미 맞게 짜여 있어서 이 테스트는 첫 실행에 통과했다.

### 남은 것
`test_e2e_flow.py` 는 게스트(org_id=NULL) 경로만 태운다. 기관(org_id) 경유 저장은
`test_persist.py`(`_org조건` 단위 테스트)로만 커버돼 있고, 여기선 안 겹치게 뺐다.

### 산출물
`tests/test_e2e_flow.py`(신규, 1건 — 4-way 루프 안에 단언 다수).

---

## 2026-09-01 · 레인 D — 명세 실측 생성 + 기관 격리 회귀 (`API_DB_명세_v1.md` · `test_tenant_isolation.py`)

전체 스위트 **134 passed · 2 skipped · 7 xfailed · 0 failed** (연속 2회 동일 — 마감 시점).

🔴 **마감 시점 미결 하나 — 계약이 「동결」한 `POST /api/guest` 가 서버에 없다.**
계약 §1·§2-2 는 「최초 진입 시 한 번 부르고 `org_id` 를 localStorage 에 담으라」고
동결로 지시하는데 그 엔드포인트가 404 다. **프론트가 계약대로 짜면 첫 화면에서 막힌다.**
구현 누락이 아니라 되돌림이다 — `expense_plans.org_id` 가 `uuid` + `orgs` FK 라
`guest_<uuid4hex>` 가 타입에서 튕기고, FK 때문에 「DB 에 아무것도 안 쓰는 발급」도
불가능하다(ai-40 실측). `server/routes_guest.py` 는 남아 있으나 `main.py` 가
`include_router` 를 안 한다. **스키마를 바꿔 물리든, 계약 §2-2 를 내리든 사용자 판단이다
— 문서만 「동결」인 채 두는 게 제일 나쁘다.** `test_계약이_동결한_게스트_발급이_살아있다`
가 xfail 로 대기한다(어느 쪽으로 풀든 그 테스트가 알린다).

### 입력
- 지시: ai-14 — ① API·DB 명세를 md 하나로(**실측 생성**, 손으로 베끼지 말 것)
  ② 기관 격리 회귀 ③ `check_items.구분` 가드
- 레인 B(ai-59)가 독립 DB 덤프를 보내 교차검증에 씀

### 판단

**(1) `app.openapi()` 만으로는 문서를 못 쓴다 — FastAPI 가 한글 클래스명을 뭉갠다**
- 비ASCII 를 `_` 로 치환해 `판정요청` → `server__models________4`, `할일` → `__` 가 된다.
  그대로 실으면 **프론트가 스키마를 못 찾는 문서**가 나온다.
- 그래서 소스를 갈랐다 — 경로·메서드 인벤토리는 `openapi()["paths"]`(프론트가 보는 스펙),
  모델 이름·필드는 **라우트 객체에 달린 실제 pydantic 클래스**. 둘이 어긋나면 표에
  `? ← 라우트 객체에서 못 찾음` 이 찍히게 해 대조 가능하게 뒀다. 현재 20/20 일치.
- 부수 함정: 이 버전은 `ModelField.type_` 이 `None` 이라 요청 모델이 전부 null 로 나온다.
  실제 클래스는 `field_info.annotation` 에 있다.

**(2) 쿼리 파라미터 절을 넣었다 (구성안에 없던 것)**
- 요청·응답 스키마만으로는 `GET /api/plans` 를 부를 수 없다 — `탭·정렬·페이지·크기·
  금액_최소…` 가 안 나온다. `Query(description=…)` 까지 같이 뽑힌다.

**(3) 남의 표를 베끼지 않는다**
- `corpus.*` 는 `check_items` 만 펴고 나머지 16표는 이름·컬럼수·행수 한 줄씩.
  베껴두면 그 사본이 먼저 낡고, 낡은 사본은 «있다» 는 착각을 만든다.
- 행수는 「재생성 시점 스냅샷 · 계약 아님」으로 못 박았다.

**(4) 🔴 격리는 «검사기에 이빨이 있는지» 부터 봤고, 거기서 예측이 하나 틀렸다**
- `_org조건` 을 `("TRUE", ())` 로 무력화하면 B 의 계획이 A 목록에 **실제로 나타난다**는
  것부터 확인했다. 이게 없으면 통과가 「격리된다」인지 「아무것도 안 본다」인지 모른다.
- **예측 오류**: `GET /api/l3/{doc_id}` 의 `(%s IS NULL OR org_id = %s)` 를 읽고
  「게스트면 필터가 꺼져 남의 문서를 읽는다」고 단정했다. **실측은 반대였다 — 게스트도
  주인도 404 다.** 원인은 격리가 아니라 SQL 이다: `$2` 가 `$2 IS NULL` 한 자리에만
  나와 `IndeterminateDatatype` 으로 터지고, `_질의` 가 삼켜 빈 리스트를 404 로 바꾼다.
  즉 그 엔드포인트는 **누구에게나 항상 404** 이고, 격리는 「막혀서」가 아니라
  「아무것도 안 나와서」 지켜지는 것처럼 보인다. SQL 을 읽고 동작을 단정한 게 잘못이다.
- 그래서 «남의 L3 문서는 404» 는 통과해도 뜻이 없어, **주인이 자기 문서를 읽을 수 있을
  때만 세도록** skip 가드를 걸었다.

**(5) 공용 자리에 증분(after − before)을 쓰면 안 된다**
- 게스트 금액합계가 700,000 기대 자리에 600,000 이 나왔다. 격리 파손이 아니라 **다른
  세션이 같은 시각 게스트 버킷을 쓰고 있었다.**
- **API 집계 ↔ 같은 경계로 짠 SQL 집계 맞대보기**로 바꿨다. 누수가 있으면 API 쪽이
  커진다 — 이게 원래 재려던 성질이고 동시 작업에 안 흔들린다.

### 산출물

```text
API_DB_명세_v1.md              752행 · API 18경로/20메서드 + tenant 12표 + check_items
db/tools/gen_api_db_spec.py    이 문서를 통째로 재생성한다 (산문까지 스크립트 안에)
tests/test_tenant_isolation.py 기관 A·B·게스트 3주체 · 실 DB · 관문 무력화 대조 포함
```

교차검증: ai-59 의 독립 덤프와 13개 표·CHECK 18개 전건 일치.

### 격리 실측 결과

```text
✅ 막힘   plans 목록 · 통계(건수·금액합계) · 상세(404, 403 아님) · tasks 목록
          persist.판정_저장(남의 plan_id · 게스트→기관) — DB 직접 조회로 «안 바뀜» 확인
🔴 뚫림   POST/PATCH .../tasks · tasks:sync — org_id 를 **아예 안 받는다.**
          읽기는 막혀 있는데 쓰기가 더 넓다. 남의 계획에 할일을 꽂고 고치고 지운다
⚠️ 검증불가 GET /api/l3/{doc_id} — 위 (4)
🔴 알려진 것 게스트끼리 서로 보임 (`org_id IS NULL` 단일 버킷)
```

### 남은 것 (조율 세션에 넘김 — 내 소유가 아니다)
- 쓰기 세 경로에 org_id 배선 (레인 B)
- `routes_l3._실_상태` SQL 수정 (레인 C). 고치면 게스트 구멍이 드러난다 — 그때 잡히게 테스트를 걸어 뒀다
- 게스트 버킷 분리는 진행 중(ai-40). 끝나면 못박은 테스트가 빨개진다 — **뒤집으라고 docstring 에 적어뒀다**

## 2026-09-01 · 레인 D — 계약을 집행 가능한 테스트로 (`test_contract.py` · `test_regression_rank.py`)

`프론트_API_계약_v1.0.md` 는 사람만 읽을 수 있어서 레인이 필드를 하나 빼먹어도 아무도
모른다. 그걸 86건의 테스트로 바꿨다. **검증만 하고 `server/` 는 안 건드렸다** —
A·B·C 가 동시에 쓰고 있어 결함은 조율 세션에 넘긴다.

### 입력
- 지시: ai-14 2026-09-01 — 레인 D 신설. 소유는 `tests/test_contract.py` ·
  `tests/test_regression_rank.py` 둘뿐
- 대조 정본: 계약 문서 §1~§9 · `server/models.py`(코드 정본) · `server/_common.py` enum

### 판단

**(1) 「키가 늘어난 것」은 응답으로는 못 잡는다 — 모델에서 잡아야 한다**
- 라우터 대부분이 `response_model` 을 걸어 **계약 밖 키가 애초에 응답에 안 실린다.**
  응답만 보면 «필드가 늘어남» 을 영원히 못 잡는다. 그래서 대조를 둘로 나눴다 —
  ① 모델 필드 집합 ↔ 계약표, ② 실제 응답 키 ↔ 계약표.
- ②가 값어치를 갖는 자리는 `response_model` 이 없는 넷(`health`·`vocab`·`programs`·
  `profile`)과 **SSE 이벤트**다. SSE 는 생 dict 라 계약을 지켜 줄 장치가 하나도 없다.

**(2) 순위 회귀는 총계로 안 보인다 — 그래서 파일을 갈랐다**
- 목록은 `_거르기 → _정렬 → 슬라이스` 가 한 함수에 얽혀 있다. 여기서 나는 회귀는
  **건수가 그대로인 채 순서만 틀어지는** 모양이다. 정렬 키를 바꿔도 건수는 5,
  페이지를 잘못 잘라도 합계는 5.
- 그래서 값의 순서·페이지 이어붙이기(합집합·중복 0·경계 순서)·«통계는 필터 적용 전»
  불변을 본다. 탭별 건수만 세는 테스트로는 셋 다 안 걸린다.

**(3) vlm A등급 금지는 «검사기에 이빨이 있는지» 부터 테스트한다**
- 목 픽스처에 vlm 출처가 없으므로 그냥 두면 이 테스트는 «안 섞였다» 가 아니라
  «아무것도 안 본다» 와 구별이 안 된다. 알려진 위반을 넣어 검사기가 잡는지 먼저 확인하고,
  실제 코퍼스의 `extraction='vlm'` 3건을 DB 에서 읽어 대조한다 (DB 없으면 skip).
- 계약 문서 §8 에 이 금지는 아직 없다. 문서 반영은 조율 세션이 한다.

**(4) 엔드포인트 내성은 `app.routes` 가 아니라 `app.openapi()["paths"]` 로 한다**
- 이 FastAPI 버전의 `include_router` 는 라우트를 평탄화하지 않고 포함 라우터 객체
  하나만 넣는다(지연 결합). `app.routes` 로 세면 **레인 A·B·C 엔드포인트 9개가 통째로
  «없음» 으로 잡힌다** — 실제로 그렇게 한 번 틀렸다. OpenAPI 가 프론트가 보는 것이기도 하다.

### 산출물

```text
tests/test_contract.py        키 집합(양방향) · 목↔실 키 동일 · 폐쇄 enum 4축 ·
                              오류 봉투 5종 · SSE 순서 · vlm A등급 금지 · 엔드포인트 16
tests/test_regression_rank.py 정렬 4종 · 페이지 이어붙이기 · 통계 불변 · 탭↔4-way 덮개 ·
                              할일 날짜순(널 맨 뒤) · 목 픽스처가 4-way 를 다 그리는가
```

`PYTHONIOENCODING=utf-8 python -m pytest tests/ -q` → **116 passed · 2 xfailed · 1 xpassed.**

### 남은 것 (조율 세션 판단 대기 — 내 소유가 아니다)

- 🔴 **`하위항목` 이 모드마다 다르다.** `models.정규화응답` 에 있는데 목·폼 경로의 SSE
  `결과` 에는 안 실린다. 자연어 실 경로만 실을 수 있어 §9 「실서버로 갈아끼워도 프론트
  코드는 안 바뀐다」가 이 필드에서만 깨진다
- 🔴 **실 판정이 터지면 `결과` 에 `_오류` 가 실린다** — 계약 밖 키. 진단용으로 남길지
  걷을지 미결 (`test_실판정_실패시에도_계약_키만_나온다` 가 xfail 로 대기)
- `PUT /api/profile` 응답 모양(`{저장, 이유, 받은값}`)이 계약 문서에 없다

## 2026-09-01 · 골든셋 균형 보강 후보 29건 — 「가능」 12 · 「판단불가」 8 · 커버리지 9

`eval.golden_set` 의 정답 분포가 «불가» 로 쏠려 상수 예측기와 붙었다는 감사(ai-25)에 대한
재료. 후보 파일만 냈고 골든셋에는 한 행도 안 넣었다.

### 입력
- 지시: ai-25 2026-09-01 — 채점 분모 62 에서 불가 41(66.1%)·조건부 17·가능 4·**판단불가 0**.
  «항상 불가» 상수 예측기의 일치율이 66.1% 다
- 재료: 오늘 4단 검증한 `corpus.rules` 72행 · `corpus.doc_articles` · `corpus.precedence_rules` 8행
  · `corpus.recheck_queue`
- 산출: `scripts/_work/_골든셋_보강후보_0901.json` 하나. 생성기는 스크래치패드

### 판단

**(1) 원문을 손으로 쓰지 않는다 — 앵커 슬라이스로 뜬다**
- 각 근거를 `(doc_id, 조번호, 시작앵커, 끝앵커)` 로 적고 `doc_articles.본문` 에서 잘라 온다.
  생성 시 `assert 슬라이스 in 본문`, 생성 후 별도 스크립트로 재감사 — 42건 전건 통과.
- 기각: 조문을 읽고 옮겨 적기 — 한 글자만 달라도 대조 감사에 걸리고, 걸리지 않으면 더 나쁘다.

**(2) 앵커는 공백·따옴표를 느슨하게 본다. 슬라이스는 그대로다**
- PDF 본문은 문장 한가운데서 줄이 바뀌고(`"포함하여
집행할"`), 여는따옴표를 두 번 쓴 자리가
  있다(`"숙박비"`). 앵커 매칭만 정규식으로 풀고 **반환은 원문 그대로** 라 verbatim 이 안 깨진다.

**(3) 「가능」은 룰을 바꾸는 게 아니라 질문이 조건을 충족시킨다**
- 72행 중 `허용='가능'` 은 1행뿐이고 71행이 조건부다. 조건부 룰의 조건을 질문에 명시해
  정답을 「가능」으로 만든다 (예: "4대사회보험에 모두 가입된 직원을 협약 시작 후에 채용해").
- 기각: 룰의 `허용` 을 손대서 가능 룰을 늘리기 — 문항을 만들려고 룰을 고치는 건 거꾸로다.

**(4) 「판단불가」는 근거의 부재를 실측한 것만 쓴다 — 8건 모두 쿼리로 확인했다**

| 후보 | 부재 실측 |
|---|---|
| N1·N2 TIPS | `rules` 0행 · `check_items` 0행 · 팁스지침 본문 전체에 '광고선전' 0회 · '지급수수료' 0회 · '멘토링비' 0회 · `precedence_rules` 8행에 TIPS 없음 |
| N3 모두의창업 창업활동비 | 제31조가 전문기관에 통째 위임 · 52개 조 전체에 금액·증빙 0 |
| N4 모두의창업 광고선전비 | 제33조①에 비목 없는데 ②는 지침 제45조를 인용 — **두 항이 어긋난다** |
| N5 모두의창업 교육훈련비 | ①에도 없고 ②의 인용 목록에도 제44조 없음 · 문서 전체 '교육훈련' 0회 |
| N6 모두의창업 인건비 | 제27조는 조제목만 인건비이고 본문이 전부 **주관기관** 인건비 |
| N7 재도전 재료비(현물) | 2026판 제15조② 현물 3종에 재료비 없음 · 33개 조 전체 '재료비' 0회 |
| N8 초격차 L3 충돌 | `precedence_rules` 에 초격차 L2>L3 행 없음 (`recheck_queue` 1547 이 같은 사유로 대기) |

- 🔴 **ai-25 가 재료로 준 «큐 잔여 9행(신판/구판 안 갈리는 좌표)» 은 안 썼다.** 그 모호함은
  우리 룰의 좌표 문제이지 규범의 공백이 아니다. 사용자 질문은 지침 제39조로 답이 선다.
  판단불가 문항으로 만들면 «측정하려는 능력» 이 아니라 우리 데이터 결함을 채점하게 된다.
- 🔴 재도전 여비 숙박비도 후보에서 뺐다. rule 486 은 «별표가 첨부돼 있지 않아 한도값을
  확정하지 못했다» 고 적었지만, `L1_공무원여비규정_20260701` 이 코퍼스에 있고(청크 32)
  **별표2 제2호에 숙박비 상한이 있다** — 판단불가가 아니라 2홉으로 답이 서는 문항이다.

**(5) 조번호는 `제N조` 만 쓴다 — 그래서 못 만든 문항이 있다**
- 창업중심대학 고유 집행기준은 `[참고3]` 에만 있고 조번호가 `참고3` 이다. K4·K5 는
  **지침 조로 답이 서는 것만** 골랐다. 프리랜서 마켓 전면금지 같은 창업중심대학 고유 기준은
  이번에 못 넣었다.
- 팁스 지침의 조번호는 `16.` 형식이다. N1·N2 는 판단불가라 정답근거를 비웠지만,
  팁스로 «답이 서는» 문항을 만들려면 조번호 표기부터 정해야 한다.

### 실측
- 후보 29건 — 가능 12 · 판단불가 8 · 커버리지 9(불가 5 · 조건부 4)
- 사업별 — 모두의창업 9 · 재도전 6 · 창업중심대학 4 · TIPS 2 · 나머지 각 2
- 비목별 10종 — 여비 5 · 인건비 4 · 광고선전비 4 · 지급수수료 3 · 외주용역비 3 · 재료비 3 ·
  기계장치 3 · 교육훈련비 2 · 특허권등 1 · 창업활동비 1
- 근거 42건 전건 `본문` 부분문자열 재감사 통과 · 조번호 형식 위반 0 · 타 사업 규정 혼입 0
- 쓰기 0 — `item_alias` 228 · `rules` 72 · `eval.golden_set` 84 전부 불변 (전후 확인)

### 함정
- **`_찾()` 의 첫 시도가 `str.find` 인 것이 중요하다.** 정규식만 쓰면 앵커가 여러 곳에
  걸릴 때 조용히 엉뚱한 데를 문다. 정확 일치 우선 → 실패 시에만 느슨하게.
- 모두의창업 제37조 표-6 은 한도값이 표 안에만 있어서 슬라이스가 표 세 줄을 통째로 문다.
  인용 채점이 «한 문장» 을 기대하면 P5 에서 걸린다 — 표 인용 범위는 ai-25 결정.
- 「가능」 문항 P11(노트북)은 지침 제39조② 한 조문 안에서 «집기·가구 불가 / 사업화 직접
  관련 PC 가능» 이 갈린다. 앞 절만 읽으면 «불가» 가 나온다 — 의도한 함정이다.

### 남은 것
- ai-25 검수 후 `eval.golden_set` 투입. 나는 안 넣는다
- N8 은 L3 업로드가 전제라 `평가범위` 를 어떻게 둘지 미정
- 창업중심대학 `참고3`·팁스 `16.` — 자유표기 조번호를 인용 좌표로 쓸지 미정
- rule 486 의 «한도값 확정 못 함» 은 정정 가능하다 (공무원여비규정 별표2 제2호). 룰 수정은
  오너 서명 대상이라 손대지 않았다

### 산출물
```text
scripts/_work/_골든셋_보강후보_0901.json
  규칙준수{}                       원문·조번호·타사업혼입·item_alias 자기신고
  합계{}                           갈래별·정답판정별·사업별·비목별
  후보[29]{후보no, 갈래, 사업명, 비목, 질문, 정답판정, 대상,
          정답근거[{doc, 조번호, 원문, 원문_한줄}], 해야할일[], 채점대상[],
          판단불가사유, 근거재료, 메모}
```

---


---

## 2026-09-01 · 코퍼스 «결손» 6건 해부 — 적재는 1건뿐이었다

배정은 「골든셋 7문항이 원리적으로 hit 불가 — 적재로 복구하라」였다. 전수로 갈라 보니
**적재가 답인 것은 하나도 없었다.** 라벨(`🔴 코퍼스 결손`)을 관측으로 착각한 배정이었다.

### 입력
`pin_golden_chunks.py --재고정` 의 미고정 6행 · `corpus.documents/doc_articles/chunks/refs` ·
`모두의 창업 프로젝트 세부관리기준(개정본).pdf` 원본 · `stage2_chunk.표인가()` · `retrieve.py` 폐포 CTE.

### 판단 — 셋을 반려하고 하나를 새로 찾았다

**(1) 운영요령 3건(gold217·225·232)은 결손이 아니라 «정답지 포인터 오류»다**
같은 고시(제2024-101호·시행 20241206)가 **두 벌**이다. 골든셋이 가리킨 PDF본은
`superseded`·`index_target=False`·청크 0. 그런데 XML본 `L1_중소기업창업지원사업운영요령_20241206`
이 `active`·41조·40청크로 이미 인덱싱돼 있다. 조 집합 39/40 일치, XML 쪽이 더 완전하다
(제14조·별지1 추가 보유). PDF본엔 「법제처 9 국가법령정보센터」 머리글 노이즈까지 섞여 있다.
🔴 **확정 전에 함수를 태웠다** — 세 문항의 근거원문을 활성본 청크에 걸어보니 전부
**원문일치**(사다리 최상단)로 잡힌다: `제21조→chunk 16118` · `제9조→chunk 16106`.
→ 적재하면 같은 규범이 인덱스에 두 벌이 된다 (CLAUDE.md 「법령 파일은 규범당 한 벌만」 위반).

**(2) 별표1(gold237) 적재는 확정 원칙 위반이다**
`L1_국고보조금통합관리지침` 부속물 **14건 전부**가 ASCII 박스표다(괘선 114~1,953 · 임계 10).
`표인가()` 가 설계대로 자른 것이고 그 주석이 「별표·한도표는 RAG 에 넣지 않고 룰 테이블로
뺀다」를 근거로 든다. **버그가 아니다.** 「카드 사용제한 업종」은 청크가 아니라 룰의 재료다.

**(3) 별지4(gold219·220) 적재는 적극적으로 해롭다**
이 문서의 별지는 동그라미 번호이고 트랙이 섞여 있다 — ①②=일반·기술트랙, ③④=**로컬트랙**,
⑤=공급기업. 두 문항의 근거원문 3구절을 전문 검색한 결과 **전부 별지④에만 있고 별지①②엔
한 건도 없다.** CLAUDE.md 가 「제3편을 남겨두면 일반·기술트랙 판정에 범위 밖 룰이 딸려온다」
고 못박은 그 자리다. → 코퍼스가 아니라 골든셋 처분 대상.

**(4) 아무도 배정하지 않은 진짜 결손 — 섹션 분리기가 동그라미 번호를 못 잡는다**
```text
RE_첨부.match('별지1')        -> True
RE_첨부.match('[별지 제①호]')  -> False      ← 부속물이 통째로 doc_articles 밖
```
그래서 「일반·기술트랙 **진출자** 사업비 집행기준」(비목별 정의·증빙·유의사항)이 인덱스에
없었다. 정확히 우리가 판정하는 물건이다.

**(5) 적용대상을 폴백에 맡기지 않았다 — 이게 이 적재의 핵심 판단이다**
`tag_apply_target.적용대상_of()` 는 태그 없는 부속물에 `'공통'` 을 준다(`:193`). 그대로 두면
`retrieve.py` 의 `적용대상 IN ('창업기업','공통')` 을 통과한다. 그런데 **별지①은 주관기관의
집행기준**이다 — '공통' 이면 주관기관 예산 규칙이 창업기업 판정에 섞인다. 명시적으로 박았다.
(승인 근거로 인용된 「홍보기념품 2만원·멘토링 30/50만원」이 실은 별지① = 주관기관 것이다.)

**(6) 배제는 화이트리스트로 구현했다**
블랙리스트면 다음에 별지가 하나 늘 때 로컬트랙이 조용히 딸려 들어온다. 헤더가 5개가
아니면 중단하고, 건너뛴 것을 「건너뜀 … (로컬트랙 — 범위 밖)」으로 **찍는다** —
「안 걸렸다」와 「걸러냈다」가 출력에서 구분돼야 한다.

**(7) 별지2만 `진입점` — 규약 예외라 근거를 남긴다**
부속물 기본값은 `폐포전용`이다(붙임 청크가 top-5 의 49.1%를 먹는데 정답은 1건이었다는 실측).
그런데 **제2편 본문은 별지①②를 한 번도 인용하지 않는다** — 제2편 구간의 '별지' 언급 3건이
전부 별지 제5호다. 인입 엣지 0이라 폐포전용이면 영원히 도달 불가다. 별지5 는 제32조 엣지가
이미 `resolved` 라 폐포전용으로 족하다.

**(8) refs 재빌드는 하지 않았다 — 폐포가 끊긴 적이 없다**
발단 사례를 직접 짚었다: `제47조·제49조 「요령 제21조」 준용 → 활성 XML 제21조 [resolved]`.
「끊겼다」로 보인 것은 같은 조의 **중복 엣지**였다(「운영요령 …」이 폐지본으로 따로 붙는다).
제14차→운영요령 폐지본 35쌍 중 **활성 쌍둥이가 없는 건 3쌍뿐이고 전부 `dst_조번호 NULL`**,
그리고 `retrieve.py:100,106` 이 시작·재귀 **양쪽 간선에** `dst_조번호 IS NOT NULL` 을 건다.
→ 실질 손실 0.

### 실측
```text
corpus.chunks          20,518 → 20,525   embedding NULL 0   chunk_len 20,525
  별지1  3청크  적용대상=주관기관 · 폐포전용     별지3·4  제외(로컬트랙)
  별지2  2청크  적용대상=창업기업 · 진입점
  별지5  2청크  적용대상=공통   · 폐포전용
게이트                  최대 937 / 1,024 · 초과 0      CPU 임베딩 44초 (GPU 미사용)
chunk_terms         1,148,718 → 1,149,877
corpus.refs            44,865 (불변 — 재빌드 안 함)
골든셋 고정             81 / 84 (내 적재로 변동 없음 — 예고한 대로)
resolved 인데 dst 청크 0건인 엣지   1,587 (44,865 의 3.5%)
  └ 분해: TIPS 별지서식 ~980 · 사례집_* ~350 · 구판 지침 19 — **대부분 설계상 정상**
  └ 진짜 오해소는 운영요령 폐지본 44건뿐
검색 실측 (별지2 순위)   사업필터 ON 2·4위 / OFF 7·6위
폐포 도달 확인          "공급기업 담합" → 제32조 1위 · 폐포가 별지2·별지5 실제 수집 ✅
```

### 함정

🔴 **«측정값 하나로 원인을 확정»의 교과서 사례가 배정 자체였다.**
`pin_golden_chunks` 가 찍은 「🔴 코퍼스 결손」은 «chunks 에 없다»에서 나온 **추론**이지
관측이 아니다. 6건을 전수로 가르니 적재가 답인 건 0건이었다. 관측(청크 0)은 전부 정확했고
틀린 건 추론이다 — CLAUDE.md 「지표를 읽을 때」가 말하는 그 형태다.

🔴 **필터 통과와 검색 도달은 다르다.** 6개 필터를 통과해도 순위에 안 들면 없는 것과 같다.
그래서 적재 후 실제 질문 3건을 태웠다. 별지2 는 사업필터 ON 2위 / OFF 7위 — **부속물은
`사업필터_기본=False` 에 특히 민감하다.**

🔴 **부속물 청크가 생기자 진단 문구가 바뀌었는데, 그 문구도 추론이다.**
별지4 실패 사유가 「코퍼스 결손」 → 「부속물 청크 7건 · 좌표 표기 불일치」로 바뀌었다.
그런데 별지4 는 표기가 아니라 **로컬트랙이라 일부러 없다.** 개선된 문구가 또 다른 추론이다.

**같은 규범이 두 벌 있는지 먼저 의심한다.** XML(법제처)과 PDF(부처 배포)로 같은 고시가
들어오면 doc_id 가 갈리고, 약칭 해소가 둘 중 하나를 골라 붙는다. 「요령」→활성 ·
「운영요령」→폐지본으로 갈린 것이 실제 사례다.

### 남은 것
- `build_refs.py` 약칭표 — 「요령」/「운영요령」이 다른 판본으로 갈린다. **위생 항목**
  (판정 영향 0 으로 실측됨). ai-25 가 「오늘은 아니다」로 보류
- 별표1 → `corpus.rules` 금지업종 등록 (ai-ba 레인) — 등록되면 gold237 이 채점 복귀
- 섹션 분리기 본체(`stage0`)의 동그라미 번호 미지원은 그대로다. 이번엔 별지 3건을
  전용 스크립트로 우회했다 — 같은 형태의 문서가 또 있는지는 안 세었다
- `audit_db.py` 🔴 6건 = `tenant.expense_plans` 5행 전 컬럼 NULL (프론트–백엔드 레인 신규)

### 산출물
```text
scripts/_work/_모두의창업_별지적재.py   별지①②⑤ 적재. 화이트리스트 배제 · 멱등(재실행 중단)
corpus.doc_articles                   +3행 (별지1·2·5)
corpus.chunks                         +7행 (전부 임베딩 포함)
```

---

## 2026-09-01 · 레인 A(persist) — 판정 영속화 잇기 + 탭 422

`server/persist.py` 신규. 판정 결과를 `tenant.decisions`(이미 있는 행)에 `plan_id` 를
잇고 `expense_plans`·`plan_tasks` 를 갱신한다. `routes_plans.py` 탭 필터 422 보강.
조율 세션 ai-14 배정, 시그니처는 ai-14 동결. main.py 판정 경로(호출부)는 미소유 — 대상 밖.

### 입력
`server/routes_plans.py`(전체, 이미 구현된 `_실_*` 4개는 참고용) · `server/routes_tasks.py`
(`동기화()` 시그니처만, 편집 안 함) · `server/_common.py` · `db/init/01_schema.sql`
(`tenant.decisions`) · `db/init/02_frontend.sql`(`tenant.expense_plans`, decisions 확장
5컬럼) · `server/models.py`(`할일동기화`, `판정요청`).

### 판단
- **INSERT 를 UPDATE 로 뒤집었다.** ai-14 최초 지시(②-1)는 `decisions` INSERT였는데,
  같은 세션이 곧바로 정정을 보냈다 — `scripts/orchestrate.py::decisions_적재()` 가
  판정 시점에 이미 그 행을 만들고(`main.py:640 _실_판정()` 이 그 `decision_id` 를
  지금 버리고 있음), 여기서 또 넣으면 판정 1건당 행이 둘 생겨 골든셋 평가 분모가
  오염된다. `persist.py` 는 코딩 시작 전에 정정을 받아 처음부터 UPDATE 로 짰다
- **expense_plans UPDATE 를 먼저, decisions UPDATE 를 그다음에 걸었다.** ai-14 지시엔
  순서가 명시 안 됐지만, plan_id 존재·기관일치 확인을 expense_plans UPDATE 의
  rowcount 로 겸해서 하므로 — 먼저 걸어 rowcount≠1 이면 decisions 는 아예 안 건드린다.
  대안(SELECT 로 먼저 확인 후 두 UPDATE)은 쿼리 한 번 더 든다고 기각
- **`routes_tasks.동기화()` 를 직접 import 해서 함수로 불렀다.** 그 라우터 함수가
  FastAPI 데코레이터 밑에 있어도 평범한 파이썬 함수라 그대로 호출된다 — 파일 편집 없음
- **테스트에서 `판정_저장()` 대신 `_실_저장()` 을 직접 부른다** (DB 경로). `test_plans.py`
  가 `_실_생성()` 을 직접 부르는 관례와 같다 — `conftest.py` 의 모듈별 MOCK 스위치
  목록(`_모듈` 튜플)에 `server.persist` 가 없어서다. MOCK 분기 테스트만 `persist.MOCK`
  을 직접 세운다

### 실측
`PYTHONIOENCODING=utf-8 python -m pytest tests/ -q` → **27 passed, 0 skipped** — 로컬에
tenant DB 가 떠 있어 `test_persist.py`·`test_plans.py` 실 경로 전부 실행됨(스킵 아님).
`같은 decision_id 로 두 번 저장해도 decisions 행 수 불변` 검증 통과(회귀 핵심 케이스).
`server/main.py --selftest` 34개 전부 통과.

### 함정
`conftest.py` 의 `_모듈` 튜플에 `server.persist` 가 빠져 있다 — 이 모듈은 계약파일
(`_common.py`·`models.py`·`mock_data.py`·`conftest.py`)에 속해 이 레인이 못 고친다.
지금은 문제 없다(`_실_저장()` 을 직접 호출해 우회) 지만, 나중에 `판정_저장()` 을 통째로
MOCK/실 전환 테스트하려면 `conftest.py` 갱신이 필요하다 — ai-14 에 보고함.

### 남은 것
`main.py::_실_판정()` 이 `orchestrate.판정()` 반환의 `decision_id` 를 버리는 문제는
이 레인 소유 밖(main.py 는 레인 C) — ai-14 가 별도로 그쪽 세션에 넘긴다고 함.
근본 해법(`orchestrate.판정()` 이 `plan_id` 를 받아 한 번에 쓰기)은 ai-25 쪽 제안 중 —
들어오면 `_실_저장()` 의 decisions UPDATE 한 줄은 없어진다.

### 산출물
`server/persist.py`(신규, `판정_저장`/`_실_저장`) · `server/routes_plans.py`(`_거르기`
탭 422 보강, 1줄) · `tests/test_persist.py`(신규, 5건).

---

## 2026-09-01 · 스키마 정합 · 증빙 게이트 · 파일 정리 — 그리고 오삭제 1건

레인 «스키마 정합 · 감사 · 잔여 데이터». 산출보다 **두 번의 자기정정이 본체다** —
`build_index.py` 오삭제와, 「52 로 복구됐다」는 근거 없는 성공 선언.

### 입력
`db/init/04_agent.sql`(강등코드 18종) · `scripts/audit_db.py`(계약 기대치) ·
`scripts/load_evidence.py`(TRUNCATE 후 파생컬럼 미복구) · `공유받은 파일/증빙서류 종합.csv`
124행 · `RAG.md` §2(코퍼스표) · 저장소 전체 파일 참조 그래프.

### 판단

**(1) 강등코드 CHECK 18 → 22 — 목록 밖은 거부한다**
`llm_validate.py` 가 `TASK_*` 4종을 새로 쏘는데 CHECK 가 18종이라 INSERT 가 죽는다.
정본 철자는 `llm_validate.py` 에 박힌 문자열이고, DDL 은 그걸 따라간다 (반대가 아니다).
오타를 통과시키면 지표가 조용히 갈라지므로 목록형 CHECK 를 유지했다.

**(2) `evidence_sources` 패키지 정합 — 문자열 리터럴을 새로 만들지 않았다**
40.3% → 100%. 지배적 원인은 산문값이 아니라 **이름 불일치**였다:
`'초격차 스타트업'` 42행 vs `programs.사업명 = '초격차 스타트업 프로젝트'`.
`programs.별칭` 에 이미 그 별칭이 있어서, 해소기를 `corpus.programs` 에서 만들면
새 리터럴이 0개다. 기각한 대안: UPDATE 한 방 — **다음 재적재에서 조용히 되돌아간다.**
해소기를 적재기 안(`_패키지_해소기`)에 심고 재실행해 멱등성을 확인하는 쪽이 진짜 수리다.

**(3) 파생 2컬럼은 «쓰기 경로가 아예 없었다»**
`비목매핑(적용=True)` 이 `sys.exit("🔴 적용 경로는 없다")` 였다. 값은 손으로 채워져
있었고, 내가 `load_evidence.py` 를 돌리자 TRUNCATE 가 52행을 날렸다.
기존 `정본비목()` 분류기를 재사용해 `파생컬럼_채우기()` 를 만들고 `적재()` 커밋 직후에
붙였다 — 사람이 다시 채우는 구조로 두면 같은 사고가 반복된다.

**(4) 증빙 #125 는 패키지 1개만 달았다**
요구처가 `rule 461`(창업중심대학) 하나뿐이다. 형제 증빙 #64~68 이 4개 사업을 달고
있다고 따라 적는 것은 **없는 근거를 만드는 일**이라 하지 않았다.

**(5) 재생성 산출물 169MB 는 지우지 않았다**
`emb.npy`·`_stage2_chunks.jsonl`·`_stage0_articles.json`. 「재생성 가능」은 「지금
안 쓴다」가 아니다 — ai-25 의 재도전 판본 교체와 GPU E2E 가 진행 중이고
`_stage0_articles.json` 은 그쪽 작업 스크립트가 실제로 읽는다.

### 실측
```text
decisions_강등코드_check        18 → 22종 (4종 INSERT 통과 · 오타 거부 · 기존 73행 무손상)
corpus.evidence_sources        125행 · 패키지 채움 125/125 (100%) · 미해소 0
   해당비목_분류                125/125     해당비목_정본  73/125
   재계산 vs DB 저장값          불일치 0
rules.증빙 완전일치             71/71 (100%) · 미일치 0종     ← 아침 608/609
audit_db.py                    ✅ 감사 통과 — 미등록 결손 없음
전 모듈 import 스모크           36개 중 35 통과
파일 정리                      추적 12건 삭제 · 비추적 7건 격리(삭제 아님) · 재생성물 보류
```

### 함정

🔴 **함수 안 import 는 정적 참조 검색에 안 걸린다.**
`build_index.py` 를 지웠는데 `version_diff.py:45` 가 **함수 본문 안에서**
`from build_index import ...` 한다. 파일 상단만 훑는 검색으로는 안 보인다.

🔴 **도크스트링은 참조수를 이기지 못한다.**
아침 감사에서 `build_index` 참조를 **내가 직접 6건으로 셌다.** 그런데 삭제 단계에서
`stage2_chunk.py` 의 "대체한다" 라는 도크스트링을 더 믿었다. 관측(6건)을 갖고 있으면서
서술(대체한다)로 덮은 것 — CLAUDE.md 「지표를 읽을 때」가 말하는 오류의 정확한 형태다.
`git checkout` 으로 복구했다.

🔴 **「52 로 되돌아왔으니 원본과 일치한다」는 근거 없는 성공 선언이었다.**
파생컬럼 복구 직후 52행을 보고 «원본 재현» 이라고 보고했는데, 지금 재현이 안 된다:
어휘집 enum(10종)은 08-30 이후 불변인데 결과는 **73행**이다. 어떤 조건으로도 52 가
안 나온다. 분류기가 사람이 비워둔 것까지 매핑하기 때문으로 보인다.
보증되는 것은 «CSV 원문에서 결정적으로 재생산된다(불일치 0)» 뿐이고,
«손으로 채운 원본과 같다» 는 **측정된 적이 없다.** 도크스트링에 그대로 적어 못박았다.

**f-string 안에 `\n` 을 실제 개행으로 넣으면 `SyntaxError` 다.** heredoc 으로 패치를
밀어 넣을 때 나온다 — 오늘 저장소에서 3건(`orchestrate.py`·`normalize_run.py`·
`load_db.py`). `load_db.py` 는 import 조차 안 됐다: **임베딩 2만 건을 지키려던 가드가
「문법이 깨져서 아무것도 못 지운다」는 형태로만 작동하고 있었다.** 소유 세션이 수리.

**비추적 파일은 삭제하지 않고 격리했다** — `git checkout` 으로 되돌릴 수 없다.

### 남은 것
- 재생성 산출물 169MB — ai-25 의 GPU E2E 완료 후 오너 판단
- 🔴 **CLAUDE.md 의 `index_guard` 진입점 서술이 낡았다.** 실제 호출부는
  `load_db.py:71` 인데 `stage0_ingest.py` 라고 적혀 있다. `RAG.md` 사본만 고쳤다
  (CLAUDE.md 는 이 세션이 편집하지 않는다)
- `kosmes_faq.json` 194건 · `_부정행위사례집_OCR.json` 42건 — 미투입 확정, 원본은 보존

### 산출물
```text
db/init/04_agent.sql          강등코드 CHECK 18 → 22종 (라이브 DB 동일 적용)
scripts/load_evidence.py      _패키지_해소기() · 파생컬럼_채우기() · --map-apply
scripts/audit_db.py           refs.dst 미해소 기대치 42 → 0 (개선이 🔴 로 보고되던 계약)
공유받은 파일/증빙서류 종합.csv   #125 교육 이수 계획서 (백업 .bak)
RAG.md                        §2 B급 행 실측 정정 · index_guard 진입점 정정
scripts/build_index.py        오삭제 → 복구
```

---

## 2026-09-01 · 레인 C — normalize 폼 경로 + L3 업로드 접수

`POST /api/normalize` 가 폼 8필드를 실제로 소비하게 만들고, `routes_l3.py` 의
`_실_업로드`/`_실_상태` 를 채웠다. 2시간 병렬 레인(B/A/C) 중 C.

### 입력
`server/routes_l3.py`(NotImplementedError 스텁 2개) · `server/main.py::_실_정규화`
(질문만 소비, 폼은 무시하던 상태) · `server/models.py` §①(계약, 변경 안 함) ·
`db/init/01_schema.sql:363-393`(`tenant.l3_documents`/`l3_articles` DDL).

### 판단
- 폼 경로는 `normalize_run.정규화()` 를 **호출하지 않는다** — 품목·금액·용도가
  이미 구조화돼 왔다. 자연어 경로만 LLM 추출을 탄다 (분기는 `body.질문` 유무).
- `질문원문` 합성은 `main.py::_합성_질문()` 신설, `routes_plans.py::_합성` 과
  동일 문장 규칙(`"{사업명}에서 {용도} {품목} {만원}을 사도 되나요?"`). MOCK·실호출
  양쪽 다 `gen()` 안에서 `out.setdefault("질문원문", ...)` 로 공통 처리 — 두 경로가
  각자 채우면 문장 규칙이 갈라진다.
- `l3_documents.파싱품질` 이 NOT NULL + CHECK(pass/warn/fail) 인데 '대기' 값이
  없다. 업로드 시점엔 평가 불가 — 'warn'(미평가) 을 임시로 넣고, API 응답의
  `상태`(파싱대기/완료/실패)는 이 컬럼이 아니라 **`l3_articles` 행 존재 여부**로
  파생한다(0건=파싱대기, >0=완료, 파싱품질='fail'=실패). 스키마에 진짜 '대기'
  컬럼을 추가하는 대안은 기각 — `db/init/` 은 소유 파일 밖(레인 C = routes_l3.py·
  main.py·tests/2개 뿐), 조율 세션에 보고 대상으로 남긴다.
- `기관명`(업로드 폼 필드)은 저장하지 않는다 — `tenant.orgs.기관명` 이 정본이고
  `orgs` 는 E 세션 소유. 이 레인이 orgs 에 쓰면 소유 경계를 넘는다.
- dangling 상세(조·참조·사유)는 항상 `[]` — 저장할 테이블이 없다. 기존 주석
  ("자리만 만들어 둔다, 채우기는 파서 연결 후")대로 자리만 유지.

### 실측
`PYTHONIOENCODING=utf-8 python server/main.py --selftest` 34개 항목 전부 통과.
`pytest tests/test_normalize_form.py tests/test_l3_upload.py -q` 10 passed.
`pytest tests/ -q` 는 `test_index_guard.py::test_allowed` 1건 실패 — 이 레인
변경 이전부터의 실패(`scripts/index_guard.py` 소유 밖, 미확인 상태로 남김).

### 함정
`_질의()`(`server/_common.py`)는 SELECT 전용처럼 보이지만 `with psycopg.connect
(...) as conn:` 컨텍스트가 성공 종료 시 커밋한다 — `INSERT ... RETURNING` 도
그대로 써서 `doc_id` 를 받았다. `_실행()` 은 rowcount 만 필요할 때 쓴다.

### 남은 것
- `l3_documents`/`l3_articles` DB 실경로는 미검증 — DSN 없이 로컬 실행, `_질의`
  가 예외를 삼켜 빈 리스트를 주므로 업로드는 항상 400("org_id 확인" 문구)로
  떨어질 것. 실 DB 붙는 세션에서 재확인 필요.
- 폼 경로의 `비목후보` 는 항상 `[]` — LLM 추출을 안 타므로. 화면 9 는 사용자
  직접 확정 경로가 있어 막히지 않는다(설계상 의도).
- `파싱품질` NOT NULL 갭은 스키마 소유 세션 보고 대상 (위 판단 참조).

### 산출물
`server/routes_l3.py`(`_실_업로드`·`_실_상태` 구현) · `server/main.py`
(`_합성_질문` 신설, `_실_정규화` 분기, `normalize()` gen() 에 질문원문 주입) ·
`tests/test_normalize_form.py`(신규 5건) · `tests/test_l3_upload.py`(신규 5건).

---

## 2026-09-01 · 별칭 유출 실측 · 검수요청 확장 · 조립기 연결 패치

세 건 다 **결정을 내리지 않고 재료만 냈다.** 골든셋 유출 판정은 평가 설계, verified 승격은
오너 검수, `orchestrate.py` 는 다른 세션 소유다.

### 입력
- ai-25 지시: item_alias 골든셋 유래 25건을 «그 별칭이 없으면 그 문항이 틀리는가» 로 실측.
  분모는 새 `eval_store.평가대상()` **62문항** (골든셋 77 → 84, 범위밖 16 제외)
- r486 을 검수요청 0순위로 · 큐 잔여 9행에 판단 항목 한 줄씩

### 판단

**(1) 별칭 기여 측정은 트랜잭션 롤백으로 한다**
- 별칭 1건 DELETE → `rule_lookup.비목확정(상품명, None)` 재실행 → `ROLLBACK TO SAVEPOINT`.
  커밋 없음. `corpus.item_alias` 228행 불변.
- 기각한 대안: 별칭을 실제로 지우고 재측정 — 8세션이 한 DB 를 쓰는 날이라 남의 판정이
  그 창에서 달라진다.

**(2) «삭제하라» 고 결론내지 않았다 — 실측 둘이 그걸 막는다**
- `변리사 수수료` 는 제거하면 벡터가 `지급수수료`(0.81)를 집는다. **틀린 비목**이다.
  없는 비목보다 나쁘다 (`rule_lookup` 이 임계 0.75 를 고른 근거와 같은 논리).
- 7개 용어를 `doc_articles` 본문에서 역으로 찾아봤다: `금 도금`·`MS Office`·`KTX` 0건,
  `책상`·`의자` 는 창업지원과 무관한 타 법령, `맥북` 은 사례집(판단불가 경로 전용).
  → 이 별칭들은 **커닝이면서 동시에 진짜 어휘 결손을 메우고 있다.** 지우면 그 7문항이
  판단불가로 떨어지는데, 지표 하락 원인이 «모델» 인지 «어휘» 인지 숫자로 안 갈린다.

**(3) `출처` 번호 9건은 범위 판정을 «못 했다» 로 남겼다**
- `골든셋51·52·54·60·62·72·75·77` 이 현재 `golden_set` 에 없다. 본세트 `no` 1~50 ·
  적대적 `A1~` · 공식 `별첨4-` 다. 별칭을 뽑던 시점의 77문항 구 번호로 보인다.
- 🔴 추정으로 «채점 밖» 이라고 적지 않았다. 구↔신 대응표가 있어야 판정된다.

**(4) programs 상한 3컬럼 — 채우지도 지우지도 말자**
- 근거가 코퍼스에 없다(공고문 축). 채우면 추측이고, 금액 비교는 코드가 하므로
  틀린 상한 = 틀린 판정이다. 읽는 코드는 현재 0개라 지금 함정은 아니다.
- `상한_근거 JSONB NOT NULL DEFAULT '[]'` 라는 «근거 없이 못 채우는» 구조가 방어다.
  컬럼을 지우면 그게 사라진다. 위험 시점은 프론트가 이 값을 쓰기 시작할 때 —
  «NULL 이면 상한을 화면에 표시하지 않는다» 를 API 계약에 못박아야 한다.

### 실측
- 골든셋 유래 별칭 25건 → 기여 있음 **11** · 기여 없음 14
- 그중 **현재 채점 62문항에 닿는 것 7건**: 금 도금 · 맥북 · MS Office · 책상 · 의자 ·
  변리사 수수료 · KTX. 6건은 제거 시 결과 **없음**, `변리사 수수료` 는 **틀린 비목**
- 기여 없음 사례: `사무실 임대료` 는 벡터가 오히려 더 높다 (alias 0.95 → vector 0.97)
- 미검수 19행 커버리지 최저 4행 r480 0.223 · r478 0.347 · r486 0.456 · r417 0.462

### 함정
- `eval.golden_set.no` 는 **세트마다 체계가 다르다** (`1~50` / `A1~` / `별첨4-`).
  숫자만 보고 문항을 이으면 조용히 어긋난다. 실제로 9건이 안 붙었다.
- `verify_rules.py --json` 은 파일을 남기지 않았다 (원인 미확인). 콘솔 출력으로 갈음했다.

### 남은 것
- 별칭 7건 처분 A/B/C — 오너
- 큐 잔여 9행 · 미검수 19행 서명 — 오너
- 🔴 **조립기 연결이 아직 안 붙었다.** 붙어야 층 B 가 최종 E2E 수치에 나타난다

### 산출물
```text
scripts/_work/_별칭유출_실측_0901.md    유출 실측 + 안 A/B/C
scripts/_work/_별칭유출_실측.json       별칭별 전/후 비목확정 원본
scripts/_work/_룰검수요청_0901.md       r486 0순위(VLM 상한 B 명시) + 큐 잔여 9행 판단 항목
scripts/_work/_큐잔여9행.json
scripts/_work/_층B_조립기연결.patch.md  orchestrate.py 패치 2곳 (ai-25 소유라 내가 안 침)
```

---

---

## 2026-09-01 · recheck_queue 정리 — 죽은 포인터 306 · 개정 오탐 160

큐 대기 515 → 355. `corpus.rules` 는 한 행도 고치지 않았다 (72행 / 미검수 19행 불변).

### 입력
- 배정: ai-25 (병렬 세션) — 「판정 룰 레인」. 우선순위 1(BASIS_DELETED) · 4(BASIS_RENUMBERED 168)
- 브리핑 전제: "BASIS_DELETED 1건 = 근거 조가 사라진 룰. 제일 위험. 개별 확인 후 처리"

### 판단

**(1) 브리핑 전제가 성립하지 않았다 — 큐의 rule 포인터가 전부 죽어 있었다**
- 실측: `rules.rule_id` 417~488 (72행) vs 큐 `대상id` 344~415 — **교집합 0**. rule 306행 전부.
- 원인은 `corpus.rules` 의 TRUNCATE RESTART IDENTITY 재적재. `recheck_queue` 는 FK 가 아니라
  참고값이라 조용히 죽는다 (`agent_a4.py` 치유() 주석이 이미 예고한 상황).
- 조치: `agent_a4.py --source db` 재실행(483 upsert) → `--heal-only`. 296행 삭제, 죽은 포인터 0.

**(2) 치유() 에 갈래 ⓪ 재지정 신설**
- 고아 10행이 전부 A2(STRICTER_L3 7)·A5(ORG_ANSWER 3) 였다. **A4 가 다시 발행하지 않으므로
  영원히 쌍둥이가 안 생긴다** → 기존 코드였다면 "근거가 사라졌다" 로 10행 전부 기각됐다.
  사라진 건 근거가 아니라 대리키다.
- 10행 전부 `(사업명, 비목)` 이 현재 룰 **1건**에 유일하게 붙었다. 2건 이상이면 재지정하지
  않는다 — 틀린 좌표는 조용해서 기각보다 나쁘다.

**(3) BASIS_DELETED 1건은 오탐**
- r486(재도전·여비)의 근거는 2026판 제17조이고 그 조제목이 「여비」다. 실재한다.
- 오탐 원인: 2025판 제17조의 `조제목` 이 `(예비` 로 잘려 있어 제목매칭이 실패 → '삭제' 로 분류.
  (2025판은 superseded·index_target=false 라 판정에는 안 쓰인다. 파싱 결손만 기록해 둔다)

**(4) BASIS_RENUMBERED 168 중 159가 같은 계열의 오탐 — 조제목이 판별자다**
- `영향_레코드` 는 `_근거조회(신doc, 구조번호)` 로 «신판 문서 + 구판 번호» 를 **일부러** 잡는다.
  그런데 그 조합은 «개정 후 근거를 새 좌표로 정정한 경우» 와 문자열상 구별되지 않는다.
- 판별자: 룰이 적어둔 좌표의 **조제목이 그 룰의 `비목`** 이면 의도된 인용이다.
  「제16조 인건비」를 근거로 둔 인건비 룰이 그렇다. `조정()` 에 갈래 ③ 으로 넣었다.
- 🔴 처음엔 이동 **목적지**(`조번호` 컬럼)의 제목과 비교해 «일치 3 / 불일치 166» 이 나왔다.
  룰이 실제로 적어둔 좌표는 `구조번호` 쪽이다. 컬럼 의미를 코드로 확인하고 나서야 뒤집혔다.
- 🔴 공백·가운뎃점을 접어야 한다. `특허권등무형자산취득비`(비목) 와
  `특허권 등 무형자산 취득비`(조제목)은 같은 말이다 — 안 접으면 11행을 놓친다.

**(5) verified 승격은 치지 않았다 — 검수요청까지만**
- 계약서 §5 가 검수를 오너 예외로 둔다. 기계가 서명하면 `UNVERIFIED_RULE` 게이트가
  그 19행에서 무력화된다. G 선례(`_G_룰검수요청.md`)와 같은 형식으로 재료만 냈다.
  ai-25 가 이 선긋기를 승인했다.

### 실측
- 큐: 518행 유지 · 대기 515 → **355**. BASIS_RENUMBERED 대기 168 → **9**, BASIS_DELETED 1 → 0
- 남은 9행은 전부 일반조 근거(`제36조 창업기업등 사업비 비목`·`제47조 사업비 집행`·`제30조`).
  비목이 NULL 이거나 조제목이 비목명이 아니라 **기계로 판별 불가** — 사람 몫
- 미검수 19행 기계 게이트: 근거 조문 실재 19/19 · 인덱스 실재 19/19 · 허용값 전부 «조건부»
  로 타 사업과 일치 · 골든셋 역검증 재실행이 G 원본과 동일(치명 0 · 안전측 과잉 1 · 미커버 3)
- 커버리지 최저 4행: r480 0.223 · r478 0.347 · r486 0.456 · r417 0.462
- 회귀 불변: 층 B `--self-test` 11/11 · `score_judgment` 치명 0 · 일치율 84.7%

### 함정
- **`verify_rules.py` 가 `사업명 IS NULL`(L1 공통 9행)에서 TypeError 로 죽고 있었다.**
  사업별 평균을 찍는 f-string 이 `None` 을 포맷하는데, 그 자리가 «임계 미만 문장» 목록
  **직전**이라 검수 재료가 통째로 안 나왔다. 요약만 보면 정상 종료처럼 보인다.
- `recheck_queue.uq_recheck_key` 에는 `대상id` 가 들어가고 치유의 `_불변열쇠` 에는 안 들어간다.
  그래서 재실행이 «쌍둥이» 를 만들 수 있다 — 둘이 같았으면 upsert 가 기존 행을 덮어
  죽은 포인터가 영원히 남았을 것이다.
- 재지정 UPDATE 는 uq 충돌을 **미리 SELECT 로 본다.** 예외로 잡으면 앞의 DELETE 만 커밋되고
  뒤가 안 돈다.

### 남은 것
- BASIS_RENUMBERED 9행 · BASIS_AMENDED 264행 · ITEM_VOCAB_RECHECK 57행 — 사람/후속
- 미검수 19행 서명 (오너) · `agent_a4.py --source xml` 경로는 안 돌렸다
- item_alias 골든셋 유래 25건 · programs 상한 3컬럼 — ai-25 대기
- 증빙 1종 `교육 이수 계획서(교육일정 포함)` — 오너 결정 (`_rules_검수_작업기록.md:135` 기등록)

### 산출물
```text
scripts/agent_a4.py
  치유()  갈래 ⓪ 재지정 신설 — (사업명,비목)이 현재 룰 1건에 유일하게 붙으면 대상id 재지정
  조정()  갈래 ③ 조제목 대조 신설 — 근거 좌표 조제목 ⊇ 비목 이면 «의도된 인용» 으로 '반영'
scripts/verify_rules.py   사업명 NULL 크래시 수정 (L1공통 라벨)
scripts/_work/_룰검수요청_0901.md    미검수 19행 검수요청 (기계 게이트 + 읽을 순서)
scripts/_work/_검수재료_0901.json    행별 근거 좌표·실재·커버리지 원본
corpus.recheck_queue  상태만 변경 (반영 160 · 대상id 재지정 10 · 중복 삭제 296)
```

---

---

## 2026-09-01 · 층 B 검증기 — 해야할일 설명 환각 대조 4종

`해야할일[].설명` 이 아무 검사 없이 화면으로 나가던 경로를 닫았다. 인용(S번호)·항목(code)은
이미 폐쇄 목록으로 닫혀 있었고 설명만 무검사였다.

### 입력
- 요구: `프로토타입_해부_구현명세.md` §2-6 «환각 위험 — 게스트는 F축이 비어 있다.
  그때 "현재 사용자 상태가 ~~니까" 를 쓰면 없는 상태를 지어낸다»
- 대상: `scripts/llm_validate.py` §5-b (기존 검사는 `code` enum 뿐)
- 소유권 실측: `llm_schema.py`·`orchestrate.py`·`stage2_chunk.py`·`01_schema.sql` 이
  같은 워킹트리에서 다른 세션에 열려 있었다 (14:09~14:22 mtime · interactive 3세션 busy).
  `llm_validate.py` 만 비어 있어 여기부터 쳤다.

### 판단

**(1) 프롬프트 가드가 아니라 검증기로 막는다**
- §2-6 은 "프롬프트에 못박을 것" 으로 적었다. 프롬프트는 요청이지 검사가 아니다.
- 기각: 프롬프트 문구만 강화 — 검사되지 않는 요청은 지표에 안 잡힌다.

**(2) 처분은 «항목 폐기» 가 아니라 «설명 떨어뜨리기»**
- 항목·`code` 는 남긴다. 화면이 `code` 로 `check_items` 마스터의 정적 설명을 조회하므로
  (§2-4) 설명이 비면 판정 이전의 고정 문구로 폴백한다.
- 기각: 항목까지 폐기 — 룰이 고른 확인항목이 사라져 오히려 위험하다. 설명이 나쁜 것과
  항목이 틀린 것은 다르다.

**(3) `f사실=None` 과 `f사실={}` 을 가른다**
- `None` = 조립기 미전달(모름) → 무발효 · `{}` = F축 없음(게스트) → 최대 강도.
- 뭉치면 조립기가 인자를 빠뜨린 순간 게스트 가드가 조용히 꺼진다.
  `체크코드_enum` 이 "사업명 없으면 공통만" 으로 좁힌 것과 같은 계열.

**(4) 숫자 화이트리스트는 프롬프트 전문에서 만든다**
- 설명의 숫자 토큰이 B0~B6 전문에 문자열로 없으면 지어낸 것이다. 한도·기한이 이 경로로 샌다.
- 만/억 전개는 한 방향만 연다 (원천 `5,000,000` → 설명 `500만` 은 통과, 역방향은 불통).
  넓히면 환각이 통과하고, 좁아서 나는 오폐기는 정적 문구 폴백이라 안전하다.

**(5) 자유문 모드는 패턴, 구조 모드는 필드**
- 층 A(상태절 필드 분리)가 아직 없어 현재 스키마는 `설명` 자유문이다. 문장에서 절만
  떼어낼 수 없으므로 상태어구 정규식이 걸리면 설명 전체를 떨군다. 층 A 가 붙으면
  `상태절` 키 유무로 갈려 패턴은 안 쓰인다 — 구조가 패턴보다 정확하다.

### 실측
- 자기검증 11건 전건 통과 — `python scripts/llm_validate.py --self-test` (DB 커넥션 0)
- 판정 회귀 불변 — `score_judgment.py --in scripts/_work/_judge_d6.jsonl`
  치명 오답 0 · 일치율 84.7%(61건) · 인용 정확도 100%. 변경 전과 같다
- `orchestrate.검증 is llm_validate.검증` → True (import 경로 정상)
- 🔴 **실전 판정에서의 발효 건수는 0이다** — 조립기가 아직 인자를 안 넘긴다 (아래 남은 것)

### 함정
- **`04_agent.sql` 의 `decisions_강등코드_check` 가 18종 목록이다.** 새 4종이 실리면
  INSERT 가 거부된다. `_마무리` 가 기록 실패를 삼키므로 **판정은 살고 decisions 행만
  조용히 유실된다** — 발효 전에 CHECK 를 22종으로 늘려야 한다. DDL 은 스키마 소유자 결정.
- `llm_validate.py` 상단에 `import re` 가 없었다 (`_RE_조` 가 `__import__("re")` 로
  우회하고 있었다). 정식 import 를 추가했다.
- 자기검증을 DB 없이 짰다. 8세션이 한 DB 를 쓰는 날 회귀가 커넥션을 잡으면 남의 작업과 엉킨다.

### 남은 것
- **조립기 한 줄** — `orchestrate.py` 가 `f사실`(B5 값 dict)·`프롬프트` 를 넘겨야 켜진다.
  `b5_문장()` 은 문자열만 만들어서 값 dict 경로가 함께 필요하다. 그 파일은 오늘 다른
  세션이 열어 두어 손대지 않았다.
- **DDL 1건** — 위 CHECK 22종.
- 층 A(스키마 갈래) · 층 C(잘림 이름) · 층 D(게스트 대조군) 미착수. 명세는 §2-7.
- 층 D 는 재도전2026 인덱스 교체가 끝난 뒤에 돌린다. 코퍼스 버전이 바뀌는 중이라
  지금 돌리면 하네스 효과와 교체 효과가 한 run 에 섞인다.

### 산출물
```text
scripts/llm_validate.py
  검증(..., f사실: dict|None = None, 프롬프트: str = "")   <- 키워드 2개 추가. 위치인자 2개 불변
  _숫자들 · _출처숫자 · _설명검사 · _설명제거              <- 신설
  _self_test()  ·  --self-test 플래그                     <- 신설 (DB 불필요)
  강등코드 18 → 22종
    TASK_STATE_UNSOURCED   F축 없이 상태 서술    -> 상태절(자유문이면 설명) 폐기
    TASK_STATE_MISMATCH    상태절 숫자가 F축 밖  -> 상태절 폐기
    TASK_NUMBER_UNSOURCED  설명 숫자가 프롬프트 밖 -> 설명 폐기
    TASK_BASIS_NOT_IN_MAP  근거S 가 s맵 밖       -> 설명 폐기 (층 A 이후 자동 발효)

프로토타입_해부_구현명세.md §2-7  하네스 4층 (A 디코딩 · B 검증 · C 길이 · D 대조군)
```

---

## 2026-09-01 · 골든셋 정합 — 외부 정답지 편입 · 채점 분모 복원

골든셋 77 → 84문항. 채점 분모 74 → **62**. 그동안 «검색 성능» 으로 보고되던 미스의
큰 덩어리가 검색기 문제가 아니라 **분모 문제**였다.

### 입력
- `2026_Finance_DATA_FOR_RAG/_골든셋/L3_2025초기창업패키지_주요질의응답집_별첨4.pdf`
- `_골든셋/_골든셋_초안.json` 메타 · `_골든셋_스테이징/_골든셋_확정본.json`
- ai-79 실측: hit@5 미스 33건 중 «필터밖» 23건(31.1%)

### 판단

**(A) 별첨4 공식 7문항 편입 — 골든셋에 외부 정답지가 0건이었다**
- 초안 메타가 «이 24문항은 창진원 공식 답변이 아니다. **별첨4 7문항만이 공식 정답지다**»
  라고 적어 놨는데, 초안 50문항 출처 분포는 `{세부관리기준 24, 통합관리지침 26}` — 별첨4 0건.
  PDF 가 폴더에만 있고 **한 번도 문항이 된 적이 없다.** 77문항 전부가 자작·자가검수였다
  (검수자 77/77 = `Claude (오너 위임, 2026-08-31)`).
- 편입 대상은 「[ 사업비 ]」 절 11문항 중 **판정형 7문항**. 나머지 4문항(인건비 산정 방법,
  비목 간 비율, 총사업비 구성, 자기부담사업비 정의)은 절차·구성 설명이라 가능/불가/조건부로
  환원되지 않는다. 7이라는 개수가 메타의 «7문항» 과 일치한다.
- 🔴 **질문·정답을 우리 말로 다듬지 않았다.** 다듬으면 «공식 정답지» 라는 성격이 사라진다.
  판정은 공식 답변의 첫 낱말(☞ 불가합니다 / ☞ 가능합니다) 을 그대로 따랐다.
  별첨4-5(인건비)·별첨4-8(광고비)는 조건이 붙어 우리 스키마로는 '조건부' 가 자연스럽지만
  **일부러 '가능' 으로 뒀다** — 시스템이 과잉 보수로 답하면 그건 잡혀야 할 오답이다.
- `채점모드='공식'` 으로 갈랐다. 자작 문항과 섞어 재면 «외부 정답지 대비 일치율» 을 못 뽑는다.

**(B) 채점 축 3개 복원 — 설계는 이미 결정돼 있었고 적재가 떨어뜨렸다**
- 초안 메타: «창업팀 전용 전환을 확정했다. 따라서 대상='주관기관' 16문항은 평가 범위 밖».
  JSON 에 `대상`·`평가범위`·`채점모드` 축이 있는데 **`load_db.py` INSERT 컬럼 목록에 없어
  DB 로 안 넘어갔다.** `eval_store.평가대상()` 은 «골든청크 있는가» 만 걸러 전건을 돌렸다.
- 그 16문항은 근거가 주관기관 운영비 조인데 검색 필터가 `적용대상 IN ('창업기업','공통')`
  이라 **구조적으로 hit 불가**다. 리랭커·가중·임베딩 무엇으로도 안 움직인다.
  분모에 두면 **영원히 못 넘는 벽이 «검색 성능» 으로 보고된다.**
- `범위밖포함=True` 인자를 남겼다 — 센터 화면이 되살아날 때의 문이다. 기본은 제외.
- 적대적 27문항은 `평가범위` 가 비어 있었다(초안 축이 본세트 기준). 전부 대상='창업기업'
  이고 치명오답 측정용이라 '주력' 으로 채웠다. NULL 로 두면 호출부마다 다르게 센다.

**(C) gold A24 근거 오염 정정**
- **예비창업패키지 문항인데 정답근거에 「모두의 창업 프로젝트」 별지4**가 들어 있었다.
  남의 사업 규정을 정답으로 지정하면 인용 정확도 채점이 오답을 정답으로 만든다.
- 조번호 표기도 틀렸다: `제22조(사업비 집행) 제1호` → 실제 조제목은 `창업기업등 사업비 비목`.
  올바른 근거는 예비창업 제22조①1호(동일 거래업체 누적 2천만원 사전심의).

**(D) 🔴 `load_db.py` 에 청크 파괴 가드**
- `load_db.py` 는 `TRUNCATE corpus.documents CASCADE` 로 시작하는데 `corpus.chunks` 가
  documents 를 FK CASCADE 로 물고 있다. **골든셋 한 줄 고치려고 돌리면 임베딩 20,518건이
  같이 날아간다.** 에러가 안 나므로 다음 검색이 0건 나올 때까지 모른다.
  → `corpus.chunks` 가 비어 있지 않으면 `--청크삭제승인` 없이는 멈추게 했다.
  골든셋 전용 경로로 `_work/_골든셋_재적재.py` 를 따로 뒀다 (적재 후 청크 수 불변을 assert).

### 실측
- 문항 77 → 84 (본세트 50 · 적대적 27 · 공식 7). 공식분 판정 분포 `{불가 5, 가능 2}`
- 축 분포: 대상 `{창업기업 67, 주관기관 16, 공통 1}` · 평가범위 `{주력 67, 범위밖 16, 유효 1}`
  · 채점모드 `{full 72, 공식 7, 치명오답만 5}`
- `pin_golden_chunks --재고정`: 정답청크 고정 **78 / 84**
- **채점 분모 74 → 62** (본세트 34 · 적대적 21 · 공식 7)
- 사업 커버리지: 예비창업 26 · 공통 26 · **초기창업 7(신규)** · 창업도약 2 · 초격차 1
  → 초기창업패키지가 0문항에서 7문항이 됐다 (전부 외부 공식 정답지)

### 함정
- `golden_set.사업명` 은 `programs.사업명` 을 FK 로 문다. JSON 의 `사업` 에는
  `공통(지침 제14차)` 처럼 **사업이 아닌 값**이 섞여 있어 그대로 넣으면 적재가 통째로 죽는다.
  `eval_store.사업키()` 와 같은 규약으로 접어야 한다 — 사업명 NULL, 원표기는 `적용범위` 로.
- `TRUNCATE eval.golden_set CASCADE` 는 `golden_chunks`·`run_items` 를 같이 지운다.
  **재적재 후 `pin_golden_chunks --재고정` 을 반드시 돌린다** — 안 돌리면 `평가대상()` 이
  0건을 돌려주고 평가가 조용히 빈다.

### 남은 것
- 🔴 **코퍼스 결손 7문항** (`pin_golden_chunks` 가 잡았다. ai-79 레인으로 넘김):
  · 「중소기업창업 지원사업 운영요령(제2024-101호)」 **청크 0건** — gold 133·141·148.
    `documents` 에는 있는데 `chunks` 가 0이다. 통합관리지침 제47조①·제49조① 이
    «운영요령 제21조에 따라» 로 직접 위임하는 상위 고시라 refs 폐포도 여기서 끊긴다
  · 부속물 미적재 — 모두의창업 `별지4`(gold 135·136·151) · 국고보조금지침 `별표1`(gold 153)
- 사업 커버리지 여전히 0인 곳: 재도전성공패키지 · 창업중심대학 · 모두의창업 · TIPS
- L3 문항군 0건 — `org_id` 를 넘기는 골든셋 문항이 아직 없다. 판정엔진 L3 경로는
  여전히 골든셋으로 평가되지 않는다 (ai-40 이 픽스처로 경로 발동만 실측 중)
- 별첨4의 나머지 4문항(절차·구성 설명형)은 편입하지 않았다. 판정형이 아니다

### 산출물
- `scripts/_work/_골든셋_정합_0901.py` (신규) — A·B 적용
- `scripts/_work/_골든셋_재적재.py` (신규) — corpus 를 건드리지 않는 골든셋 전용 적재
- `scripts/load_db.py` — 골든셋 축 3개+비목 적재 · 🔴 청크 파괴 가드
- `scripts/eval_store.py` — `평가대상(범위밖포함=False)`
- `2026_Finance_DATA_FOR_RAG/_골든셋_스테이징/_골든셋_확정본.json` — 84문항 + 변경이력
- DB: `eval.golden_set` + `대상`·`평가범위`·`채점모드` (컬럼 주석 포함)

## 2026-09-01 · DB 감사 3건 — 판본 역전 · enum 누수 · 중복 컬럼

오너가 테이블 9개를 실측 감사해 지적한 것 중 3건을 고쳤다.

### 입력
- 지적: ① `check_items.사업명 NULL` ② `documents.parse_quality=low`·`roles` ③ 재도전 구판 인덱스
- 실측 근거: `docker exec suddoe-db psql` 직접 조회 (아래 수치 전부 그 출력)
- 선행: `scripts/_work/_G_재도전2026_적재.py` 가 `doc_articles`·`status` 까지만 돌려놓은 상태

### 판단

**(1) `llm_schema.체크코드_enum()` 에 사업 필터 추가**
- 증상: `SELECT code FROM corpus.check_items ORDER BY code` — 필터가 없어 52개가
  통째로 guided_json enum 에 들어갔다. 예비창업 판정에 LLM 이 `기장대행한도_재도전` 을
  고를 수 있었다. 인덱스는 `(사업명,비목,구분)` 로 만들어 놓고 그 키로 조회하지 않았다.
- `WHERE 사업명 IS NULL OR 사업명 = %s`. `orchestrate.py` 호출부가 `사업명` 을 넘긴다.
- 🔴 **인자를 안 주면 공통 38개만 준다.** 전체를 기본값으로 두면 호출자가 인자를
  빠뜨린 순간 조용히 예전 버그로 돌아간다. 항목이 모자란 쪽이 남의 사업 항목을
  제안하는 것보다 안전하다.

**(2) 재도전성공패키지 인덱스 판본 교체 — 원인은 4중이었다**
"2026판이 `index_target=false`" 는 증상이고, 실제로는 stale 상수 3개 + 태깅 결손이었다.
- `load_db.py::현행` / `tag_apply_target.py::현행` / `stage2_chunk.py::사업_of_doc`
  **셋 다 2025년판을 가리켰다.** 하나만 고치면 다음 재적재에서 조용히 되돌아간다.
- `_stage0_articles.json` 의 2026판이 `articles=0` — Stage 0 이 스캔본을 못 읽었고,
  VLM 판독은 DB 에만 직접 들어가 산출물에 반영되지 않았다. 태거가 이 파일을 읽으므로
  태깅이 0건이 됐고, 태깅 없는 조는 `stage2_chunk` 가 "미결컷" 으로 버린다.
- 그 결과 33조 중 11조가 미결이었는데 **그 11조에 사업비 비목 6조가 전부 들어 있었다**
  (회의비·여비·지급수수료·외주용역비·기계장치·광고선전비). `corpus.rules` 의 재도전
  9행이 근거로 삼는 조가 정확히 그것들이다.
- 원인: `절_상속()` 이 절 헤딩을 **앞 조 본문 꼬리**에서 긁는다. 조문 재조립의
  부산물에 기댄 방식이라, 조 단위로 정제돼 오는 VLM 판독본에는 꼬리가 없다.
  판독본은 대신 `장` 을 명시로 들고 온다 (`제5장 사업비의 구성 / 제2절 (예비)재창업자`).
  → `절_상속()` 이 명시 `장` 필드를 우선 읽게 고쳤다. 판정 규칙은 하나(`절값()`)만 둔다.
- `parse_quality` 를 low→high 로 올리고 `extraction='vlm'` 은 유지했다.
  대안(둘 다 low 유지)을 기각한 이유: 검색 필터가 `parse_quality='high'` 라
  low 면 인덱스에 넣어도 안 잡힌다. 두 플래그는 다른 일을 한다 —
  `parse_quality`=추출을 믿을 만한가(검색 진입 자격), `extraction`=어떻게 뽑았나(인용 등급).
  `VLM_DOWNGRADE` 는 그대로 발화해 A등급 인용을 막는다. 판정은 되고 등급만 B다.
- `stage2_chunk.py` 전체 재실행(20,525청크 TRUNCATE + GPU 팟)을 기각하고 그 문서만
  갈아 끼웠다. 청킹 규칙이 갈리면 안 되므로 `stage2_chunk` 함수를 import 해서 썼다.
  임베딩은 CPU 28건 36초 — 팟을 열 이유가 없다. `normalize_embeddings=True`·`MAX_SEQ=1024`
  는 본 파이프라인과 동일. GPU 는 fp16, 여기는 fp32 — 정규화 벡터라 무시할 차이.

**(3) `documents.roles` 컬럼 삭제**
- `load_db.py` 가 `["judgment_index"] if index_target else []` 로 넣었다.
  교차 확인: `roles @> {judgment_index} AND index_target=false` → **0건**. 완전한 복사본.
- 스키마 주석이 예고한 `rule_source`·`diff_only` 를 쓰는 코드는 끝내 없었다.
- 대안(제대로 채우기) 기각: 지금 그 값을 읽는 코드가 하나도 없다. 소비자가 생길 때
  다시 만드는 게 맞다. 같은 사실이 두 컬럼에 있으면 필터를 어디에 걸지가 매번 흔들린다.
- `stage0_ingest.py` 의 **매니페스트 `role` 은 남겼다** — 그건 라우팅(`NEEDED_ROLES`)과
  골든셋 격리에 쓰이는 별개 개념이다. DB INSERT 에서만 뺐다.

### 실측
- `체크코드_enum`: 공통 38 · 예비창업 40 · 재도전 41 · 초격차 41 (사업 전용 code 가 안 섞인다)
- 태깅: 결정률 97%→99% · 미결 11조→5조. 사업비 비목 6조가 전부 `창업기업` (2025판과 일치)
- 청크: 재도전 2025판 35 → 2026판 28 (적용대상 창업기업 16 · 주관기관 12)
  · 토큰 최대 428 평균 213 · 1,024 초과 0건
- 전체 청크 20,525 → 20,518 · `embedding IS NULL` 0건
- BM25 재적재: `chunk_terms` 1,148,718 · 어휘 16,491 · 평균길이 120.4
- `smoke_search.py`: "홍보용 기프티콘 뿌려도 되나요?" → **2026판 제21조(광고선전비) top-1 (0.580)**
- `orchestrate.판정(재도전, dry)`: `b4(룰)=True` · 비목=지급수수료 · 경로 `1정규화+3검색+4조립`
  (교체 전에는 이 사업이 구판 청크로만 돌았다)

### 함정
- `stage2_chunk`·`stage2_bm25`·`tag_apply_target` 이 **각자 모듈 최상단에서**
  `sys.stdout = io.TextIOWrapper(sys.stdout.buffer, ...)` 를 한다. 둘 이상 import 하면
  두 번째 래퍼가 첫 번째를 밀어내고, 참조를 잃은 첫 래퍼가 GC 될 때 fd 1 을 닫는다 →
  이후 모든 `print` 가 `ValueError: I/O operation on closed file`. 래퍼를 리스트에
  보관해 살려 둬야 한다.
- `chunks.사업명` 은 `TEXT[]` 다. 문자열을 넣으면 `malformed array literal`.
  정본 매핑은 `stage2_chunk.사업_of_doc` 하나뿐 — 새 문자열을 쓰지 말고 조회한다.
- `stage2_chunk` 에 `MAX_SEQ` 가 없다. 그건 `stage2_embed` 에 있다 (`GATE_TOK` 과 다른 상수).

### 남은 것
- 재도전 2026판 미결 5조: 제1조(목적) · 제6조(성실경영 평가) · 제25조(연합프로그램 운영)
  · 제31조(제재) · 부칙. 2단 LLM 대기이고 인덱스에 없다. 룰 근거에 안 쓰이므로 판정 무영향.
  🔴 2025판에서는 제1조·제31조(구 제33조)가 2단으로 `공통` 이었다 — 조번호 시프트로
  `_apply_target_2단.json` 매칭이 끊긴 것이다. 조제목 재매칭을 넣을지는 미결.
- `corpus.rules` 재도전 9행이 아직 `verified=false`(검수자 `G세션-4단검증`).
  구판 인덱스 때문에 검수를 못 붙였던 것이므로 이제 검수 가능하다.
- `documents.version` 표기가 문서마다 다르다 (`제11차, 2026년` vs `2025년`).
  버전스탬프에 그대로 나간다. 미정리.
- `recheck_queue` 의 `ACTIVE_NOT_LATEST` 1건이 `상태='반영'` 인데 실제 반영은 지금 끝났다.

### 산출물
- `scripts/_work/_재도전2026_인덱스교체.py` (신규) — 판본 교체 전용. 미리보기/`--commit`
- `scripts/llm_schema.py` — `체크코드_enum(dsn, 사업명)`
- `scripts/orchestrate.py` — `체크코드_enum(사업명=사업명)`
- `scripts/tag_apply_target.py` — `절_상속()` 명시 `장` 우선 · `현행` 상수
- `scripts/stage2_chunk.py` — `사업_of_doc` 재도전 키
- `scripts/load_db.py` — `현행` 상수 · `roles` 제거
- `scripts/stage0_ingest.py` — INSERT 에서 `roles` 제거
- `db/init/01_schema.sql` — `documents.roles` 정의 삭제
- `2026_Finance_DATA_FOR_RAG/_stage0_articles.json` — 2026판 33조 + `장`
- `2026_Finance_DATA_FOR_RAG/_apply_target.json` · `_apply_target_todo.json` — 재생성
- DB: `ALTER TABLE corpus.documents DROP COLUMN roles`

## 2026-09-01 · `build_refs.py` — 「법」 약칭 해소 · 경계 검사 · 계열 가드

참조 그래프의 규범명 해소를 셋 고쳤다. resolved 17,386 → 32,043.

### 입력

- `scripts/build_refs.py` (수정 전 백업: scratchpad `build_refs.py.bak`)
- 입력 산출물 `2026_Finance_DATA_FOR_RAG/_stage0_articles.json` (283문서)
- 발단: `tenant.decisions` 73건 중 유일한 B등급 1건(decision_id 63)의 강등 사유가
  `DANGLING_WARN` — 참조문자열 `법 제28조부터 제31조까지`

### 판단

- **약칭은 문서가 스스로 정의한다.** 종전 `build_refs.py:256` 은 `법|시행령|시행규칙` 을
  "어느 법인지 확정 불가" 로 무조건 dangling 처리했다. 실제로는 제1·2조가
  `「중소기업창업 지원법」(이하 "법"이라 한다)` 로 적어둔다. 문서별 약칭표를 걷어 해소한다.
  대안(전역 별칭 사전에 `법 → 중소기업창업지원법` 하드코딩)은 기각 — 문서마다 「법」이 다르다.
- **약칭표는 문서 전체에서 한 번 걷는다.** 정의는 제1·2조, 사용은 제20조·제51조라
  조 단위 스캔 중에 걷으면 만나지 못한다. 삭제·범위밖 조도 정의 원천으로는 읽는다
  (여기서 만드는 것은 엣지가 아니라 사전이다).
- **경계 검사를 넣었다.** 공백 제거 사본에서 토큰 `법` 은 「부가가치세법」의 꼬리에도 걸린다.
  원문 좌표(`imap`)로 되돌려 앞 글자가 한글이면 버린다. `지침|요령|관리기준|기준` 에는
  적용하지 않는다 — 「세부관리기준」처럼 앞에 한글이 붙는 것이 정상이고 해소 대상도 자기 문서다.
- **단일 조 참조를 처음 잡는다.** 기본 「조」 문형은 `지침|요령|관리기준|기준` 만 잡았다.
  `법 제51조제6항` 계열이 통째로 유실됐다. 문서가 정의한 약칭에 한해 켰다 —
  정의 없는 문서에서는 아무것도 새로 잡지 않으므로 근거 없는 엣지가 늘지 않는다.
- **범위 문형을 조 문형보다 먼저 태운다.** `법 제28조부터 제31조까지` 는 조 문형에도
  `법 제28조` 로 걸려 같은 참조가 엣지 두 개가 된다(dst 는 같은 제28조 — 정보가 아니라 중복).
  범위가 먹은 구간을 기록해 조 문형에서 건너뛴다.
- **계열 가드 — 법과 시행령을 넘나드는 부분일치를 막는다.** 「법인세법」이
  「법인세법시행령」에 붙어 있었다(길이차 3). 본법이 코퍼스에 없어서 생긴 대체다.
  없는 것을 없다고 해야 수집 결손이 드러난다. 조용히 옆 문서로 붙이면 엉뚱한 조문이
  근거로 인용되고 결손은 영원히 안 보인다.
- **모법 유도 fallback 은 기각.** 「법」 정의가 없는 시행령·시행규칙 8건에 대해
  제명에서 모법을 유도해봤더니 7건 해소 가능이지만 그중 1건이 오매칭
  (`공공기록물관리에관한법률시행규칙` → **시행령**). 이득 8건에 오답 위험 1건은 안 맞는다.
  `normalize_refs.py` 의 원칙("확신이 없으면 미매칭으로 남겨라")을 따랐다.
- **`normalize_refs.py`** — 고칠 것이 0건일 때 `해소건/총*100` 이 ZeroDivisionError 로
  죽었다. build_refs 가 처음부터 doc_id 를 넣으면 정상적으로 0이 된다. 가드 추가.

### 실측

`scripts/build_refs.py` 출력 · `corpus.refs` 질의.

| | 전 | 후 |
|---|---|---|
| 엣지 | 28,701 | **44,855** |
| resolved | 17,386 (60.6%) | **32,043 (71.4%)** |
| dangling | 11,268 | 12,769 |
| shifted | 47 | 43 |
| 조 지정 dangling — **진입점 문서발** | **29** | **19** |
| 조 지정 dangling — 폐포전용 문서발 | 66 | 1,142 |
| "규범명이 앞에서 잘림" 으로 버려진 것 | 83 | 30 |
| 중복 엣지(같은 src·문자열·dst) | 미측정 | **0** |

- 약칭 정의 보유 문서 142/283. 약칭으로 해소 15,204건. 약칭은 풀었으나 코퍼스에 규범 없음 1,120건
- 약칭 종류 상위: 법 121 · 영 47 · 규칙 9 · 보조금법 6 · 촉진법 6 · 운영지침 6 · 혁신법 5
- 폐포전용발 dangling 이 66 → 1,142 로 는 것은 계열 가드가 **덮여 있던 수집 결손을 드러낸 것**이다.
  판정 폐포는 진입점에서 깊이 1 이라 판정 경로에 들어오는 것은 진입점발 19건뿐이다
- 발단이던 decision_id 63 의 참조는 원문이 `법 제28조…` 가 아니라
  **`공공재정환수법 제28조부터 제31조까지`** 였다. 옛 토큰이 「공공재정환수**법**」의 꼬리를
  물어 규범명을 잘라먹고 있었다. 지금은 `L1_공공재정부정청구금지및부정이익환수등에관한법률_20240927`
  제28조로 해소된다. 운영요령 제20조발 참조 8건이 전부 resolved

### 함정

- 🔴 **`load_db.py` 를 쓰면 안 된다.** `TRUNCATE corpus.documents CASCADE` 가 들어 있어
  청크 20,525개와 임베딩까지 날아간다. refs 만 갈아끼우는 스크립트를 따로 썼다
  (scratchpad `reload_refs.py` — TRUNCATE corpus.refs + INSERT).
- `RE_약칭`(괄호 제거)이 `RE_약칭정의`(약칭 수집)보다 먼저 돌면 사전이 빈다. **수집이 먼저다.**
- 부분일치의 길이차 상한 12는 계열이 다르면 무의미하다. 「법인세법」과 「법인세법시행령」의
  차이는 3이다.

### 남은 것

- **미검증: 실제 판정 재실행.** decision_id 63 을 다시 돌려 A등급이 되는지는 GPU 가 필요해
  확인 못 했다. refs 상으로 강등 사유가 사라진 것까지만 확인했다
- **수집 결손 (계열 가드가 드러냄).** 본법 미수집 상위:
  「법인세법」425 · 「자본시장과 금융투자업에 관한 법률」334 · 「전자정부법」293 ·
  「지방세법」107 · 「상속세 및 증여세법」99 · 「국민건강보험법」90.
  전부 폐포전용 계열이라 사업비 판정 영향은 낮다. 수집 여부는 사람이 정할 문제
- **진입점발 조 지정 dangling 19건** — 전부 TIPS 총괄운영지침 별지서식과
  `L1_국가연구개발혁신법시행규칙` 서식2. 별지서식은 본문과 분리된 문서라 정의 조항을
  안 갖고 있다. 상위 문서의 약칭표를 물려주는 방법이 있으나 문서 묶음 관계가 정의돼 있지 않다
- `_refs.json` 을 다시 만들었으므로 이 산출물을 읽는 다른 경로(`agent_a4.py` 등)의
  회귀는 미확인

### 산출물

| 파일 | 내용 |
|---|---|
| `scripts/build_refs.py` | 수집_약칭 · 경계_ok · _동적패턴 · _약칭해소 · _계열 추가 |
| `scripts/normalize_refs.py` | 0건일 때 ZeroDivisionError 가드 |
| `2026_Finance_DATA_FOR_RAG/_refs.json` | 엣지 44,855 · `약칭` 통계 절 추가 |
| `corpus.refs` | 재적재 44,855행 |

---


## 2026-09-01 · 판정 척추 A1~A12 — 실전 E2E 완주

`judge_cli` 한 줄로 (1)~(7) 이 실제 LLM 까지 돌고 골든셋 77문항 실전 지표가 남았다.

### 입력

- 계약: `0831_최종구현.md` §4 동결 인터페이스 · §8-A 태스크
- 기존: `assemble_context.py`(조립) · `llm_validate.py`(검증) · `llm_schema.py` · `judge_run.py --isolated`
- 남의 모듈: `retrieve.py`(C) · `rule_lookup.py`(B) · `l3_load.py`(E) · `case_search.py`(F) · `eval_store.py`(D)
- 실행: `orchestrate.py --golden --eval-log`, `VLLM_URL` = RunPod H100 프록시

### 판단

- **스텁을 먼저 짜고 남의 모듈을 기다리지 않았다.** C 스텁은 `eval_retrieval.py` 의 실제
  SQL 을 그대로 써서 배관 검증이 의미를 갖게 했다. 착수 40분 뒤 B·C·E·F 가 전부 실물로
  붙어 스텁은 한 번도 안 쓰였지만, 기다렸으면 드라이런이 그만큼 늦었다
- **사업 필터를 판정 경로에서만 켠다** (`orchestrate.사업필터`). `retrieve.사업필터_기본` 은
  False 유지. 대안(모듈 기본값 변경)을 기각한 이유: `eval_retrieval.py` 회귀 기준선 52.9% 가
  8세션 공통 기준선이라 조용히 갈린다. 켠 근거는 지표가 아니라 `CLAUDE.md` 인덱싱 경계
  ("남의 규정이 인용되는 순간 그 자체가 오답")
- **게이트 B 임계 0.0 확정.** 아래 실측대로 분리 임계가 없다. 대안(0.52)을 기각한 이유:
  hit 3건을 버리면서 miss 24건을 못 막는다
- **`PRECEDENCE_FLIP` 은 조건부에서 멈춘다.** `Agent.md` §5 는 "뒤집힌다" 로만 적혀 있으나
  '가능' 까지 올리면 L1·L2 인용 없이 관대한 쪽 두 칸이 된다. 오답 비대칭 우선
- **`DANGLING_WARN` 은 조 지정 dangling 만 센다** (C 실측 채택). 전량을 세면 3문항 중
  1문항에서 울려 강등코드가 신호를 잃는다
- **`조립()` 을 3-튜플로 바꿨다** (hot file 변경 1회). 참조사슬을 여기서 안 돌려주면
  검증기가 `참조사슬=[]` 하드코딩으로 나간다 — 실제로 그랬다
- **커넥션 전부 `autocommit=True`.** (4) LLM 호출이 30초를 넘기는데 읽기 트랜잭션을 쥐면
  다른 세션 DDL 이 막힌다. 8세션 병렬에서 실제로 15분 교착났다
- **`normalize_run.슬롯1_스키마()` 를 새로 만들었다.** `llm_schema.정규화_스키마()` 는
  `{비목,금액,집행예정일,거래처,불확실}` 인데 §4 동결 인터페이스가 **품목·용도**를 요구한다.
  `llm_schema.py` 가 오늘 무소유라 손대지 않고 병존시켰다 — 🔴 정본이 둘이다

### 실측

```text
드라이런 77문항        전건 통과 · 실패단계 0 · 게이트 D 77/77
  s맵            min 17 · 중앙 39 · max 133
  프롬프트       중앙 8,640자 · 최장 25,111자 (≈15~17.6k 토큰)
  참조사슬       총 134 · 평균 1.7/문항        dangling 총 44 (조 지정 1)

게이트 B 분리 임계 없음 (74문항 · D3 golden_chunks 고정)
  hit  min 0.443 p25 0.549 med 0.594 p75 0.656 max 0.725  (41건)
  miss min 0.467 p25 0.514 med 0.551 p75 0.615 max 0.718  (33건)
  임계 0.52 → 12건 차단(그중 hit 3) · 남은 miss 24건 통과

실전 E2E 77문항 (run_id=189 · Qwen3-32B-AWQ · H100 PCIe · 온도 0 · 변형 V0)
  판정 일치율   67.5% (52/77)      격리(D6) 84.7% 대비 -17.2%p
  치명 오답률    0.0% (0건)
  인용 정확도   29.7% (인용 72건 기준)   격리 100% 대비 -70.3%p
  판단불가율     6.5% (5건 — 전부 max_tokens 잘림)
  지연 p50 18.7s · p95 35.1s · 총 1,884초
  강등코드      PREMISE_UNMAPPED 39 · CITE_HANG_MISMATCH 12 · DANGLING_WARN 1 · 무강등 27
  배선 확인     비목 53/77 · 신뢰등급 A71/B1 · 버전스탬프 72/77

fault injection 5종 전부 제 단계에서 판단불가
  db→DB · timeout→정규화 · schema→게이트D INVALID_JUDGMENT
  empty→게이트B · cite→CITE_NOT_IN_MAP+NO_CITATION

회귀 3종  audit_db exit 0 · eval_retrieval hit@5 52.9% 불변 · score_judgment 치명 0 · 84.7%
GPU       창 3회 · $8.02 → $1.52 · 팟 0 · 대장 비어 있음
```

### 함정

- 🔴 **PG 가 감지 못 하는 애플리케이션 레벨 교착.** 한 프로세스가 앞 커넥션으로 락을 쥔 채
  뒤 커넥션에서 그 락을 기다리면, 쥔 쪽이 `idle in transaction` 이라 대기 그래프에 없다.
  푼 방법: **멱등한 쪽(D의 DDL)만 `pg_cancel_backend`.** 미커밋 INSERT 가 있을 수 있는
  트랜잭션은 죽이지 않는다. DDL 앞에 `SET lock_timeout='10s'`
- 🔴 **RunPod 프록시 앞단 Cloudflare 가 `Python-urllib/3.x` 를 403(code 1010)으로 끊는다.**
  같은 요청이 curl 로는 통과. `User-Agent` 명시로 해결
- 🔴 **SSH 키는 팟 생성 시점에만 붙는다.** 키 없이 판 팟은 접속 불가 → 폐기.
  `runpodctl doctor` 를 **열기 전에** 돌린다
- 🔴 **Windows 에서 scp 한 셸 스크립트는 CRLF 라 `set -o pipefail` 이 깨진다.** `sed -i 's/\r$//'`
- 🔴 **`--max-model-len 16384` 는 최장 문항을 자른다.** B6(질문)이 맨 뒤라 질문이 날아간다.
  24576 으로 올렸다
- 🔴 **`max_tokens=1500` 이 thinking + 긴 해야할일 배열에 모자란다.** 5건이 JSON 중간에 잘려
  판단불가로 닫혔다. 이 5건이 판단불가율 6.5% 의 전부다 — **모델의 판단이 아니다**
- 파이썬 stdout 을 파일로 리다이렉트하면 블록 버퍼링된다. 진행이 안 보인다고 멈춘 게 아니다
- bash 히어독 안에서 `\\n` 이 실제 개행으로 접혀 파이썬 파일이 깨졌다. 코드 생성은
  히어독 대신 Edit 툴을 쓴다

### 남은 것

- **A12 변형 6개 미실행.** 선언·배선 완료(`--변형 V1`~`V6`), 채택 기준 코드에 못박음.
  잔액 $1.52 로는 1개도 못 돌린다 (1개 ≈ 40분 ≈ $1.9)
- `max_tokens` 1500 → 3000 (내일 첫 수정)
- `llm_schema.정규화_스키마()` vs `normalize_run.슬롯1_스키마()` 정본 하나로 정리
- `CITE_HANG_MISMATCH` 12건 — 조립기 항 분해와 청크 `항호` 가 어긋나는 원인 미조사
- 게이트 C 임계(0.15/0.35) 미측정. `item_alias` 228쌍이 생겼으니 이제 잴 수 있다
- 강등코드 18종 중 15종은 골든셋에 경로가 없어 미발화 (org 지정 판정 필요)
- 전제 101건이 **전부** 미매핑 — 골든셋에 org·F프로필이 없어서다

### 산출물

```text
신규  scripts/normalize_run.py    (1) 정규화 + 공용 vLLM 클라이언트 + 슬롯1 스키마
      scripts/orchestrate.py      판정() · 게이트 A~D · 전제해소 · decisions 적재 · fault
      scripts/judge_cli.py        사람용 입구
수정  scripts/assemble_context.py 3-튜플 · 변형 6종(b0/변형들)
      scripts/llm_validate.py     §5 6규칙 · 강등코드 18종 · 사고기록()
      scripts/judge_run.py · scripts/index_guard.py(L3 차단) · scripts/audit_db.py(사유 2건)
      scripts/pod_serve.sh        max-model-len 24576
데이터 tenant.decisions · tenant.unmapped_premise · eval.runs(run_id=189)
      corpus.doc_articles +33조 (재도전2026 · G 요청 실행)
파일  scripts/_work/_dryrun_77.jsonl · _e2e_V0.jsonl · _e2e_V0.log
```

---


<!-- 작업 현황.md 14~164행 -->
## 2026-09-01 (화) · 🔴 붙임2 비목 해설표 복귀 — 검색 hit@5 55.4% → 60.8%

**골든셋이 A등급 정답근거로 지정한 표를 우리가 검색에서 빼두고 있었다.**

```text
                 전       후          (분모 74 · 사업필터 on · run_id=190)
RRF hit@5      55.4%  →  60.8%     +4문항 · 적중에서 빠진 문항 0
RRF hit@50     68.9%  →  75.7%
필터밖           23   →    18       (scope 탈락 청크 14 → 2)
적대적 hit@50  70.8%  →  91.7%
사업지정 48건   33.3% →  41.7%
```

- 🔴 **`retrieval_scope` 가 문서·청크 두 곳에 있고 검색 필터는 청크 쪽을 읽는다.** 137건이
  다른데 드리프트가 아니라 **의도된 청크 단위 강등**(붙임·별표·참고 119건). 문서 기준으로 세면
  1,591, 청크 기준(=실제 후보 풀)은 1,478. **이 사실이 어디에도 없어 오늘 실제로 오독이 났다** —
  `RAG.md` §4-2b 로 박았다
- 발견 경로: 미스 33건 분해에서 필터밖 23건의 내역을 조건별로 쪼갰더니 `retrieval_scope` 14 /
  `적용대상` 6 이었다. **필터밖 = 주관기관 화자 문제로만 알고 있었는데 아니었다**
- 강등 사유였던 "붙임이 top-5 의 49.1% 를 먹는데 정답은 1건" 의 실체는 **표 파싱 결함**이다.
  4단 표를 세로로 읽어 「창업프로그램운영비」가 `창업/프로/-/그램/운영비` 5줄로 갈린다.
  **원인을 고치는 대신 증상을 인덱스에서 치웠고** 그 대가로 핵심 근거표를 잃었다
- 복귀는 24청크 UPDATE 뿐 — **재임베딩 0건 · GPU 미사용 · 완전 가역**
- 🔴 **재범람은 실제로 관측된다(총계에 가려진다).** gold 48 20→34위 · 53 29→44 · 72 21→34 ·
  **74 는 39→53 으로 후보 50 밖 이탈.** top-5 에서 빠진 건 없지만 여유가 줄었다 →
  붙임2 재파싱이 이 작업의 마무리다
- 교차확인: ai-25 가 독립적으로 gold 63·65·68 의 정답근거가 `{세부관리기준, 붙임2}` 임을 확인.
  내가 센 8건과 겹친다

## 2026-09-01 (화) · refs 재빌드 — 미규정위임 라벨 정정 · 재도전 폐포 연결

```text
엣지      44,855 → 44,865      문서 278 → 279
resolved  32,043 (71.4% 불변)  dangling 12,769 → 12,779   shifted 43 불변
```

- 🔴 **`미규정위임` 10건이 dst 가 NULL 인데 `resolved` 였다.** "정하지 아니한 사항은 상위
  규범에 따른다" 는 **어느 조인지 지목하지 않는다.** `resolved` 는 "대상이 코퍼스에 있고
  조번호도 맞다" 는 뜻이라 거짓 라벨이었다 → `dangling` 으로 정정.
  폐포 CTE 가 `dst_조번호 IS NOT NULL` 로 걸러내 **판정 영향은 없었다**
- ✅ **재도전 2026판 폐포가 이어졌다** — 10엣지 추가(준용 7·인용 2·별표참조 1).
  제8·9·19·23·28·29·31조가 통합관리지침 제14차로 연결된다. 판본 교체 전에는 끊겨 있었다
- 🔴 `load_db.py` 를 안 썼다 (`TRUNCATE corpus.documents CASCADE` 가 청크·임베딩까지 날린다).
  `TRUNCATE corpus.refs` + INSERT 만 하는 스크립트를 따로 썼다
- 검증 4종 통과: `normalize_refs` 고칠 것 0건(멱등) · resolved 인데 dst NULL 0 ·
  resolved 인데 dst 가 documents 에 없음 0 · 코퍼스 밖 dst 눕힘 0

## 2026-09-01 (화) · 재도전 인덱스 교체 · `roles` 컬럼 DROP — 청크 20,525 → 20,518

**재도전성공패키지가 드디어 현행 기준으로 판정한다.** 종전엔 2025년 구판이 인덱스를 차지하고
있어 작년 기준으로 답하고 있었다. (실행은 ai-25 세션, 수치는 이쪽에서 재확인)

```text
corpus.chunks       20,525  →  20,518      embedding NULL 0건
chunk_terms      1,301,584  →  1,148,718   chunk_len 20,518 · 어휘 16,491 · 평균길이 120.4
진입점               1,598  →  1,591       폐포전용 18,927 불변
재도전 인덱스   2025판 35청크 → 2026판(11차 개정) 28청크
                (창업기업 16 · 주관기관 12 · 토큰 최대 428 평균 213 · 1,024 초과 0)
태깅 결정률           97%  →  99%          (조 376 / 결정 371 / LLM 대기 5)
```

- 🔴 **stale 상수가 세 곳에 흩어져 있었다** — `load_db.py::현행` · `tag_apply_target.py::현행` ·
  `stage2_chunk.py::사업_of_doc`. **하나만 고치면 재적재 때 되돌아간다.** 셋 다 교체
- 근본 원인은 `_stage0_articles.json` 의 2026판이 `articles=0` 이었던 것 —
  **VLM 판독이 DB 에만 들어가고 산출물에 반영되지 않았다** → 태깅 0건 → 청크 0.
  `tag_apply_target.절_상속()` 이 명시 `장` 필드를 우선 읽도록 고쳤다
  (VLM 판독본은 절 헤딩이 앞 조 본문 꼬리에 안 붙는다)
- 🔴 **교체했지만 재도전 경로는 두 겹으로 막혀 있다** — ① 근거 28청크가 전량
  `extraction='vlm'` 이라 `Agent.md` §5 에 따라 **신뢰등급 A 가 구조적으로 안 나온다**
  ② 룰 9행이 `verified=false` 라 단독 "가능" 이 안 나간다.
  **골든셋 재도전 문항이 0건이라 이 경로는 여전히 미측정이다**
- `corpus.documents.roles` 컬럼 **DROP** — `index_target` 의 완전 복사본이었다 (교차확인 위반 0건)
- 반영: `README`·`서비스 아키텍쳐`·`RAG`·`데이터 전처리 파이프라인`·`db/README`

## 2026-09-01 (화) · `build_refs.py` 약칭 해소 — resolved 60.6% → 71.4%

참조 그래프의 규범명 해소를 셋 고쳤다. 발단은 `tenant.decisions` 73건 중 유일한 B등급
1건(decision_id 63)의 강등 사유가 `DANGLING_WARN` 이었던 것.

```text
엣지            28,701  →  44,855
resolved        17,386  →  32,043   (60.6% → 71.4%)
dangling        11,268  →  12,769
shifted             47  →      43
진입점발 조지정 dangling  29 →  19
중복 엣지(같은 src·문자열·dst)      0
```

- **약칭은 문서가 스스로 정의한다.** 종전엔 `법|시행령|시행규칙` 을 "어느 법인지 확정 불가"
  로 무조건 dangling 처리했다. 실제로는 제1·2조가 `「중소기업창업 지원법」(이하 "법")` 로
  적어둔다. 문서별 약칭표를 걷어 해소 — **정의 보유 142/283 문서 · 약칭으로 15,204건 해소**
- 🔴 **경계 검사** — 공백 제거 사본에서 토큰 `법` 이 「부가가치세법」 꼬리에도 걸렸다.
  원문 좌표로 되돌려 앞 글자가 한글이면 버린다. 발단이던 decision 63 의 참조는 원문이
  `법 제28조…` 가 아니라 **`공공재정환수법 제28조…`** 였다 — 옛 토큰이 규범명을 잘라먹고 있었다
- 🔴 **계열 가드** — 「법인세법」이 「법인세법시행령」에 붙어 있었다(길이차 3).
  없는 것을 없다고 해야 수집 결손이 드러난다. 폐포전용발 dangling 66 → 1,142 로 는 것은
  **덮여 있던 수집 결손이 드러난 것**이다 (본법 미수집: 법인세법 425 · 자본시장법 334 · 전자정부법 293 …)
- **모법 유도 fallback 은 기각** — 8건 중 1건이 오매칭(`공공기록물…시행규칙` → **시행령**).
  이득 8건에 오답 위험 1건은 안 맞는다
- 🔴 **`load_db.py` 를 쓰면 안 된다** — `TRUNCATE corpus.documents CASCADE` 가 청크 20,525와
  임베딩까지 날린다. refs 만 갈아끼우는 스크립트를 따로 썼다
- **미검증**: decision 63 재실행(A등급 회복 여부)은 GPU 가 필요해 못 했다. refs 상으로
  강등 사유가 사라진 것까지만 확인
- 산출: `scripts/build_refs.py` · `scripts/normalize_refs.py`(0건 ZeroDivisionError 가드) ·
  `_refs.json` · `corpus.refs` 44,855행 재적재

## 2026-09-01 (화) · 프로토타입 해부 명세 — 어제 판단 2건을 뒤집었다

- `프로토타입_해부_구현명세.md` 신설 (541줄). 실물 코드·DB 실측 기준 화면 단위 구현 지시
- 뒤집은 것 2건: ① step1 폼 → 자연어 textarea 교체 **철회, 폼 유지** (근거였던
  "`item_alias` 학습 소실" 이 성립하지 않는다) ② step3 질문뱅크 **전체 제거 → 위치·형태 변경**
  (요구는 "판정 전 60문항" 이 아니라 "판정 후 맥락 기반 확인사항")
- 그대로 유효: 세목·세세목 제거 / AI 챗봇 제거 / 건별 첨부 제거 / 4-way enum 전면 교체 /
  판단불가 경로 신설 / F 프로필 신설
- `프론트_데이터요구서_0901.md` 수령 (프론트 → 인프라, 669줄) — 프로토타입 16화면 실측 +
  08-30 매칭 회의 결과. 마감 2026-09-07

## 2026-09-01 (화) · 🔴 Agent 층 완결 — 실전 E2E 첫 지표 (8세션 병렬)

**`judge_cli` 한 줄로 (1)→(7) 이 실제 LLM 까지 끝까지 돌았다.** 골든셋 77문항이
`eval.runs`(run_id=189) 에 남았다. 전 세션(A~H) DONE. 기록 정본은 `결과보고.md`.

```text
                     격리(D6)      실전 E2E
  판정 일치율         84.7%   ->    67.5%      낙폭 -17.2%p
  치명 오답률          0.0%   ->     0.0%      불변 — 정지 조건 통과
  인용 정확도        100.0%   ->    29.7%
  판단불가율           0.0%   ->     6.5%
  검색 hit@5            -          55.4%      (74문항 / 사업필터 on)
```

- 🔴 **판단불가 5건은 모델의 판단이 아니라 `max_tokens=1500` 잘림이다.** 다섯 건 모두
  `{"판정": "조건부", ...` 까지 정상 생성 후 절단. 모델이 스스로 판단불가를 고른 횟수는 **0**.
  → 다음 작업 1번은 1500 → 3000. **나머지 전부의 선행조건**이다 (잘림이 섞인 숫자는 해석 불가)
- 🔴 **낙폭은 검색이 만들었다.** 미스 33건 분해 = **필터밖 31.1% / 랭킹 9.5% / 후보밖 4.1%**.
  검색기 튜닝으로 고칠 수 있는 건 9.5% — 180조합 스윕이 최고 +1문항(노이즈)이었던 이유다.
  **RRF 가중을 만지지 말 것**
- 🔴 **골든셋 77문항이 오늘 밤 작업의 절반을 원리적으로 검출하지 못한다** (사업 6종·org 없음):
  재도전 9행 · TIPS 결정 · L3 오버레이 · 숨은 한도가 전부 미측정. B 파일 수정 8건에
  지표가 한 자리도 안 움직인 것이 증거
- **`corpus.rules` 54 → 72행** (L1 공통 9 분리 + 재도전 9). 새 18행은 `verified=false`
- **회귀 3종 → 5종** — `rule_lookup --self-test` · `--smoke`(270조합) · `audit_db` ·
  `score_judgment`(치명 0) · `eval_retrieval`(hit@5 52.9% 불변)
- 🔴 **다섯 번 같은 실수가 났다 — «측정값 하나로 원인을 확정하고, 그 값이 성립하는 조건을
  안 셌다».** 처방 둘: (1) 함수를 실제로 태워 보기 전에 원인을 확정하지 않는다
  (2) 관측과 추론을 문서에서 시각적으로 갈라 쓴다. 대조표는 `결과보고.md` 각 세션 섹션에 있다
- GPU: 창 3회(지시는 2회 — 명시적 이탈). H100 PCIe 로 E2E 77문항 31분 완주.
  잔액 $8.02 → $1.52 · 도는 팟 0
- 🔴 오너가 볼 것 둘: `scripts/_work/_G_룰검수요청.md` §6 룰 검수 5건 ·
  **주관기관 화자 지원 여부**(골든셋 17문항이 구조적 hit 불가 — `서비스 아키텍쳐.md` §11-14)

