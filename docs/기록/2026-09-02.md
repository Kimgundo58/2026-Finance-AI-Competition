# 2026-09-02

문서·코드 정리 야간 작업. 7세션 병렬 (중앙 ai-ae · 백엔드 BE + B1 · 워커 W1~W4).
브랜치 `cleanup/docs-py-2026-09-02` · 기준선 태그 `baseline-0902-정리전`.

---

## 한 장 요약

| | 전 | 후 |
|---|---|---|
| 루트 md | 29개 | **3개** (CLAUDE.md · README.md · 법령 링크모음) |
| 읽는 문서 | 62개 · 24,000줄 · 상한 없음 | **43개 · 2,782줄 · 전부 100줄 이하** |
| 기록 | 4개 8,227줄 뭉텅이 | 날짜별 7개 + 목차 (`docs/기록/`) |
| 참고 자료 | 없음 (지우거나 안 지우거나) | **`docs/부록/` 13개** — 100줄 면제 |
| `scripts/_work` | 94개 | 17개 (77개 아카이브) |
| 공통 모듈 | 0개 | `scripts/_lib/{db,paths}.py` — 23개 파일 전환 |
| pytest | 134 passed · 2 skipped · 6 xfailed | **140 passed · 2 xfailed** (총 142 불변) |
| e2e dry 93문항 | 「10/93」에서 멎음 | **93/93 · 55초** |
| 실전 e2e 일치율 | — (측정 불가, 멎었다) | **65.6%** · 기준선 +12.9 · 치명 0 (run 191) |
| 프롬프트 초과 | 12건 | **0건** (`max-model-len` 40,960) |

---

## 🔴 오늘 밤 제일 큰 것 — 판정 엔진 버그 둘

정리하러 들어갔다가 찾았다. 둘 다 **정리와 무관한 기존 버그**다.

### 1. 통과 조건 C(비목 갈림)가 종료 조건 없는 무한 재귀

`orchestrate.py` 가 비목이 갈릴 때 후보별로 다시 판정하는데, **후보 비목을 재귀에 안 넘겼다.**
질문·사업명이 그대로 들어가니 재귀 안에서 같은 후보가 또 나오고 분기 조건이 영영 안 꺼진다.

- 증상: `eval_e2e` 가 「10/93」을 찍고 멎었다. 두 번 다 같은 자리
- 🔴 「10/93」은 **「11번째에서 멎었다」가 아니었다.** 진행 표시가 10건마다만 찍힌다
- 고침: 하위호환 키워드 `_비목고정` 을 종료 조건으로. 재귀에 후보를 고정해 내려보낸다
- 실측: gold_id=360 무한 → **27초** · 전수 dry 멎음 → **93/93 · 55초**

### 2. 비목후보 폴백이 dict 모양만 가정 — 93문항 중 23건이 죽었다

`orchestrate.py:468` 이 `비목후보` 를 `{비목, 신뢰도}` 객체로만 읽는다.
LLM 모드 스키마는 그 모양을 강제하지만 **dry 의 `규칙_정규화()` 는 문자열 리스트**를 준다.

- 23건 전부 `TypeError: string indices must be integers`
- 고침: 두 모양을 다 걸러 받는다. **dry 분기를 추가하지 않았다** —
  LLM 이 스키마를 어겨 문자열을 섞어 보내면 실전에서도 같은 자리가 터진다. 방어 결손이었다
- 실측: 경로 실패 **23 → 0** · 근거적중 60.5% → 81.4%
- 🔴 **근거적중 상승을 「검색이 좋아졌다」로 읽으면 안 된다.** 검색기는 한 줄도 안 바뀌었다.
  23건이 예외로 죽어 0으로 세어지던 것이 이제 실제로 검색까지 간 것이다

### 규명 절차 (다음에 같은 증상이 나오면 그대로 따라가면 된다)

1. `--limit` 이분탐색 — 13·14 통과 / 15 정지 → 범인을 한 문항으로 좁힘
2. 그 문항을 **새 커넥션으로 단독** 실행 → 「앞 문항이 남긴 상태」 가설 폐기
3. 🔴 **`faulthandler` 로 두 시점 스택** — 같은 프레임이 115겹 → 300겹으로 늘어남.
   DB 대기가 아니라 자기 호출이라는 게 한 줄로 나왔다. 추측 100줄보다 이게 빨랐다

---

## 오너 판단이 필요한 것

`docs/9_미결.md` · `docs/9-1_법적_지적사항.md` · `docs/9-2_백엔드_프론트_미결.md`

| # | 항목 | 왜 사람이 정해야 하나 |
|---|---|---|
| BE1 | 게스트를 `expense_plans` 에 무는 방법 | `org_id` 가 uuid+FK 라 `guest_<hex>` 가 타입에서 거부된다(INSERT 3발 실측). 제3안(nullable `guest_key TEXT`)은 테넌시 축이 둘이 되고 RLS 가 그 축을 안 본다 |
| BE2 | **「회의 확정」 5건이 계약과 반대다** | AI CHAT 하나가 아니라 한 갈래다. 데이터요구서 하나만 반대편이고 나머지 3문서는 계약 편. **다섯을 한 번에 정하면 된다** |

> 🔴 **BE2 정정 (같은 날 후반)** — 프론트 실물 번들을 받아 대조하니 **다섯 다 이미
> 만들어져 있었다.** 데이터요구서는 계약과 싸운 게 아니라 만들어진 프론트를 받아적은 것이었다.
> 「구현하지 마라」는 이미 만든 걸 버리라는 지시가 되어 철회했다. 정본은 `docs/9-2` BE2.
> (기록이라 위 행은 지우지 않고 정정만 붙인다)
| BE3 | `04_agent.sql:14` 의 `\set ON_ERROR_STOP on` 을 지울까 | psql 전용. 실 배포 경로엔 결함 없다(리포에 psycopg 로 `db/init` 을 태우는 코드가 없음). 재현 도구를 짜는 사람만 걸린다 |
| — | **부속표가 프롬프트의 85%** | 붙임2 해설표는 2026-09-01 에 **일부러** 되돌린 것이고 그때 hit@5 가 55.4→60.8 올랐다. 「빼면 프롬프트가 준다」와 「빼면 검색이 나빠진다」가 정면 충돌 |
| — | 법적 지적사항 B-1~B-11 | 개인정보처리방침·동의·파기 절차가 아직 없다 |

### 🔴 하마터면 사라질 뻔한 규칙 하나

```
「PMS 원문은 서버로 보내지 않는다. 브라우저에서 파싱해 확인된 숫자만 전송한다.
 실명·과제번호가 서버에 도달하면 안 된다.」
```
`프론트 연동 사양.md §7` 에만 있었고 어느 요약에도 없었다. 지금은
`docs/2_구조/2-6_수집과_보관.md` 맨 위에 있다.
W4 가 「§1~7·9~11 유효」라는 한 줄을 내용 유실 위험으로 올렸고, BE 가 원문을 열어 확인했다.
**둘 중 하나만 빠졌어도 오늘 밤 사라졌다.**

---

## 문서 구조

```
CLAUDE.md            100줄 — 판단이 필요한 원칙만
README.md            100줄 — 정문 (문제·접근·지표)
docs/README.md            읽는 순서 · 계층 · 무엇을 믿나
docs/0_현황.md            지금 상태 · 구현 순서 · 실측
docs/9_미결.md            제품·설계 미결
   9-1_법적_지적사항.md   개인정보·전자금융 B-1~B-11
   9-2_백엔드_프론트_미결.md
docs/1_배경(3) 2_구조(6) 3_데이터(6) 4_검색(5) 5_룰(3) 6_LLM(3) 7_백엔드(8) 8_운영(3)
docs/부록/(13)           참고 자료. 100줄 면제. 통독 대상 아님
docs/기록/(17)           날짜별 + 레인. 소급 수정 금지. 100줄 면제
```

**「100줄에 못 들어가면 버린다」를 안 했다.** `docs/부록/` 을 만들어 전문을 통째로 옮겼다 —
「git 이력에 남으니 손실 아니다」는 실무적으로 손실이다. 아무도 문서를 찾으려고 이력을 뒤지지 않는다.

### 흡수 안 된 채 지워질 뻔한 문서 4개 (전부 부록으로 살렸다)

| 문서 | 줄 | 어떻게 알았나 |
|---|---:|---|
| `입력_적법성_점검.md` | 762 | 내가 삭제 목록을 훑다 `grep -rln` 으로 docs 언급 0건 확인 |
| `데이터 설명.md` | 271 | 〃 |
| `데이터 전처리 파이프라인.md` | 673 | W2 의 「완전 버림」 목록에 §5·§6·§7-2 |
| `프로젝트설명.md` | 249 | 〃 §3-2·§5 |

🔴 **워커가 「버린 내용 전량 목록」을 남긴 덕이다.** 목록이 없었으면 그냥 지웠고 아무도 몰랐다.

---

## 내가 틀린 것

기록에 남긴다. 다음 사람이 나보다 나으려면 필요하다.

| 무엇 | 어떻게 드러났나 |
|---|---|
| **「e2e 가 11번째에서 멎는다」** | 진행 표시가 10건마다 찍히는 것을 「멎은 지점」으로 읽었다. 배치가 있다고 가정하고 워커에게 풀·배치를 조사시켰다 — **코드를 읽으니 배치가 아예 없었다.** 취소하고 정정 |
| **용어 건수 43·91·23** | `grep -ro` 를 `--include` 없이 돌려 `__pycache__/*.pyc` 를 같이 셌다. 실측은 24·63·18. BE 가 「조건이 다른 run 이라 빼지 않았다」고 멈춘 것이 옳았다 |
| **`corpus.refs` 끊긴 참조 19** | 훨씬 좁은 부분집합(진입점발 조지정)을 전체처럼 적었다. 실측은 dangling **12,779**, 그중 조 지정 1,150 |
| **워커 레인에 내가 썼다** | `orchestrate.py` 수정이 W3 의 되돌리기에 통째로 사라졌다. 「고쳤다」와 「지금 파일에 있다」는 다른 문장이다. 지금은 재적용 후 **검증 직후 즉시 커밋** |
| **「레인 훅이 막아준다」** | 아래 |

### 🔴 레인 훅은 한 번도 발동하지 않았다

`.claude/hooks/guard_lanes.py` 를 만들어 세션별 소유 경로를 강제했다고 여러 번 말했다. **틀렸다.**

- 증거: W2 가 W4 소유(`docs/6_LLM/` 3벌)를 막힘 없이 고쳤고, W4 는 W2 소유(`docs/9_미결.md`)에 썼다
- 원인: Claude Code 는 훅 설정을 **세션 시작 시점에 읽는다.** 6세션 전부 배선 전에 열렸다
- 따라서 **오늘 밤 충돌이 거의 안 난 건 훅 덕분이 아니라 소유 분할과 보고 규율 덕분이다**
- 다음에 이 방식을 다시 쓰려면 **훅을 먼저 배선하고 세션을 열어야 한다**

---

## 안 한 것 · 남은 부채

| | 왜 |
|---|---|
| 코드 주석의 옛 문서 인용 **260건** | `scripts` 155 · `db/init` 36 · `server` 25 · 그 외. 전부 「설계 근거는 `RAG.md` §4-3」 식 주석이고 실행에 영향이 없다. 260곳을 고치는 대신 `docs/부록/옛_문서_지도.md` 대응표로 푼다 |
| `docs/부록/` 안의 옛 이름 인용 | **고치지 않는다.** 이관은 옮기는 일이지 고치는 일이 아니다 |
| DB 스키마 이름 개명 | `corpus`/`tenant`/`eval` 은 AI 조어가 아니라 DB 표준 네이밍이다. 개명하면 60개 파일의 SQL 문자열을 동시에 고쳐야 하고 라이브 DB 마이그레이션이 된다 |
| xfail 2건 | `test_contract.py:180 받은값`(의도된 계약 xfail) · `test_judge_persist.py::test_저장_이벤트에_decision_id가_실제로_채워진다`. 🔴 후자는 **GPU 와 무관하다** — 아래 정정 참조 |
| `PUT /api/profile` 쓰기 · 게스트끼리 노출 | 범위 밖 / 로그인 대기. **못박은 테스트를 그대로 뒀다** — 숨기지 않고 드러내는 방식 |

---

## 세션별 산출

| | 한 일 |
|---|---|
| **BE** (ai-c7) | L3 GET 404 수정(원인은 「쿼리가 죽는다」가 아니라 HTTP 가 주는 **str** 바인딩) · 할일 org_id 3경로 · `persist.py` 위치인자 사고 예방 · 빈 DB 재현 차집합 양방향 0 · `\set ON_ERROR_STOP` 함정 발견 · 프론트 7벌 2,729→168줄 · xfail 4 + skip 2 닫음 |
| **B1** (ai-31) | 할일 org_id 배선 · 빈 DB 재현 검증(별도 `suddoe_verify`, 원본은 SELECT 만) |
| **W1** (ai-e5) | 기록 8,227줄 날짜별 분할(손실 0) · **e2e 정지 원인 규명** · 실패 23건 규명 · 프롬프트 병목 분해 · GPU 크기 산정 · **run 191 실측**(팟 close 확인) |
| **W2** (ai-8a) | `docs/1~4` 18개 신설 · docs 전체 용어 치환 · 부록 9개 이관 · 옛 문서 지도 · 링크 **215개** 전수 검증 |
| **W3** (ai-68) | `scripts/_lib` 공통 모듈 · 23개 파일 전환 · 주석 378건 + 식별자 18건 치환(교차 참조 0 인 것만). `ast` 의 col_offset 이 UTF-8 **바이트** 오프셋이라 docstring 스팬이 밀려 코드를 깨뜨리는 버그를 스스로 발견해 고침 |
| **W4** (ai-5e) | `_work` 94→17 이관 · `docs/5·6·8` 9개 신설 · 루트 md 26개 참조 전수 조사 · 법적 점검 762줄 3분할 |

---

## GPU 실측 — run_id=191 (구현 순서 7-b)

A40 $0.44/h · 볼륨 없음(상시 과금 안 만든다) · 총 1.4~1.6h ≈ **$0.6~0.7** · 팟 close 확인.

🔴 **`--max-model-len 40960` 이 첫 시도에 기동됐다. YaRN 을 안 썼다.**
YaRN 을 켰으면 「모델이 바뀐 것」과 「max_tokens 이 바뀐 것」이 섞여, 7-b 가 없애려던
바로 그 병이 재발했을 것이다. 프롬프트 초과 **12건 → 0건**.

| 구간 | 문항 | 일치율 | 기준선 | 차 | 치명 | 근거적중 | 인용적중 |
|---|---:|---:|---:|---:|---:|---:|---:|
| **전체** | 93 | **65.6%** | 52.7% | **+12.9** | **0** | 76.7% | 68.6% |
| 공통 | 26 | 92.3% | 61.5% | +30.8 | 0 | 92.3% | 84.6% |
| 사업지정 | 67 | 55.2% | 49.3% | +6.0 | 0 | 70.0% | 61.7% |
| 본세트 | 34 | 88.2% | 58.8% | +29.4 | 0 | 88.2% | 76.5% |
| 공식 | 7 | 71.4% | 71.4% | 0.0 | 0 | 71.4% | 71.4% |
| 🔴 보강 | 28 | 35.7% | 42.9% | -7.1 | 0 | 90.5% | 90.5% |
| 🔴 적대적 | 24 | 66.7% | 79.2% | -12.5 | 0 | 50.0% | 37.5% |

- 판단불가 4건 = **모델선택 0 + 실패 경로 4**. 잘림으로 인한 판단불가가 사라졌다
- 경로 실패 **0건** — 오늘 고친 468행 폴백의 효과다

### 🔴 2026-09-01 의 67.5% 와 빼지 마라
문항수 77→93 · `max_tokens` 1500→3000 · `max-model-len` 24,576→40,960 ·
GPU H100→A40 · 버그 2건 수정 — **다섯 축이 동시에 달라졌다.**

### 다음이 볼 자리
- **「조건부」로 쏠린다** — 가능→조건부 11 · 불가→조건부 8 · 판단불가→조건부 6 = 25건
- **`PREMISE_UNMAPPED` 43건**(46%) — 전제가 매핑되지 않는다
- **보강 세트가 근거적중 90.5% 인데 일치율 35.7%** — 근거를 찾고도 판정이 갈린다.
  이건 검색 문제가 아니라 판정 문제다

### 팟에서 겪은 함정 하나
`vllm 0.11.0` + `transformers 5.16.1` 이 토크나이저 API 불일치로 부팅을 죽였다
(`Qwen2Tokenizer has no attribute all_special_tokens_extended`).
`pod_setup.sh` 머리에 이미 적혀 있던 2026-08-31 함정 ②가 **다른 버전으로 재발**한 것이다.
팟에서 `transformers<5`(4.57.6)로 내려 해결했고, `constraints.txt` 두 분기에 못박았다.
실제 검증은 다음 팟 개설 때다 — **오늘 확인 안 됐다.**

### 🔴 같은 xfail 을 셋이 각자 다르게 틀리게 읽었다 — 세 겹이었다

`test_judge_persist.py::test_저장_이벤트에_decision_id가_실제로_채워진다` 하나를 두고
**`XFAIL` 이라는 같은 한 글자를 세 사람이 각자 다른 원인으로 읽었다.**

| # | 적혀 있던 사유 | 누가 | 어떻게 깨졌나 |
|---|---|---|---|
| ① | 「레인 A 의 `persist.py` 대기」 | 이전 세션 | 그 파일은 2026-09-01 에 들어왔다. 낡은 사유 |
| ② | 「GPU 판정이 있어야 한다」 | **BE 가 오늘 밤 적었다** | 인수인계 문서의 「GPU 팟 미가동」을 옮기며 원문에 없던 GPU 를 넣었다. W1 이 GPU 를 열어 태웠는데 그대로 XFAIL |
| ③ | 「실제 `plan_id=1` DB 행이 없어서」 | **내가 적었다** | BE 가 `plan_id=1` 을 실제로 INSERT 하고 태웠다. 그대로 XFAIL |

④ **정정을 한 자리만 고치고 「고쳤다」고 보고했다** — BE 가 커밋 뒤에 스스로 잡았다.
같은 오기가 `_레인_BE.md`·`docs/7_백엔드/README.md`·`_레인_W1.md` 에 더 있었다.
`grep` 한 번이면 5초였다. **「고쳤다」와 「파일에서 사라졌다」는 다른 문장이다** —
오늘 밤 `orchestrate.py` 에서 내가 겪은 것과 같은 모양이고, 이번엔 BE 가 냈다.

**실제 원인** (`--runxfail` 로 실패 «내용» 을 보고서야 나왔다):
```
AssertionError: assert 9001 == 777
```
`monkeypatch.setattr(main, "MOCK", False)` 는 **`main` 모듈의 `MOCK` 만** 뒤집는다.
`persist.py:26` 이 import 시점에 `from ._common import MOCK` 로 **자기 모듈 상수를 따로**
들고 있어 그대로 True → 목 분기의 하드코딩 `decision_id=9001` 이 나온다.
**GPU 도, `plan_id` 행도 원인이 아니다. 모듈 경계 문제다.**
닫으려면 `persist.MOCK` 도 같이 뒤집고 계획 행을 만들면 된다
(`test_contract.py` 의 `모드()` 가 이미 전 모듈을 한 번에 뒤집어 이걸 피해가고 있다).

🔴 이게 CLAUDE.md 가 말하는 **「측정값 하나로 원인을 확정하고, 그 값이 성립하는 조건을 안 셌다」**
의 교과서적 사례다. 셋 다 `XFAIL` 이라는 **결과**만 보고 원인을 **추정**했다.
정지 조건을 발동시키기 전에 `--runxfail` 로 실패 내용을 한 번만 봤으면 5분에 끝났다.
사유만 사실로 고쳤고 **테스트는 안 닫았다** — 처리는 오너 판단이다.

---

## 마감 후 정밀 검토 (2026-09-02)

작업이 끝난 뒤 「뭘 잘못했나」를 실측으로 훑었다. 나온 것을 전부 적는다.

### 🔴 정정 — 「할일 org_id 배선으로 격리를 닫았다」는 과장이었다

BE 가 마감 후 재감사해서 잡았다. **`org_id` 는 인증된 신원이 아니라 쿼리 파라미터, 즉 자기신고다.**
```
GET  /api/plans?org_id=<A의 uuid>         → 200, A 의 계획·금액이 그대로 나온다
POST /api/plans/{A계획}/tasks?org_id=<A>   → 201, 남의 계획에 쓴다
```
오늘 밤 만든 건 **스코핑 필터**(정직한 호출자에게 남의 게 안 섞인다)이지 **접근 제어**가 아니다.
「9케이스 통과」는 **호출자가 정직할 때만** 성립한다.
완화 요인은 `org_id` 가 122비트 uuid 이고 이를 노출하는 엔드포인트가 0개라는 것뿐이다 —
🔴 **화면 3(기관 선택)용 기관 목록 API 가 org_id 를 실어 내보내는 순간 그 방어가 사라진다.**
같은 뿌리로 `POST /api/l3/upload` 는 아예 미감사다(L3 **읽기**와 할일 **쓰기**만 봤다).
→ [[9-2_백엔드_프론트_미결]] BE4·BE5 로 올렸다.

### 감사에서 고친 것

| | 무엇 | 왜 |
|---|---|---|
| 1 | `docs/부록/` 12개 배너에 **시점 경고** 추가 | `전처리_파이프라인_전문.md` 가 `refs 28,701`(실제 44,865)을 담고 있었다. 「참고 자료」라고만 적혀 있으면 **현재값으로 읽힌다.** 수치는 안 고쳤다 — 이관은 옮기는 일이다 |
| 2 | `chunk_terms` 1,148,718 → **1,149,877** (3곳) | DB 실측과 어긋나 있었다 |
| 3 | `4-5_적재_지표` 의 `max_tokens=1500` 에 시점 명시 | run 189 조건인데 현재값처럼 읽혔다 |
| 4 | `orchestrate.py` 갈래 재귀 **오답 표기 경로 제거** | `_비목고정` 이 후보에 없으면 `후보[:1]` 로 물러났다 → **다른 비목으로 판정하고 고정 비목 이름표가 붙는다**(호출부가 덮어쓴다). 고정 비목으로 새로 세우게 고쳤다 |
| 5 | `orchestrate.py` 비목후보 **자료형 방어** | dict 를 넘기면 키가 비목이 됐다. list/tuple 만 순회한다 |
| 6 | `_레인_W1.md` 코드펜스 이스케이프 | 본문 속 ``` 언급을 렌더러가 펜스로 읽어 문서가 깨졌다. 표기만 고쳤다 |

### 확인했고 문제 없던 것

| 항목 | 결과 |
|---|---|
| **내용 손실** | 지워진 15개 md 의 «사실»(수치·식별자·조문·상수) 보존율 **88~100%**. 없는 것은 구 코퍼스 값·큰 수의 부분문자열·`KOBEST` 하나뿐 |
| 위키링크 | **215개 전수, 깨짐 0** |
| DB vs 문서 | chunks·articles·refs·rules·prec·golden 전부 일치 (`chunk_terms` 만 어긋나 위에서 고침) |
| 아카이브 77개 참조 | 3건 — 전부 주석 인용·bare 기본값. 실행 영향 0 |
| 훅 6종 | 전부 실재·배선 확인 |
| 핵심 규칙 모순 | LLM 2회·L2>L3·청킹 단위·검색 필터·정답셋 인덱스 금지 — **반대 서술 0건** |
| `_lib` 전환 | 23 전환 / 23 미전환 / **반쯤 전환 0** |
| 표 열 수·중복 문장 | 어긋남 0 · 3개 파일 이상 중복 0 |
| dry e2e (하드닝 후) | **93/93 · 실패 0** — 무손상 |

### 🔴 감사 자체에서 내가 두 번 틀렸다

1. **줄 단위 대조로 「보존율 1.3%」가 나왔다.** 요약본에 줄 단위 잣대를 댄 것이라 무의미했다.
   사실 단위(수치·식별자·조문)로 바꾸니 88~100% 였다. **잣대가 대상에 안 맞으면 숫자가 거짓말을 한다.**
2. **그 사실 감사 스크립트도 처음엔 틀렸다.** `git ls-files` 가 한글 경로를 8진 이스케이프로
   내놔서 **한글 경로 문서를 하나도 안 읽었다.** 그대로 믿었으면 「12% 보존」이라는
   허위 경보를 보고할 뻔했다. 파일시스템 순회로 바꿔 다시 쟀다.

### 남긴 부채

- 축별 설계 4종(`RAG`·`LLM`·`Agent`·`rule_base`)은 **부록 전문 사본이 없다.** 다른 8개는 있다.
  사실 감사로 실질 손실이 없음을 확인했지만 **내 규칙이 일관되지 않았다** — 원본이 필요하면
  `git show baseline-0902-정리전:RAG.md`
- `orchestrate.py` 의 미사용 import `f_경로집합` — 기준선에도 있던 것이라 손대지 않았다
- 코드 주석의 옛 문서 인용 260건 — `docs/부록/옛_문서_지도.md` 로 푼다

## 🔴 마감 후 3차 — RLS 는 지금 아무것도 안 막는다

BE 가 자기 어젯밤 근거를 스스로 무효화했다. 내가 확인했고 **BE 가 본 것보다 한 겹 더 나쁘다.**

```
tenant 11개 표 전부  relforcerowsecurity = false   ← 소유자에게 정책을 강제하지 않는다
앱 접속 롤 postgres  rolbypassrls        = true    ← 슈퍼유저는 RLS 를 아예 우회한다
결과                 정책을 무시하고 l3_articles 17행이 그대로 보인다
```
**이유가 둘이고 각각 단독으로 충분하다.** `FORCE ROW LEVEL SECURITY` 를 켜도 ②가 남는다.

→ 어젯밤 「스키마를 못 바꾸는 이유는 5개 표의 RLS 와 묶여서」라고 적었다.
마이그레이션 비용으로는 맞지만 **「RLS 가 지키고 있다」로 읽힐 자리였다. 지키고 있지 않다.**
실질 방어는 애플리케이션 `WHERE` 하나뿐이고 그 `WHERE` 가 자기신고 `org_id` 를 믿는다 —
**두 약점은 직렬이 아니라 같은 지점이다.**

쓰기 IDOR 도 2곳이 아니라 **3곳**이었다(B1 이 `POST /api/plans` body `org_id` 미검증을 찾았다).
낱개로 두면 또 하나씩 나오므로 BE5 를 「쓰기 IDOR 3곳」으로 넓히고
**개별 패치가 아니라 신원 검증 한 곳으로 푼다**고 적었다.

### 판정 «안쪽» 은 깨끗하다 (W1 격리 감사 4항목)

L3 로드 org 스코프 교차 0건(양방향) · 검색 `layer`/`적용대상` 하드코드(끄는 파라미터 없음) ·
정답셋 누수 0 · L4 누수 0. `chunks` INSERT 경로 2개뿐이고 둘 다 `index_guard` 를 탄다.
🔴 **정답셋 0건을 「양성 대조로 ILIKE 가 작동함을 먼저 확인」하고 냈다** —
그게 「0건」과 「검색이 안 걸려서 0건」을 가른 자리다.

### 같은 값을 다르게 본 일 (셋째 사례)

BE 가 `expense_plans` 를 몇 초 간격으로 두 번 세서 **4행 → 0행**을 봤다. 잔재가 아니라
다른 세션 픽스처가 그 사이에 생겼다 지워진 것이다. W1 과 BE 가 `l3_documents` 를
3행/2행으로 다르게 본 것도 같다 — **둘 다 맞았고 시점이 달랐다.**
내 「보존율 1.3% → 12% → 실제 88~100%」와 같은 계열이다:
**같은 대상을 다른 조건에서 재면 다른 숫자가 나오고, 조건을 안 적으면 그게 모순으로 보인다.**
