# 9. 미결

- 사람이 결정해야 하는 것은 무엇인가
- 실측이 끝나면 저절로 풀리는 것은 무엇인가
- 설계는 됐는데 코드가 없는 것은 무엇인가

## 사람이 결정하거나 구해와야 하는 것

| # | 항목 |
|---|---|
| 1 | **데모용 L3 샘플** — 건국대가 주관하는 중기부 사업 규정 확보. 기존 참고본은 과기부 계통이라 위임 사슬이 안 닿는다 (사실축 검증용으로는 유효) |
| 3 | **수익 모델** |
| 5 | **호스팅** — HF Spaces vs VM · L3 원본 보관 여부 |
| 11 | 🔴 **교육훈련비 판정이 사업마다 갈린다** — 예비·초기 `가능`(금지예시 0) vs 신규 3사업 `조건부`(금지예시 3~4). 지침 제44조②(본인부담금 불가)·③(4대보험 가입자 한정)이 전 사업에 걸리는데 기존 2개에 반영이 없다. 「붙임2만 보고 L1 조문을 놓친」 유형의 4번째 사례로 보이나, 오너 검수를 거친 행이라 임의로 뒤집지 않았다 |
| 12 | **`rules` 남은 2개 사업** — 아래 별도 절 |
| 14 | 🔴 **주관기관 화자를 지원할 것인가 — 제품 정의 문제다.** 정답셋 17문항의 근거가 `예비창업 제16~20조`(주관기관 자기 운영비)인데 검색 필터가 `적용대상 IN ('창업기업','공통')` 이라 **구조적으로 hit 불가**다. 리랭커·가중·임베딩 무엇도 안 움직인다. 이 17문항을 뺀 71.9% 를 성능 지표로 쓰면 안 된다 — 분모를 유리하게 고른 것이다 |
| 15 | **룰 검수 5건** — `corpus.rules` 72행 중 19행이 `verified=false` 라 그 경로에서 단독 「가능」이 안 나간다 |
| 16 | **화면 9 상호작용** — 사례 자동 노출 → 사용자 클릭식. 임계 조정으로는 안 풀린다. 단 `max_tokens` 3000 재측정 뒤에 정한다 |

### #12 — 재도전 · TIPS

**재도전** — 인덱스 교체는 끝났다 (2026-09-01). 「2026년 11차 개정」이 `active` ·
`index_target=true` · `parse_quality=high` 로 28청크(창업기업 16 / 주관기관 12).
🔴 두 겹으로 막혀 있다.

1. 근거 28청크가 전량 `extraction='vlm'` → 판독본 기반 경고가 강제돼 **신뢰등급 A 가 구조적으로 안 나온다**
2. 룰 9행이 `verified=false` → 단독 「가능」이 안 나간다

**TIPS** — 비목이 R&D 계통(연구재료비 등)이라 어휘 사전 10종과 다르고, 우선순위 조항이
없다(운영사 체계). 설계 결정이 먼저다.

🔴 **둘 다 정답셋 문항이 0건이라 지금은 측정 자체가 안 된다.**

## 실측으로 판가름 나는 것 — 지금 논쟁하지 마라

| # | 항목 | 판단 근거 |
|---|---|---|
| 6 | ④-a 채택 (LLM 2회 vs 3회) | ④-b 단일 호출과 A/B — **모델별로 따로** |
| 7 | 임계치 목록 — 검색 통과조건 · ④-a 불확실 · 비목 신뢰도 · 투표 N · RRF 가중 | 전부 정답셋 |
| 8 | Stage 0.5 태깅의 실제 필요성 | `적용대상` 필터가 배제하는 건 205건뿐이다 (공통 20,149 / 주관기관 205 / 창업기업 164). 안전하지만 효과는 거의 없다 |
| 9 | ④ 오픈소스 배치의 품질 | 사다리 1~4·6단계 |
| 13 | 🔴 **검색 품질 — 그러나 랭킹은 손대지 마라** | 미스 분해는 [[0_현황]] 참조. 고칠 수 있는 건 9.5% 뿐 |

## 설계됐으나 미구현

- 어댑터 `f_axis` 라우팅 차단 조건 — 코드베이스 전체에 `f_axis` 식별자가 없다 (2026-08-31 실측)

## 의존 관계 — 잊지 마라

⚠️ **④-a 를 기각하면 화면 7(조항별 판단 구조)의 재료 산출 방식을 재설계해야 한다.**

## 백엔드 · 프론트 미결

`docs/7_백엔드/` 참조. 2026-09-02 밤 기준 열린 건:

| # | 항목 |
|---|---|
| B1 | **게스트를 `expense_plans` 에 무는 방법** — `org_id` 가 uuid + FK 라 `guest_<hex>` 가 타입에서 거부된다(실측). 제3안(nullable `guest_key TEXT`)은 테넌시 축이 둘이 되고 RLS 가 그 축을 안 본다. 오너 판단 대기 |
| B2 | 🔴 **AI CHAT(화면 12)을 만드는가** — 문서 넷이 서로 반대다. 아래 표 |
| B3 | `db/init/04_agent.sql:14` 의 `\set ON_ERROR_STOP on` (psql 전용) 을 지울 것인가 — 실 배포 경로에 결함은 없다(리포에 psycopg 로 `db/init` 을 태우는 코드가 없음). 재현 도구를 짜는 사람만 걸린다 |

### B2 — 「회의 확정」 5건이 계약과 반대다

낱개가 아니라 **한 갈래다.** `프론트_데이터요구서_0901` 하나만 반대편이고 나머지 3문서는
계약 편이다. 오른쪽은 2026-09-02 서버 실측.

| # | 화면 | 데이터요구서 「회의 확정」 | 서버 실측 |
|:--:|:--:|---|---|
| 1 | 12 | AI CHAT 구현확정 · `POST /api/chat` | 18경로에 `chat` **0건** |
| 2 | 13 | 집행일정 DB추가 · 서버연동 | 계약 §8 MVP 밖 |
| 3 | 9 | 지급수수료 **16종 통합** · 26칸 | `/api/vocab` **10종**, 하위항목 키 없음 |
| 4 | 4 | DOC·DOCX 유지 | 실제로 **415** 를 돌려준다 |
| 5 | 4 | 20MB | 서버 **30MB** (프론트가 엄격한 건 무해) |

**추론** — 계약(동결·최신)과 확정 원칙은 「안 만든다」 쪽이다. 다만 「회의 결과」는 사람
사이의 결정이라 코드로 확인할 수 없다. **어느 쪽이 이기는지 판정하지 않았다.**
다섯을 한 번에 정하면 된다 — 낱개로 다섯 번 판단할 일이 아니다.

## ⚠️ 화면 번호 체계가 세 벌이다

| 체계 | 쓰는 문서 | 번호 |
|---|---|---|
| A | 연동 사양 · 프로토타입 검토 | 3·4·5·6·7·9 (**0·1·2·8 결번**) |
| B | 데이터요구서 — **정본** | 1~14 |
| C | 백엔드프론트비교 · 기능구분 | P0~P12 |

**같은 「화면 5」가 문서마다 다른 화면이다.** 🔴 세 체계의 대응표는 **원본 어디에도 없다** —
`docs/7_백엔드/프론트_화면별_지원표.md` §0 의 매핑은 추정이고, 그 사실이 거기 명시돼 있다.
화면 번호를 쓸 때는 어느 체계인지 반드시 밝힌다. `docs/7_백엔드/` 산출물은 B 로 통일했다.
