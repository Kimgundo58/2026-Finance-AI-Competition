# 9. 미결

- 사람이 결정해야 하는 것은 무엇인가
- 실측이 끝나면 저절로 풀리는 것은 무엇인가
- 설계는 됐는데 코드가 없는 것은 무엇인가

> 갈래가 넷이라 파일이 넷이다 — [[9-1_법적_지적사항]] · [[9-2_백엔드_프론트_미결]] ·
> [[9-3_실측에서_나온_것]]. 여기는 **제품·설계** 미결이다.
> ⚠️ 항목 중 일부(예: `max_tokens` 재측정 전제조건)는 [[0_현황]] 실측이 갱신되면서
> 이미 풀렸을 수 있다 — 항목별 최신 여부는 0_현황.md 와 대조한다.

## 2026-09-04 QA 직전 판 — 닫힌 것 / 오너 승인 대기

병렬 6레인(중앙 ai-c4)이 「팀원이 만질 수 있는 제품」을 세우고 양쪽 배포했다.

**닫혔다** (실측·되읽기·배포 확인):
- 🔴 **403 블로커** — 로그인은 됐고 `tenant.accounts` 명부에 이메일이 없었다. QA 계정
  2건(prototype·test → 경상국립대 창업중심대학사업단)을 로컬·운영 «양쪽» INSERT. 비특권
  롤 실경로·실서버 200 확인 (PR #22)
- 🔴 **#18 데모 기관** — 경상국립대학교 창업중심대학사업단 확정. 코드·문서·화면·데이터
  전부 반영. 프론트 별칭 번들 「건국대」 0건·「경상국립대」 11건. 백엔드 문서 건국대 제거
  (기록은 배너만). org_programs 에 창업중심대학 사업 등록 보정(rules 9행 실재)
- 🔴 **#20 `SUDDOE_ORG_PARAM=0`** — v4 에 넣었다. 무토큰 `?org_id=` → 401 실측. 이유는
  plans 읽기가 아니라 L3 업로드 쓰기 차단(실누수는 RLS 가 이미 막음 — 심층방어)
- **판정 캐시 DB 이전**(재배포로 안 날아감) · **L3 파싱 GUC 누락**(비특권 롤에서 영원히
  「대기」던 것) · **파싱품질 노출** · **GPU wake/reap·하루기동캡·vLLM헬스 분리** · **사고흔적
  계측** — 전부 v4 배포. 컨테이너 파일 md5 == 커밋 byte 5/5 일치(v3 더러운빌드 재발 없음)
- **운영 데모데이터** — 협약금액·계획 4건·할일·profile. 실서버 되읽기 확인
  (🔴 plans 4-way 는 seed «표시값»이지 판정기 산출이 아니다 — vLLM 죽어 있음)

**오너 승인 대기** (GPU — 「과사용 절대 금지·면밀히 관찰」 지시라 부재 중 안 켬):
- 🔴 **GPU 창 1회** — 실판정 리허설(게이트 1·3·decisions 되읽기) · Q4 출력토큰/사고흔적
  발화율 측정 · Q2 RunPod stop→start roundtrip 실물 검증. 전부 vLLM 팟이 살아야 한다.
  코드·스크립트는 준비됨(무인 실행). 오너가 충전·승인하면 팟 1개·2시간 상한·수동종료로 1회
- ✅ **무인 자동정지 배선 완료(2026-09-05 확인)** — 🔴 「`RUNPOD_API_KEY`/`POD_ID` 가 Cloud Run
  에 없다」는 낡은 서술이다. `gcloud run services describe suddoe-api` 실측으로 **둘 다
  secretKeyRef 로 연결돼 있다**(`RUNPOD_POD_ID=5ofl00dpzidchp` · `SUDDOE_GPU_WAKE_DAILY_CAP=8`
  · `SUDDOE_GPU_IDLE_MIN=15`). 남은 것은 코드 동작 검증(watchdog·wake·reap 실판정 리허설)
  뿐이고 그건 위 「GPU 창 1회」항목과 같다
- **Cloud Scheduler → `/api/gpu/reap` 이관** — 계약만 있고 미배선(로컬 워치독 스레드와 이중).
  PC 꺼지면 로컬 워치독도 죽으니 3주 무인엔 이관 필요
- **판정 품질 재측정(Q3)** — 오늘 안 열었다(GPU 직렬 6h 초과). 창업중심대학 골든셋 4문항뿐
  — 데모 사업 판정 품질을 잰 적이 거의 없다. 캐시 계약검사 L3/tasks 확장도 미결(F 단계)

## 사람이 결정하거나 구해와야 하는 것

| # | 항목 |
|---|---|
| 1 | ~~데모용 L3 샘플 — 건국대가 주관하는 중기부 사업 규정 확보~~ ✅ **경상국립대(창업중심대학사업단) 실물로 확보** — 2024-05 사업비 사용 안내문·사업비 변경 신청서(`_테스트_L3/`), startup.gnu.ac.kr 자료실 원본. 중기부→창진원→창업중심대학 위임 사슬이 그대로 닿는다. 건국대 레퍼런스(`건국대학교 레퍼런스/`)는 과기부 훈령 계통이라 판정 데모 L3 로는 여전히 못 쓴다 — 사실축 검증용으로만 남긴다 |
| 3 | **수익 모델** |
| 5 | **L3 원본 보관 여부** — 파싱 후 폐기인가 보관인가. ✅ 함께 묶여 있던 「호스팅 HF Spaces vs VM」은 결정됐다 — Cloud Run + Cloud SQL(asia-northeast3) → [[2_구조/2-4_배포와_인증]] |
| 11 | 🔴 **교육훈련비 판정이 사업마다 갈린다** — 예비·초기 `가능`(금지예시 0) vs 신규 3사업 `조건부`(금지예시 3~4). 지침 제44조②(본인부담금 불가)·③(4대보험 가입자 한정)이 전 사업에 걸리는데 기존 2개에 반영이 없다. 「붙임2만 보고 L1 조문을 놓친」 유형의 4번째 사례로 보이나, 오너 검수를 거친 행이라 임의로 뒤집지 않았다 |
| 12 | **`rules` 남은 2개 사업 — 이제 재도전 하나다.** TIPS 는 «룰을 만들지 않는다» 로 결정났다(2026-08-31). 아래 별도 절 |
| 14 | 🔴 **주관기관 화자를 지원할 것인가 — 제품 정의 문제다.** 정답셋 17문항의 근거가 `예비창업 제16~20조`(주관기관 자기 운영비)인데 검색 필터가 `적용대상 IN ('창업기업','공통')` 이라 **구조적으로 hit 불가**다. 리랭커·가중·임베딩 무엇도 안 움직인다. 이 17문항을 뺀 71.9% 를 성능 지표로 쓰면 안 된다 — 분모를 유리하게 고른 것이다 |
| 16 | **화면 9 상호작용** — 사례 자동 노출 → 사용자 클릭식. 임계 조정으로는 안 풀린다. ✅ 선행조건이던 `max_tokens` 3000 재측정은 끝났다(run 191) — **이제 정할 수 있다** |
| ~~17~~ | ~~`rule_lookup.강등권고` 를 지울지 배선할지~~ ✅ **오너 결정 (b) — 엄격한 쪽 배선(2026-09-05).** `rule_lookup.py:1039`(`eff["verified"]`, 872행에서 `all(r["verified"] for r in rows)`로 계산됨)의 `강등권고` 필드는 **읽는 곳이 0곳**(grep 확인)이라 지금은 죽은 필드다. 실제 강등은 `llm_validate.py:466`가 `not any(verified)`로 따로 한다. 오너가 `all()`(엄격) 쪽을 배선하기로 결정했다 — **코드 배선은 아직 안 됐다.** 백엔드 레인 소관 → [[6_LLM/6-4_검증과_강등]] |

### 배포·데모에서 걸린 것 (2026-09-03) — 배선 상세는 [[9-2_백엔드_프론트_미결]] 소관

| # | 항목 |
|---|---|
| ~~18~~ | ~~데모계정~~ ✅ **닫힘** — 경상국립대학교 창업중심대학사업단으로 확정(위 2026-09-04 절 참고). `tenant.accounts` **3행**(demo·prototype·test) 실재 확인(2026-09-05, `select count(*)`). 「0행 → 전부 403」은 낡은 서술이다 |
| 19 | **게스트 쓰기 설계** — 게스트 행은 `org_id IS NULL` 인데 RLS 조건이 `org_id = current_org()` 라 구조적으로 못 넘는다. 「게스트는 읽기 전용」인지 「예외 정책을 판다」인지 |
| 20 | **`SUDDOE_ORG_PARAM=0` 전환 시점** — 🔴 **선행조건은 풀렸다.** 「프론트 Bearer 전환 PR 미머지」는 낡았다 — `19eae04`(PR#1)이 `main` 에 들어갔고 **최신 프로덕션 번들에 `org_id=` 0건**(09-03 ai-a3 실측, `bb5e172` 배포본). 남은 판단은 「게스트 온보딩 업로드를 죽여도 되나」 하나뿐이고 그건 #19 와 한 몸이다 |
| 21 | **목 서버 재배포** — `362008123976` 은 `gcloud projects describe` 가 **permission denied**(계정 `dogun5578@gmail.com`, 09-03 ai-a3 실측). 🔴 **오너가 «다른 구글 계정» 으로 편집자 초대를 받았을 수 있다 — 「불가」로 닫기 전에 어느 계정인지부터 확인한다.** 우리 프로젝트(`…-a8f`/1081277785480)엔 `suddoe-api`·`suddoe-db` 가 있다 → [[7_백엔드/프론트_목서버_배포지연_0903]] |
| 22 | **D5 AI 챗봇** — 오너 결정은 「일단 빼되 나중에 가능하면」. 재검토 시점 미정 |
| ~~23~~ | ~~GPU 팟 `xcjkf0gy6d6bjp` 처분~~ ✅ **닫힘** — 2026-09-05 `runpodctl pod list` 실측으로 **팟 0개**(가동·정지 전부 없음). 처분할 대상이 이미 없다 |
| ~~24~~ | ~~GPU 무인화 걸림돌 — VLLM_URL 이 env~~ ✅ **닫힘(2026-09-05, `c0fb014`)** — `ops.gpu_pod` DB 배선 완료(`adapter.vllm_url()`·`gpu_watchdog._팟id()`). 🔴 남은 것: wake 가 **새 팟을 만드는** 경로는 아직 없다(기존 팟 start 뿐) → [[8_운영/8-7_GPU_무인운전]] |
| 25 | **09-05 재적재 53청크(8개 조) 중 5개 조가 검색·참조 양쪽으로 도달 불가할 수 있다** (ai-35 실측 보고, 이 세션 재현 안 함) — 채점 분모 295 중 5문항이 구조적으로 hit 불가할 가능성. #14 와 같은 갈래 → [[4_검색/4-3_참조_확장]] |

### #12 — 재도전(미결) · TIPS(결정됨)

**재도전** — 인덱스 교체는 끝났다 (2026-09-01). 「2026년 11차 개정」이 `active` ·
`index_target=true` · `parse_quality=high` 로 28청크(창업기업 16 / 주관기관 12).
두 겹 중 **한 겹이 오늘 풀렸다.**

1. 🔴 **남았다** — 근거 28청크가 전량 `extraction='vlm'` → 판독본 기반 경고가 강제돼 **신뢰등급 A 가 구조적으로 안 나온다**
2. ✅ **닫혔다**(2026-09-03) — 룰 9행 `verified=false` 로 인한 강등. 74/74 검수로 `llm_validate.py:464` 가 안 걸린다. 🔴 **강등이 사라진 것이지 판정이 맞다는 뜻이 아니다** — 검수 후 재측정은 아직 없다

**TIPS** — ✅ **설계 결정은 2026-08-31 G세션에서 이미 났다.** 상위 규범이 통합관리지침이
아니라 「국가연구개발사업 연구개발비 사용 기준」이라 위임 계통이 다르고, 비목이 R&D 계통
(연구수당·학생인건비·위탁연구개발비 등)이라 창업 10종과 교집합이 사실상 없다.
→ **`rules` 0행 → `effective_rule()` None → 판단불가가 «의도된 설계»** 다.
억지 매핑하면 지침 제41조("대표자 인건비 불가")가 TIPS 연구수당 질문에 **인용까지 달고**
발화한다 — 틀린 「불가」가 아니라 근거 붙은 확신이라 더 나쁘다.
근거: [[부록/룰_검수_요청_미결]] §3.

✅ **닫혔다**(2026-09-05) — `precedence_rules` 에 TIPS 1행 추가 완료
(`prec_id=9 · 사업명='TIPS' · 우선계층='L1' · 열위계층='L2' · 범위='unspecified_only' ·
우선규범='국가연구개발사업 연구개발비 사용 기준'`, 운영지침 16.나.2)가)① 근거 · verified=true).
로컬·운영 양쪽 9행, `_precedence_rules.json` 도 갱신됨
(`select 사업명,우선계층,열위계층 from corpus.precedence_rules` 로 재확인).
🔴 「TIPS 는 우선순위 조항이 없다」는 낡은 전제였다 — 최초 검수(08-31)는 「미발견」으로
닫았으나 09-05 재조회에서 조항을 찾았다(적용범위 조가 아니라 비목 사용기준 조 안에 있어
처음엔 놓쳤다). `2026_Finance_DATA_FOR_RAG/_precedence_rules.json` 의 최상단 "핵심발견"
요약문(`"TIPS 는... 어느 조항도 없다"`)은 `rules` 배열과 어긋난 채 남아 있다 — 별도로 고칠 것.
🔴 이 TIPS 행은 L1>L2 라 「8개 사업 중 6개가 L2>L3」(CLAUDE.md) 집계에는 안 들어간다 —
행 수(8→9)와 사업 수 문장을 같은 자리에서 섞지 않는다.

🔴 **「정답셋 문항 0건」은 이제 사실이 아니다** (2026-09-02 실측):
재도전성공패키지 **6문항** · TIPS **2문항**이 `eval.golden_set` 에 있다.
측정은 되지만 위 두 겹(vlm 근거 · verified=false)이 그대로라 **결과 해석에 그 제약이 붙는다.**


## 실측으로 판가름 나는 것 — 지금 논쟁하지 마라

| # | 항목 | 판단 근거 |
|---|---|---|
| 6 | ④-a 스크리닝(조항별 독립판정) 채택 여부 — LLM 2회 vs 3회.<br>🔴 **④'(근거보강 재판정)과는 다른 항목이다** — 이름이 비슷해 섞인다 | ④-b 단일 호출과 A/B — **모델별로 따로**.<br>🔴 **2026-09-05 오너가 「2~3회」로 해제했다.** 근거는 지연 실측이다 — 사용자 1건 92초(정규화 25 + 판정 66)이고 판정 66초 중 **55초가 프롬프트 26k토큰 읽기**인데 그 프롬프트의 **76%가 B3(참조 확장)** 이다. B3 를 빼면 26k→5.5k 토큰, 판정 66→23초(추정).<br>해제 조건 넷: ⑴ 상한 **3회 고정** ⑵ 「근거가 모자란가」는 **LLM 이 아니라 코드가** 정한다(LLM 에 되물으면 「충분하다」를 잘못 답해 근거 없이 답이 나간다) ⑶ 두 호출을 **동시에 쏘는 안**도 후보다(최악 지연이 지금과 같고 호출 수가 늘 3회라 재현성이 오히려 낫다) ⑷ **A/B 로 「폐포 없이 몇 % 맞히나」를 먼저 재고** 고른다.<br>⚠️ **`CLAUDE.md` 「확정 원칙 — 재논의 금지」 절에는 넣지 않는다** — 아직 안 정해진 것을 확정 절에 두면 그 절이 뜻을 잃는다(2026-09-05 오너 지시로 되돌림). 코드도 아직 2회다 — `orchestrate.판정(..., 폐포사용=True)` 는 스위치일 뿐 기본값이 현행 동작이다.<br>🔴 **문서끼리 아직 안 맞는다** — `docs/7_백엔드/API_계약_v1.0.md` 는 "LLM 2~3회, 상한 3(CLAUDE.md)"이라 인용하는데 `CLAUDE.md` 본문은 여전히 "판정 1건 = LLM 2회"다. **`CLAUDE.md` 가 맞는 상태이고**(오너가 확정 절에 넣지 말라고 되돌렸다) API 계약 쪽 인용이 앞서 나간 것이다 |
| 7 | 임계치 목록 — 검색 통과조건 · ④-a 불확실 · 비목 신뢰도 · 투표 N · RRF 가중 | 전부 정답셋 |
| 8 | Stage 0.5 태깅의 실제 필요성 | `적용대상` 필터가 배제하는 건 205건뿐이다 (공통 20,149 / 주관기관 205 / 창업기업 164). 안전하지만 효과는 거의 없다 |
| 9 | ④ 오픈소스 배치의 품질 | 사다리 1~4·6단계 |
| 13 | 🔴 **검색 품질 — 그러나 랭킹은 손대지 마라** | 미스 분해는 [[0_현황]] 참조. 고칠 수 있는 건 9.5% 뿐 |

## 설계됐으나 미구현

- 🔴 **어댑터 `f_axis` 라우팅 차단 조건 — 이제 사실이 아니다.** "코드베이스 전체에 `f_axis`
  식별자가 없다(2026-08-31 실측)"는 낡은 서술이다. `scripts/adapter.py`(35건)·`server/main.py`
  에 구현돼 있고 `GET /admin/gate`로 노출·자가검사까지 있다(2026-09-02 grep 재확인)
- 🔴 **`f_axis` 코드 게이트를 열 것인가 — 문서와 코드가 지금 어긋나 있다.**
  2026-09-05 오너가 `CLAUDE.md` 의 「사실축 호출 외부 API 금지」 원칙을 **해제**했다.
  그런데 `scripts/adapter.py` 의 2겹 게이트는 그대로라, **문서는 열렸고 코드는 막혀 있다.**
  실동작은 종전과 같다(정규화·판정조립은 여전히 외부로 못 나간다). 정할 것: 게이트를
  ⑴ 제거하나 ⑵ 기본 차단 + 환경변수로 여나 ⑶ 자리별 허용목록으로 바꾸나.
  🔴 게이트를 열기 전에 재야 할 값이 하나 있다 — **26k 프롬프트 1회 API 왕복 지연**.
  이게 자체 GPU 보다 안 빠르면 여는 이유가 없다
- **자체 GPU** — ~~실서비스 사실축 슬롯의 전제(외부 API 금지)~~ → 위 해제로 **전제가 아니게
  됐다.** 배치 평가는 GPU 가 싸고(295건 $3.0) 실서비스 단건은 API 가 싸다(GPU 상주
  $1.59/h = 하루 $38) — 자리를 나누는 그림이 후보다. 지금은 RunPod 임대뿐 · 마감 후 사안

## 닫힌 것 — 지우지 않고 근거를 남긴다

| # | 항목 | 닫힌 근거 |
|---|---|---|
| 15 | 룰 검수 오너 응답 대기 6건 | ✅ 2026-09-03 — `corpus.rules` **74/74 검수 · 미검수 0**. `llm_validate.py:464` 의 `not any(verified)` 가 걸리는 조합이 **0/80**(직전 12건: 재도전 9 · 모두의창업 3). 요청서·대조표는 [[부록/룰_검수_요청_미결]] · [[부록/룰_검수_대조표]]. 🔴 run 191 은 검수 «이전» 값이라 지표엔 미반영 |
| — | RLS 로 `tenant.*` 쓰기가 막힌다 | ✅ 2026-09-03 — 로컬 비특권 롤 `POST /api/plans` 201(W-RLS · ai-de). 🔴 **「Cloud SQL 에 GRANT 를 아직 안 쳤다」고 적었던 것은 틀렸다**(`e43bdc0`·`8ba16d4`). 운영에 직접 붙어 재니 GRANT 도 RLS 정책 12개도 **이미 있었다**. 빈 구멍은 `ALTER DEFAULT PRIVILEGES … ON SEQUENCES` 하나. 막던 것은 권한이 아니라 **신원**이었다(`tenant.accounts` 0행 + 읽기 정책 없음 → 로그인 조회 0행 → 403) → `tenant.계정찾기()` SECURITY DEFINER 로 닫힘 |
| — | 판정 절반이 조용히 저장 소실 | ✅ 2026-09-03 — `_unmapped_적재()` 42501 이 트랜잭션을 abort 시키고 **`COMMIT` 이 «성공» 을 돌려주는데 잔여 0건**이었다. 발동 조건이 `PREMISE_UNMAPPED` 와 **동치**라 run 191 기준 43/93 = 46.2%. `91fd456`(SAVEPOINT) → #17 병합 후 백엔드 브랜치에서 **되읽기 1건 확인**(ai-a3). 🔴 `try/except` 만으로는 안 고쳐진다 — abort 상태는 파이썬 예외를 삼켜도 안 풀린다 |
| — | 100줄 초과 2건(구 #24) | ✅ 2026-09-03 — **쪼갰다.** `프론트_레포_0903`(112→99)에서 §6 을 [[프론트_목서버_배포지연_0903]] 로, `8-5_배포_레시피`(115→99)에서 §8 을 [[8-6_배포_실측]] 로. 🔴 수치는 하나도 안 지웠고 원문 그대로 옮겼다. `docs/7_백엔드/README.md` 색인 한 줄은 **아직 안 넣었다**(소유 밖) |

## 2026-09-06 오늘 닫힌 것 — 판정 조립 결함 9건 (전부 커밋으로 확인)

```
사례 인덱스(case_chunks) 드랍             d26b96b  백업(scratchpad/_백업_case_chunks_0906.json)
                                                    후 DROP · 뷰(v_적재현황) 재정의 · 호출부 2곳 제거
사전승인_조건 병합 손실                    8ae285f  「첫 non-null 하나만」 쓰던 병합을 UNION 으로.
                                                    21/21 조합에서 L2 문장이 매일 버려지고 있었다
사전승인_조건 배열화(TEXT->TEXT[])         fe58488  한 행이 조건 2개 이상을 담게. 마이그레이션
                                                    전후 65조합 대조로 eff·B4문장 완전동일 확인
전제해소 즉시검증이 «항상 0»              bdd6d25  f값 미전달로 즉시검증(a)이 구조적으로 0건
                                          +99c333f B5 가 없는 컬럼(협약총액)을 물어 트랜잭션이
                                                    abort → 그 뒤 전부 조용히 빈 값. 실경로에서
                                                    즉시검증 0→2건(시연 org, 전제 5건 기준)
골든고정()이 라벨 변경을 못 잡음           e3dece0  씨앗이 golden_chunks 뿐이라 정답판정을 바꿔도
                                                    지문 불변 — 다음 run 이 「이전과 같은 정답지」로
                                                    보일 뻔했다. golden_set 통계를 지문에 더함
seed_rules.py 로 TRUNCATE 하면 되돌릴 수 없음  aa490fc  corpus.rules 에 BEFORE TRUNCATE 트리거.
                                                    seed_rules.py 는 09-02 스냅샷이라 재적재하면
                                                    오늘 반영분(배열121·L1단서7·UNION·골든5건)이
                                                    전부 사라진다 — DB 에서 원천 차단
item_vocab PK 충돌(TIPS 인건비 저장 불가)  5ca89ad  PK 를 (비목)→(비목,계통)으로. TIPS 「인건비」가
                                                    창업 「인건비」와 이름이 겹쳐 못 들어가고 있었다
l3룰() 이 다중 매치 중 첫 건만 반환         df098d1  나머지가 조용히 샜다(시연 org 9개 비목 전부
                                                    다중매치). 진짜 충돌이 아니라 「둘 다 실어야
                                                    하는」 사실이라 `_룰조립_다중()` 신설 — 8ae285f
                                                    와 같은 결론이 다른 자리에서 또 나왔다
정본 규칙 미정(파일 vs DB)                 01edc96  룰=corpus.rules·골든셋=eval.golden_set 이 DB
                                                    정본으로 확정. `scripts/정본_동기화.py` 로
                                                    골든셋 130칸(정답판정3·verified127) 동기화
```

🔴 이 세션이 직접 `git show -s --format='%B' <hash>` 로 9건 전부의 커밋 본문·diff stat 을
재확인했다(위 요약은 그 본문에서 뽑았다) — 목록만 보고 옮겨 적지 않았다.

## 2026-09-06 새로 나온 것 — 검증했거나 검증 범위를 적었다

- 🔴 **금지예시 매칭이 「불가」를 거의 못 잡는다 — 상한이 8건(4.4%)이다.** `eval.golden_set`
  정답판정='불가' **182건**(이 세션이 직접 재확인)을 룰이 잡는 건 8건뿐이라는 보고를 받았다.
  매칭이 「예시 ⊂ 품목」인데 측정이 질문 «전체»를 넣은 값이라, 실제 사용자 문장으로는
  이 8건도 더 낮을 수 있다. §④「불가가 룰로 안 나온다」(9-4) 의 후속 수치로 본다
  — 🔴 8건 자체는 이 세션이 rule_lookup 을 다시 태워 재현하지 않았다. 182 라는 분모만
  DB 로 확인했고, 적중 8건은 전달받은 값이다
- **TIPS 는 `corpus.rules` 가 0행** — 이 세션이 직접 확인(`select count(*)`). 그 결과
  `eval.golden_set` 의 TIPS **23문항(불가 14 · 조건부 4 · 판단불가 3 · 가능 2)**이 룰 경로로는
  구조적으로 못 맞혀진다. #12 절의 "TIPS 는 룰을 만들지 않는다"는 설계 결정 그대로이니
  이건 새 미결이 아니라 **그 결정의 대가가 골든셋 채점에 몇 문항인지 처음 숫자로 잡힌 것**이다
- **기한 근거 45건이 원문에 없다** — `scratchpad/기한근거_매칭.json`(52건 감사)에서
  「근거없음」34 + 「근거없음(무관한 기한표현 존재)」11 = **45**(이 세션이 파일을 직접 열어 확인).
  지금은 코드에 «운영기본값»으로 박혀 있다. 지울지 원문을 더 찾을지가 미결이다
- 🔴 **표 잔재 제거가 정답청크 몇 개에 걸리는지 — 숫자가 안 맞는다.** 표복구 잔재가 남은
  11개 조([[8_운영/8-2_점검_절차]] 갈래와 별개, `scratchpad/P_잔재제거_설계.md` 대상)에
  `eval.golden_chunks` 가 몇 개나 물려 있는지 «39개»라는 보고를 받았는데, 이 세션이
  `select count(*)/distinct chunk_id from eval.golden_chunks where (doc_id,조번호) in (그 11개)`
  로 직접 재보니 **행 125건·distinct chunk_id 33개**로 «둘 다» 39와 다르다. 닻이 다른 것으로
  보인다(잔재로 지울 «구간»에 걸리는 것만 센 값일 수 있다) — **어느 쪽이 맞는 셈법인지부터
  정해야** 표 잔재 제거가 골든셋에 미치는 영향을 말할 수 있다. `_오버레이()` 의 적용층 판정
  자체(사전승인_조건이 기여도에 안 잡히던 것)는 `df098d1` 로 **이미 닫혔다** — 헷갈리지 말 것
- **L1·L2 규정집 자동 적재 경로가 아직 없다** — CLAUDE.md 가 오늘 "L1·L2 도 각 발행처
  페이지에서 받아 자동 변환·적재한다"로 원칙을 열었으나 크롤러는 `scripts/archive/crawling/**`
  의 일회성 수집뿐이고 폴링 배치·`POST /admin/ingest`·`parse_quality` 자동판정이 0줄이다
  (CLAUDE.md 본문 그대로 인용 — 이 항목은 원칙 문서에 이미 적혀 있어 새로 만들지 않고
  포인터만 남긴다). 작업 목록은 `LLM제외_백엔드_구현_작업_TASK.md` §7

## 의존 관계 — 잊지 마라

⚠️ **④-a 를 기각하면 화면 7(조항별 판단 구조)의 재료 산출 방식을 재설계해야 한다.**


## L3 HWP — 고친 것과 남은 것 (2026-09-03 · 경상국립대 실물 2건 실측)

**고쳤다** (`d51a1a0`). 서러게이트 쌍 22개(11쌍) 미결합 → 결합, 고아는 U+FFFD 치환하고
로그를 남긴다. 단락 폴백이 100자 미만을 «버리던» 것 → 100자를 채울 때까지 «이어 붙인다».
실측 원문 27,774자 → 적재 26,142자(**94.1%**, 이전 16%). 공백 제외 손실 **0자**.
잃던 꼬리 44%(`[첨부1~4] 비목별 집행기준·증빙서류` — 판정이 실제로 쓰는 구간)가 돌아왔다.

**남은 것**

- 🔴 `GET /api/l3/{doc_id}` 응답에 **`파싱품질` 이 안 실린다.** 사용자가 warn/fail 을 못 본다.
  16% 손실 때 아무 예외도 안 나고 화면엔 「35개 조항 인식됨」으로 보였던 것이 이 때문이다.
- **L3 가 늘 조문 형식이라는 전제가 틀렸다.** 이 문서에는 자기 조가 **0개**고 `제N조` 7건은
  전부 남의 규정 인용이다(통합관리지침 제35조·제48조, 민법 제767조 등). 뼈대는 `Ⅰ·Ⅱ + [첨부1~4]`.
  조 분해가 실패한 게 아니라 **형식이 다르다.** 참조 해소(조 제목 재매칭)가 이 문서엔 안 걸린다.
  → 인용이 `단락047` 로 나간다. 이 문서로 골든셋을 짜면 **정답 축을 조번호로 둘 수 없다.**
- **표는 셀 텍스트만 산다 — 행–열 짝이 없다**(`|` 0개·탭 0개). 「비목 ↔ 증빙」 짝이 흐트러질 수 있다.
- **서식 파일 문구.** 변경신청서양식은 조 0개가 정상인데 `fail` 로 떠 「업로드가 깨졌다」로 읽혔다.
  위 수정으로 4조각이 잡혀 `warn` 이 되지만, 대신 **서식이 규정 문서로 취급된다.** 어느 쪽이 나은지 미정.

오염 재확인 — `corpus.documents` 에 '경상국립대'·'창업중심대학사업단'·두 파일명 각 **0행**,
`corpus.chunks` **20,525 불변**. 실물 2건은 `_골든셋/` 아닌 `_테스트_L3/` 에 있다.


## 2026-09-05 오너 라운드(판정 flow)에서 나온 것 — ai-04

### 닫혔다 — 프록시 천장 125.8초

「HTTP 524 의 원인이 무엇이고 무엇으로 푸나」가 닫혔다. 실측은
`scratchpad/인계_0905/천장_결정시험.py` (A100 · `ignore_eos=True`·12,000토큰 강제):
`stream=false` 125.91초/125.74초 **524 재현** · `stream=true` 214.44초 **완주**.
⇒ 클라이언트 타임아웃 상향으로는 «절대» 안 풀린다. 스트리밍이 푼다(기본 ON, `977572c`).
판정이 안 바뀐다는 것도 관측이다 — stream 두 값이 같은 출력(5,773토큰·12,013자)을 냈다.

### 🔴 「훅이 파일 소유권을 강제한다」는 오늘 조건에서 **거짓이었다**

`.claude/_lanes.json` 이 2026-09-04 QA 판이라 **오늘 세션이 한 개도 등록돼 있지 않다.**
`guard_lanes.py` 는 설계상 「미등록은 통과」다 ⇒ 오늘 파일 소유권은 사람 규율로만 걸려 있었다.
실제로 5개 세션이 한 워킹트리에 올라가 서로의 파일을 덮어쓸 뻔했다(워크트리 분리로 수습).
**결정 대기**: 오늘 판으로 갱신할 것인가. 갱신해도 워크트리(-J1·-J2)엔 안 걸린다 —
훅이 세션 cwd 쪽 파일을 읽으므로 트리마다 따로 써야 한다.

### 🔴 `runpod_pod.py open` 에 `--ports` 기본값이 없다 — 오늘 팟 하나를 버렸다

템플릿 기본 포트(8888·22)만 열려 vLLM 8000 이 프록시에 안 잡혔다(`404`). 포트는
**생성 시점에만** 정해진다 ⇒ 팟을 지우고 다시 열었다(약 $0.5·20분 손실).
제안: `open` 의 `--ports` 기본값을 `'8000/http,8888/http,22/tcp'` 로 둔다.
(파일 소유가 Q2 라 여기 적어만 둔다)

### 🔴 골든셋 분모가 문서 안에서 둘이다 — `docs/0-1c` 「60건」

`eval.golden_set` 315행 = 채점 **295** + `평가범위='범위밖'` **20**.
그런데 「구조적 미도달 60건」은 **315 분모**로 세어졌고 「근거적중 51.3%」는 **295 분모**다.
범위밖 20건: 330 331 332 333 334 335 336 337 343 346~352 387 388 405 433.
G1 의 P1 22건 중 상당수가 이 안에 있다 ⇒ 실제로 채점에 걸리는 건 6건 안팎이다.
**문서 수정 필요** — 「60」을 고치기 전에 P2 쪽 38 vs 39(J3 재현치) 차이부터 맞춘다.

### gold 429 — 버전 판별이 DB 로는 불가능하다

제33조②(광고선전비 인용)와 별지2(항목 자체가 없음)가 같은 규정 안에서 상충한다.
`corpus.chunks.version` 이 이 문서 전체 NULL 이라 어느 쪽이 최신인지 못 가른다.
원본 PDF/HWP 표지 개정일자를 열어야 닫힌다.

### 사고 토큰이 예산을 다 먹어 `content` 가 빈 채 끝나는 실패모드

`max_tokens` 가 작으면 Qwen3 가 사고만 하다 예산을 소진하고 `finish_reason='length'` 로
끝나는데 **예외는 안 난다.** 판단불가로 올라가지만 그건 「모델이 모른다」가 아니라
「자리를 안 줬다」다. 정규화는 2000 으로 올려 뒀고 판정은 1500 이다 — 판정 쪽 여유를
이번 run 의 `종료이유` 분포로 확인한다.

이번 run(195)으로 답이 나왔다: **판정 `종료이유` 는 stop 277 · length 0.** 판정 1500 은
자리가 모자라지 않았다. 다만 vLLM 서버는 `length` 를 36건 셌는데, **정규화에 계측이
없어 문항 귀속이 안 된다** — 아래 항목으로 넘긴다.

### 🔴 정규화에 계측이 없다 — 「판정은 안 잘렸고 나머지는 못 쟀다」까지만 말할 수 있다

J2 계측 ①(`추론content있음`·`길이`)이 **판정에만** 들어갔다. run 195 에서
판정 `length` 는 0인데 서버 계수는 36건이라 차이가 정규화 쪽인데, `정규화메타` 가
안 실려 **어느 문항인지·비목 쏠림이 있는지 못 낸다.**
⇒ 다음 run 전에 정규화 호출부에도 같은 2줄을 넣는다. 넣기 전에는
「정규화가 잘렸다」로 적지 않는다 — **「못 쟀다」** 다.

### 🔴 `runpod_pod.py close` 가 `ops.gpu_pod` 를 안 고친다

팟이 실제로 사라진 뒤에도 그 행이 `상태='가동'` + 살아있는 `vllm_url` 로 남는다.
2026-09-05 실측: `close` → `ls` 「도는 팟 없음」인데 `ops.gpu_pod` 는 그대로였다.
**죽은 URL 이 살아있게 보인다.** 다음 세션이 그걸 믿으면 연결 실패로 시간을 버린다.
손으로 `'중지'` 로 되돌렸다. `close` 경로에 넣어야 한다 (`ops.gpu_pod` 는 중앙 소유).

### 🔴 잡음 바닥이 0 이 아니다 — A/B 문턱을 세울 때 빼먹지 마라

run 194→195 에서 **경로도 근거적중도 같은데 판정만 뒤집힌 문항이 4건**(370·393·542·555)
있다. vLLM 연속배칭은 결정적이지 않다. ⚠️ 스트리밍도 같이 바뀌어 순수 잡음과 분리는
안 되므로 **「1.4%(4/295) 이상」** 까지만 말할 수 있다.
⇒ McNemar 의 `m` 에는 이 잡음이 섞인다. **분모가 작은 부분셋에서 m 이 6~10 이면
그 대부분이 잡음일 수 있다.** 「같은 조건 두 번 돌리기」로 바닥을 한 번은 재야 한다.

### 🔴 「악화 N건」을 셀 때 «실패했는데 정답» 을 먼저 빼라

run 194→195 악화가 겉으로 10건인데, 그중 **5건은 194 에서 파이프라인이 넘어져
결과가 「판단불가」가 됐고 정답도 「판단불가」라 우연히 맞은 것**이다(429·463·569·573·616).
「맞던 게 틀려졌다」가 아니다. 진짜 악화는 5건(509 + 위 잡음 4건)이다.
⇒ run 간 비교 코드에 이 공제를 넣는다. 안 넣으면 개선을 과소, 악화를 과대 평가한다.
