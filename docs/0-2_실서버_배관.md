# 0-2. 실서버 배관 — 무엇이 실제로 막고 있나 (2026-09-03)

> [[0_현황]] 에서 갈라냈다 (100줄 상한). **판정 정확도는 [[0-1_판정_정확도]] 다 — 섞지 마라.**
> 🔴 여기를 다 고쳐도 정확도는 안 오른다. 오늘 실측이 그 반대를 가리킨다.
> 🔴 「🔴 정정」으로 시작하는 절은 **시점 기록**이다 — 오전 판단이 틀렸다는 기록이라 문장을 다듬지 않는다.

## 실서버 — 2026-09-03 (중앙 실측)

`https://suddoe-api-1081277785480.asia-northeast3.run.app`

- `/api/health` 200 · `"모드":"live"` · 0.27s · `/api/orgs` 413건 · CORS PATCH 200 + ACAO
- DB 는 Cloud SQL `suddoe-db`(asia-northeast3) — 로컬 덤프 321MB 적재 오류 0 · 12테이블 행수 일치
- 🔴 **정정 (2026-09-03 오후 · ai-b2 + 중앙 실측).** 오전에 「GRANT 미적용 → 실서버 쓰기 전멸」이라고
  적은 것은 **틀렸다.** 갈라 쓴다 — 뭉치면 또 틀린 문장이 된다.
  - **DB 층은 이미 열려 있었다.** GRANT 는 진작 쳐져 있었고 `pg_policies` tenant 12개 · `org_isolation`
    `cmd=ALL` `qual=(org_id = current_org())` 운영에 실재. 비특권 롤 `suddoe_app` 으로 Cloud SQL 에
    직접 붙어 `tenant.expense_plans` INSERT **성공**(plan_id 23226 · 커밋 · 재조회 · 삭제).
    `decisions`·`f_profile`·`l3_documents`·`plan_tasks` 도 성공. 남의 org_id 는 42501(격리 정상).
    `corpus.rules` INSERT 42501(읽기전용 유지). **실제로 비어 있던 구멍은 `ALTER DEFAULT PRIVILEGES
    … ON SEQUENCES` 하나뿐**이고 이날 쳤다 — 없으면 새 표에서 INSERT 는 되고 serial 시퀀스만 42501 이라
    **RLS 위반과 증상이 같다.**
  - **그런데 API 로는 여전히 못 쓴다.** 중앙 실측 — 실서버 `POST /api/plans`(무인증) →
    `{"오류":"지출계획을 저장하지 못했습니다 (DB 연결 실패)","상태":503}`.
    🔴 **이 문구가 거짓이다.** 기전: 주체 없음 → `_org_세우기()` 가 GUC 를 안 세움 → `current_org()` NULL
    → RLS 가 INSERT 차단(42501) → `_질의()` 가 예외를 삼켜 `[]` → `routes_plans.py:283` 이 503.
    **원인 셋(DB 다운 · RLS 차단 · 제약 위반)이 한 문구로 나온다.** `auth._계정조회()` 가 이미 데인 자리와
    같은 종류다(`예외전파=True` 가 그래서 생겼다).
  - **막고 있는 것은 권한이 아니라 신원이다.** 로그인이 안 되면 GUC 가 안 서고, 로그인은
    `tenant.accounts` 0행 + `accounts` 에 읽기 정책이 없어 비특권 롤로 조회가 0행이라 막혀 있다
    (ai-de 실측: `postgres` 찾음 / `suddoe_app` None → 403). 처방은 `SECURITY DEFINER` 로그인 조회 함수(안 b).
  - 로컬: 비특권 롤로 `POST /api/plans` **201 저장 확인**(W-RLS · ai-de · 2026-09-03)
- **첫 실판정을 오늘 처음 돌렸다.** 이벤트열은 계약과 100% 일치 · 판정 「조건부」.
  🔴 인용이 화면에 **0건**으로 나갔다 — `_실_판정` 의 키 이름 결함(고쳤다). **재측정 대기**.
  「이벤트열이 계약과 맞다」는 «안 죽는다» 의 증거지 «판정이 맞다» 의 증거가 아니다

## 🔴 실서버 롤에서 판정이 조용히 판단불가로 닫힌다 (2026-09-03 · 미해결)

실측 — 로컬 DB · 비특권 `suddoe_app` · GUC 세운 채 · `orchestrate.전제해소(기록=True)` 실호출 · 롤백 (ai-b2)

| | 관측 |
|---|---|
| ⓐ | `_unmapped_적재()` → **42501 InsufficientPrivilege**. 대조군 `postgres` 는 통과 |
| ⓑ | 같은 트랜잭션의 `decisions` INSERT → **25P02 InFailedSqlTransaction**. 같은 INSERT 를 «새 트랜잭션» 에서 치면 통과 — 권한이 아니라 순수 abort 연쇄 |
| ⓒ | `orchestrate.py:705` 의 `except Exception` 이 받아 `_마무리(cur=None, 기록=False)` 로 닫는다 → **판정 「판단불가」 · 요약 「내부 오류로 판정을 내리지 않았습니다.」 · `decision_id` 없음(저장 0건)**. 예외는 안 샌다 — HTTP 는 정상 응답이다 |

🔴 **증상은 「사라진다」가 아니라 「조용히 판단불가로 닫힌다」다.** 미매핑 전제가 하나라도 나오면 그 판정이 통째로 닫힌다.
그리고 **판단불가율이 «모델선택» 처럼 보인다** — CLAUDE.md 「판단불가율은 «모델선택 / 실패경로» 로 갈라 센다」가 정확히 이 자리다.
지금은 강등사유 문자열이 유일한 단서다. **실서버 롤로 도는 run 이 섞이면 그 run 의 판단불가율은 모델 값이 아니다.**

- 🔴 **빈도 미측정.** 「몇 번에 한 번이냐」가 없어 배포 블로커인지 아닌지가 아직 안 갈린다 (ai-b2 측정 중).
  로컬 `unmapped_premise` 89행 · `decisions` 80행은 **BYPASSRLS 인 `postgres` 로 쌓인 값**이라 실서버 빈도를 못 잰다
- 원인은 셋이고 **갈라서 고친다**
  1. **스키마·정책** — `unmapped_premise` 는 org 축이 없는 전사 통계표인데 RLS 만 켜고 정책 0개다.
     08-31 의도는 「읽기를 닫는다」였는데 `ENABLE RLS` 가 쓰기까지 닫았다. 비교할 org 컬럼이 없어 **정책으로는 못 푼다** → 오너 결정
  2. **코드 결함 아님** — INSERT 에 `org_id` 가 없는 건 맞지만 그 표에 `org_id` 컬럼이 애초에 없다. 중앙 가설 기각(실측)
  3. 🔴 **별개 결함 — `_unmapped_적재()` 에 `try/except` 가 없다.** 통계 한 줄이 판정 저장을 죽인다.
     `try/except` 만으론 부족하고 **savepoint** 가 있어야 abort 된 트랜잭션에서 `decisions` 가 산다 (ⓑ 가 증거)
- 🔴 오늘 **네 번째** 「로컬 superuser 가 가렸다」다. ⓐ목이 인용을 박았다 ⓑconftest 가 목으로 흘렸다 ⓒ`postgres` bypassrls ⓓ이것
