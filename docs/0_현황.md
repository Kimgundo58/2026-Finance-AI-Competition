# 0. 현황

- 지금 무엇까지 됐나
- 다음에 무엇을 치나
- 지금 수치는 얼마인가

> 실측 수치는 SessionStart 훅이 매 세션 다시 뽑는다. 이 표와 다르면 훅이 맞다.
> 🔴 **갈래가 둘이고 섞으면 안 된다** — 판정 정확도 [[0-1_판정_정확도]] · 실서버 배관 [[0-2_실서버_배관]].
>   오늘 실측은 「배관을 고치면 정확도가 오른다」의 **반대**다. 정확도 병목은 룰 테이블과 코드에 있다.

## 구현 순서

**UI 보다 CLI 판정기가 먼저다.** 판정 품질이 정답셋을 못 넘으면 UI 는 의미가 없다.

| | 단계 | 상태 |
|---|---|---|
| 1 | 판정 단계 격리 테스트 (일치율 84.7%) | ✅ |
| 2 | Stage 0 표 추출 (22문서 · 표 469 · 실패 0) | ✅ |
| 3 | 정답셋 검수 종결 — **113행 전량 verified** (채점 분모 93) | ✅ |
| 4 | 룰 테이블 `corpus.rules` 74행 · 검수 74/74 · 미검수 0 | ✅ 2026-09-03 · 🔴 [[0-1_판정_정확도]] |
| 5 | 청킹 단위 확정 — 조 + 900토큰 초과 시 항 | ✅ |
| 6 | BM25 + RRF 검증 · 가중 0.9/0.1 | ✅ |
| 7 | 쿼리 파이프라인 + CLI 판정기 `judge_cli` | ✅ 2026-09-01 |
| 7-b | `max_tokens` 3000 · `max-model-len` 40960 재측정 | ✅ 2026-09-02 (run 191) |
| 7-c/d | 정답셋에 재도전·TIPS·L3 문항 추가 · A12 프롬프트 6변형 A/B | 미착수 |
| 8 | F1·F3·F4 폼 + 전제 해소 루프 | 미착수 |
| 9 | 모델 검증 사다리 (RunPod) | 미착수 |
| 10 | 웹 UI + 배포 | 🔴 떴다 · 실서버 **쓰기 미개통** |

## 실서버 — 2026-09-03  → 상세 [[0-2_실서버_배관]]

`https://suddoe-api-1081277785480.asia-northeast3.run.app`

- `/api/health` 200 · `"모드":"live"` · 0.27s · `/api/orgs` 413건 · CORS PATCH 200 + ACAO
- DB 는 Cloud SQL `suddoe-db`(asia-northeast3) — 로컬 덤프 321MB 적재 오류 0 · 12테이블 행수 일치
- 🔴 **DB 층은 이미 열려 있다. 막는 것은 권한이 아니라 신원이다** — `tenant.accounts` 0행 →
  로그인 불가 → GUC 미설정 → `current_org()` NULL → RLS 가 INSERT 차단. 그런데 API 는 그걸
  **「DB 연결 실패」로 잘못 말한다**(원인 셋이 한 문구로 나온다). 오전의 「GRANT 미적용」은 틀렸다
- ✅ **실서버 롤에서 판정이 조용히 「판단불가」로 닫히던 것 — 닫혔다** (2026-09-03).
  빈도는 «미측정» 이 아니라 **46.2%** 다. `_unmapped_적재()` 가 도는 조건과 `PREMISE_UNMAPPED` 가
  붙는 조건이 `orchestrate.py:683` 에서 **같은 `해소["미매핑"]`** 이라 상관이 아니라 **동치**이고,
  run 191 에서 43/93 이다. 🔴 그 위에 한 겹 더 있었다 — abort 된 트랜잭션의 `COMMIT` 이
  **«성공» 을 돌려주는데 잔여 0건**이라 코드는 저장했다고 믿는다.
  `91fd456`(SAVEPOINT) → #17 병합. 병합 후 백엔드 브랜치에서 **비특권 롤로 되읽기 1건 확인**(ai-a3).
- **첫 실판정을 오늘 처음 돌렸다.** 이벤트열은 계약과 100% 일치 · 판정 「조건부」.
  🔴 인용이 화면에 **0건**으로 나갔다 — `_실_판정` 의 키 이름 결함(고쳤다). **재측정 대기**.
  「이벤트열이 계약과 맞다」는 «안 죽는다» 의 증거지 «판정이 맞다» 의 증거가 아니다

## 실측 (2026-09-02 기준 · 날짜 붙은 줄만 09-03)

| 항목 | 값 |
|---|---|
| `corpus.chunks` | 20,525 (L1 20,030 · L2 495 · 232문서) |
| `corpus.refs` | 44,865 — resolved 32,043(71.4%) · shifted 43 · 끊김 12,779 (그중 조 지정 1,150. 나머지는 검색 범위 밖 인용이라 정상이다) |
| `corpus.doc_articles` | 26,304 |
| `corpus.rules` | **74 · 검수 74/74 · 미검수 0** (2026-09-03) — 미검수로 인한 강등 **0/80조합**. 🔴 검수해도 `허용` 분포는 **조건부 73 · 가능 1 · 불가 0** 이다 — 판정 품질 최대 병목 → [[0-1_판정_정확도]] |
| `corpus.precedence_rules` | **9**(2026-09-05, TIPS 1행 추가 — 아래 「종결된 것」 참조) |
| `eval.golden_set` | 🔴 **315**(2026-09-04 재구축) — 기존 113(verified, 채점 분모 93) + 신규 202(8사업 전수·verified=false·미검수) → [[9_미결]] eval run 재측정 대기(분모 315) |
| `tenant.decisions`/`orgs`/`org_programs`/`corpus.programs` | 73 / 412 / 1,050(주관기관289·운영사479·운영기관118·멘토164) / 8 — 실서버 `/api/orgs` 는 **413**건(2026-09-03) |
| BM25 `chunk_terms` | 1,149,877 |
| pytest | **254 passed · 4 xfailed · 실패 0** (2026-09-03 오후 · 중앙 실측. xfailed 4 중 2 는 금액비교 통로 잠금 — 결함이 닫히면 XPASS 로 빨개진다). 09-02 의 1 failed(`test_e2e_flow.py::…탭에_반영`)는 재현되지 않았다 — 경합이었는지 결함이었는지는 끝내 못 갈랐다 |

## 판정 성능 — run 191  → 표와 분해는 [[0-1_판정_정확도]]

2026-09-02 · 정답셋 93문항 실전 E2E · A40 · `max-model-len` 40960 · `max_tokens` 3000 · 4,019초.

- **전체 일치율 65.6%**(다수결 기준선 52.7% · **+12.9** · 치명 **0** · 근거적중 76.7% · 인용적중 68.6%)
- 🔴 **프롬프트 초과 0건** — 잘림이 안 섞인 첫 실전 측정(7-b 달성). 룰 검수 74/74 **이전** 값이다
- 🔴 최대 오류 갈래는 **「조건부」로 쏠린다** — 정답=가능 **68.8%** · 정답=판단불가 **85.7%** 가 전부
  조건부로 샌다. **GPU 도 배관도 아니다.** 검색 미스도 튜닝으로 고칠 수 있는 건 9.5% 뿐이다
- 🔴 **이 값을 2026-09-01 의 67.5% 와 빼면 안 된다** — 다섯 축이 동시에 달라졌다

## 종결된 것 — 다시 열지 마라

| 항목 | 근거 |
|---|---|
| ANN 도입 | 미사용 확정. 20,055청크 전량스캔 134ms·recall 100%, HNSW 는 recall 82~91% |
| 정답셋 검수(구판 113행) | 113 전량 verified(2026-08-31 77/77 종결 + 확장분) · precedence **9/9**(TIPS 1행 추가, 아래 참조) — 🔴 2026-09-04에 신규 202행(미검수·8사업 전수)이 더해져 golden_set 총 **315행**. 「113」을 전체로 읽지 말 것 → 위 실측 |
| precedence TIPS 1행 | 2026-08-31 결정 · 2026-09-05 근거 재조회로 확정(16.나.2)가)①, verified) — L1>L2(미규정만). "TIPS는 조항이 없다"는 낡은 전제였다 |
| 룰 오너 검수 | 🔴 **74/74 완료**(2026-09-03). 기계 4단 검증(19/19)과 «오너 검수»는 다른 것이었고, 이제 둘 다 닫혔다 → [[9_미결]] #15<br>🔴 **「닫혔다」를 「룰 문제가 끝났다」로 읽지 말 것** — 검수한 것은 **행의 정확성**이지 분포가 아니다 → [[0-1_판정_정확도]] |
| 스키마 · 비목 사전 | `corpus`/`tenant`/`eval` 분리(`public` 0개) · 31비목 `guided_json_enum` 10종 |
| 재도전 판본 역전 | 현행판(2026년 11차 개정) 28청크. 🔴 «올바른 판본» 의 증거지 «판정이 선다» 가 아니다 — 28청크 전량 `vlm` → [[9_미결]] #12 |

> 다음 — 안 정해진 것 [[9_미결]] · 도는 방식 [[2_구조/2-2_판정_흐름]] · 시점 기록 [[기록/00_기록_목차]]
