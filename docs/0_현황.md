# 0. 현황

- 지금 무엇까지 됐나
- 지금 수치는 얼마인가
- 다음에 무엇을 치나

> 실측은 SessionStart 훅이 매 세션 다시 잰다. 이 표와 다르면 훅이 맞다.
> 갈래 둘 — 판정 정확도 [[0-1_판정_정확도]] · 실서버 배관 [[0-2_실서버_배관]]. 섞지 않는다.

## 진행 상태

CLI 판정기(`judge_cli`)·검색(BM25+RRF)·전제 해소·L1/L2/L3 적재·실서버 배관까지 떴다.
남은 건 F1·F3·F4 폼 + 전제 해소 루프, 모델 검증 사다리(RunPod), 판정 품질 재측정 — [[9_미결]].

## 코퍼스·룰 실측 (`select count(*)` 계열로 직접 재확인)

| 항목 | 값 |
|---|---|
| `corpus.chunks` | 20,556 · embedding 100% (L1 20,031 · L2 525 · L3 0) |
| `corpus.rules` | 82 · verified 82/82 (TIPS 8행 포함) |
| `corpus.item_vocab` / `item_alias` | 18(창업10·RND8) / 326 |
| `corpus.check_items` | 52 (결제전43 · 결제후9) |
| `eval.golden_set` | 342 · verified 315 (본세트244 · 적대적31 · 보강29 · L3 27 · 공식11) |
| `tenant.l3_documents` / `l3_articles` | 4 / 224 |
| 최신 실측 run | 195 (196은 dry-run 지문검증 · 실측 아님) |

## 판정 성능 — run 195 (최신 e2e)

| 구간 | 문항수 | 일치율 | 다수결기준선 | 초과 |
|---|---|---|---|---|
| held-out(본세트+공식) | 239 | 64.4% | 57.7 | +6.7 |
| 전체 | 295 | 61.4% | 55.9 | +5.4 |
| 적대적 | 28 | 57.1% | 78.6 | **−21.4** |

🔴 적대적 구간을 숨기지 않는다 — 유리한 것만 골랐다는 인상을 주면 안 된다.
🔴 「held-out41」·「튜닝52」라는 이름을 쓰지 않는다 — 실제 크기는 239 · 56이다.
🔴 run 194(44.7%)는 인용 금지 — 96건 타임아웃으로 측정 자체가 안 된 값이다.
지표명 병기(run194→195): `판단불가_실패경로` 94→11 · `경로빈도.실패` 72→4 — 분모가 다른 두 지표.

## L3 — 고유 조건 9개가 천장

경상국립대 L3 문서(203 article) 전량 검토: L1/L2와 실제로 갈리는 조건은 **9개**뿐,
나머지 ~70%는 L2 세부관리기준 원문 복붙. 골든셋 L3 27문항 = 9조건 × 3버전(공식+변형2).
🔴 **분모는 조건단위 9다.** 27로 나누면 판정력이 부풀려진다.

## 실서버 → 상세 [[0-2_실서버_배관]]

`https://suddoe-api-1081277785480.asia-northeast3.run.app` — `/api/health` 200,
DB는 Cloud SQL `suddoe-db`(asia-northeast3) 연결. 남은 배관 이슈는 [[9_미결]] 참고.

> 다음 — 안 정해진 것 [[9_미결]] · 도는 방식 [[2_구조/2-2_판정_흐름]] · 시점 기록 [[기록/00_기록_목차]]
