# 0. 현황

- 지금 무엇까지 됐나
- 지금 수치는 얼마인가
- 다음에 무엇을 치나

> 수치는 문서보다 DB 가 이긴다. 이 표는 2026-09-07 마감 시점 기록이다.
> 판정 정확도 상세는 [[0-1_판정_정확도]].

## 진행 상태

판정 파이프라인(정규화 → 룰 → 검색 → 판정 → 검증) · L1/L2/L3 적재 · 실서버(Cloud Run + Cloud SQL) ·
프론트(Vercel) · 판정 캐시 · 문의초안 · 규정집 자동 수집 경로(`/ingest/weekly`, 스케줄러 미연결)까지 떴다.
남은 것은 [[9_미결]].

## 코퍼스·룰 실측 (운영 Cloud SQL, 2026-09-07)

| 항목 | 값 |
|---|---|
| `corpus.documents` | 283 |
| `corpus.chunks` | **20,648** (운영). 로컬은 20,556 으로 8문서 뒤처짐 — [[9_미결]] #27 |
| `corpus.refs` | 44,865 |
| `corpus.rules` | 82 · verified 82/82 (TIPS 8행 포함) · 금지예시 358 · 허용예시 225 |
| `corpus.precedence_rules` | 9 |
| `corpus.item_vocab` / `item_alias` | 18(창업 10 · RND 8) / 326 |
| `corpus.check_items` | 52 (결제전 43 · 결제후 9) |
| `eval.golden_set` | 342 · verified 315 (본세트 244 · 적대적 31 · 보강 29 · L3 27 · 공식 11) · 채점 분모 320 |
| `tenant.l3_documents` / `l3_articles` | 4 / 224 |
| `tenant.orgs` / `org_programs` | 413 / 1,063 |

## LLM 경로 (2026-09-07)

판정·정규화 모두 **Qwen3.7-plus**(DashScope 국제판, `SUDDOE_LLM=qwen`). GPU 팟은 **정지**
(재기동 금지 — 재는 값이 필요하면 오너 승인 먼저). 정규화는 thinking off 로 44.0초 → 4.1초
(출력토큰 2,466 → 97). 판정은 thinking on. `qwen3.7-plus` 는 thinking 과 json_schema 를 같이 쓴다
— 「같이 못 쓴다」는 `qwen3-32b` 한정이었다. 상세 [[6_LLM/6-1b_LLM_백엔드]].

## 판정 성능 — run 206 (320문항 · 운영 코퍼스 기준)

| 축 | 값 |
|---|---|
| 전체 일치율 | **67.8%** (다수결 기준선 57.8 · +10.0%p) |
| 세트별 | 적대적 96.3 · 공식 70.0 · 본세트 69.3 · 공통 68.2 · L3 55.6 · 보강 25.0 |
| 정답유형별 | 불가 85.9 · 조건부 69.4 · **가능 14.3** · **판단불가 9.5** |
| 인용 정확도 / 근거 도달률 | 63.9% / 63.2% |
| 치명 오답 ("불가"를 "가능") | **0 / 320** |
| 전수 평가 1회 비용 | $2.92 (입력 403만 · 출력 80만 토큰) |

병목은 검색이 아니라 판단이다 — 적대적 세트는 근거를 못 찾고도 맞히고(인용 33.3),
보강 세트는 근거를 찾고도 틀린다(인용 81.0). 약점은 「가능」과 「판단불가」 유형.
run 202 이전 수치는 세트 구성·코퍼스가 달라 직접 비교하지 않는다.

## L3 — 고유 조건 9개가 천장

경상국립대 L3 문서(203 article) 전량 검토: L1/L2와 실제로 갈리는 조건은 **9개**뿐,
나머지 약 70%는 L2 세부관리기준 원문 복붙. 골든셋 L3 27문항 = 9조건 × 3버전(공식+변형2).
🔴 **분모는 조건단위 9다.** 27로 나누면 판정력이 부풀려진다.

## 실서버

`https://suddoe-api-1081277785480.asia-northeast3.run.app` — Cloud Run **v30 · 리비전 00043**
(2026-09-07). 기동 시 동기 워밍업 약 44초, 이후 `/admin/warmup` 0.2초. `/api/health` 200,
DB 는 Cloud SQL `suddoe-db`(asia-northeast3). 판정 1건 실측 약 60초 — 그중 판정 LLM 55~59초
(thinking 토큰). 프론트 `fb0187a`(대기 문구 「보통 1분」). 남은 배관 이슈는 [[9_미결]].

배포는 수동(`docker push` → `gcloud run deploy`). GitHub 와 자동 연동은 없다.

## 저장소 정리 (2026-09-07)

서빙 import 그래프(`server.main` 기준) 밖의 개발 도구·작업 기록·부록을 뺐다. 커밋 메시지의
AI 공동저자 꼬리표를 걷어내며 히스토리를 재작성해 **커밋 해시가 전부 바뀌었다** —
09-07 이전 해시 인용은 옛 값이다. 검산 도구는 `scripts/tools/`, 구 파이프라인은 `scripts/archive/`.

> 다음 — 안 정해진 것 [[9_미결]] · 도는 방식 [[2_구조/2-2_판정_흐름]]
