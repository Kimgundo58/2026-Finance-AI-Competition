# 0. 현황

- 지금 무엇까지 됐나
- 지금 수치는 얼마인가
- 다음에 무엇을 치나

> 실측은 SessionStart 훅이 매 세션 다시 잰다. 이 표와 다르면 훅이 맞다.
> 갈래 둘 — 판정 정확도 [[0-1_판정_정확도]] · 실서버 배관 [[0-2_실서버_배관]]. 섞지 않는다.

## 진행 상태

CLI 판정기(`judge_cli`)·검색(BM25+RRF)·전제 해소·L1/L2/L3 적재·실서버 배관까지 떴다.
남은 건 F1·F3·F4 폼 + 전제 해소 루프, 모델 검증 사다리(RunPod), 판정 품질 재측정 — [[9_미결]].

## 코퍼스·룰 실측 (`select count(*)` 계열로 직접 재확인)

| 항목 | 값 |
|---|---|
| `corpus.chunks` | 로컬 20,556 🔴 ≠ 운영 20,648(8문서 +92행, 2026-09-07). 운영 기준 동기화 진행 중 — [[9_미결]] |
| `corpus.rules` | 82 · verified 82/82 (TIPS 8행 포함) |
| `corpus.item_vocab` / `item_alias` | 18(창업10·RND8) / 326 |
| `corpus.check_items` | 52 (결제전43 · 결제후9) — 로컬·운영 6칼럼 스키마 드리프트 닫힘(2026-09-07) |
| `eval.golden_set` | 342 · verified 315 (본세트244 · 적대적31 · 보강29 · L3 27 · 공식11) |
| `tenant.l3_documents` / `l3_articles` | 4 / 224 |
| 최신 실측 run | **202**(320문항) |

## LLM 경로 (2026-09-07)

판정·정규화 모두 **Qwen3.7-plus**(DashScope 국제판). GPU 팟은 **정지**(재기동 금지 — 재는 값이
필요하면 오너 승인 먼저). 정규화는 thinking=False 로 44.0초 → 4.1초(출력토큰 2,466→97).
🔴 「DashScope 는 thinking·json_schema 를 같이 못 쓴다」는 `qwen3-32b` 한정이었다 —
`qwen3.7-plus` 는 둘을 같이 쓴다. 판정(④)은 안 건드렸다 — 자세한 경위 [[기록/2026-09-07]].

## 판정 성능 — run 202 (320문항)

| 세트 | 문항수 | 일치율 |
|---|---:|---:|
| 공식 | — | 70.0% |
| 공통 | — | 69.3% |
| 사업지정 | — | 65.3% |
| L3 | 27 | 59.3% |
| 보강 | — | 39.3% |
| **전체** | **320** | **67.5%**(다수결기준선 59.8 · +9.5) |

인용 57.8% · 근거도달 56.6%. 치명오답 — 코드 정의 1건 / 넓은 정의 25건(정의 두 종 병존,
[[기록/2026-09-07]] 정정 참고). 🔴 「L3 가 평가에 안 실렸다」는 틀린 서술이었다 — L3 27문항
중 26건이 실제로 L3 문서를 인용했다(`설정.L3.B1블록_문항수=0` 은 죽은 필드였다).
run 194·195 는 옛 산출이니 세트 구성이 다르다 — 직접 비교하지 않는다.

## L3 — 고유 조건 9개가 천장

경상국립대 L3 문서(203 article) 전량 검토: L1/L2와 실제로 갈리는 조건은 **9개**뿐,
나머지 ~70%는 L2 세부관리기준 원문 복붙. 골든셋 L3 27문항 = 9조건 × 3버전(공식+변형2).
🔴 **분모는 조건단위 9다.** 27로 나누면 판정력이 부풀려진다.

## 실서버 → 상세 [[0-2_실서버_배관]]

`https://suddoe-api-1081277785480.asia-northeast3.run.app` — Cloud Run **v20 ·
suddoe-api-00032-d24**, 프론트 `fae297d`. `/api/health` 200, DB는 Cloud SQL
`suddoe-db`(asia-northeast3) 연결. 남은 배관 이슈는 [[9_미결]] 참고.

> 다음 — 안 정해진 것 [[9_미결]] · 도는 방식 [[2_구조/2-2_판정_흐름]] · 시점 기록 [[기록/00_기록_목차]]
