# 0. 현황

- 지금 무엇까지 됐나
- 다음에 무엇을 치나
- 지금 수치는 얼마인가

> 실측 수치는 SessionStart 훅이 매 세션 다시 뽑는다. 이 표와 다르면 훅이 맞다.

## 구현 순서

**UI 보다 CLI 판정기가 먼저다.** 판정 품질이 정답셋을 못 넘으면 UI 는 의미가 없다.

| | 단계 | 상태 |
|---|---|---|
| 1 | 판정 단계 격리 테스트 (일치율 84.7%) | ✅ |
| 2 | Stage 0 표 추출 (22문서 · 표 469 · 실패 0) | ✅ |
| 3 | 정답셋 검수 종결 (77문항 verified) | ✅ |
| 4 | 룰 테이블 `corpus.rules` 72행 / 7사업 + L1 공통 9 | ✅ (검수 53/72) |
| 5 | 청킹 단위 확정 — 조 + 900토큰 초과 시 항 | ✅ |
| 6 | BM25 + RRF 검증 · 가중 0.9/0.1 | ✅ |
| 7 | 쿼리 파이프라인 + CLI 판정기 `judge_cli` | ✅ 2026-09-01 |
| **7-b** | **`max_tokens` 1500 → 3000 재측정** | 🔴 **나머지 전부의 선행조건** |
| 7-c | 정답셋에 재도전 · TIPS · L3 문항 추가 | 미착수 |
| 7-d | A12 프롬프트 변형 6개 A/B | 미착수 |
| 8 | F1·F3·F4 폼 + 전제 해소 루프 | 미착수 |
| 9 | 모델 검증 사다리 (RunPod) | 미착수 |
| 10 | 웹 UI + 배포 | 미착수 |

**7-b 가 먼저인 이유** — 1500 인 채로 잰 값은 「잘림」과 「판정」이 섞여 해석이 안 된다.

## 실측 (2026-09-02 기준)

| 항목 | 값 |
|---|---|
| `corpus.chunks` | 20,525 (L1 20,030 · L2 495 · 232문서) |
| `corpus.refs` | 44,865 — resolved 32,043(71.4%) · shifted 43 · 끊긴 참조 12,779 |
| └ 끊긴 참조 중 조 지정된 것 | 1,150. 나머지는 검색 범위 밖 인용이라 정상이다 |
| `corpus.doc_articles` | 26,304 |
| `corpus.rules` | 72 (7사업 + L1 공통 9) |
| `corpus.precedence_rules` | 8 |
| `eval.golden_set` | 113 |
| `tenant.decisions` | 73 |
| BM25 `chunk_terms` | 1,148,718 |
| pytest | 134 passed · 2 skipped · 6 xfailed |

## 판정 성능 (2026-09-01 · 정답셋 77문항 실전 E2E)

| 지표 | 값 |
|---|---|
| 일치율 | 67.5% |
| 치명 오답 | **0** |
| 인용률 | 29.7% |
| 검색 hit@5 | 55.4% → 60.8% (붙임2 비목 해설표 복귀 후) |

🔴 **조건이 다른 run 끼리 hit@k·일치율을 빼지 마라.** 사업필터·정답출처·분모·규정 모음
버전이 다르다. 설정은 `eval.runs.설정` 에 전부 박혀 있다.

## 검색 미스 33건 분해 — 랭킹은 손대지 마라

| 원인 | 비중 |
|---|---|
| 필터밖 | 31.1% |
| 랭킹 | 9.5% |
| 후보밖 | 4.1% |

검색기 튜닝으로 고칠 수 있는 건 9.5% 뿐이다. 180조합 스윕이 최고 +1문항(노이즈)이었던
이유가 이것이다. **RRF 가중을 만지지 마라.** 실제 병목은 필터밖 = [[9_미결]] #14 의
제품 정의 결정이다.

## 종결된 것 — 다시 열지 마라

| 항목 | 근거 |
|---|---|
| ANN 도입 | 미사용 확정. 20,055청크에서 전량스캔 134ms·recall 100%, HNSW 는 recall 82~91% |
| 청킹 단위 | 조 + 900토큰 초과 시 항 |
| 정답셋·룰 D단계 검수 | golden_set 77/77 · rules 19/19 · precedence 8/8 |
| tenant 스키마 분리 | `corpus`/`tenant`/`eval` · `public` 테이블 0개 |
| 재도전 판본 역전 | 현행판(2026년 11차 개정) 28청크로 교체 |
| 비목 어휘 사전 | 31비목 · `guided_json_enum` 10종 |

## 다음 문서

- 무엇이 안 정해졌나 → [[9_미결]]
- 어떻게 도는가 → [[2_구조/2-2_판정_흐름]]
- 언제 무엇을 했나 → [[기록/00_기록_목차]]
