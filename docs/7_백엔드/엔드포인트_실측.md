# 엔드포인트 실측 (2026-09-02)

뜬 방법: `app.openapi()["paths"]` — HEAD·OPTIONS 제외.
🔴 `app.routes` 로 세면 안 된다. 이 FastAPI 버전은 `include_router` 를 평탄화하지 않아
라우터 9개가 통째로 빠진다.

**경로 18 · 메서드 20.** 계약 v1.0.1 §1 표와 일치한다 (오늘 변경분은 파라미터만이다).

## `/api` — 프론트 계약 대상 16메서드

| 메서드 | 경로 | 쿼리 파라미터 |
|---|---|---|
| GET | `/api/health` | — |
| GET | `/api/vocab` | 사업명 |
| GET | `/api/programs` | — |
| POST | `/api/normalize` | — |
| POST | `/api/judge` | 목 |
| GET | `/api/plans` | 탭, 사업명, 확정비목, q, 금액_최소, 금액_최대, 정렬, 페이지, 크기, org_id |
| POST | `/api/plans` | — |
| GET | `/api/plans/{plan_id}` | org_id |
| POST | `/api/plans/{plan_id}/tasks` | **org_id** 🆕 |
| PATCH | `/api/plans/{plan_id}/tasks/{task_id}` | **org_id** 🆕 |
| POST | `/api/plans/{plan_id}/tasks:sync` | **org_id** 🆕 |
| GET | `/api/tasks` | 상태, 구분, plan_id, 일정만, 이후, org_id |
| POST | `/api/l3/upload` | — (org_id 는 form 필드 · 필수) |
| GET | `/api/l3/{doc_id}` | org_id |
| GET | `/api/profile` | org_id |
| PUT | `/api/profile` | org_id |

🆕 = 2026-09-02 추가. 셋 다 선택 파라미터라 **OpenAPI 스펙의 메서드 수는 안 늘었다.**

## `/admin` — 계약 밖 4메서드

| 메서드 | 경로 | 쿼리 파라미터 |
|---|---|---|
| GET | `/admin/cost` | — |
| GET | `/admin/gate` | — |
| GET | `/admin/queue` | 종류, 사유코드, 사업명, 상태, limit, offset |
| POST | `/admin/warmup` | — |

프론트 계약 대상이 아니다. `tests/test_contract.py` 의 「늘어남」 검사도 `/api` 만 본다.

## 이 표를 잠그는 테스트

- `test_계약_엔드포인트가_전부_살아있다` — 빠짐 검사
- `test_계약에_없는_api_가_늘지_않았다` — 늘어남 검사 (`/api` 한정)

둘 다 `tests/test_contract.py`. 경로를 늘리거나 지우면 빨개진다.
