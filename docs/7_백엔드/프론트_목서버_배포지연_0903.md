# 프론트 목 서버 — 배포본이 레포보다 뒤에 있다 (2026-09-03 중앙 실측)

> [[프론트_레포_0903]] §6 을 갈라낸 것이다 (100줄 상한). 레포·`lib/`·환경변수는 그쪽.

프론트 팀원이 올린 Cloud Run 목 서버는 `4ba8d3f` 기준 — 즉 `1e64150` **이전**이다.
레포에서 고친 것이 브라우저에는 아직 안 닿는다.

```
GET  /api/health                                            200  {"모드":"mock"}
OPTIONS /api/plans/1/tasks/11  + Request-Method: PATCH       400   ← 여전히 막혔다
OPTIONS /api/plans             + Request-Method: POST        200   ← 대조군
```

주소: `https://suddoe-mock-api-362008123976.asia-northeast3.run.app`

- 프론트에 「CORS 고쳤다」만 전하면 안 된다. **「`1e64150` 이후로 목 서버를 다시 올려라」**까지 가야 한다
- 🔴 `GET /api/plans/{id}` 500 — **배포본에서 끝까지 재현했다** (ai-a3, 같은 날 오후).
  `POST` 가 201 인 걸 알고 나서야 이 사슬을 태울 수 있었다:
  ```
  POST /api/plans            → 201  plan_id=8
  GET  /api/plans/8  (새 것)  → 500  Internal Server Error   🔴
  GET  /api/plans/1  (시드)   → 200                          ← 대조군
  GET  /api/plans            → 200  건수 8 (목록엔 반영된다)
  ```
  **「새로 만든 계획에서만 난다」가 추정이 아니라 실측이 됐다.** 사용자 흐름으로는
  「계획을 만들면 목록엔 보이는데 눌러서 들어가면 오류」다 — 데모에서 반드시 밟는 자리다.
- 🔴 **정정 (같은 날 오후 · ai-a3 재측정).** 위 괄호에 적었던 「`POST /api/plans` 가 400 · `python-multipart`
  때문으로 추정」은 **사실이 아니다. 다시 재니 201 이다.**
  ```
  POST /api/plans   (목) --data-binary @UTF-8파일 → 201 {"plan_id":7,…}
  POST /api/normalize(목) → SSE 정상 (진행 → 필드 {"품목":"맥북"})
  ```
  400 은 **클라이언트 인코딩 문제**였다. Git Bash 에서 `curl -d '{"사업명":…}'` 로 한글을 인라인으로 넘기면
  바이트가 깨져 `{"detail":"There was an error parsing the body"}` 가 난다. `--data-binary @파일` 로 바꾸면 201 이다.
  🔴 **단서는 「목·실 양쪽이 똑같이 400」이었다는 것이다 — 서버 둘이 같은 증상이면 서버가 아니라 클라이언트다.**
  추정을 「재지 않았다」로 표시해 둔 덕에 뒤집을 수 있었다. 표시가 없었으면 실측으로 굳었다.
  (PATCH 프리플라이트 400 은 **여전히 참이다** — 그건 재배포로만 풀린다)
- 시드 `GET /api/plans/1` 은 200 이다 — 500 은 **새로 만든 계획에서만** 난다는 진단과 어긋나지 않는다

## 🔴 「우리가 못 만진다」 — 이제 추정이 아니라 실측이다

오전엔 인계 문서를 근거로 적었는데, `gcloud` 로 직접 쟀다 (ai-a3, 계정 `dogun5578@gmail.com`).

```
$ gcloud projects list
  gen-lang-client-0130904016   550540589624    Gemini API
  glass-arcade-464115-k8       775507864313    My First Project
  project-35d896d7-67d7-4b2a-a8f  1081277785480  My First Project   ← 실서버·Cloud SQL 이 여기
  (362008123976 은 목록에 없다)

$ gcloud projects describe 362008123976
  ERROR: [dogun5578@gmail.com] does not have permission to access projects instance
```

- 우리 프로젝트에 있는 것: Cloud Run `suddoe-api` · Cloud SQL `suddoe-db`(POSTGRES_17)
- 목 서버가 있는 `362008123976` 은 **이 계정으로 조회조차 안 된다** — 재배포·환경변수 변경 불가
- 🔴 **단, 「불가」로 닫기 전에 계정을 확인하라.** 오너가 «다른 구글 계정»으로 편집자 초대를 받았을
  수 있다. 그 계정으로 `gcloud auth login` 하면 결과가 달라진다. 지금 결론은 정확히
  **「`dogun5578@gmail.com` 으로는 안 된다」**까지다 — 그 이상으로 쓰지 마라

→ 누가 언제 올릴지는 [[9_미결]] #21.
