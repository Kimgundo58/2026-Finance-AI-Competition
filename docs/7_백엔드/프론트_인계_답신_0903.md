# 프론트 인계 답신 (2026-09-03)

`프론트_백엔드_인계_수정1차본.md`(550줄) 전건에 답한다. **긴급 3건은 닫혔고, 실서버 주소가 바뀐다.**
원문 `docs/부록/프론트_인계_수정1차본_0903.md` · 계약 `API_계약_v1.0.md` · 화면 대응 `프론트_화면별_지원표.md`.

## 0. 🔴 먼저 — 실서버는 이미 우리 쪽에 서 있다

```
목  서버 (그쪽 GCP 362008123976)  https://suddoe-mock-api-362008123976.asia-northeast3.run.app
실 서버 (우리 GCP · 신규)         https://suddoe-api-1081277785480.asia-northeast3.run.app
```

§2-1 ②의 「실서버도 제가 올립니다」는 **안 해도 된다.** 우리가 09-03 에 올렸다. §3-3 의 네 물음
(파이썬 버전 · 환경변수 · 임베딩 로딩 · vLLM)도 답이 필요 없어졌다 — `Dockerfile`·`requirements-api.txt` 가 레포에 있다.

- `/api/health` → `{"ok":true,"모드":"live"}` · 0.27s · DB 는 Cloud SQL `suddoe-db` · 규정 20,525조각 · 기관 413건
- 🔴 **읽기만 된다. 다만 원인이 우리가 오전에 말한 것과 다르다** — 권한이 아니라 **신원**이다.
  DB 는 이미 열려 있다(비특권 롤로 Cloud SQL 에 계획·판정·업로드·프로필 INSERT 전부 실측 성공).
  막는 건 **로그인이 안 서서 「어느 기관인가」가 안 정해지는 것**이고, 그러면 격리 규칙이 쓰기를 막는다.
  → 🔴 **`POST /api/plans` 가 지금 `503 "DB 연결 실패"` 를 돌려주는데 그 문구는 거짓이다.**
  DB 는 붙어 있다. 문구도 오늘 고친다 — 프론트에서 이 503 을 「서버 죽음」으로 처리하지 말 것

**목 서버는 그쪽 프로젝트라 우리가 못 만진다.** 지금 배포본이 레포 `4ba8d3f` 라 오늘 고친 게 안 닿아 있다.
아래 §2 를 반영하려면 **목 서버 재배포가 필요하다.**

## 1. 🔴 긴급 3건 — 닫혔다

| § | 요청 | 상태 |
|---|---|---|
| 3-1 | CORS 에 `PATCH` | ✅ `main.py:336` · **실서버 프리플라이트 실측 200, `allow-methods` 에 PATCH 있음** |
| 3-2 | 새 계획 조회 500 | ✅ `routes_plans.py` 상세 — `{**행, "정규화": 행.get("정규화") or {}}` 로 고쳤다 |
| 부록D | 목 「결제후」 할일 0건 | ✅ `mock_data.py:117~138` 에 결제후 3건(세금계산서 · 자산관리대장 · 사업자등록증) |

**세 건 모두 실서버엔 이미 반영돼 있다. 목 서버는 재배포해야 보인다.** 프론트 우회로는 그때 빼면 된다.

## 2. §3-5 여덟 물음 — 답

| # | 물음 | 답 |
|:--:|---|---|
| 1 | 목에 결제후 할일 | ✅ 넣었다 (위) |
| 2 | SSE `해야할일` 에 `구분`·`code` | 🔧 **오늘 작업 중.** 스키마(`models.py:126·140`)엔 있는데 SSE 로 안 나갔다. 오늘 중 실린다 |
| 3 | 폼 경로 `비목후보` | 🔧 검토 중. **LLM 호출을 늘려야 하면 안 넣는다**(판정 1건 = LLM 2회가 확정 원칙). 오늘 중 답 |
| 4 | `확정비목` 을 엔진에 넘기나 | **(A) 넘긴다.** 사용자 선택을 존중한다. 화면 11 에 별도 표시 자리는 안 만들어도 된다 |
| 5 | `PUT /api/profile` 실서버 | 엔드포인트는 산다(200). **저장은 GRANT 열려야 된다** — 오늘 안. 프론트는 그대로 두면 된다 |
| 6 | `.docx` | **안 받는다.** `accept` 에서 빼는 판단이 맞다. 파서가 생기면 그때 다시 넣는다 |
| 7 | `/api/programs` 의 `비고` | 🔧 실서버 실측 확인 중. **목에서만 나는 문자열인지 오늘 답한다** |
| 8 | `/api/profile` F3·F4 | **이번 제출 범위에서 뺀다.** 마이페이지는 F1(협약기간·사업비)만 |

## 3. §3-6 기관 목록 API — ✅ 이미 있다

```
GET /api/orgs?q=<검색어>&page=&size=   →  {"총건수":413, "항목":[{"slug","기관명","사업명"[]}]}
```

**실서버에서 413건 실측.** §4-1 대로 **`org_id` 를 응답에 안 싣는다.**
밖으로 나가는 손잡이는 `slug`(HMAC · 역산 불가)뿐이고 slug→org_id 는 서버 안에서만 푼다.

🔴 **온보딩②에서 고른 값은 `slug` 를 그대로 들고 다니면 된다.** `org_id` 를 프론트가 만질 일은 없다.

> 다만 이걸 「격리」로 읽지는 말 것. org_id 는 공개 기관명에서 재계산된다(411/413 실측). 진짜 격리는 RLS 가 한다.

## 4. §3-4 시연용 기관 — 결정 대기

`tenant.accounts` 가 **0행**이라 지금은 로그인이 성공해도 전부 거부된다. 건국대학교로 심는 안을
올려 뒀고 **오너 결정 대기다.** 정해지면 slug 하나를 바로 준다.

## 5. §3-7 P11 · §3-8 AI 챗봇

- **P11(규정·지침)** — 폐기가 아니라 **이번 제출 범위 밖**이다. 대응 화면 없는 게 맞다
- **AI 챗봇(화면 12)** — 🔴 **ⓑ 이번 범위에서 뺀다.** 엔드포인트를 만들 계획이 없다.
  「하드코딩 답변을 심사위원이 보는 게 제일 나쁘다」에 동의한다. **메뉴에서 빼는 쪽**을 권한다
  (「준비 중」은 눌러볼 여지를 남긴다). 여유가 생기면 그때 얹는다

## 6. 🟩 프론트가 지금 바로 해도 되는 것 — 우리와 안 겹친다

우리는 오늘 **서버 코드(`server/`) · DB 권한 · 테스트**만 만진다. 프론트 레포는 안 건드린다.

| | 작업 | 비고 |
|:--:|---|---|
| 1 | 배지 문구 정리 + **`문의초안` 노출** | 무충돌. 심사에서 좋은 장면이 된다는 판단에 동의한다 |
| 2 | 시연 시나리오 4종 리허설 | 무충돌 |
| 3 | 규정 업로드 배선 (`accept` 정리 · 20MB→**30MB**) | 무충돌. 🔴 **실서버 업로드 성공은 GRANT 이후** |
| 4 | 기준연도 · 기관 라벨 | 무충돌 |
| 5 | **온보딩 기관 선택** | ✅ 막힘 해제 — `GET /api/orgs` (§3) |
| 6 | **할일 체크 저장** | ✅ 막힘 해제 — 실서버 PATCH 열렸다. 목 서버는 재배포 후 |
| 7 | 챗봇 메뉴 정리 | §5 |

## 7. 🔴 오늘 하루만 조심할 것 하나

**실서버로 base URL 을 돌려도 읽기 동선은 전부 산다**(홈 · 목록 · 상세 · vocab · programs · orgs · 판정).
그런데 **쓰기 동선 4개는 GRANT 전까지 실패한다** — 계획 저장 · 할일 체크 · 프로필 저장 · 업로드.
→ **개통되는 즉시 연락한다.** 그때 전환하는 게 안전하다.

## 8. §4 「바꾸면 조용히 깨지는 것」 — 다섯 건 다 유지한다

`org_id` 미노출(4-1) · 계약 표면(4-2) · 상태 어휘 4종(4-3) · 비목 별칭 흡수(4-4) · 목/실 응답 동형(4-5).

🔴 특히 **4-5 는 어제 우리가 실제로 데인 자리다.** 목이 `인용` 을, 실경로가 `인용목록` 을 쓰고 있어서
**테스트 241건이 전부 초록인 채로 실서버 판정 전수가 근거 0건으로 나갔다.** 고쳤고,
「목 응답 키집합 == 실경로 응답 키집합」을 오늘 회귀 테스트로 못 박는 중이다.
