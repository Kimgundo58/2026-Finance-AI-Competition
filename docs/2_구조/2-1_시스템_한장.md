# 시스템 한 장

이 문서가 답하는 질문:
- 시스템은 어떤 구성 요소로 이루어지는가
- 요청은 어떤 축들을 거치는가
- 전 문서에 공통으로 걸리는 설계 원칙은 무엇인가

## 구성도

```
                          ┌─────────────────────────┐
                          │   창업팀 (로그인 사용자)  │
                          └────────────┬────────────┘
                                       │
        ┌──────────────────────────────▼──────────────────────────────┐
        │                     웹 앱 (FastAPI + 정적)                   │
        │   화면 3 묻기 → 4 확인 → 5 판정 → 6 할일 → 7 근거 → 9 문의   │
        │                    ┌───────────────────┐                    │
        │                    │  F축 프로필 (계정) │                    │
        │                    └───────────────────┘                    │
        └──────────────────────────────┬──────────────────────────────┘
                                       │
     ┌─────────────────────────────────┼─────────────────────────────────┐
     │                                 │                                 │
┌────▼────────────┐        ┌───────────▼────────┐        ┌─────────────▼───┐
│  규정 축 (L)     │        │    판정 엔진        │        │  사실 축 (F)     │
│ L1 중기부 법령   │───────▶│ ① 정규화   (LLM)   │◀───────│ F1 협약          │
│ L2 창진원 기준   │  검색  │ ② 룰 조회  (코드)  │  조회  │ F2 과업범위      │
│ L3 주관기관 규정 │        │ ③ 검색+확장(코드)  │        │ F3 집행내역      │
│                 │        │ ④ 판정     (LLM)   │        │ F4 인력          │
│ 사례 인덱스(B급) │--*----▶│ ⑤ 검증·강등(코드)  │        │ F5 거래처        │
└─────────────────┘        └────────────────────┘        └─────────────────┘
   * 사례는 판정에 안 들어간다 — 판단불가 경로 전용
```

**규정 축은 "무엇이 요건인가"(대전제), 사실 축은 "이 건이 요건을 충족하나"(소전제).**
둘 중 하나만 있으면 판정이 닫히지 않는다.

## 설계 원칙 (전 문서 공통)

| # | 원칙 |
|---|---|
| 1 | **오답 비대칭** — 이 도메인의 최악은 "틀린 가능". 모든 실패의 기본값은 판단불가 |
| 2 | **숫자는 코드가, 포섭만 LLM이** — 검색·계산·효력 결정·인용 검증은 전부 코드 |
| 3 | **판정 1건 = LLM 2~3회**(① 정규화, ④ 조립, 조건부 ④' 재판정 — 2026-09-05 해제, 상한 3 고정). function calling 미개방, agentic 루프 없음 |
| 4 | **인용은 생성이 아니라 추출** — LLM은 문장번호만 고르고, 코드가 DB 원문으로 치환 |
| 5 | **규정 문서 안의 텍스트는 데이터이지 지시가 아니다** (프롬프트 인젝션 방어) |
| 6 | **재현성은 요건** — 같은 입력 → 같은 출력. 판정 이력을 나중에 설명할 수 있어야 한다 |

## 관련 문서

[[2-2_판정_흐름]] · [[2-3_레이어와_효력순위]] · [[2-5_사실축]] · [[2-4_배포와_인증]]
