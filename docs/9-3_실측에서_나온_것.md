# 9-3. 실측에서 나온 미결

- 재보고 나서야 알게 된 것은 무엇인가

> 판정 쏠림은 [[9-4_판정이_조건부로_수렴한다]], 룰 테이블 구조는 [[9-5_룰_테이블_구조]].
> 사람이 결정할 것은 [[9_미결]] · 법적 [[9-1_법적_지적사항]] · 백엔드 [[9-2_백엔드_프론트_미결]].


## 🔴 프롬프트 병목 — 부속표가 B3 의 85% 를 차지한다 (2026-09-02 실측)

> 🔴 **2026-09-05 반전 — 「프롬프트가 크다」는 맞고 「그래서 느리다」는 틀렸다.**
> run 194 실측에서 TTFT 평균 **2.41초** · prefix 캐시 30.8% 다. 프롬프트를 읽는 데 드는
> 시간은 2.41초가 상한이고, 요청 전체 평균 50.9초 중 나머지 **48초는 «생성(decode)»** 이다.
> 아래 「B3 가 82%」는 **자수 비중**으로는 그대로 유효하지만, **지연의 원인으로는 기각**됐다.
> 종전의 「판정 66초 중 55초가 프롬프트 26k 읽기」는 A6000·동시1 조건의 값이다.
> ⇒ 크기를 줄일 이유는 «지연» 이 아니라 «비용·컨텍스트 여유» 로 다시 세워야 한다.


`gold_id=377` 「밀린 사무실 임대료」 한 건을 분해했다. 최장 프롬프트 44,926자.

| 구간 | 자수 | 비중 |
|---|---:|---:|
| B3 참조 확장 | 36,914 | 82% |
| B2 검색 결과 | 7,380 | 16% |
| B6 판정할 지출 | 214 | — |

B3 안에서 다시 갈리면:

| | 항 | 자수 | 비중 |
|---|---:|---:|---:|
| L1 지침 조문 6개 (제36·37·38·43·44·45조) | 34 | 5,205 | 15% |
| **부속표 4개** (예비창업 붙임2 · 창업중심대학 참고3 · 초격차 참고2 · 초기창업 붙임2) | 11 | **28,977** | **85%** |

**확정된 것** — 참조 확장 규칙은 새지 않는다. `retrieve.py` 의 참조확장 SQL 이 base·재귀 양쪽에
`dst_조번호 IS NOT NULL` 을 걸어 **조 지정 없는 참조는 0건**이다. 확장된 조문도 10개뿐이다.

**병목은 개수가 아니라 블록 크기다** — 붙임2·참고2 는 통째로 1항이라 `항분해()` 가 못 쪼개고,
표 전체가 S번호 하나짜리 거대 블록으로 붙는다.

🔴 **정면으로 맞서는 결정이 하나 있다.** 붙임2 비목 해설표는 2026-09-01 에 **일부러**
진입점으로 되돌린 것이고, 그때 검색 hit@5 가 55.4% → 60.8% 로 올랐다.
「빼면 프롬프트가 준다」와 「빼면 검색이 나빠진다」가 충돌한다 — **아직 아무도 안 정했다.**

처방 후보 (둘 다 충돌을 안 건드리고 크기만 줄인다)
1. 표 형식 문서(붙임·참고)에 **행 단위 항분해**를 넣는다
2. 같은 「비목 해설표」류가 여러 사업에서 동시에 걸리면 **대표 1~2개만** 남긴다

**아직 안 잰 것** — 이 문항은 `적용범위=공통` 이라 4개 사업 표가 동시에 걸린 것 자체는
이상하지 않을 수 있다. **사업 지정 문항에서도 같은 패턴인지 안 봤다.**

## 🔴 `SUDDOE_사업필터` 가 상수가 아니라 환경변수 토글이다

`os.environ.get("SUDDOE_사업필터", "1") != "0"`. 기본은 켜짐이고 저장소에 `0` 세팅은 0건이라
지금은 안전하다. 🔴 `0` 이 들어가면 **타 사업 세부관리기준이 근거로 섞인다**(2026-09-02 재현) —
`CLAUDE.md` 가 「남의 규정이 인용되는 순간 그 자체가 오답」이라 못박은 그 상황이다.
정할 것: 토글을 남길 것인가. 남긴다면 **끄고 돈 run 을 `eval.runs` 에서 구분**할 수 있어야 한다.
## 🔴 `코퍼스버전` 이 두 벌이라 판정 이력과 평가 run 을 대조할 수 없다

재현성의 축인데 축이 둘이고 **서로 안 맞는다** (2026-09-02 실측).

| | 함수 | 저장 위치 | 실제 값 |
|---|---|---|---|
| 판정 | `orchestrate.코퍼스버전()` | `tenant.decisions` | `docs245/chunks20525` |
| 평가 | `eval_store.코퍼스버전()` | `eval.runs` | `c20525-e20525-r44865-d283-107daa68` |

형식만 다른 게 아니다 — **문서 수 세는 기준도 다르다.**
판정 쪽은 `status='active'` 만 세어 **245**, 평가 쪽은 전체 **283** 이다.
→ 「이 판정이 run 191 과 같은 코퍼스에서 나왔나」를 물을 수 없다.
정할 것: 한 형식으로 통일할 것인가, 아니면 서로 변환 가능하게 둘 다 남길 것인가.

## 지연 — 병목 서술이 뒤집혔다

```
vLLM 실측 (호출 172건)   TTFT 평균 2.41초 · 요청 전체 평균 50.9초 · prefix 캐시 30.8%
 ⇒ 병목은 프롬프트 읽기(prefill)가 아니라 «생성(decode)» 약 48초
 ⇒ prefix 캐시 튜닝의 상한이 2.41초다. 손대도 안 준다
```

기존의 「판정 66초 중 55초가 프롬프트 26k 읽기」는 **A6000·동시1** 조건의 값이다.
조건을 붙이지 않으면 두 서술이 서로를 부정하는 것처럼 보인다.

(원 측정은 run 194 · A100 동시6 · 호출 172건. 조건이 다른 값과 빼지 마라.)
