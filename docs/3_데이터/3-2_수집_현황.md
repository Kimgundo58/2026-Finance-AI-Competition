# 수집 현황 — L1 조달

이 문서가 답하는 질문:
- L1 법령 모음은 어디서 어떻게 조달했는가
- 재크롤링이 필요할 때 무엇을 다시 돌리는가
- 법령별 인용 조항 상세는 어디서 찾는가

> **조달은 끝난 작업이다.** 이 문서는 law.go.kr 을 다시 칠 때(개정 대응·범위 확장)만 편다.
> 수량 실측은 SessionStart 훅이 기준이다.

## 자산 위치

| 자산 | 위치 |
|---|---|
| 현행 법령·행정규칙 XML | `법령 PDF/L1_법령/` |
| 구판(시점본·연혁) XML | `법령 PDF/L1_법령/연혁/` |
| 첨부 (별표·본문공백 보완) | `법령 PDF/L1_법령/첨부/` |
| 행정규칙 별표 (HWP→PDF 변환 완료) | `법령 PDF/L1_법령/별표/` + `_hwp변환/` |
| 범위 밖 보류 | `법령 PDF/L1_법령/_범위밖_보류/` |

수집 경로는 국가법령정보 OPEN API(`law.go.kr/DRF`), 포맷은 XML.

## 스크립트

| 스크립트 | 역할 |
|---|---|
| `scripts/Law_Crawling.py` | 창진원(L2) 배치 |
| `scripts/mss_crawl.py` | 중기부(L1) 배치 |
| `scripts/fetch_admrul_history.py` / `_attachments.py` / `_appendix.py` | 행정규칙 구판·첨부·별표 |
| `scripts/build_delegation_paths.py` | 위임 계통 복원 |
| `scripts/build_law_sources.py` | 출처 통합 인덱스 + 판정 인덱스 편입 판정 |
| `scripts/build_mss_doc.py` | 중기부_법령_링크모음.md 재생성 |
| `scripts/convert_hwp.py` | HWP/HWPX → PDF |
| `scripts/verify_corpus.py` | 마스터 ↔ 디스크 전수 대조 |

## 운영 규칙 (핵심)

- **파일은 규범당 한 벌만.** 출처는 `_law_sources.json`의 `sources` 배열로 구분
- **판정 인덱스 편입은 `index` 플래그** — 직접참조 여부로만 결정 (위임으로 딸려온 것은 제외)
- 위임 추적 3단계(법률→시행령·시행규칙→위임 행정규칙), `위임구분=인용법령`은 제외

## API 함정 (상위 5, 전체 14건은 원본 참조)

| # | 함정 | 대응 |
|---|---|---|
| 1 | JSON은 단일 원소가 배열이 아닌 객체로 온다 | XML 채택 |
| 2 | `lsDelegated` 위임 일련번호는 구판 | 개정 불변 키로 현행판 재조회 |
| 4 | `위임구분=인용법령`은 위임이 아니라 참조 | 따라가지 않음(전체 법령 번짐) |
| 12 | `admbyl` 별표 확장자 `.hwp`인데 실제 HWPX 다수 | 매직바이트 판별 |
| 14 | 다단 레이아웃 PDF는 컬럼이 단어 사이에 끼어든다 | 좌표 기반 컬럼 분리 |

전체 함정 목록·재실행 명령·산출물 파일 설명: [[부록/법령_수집_전문]].

## 법령별 인용 조항 상세 — 링크모음 3개

세부관리기준이 실제로 인용하는 개별 법령·조항 매핑은 룩업 테이블이라 이 문서에
옮기지 않는다. 원본을 그대로 참조한다.

| 문서 | 다루는 사업 |
|---|---|
| `법령 연계 모음/문서_법령_링크모음.md` | 초기창업·초격차·예비창업·재도전·창업도약·창업중심대학·모두의창업·TIPS 통합 |
| `중기부_법령_링크모음.md` | L1(중기부) 문서가 참조하는 법령·행정규칙 마스터 |
| `법령 연계 모음/초기창업패키지 & 초격차 패키지.md` | 초기창업·초격차 파일별 상세(원본 조사) |

## 관련 문서

[[3-1_검색_범위]] · [[3-4_데이터셋_구조]]
