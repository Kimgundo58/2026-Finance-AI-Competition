# 데이터 전처리 파이프라인

작성 2026-08-30 · 대상 저장소 **Supabase (Postgres 17 + pgvector)**

> 원문 문서를 판정 인덱스에 넣을 수 있는 형태로 바꾸는 오프라인 공정의 사양이다.
> 설계 근거는 `RAG.md`, 룰 규격은 `rule_base.md`, 상위 구조는 `서비스 아키텍쳐.md` 에 있다.
> 이 문서는 **무엇을 어떤 순서로 어떻게 자동화하는가**만 다룬다.

---

## 1. 설계 원칙

| # | 원칙 | 구현 |
|:-:|---|---|
| 1 | **단계는 파일로 끊는다** | 각 단계의 산출물이 JSON 파일 하나다. 다음 단계는 그 파일만 읽는다. 중간을 고쳐도 앞 단계를 다시 돌리지 않는다 |
| 2 | **멱등하다** | 같은 입력에 같은 산출. 실행 횟수가 결과를 바꾸지 않는다 |
| 3 | **실패가 조용히 지나가지 않는다** | 모든 단계에 검증 게이트가 있다. 통과 못 한 문서는 산출물에 남되 `parse_quality='low'` 로 표시되고 판정 인덱스에서 빠진다 |
| 4 | **코퍼스 경계는 코드가 강제한다** | 문서로 적은 규칙은 지켜지지 않는다. `scripts/index_guard.py` 가 경로·레이어를 조건 없이 거부한다 |
| 5 | **인용은 추출이지 생성이 아니다** | 조번호·본문은 원문에서 그대로 온다. 어느 단계도 조번호를 새로 짓거나 정정하지 않는다 |

원칙 5가 자동화 설계를 가장 크게 제약한다. 조번호가 원문에서 중복되거나 역순이어도
파이프라인은 그것을 고치지 않는다. 고치면 사용자가 실제 문서에서 그 조를 찾지 못한다.
원문 오류는 오류인 채로 보존하고 플래그를 단다.

---

## 2. 전체 흐름

```
[원본]                  [단계]                        [산출물]                    [소비처]

법령 XML 219        +-- Stage 0 ----------------> _stage0_articles.json  ---+
(law.go.kr)         |    파싱 / 조 단위 분해        _stage0_report.json      |
                    |    검증 게이트 V1~V3                                   |
L2 세부관리기준 8   |                                                        |
중기부 배포본 9     +-- Stage 0-T --------------> _tables.json  -------------+
사례집 10           |    표 추출 (별도 경로)                                 |
                    |                                                        |
HWP -> 한컴 변환    +-- Stage 0.5 --------------> _apply_target.json  -------+
(수동 1회, 완료)    |    적용대상 태깅                                       |
                    |    {주관기관|창업기업|공통|혼재}                       |
                    |                                                        |
                    +-- Stage 0.7 --------------> _refs.json  ---------------+
                    |    참조 그래프 (정규식)                                |
                    |                                                        |
                    +-- Stage 0.8 --------------> _precedence_rules.json ----+
                    |    우선순위 조항                                       |
                    |                                                        |
                    +-- Stage 1 ----------------> rules / _비목_어휘집.json -+
                         룰 컴파일 + 사람 검수                               |
                                                                             v
                                                                   [ Stage 2 경계 ]
                                                                   청킹 / 임베딩 / BM25
                                                                   -> Supabase 적재
```

**Stage 0.5 / 0.7 / 0.8 은 서로 독립이다.** 셋 다 Stage 0 산출물만 읽는다.
병렬로 돌려도 되고 하나가 실패해도 나머지는 진행된다.

**Stage 1 은 Stage 0-T 에 의존한다.** 한도와 증빙은 본문이 아니라 부속표에 있다.

---

## 3. 대상 코퍼스

### 3-1. 판정 인덱스에 들어가는 것

| 레이어 | 원천 | 라우팅 원본 | 실측 |
|---|---|---|---:|
| L1 | `법령 PDF/` 법령·행정규칙 XML | `_law_sources.json` 의 `index` 플래그 | **229 규범** |
| L1 | `2026_Finance_DATA_FOR_RAG/중기부/` | 디렉토리 스캔 | 9 |
| L2 | `2026_Finance_DATA_FOR_RAG/창진원/` 현행 세부관리기준 | 디렉토리 스캔 | **8 사업** |
| B급 | `사례집/` + `kosmes_faq.json` | 디렉토리 스캔 | 10 + 194 |
| L3 | 사용자 업로드 | 런타임 | 기관당 1벌 |

라우팅 원본이 둘인 이유가 있다. 법령은 위임 사슬을 따라 수집한 것이라
"이 규범이 판정에 필요한가" 를 사람이 판단해 `index` 플래그로 박아 뒀다(274건 중 229건 채택).
디렉토리 스캔으로는 그 판단을 복원할 수 없다.

### 3-2. 제외되는 것

성격이 다른 두 층이 있고 구현 위치도 다르다.

| 층 | 대상 | 어디서 막나 | 성격 |
|---|---|---|---|
| 절대 규칙 | `archive/` · `_골든셋/` · `_테스트_L3/` · `_범위밖_보류/` · 레이어 L4·L5 | `index_guard.py` | 넣으면 **그 자체가 오답**. 예외 없음 |
| 스코프 판단 | `PMS/` 42 · `_정리보류/` 3 · 법령 구판 944 | 오케스트레이터 | 지금은 불필요. 나중에 바뀔 수 있다 |

절대 규칙을 게이트로 뺀 이유는, `CLAUDE.md` 가 오래전부터 "인덱서가 경로 블랙리스트를
코드로 강제한다" 고 선언했으나 2026-08-28 확인 시점에 **실제 코드에는 없었기** 때문이다.
지금은 `tests/test_index_guard.py` 가 회귀를 막는다.

새 인덱싱 경로를 만들면 이 게이트를 반드시 태운다.

### 3-3. 파생 자료

`공유받은 파일/` 의 비목·증빙 종합표는 세부관리기준 21개 판본을 대조해 만든 2차 편집물이다.
**판정 인덱스에 넣지 않는다.** 용도는 §7-2 에 적었다.

---

## 4. 단계별 사양

### Stage 0 — 조(條) 단위 분해

**진입점** `scripts/stage0_run.py` · **산출** `_stage0_articles.json` · `_stage0_report.json`

형식별로 경로가 갈린다.

| 형식 | 경로 | 근거 |
|---|---|---|
| XML | `extract_xml()` — 그대로 조 단위 | law.go.kr 응답이 이미 조 구조다. 텍스트로 평탄화한 뒤 재분해하면 오히려 깨진다 |
| PDF | **`pdftext.extract_meta()`** -> `split_articles()` | 아래. `stage0_extract.extract_pdf()` 를 쓰면 안 된다 |
| HWP (조달분 L1·L2) | **파이프라인에 파서를 두지 않는다.** 한컴오피스로 1회 수동 변환한 PDF 를 쓴다 | 오픈소스 HWP 파서의 **표 추출** 정확도가 실무 수준에 못 미친다 — 조달분은 별표·표 위주라 치명적 |
| HWPX·HWP (사용자 업로드 L3) | **파서를 넣는다** (2026-08-30 결정). HWPX 는 zip/XML 직파싱, HWP 5.0 은 `hwp_extract.py` 계열 텍스트 추출. 이후 `split_articles()` 이하 동일, 게이트 V1~V4 동일 적용 | L3 규정은 조문 텍스트 위주라 표 약점이 덜하다. HWPX 는 논리 구조가 있어 PDF 좌표 재조립보다 정확. 배포용(DRM) HWP 는 파싱 실패 → PDF 변환 재업로드 안내 |

🔴 **PDF 는 반드시 `pdftext` 를 탄다.** `stage0_extract.extract_pdf()` 는 문자중복 레이어도
다단 조판도 해소하지 않는다. 통합관리지침 제14차가 `"창창업업기기업업등등"` 상태로 들어오면
`제\d+조` 가 하나도 안 걸려 `outline_numbered` 로 떨어지고 조 88개가 30개가 된다.
판정 최상위 근거 문서다.

🔴 **XML 은 `조번호_int` 를 조번호 문자열에서 파싱해 채운다.** `extract_xml()` 이 그 키를
주지 않는다. 비면 법령 229건 22,551조 전량이 `None` 이 되어 V1 단조성 검증이 무력화되고
`ix_articles_doc (doc_id, 조번호_int)` 정렬도 의미를 잃는다.

#### 텍스트 추출 — 조판 사고 3종을 좌표로 해소한다

| 사고 | 검출 | 처리 |
|---|---|---|
| 문자중복 레이어 | 같은 글자가 2겹 | `dedupe_chars()` |
| 2단 조판 | 세로 거터 (페이지별 검출) | 컬럼별 분리 후 재조립 |
| 4분면(4-up) | 세로 거터 + 가로 띠 (4중 조건) | 좌상 -> 우상 -> 좌하 -> 우하 |

`pdfplumber.extract_text()` 를 직접 부르지 않는다. 문자중복이 있는 문서에서
`제제5조조(전전문문기기관관)` 처럼 나와 `제\d+조` 정규식이 하나도 안 걸리고 **조용히 실패**한다.
`.claude/hooks/check_pdf_extract.py` 가 직접 호출을 경고한다.

다단 조판은 실측 5건이다(예비·초기·재도전·창업중심대학 거터 x421, 초격차 x297).
초격차는 2단이 아니라 A5 4쪽을 A4 한 장에 앉힌 4분면이다.
정규식 앵커 조정으로는 본문 혼입이 풀리지 않는다.

#### 섹션 분리 — 순서가 결과를 바꾼다

```
1. 목차 컷    점선 지도(·····) 10회 이상 + 마지막이 앞 30% 안
2. 부칙 탐지  줄머리 마커 + 뒤 300자에 "시행" 언명이 있는 첫 마커
3. 붙임 탐지  부칙 뒤에서만 찾는다
```

부칙 판별에 위치 비율이나 조 개수를 쓰지 않는다. 붙임이 길면 부칙이 문서의 27% 지점까지
앞으로 밀리고(초격차), TIPS 부칙은 `제N조` 가 아니라 `1. 동 지침은 …부터 시행한다` 로 시작한다.

붙임을 부칙 뒤에서만 찾는 것도 실측 근거가 있다. 8건 전수에서 섹션 헤더 후보 25개 중
**부칙 뒤 21개는 전부 진짜 헤더, 부칙 앞 4개는 전부 본문 속 참조**였다.

이 단계를 빠뜨리면 인용 위치가 틀린다. 실측: 부칙 `제1조(시행일)` 가 본칙 `제1조(목적)` 를
덮어써 그 조가 문서 끝까지 삼켰고, **붙임2 내용이 "제1조" 로 인용되는 상태**가 됐다.

#### 조 분해 전략 5종 — 앞에서부터 시도해 처음 성립하는 것을 쓴다

| 순위 | 전략 | 패턴 | 실측 |
|:-:|---|---|---:|
| 1 | `jo_titled` | `제N조(제목)` | 30 |
| 2 | `outline_numbered` | `제N장` -> `N.` -> `가.` | 2 |
| 3 | `jo_bare` | 제목 없는 `제N조` | 0 |
| 4 | `jang` | `제N장` | 0 |
| 5 | `paragraph` | 빈 줄 2개 | 15 |
| — | `case_paragraph` | 사례 레이어 — 조 분해 안 함 | 10 |
| — | `xml_native` | law.go.kr XML | 229 |

`outline_numbered` 가 `jo_bare` 보다 앞인 것이 중요하다. TIPS 본문에는 타 법령 인용
(`「중소기업창업지원법」 제29조`)이 널려 있어 `jo_bare` 로 떨어지면 **인용을 조 헤딩으로
오인**한다. 순서를 바꾸기 전 실측에서 TIPS 6건이 그렇게 깨졌다.

`outline_numbered` 는 표제와 중첩 열거를 **번호 단조성**으로 가른다. 둘 다 `N.` 표기라
정규식으로는 구분할 수 없다. 표제 번호는 장을 넘어 이어지고(제2장이 29~31), 중첩 열거는
1부터 다시 돈다. TIPS 2026 기준 후보 335개에서 표제 35개를 골라 목차와 정확히 일치했다.

`paragraph` / `jang` 으로 떨어진 문서는 `parse_quality='low'` 이고 판정 인덱스에서 뺀다.
구조가 없으면 인용 단위가 성립하지 않는다.

#### 사례 레이어에는 조 분해를 태우지 않는다

🔴 사례집·재결례집·판례는 조 체계 문서가 아니라 Q&A / 사건 단위인데, `split_articles` 를
태우면 **본문에 인용된 타 법령 조문을 그 문서의 조로 잡는다.** 실측:

| 문서 | 첫 조로 잡힌 것 | 실제 정체 |
|---|---|---|
| 사례집_권익위재결례집_1 | `제382조의3(이사의 충실의무)` | **상법** 조문 |
| 사례집_KISTEP_판례조사분석 | `제35조(연구개발과제의 성실 수행)` | **국가연구개발혁신법** 조문 |
| 사례집_한국연구재단_QA사례집 | `제73조(사전 승인 대상)` | 인용된 타 규정 |

셋 다 `quality=high` 로 나왔다. 그대로 두면 **남의 법 조문이 사례집 doc_id 를 달고 조 단위로
인덱스에 들어간다.** 인용하면 출처가 틀린 답이 된다.

사례는 `case_chunks`(B등급)로 가고 청킹 단위는 Q&A 다. Stage 0 에서는 단락 분할(`case_paragraph`)
까지만 하고 `parse_quality='low'` 로 두어 판정 인덱스에서 자동으로 빠지게 한다.

**빈 줄만으로 자르면 안 된다.** PDF 텍스트에는 빈 줄이 거의 없어서 실측(권익위재결례집)에서
**한 단락이 222,683자**가 나왔다 — 문서 하나가 통째로 단락 하나다. 빈 줄 분할을 먼저 하고,
3,000자를 넘는 덩어리는 줄 단위로 다시 묶어 자른다. 최종 청킹 단위(Q&A)는 Stage 2 가 정하므로
여기서는 "덩어리가 과하게 크지 않을 것" 까지만 보장한다. 292단위 / 최장 3,039자로 정상화됐다.

#### 조번호 중복 — 두 경우의 처리가 정반대다

| 경우 | 판별 | 처리 |
|---|---|---|
| 목차 중복 | 조제목이 **같다** | 긴 쪽 하나만 |
| 원문 오류 | 조제목이 **다르다** | 둘 다 보존, 뒤엣것에 `[2]` |

창업중심대학 2025 원문에 제35조가 둘이다(이의신청 / 권리 의무 이전). 조번호만으로 묶으면
조제목과 본문이 어긋난 레코드가 생기고 한 조가 통째로 사라진다.
`UNIQUE(doc_id, 조번호)` 제약 때문에 접미 없이는 둘을 함께 적재할 수도 없다.

#### 검증 게이트

| 코드 | 검사 | 실패 시 |
|---|---|---|
| V1 | 조번호 단조 증가 | 플래그 -> 사람 확인 |
| V2 | 조 개수 >= 5 | 파싱 실패로 간주 |
| V3 | 빈 조(본문 50자 미만) 비율 < 10% | 텍스트 추출 실패 의심 |
| — | 원문 조번호 중복 | `원문오류` 로 **별도 집계** |
| — | 폐지 조문 수 | `참고:` 로 **경고에서 제외** |

원문 중복으로 생긴 역전을 V1 에서 빼는 이유는, 섞어 세면 진짜 조판 사고를 이 소음에
묻어버리기 때문이다. V1 은 파서를 의심하는 신호로만 쓴다.

**인용을 조 헤딩으로 오인하지 않는다 — 앞뒤를 함께 본다.**

| 방향 | 신호 | 예 |
|---|---|---|
| 앞 | 규범어 접두 · 닫는 인용부호 | `지침 제65조(…)` · `「…법」제43조` |
| 뒤 | 닫는 괄호에 **띄어쓰기 없이 붙는 조사** | `제65조(제재 및 환수)에 따른다` |

뒤쪽 검사가 없으면 규범어 없는 인용을 놓친다. 통합관리지침 제14차에 이런 문형이 있다:

```
제56조(특별평가) ③ … 환수 및 제재 처분은 제65조(제재 및 환수)에 따른다.
④ 제1항에도 불구하고 …
```

이 인용을 헤딩으로 잡으면 **제56조가 ④항을 잃고**, 제65조가 제56조와 제57조 사이에 끼어
조번호가 역전된다.

조사 목록에서 `이 / 가 / 은 / 는 / 도` 는 뺀다 — `제1조(시행일)이 기준은 …`(초격차)처럼
지시어로 시작하는 진짜 헤딩이 있어 넣으면 조를 놓친다. 조 헤딩을 놓치는 것이
인용을 하나 더 잡는 것보다 나쁘다.

**폐지 조문을 빈 조로 세지 않는다.** 법령 XML 은 폐지된 조를 지우지 않고 자리만 남긴다
(`제11조 삭제 <2003.8.26>`). 실측: L1 219건의 빈 조 1,964개 중 **1,667개(85%)가 삭제
조문**이었고, V3 경고 46건이 전부 여기서 나왔다. V3 는 텍스트 추출 실패를 잡는 검사이므로
삭제 조를 세면 검사 자체가 무의미해진다.

삭제 조는 `삭제=true` 로 표시해 둔다. **Stage 2 가 인덱스에서 뺀다** — 효력 없는 조를
근거로 인용하면 오답이고, Stage 0.7 도 폐지 조를 참조 원천으로 삼지 않는다.

리포트 플래그를 두 층으로 가른다. `V*` / `원문오류` 는 사람이 봐야 하는 경고이고,
`참고:` 접두는 사실 기록이다. 섞으면 진짜 파싱 실패가 소음에 묻힌다.

#### 문자 위생

Postgres 적재 전 필수. `NUL(0x00)` 은 `DataError`, 짝 없는 서로게이트(U+D800~DFFF)는
인코딩 오류를 낸다. 실측: 협성대 지침 PDF 에 NUL, HWP 별표에 서로게이트.

### Stage 0-T — 표 추출

**진입점** `scripts/extract_tables.py` · **산출** `_tables.json`

L2 세부관리기준 뒤 부속표에 비목 카탈로그·증빙 매핑·한도가 있다.
텍스트로만 뽑으면 셀 경계가 사라져 행이 뒤섞인다. 실측: 예비창업 붙임2 를 raw text 로 읽으면
`비목 / 정의 / 증빙 / 유의사항` 4열이 한 줄로 붙어 어느 값이 어느 비목인지 복원할 수 없다.

**텍스트 추출과 분리된 경로다.** 같은 PDF 를 두 번 읽는다. 표는 좌표(괘선) 기반이고
본문은 문자 흐름 기반이라 한 번에 처리할 수 없다.

**대상은 룰 소스가 되는 문서뿐이다.** 대상을 L2 전체(38건)로 두면 이렇게 된다:

| 문서 | 표 수 | 문제 |
|---|---:|---|
| TIPS 별지서식 | **469** | 양식이라 입력칸 격자가 전부 표로 잡힌다 |
| 모집공고 | 31 | 선정 절차·일정표. 비목 규정이 아니다 |

표가 많은 것이 문제가 아니라 **룰이 아닌 것이 룰 소스에 섞이는 것**이 문제다.
`is_rule_source()` 가 세부관리기준·운영지침만 남기고 별지서식·공고·현황을 뺀다 — L2 38건 중 22건.

**추출 전략은 괘선(`lines`) 고정이다.** 괘선 없는 표까지 `text` 전략으로 긁으면
본문 단락이 표로 잡혀 오탐이 폭증한다.

**섹션 라벨은 Stage 0 의 `RE_ATTACH` 를 그대로 쓴다.** 사본을 두면 두 곳이 어긋난다 —
줄머리 앵커 없는 사본을 썼더니 본문 속 참조(`[붙임 2]에서 정하는 바에 따른다`)를 헤더로 잡아
예비창업 p2~4 의 본문 표가 붙임1/붙임2 로 잘못 태깅됐다. 실제 붙임은 p6 부터다.

**페이지를 넘는 표를 잇는다.** 부속표는 대부분 여러 쪽에 걸친다. 쪽마다 끊으면
"비목 하나 = 행 하나" 가 깨져 Stage 1 이 비목과 값을 짝지을 수 없다.
이음 조건은 다음 쪽 + 열 수 동일 + 새 섹션 헤더가 시작되지 않았을 것.

검증 기준은 표 개수가 아니라 **비목 커버리지**다. 예비창업 2025 붙임2 에서
재료비·외주용역비·기계장치·특허권·인건비·지급수수료·여비·교육훈련비·광고선전비·창업활동비
10개가 모두 잡히고, `월 50만원` · `1인 1일 최대 30만원` · `2천만원 초과` 한도가
셀 안에 그대로 남는지를 본다.

Stage 1 의 입력이므로 **Stage 1 보다 먼저 서야 한다.**

### Stage 0.5 — 적용대상 태깅

**입력** `_stage0_articles.json` · **산출** `_apply_target.json` -> `chunks.적용대상`
**값** `주관기관 | 창업기업 | 공통 | 혼재`

사용자는 창업기업이다. 이 필터가 없으면 "노트북 사도 되나요?" 에 주관기관 전담인력
규정이 섞여 나온다.

**대상은 L2 세부관리기준 + 통합관리지침이다.** 법령 219건에는 걸지 않는다.
「산업안전보건법 시행규칙」 390조에 주관기관/창업기업 구분은 존재하지 않는다.

🔴 **절(節) 헤딩은 적용대상을 선언하지 않는다.** 7개 사업의 장 제목을 전수로 뽑으면 이렇다:

```
총칙 / 추진체계 / 선정 / 협약 / 사업운영 / 사업비의 구성 및 집행 /
사업비의 관리 원칙 / 보고(점검) 및 평가 / 사업비 정산 및 제재·환수 / 행정 사항
```

**절차 구분이지 주체 구분이 아니다.** 상속시킬 헤딩이 없다.
실제로 주체를 선언하는 것은 **조 제목**이다 — `제15조(주관기관 사업비 비목)` /
`제22조(창업기업등 사업비 비목)`.

그래서 2단으로 간다.

| 단 | 방법 | 확신도 | 실측 |
|:-:|---|---|---:|
| 1 | 조제목 주어 | 0.95 | 96 |
| 1 | 본문 첫머리(200자) 주어 | 0.75 | 151 |
| 2 | LLM — 1단이 못 가른 것만 | — | **175** → ✅ 실행 완료 (로컬트랙 컷 후 **105조**) |

현행 9문서 422조 기준 결정적 단계가 **59%** 를 덮는다. 남는 175조는 주어가 둘 다
나오거나(혼재 127) 어느 쪽도 없는(미결정 48) 조다.

✅ **2단까지 완결됐다 (2026-08-31 실측).** 위 숫자는 로컬트랙 컷 **이전**(422조) 기준이다.
제3편 45조를 자른 뒤의 확정 실측은 **9문서 377조 · 결정 377 · 미결 0** — 판정경로는
조제목 91 / 본문주어 77 / 절상속 104 / **2단 LLM 105**, 값분포는 주관기관 183 / 창업기업 136 /
공통 58 이다. 2단은 대화 세션 판단이고 `verified=true`(검수자 Claude, 오너 위임).
블라인드 재태깅 검증은 1단 조제목 경로 96건에서 **96/96 일치**(무작위 기준선 52.1%) —
다만 그 96건은 조제목에 키워드가 뚜렷한 쉬운 문항이라 일치율이 그대로 옮겨가지 않는다.
근거는 `_apply_target_2단.json`.

혼재를 결정으로 세면 87% 가 나오지만 그건 부풀린 숫자다. *"창업기업은 주관기관에
제출한다"* 는 혼재가 아니라 창업기업 조이고, 그 판별은 규칙으로 안 된다.

**현행/구판 구분이 파이프라인에 없다.** `documents.status` 가 스키마에는 있는데 Stage 0 이
채우지 않는다. 구판까지 태깅하면 판정 인덱스에 안 들어갈 것을 태깅하는 낭비다
(27문서 948조 vs 현행 9문서 422조). `tag_apply_target.현행` 에 임시 상수를 뒀다 —
**`status` 를 Stage 0 이 채우면 그 상수는 지운다.**

**결정적 단계를 먼저 두는 이유는 비용이 아니라 재현성이다.** 규칙으로 갈리는 것을 LLM 에
넘기면 같은 조가 실행마다 다르게 태깅될 수 있고, 그러면 검색 결과가 흔들린다.

조 단위로 태깅하면 항으로 쪼개도 상속되므로 **청킹 단위 결정을 기다릴 필요가 없다.**
임베딩보다 먼저 서야 한다 — 청크 적재 시점에 필드가 채워져 있어야 필터가 걸린다.

🔴 **태깅 대상 밖의 기본값은 `공통` 이다. `null` 이 아니다.**

태깅은 현행 9문서 422조만 건다. 나머지 **L1·L2 264문서 23,324조**는 태깅 대상이 아니다 —
「산업안전보건법 시행규칙」 390조에 주관기관/창업기업 구분은 존재하지 않는다.
그런데 검색 필터가 `적용대상 IN ('창업기업','공통')` 이고 SQL 의 `NULL` 은 `IN` 을
통과하지 못한다. **비워 두면 23,324조가 조용히 검색에서 빠진다** — 근거 누락은 오답이고,
조용히 빠지는 것은 알아챌 방법이 없어 가장 나쁜 실패다.

| 구간 | 값 | 뜻 |
|---|---|---|
| 태깅 대상 밖 (264문서 23,324조) | **`공통`** | 주체 구분이 존재하지 않는 규범 |
| 태깅 대상 안 · 결정됨 (**377조 전량**) | `주관기관` \| `창업기업` \| `공통` | 1단 결정적 272 + 2단 LLM 105 (2026-08-31 완결) |
| ~~태깅 대상 안 · 미결~~ | ~~`null`~~ | ✅ **0건** (2026-08-31 실측). Stage 2 의 null 배제 로직은 폐기가 아니라 **회귀 방어**로 남긴다 |
| 부칙 · 붙임 | **`공통`** | 본칙이 아니라 태깅에서 빠졌다. 시행일·경과조치는 누구에게나 적용된다 |

두 종류의 `null` 을 섞으면 안 된다. Stage 2 는 반드시
`tag_apply_target.적용대상_of(doc_id, 조번호, 맵)` 을 거친다 — 이 함수가 구분을 판다.
규칙과 규모는 `_apply_target.json` 의 `기본값` 필드에 함께 실린다.

LLM 대기 175조 중 24조는 모두의창업 제53조 이후, 즉 **로컬트랙이라 범위 밖**이다.
제3편을 자르면 151조로 줄어든다.

✅ **실제로 잘랐다** — `scripts/scope.py::범위밖_조()` 를 Stage 0.5·0.7·2 가 공유한다.
적재된 「모두의 창업 프로젝트 세부관리기준」 청크 54개의 조번호 최대치는 **제52조**이고
제53조 이후는 **0건**이다 (2026-08-31 `corpus.chunks` 실측). 컷 규모는 45조.

### Stage 0.7 — 참조 그래프

**진입점** `scripts/build_refs.py` · **산출** `_refs.json` -> `corpus.refs`

**규범명 정규화가 해소율을 좌우한다.** 세 가지를 걷어내야 코퍼스와 대조가 된다:

```
약칭 괄호     「중소기업창업 지원사업 운영요령(이하 "운영요령"이라 한다)」
파일명 구분자  L1_중소기업창업_지원사업_통합관리지침_제14차개정_20251223
판본 꼬리표    (제2024-101호)(20241206)
```

안 하면 **코퍼스에 있는 규범도 dangling 으로 샌다.**

제명 별칭도 함께 먹인다. 세부관리기준의 인용 표기가 법령의 현행 제명과 다른 경우가 있고,
그러면 규범을 갖고 있어도 참조 폐포가 거기서 끊긴다.

| 인용 표기 | 코퍼스의 제명 |
|---|---|
| 근로자직업능력개발법 | 국민 평생 직업능력 개발법 (제명 개정) |
| 외부감사법 시행령 | 주식회사 등의 외부감사에 관한 법률 시행령 |
| 중소기업기술개발 지원사업 관리지침 | 중소기업기술개발 지원사업 운영요령 |
| 대·중소기업 상생협력… | 대ㆍ중소기업 상생협력… (가운뎃점 U+00B7 / U+318D) |

**남은 dangling 은 코퍼스 경계 판단의 재료다** — 현행 판정 대상이 인용했는데 코퍼스에
없는 규범이 무엇인지가 여기서 나온다 (`법령 PDF/_missing_norms.json`).

L3 문서가 *"통합관리지침 제33조부터 제42조까지에 따른다"* 로 끝나면 그 문서만으로는
답이 없다. 가리키는 곳을 따라가야 한다. 이것이 RAG 가 존재하는 이유다.

한 행이 엣지 하나다. 그래프 DB 를 쓰지 않는다 — **깊이 1**, 재귀 CTE 로 밀리초다.
🔴 `dst_조번호` 가 NULL 인 엣지는 폐포에서 확장하지 않는다.
「소득세법」처럼 조 없이 법률명만 인용한 것이라 근거로 특정할 수 없다 (`RAG.md` §4-3).

| 해소 상태 | 뜻 | 실측 (전 코퍼스 · 2026-09-01) |
|---|---|---:|
| `resolved` | 대상이 코퍼스에 있고 조번호도 맞다 | **32,043** (71.4%) |
| `shifted` | 조번호가 구판이라 **조제목으로 재매칭**했다 | **43** |
| `dangling` | 대상이 코퍼스에 없다 | **12,769** |

총 44,855엣지. 2026-09-01 에 약칭(「법」·「영」) 해소 · 경계 검사 · 계열 가드를 넣어
28,701 → 44,855 / resolved 60.6% → 71.4% 가 됐다 (`RAG.md` §4-3b).

**참조는 실제로 깨져 있다.** 조번호가 아니라 조제목으로 재매칭해야 한다.
`dangling` 은 판정 시점이 아니라 **업로드 시점에** 사용자에게 알린다.

엣지 키는 `(src_doc_id, src_조번호, 참조문자열)` 이다. 문서 단위로 조인하면
그 문서의 참조가 전부 딸려온다.

### Stage 0.8 — 우선순위 조항

**진입점** `scripts/build_precedence.py` · **산출** `_precedence_rules.json`

계층 충돌은 "아래가 엄격하면 이긴다" 가 아니다. 오히려 반대가 규칙이다.

| 사업 | L2 > L3 | L1 > L2 (미규정만 L2) |
|---|:-:|:-:|
| 예비 · 초기 · 재도전 · 도약 · 창업중심대학 | O | |
| 모두의창업 | O | O |
| 초격차 | | O |
| TIPS | | |

8개 사업 중 6개가 "주관기관 내부규정과 상충되는 경우 본 관리기준을 우선한다" 를 명시한다.
**L3 가 더 엄격해도 진다.** TIPS 는 R&D 라 주관기관이 아니라 운영사 체계여서 조항이 없다.

**문서 전체를 읽는다.** 적용범위 조가 앞에만 있다는 보장이 없다.

**범위 밖 편(篇)은 잘라낸다.** 모두의창업은 제1편 총칙 / 제2편 일반·기술트랙 /
제3편 로컬트랙 구조인데 **우리 범위는 일반·기술트랙**이다. 로컬트랙은 상위 규범이
통합관리지침이 아니라 「신사업창업사관학교 운영지침」이라 계통이 다르다.
`precedence_rules` 조회 키가 `사업명` 이라 두 트랙이 같은 키를 쓰므로, 남겨두면
일반·기술트랙 판정에 범위 밖 룰이 딸려온다.

컷은 **마지막 매치**를 쓴다 — 목차에도 `제3편 로컬트랙` 이 있어서 첫 매치를 잡으면
문서가 목차 끝에서 잘린다.

**`우선규범` 을 함께 기록한다.** `L1 > L2` 를 뭉뚱그리면 어느 L1 인지 모른다.

산출은 `verified=false` 다. 사람 검수 대상이다.

### Stage 1 — 룰 테이블 컴파일

**진입점** `scripts/seed_rules.py` · **산출** `corpus.rules` · `_비목_어휘집.json`

별표·한도표는 RAG 에 넣지 않고 룰 테이블로 뺀다. 숫자 비교는 코드가 한다.
검색으로 한도를 찾으면 "5만원" 이 든 다른 조항이 먼저 잡힐 수 있다.

현재 **72행 / 7개 사업 + L1 공통 9** (2026-09-01 실측):

```
L1 공통      9   verified=false      <- 통합관리지침 조건. 사업명 NULL
예비창업    10   verified=true       초기창업     9   true
초격차       9   true                창업중심대학 9   true
창업도약     9   true                모두의창업   8   true 7 / false 1
재도전       9   verified=false      TIPS         0   (설계 결정 선결)
```

🔴 **`layer='L1'` 행이 갈라져 나왔다.** 종전 54행은 전부 `layer='L2'` 라 L1 통합관리지침
조건이 L2 6곳에 복사돼 있었다 — L1 개정 시 6곳을 따로 고쳐야 했다. 이에 따라
**`rules.사업명` 이 nullable** 이 됐다(L1 공통행은 NULL). 조회 코드가 이걸 전제한다.

검수는 **53/72**. 절차는 `룰테이블_검수가이드.md`, 미검수 19행의 결정 요청은
`scripts/_work/_G_룰검수요청.md` §6. 남은 결손은 §8 미결 참조
(재도전=2026-09-01 에 현행판 28청크로 인덱스 교체 완료. 단 근거가 전량 `extraction='vlm'` 이라
신뢰등급 A 불가 / TIPS=비목 체계 상이).

🔴 **stale 상수가 세 곳에 흩어져 있었다** — `load_db.py::현행` · `tag_apply_target.py::현행` ·
`stage2_chunk.py::사업_of_doc`. **하나만 고치면 재적재 때 되돌아간다.** 판본을 바꿀 때는
세 곳을 함께 본다. 근본 원인은 `_stage0_articles.json` 의 2026판이 `articles=0` 이었던 것
(VLM 판독이 DB 에만 들어가고 산출물에 반영되지 않았다) → 태깅 0건 → 청크 0.

**근거 조번호는 사업마다 다르다.** 예비 제22조 = 초기 제21조 (둘 다 「창업기업등 사업비 비목」).
초기창업 제22조는 「사업비 관리」라 무관하다. 사업별 상수로 분리한다.

---

## 5. 자동화 설계

### 5-1. 재실행 단위

각 단계는 산출물 파일 하나를 완전히 다시 쓴다. 증분 갱신을 하지 않는다.
문서 300건 규모라 전체 재실행이 수 분이고, 증분 로직의 상태 관리 비용이 이득보다 크다.

| 단계 | 재실행 시간 | 트리거 |
|---|---|---|
| Stage 0 | XML 219건이 2초. PDF 포함 수 분 | 파서 수정 / 원본 추가 |
| Stage 0.7 | 약 6분 | **Stage 0 산출물 변경 시 필수** |
| Stage 0.8 | 약 3분 | 동일 |
| Stage 1 | 초 단위 | 표 추출 결과 변경 시 |

**의존 관계를 지키지 않으면 조용히 낡은 산출물이 남는다.** 실제로 `stage0_articles.py` 를
3회 수정한 뒤 `build_refs.py` 를 안 돌려 `_refs.json` 이 구 파서 기준으로 남아 있었다.

### 5-2. 텍스트 캐시

PDF 추출이 파이프라인에서 가장 느리다. `scripts/_work/_text_cache/*.txt` 에 추출 결과를
캐시하면 파서 로직만 고칠 때 재추출 없이 검증을 돌릴 수 있다.
캐시 무효화는 수동이다 — `pdftext.py` 를 고쳤으면 캐시를 지운다.

### 5-3. 검증을 먼저 통과시킨다

패치가 조용히 실패한 사례가 반복됐다.

- `timeout 100 python … | tail -3` — 파이프 뒤 `tail` 의 종료코드가 0이라 성공으로 보였고
  JSON 은 갱신되지 않았다. **파이프로 종료코드를 가리지 않는다**
- 문자열 앵커 불일치로 `AssertionError` 가 났는데 뒤 명령이 계속 돌아 같은 결과가 출력됐다

파서를 고치면 **조 헤딩이 살아남는지**를 명시적으로 검증한다. 인용 필터를 과하게 걸어
조 36개가 21개로 줄어든 전례가 있다. 조 헤딩을 놓치는 것이 인용을 하나 더 잡는 것보다 나쁘다.

```
RE_ATTACH   [참고1] 섹션 헤더     True
RE_ATTACH   본문 속 참조          False
RE_JO       줄머리 조 헤딩        True    <- 이게 False 가 되면 안 된다
_is_citation 「법」제43조          True
```

### 5-4. 실패 처리

문서 하나의 실패가 배치를 중단시키지 않는다. 예외는 잡아 리포트에 기록하고 다음으로 간다.
리포트는 네 갈래다.

```
성공        게이트 통과
게이트경고  분해는 됐으나 플래그가 붙었다 -> 사람 확인
실패        예외. 파일 없음 / 형식 미지원 / 추출 오류
제외        대상이 아니다 -> 사유를 함께 기록
```

**제외에 사유를 남기는 것이 중요하다.** 사유가 없으면 "빠뜨린 것" 과 "뺀 것" 을 구분할 수 없다.

### 5-5. 저장소 경계

Stage 0~1 산출물은 전부 JSON 파일이다. Supabase 적재는 Stage 2 에서 한 번에 한다.
전처리가 DB 를 직접 쓰지 않으므로 스키마가 바뀌어도 전처리를 다시 돌릴 필요가 없다.

`db/init/01_schema.sql` 은 로컬 `corpus` 스키마 초기화용이고,
`tenant` 스키마 DDL 은 `RAG.md` §2-3 이 정본이다.

### 5-6. 실행 환경 제약

- 모든 파이썬 호출에 `PYTHONIOENCODING=utf-8` 이 필요하다. 없으면 한글 출력이 깨진다.
  `.claude/hooks/check_bash_encoding.py` 가 누락된 호출을 차단한다
- 훅이 상대경로로 등록돼 있어 **작업 디렉토리를 하위 폴더로 옮기면 훅 4종이 전부 깨진다.**
  파이프라인 스크립트는 항상 프로젝트 루트에서 실행한다

---

## 6. 실행

```powershell
$env:PYTHONIOENCODING = "utf-8"

# Stage 0 — 조 분해 (약 12분)
python scripts/stage0_run.py

# Stage 0-T — 표 추출 (약 8분). Stage 1 의 입력이다
python scripts/extract_tables.py

# Stage 0.5 / 0.7 / 0.8 — 서로 독립, 순서 무관. 전부 Stage 0 산출물을 읽는다
python scripts/tag_apply_target.py
python scripts/build_refs.py
python scripts/build_precedence.py

# Stage 1
python scripts/build_item_vocab.py
python scripts/seed_rules.py

# 검증
python scripts/_work/verify_split.py     # 조 분해 상태
python scripts/verify_corpus.py          # 마스터 <-> 디스크 전수 대조
pytest tests/test_index_guard.py         # 코퍼스 경계 회귀
```

법령 수집(law.go.kr OPEN API)은 끝난 작업이다.
재실행 명령과 검증된 API 함정 14개는 `법령_크롤링_현황.md` §11·§12 에 있다.

---

## 7. 현재 상태

### 7-1. 단계별

| 단계 | 코드 | 산출물 | 실측 |
|---|---|---|---|
| Stage 0 | `stage0_run.py` | `_stage0_articles.json` · `_stage0_report.json` | **286문서 26,516조** · 성공 258 / 경고 28 / 실패 0 / 제외 114.<br>⚠️ 이건 **파싱 산출**이다. DB 적재분은 `corpus.documents` 283 / `corpus.doc_articles` **26,268조**(삭제조 1,730 포함, 본문 빈 조 0). 인용의 정본은 DB 다 |
| Stage 0-T | `extract_tables.py` | `_tables.json` | 22문서 표 578 -> 병합 **469** · 실패 0 |
| Stage 0.5 | `tag_apply_target.py` | `_apply_target.json` · `_apply_target_2단.json` · `_apply_target_todo.json` | ✅ **재도전 2026판 반영 (2026-09-01)** — 조 376 / 결정 371 / LLM 대기 5, 결정률 **99%**. `절_상속()` 이 명시 `장` 필드를 우선 읽는다 (VLM 판독본은 절 헤딩이 앞 조 본문 꼬리에 안 붙는다). ~~2026-08-31: 현행 9문서 377조 · 결정 377 · 미결 0~~ (조제목 91 / 본문주어 77 / 절상속 104 / 2단 LLM 105) · 대상 밖 264문서 23,324조는 기본값 `공통` |
| Stage 0.7 | `build_refs.py` | `_refs.json` | **278문서 44,855엣지** · resolved 71.4% (2026-09-01 약칭 해소분). 🔴 `load_db.py` 로 재적재하지 말 것 — `TRUNCATE corpus.documents CASCADE` 가 청크·임베딩까지 날린다 |
| Stage 0.8 | `build_precedence.py` | `_precedence_rules.json` | 8건. 산출 시 `false` -> 검수 후 `true` |
| Stage 1-a | `build_item_vocab.py` | `_비목_어휘집.json` | **31 비목** (기간 9 / 사업별 22) · enum 확정 10 |
| Stage 1 | `seed_rules.py` | `corpus.rules` | **72행** = L1 공통 9 + L2 63 / 7사업 (2026-09-01). 산출 시 `false` -> 검수 후 `true` (현재 53/72) |
| Stage 2 | `stage2_chunk.py` · `stage2_embed.py` · `stage2_bm25.py` | `corpus.chunks` · `chunk_terms` · `chunk_len` | ✅ **재작성·적재 완료 (2026-09-01 실측)** — 232문서 **20,518청크** 전량 임베딩(1024차) · BM25 `chunk_terms` **1,148,718행** · 진입점 **1,591** / 폐포전용 **18,927**. `build_index.py` 는 구 코퍼스판이라 **폐기 대상** |

`_stage0_articles.json` 은 74MB 라 저장소에 두지 않는다(`.gitignore`). 조 본문 전량 덤프라
원본 코퍼스를 JSON 으로 복제한 것이고, `stage0_run.py` 로 언제든 재생성된다.
메타데이터는 `_stage0_report.json`(108KB)에 있다.

**`stage0_ingest.py` 는 폐기 대상이다.** 2026-08-12 에 동결된 `manifest.json` 을 라우팅
원본으로 읽는다. `stage0_run.py` 가 대체한다.

### 7-2. 외부 반입 자료 — `공유받은 파일/`

세부관리기준 21개 판본(2022~2026)을 대조해 만든 비목·증빙 종합표.

| 파일 | 규모 | 용도 |
|---|---:|---|
| `비목명 종합.csv` | 56 비목 × 8사업 | **비목 어휘집 시드**(`rule_base.md` §1-b) · 룰 커버리지 체크리스트 · Stage 0-T 검증 대조본 |
| `증빙서류 종합.csv` | 124 증빙 | 증빙 매핑 · 발급처 안내 (화면 6 재료) |

**판정 인덱스에는 넣지 않는다.** 근거 조번호가 없고 실무 관행 보충 설명이 섞여 있어
A등급 인용의 원천이 될 수 없다. 2차 편집물은 원문을 대체하지 못한다.

쓰임새는 반대 방향이다. Stage 0-T 가 원문에서 표를 뽑으면 **이 표와 대조해 누락을 찾는다.**
✅ 2026-08-31 에 4개 사업이 추가돼 54행 / 6개 사업이 됐고, 2026-09-01 에 L1 공통 9 +
재도전 9 가 더해져 **72행**이 됐다 (재료는 `_tables.json` 이 아니라 `corpus.doc_articles`
본문 — 표는 셀이 열 단위로 잘려 있어 조문 재조립본이 정확했다).
남은 결손은 TIPS 뿐이고, 이 표는 그 대조에 계속 쓴다.

---

## 8. 미결

| # | 항목 | 왜 결정이 필요한가 |
|:-:|---|---|
| ~~1~~ | ~~청킹 단위 (조 vs 항)~~ | ✅ **종결** — 조 기준 + KURE 900토큰 초과 시 항 으로 확정하고 재인덱싱까지 마쳤다 (현재 **20,518청크**). `RAG.md` §3-4 |
| ~~2~~ | ~~청크 수가 1만을 넘는다~~ | ✅ **종결 — ANN 미사용 확정** (2026-08-31 `scripts/bench_ann.py` 실측). 적재 실측은 예측 22,400 이 아니라 **20,055청크**(현 20,518). 전량스캔 **134ms · recall 100%** 로 예산(top-50 < 200ms) 안이고, HNSW 는 켜지는 구간(ef≤100)에서 recall 82~91%(최악 30%), 안 켜지는 구간(ef≥200)에서는 플래너가 버린다. 근사 검색의 리콜 손실이 곧 인용 누락이라는 방침이 실측으로 확인됐다. 코퍼스가 크게 늘면 같은 스크립트로 재측정 |
| 3 | TIPS 자기인용 미수집 | TIPS 는 자신을 `이 지침 7.(사업신청·접수)` 로 인용한다. `build_refs.py` 의 참조 문형에 이 형태가 없다 |
| 4 | HWP 변환 미매칭 26건 | 대부분 구판·운영사 현황이라 현행 판정 대상이 아니다. 리포트로 확인 후 확정 |
| 5 | TIPS 우선순위 조항 없음 | R&D 라 주관기관이 아니라 운영사 체계다. 충돌 해소를 무엇으로 할지 미정 |
| ~~6~~ | ~~모두의창업 제3편(로컬트랙) 청크 제외~~ | ✅ **종결** — `scripts/scope.py::범위밖_조()` 로 한 벌만 두고 Stage 0.5·0.7·2 가 공유한다. 적재 실측(2026-08-31): 해당 문서 청크 **54개 · 조번호 최대 제52조 · 제53조 이후 0건**. 컷 45조. `doc_articles` 원문 대장에는 그대로 남아 있다 |

---

## 부록. 문서 관계

| 문서 | 담당 |
|---|---|
| **이 문서** | 전처리 공정의 순서·사양·자동화 |
| `RAG.md` | 코퍼스 경계 · 저장소 스키마 · 검색 · 참조확장 |
| `rule_base.md` | 룰 테이블 규격 · 한도 · precedence |
| `LLM.md` | 슬롯 · 프롬프트 · 출력 스키마 |
| `Agent.md` | 위 셋을 엮는 순서 · 게이트 · 실패 경로 |
| `서비스 아키텍쳐.md` | 상위 구조. **충돌 시 이 문서가 이긴다** |
| `법령_크롤링_현황.md` | L1 수집 방법 · API 함정 |
| `실제 구현.md` | 산출물이 어떻게 만들어졌는가의 이력 |
